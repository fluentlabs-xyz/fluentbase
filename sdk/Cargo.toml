[package]
name = "fluentbase-sdk"
description = "Fluentbase SDK"
version = "0.1.0"
edition = "2021"

[dependencies]
fluentbase-rwasm = { path = "../rwasm", optional = true }

[lib]
crate-type = ["cdylib", "rlib"]

[features]
default = []
runtime = ["dep:fluentbase-rwasm"]

#[profile.release]
#lto = true
#opt-level = 'z'
#strip = true
#[no_mangle]
fn system_calldatasize() {
    // let ci = ExecutionContext::contract_input_size();
    // let v = ci.to_be_bytes();
    //
    // stack_push_u256(SP_BASE_MEM_OFFSET_DEFAULT, u256_from_be_slice(&v));
}

[package]
name = "fluentbase-contracts"
version.workspace = true
description.workspace = true
authors.workspace = true
repository.workspace = true
edition.workspace = true
readme.workspace = true
license.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
fluentbase-sdk = { workspace = true }

[lib]
path = "lib.rs"

[build-dependencies]
fluentbase-build = { workspace = true }
fluentbase-sdk = { workspace = true }
alloy-genesis = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
cargo_metadata = "0.19.1"

# The following dependencies are not used directly but listed here to trigger execution of build.rs whenever they are changed
revm-precompile = { workspace = true }
fluentbase-evm = { workspace = true }
#fluentbase-svm = { workspace = true }

[features]
default = ["std"]
debug-print = ["fluentbase-sdk/debug-print"]
# Builds the staking artifact with its devnet-only view handlers. Consumed by
# `e2e` and by the devnet genesis; the production artifact leaves it off.
devnet-views = []
svm = []
wasmtime = []
std = [
    "fluentbase-sdk/std",
    "alloy-genesis/std",
    "serde/std",
    "serde_json/std",
]
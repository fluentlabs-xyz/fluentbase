# Строка 4.2, заход Б1, третий проход — находки ревью `E4-2-B1-REVIEW.md`

Ты — исполнитель (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `d2f215f1`; объект — незакоммиченное рабочее дерево захода Б1: `crates/dpos/consensus/src/{cert_inlet,cold_start_jump,dpos,executor,outer,plane_upstream}.rs`, `testbed/{fakes,preconditions,stand,tests}.rs`, `crates/node/src/dpos.rs`. **Чужое в `git status`** — `devnet/**`, `.dpos-study/history/E4-ORCHESTRATOR.md` — не трогать. Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`; `reader/` = `crates/dpos/staking-reader/src/`.

## Правила

1. **Агентов не запускать.** 2. **Git только на чтение.** 3. **Файлы на запись**: те же 11 + `cert_follow.rs` (только B1-10), `node/cert_inlet.rs` (только строка комментария `:30`, B1-09), `testbed/byzantine_roles.rs` (если нужно для B1-03), `.dpos-study/history/E4-2-B1.md` (дописать **часть В** в конец; выше не переписывать, кроме пометок «[исправлено в части В]» у Д-87, Д-89, §0(3), §0(6), §0(7)(б)). **Запрещены**: `epoch_manager.rs`, `committee/**` (читать можно), `reader/**`, `beacon/**`, `p2p/**`, `devnet/**`, `contracts/**`, `Cargo.toml`, `.claude/**`. 4. file:line на каждое утверждение. 5. Без `#[allow]`/`todo!`/таймеров; `unwrap` в тестах можно. 6. Комментарии — не свидетельство. 7. Читать целыми строками. 8. **Правила прогонов**: все правки батчем → один прогон затронутых тестов → ворота один раз в конце; «красный до правки» — только для двух юнитов (B1-01 и B1-04, ниже); мутация — не более двух (`--exact`); ×3 — переписанные стенд-тесты один раз в конце. 9. `CARGO_BUILD_JOBS=6`; `Blocking waiting for file lock` — ждать. 10. Старые тесты не ослаблять; покраснение = находка.

## Что прочитать

1. `.dpos-study/history/E4-2-B1-REVIEW.md` целиком (§0 (2), (9), (10); §1 B1-01…B1-17; §5; §6).
2. `.dpos-study/history/E4-2-B1.md` §0, §1 (Д-84…Д-91), §4, §5; `E4-2-A.md` часть В — образец формы части В.
3. `.dpos-study/history/E4-CORE-DESIGN.md` §5.2 «Триггер и лестница» (`:506-533`), §5.4 строка «Отставший узел вне реестра (чистый follower)» (`:549-572`) — проект ошибочно считает WS-inlet «лестницей» follower'а (B1-01 показывает, что inlet не переигрывает промежуточных высот); §5.4 строки «Посадка не на заверенную ветку» и «reth Invalid» (`Fault::corruption`).
4. Код: `dpos.rs:3240-3340` (follower: модуль комитета с `RethAnchor(finalized_cursor)`, геометрия локальна `:3256`; `ReJump{probe: None, tracked_epoch: None}` `:3327-3334`), `:2435-2600` (замыкания пробы/прыжка валидатора — образец), `:3540-3550`; `executor.rs:2138-2245` (`probe_frontier`; ранний выход на `probe == None` `:2142-2144`), `:1482-1497` (арм `InvalidTarget`), `:2503-2505` (комментарий промаха `pair_at`), `:372-375`/`:425-436` (`pair_at`), `:2818-2820` (`seed_boundary_below_floor`); `cold_start_jump.rs:128-139`, `:792-821`, `:909-931`, `:965`, `:1542`; `reader/epoch_transition.rs:550-585` (правило `T`: на терминале эпохи `T = epoch(fin)+1`, иначе `epoch(fin)` — читать, повторить формулу у себя); `committee/mod.rs:398-416` (`geometry()`, `epoch_of`, `anchor_hash`; `Geometry::epoch_of` `:557`); `cert_follow.rs:55-95` (`CertUpstream`); `cert_inlet.rs:2729-2762` (`UpstreamResolver::fetch_targeted`), `:1100-1106`; `plane_upstream.rs:263-281` (`FrontierMarshal for MarshalMailbox`), `:652-663`; `outer.rs:141-152`; `testbed/tests.rs:1355-1405` (пин лестницы), `:2835-2854` (шов R-008); `testbed/preconditions.rs:286`, `:412-420`; `testbed/fakes.rs` (`UpstreamCounters`, `FakeUpstream::get_finalization`).

## Решения оркестратора (исполнить ровно так; отклонение — только с причиной file:line в В§0)

| id | решение | что сделать |
|---|---|---|
| B1-01 | **FIXED** | Follower получает ту же лестницу, что валидатор: `ReJump.probe = Some(...)` над его `CertUpstream` (WS-хэндл: `get_latest` + `get_finalization(height)`), `ReJump.tracked_epoch = Some(...)` с ЛОКАЛЬНЫМ `T` — у follower'а нет `EpochTransition`, но есть модуль комитета с геометрией (`committee.geometry()`/`epoch_of`) и его ordering-финализированный курсор; `T` считать ТОЙ ЖЕ формулой, что ET (`epoch_transition.rs:550-585`): `T = epoch_of(fin) + 1`, если `fin` — последний блок эпохи, иначе `epoch_of(fin)`. Где взять `fin`: тот же курсор, что у `RethAnchor(finalized_cursor)` (`dpos.rs:3253-3256`) — не новый источник. Ступень `Finalized{last(T+1)}` уходит через `marshal.hint_finalization` → `Hybrid` → `UpstreamResolver` (WS by-height; цели там роняются законно — источник один) → `handler.deliver` → `verify_delivered` (схема в окне: `last(T+1) ≤ last(epoch(fin)+2)`) → `Update::Tip` → триггер по tip'у. Замыкание пробы — вынести общий конструктор из `dpos.rs:2440-2530` (валидатор) и переиспользовать, чтобы не было двух копий. Юнит **красный до правки**: executor в «форме follower'а» (`probe: Some`, `tracked_epoch: Some(local T)`) при замёрзшем tip'е кладёт ступень `last(T+1)` (наблюдай `hint_finalization` фейкового marshal'а); на HEAD-дереве (`probe: None`) — ступени нет. Плюс юнит на формулу `T` против правила ET (две точки: середина эпохи, терминал). Стенд follower'а не строит — записать в В§0, что живой прогон следующего класса остаётся открытой приёмкой. Д-nn: §5.4 «inlet — лестница follower'а» неверно; лестница follower'а — та же проба, ступень обслуживает WS-upstream. Комментарии `dpos.rs:3327-3334` переписать. |
| B1-02 | **FIXED (текст + ассерт)** | Д-87 переписать: «дыр не бывает» — свойство фикстуры; прыжок на ступень выше контигуозного края (ступень/`hint_finalization(frontier)` до двух эпох выше `fin`) уводит пол в `target − 2K` и всё невытянутое `try_repair_gaps` уходит под пол — `seed_boundary_below_floor` (`executor.rs:2818`) остаётся нужным; §5.2 в этом верен. Ассерт `preconditions.rs:412-420` `missing.is_empty()` — оставить как НАБЛЮДЕНИЕ с честным текстом (что в этой фикстуре цели прыжков 128/160 — контигуозные вершины) или заменить на «дыра, если есть, лежит ниже пола и терминалы вне неё есть у всех» — выбери то, что не вводит в заблуждение, и запиши. Комментарий в коде `cold_start_jump.rs`/журнале, где утверждалось «класс дыры исчез», исправить. |
| B1-03 | **FIXED** | Пин лестницы должен отделять «ступень обслужена» от контигуозного догона: в стендовом `FakeUpstream::get_finalization` (fakes) записывать высоты by-height пуллов и их исходы (`served_heights: Vec<(u64, bool)>` или аналог в `UpstreamCounters`), и в тесте лестницы утверждать: каждая названная ступень `last(T+1)` была ЗАПРОШЕНА by-height и ДОСТАВЛЕНА (нецелевой `get_finalization(last(T+1))` на плоскости уходит и без A2-05), и tip достиг её после доставки. Если стенд показывает, что ступень НЕ запрашивается (кто-то дедуплицирует, или marshal не ставит `HintFinalized` выше `max_repair`) — это находка: записать с числами, не подгонять. `LADDER_REACH_TICKS` при этом либо уходит, либо остаётся с честной докой «граница, не механизм». |
| B1-04 | **FIXED** | Арм `InvalidTarget` в executor'е (`:1482-1497`): при цели из СОБСТВЕННОГО архива (steady-state — единственный вызывающий этого арма) исход — `Fault::corruption` (§5.4: EL сел не на заверенную ветку / reth отверг заверенную ветку), не `rotate`. Текст `warn!`/ошибки — по факту (две причины: reth `Invalid` на sync, `holds(result) == false`). Cold-start путь (`classify_jump_outcome`) не трогать — там цель пока внешняя (Б2). Юнит **красный до правки**: `InvalidTarget` из спавненного прыжка ⇒ `Fault`, `rotate` не вызван. Проверь, что ни один старый тест не пинует `rotate` на `InvalidTarget` в steady-state (если пинует — переписать с записью). |
| B1-05 | **RECORDED** | Одним абзацем в В§0 + Д-nn: холодный старт получил сверку посадки; вход «EL исполнен выше подделанной цели» теперь `InvalidTarget ⇒ Done(Ok(None))` вместо `AuthFailed ⇒ RotateAuth` — для Б2. |
| B1-06 | **FIXED (журнал)** | Д-89 и §0(3): механизм — `PrioritySet` по дедлайну, неотвечаемый ключ ≈ одна отправка на `timeout + retry` (5.5 s); измерение не оспаривается. Пометка «[исправлено в части В]». |
| B1-07 | **FIXED (журнал/док)** | Нагрузка ≈0.18 запр./с на узел у живого кончика; поправить числа в журнале §0(3)/§5 и в доке `executor.rs`, если там «1/с». |
| B1-08 | **FIXED** | Комментарий промаха `pair_at` (`executor.rs:2503-2505`): архивы immutable; реальный промах — `last_tip_height` засеян `cfg.last_consensus_finalized_height` при пустом архиве после пред-движкового прыжка. |
| B1-09 | **FIXED** | Две строки комментариев про `live_height` (`node/dpos.rs:939`, `node/cert_inlet.rs:30`). |
| B1-10 | **FIXED** | A2-05: `CertUpstream` получает `get_finalization_targeted(height, targets: NonEmptyVec<PeerPubkey>)` с дефолтом → `get_finalization(height)`; `PlaneUpstreamHandle` реализует через `fetch_targeted` фронтир-резолвера; `UpstreamResolver::fetch_targeted` (`cert_inlet.rs:2729-2762`) передаёт цели, а не роняет; `debug!`/комментарии Д-77 в `cert_inlet.rs`/`outer.rs` переписать. Прогнать ролевые и лестницу; если ступень с целями НЕ уходит (`committee[T+1]` вне `participants` фронтир-резолвера) или тесты краснеют — записать механизм с file:line и числом, откатить только эту правку и оставить RECORDED с точной причиной. Ассерт `requests_created{Dropped} = 0` — если метрика семейства доступна через `Outcome` (подбери по имени с меткой), поставить в тесте лестницы. |
| B1-11 | **RECORDED + комментарий** | Шов R-008 сужает условие; поправить комментарий `tests.rs:2835`; Д-nn одной строкой. |
| B1-12 | **RECORDED** | Одной строкой (Б2: холодный старт и порог 1024 при `interval < 512`). |
| B1-13 | **FIXED** | Переименовать тест `preconditions.rs:286` под содержание (например `a_node_more_than_two_epochs_behind_freezes_its_marshal_at_the_ceiling_and_climbs_it_by_jumps_to_its_own_tip`); `PLAN.md`/`E4-PRECONDITIONS.md` правит оркестратор — назови в В§0 новое имя и одно предложение для Д3. |
| B1-14 | **FIXED** | `cert_inlet.rs:1100-1106`: либо положительный контроль (проверенный серт ⇒ `dkg_rx` непуст) в том же тесте, либо убрать бессодержательный ассерт. |
| B1-15 | **FIXED** | Один `pair_at`: `BlockFetcher::pair_at` для `MarshalMailbox` делегирует `FrontierMarshal::pair_at` (или наоборот) — одно тело. |
| B1-16 | **FIXED** | Ремарка `dpos.rs:2437-2438`. |
| B1-17 | **FIXED** | Строка паники `cold_start_jump.rs:1542`. |

## Ворота (сам, verbatim по одной строке, ОДИН раз в конце)

`cargo test -p fluentbase-consensus --lib` (685 + новые); `… --features dpos-devnet-byzantine testbed::` (44/0); `cargo test -p fluentbase-consensus --lib testbed::` (36/0); `cargo test -p fluentbase-node --lib` (57/0); `cargo test -p fluentbase-staking-reader` (63/0); `cargo test -p fluentbase-consensus --test slasher_integration` (16/0); `cargo clippy -p fluentbase-consensus -p fluentbase-node --all-targets` (одна чужая `large_enum_variant`) и с фичей (0); `cargo fmt -p fluentbase-consensus -p fluentbase-node -- --check` (0); `cargo doc -p fluentbase-consensus --no-deps 2>&1 | grep -c "unresolved link"` (7). Переписанные стенд-тесты (лестница, `preconditions.rs` переименованный, R-004, R-008) — ×3.

## Журнал — часть В в конце `.dpos-study/history/E4-2-B1.md`

`# Часть В — заход Б1, третий проход: находки ревью E4-2-B1-REVIEW.md`. В§0: (1) `FIXED n · RECORDED m · REJECTED k`; (2) ворота verbatim + ×3; (3) тесты — имя / что утверждает / красный до правки verbatim или мутация; (4) новые Д-nn (после Д-91) и переписанные Д-87/Д-89; (5) B1-01 — что именно даёт follower'у лестницу (file:line проводки), откуда `T`, что остаётся открытой приёмкой; (6) B1-10 — ушла ли целевая ступень на провод (чем наблюдал); (7) что в §5.2/§5.4 остаётся неверным по коду; (8) новое имя теста предусловия + предложение для Д3. В§1 таблица `id | исход | что сделано | file:line`. В§2 раскладка коммитов (группы; какие не собираются по отдельности — по подписям). В§3 слабее всего / что уходит в Б2 и В.

В финальном ответе — только В§0.

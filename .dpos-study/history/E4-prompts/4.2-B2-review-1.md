# Ревью строки 4.2, заход Б2 (холодный старт по «Правилу единому»; follower `sync_to` только с проверенным входом; отброс `Latest` вне окна; стадии `verify_jump_*` и `JumpOutcome::{BadTarget, AuthFailed}` сняты)

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база — `HEAD` = `f8ec4939` (`git rev-parse HEAD`). Объект — **незакоммиченное рабочее дерево** захода Б2: файлы из `git status --short | grep -v devnet/` под `crates/` (список — в §0(0) журнала исполнителя; сверь с `git status`). **Всё остальное в `git status` — чужое** (`devnet/**`, `.dpos-study/history/E4-ORCHESTRATOR.md`) — не трогать. Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`.

## Правила

1. **Агентов не запускать.** 2. **Git только на чтение.** Единственный файл на запись — `.dpos-study/history/E4-2-B2-REVIEW.md`; мутации продакшн-кода ради чувствительности тестов — только с откатом и `md5sum` до/после. 3. Каждое утверждение — `file:line` дерева и, где есть «до», `HEAD:file:line`. 4. Комментарии, доки, `.claude/dpos_architecture/`, журнал исполнителя — не свидетельство; свидетельство — код и прогон, который ты сделал сам. 5. **Полнота, не фильтр**: все находки, включая мелкие и неуверенные, с серьёзностью (BLOCKER / SERIOUS / MODERATE / MINOR / NIT) и «чем пытался опровергнуть»; не объединять разные находки ради краткости — фильтрует оркестратор. 6. Читать целыми строками. 7. `CARGO_BUILD_JOBS=6`; `Blocking waiting for file lock` — ждать. 8. `.claude/COMMONWARE_INTERNALS.md` и `.claude/RETH_INTERNALS.md` — ДО checkout'ов (reth: `CanonicalInMemoryState::get_finalized_num_hash`, что в нём после graceful restart / devp2p-синка без FCU / снапшота; engine `forkchoiceUpdated` с head=hash без известного блока; commonware: `Fetcher` — `Payload::Error` ⇒ `add_retry`, `excluded`; marshal `handle_deliver` структурная проверка).

## Вход

- Спека: `.dpos-study/PLAN.md:103`; `.dpos-study/history/E4-CORE-DESIGN.md` §5.2 `:521-533` («Порядок «аутентификация до `sync_to`»», «Правило единое», «Д-2 — что сейчас и что для прода», диаграмма), §5.4 `:565-568`, §5.5 `:576`; `.dpos-study/DECISIONS.md` Д-2 (`:18`), П-4 (`:87`).
- Постановка: `.dpos-study/history/E4-prompts/4.2-B2-impl-1.md` (в ней — якоря HEAD, проверенные оркестратором, и решения оркестратора (а)–(е) в файле состояния). Журнал: `.dpos-study/history/E4-2-B2.md` — читать ПОСЛЕ диффа. Контекст Б1: `history/E4-2-B1.md` часть В §0(4) (Д-92…Д-98), `E4-2-B1-REVIEW.md` §6.
- Дифф: `git diff HEAD --stat -- crates/` (14 файлов, +1301/−1931), затем по файлам. Якоря дерева (проверены оркестратором): `dpos.rs` — `ColdStartKind::ElFinalized` `:1287`, `resolve_cold_start_kind` `:1289-1331` (арм `:1310-1329`), `FreshFollowerEntry`/`fresh_follower_entry` `:1339-1369`, арм `ElFinalized` в `launch` `:1739-1766`, арм #12 `:1810-1840`, follower-армы `:2876-2931` (`sync_to_checkpoint` `:2904`, devnet `get_latest ⇒ sync_to` `:2913-2927`), пороги `:2343`, `:3081`; `executor.rs` — посев `:1265`/`:1290`, heartbeat `:1726`, гейт `maybe_re_jump` `:2511-2520`, снятие `crash_recover` на drain `:1417-1436`; `cold_start_jump.rs` — `sync_to_checkpoint` `:626-655`, `jump_to_target` `:782-789` (без `CommitteeSource`/RNG), `sync_to` `:802`, L1 `:855-870` (`Err ⇒ Stalled` `:870`), `verify_jump_authenticated` `:722-727` (без `l1_checkpoint`), `assert_l1_checkpoint` `:903-925`; `plane_upstream.rs` — отброс `:479-483`, `FRONTIER_DROPPED` `:121`, доки `:51-84`, `produce` без ответа `:518-529`; `node/dpos.rs:2365` `is_deployed_network`, `node/cert_follow/mod.rs:238`; `testbed/fakes.rs:1360-1372` (`sync_to_checkpoint` паникует), `stand.rs:2237`; журнал §1 Д-99…Д-107, §3 семь групп, §4 семь слабых мест (особенно 1 — унаследованные правки без заметок автора, 3 — #12 не прогонялся, 6 — якорь `rf_num` vs курсор marshal у follower с пустым архивом).

## Форма результата `.dpos-study/history/E4-2-B2-REVIEW.md` (§0–§6 как `E4-2-B1-REVIEW.md`)

**§0 Прямые ответы** (нумерованные, каждый с [KNOWN]/[ГИПОТЕЗА]):

1. **Ворота** — полный набор УЖЕ прогнан оркестратором на этом дереве (решение пользователя 2026-09-12: один прогон набора на заход, не три): выводы в `/tmp/claude-1000/-home-djadjka-Work-fluentbase/497904b9-e3ec-49b9-adb4-1fca928058a1/scratchpad/gates/b2f-*.txt` (`b2f-status.txt` — коды выхода; `b2f-tree.md5` — снимок дерева, сверь `md5sum -c`). Прочитай их и приведи `test result` verbatim; НЕ перегоняй набор. Гоняй сам ТОЛЬКО точечные тесты (`--exact`), мутации и то, что нужно для конкретной находки. Числа журнала (lib 682/0) сравни с `b2f-lib.txt`.
2. **Якорь холодного старта валидатора** (центральный вопрос). Для каждого случая — graceful restart с populated-архивом; пустой архив + EL за эпохой 0 (архив потерян / EL со снапшота / devp2p-синк без FCU); crash-survivor #12 — назови по коду дерева `(anchor_height, anchor_hash)`, кто и когда писал этот тег в reth (`RETH_INTERNALS.md` → `CanonicalInMemoryState::finalized_block`, `with_latest` при рестарте; наш `sync_to`/FCU-писатели на HEAD), и является ли якорь «проверенным» в смысле §5.2 (модуль читает `committee[epoch(anchor)+2]` на этом хэше). Отдельно: **тег `finalized` после devp2p-синка без единого FCU** — это `None` ⇒ генезис-fallback (`derive_cold_start_heights`)? Тогда что делает арм «пустой архив + EL за эпохой 0» — якорится на генезис (codeless ChainConfig — крах, `HEAD:dpos.rs:1847-1852`)? Если такой путь есть — BLOCKER с file:line и сценарием; если исполнитель его закрыл — чем. Сравни с HEAD: там пустой архив требовал посадки прыжка (retry forever); в дереве узел стартует на якоре и идёт лестницей — регрессия ли это для узла, у которого EL-тег стоит на эпохе, чьи комитеты уже полностью ротированы (§5.4 «ступень никем не обслужена»)? Скажи прямо: какой путь у такого узла остаётся по коду дерева.
3. **#12 (crash-survivor `DeferToElSync`)**: на HEAD закрытие EL-разрыва — пред-движковый прыжок (devp2p фаст-форвард). Исполнитель утверждает (журнал Д-99): steady-state прыжок здесь структурно не может сработать (`last_tip_height` и `ordering_finalized` засеяны одним значением `:1265`/`:1290`, гейт `:2511-2520` запрещает спавн при непустом drain), разрыв закрывает стартовый backfill-drain executor'а (`:1381-1443`) поблочно derive+import из собственного архива, гейдж `crash_recover` снимается на последней высоте drain (`:1417-1436`). Проверь каждое звено по коду: верно ли, что drain действительно доходит до архивного курсора при ГЛУБОКОМ разрыве (что на HEAD было capped `MAX_COLD_RECOVER` — где cap теперь?), что `ordering_finalized`, засеянный курсором marshal при reth ниже него, не ломает derive (это поведение HEAD или новое?), цена по времени против HEAD (назови, не оценивай), и есть ли состояние взаимного ожидания (drain ждёт блок, которого в архиве нет — арм `None` `:1437+` — fatal? а прыжок запрещён). file:line на каждое.
4. **Follower**: три арма после правки. (а) datadir-арм без `sync_to`: якорь `rf_hash` — что, если `rf` далеко позади upstream'а (лестница; регрессия по времени против HEAD — назови, не оценивай); (б) fresh-арм: форма checkpoint (hash / `(height, hash)`), как EL синкается к hash без известной высоты (reth FCU head=unknown hash ⇒ SYNCING, devp2p ищет по хэшу? — `RETH_INTERNALS.md`), `assert_l1_checkpoint` со строками verbatim остался; отказ без checkpoint на deployed-сети — предикат тот же, что у `load_bls_keypair` (file:line), текст; devnet-арм с `get_latest ⇒ sync_to` — единственный ли оставшийся непроверенный `sync_to` в крейте (`grep -n 'sync_to('`), и стоит ли за предикатом; (в) второй прыжок follower'а удалён — чем follower закрывает глубокий разрыв (лестница Б1 — назови `probe`/`tracked_epoch` проводку `dpos.rs`).
5. **A2-04**: `deliver` при `OutOfWindow`/`NotReadable` — отброс + `true`, оба арма, метрика; юниты инвертированы; `grep -n get_latest` по крейту и `node/` — остались только проба и devnet-арм? Стендовые результаты по Д-72 (by-height теряет retry-драйвер): при отброшенном by-height ответе — кто повторит запрос (CW `Fetcher` считает ответ доставленным? `deliver=true` закрывает fetch), и достижимо ли, что marshal просит высоту вне окна (потолок `last(epoch(fin)+2)` — `fin` чей); если достижимо — SERIOUS с file:line.
6. **Стадии/варианты**: `jump_to_target` без `verify_jump_structural`/`_authenticated` — подтверди, что пара из `pair_at` действительно прошла и структурную проверку (CW `handle_deliver`, `core/actor.rs` — по докам, строка), и BLS (`verify_delivered` под `EpochSchemeProvider`); `BadTarget`/`AuthFailed` = 0 идентификаторов (`grep`); L1 `Err ⇒ Stalled`; арм «нечитаемый комитет + L1 ⇒ Ok» в `verify_jump_authenticated` — снят или достижим (оба оставшихся потребителя передают `None`?); `refetch_verified_archive_hole`/`fetch_verified_boundary` не ослаблены; `JUMP_THRESHOLD` — читатели.
7. **Стенд**: (а) роль «пир без данных» — что она реально показывает: различает ли стенд «нет данных» от лжи на проводе (`FakeUpstream`/`Producer` — `None` = что в CW?), не вакуумен ли пин `deliveries_rejected == 0`; мутация: заставь `deliver` возвращать `false` на `None` — краснеет ли; (б) пин отброса вне окна — какой узел, какой счётчик, не ослаблены ли ассерты лестницы/предусловий; (в) переписанные ассерты: «что доказывал / что доказывает» — верно ли для каждого.
8. **Юниты**: (а) якорь пустого архива — красный до правки verbatim (журнал) — воспроизведи мутацией ≤ 2; (б) `jump_to_target` со сломанным multisig ⇒ `Landed` — не вводит ли в заблуждение (документировано ли, что защита — в `deliver`/`verify_delivered`, со ссылкой на юнит)? (в) отказ fresh-datadir — юнит есть или почему нет.
9. **Граница/гигиена**: `pub` изменения, `#[allow]`, `todo!`, таймеры, `unwrap` на продакшн-пути; правки вне списка постановки; `node/cert_follow/mod.rs` и `node/dpos.rs` — только проводка?
10. **Где журнал вводит в заблуждение**; §5.2/§5.4 расхождения журнала §0(7) — подтверди/опровергни каждое; §0(9) риск — согласен ли с формулировкой и с «путём, который остаётся»; hard-stop оркестратора: (2) — требуется ли менять Д-2/П-4; (3) — есть ли BLOCKER, на который проект не отвечает; (4) — опроверг ли стенд проект на центральном пути — да/нет со свидетельством.

**§1 Находки** — `id | серьёзность | file:lines | HEAD-якорь | что не так | чем пытался опровергнуть | уверенность`; id `B2-nn`. BLOCKER — регрессия на пути холодного старта/прыжка/доставки против HEAD, не предписанная проектом, или доказанная неверность проекта на центральном пути. **§2 Поведение по ханкам.** **§3 Граница.** **§4 Ворота** verbatim. **§5 Оставить как есть** — непустой и конкретный. **§6 Вне рамок** — для В, 4.3, Э7.

В финальном ответе — только §0 (кратко) и таблица §1.

# Строка 4.1, заход Б2 — одна карта «запись + схема»: `EpochSchemeProvider` как вид на модуль, один продюсер, `CertInlet.schemes` и `scheme_at_finalized_tip` удаляются, один `EpochTransition`, `latest_live` → запись модуля

Ты — исполнитель (Opus 5) одной строки плана. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`; заходы А и Б1 закоммичены (`git log -12`). Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`; `reader/` = `crates/dpos/staking-reader/src/`.

## Правила (обязательные)

1. **Агентов не запускать.** 2. **Git только на чтение.** 3. **Файлы на запись — ТОЛЬКО**: `committee/**`; `outer.rs`; `epoch_manager.rs`; `engine.rs`; `cert_inlet.rs`; `node/cert_inlet.rs`; `node/dpos.rs`; `dpos.rs` (consensus); `lib.rs` (реэкспорты); `reader/epoch_transition.rs` (только удаление `soft_enter_span` и его тестов); `testbed/stand.rs`, `testbed/fakes.rs`, `testbed/tests.rs`, `testbed/byzantine_roles.rs` — ТОЛЬКО правки, нужные для компиляции стенда под новые сигнатуры (ассерты старых тестов не менять); `crates/dpos/consensus/tests/*.rs` — то же; `.dpos-study/history/E4-1-B2.md`. Запрещено: `beacon/**` целиком (сигнатуры `Beacon::signer(epoch, &ValidatorSetSnapshot, …)`, `oracle_for` — как есть; если модулю нужен снимок — строй его из записи), `executor.rs` (кроме нуля правок), `slasher/**`, `application.rs`, `devnet/**`, `Cargo.toml`, `contracts/**`. 4. Каждое утверждение — file:line. 5. Без `#[allow]`/`todo!`/таймеров/`unwrap` на продакшн-пути без строки Д-nn. 6. Комментарии — не свидетельство. 7. Читать целыми строками. 8. Стенд 40/0 без правки ассертов старых тестов; старый тест покраснел ⇒ находка в §7, разбор причины, не правка теста.

## Что прочитать ДО кода

1. `.dpos-study/PLAN.md:54`; `.dpos-study/history/E4-CORE-DESIGN.md` §5.1 (`443-505`): абзацы «Кэш и ретенция», «Кто читает через модуль после 4.1 (и что удаляется)», «Один `EpochTransition`»; §5.4; §5.5; §7 «4.1 наполовину» (нельзя оставлять `CertInlet.schemes` рядом с модулем; ни одного продюсера `EpochSchemeProvider` мимо модуля).
2. Журналы `history/E4-1-A.md` (части А и В), `history/E4-1-B1.md` (§7 — что Б1 оставил Б2), ревью `E4-1-A-REVIEW.md`, `E4-1-B1-REVIEW.md` (если есть).
3. Модуль `committee/**` целиком.
4. Код: `outer.rs:195-260`, `:238-420` (`EpochSchemeProvider`, `register`, `verifier_epochs`, `latest_scheme`, `CertProvider`), `:540-560`, `:1150-1200` (`register_scheme`, `soft_enter_span`), `:1315-1330` (`cold_start_register`); `epoch_manager.rs:460-520`, `:735-800`, `:960-1020`, `:1020-1110`, `:1110-1300` (`reconcile_roles` → `SignerVerdict` → `spawn_engine`), `:1320-1345` (`soft_enter`), `:1400-1430` (`reconcile_live`), `:1480-1560` (`spawn_engine`), `:1618-1640` (`register_soft_entered`), `:1829-1900` (`pipeline_catchup_span`), тесты `epoch_manager` по `soft_enter_span`/`register_scheme`; `engine.rs:190-300` (`register_scheme` `:225`, `WeightedVrf::try_new(&cfg.snapshot)` `:288`); `cert_inlet.rs:225-265`, `:365-400`, `:680-720`, `:820-880` (`schemes`, `scheme_at_finalized_tip`, `CachedScheme`), `:1008` (тестовый комитет); `node/cert_inlet.rs` целиком; `node/dpos.rs` — `inlet_setup` (`:733-770`), `build_beacon_plane` ET (`:1530-1545`), bridge/forwarder; `dpos.rs:1990-2060` (signer-ET `EpochTransition::new` + `cold_start`), `:2715-2730` (`cold_start_register`), `:3225-3256` (`soft_enter_committees`), `:3655-3690` (follower `cold_start_register`), `:3809-3890` (inlet-источник follower'а); `reader/epoch_transition.rs:129-231`, `:615-760`; `weighted_vrf.rs:80-120`.

## Что сделать

### Б2.1. Одна карта: схема живёт в записи модуля

- `EpochEntry { record, scheme: Mutex<Option<Arc<BlsScheme>>> }` (или эквивалент) в `committee/store.rs`; трейт `Committee` получает `fn scheme(&self, epoch) -> Option<Arc<BlsScheme>>` и `fn upgrade_scheme(&self, epoch, scheme: BlsScheme) -> bool` (правила ТОЛЬКО как у сегодняшнего `register` `outer.rs:332-376`: vacant ⇒ insert; те же participants ∧ verifier→signer ⇒ overwrite; signer→verifier, потеря оракула ⇒ refuse + `error!` + метрика `ORACLE_DROP_REFUSED`; «другой комитет» — невозможен структурно: `scheme.participants()` обязан равняться `record.bls`, иначе refuse + `error!`).
- **Один продюсер verify-схем**: конструктор `CommitteeStore` получает `verifier: Arc<dyn Fn(&CommitteeRecord) -> Option<BlsScheme> + Send + Sync>` (строится в `outer.rs`/`dpos.rs` из `chain_id` + `randomness.oracle_for(epoch)` — ровно то, что сегодня делает `soft_enter_verifier` + `register_for_span`), и `install` кладёт запись ВМЕСТЕ с её verify-схемой атомарно. Signer-схема — единственный upgrade: `epoch_manager` перед `spawn_engine` зовёт `upgrade_scheme(epoch, signer_scheme)`; `engine.rs:225` `(cfg.register_scheme)` — удалить (движок получает готовую схему; `WeightedVrf::try_new` — из записи модуля (`weights` обязательны) ДО спавна, E4-10).
- `EpochSchemeProvider` (`outer.rs:257-420`) — становится тонким видом на `Arc<dyn Committee>`: `scoped(epoch)` = `scheme(epoch)`, `verifier_epochs()` = эпохи карты с verify-схемой, `latest_scheme()` = схема наивысшей эпохи карты; собственная `BTreeMap` удаляется; `register` удаляется (или остаётся как `upgrade_scheme` прокси — записать). Ретенция — только модуля (по полу окна). Тесты `outer.rs:1916-2000` (`register_refuses_…`) переписать на `upgrade_scheme` без ослабления утверждений.
- Удалить продюсеров: `cold_start_register` (`outer.rs:1322`, вызовы `dpos.rs:2730`, `:3686`) — при старте достаточно `store.committee(initial_epoch)` (запись + verify-схема появляются сами; громкий отказ старта уже через модуль после Б1); `register_soft_entered` (`epoch_manager.rs:1618`) — `soft_enter` = `store.committee(epoch)` + `roles.insert(Verifier)`; `soft_enter_span`/`SoftEnterCommittees` (`outer.rs:199`, `:557`, `:1169-1199`; `epoch_manager::Config::soft_enter_span` `:594`; ET `soft_enter_span` `reader/epoch_transition.rs:705-738`; follower `soft_enter_committees` `dpos.rs:3225-3256`) — `pipeline_catchup_span` читает `store.committee(e)` для `e ∈ [from, to]` до первого `Err` (в окне модуля; `OutOfWindow` сверху = стоп), hint marshal'у как сейчас; `register_scheme` closure (`outer.rs:1154`) удалить.

### Б2.2. `CertInlet` без второй таблицы

- `CertInlet.schemes` (`cert_inlet.rs:397`, `:499`, `:693`, `:832`, `:874`), `CachedScheme` (`:369`), `CommitteeSource::scheme_at_finalized_tip` (`:245-265`) и его реализации (`RethCommitteeSource`, `JumpCommittees` в `fakes.rs`) — удалить. Inlet получает `Arc<dyn Committee>` и берёт `scheme(epoch)`: `None` ⇒ тот же Defer-путь, что сегодняшний «не закоммичено» (skip + счётчик, без `Err`), `Some` ⇒ verify; verify-FAIL ⇒ данные-фолт как сейчас (evict больше нечего — схема из записи не бывает stale по построению; записать). `CommitteeSource::scheme_at(epoch, landing_hash, oracle)` — ОСТАЁТСЯ до 4.2 (прыжок).
- `node/cert_inlet.rs::committee_source` + `finalized_hash`-closure (`node/dpos.rs:748-770`) и follower inlet-источник (`dpos.rs:3809-3887`, `committee_read_deferred`-проба) — удалить: inlet'у нужен только `Arc<dyn Committee>` (+ `CommitteeSource` для прыжка, если он его держит — проверь).

### Б2.3. Один `EpochTransition`

- Signer-плоскостной ET (`dpos.rs:2040-2053`, `cold_start` `:2055-2060`, его `on_finalized`-хук и `PENDING_RETRY_BACKOFF`-цикл — найди все места) — удалить; beacon-плоскостной ET (`node/dpos.rs:1530-1537`) получает `Some(bridge_tx)`, и bridge/forwarder переезжает туда, где ET строится (порядок инициализации §5.1 «Один EpochTransition»: ET до движка, движок получает тот же `Arc` и `bridge_rx`). Стенд: `stand.rs` строит ET один раз на узел — проверь, что `tracked_mismatches` (`stand.rs:794-797`) больше нечего считать, и запиши, что с ним стало.
- Канал границ: `(epoch, ValidatorSetSnapshot)` → `epoch` (снимок для `reconcile_roles`/`signer`/`WeightedVrf` берётся из записи модуля: добавь `CommitteeRecord::snapshot_view() -> ValidatorSetSnapshot` с `weights: Some`, `tombstoned: false` — записать как Д-nn: beacon API берёт снимок, beacon не трогаем). `latest_live: Option<(Epoch, Snapshot)>` → `latest_live: Option<Epoch>`; `reconcile_live` читает `store.committee(epoch)`.
- Cold start валидатора: `initial_snapshot` (`dpos.rs:1997-2017`) → запись модуля (после Б1 уже так; убедись, что второго `epoch_committee_snapshot` там нет).

### Б2.4. Ворота (сам, verbatim по одной строке)

`cargo test -p fluentbase-consensus --lib`; `… --features dpos-devnet-byzantine testbed::` (40/0); `cargo test -p fluentbase-node --lib`; `cargo test -p fluentbase-staking-reader` (минус тесты `soft_enter_span`); `cargo test -p fluentbase-consensus --tests` (integration); `cargo clippy -p fluentbase-consensus -p fluentbase-node --all-targets` и с фичей; `cargo fmt --check`; `cargo doc -p fluentbase-consensus --no-deps 2>&1 | grep -c "unresolved link"` (9).

### Б2.5. Журнал `.dpos-study/history/E4-1-B2.md`

Форма — как `E4-1-B1.md`. §0 обязан ответить: (1) сколько карт «эпоха → схема» осталось в процессе (ожидание: одна — модуля); (2) продюсеры схем поимённо после захода (ожидание: `install` + `upgrade_scheme`); (3) сколько `EpochTransition` строит валидатор, follower, стенд; (4) оставшиеся сайты `epoch_committee_snapshot(` вне `committee/` и ET (ожидание: tombstone-поллер, `scheme_at(landing)` прыжка — до 4.2, ET-собственные); (5) `#[test]` HEAD vs дерево по файлам, удалённые поимённо; (6) ворота; (7) что из §5.1 неверно по коду; (8) запрещённое на продакшн-пути. §3 Д-nn продолжает нумерацию (после Б1). §6 логические коммиты с проверенной компилируемостью. §7 — что осталось заходу Б3 (стенд: `FakeStaking` по ветке хэша, кольцо, тумбстоуны, три стенд-теста, e2e-пин) и 4.2.

Заканчивай только когда всё сделано и ворота зелёные; невозможное — сделать остальное, назвать в §7.

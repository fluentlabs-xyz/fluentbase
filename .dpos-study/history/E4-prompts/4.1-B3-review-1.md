# Ревью строки 4.1, заход Б3 (стенд: `FakeStaking` по ветке хэша, кольцо весов, тумбстоуны; три стенд-теста; e2e-пин `commit_height`)

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база — `HEAD` (`git rev-parse HEAD`; последний код-коммит захода Б2′ — `479b60b3`). Объект — **незакоммиченное рабочее дерево** захода Б3: `crates/dpos/consensus/src/testbed/{fakes,stand,mod}.rs`, новый `crates/dpos/consensus/src/testbed/committee_tests.rs`, новый `e2e/src/staking_commit.rs`, `e2e/src/lib.rs`. **Всё остальное в `git status` — чужое** (`contracts/staking/**`, `crates/staking-abi/src/lib.rs`, `devnet/**`, `target-contract/`, `.dpos-study/history/E4-ORCHESTRATOR.md`) — не трогать, не читать как часть захода. Пути без префикса — от `crates/dpos/consensus/src/`.

## Правила

1. **Агентов не запускать.** 2. **Git только на чтение.** Единственный файл на запись — `.dpos-study/history/E4-1-B3-REVIEW.md`. Мутации продакшн-кода ради проверки чувствительности тестов разрешены ТОЛЬКО с откатом и `md5sum` до/после (как делал исполнитель); в дереве после тебя не должно остаться ни одного изменённого байта, кроме файла ревью. 3. Каждое утверждение — `file:line` дерева. 4. Комментарии, доки, журнал — не свидетельство; свидетельство — код и прогон, который ты сделал сам. 5. **Полнота, не фильтр**: BLOCKER / SERIOUS / MODERATE / MINOR / NIT + «чем пытался опровергнуть». 6. Читать целыми строками. 7. `CARGO_BUILD_JOBS=6`; `Blocking waiting for file lock` — ждать (оркестратор гоняет ворота параллельно). 8. Постановка исполнителя: `.dpos-study/history/E4-prompts/4.1-B3-impl-1.md`. Журнал: `.dpos-study/history/E4-1-B3.md` — читать ПОСЛЕ диффа.

## Вход

- Спека: `.dpos-study/PLAN.md:100` (строка 4.1, «Стенд: …» и «проверка»); `.dpos-study/history/E4-CORE-DESIGN.md` §7 «4.1 … Проверка», §5.6, §6 «Следствия 5.0» (e2e-пин `commit_height(E) = start(E−2)`, для E ≤ 2 — блок 1).
- Код: `committee/{mod,store}.rs` (`Geometry::commit_height` `mod.rs:579-585`; `store.rs:437-475` окно/`NotReadable`/`Ok(None)`; `is_transient`); `testbed/fakes.rs` (дифф целиком: `BranchCommittees`, `Branch`, `branch_of` `:1086`, `with_schedule` `:1034`, `weights_at`, тумбстоуны, `StakingReads::module_snapshot`/`speculative` `:1184-1200`, `epoch_committee_snapshot` `:1209`); `testbed/stand.rs` (дифф: `StandConfig` новые поля, `Outcome::committee_records`/`hashes`, порядок collect `:1314` против `:1369`, `DebuggingRecorder`); `testbed/committee_tests.rs` целиком (три теста `:185`, `:377`, `:571`); `testbed/preconditions.rs:1-60` (форма посылок); `e2e/src/staking_commit.rs` целиком, `e2e/src/staking_cost.rs` (образец контекста), `crates/staking-abi/src/lib.rs` (селекторы; в дереве чужие правки — читать, не править); `contracts/staking/src/consensus.rs` (`commitEpochCommittee`, `EpochNotYetCommittable`, горизонт `MAX_COMMITTEE_LOOKAHEAD_EPOCHS`, генезисный клэмп) — только чтение.

## Форма результата `.dpos-study/history/E4-1-B3-REVIEW.md` (§0–§6 как `E4-1-B2p-REVIEW.md`)

**§0 Прямые ответы** (нумерованные, каждый с [KNOWN]/[ГИПОТЕЗА]):

1. **Ворота** — прогнать САМОМУ, verbatim по одной строке: `cargo test -p fluentbase-consensus --lib` (журнал 671/0); `… --features dpos-devnet-byzantine testbed::` (43/0); `cargo test -p fluentbase-consensus --lib testbed::` без фичи (35/0); `cargo test -p fluentbase-node --lib` (57/0); `cargo test -p fluentbase-staking-reader` (63/0); `cargo test -p fluentbase-e2e --release a_committee_is_first_committable` (1/0); `cargo clippy -p fluentbase-consensus -p fluentbase-node --all-targets` и с фичей (одна чужая `large_enum_variant`); `cargo clippy -p fluentbase-e2e --all-targets`; `cargo fmt --check` по файлам захода; `cargo doc -p fluentbase-consensus --no-deps 2>&1 | grep -c "unresolved link"` (8). Прогони три новых стенд-теста ТРИЖДЫ (детерминизм: одинаковые якоря `64, 63, 64, 63`, `out_of_window = 72`, `not_readable = 3`?) и запиши, есть ли дрожание.
2. **Продакшн-код не тронут** — `git diff HEAD --stat -- crates/ e2e/` содержит только файлы захода; `git diff HEAD -- crates/dpos/consensus/src/committee` пуст; md5 `committee/store.rs` = HEAD (`git show HEAD:… | md5sum`).
3. **Старые тесты**: `git diff HEAD -- testbed/tests.rs testbed/preconditions.rs` пусты; ни один старый ассерт в `stand.rs`/`fakes.rs` не изменён (по диффу `^-.*assert`); 40 старых стенд-тестов идут при `committees_by_branch = None` по ТОМУ ЖЕ пути, что до захода (открой `epoch_committee_snapshot` `fakes.rs:1209` и `weights_at`: при дефолтной конфигурации ответ побайтно прежний? кольцо `epoch + 16 ≤ current + 2` — не задевает ли старые прогоны, доходящие до эпохи ≥ 14? найди самый длинный старый прогон).
4. **Тест 1 (`four_nodes_at_four_heights_…`)** — что именно НЕ вакуумно: журнал §7.1 честно говорит, что равенство записей вакуумно относительно выбора канонической высоты и что ловят `speculative == 0` + ассерт по `Outcome::hashes`. Проверь по коду: (а) `branch_of` (`fakes.rs:1086`) — как фейк решает «канон или нет» и может ли продовый читатель модуля на стенде вообще получить неканонический хэш (журнал §7.2 говорит — нет: `executed_state_hash` отдаёт только канон) ⇒ что тогда доказывает тест сверх «фейк работает»? (б) посылка «спекулятивная ветка реально существовала» — как проверяется (`el_events`/`head_gap_on_fcu`?); (в) тумбстоун-страддл `64, 63, 64, 63` — измеренный литерал; при сдвиге якорей тест красный с внятным сообщением (посылка 4)? (г) повтори мутацию (в) (тумбстоун в значение) сам.
5. **Тест 2 (`a_node_below_the_chain_refuses_…`)** — Д-53: постановка просила `NotReadable` на бэкфилле, тест пинует `OutOfWindow{above}` и отдельно `NotReadable` на якоре 0. Проверь арифметику по коду (`commit_height(E) = start(E−2)`, окно `+2`): верно ли, что для внутриоконной эпохи `NotReadable{ниже commit_height}` недостижим после заморозки геометрии? Если да — §5.6 проекта надо уточнять (скажи, что именно). Посылка «узел ниже `commit_height(E)` в момент запроса» — доказана через `Outcome`? `module_snapshot` (Д-54) считается ровно в порту модуля `EpochReads` — открой `fakes.rs:1184-1200`; «один снимок на эпоху» после догона — не вакуумно (мутация (б) повторена?).
6. **Тест 3 (`a_weightless_committee_…`)** — `weights: None` на уровне контракта (Д-52): кто ещё на стенде читает `weights` из снимка (`grep -n weights` по `epoch_transition.rs`, `scheme.rs`, `application.rs`, `cert_inlet.rs`, `slasher/`) — правда ли, что только модуль? Метрики через `DebuggingRecorder` (Д-55): потоко-локальный рекордер видит ВЕСЬ прогон (детерминированный раннер однопоточный — открой `testbed/capture.rs`)? Ассерт «ровно 4 `error!` и ни одной другой ERROR-строки» — устойчив ли к посторонним `error!` (ложное красное, не ложное зелёное — приемлемо?). Повтори мутацию `store.rs` (`unwrap_or_else(vec![1;n])`) сам.
7. **e2e-пин** — открой `e2e/src/staking_commit.rs` целиком: контекст (`with_full_genesis`, 4 валидатора, interval 100, activation 1000), `set_block`, ревёрт по селектору `EpochNotYetCommittable(uint64,uint64)` и обоим аргументам; граница `start(3)−1 = 1299` ревёрт / `1300` проходит; горизонт `+2`; генезис: блок 1 → 0,1,2 проходят, 3 — ревёрт. Сверь с контрактом (`contracts/staking/src/consensus.rs`): формула `current_epoch` при `block < activation` (клэмп 0?), условие `target ≤ current + 2`, что `commitEpochCommittee` — `SYSTEM_CALLER`-only и как e2e это обходит. Что тест НЕ доказывает (журнал §0.2: активация 0 запрещена; `node/evm.rs` не участвует) — подтверди/дополни. Мутацию «принятый вызов на `start(C)−1`» повтори.
8. **Формула кольца** `weights_at`: `epoch + WEIGHT_RING_EPOCHS ≤ current + MAX_COMMITTEE_LOOKAHEAD_EPOCHS` — сверь с контрактом (`contracts/staking` — где кольцо весов пишется/читается; `staking-abi/src/lib.rs:204-214`) и с `committee/mod.rs:42-56` (`const _: () = assert!`); знак/граница совпадают? На стенде не исполняется (§7.6) — предложи, как дёшево исполнить (короткий interval?) или скажи, что нельзя.
9. **`Outcome::committee_records` в конце прогона через трейт** (Д-56) — порядок collect (`stand.rs:1314` vs `:1369`) и что это меняет для ассертов; есть ли ассерт, который читает эпоху, не прочитанную прогоном, и получает свежее чтение на финальном якоре (тогда он пинует не то, чем узел пользовался)?
10. **Граница/гигиена**: новые `pub(super)`; `#[allow]`/`todo!`/таймеров нет; `unwrap` только в тестах; `StandConfig` — новые поля с дефолтами; размер/время трёх тестов (журнал: 22 с на три?) — приемлемо для набора.
11. **Где журнал вводит в заблуждение**; что из §7 проекта «Проверка 4.1» / §5.6 неверно (журнал §0.4 называет четыре пункта).

**§1 Находки** — `id | серьёзность | file:lines | что не так | чем пытался опровергнуть | уверенность`; id `B3-nn`. **§2 Поведение по ханкам.** **§3 Граница.** **§4 Ворота** verbatim (включая три прогона детерминизма). **§5 Оставить как есть** — непустой. **§6 Вне рамок** — для закрытия 4.1 (что из R-027/R-040/R-019/R-076/R-075/R-123 стенд теперь реально пинует, а что нет), 4.2, 4.3.

В финальном ответе — только §0 (кратко) и таблица §1.

# Ревью строки 4.1, заход Б1 (проводка модуля `committee/` в узел, follower, слэшер и стенд; снятие трёх `CommitteeReads`-impl и `qual_read_at`)

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база — `HEAD` (`git rev-parse HEAD`), объект — **незакоммиченное рабочее дерево** (`git diff HEAD`, `git status --short`; файлы под `devnet/` — чужая работа, игнорировать). Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`; `reader/` = `crates/dpos/staking-reader/src/`.

## Правила

1. **Агентов не запускать** — один контекст должен держать спеку, дифф и журнал одновременно.
2. **Git только на чтение.** Никаких правок файлов вообще, кроме одного: результат — `.dpos-study/history/E4-1-B1-REVIEW.md`.
3. Каждое утверждение — с `file:line` дерева и, где есть «до», `HEAD:file:line`. Утверждение без якоря не засчитывается.
4. Комментарии, док-комментарии, `.claude/dpos_architecture/` и сам журнал исполнителя — НЕ свидетельство поведения; в этом коде они дрейфуют. Свидетельство — код и прогон, который ты сделал сам.
5. **Полнота, не фильтр**: перечисляй всё найденное с серьёзностью (BLOCKER / SERIOUS / MODERATE / MINOR / NIT); отбор делает оркестратор. Каждой находке — «чем пытался опровергнуть и почему не вышло».
6. Читать файлы целыми строками (Read / `sed -n 'N,Mp'`), никогда `cut -c`/`head -c`.

## Вход

- Спека: `.dpos-study/PLAN.md:54` (строка 4.1); `.dpos-study/history/E4-CORE-DESIGN.md` §5.1 (`443-505`) — особенно таблица «Кто читает через `CommitteeReads`», «Кто читает через модуль после 4.1», «`tombstoned`», «Один `EpochTransition`»; §5.4 (`549-572`); §5.5 (`574-577`); §7 пункт «4.1 наполовину».
- Что было ДО этого захода: журнал `.dpos-study/history/E4-1-A.md` (части А и В) и ревью `E4-1-A-REVIEW.md` — заход А уже закоммичен; постановка захода Б1 — `.dpos-study/history/E4-prompts/4.1-B1-impl-1.md` (список разрешённых файлов, что Б1 обязан и что запрещено).
- Журнал исполнителя Б1: `.dpos-study/history/E4-1-B1.md` — читать ПОСЛЕ диффа.
- Дифф: `git diff HEAD --stat`, затем `git diff HEAD -- <файл>` по каждому из 19 файлов под `crates/`; `.dpos-study/history/E4-ORCHESTRATOR.md` в диффе — правка оркестратора, игнорировать.
- Контекст в коде (дерево): `committee/{mod,store,facade}.rs` целиком; `node/dpos.rs` — `build_beacon_plane` (курсор, `RethAnchor`, `GeometryRx`, фасад), tombstone-поллер, `evidence_committee_for`, `launch_dpos_layer` (`ProviderExecutedChain::with_cursor`); `node/ordering.rs`; `node/cert_follow/mod.rs`; `dpos.rs` (consensus) — `launch_follower` (стор, фасад, `committee_at`), удалённый watchdog, `initial_snapshot` (`:1995`); `executor.rs` — `AnchorAdvancedFn` и три вызова; `outer.rs` — `slasher::Config`, `executor::Config`, снятие `with_randomness`; `slasher/actor.rs` — `resolve_committee`; `application.rs` — снятие `randomness`; `beacon/plane.rs`, `beacon/follower.rs` — снятие `qual_read_at`; `testbed/stand.rs`, `testbed/fakes.rs`, `testbed/tests.rs`.

## Форма результата `.dpos-study/history/E4-1-B1-REVIEW.md` (как `history/E5-0-REVIEW.md` §0–§6)

**§0 Прямые ответы** (нумерованные, каждый с [KNOWN]/[ГИПОТЕЗА]):
1. Ворота — прогнать САМОМУ и вставить verbatim по одной строке: `cargo test -p fluentbase-consensus --lib` (база до захода 664/0); `… --features dpos-devnet-byzantine testbed::` (40/0); `cargo test -p fluentbase-node --lib` (59/0 до; ожидание 57/0 — два теста курсоров удалены); `cargo test -p fluentbase-staking-reader` (60/0); `cargo test -p fluentbase-consensus --test slasher_integration`; `cargo clippy -p fluentbase-consensus -p fluentbase-node --all-targets` и с фичей; `cargo fmt --check`; `cargo doc -p fluentbase-consensus --no-deps 2>&1 | grep -c "unresolved link"` (9). Расхождения с журналом — назвать.
2. Один якорь: сколько `FinalizedCursor` рождается у валидатора и у follower'а (grep `FinalizedCursor::default\|with_cursor`), держат ли `RethAnchor` и `ProviderExecutedChain` ОДИН и тот же `FinalizedCursor` (проследи `Arc`/клоны по коду, не по журналу); три вызова `anchor_advanced` стоят СТРОГО ПОСЛЕ `advance_finalized` (`executor.rs` — открой все три); есть ли четвёртая точка, где курсор двигается без вызова.
3. Стартовое окно: до `executor::Actor::init` якорь = 0 (курсор ещё не засеян) — кто в этот момент уже читает через модуль (beacon-плоскость, ET-поллер, слэшер, tombstone-поллер?) и что они получают (`NotReadable{ready_at: 0}`); есть ли потребитель, который на `NotReadable` при старте делает что-то необратимое (мемо `frozen_dkg_qual`, отказ старта, `Permanent` у слэшера, пропуск границы ET). Отдельно: `GeometryRx` (Д-15) — пока геометрия не заморожена, всё `NotReadable`; кто зависит от того, что раньше читал комитет ДО заморозки геометрии (сравни с `HEAD:node/dpos.rs:1383-1500` — курсор `max(fin, live)` читал сразу)?
4. Поведение по каждому переведённому носителю против HEAD: `PlaneCommitteeReads` → фасад (что теряется: `live`-курсор — по проекту принято; что-то ещё?); `FollowerCommitteeReads` → фасад; `evidence_committee_for` → модуль (memo снят); tombstone-поллер (`executed_state_hash(fin)` вместо `block_hash(fin)` — есть ли регресс на бэкфилле: раньше читал по заголовку, теперь паркуется?); слэшер `resolve_committee` — маппинг `NotReadable`/`Read(transient)` ⇒ `Transient`, `OutOfWindow` ниже/`Read(permanent)` ⇒ `Permanent`, а `OutOfWindow` ВЫШЕ окна (`is_transient() == true`)? — открой код; watchdog удалён — что-нибудь ещё его звало/ждало; follower `committee_at` из записи (Д-18: `tombstoned = false`, `activation_epoch = 0` — найди КАЖДОГО читателя этих двух полей на пути `boundary_rx` → `reconcile_roles` → `signer`/`WeightedVrf`/`FluentApp::committee_index` и скажи, кто из них их читает); `FluentApp::randomness` снят — grep читателей.
5. Стенд: `a_committee_not_yet_committed_is_not_read_early` — посылка `sum > 50` заменена на «каждая пройденная эпоха прочитана ≥ 1 раз»; это (а) равносильная защита от вакуумности, (б) ослабление, (в) усиление? Открой тест на HEAD и в дереве, скажи, что именно он теперь доказывает и что перестал. Другие старые ассерты стенда изменены? (`git diff HEAD -- testbed/` по `assert`).
6. Граница: `beacon::CommitteeReads` — ровно одна реализация (фасад)? `qual_read_at` удалён из трейта и обеих проекций? Что из `pub` в `committee/` и `node/ordering.rs` появилось/пропало; `ProviderExecutedChain::new` удалён — все вызывающие переведены?
7. Тесты: счёт `#[test]` HEAD vs дерево по каждому из 19 файлов; удалённые поимённо — оправдано ли (предмет исчез); новые — что утверждают, «был ли красным» / мутация.
8. Продакшн-путь: `unwrap`/`expect`/`#[allow]`/`todo!`/таймеры/`dbg!` — новые, с якорями.
9. Где журнал вводит в заблуждение — утверждения журнала, не подтверждённые деревом (с якорем).
10. Что из §5.1 оказалось неверным против кода (журнал называет два места — Д-15 ленивая геометрия и невозможность громкого отказа старта через модуль до `OuterBuilder::build`; подтверди или опровергни по коду; есть ли третье).
11. Правка вне списка файлов (Д-16 `outer.rs`): чистая проводка или изменение поведения? Перечисли ханки.

**§1 Находки** — таблица `id | серьёзность | file:lines (дерево) | HEAD-якорь | что не так | чем пытался опровергнуть и почему не вышло | уверенность`. Серьёзность BLOCKER — только для регрессии поведения на пути голосования/финализации/границы, которую проект не предписывал.
**§2 Поведение по ханкам против HEAD** — по каждому тронутому файлу.
**§3 Граница** — таблица имён `pub`.
**§4 Ворота** — вывод команд verbatim.
**§5 Оставить как есть** — непустой.
**§6 Замечено вне рамок Б1** — для Б2 (`EpochSchemeProvider` как вид, `CertInlet.schemes`, один ET, `latest_live`), Б3 (стенд `FakeStaking` по ветке хэша), 4.2, 4.3.

В финальном ответе — только §0 (кратко) и таблица §1.

# Строка 4.1, заход Б2, третий проход — находки ревью `E4-1-B2-REVIEW.md`

Ты — исполнитель (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`; объект — незакоммиченное рабочее дерево захода Б2 (19 файлов под `crates/`, `git status --short | grep crates/`). База захода — `89046c93`; HEAD ветки ушёл чужими коммитами только по `contracts/staking/**` и `.dpos-study/**`, так что для `crates/` `git diff HEAD` = дифф захода. В `git status` посторонние `devnet/**`, `target-contract/`, `contracts/**`, `.dpos-study/history/E4-ORCHESTRATOR.md` — не твоё, не трогать. Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`; `reader/` = `crates/dpos/staking-reader/src/`.

## Правила

1. **Агентов не запускать.** 2. **Git только на чтение.** 3. **Файлы на запись**: `committee/**`; `epoch_manager.rs`; `outer.rs`; `cert_inlet.rs`; `node/cert_inlet.rs`; `dpos.rs` (consensus; cold start валидатора `:1985-2016` и follower `:3400-3425`, follower-цикл inlet'а); `scheme.rs` (ТОЛЬКО B2-12); `testbed/stand.rs`, `testbed/tests.rs` (новые тесты — в конец; старые ассерты не трогать); `.dpos-study/history/E4-1-B2.md` (дописать **часть В** в конец; части выше не переписывать, кроме исправления трёх якорей B2-13 прямо в тексте Д-29/Д-26/Д-33 с пометкой «[исправлено в части В]»). Всё остальное — запрещено (`beacon/**`, `executor.rs`, `engine.rs`, `reader/**`, `node/dpos.rs`, `devnet/**`, `Cargo.toml`, `contracts/**`, `.claude/**`). 4. Каждое утверждение — file:line. 5. Без `#[allow]`/`todo!`/таймеров/поллинга; `unwrap` на продакшн-пути — только `Mutex::lock().unwrap()`. 6. Читать целыми строками. 7. Тест пишется ДО правки и обязан быть красным до неё (verbatim вывод в журнал). 8. Cargo: `CARGO_BUILD_JOBS=6`; `Blocking waiting for file lock` — ждать.

## Что прочитать

1. `.dpos-study/history/E4-1-B2-REVIEW.md` целиком (§0, §1 B2-01…B2-15, §5, §6).
2. `.dpos-study/history/E4-1-B2.md` §0, §3 (Д-24…Д-34), §7, §8; `.dpos-study/history/E4-1-B1.md` часть В как образец формы.
3. `.dpos-study/history/E4-CORE-DESIGN.md` §5.1 (`443-505`) — «громкий отказ старта», `subscribe` = «wake and re-ask».
4. Код: `committee/{mod,store}.rs` целиком; `epoch_manager.rs:500-640` (поля, `new`), `:700-830` (select: `boundary_rx` `:754-762`, `committee_readable.changed()` `:803-805`), `:1040-1100` (`is_live_epoch`, начало `reconcile_roles`, ранний `return` `:1073-1083`), `:1480-1500` (`reconcile_live`), `:1850-1890` (`register_span`); `dpos.rs:1960-2016` (cold start валидатора), `:2580-2605` (форвардер границ), `:3400-3460` (follower cold start и его `boundary_tx`); `reader/epoch_transition.rs:660-700` (`cold_start`, `boundary_tx.try_send` `:667` — ТОЛЬКО читать: стреляет ли cold start границей в мост); `node/cert_inlet.rs:84-97`; `cert_inlet.rs:43-51`, `:511-752` (`ingest`); `testbed/stand.rs:2036-2062`; `HEAD:testbed/stand.rs:2080-2100` (`git show HEAD:… | sed -n`) — что утверждал стенд ДО захода; `outer.rs:260-280`, `:910-925`; `scheme.rs:50-70`.

## Решения оркестратора по каждой находке (исполнить ровно так; отклонение — только с причиной file:line в В§0)

| id | решение | что сделать |
|---|---|---|
| B2-01 | **FIXED** | Две части. (а) **Постоянный отказ на cold start — снова громкий**, как предписывает §5.1: на валидаторе (`dpos.rs:1991-2013`), follower'е (`:3408-3421`) и стенде (`stand.rs:2051-2062`) `Err(e)` с `!e.is_transient()` ⇒ `bail!`/паника стенда с прежним операторским текстом; транзиентный (`NotReadable`, `Read(transient)`, `OutOfWindow` сверху) ⇒ `warn!` + повтор. (б) **Повтор обязан существовать по коду, а не по комментарию**: установи по `reader/epoch_transition.rs:660-700` и `dpos.rs:2580-2605`/`:3434-3460`, доходит ли cold-start эпоха до `epoch_manager` как граница (`boundary_rx`) на валидаторе и на follower'е. Если доходит — повтор даёт механизм B2-02 (ниже) и нужно только записать цепь file:line. Если на каком-то классе узлов НЕ доходит — `epoch_manager::Actor::new` получает стартовую эпоху (`initial_epoch`) и кладёт её в `deferred_reconciles` (B2-02) при постройке, чтобы первое же пробуждение модуля её реконсилировало. Тест `epoch_manager` (см. B2-02) покрывает обе. Д-27 переписать в части В как «постоянный — громко, транзиентный — warn + повтор через `subscribe`». |
| B2-02 | **FIXED** | В `epoch_manager` — множество `deferred_reconciles: BTreeSet<Epoch>`: ранний `return` в `reconcile_roles` при `Err` чтения (`:1073-1083`) вставляет `epoch`; ветка `committee_readable.changed()` (`:803-805`) сначала дренирует множество — для КАЖДОЙ отложенной эпохи вызывает `reconcile_roles(epoch, muxes)` БЕЗ гейта `is_live_epoch` (это первый прогон реконсила по этой эпохе — бухгалтерия и `soft_enter` ещё не бежали, так что даунгрейд-churn из дока `reconcile_live` `:1480-1485` невозможен — ПРОВЕРЬ это по коду: что до `return` не пишется ничего, и назови file:line), затем как сейчас `reconcile_live`. Успешный проход `reconcile_roles` удаляет эпоху из множества. Тест в `epoch_manager` `mod tests` (образец — существующие тесты актора с фейковым `Committee`; `SchemeCommittee` из `committee::testing` умеет `|_| None` — если нужен фейк, который сначала `Err(NotReadable)`, потом `Ok`, добавь его в `committee/mod.rs` `mod testing`): граница `E` приходит при нечитаемом комитете, `highest_observed_epoch > E`, затем модуль публикует пробуждение и комитет становится читаем ⇒ `reconcile_roles(E)` исполнен (наблюдай по `roles`/`highest_entered_epoch`/вызову `soft_enter` — что доступно). Красный ДО правки — обязателен, вывод verbatim. Д-nn. |
| B2-03 | **RECORDED** | Ленивое чтение в `CertProvider::scoped` — свойство «вида на карту» §5.1, стоимость не разобрана. В код не лезть. В В§0 одной строкой + Д-nn («промах `scoped` = два staticcall'а в акторе marshal'а; замер — Б3/Э7; смягчение — прогрев эпохи сертификата при `soft_enter`, если замер покажет») + В§3. |
| B2-04 | **FIXED** | `CertInlet::ingest` не возвращает `Err` ни на одном пути (`cert_inlet.rs:511-752`) — сменить тип на `()` (или на то, что реально различается), убрать обе мёртвые ветки `if let Err(e) … "cert-inlet fatal (committee read)" … break` (`node/cert_inlet.rs:91-94`, follower-цикл в `dpos.rs` — найди), тесты inlet'а поправить по типу без изменения ассертов. Д-34 в части В дополнить: «фатал не пропал, а стал мёртвым кодом — удалён». |
| B2-05 | **FIXED** | Стендовый ассерт ослаблен до вакуумного (`stand.rs:2051-2062`: `assert!(e.is_transient())` при якоре 0 на сборке). Постановка запрещала менять ассерты старых тестов. Восстановить силу утверждения, не возвращая прямого чтения контракта: свойство «комитет cold-start эпохи у каждого узла непуст и прочитан» должно утверждаться там, где якорь уже не 0 — например, в `Outcome`/финальной проверке стенда (`committed[E0] ≥ 1` у каждого узла и запись через фасад непуста), либо первым делом после первого derive. Часть (а) B2-01 (постоянный ⇒ паника) при этом остаётся на сборке. Назови в В§0, что именно теперь доказывает стенд про cold-start эпоху и на каком месте. |
| B2-06 | **FIXED (минимально)** | `BeaconSlot`: второй `set` — не `let _ =`, а `error!` с текстом «beacon rebuilt in-process; committee module keeps the FIRST beacon — every later epoch would get no scheme» (сегодня недостижимо — `run_node_stack` ветвится однократно; сторож на будущее). Одна строка + тест модуля не нужен. |
| B2-07 | **FIXED** | Док `cert_inlet.rs:43-51`: `probe_inconsistency` ⇒ `ReadError::Backend` (`executed.rs:64-68`), `is_transient() == false`, модуль печатает `error!` и тикает `dpos_committee_read_permanent_total{reason="anchor_fault"}` — переписать по факту. |
| B2-08 | **FIXED** | `register_span` (`epoch_manager.rs:1870-1878`): ломать цикл на `committee(epoch)` `Err` (запись не читается), а не на `scheme(epoch).is_none()`; `scheme == None` при читаемой записи — `warn!` и продолжать. |
| B2-09 | **RECORDED** | Ретенция по полу окна вместо счёта регистраций — предписано §5.1; следствие («долго не регистрировавший узел теряет записи ниже `epoch(anchor)−8`») одной строкой в В§0 + Д-nn. |
| B2-10 | **FIXED** | Тест в `committee/tests.rs` на четвёртый отказ `upgrade_scheme` («нет записи» ⇒ `false`, карта не тронута). Красный до правки невозможен (поведение уже есть) — мутация: убрать проверку ⇒ тест красный; вывод verbatim. |
| B2-11 | **RECORDED** | Четыре файла вне списка — Д-31 стоит; одной строкой. |
| B2-12 | **FIXED, если дёшево** | `scheme.rs:57` `soft_enter_verifier` без продакшн-вызывающих: удалить, тестовые вызовы (`epoch_manager.rs:2152`, `:3056`, `mod tests`) перевести на то, чем пользуется продакшн (`committee::epoch_verifier`/`build_verifier`). Если перевод тестов требует больше ~30 строк — RECORDED с причиной. |
| B2-13 | **FIXED (журнал)** | Три якоря в §3: Д-29 `epoch_manager.rs:832` → реальная строка ветки `committee_readable.changed()`; Д-26 `store.rs:349` → реальная строка `resolve_scheme`; Д-33 `dpos.rs:2288` → реальная строка форвардера. После ТВОИХ правок строки поедут ещё — ставь по итоговому дереву. |
| B2-14 | **FIXED** | Комментарии `testbed/stand.rs:1652-1656`: якоря `cert_inlet.rs:896-898` → `:715`-область (тее-запись), `:343-347` → `:264` (поле) — по итоговому дереву. |
| B2-15 | **RECORDED** | `latest_scheme` может назвать `epoch(anchor)+2` ⇒ `peers_for_finalization` адресует ещё не живую эпоху; класс тот же, что на HEAD, потолок выше. Одной строкой + В§3 для 4.3 (`TrackedPeers.primary = C[E−1]∪C[E]∪C[E+1]` — `+2` вне окна `track`). |

## Ворота (сам, verbatim по одной строке)

`cargo test -p fluentbase-consensus --lib` (665 + новые); `… --features dpos-devnet-byzantine testbed::` (40/0 — ни один старый ассерт не тронут); `cargo test -p fluentbase-node --lib` (57/0); `cargo test -p fluentbase-staking-reader` (58/0); `cargo test -p fluentbase-consensus --test slasher_integration` (16/0); `cargo clippy -p fluentbase-consensus -p fluentbase-node --all-targets` (одна чужая `large_enum_variant`) и с `--features dpos-devnet-byzantine`; `cargo fmt --check` (0); `cargo doc -p fluentbase-consensus --no-deps 2>&1 | grep -c "unresolved link"` (9).

## Журнал — часть В в конце `.dpos-study/history/E4-1-B2.md`

`# Часть В — заход Б2, третий проход: находки ревью E4-1-B2-REVIEW.md`. В§0: (1) `FIXED n · RECORDED m · REJECTED k` с id; (2) ворота verbatim; (3) новые/изменённые тесты — имя / что утверждает / красный ДО правки (verbatim) или мутация; (4) новые строки Д-nn (нумерация после Д-34) и переписанные Д-27/Д-34; (5) цепь доставки cold-start эпохи в `epoch_manager` на валидаторе и follower'е (file:line) — вывод B2-01(б); (6) что в §5.1 после прохода по-прежнему неверно по коду. В§1 таблица прослеживания `id | исход | что сделано | file:line`. В§2 обновлённая раскладка логических коммитов Б2 (§6 журнала + что добавил проход В; компилируемость группы проверить `cargo check … --all-targets` реально). В§3 где проверка слабее всего / что остаётся 4.2, 4.3, Э7.

В финальном ответе — только В§0.

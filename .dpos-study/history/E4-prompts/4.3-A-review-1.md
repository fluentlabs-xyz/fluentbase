# Ревью строки 4.3, заход А (`TrackedPeers{primary, secondary}`, `Ingress` на BEACON/EVIDENCE, лимиты `deque_size`)

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база — `HEAD` (`git rev-parse HEAD`; ожидается `86fe7098`). Объект — **незакоммиченное рабочее дерево** захода А строки 4.3: файлы из `git status --short | grep -v devnet/` под `crates/`. **Всё остальное — чужое** (`devnet/**`, `.dpos-study/history/E4-ORCHESTRATOR.md`) — не трогать. Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`; `reader/` = `crates/dpos/staking-reader/src/`; `p2p/` = `crates/dpos/p2p/src/`.

## Правила

1. **Агентов не запускать.** 2. **Git только на чтение.** Единственный файл на запись — `.dpos-study/history/E4-3-A-REVIEW.md`; мутации продакшн-кода ради чувствительности тестов — только с откатом и `md5sum` до/после. 3. Каждое утверждение — `file:line` дерева и, где есть «до», `HEAD:file:line`. 4. Комментарии, доки, `.claude/dpos_architecture/`, журнал исполнителя — не свидетельство; свидетельство — код и прогон, который ты сделал сам. 5. **Полнота, не фильтр**: все находки, включая мелкие и неуверенные, с серьёзностью (BLOCKER / SERIOUS / MODERATE / MINOR / NIT) и «чем пытался опровергнуть»; не объединять разные находки. 6. Читать целыми строками. 7. `CARGO_BUILD_JOBS=4` (параллельно идут ворота оркестратора на 6). 8. `.claude/COMMONWARE_INTERNALS.md` — ДО checkout'а (peer sets/tracker `:363`, buffered retain, resolver participants, authenticated dial policy).

## Вход

- Спека: `.dpos-study/history/E4-CORE-DESIGN.md` §5.3 (`:534-548`), §10 §0.6/§0.8/A63/C.3; `PLAN.md:104`; `REGISTER.md` R-054, R-029, R-013, R-037, R-023, R-096; `E4-PRECONDITIONS.md` Д5, Д8.
- Постановка: `.dpos-study/history/E4-prompts/4.3-A-impl-1.md`. Журнал: `.dpos-study/history/E4-3-A.md` — читать ПОСЛЕ диффа.
- Дифф: `git diff HEAD --stat -- crates/`, затем по файлам. Якоря дерева (проверены оркестратором, [KNOWN]): `reader/epoch_transition.rs` — `TrackedPeers` `:138-147` (+ `primary()` `:151`, `epochs_of()` `:161`, `is_secondary_only()` `:169`), трейт `:182`, `assemble_tracked_peers` `:739-760`, `push_neighbour_committee` `:767-795`, вызовы `track` `:845`, `:863`; `p2p/lib.rs` — адаптер `:309-318`, `TrackedWindow` `:333-364`, `Ingress`/`EpochMask` `:409-470`; `consensus/dpos.rs` — `GatedReceiver` `:70-140`, метрика `:63-68`, `deque_size` `:2563`/`:3422`; `node/dpos.rs` — окно `:1315-1324`, EVIDENCE `:1817-1822`, BEACON `:1891-1897`; `beacon/actor.rs` — `epoch_is_actionable` `:1858-1864`, `beacon_member` `:1878-1880`, `on_message` `:1882-1925`, `on_confirm` окно `:1432-1442`; `slasher/gossip.rs` `ingest_batch` `:106-175`; `dkg_transport.rs:135`; `stand.rs` `TrackSink` `:1044-1104`, `deque_size` `:2342`; новые тесты `testbed/tests.rs:4131` (`a_registry_only_node_…`), `a_beacon_frame_from_a_non_member_costs_no_committee_read_beyond_the_check`, `an_evidence_batch_from_outside_the_epochs_committee_resolves_nothing` (grep); механические правки вне списка: `p2p/tests/convergence.rs:64-72`, `consensus/tests/slasher_integration.rs:880-890`. Diff: 12 файлов +1170/−166.

**Отдельно проверь (замечания оркестратора):** (а) переписанный старый стенд-тест 4b `a_node_outside_the_tracked_peer_set_stands_at_the_boundary` (`tests.rs:505-580`, `git diff HEAD`): `latest_delivered == 0` → `> 0`, порог `< 10` → `< 15`, ассерт `finalized_delivered == 0` УДАЛЁН без замены — механизм (C[E−1] держит выбывшего ещё эпоху) верен по §5.3? снятое свойство «by-height ответ по несуществующей линии» потеряно или перепинено где-то ещё? фальсификатор действительно строже?; (б) юнит beacon'а (журнал §2 п.1): конфирмация до первого тика высоты теперь отбрасывается — на рестарте посреди эпохи `last_height` актора засеивается чем и когда (grep `last_height`), может ли честная конфирмация соседа быть отброшена в окне до первого `on_height`?; (в) Д-123 — два источника классификации на BEACON (`committee_for` vs `TrackedWindow`): расхождение «окно пустое / `committee_for` отвечает» — что происходит; (г) Д-126 — `now+3` дропается: узел, отставший по `last_height` на эпоху, теряет DKG-трафик соседей? при каком разбросе это ломает церемонию; (д) `GatedReceiver` без своего юнита; (е) `EpochMask` при `epochs_of` > 3 (невозможно по построению?).

**Ворота: полный набор УЖЕ гонит/прогнал оркестратор** — `…/scratchpad/gates/a3f-*.txt` (`a3f-status.txt` коды, `a3f-tree.md5` снимок — сверь `md5sum -c`). Если `DONE` ещё нет — сначала ревью кода, §4 в конце, ждать `sleep 60` не дольше 40 мин. Набор НЕ перегонять; только точечные тесты и мутации.

## Форма результата `.dpos-study/history/E4-3-A-REVIEW.md` (§0–§6 как `E4-2-V-REVIEW.md`)

**§0 Прямые ответы** (нумерованные, каждый с [KNOWN]/[ГИПОТЕЗА]):

1. **Ворота** — verbatim из файлов оркестратора.
2. **`TrackedPeers`**: подпись изменена во всех четырёх местах (`reader/epoch_transition.rs` трейт + два вызова + два тест-синка, `p2p/lib.rs` `OracleHandle`, `stand.rs` `TrackSink`)? Состав primary = `C[E−1] ∪ C[E] ∪ C[E+1]` — откуда записи (модуль vs reader), что при незакоммиченном `C[E+1]`, что при `C[E−1]` ниже активации/окна; secondary = реестр на якоре; `check_peer_set_size` только на primary — по CW (`tracker`) это верно? Реестровый не-член ушёл из primary — какие последствия для `buffered` (тела реестровых узлов) и для resolver (`participants`) — по CW file:line; **регрессия для входящего члена `C[E+1]`, чьё тело DKG должно удерживаться `buffered`** (комментарий на HEAD `epoch_transition.rs:663-670`) — сохранено?
3. **`Ingress`**: источник окна (тот же набор, что в `track`, без `Provider::peer_set` на кадр?), маска эпох, tombstoned ⇒ `Dropped` до декода — где источник `TombstoneSet`; BEACON: что декодируется ДО проверки членства (заголовок/эпоха), `Confirm.target_epoch ∉ [now, now+2]` — источник `now`, отброс до `committee_for`; EVIDENCE: `from` прокинут, членство ∧ окно до `committee_for`; есть ли путь, где кадр не-члена всё ещё вызывает EVM-чтение (grep `committee_for`/`committee(`) — если да, серьёзность; **гонка «peer-set обновился, окно `Ingress` ещё старое»** — кадр члена новой эпохи отброшен на время? как долго и что его повторяет.
4. **VOTE/CERT backup**: подтверждение исполнителя, что потребителя нет (`node/dpos.rs` route-miss наблюдатели) — перепроверь; `epoch_from_subchannel` — только метка?
5. **Лимиты**: `deque_size` BROADCAST 64→4 во всех трёх местах (`dpos.rs` ×2, `stand.rs`); байтовый лимит — есть у CW `buffered::Config` или записано как граница; DKG bodies `MAX_COMMITTEE_SIZE`→2 — DKG-стенд-тесты (B1/B2/B3/C9, разрез сети, `dkg_bodies_per_peer_…`) зелёные в воротах оркестратора? Что случится при перепредложении после nullify с `deque_size = 2` — по CW `buffered/engine.rs` (eviction политика) file:line.
6. **Стенд**: тиры `commonware_p2p::simulated` моделируются или тест на authenticated-транспорте; два теста — что пинуют, невакуумность (мутация красит?), «`FakeStaking`-чтения не вызваны» — как считается (`StakingReads`).
7. **Юниты «красный до правки»** — воспроизведи один (HEAD-версия теста над HEAD-кодом красная?); мутации ≤ 2.
8. **Гигиена**: `pub` изменения, `#[allow]`, `todo!`, таймеры, `unwrap`; правки вне списка (особенно `beacon/**` кроме `actor.rs`/`dkg_transport.rs`, `committee/**`, `preconditions.rs`).
9. **Где журнал вводит в заблуждение**; расхождения с §5.3; hard-stop оркестратора: (2) П-5/П-4 менять? (3) BLOCKER без ответа проекта? (4) стенд опроверг проект? — да/нет со свидетельством.

**§1 Находки** — `id | серьёзность | file:lines | HEAD-якорь | что не так | чем пытался опровергнуть | уверенность`; id `P-nn`. **§2 Поведение по ханкам.** **§3 Граница.** **§4 Ворота** verbatim. **§5 Оставить как есть** — непустой. **§6 Вне рамок** — Э5, Э7.

В финальном ответе — только §0 (кратко) и таблица §1.

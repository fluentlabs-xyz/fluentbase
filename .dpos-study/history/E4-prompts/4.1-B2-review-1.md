# Ревью строки 4.1, заход Б2 (одна карта «запись + схема», один продюсер схем, `CertInlet` без второй таблицы, канал границ по `Epoch`)

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`. Объект — **незакоммиченное рабочее дерево** захода Б2: 20 файлов под `crates/` (`git status --short | grep crates/`). **База захода — `89046c93`**, но HEAD ветки ушёл вперёд (`52c84f6d`) чужими коммитами, которые трогают только `contracts/staking/**` и `.dpos-study/**`; поэтому для файлов под `crates/` `git diff HEAD -- <файл>` и `git diff 89046c93 -- <файл>` совпадают — используй `HEAD`. В `git status` посторонние `devnet/**`, `target-contract/`, `.dpos-study/history/E4-ORCHESTRATOR.md` — не твоё, игнорировать. Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`; `reader/` = `crates/dpos/staking-reader/src/`.

## Правила

1. **Агентов не запускать** — один контекст должен держать спеку, дифф и журнал одновременно.
2. **Git только на чтение.** Никаких правок файлов вообще, кроме одного: результат — `.dpos-study/history/E4-1-B2-REVIEW.md`.
3. Каждое утверждение — с `file:line` дерева и, где есть «до», `HEAD:file:line`. Утверждение без якоря не засчитывается.
4. Комментарии, док-комментарии, `.claude/dpos_architecture/` и сам журнал исполнителя — НЕ свидетельство поведения; в этом коде они дрейфуют. Свидетельство — код и прогон, который ты сделал сам.
5. **Полнота, не фильтр**: перечисляй всё найденное с серьёзностью (BLOCKER / SERIOUS / MODERATE / MINOR / NIT); отбор делает оркестратор. Каждой находке — «чем пытался опровергнуть и почему не вышло».
6. Читать файлы целыми строками (Read / `sed -n 'N,Mp'`), никогда `cut -c`/`head -c`.
7. Cargo: параллельно с тобой могут идти прогоны оркестратора — `Blocking waiting for file lock` нормален, жди; ставь `CARGO_BUILD_JOBS=6`.

## Вход

- Спека: `.dpos-study/PLAN.md:54` (строка 4.1); `.dpos-study/history/E4-CORE-DESIGN.md` §5.1 (`443-505`) — «Один продюсер схем», «Один `EpochTransition`», `install`/`subscribe`/«громкий отказ старта»; §5.5 (`574-577`) — что удаляется; §7 пункт «4.1 наполовину» («две авторитетные таблицы за общим типом»).
- Что было ДО: журналы `history/E4-1-A.md` (части А, В), `history/E4-1-B1.md` (части А, В), ревью `E4-1-A-REVIEW.md`, `E4-1-B1-REVIEW.md` — заходы А, Б1 закоммичены. Постановка Б2 — `.dpos-study/history/E4-prompts/4.1-B2-impl-1.md` (список разрешённых файлов; что Б2 обязан — Б2.1 одна карта, Б2.2 inlet, Б2.3 один ET, Б2.4 `latest_live`; что запрещено).
- Журнал исполнителя Б2: `.dpos-study/history/E4-1-B2.md` — читать ПОСЛЕ диффа.
- Дифф: `git diff HEAD --stat -- crates/`, затем `git diff HEAD -- <файл>` по каждому из 20 файлов.
- Контекст в коде (дерево): `committee/{mod,store,facade,tests}.rs` целиком; `outer.rs` — `EpochSchemeProvider` (`:230-280`), `impl CertProvider`, `boundary_tx`, `peers_for_finalization` (`:915`); `epoch_manager.rs` — `reconcile_roles`, `reconcile_live`, select-ветка `committee.subscribe()` (`:832`), `upgrade_scheme` перед `spawn_engine` (`:1362-1375`), `register_span` (`:1859`); `engine.rs` — снятая регистрация; `cert_inlet.rs` — `ingest`, `CommitteeSource` (`:114`), `RethCommitteeSource::build_at` (`:157`); `node/cert_inlet.rs` — снятый фатальный путь (`HEAD:node/cert_inlet.rs:120-123`); `dpos.rs` (consensus) — cold start валидатора (`HEAD:dpos.rs:1996-2011` `bail!` → дерево), валидаторский форвардер границ (`:2288`), follower inlet-источник (`HEAD:dpos.rs:3678-3730` удалён), `build_follower` (`:3270`); `node/dpos.rs` — `build_beacon_plane` (`:1355` ET, `:1493` коалесценция, `:1538` `on_finalized`, `:1787` бикон из фасада), tombstone-поллер (`:1612`); `reader/epoch_transition.rs` — `cold_start` и `anchor_height` (`:296-319`, `:495-530`), `is_epoch_boundary_frozen` (`:237`); `testbed/stand.rs` (`:1563` ET, `:1638-1650` `StandAnchor`), `testbed/fakes.rs`; `tests/slasher_integration.rs`.

## Форма результата `.dpos-study/history/E4-1-B2-REVIEW.md` (как `history/E4-1-B1-REVIEW.md` §0–§6)

**§0 Прямые ответы** (нумерованные, каждый с [KNOWN]/[ГИПОТЕЗА]):

1. Ворота — прогнать САМОМУ и вставить verbatim по одной строке: `cargo test -p fluentbase-consensus --lib` (журнал: 665/0; база Б1 668/0); `… --features dpos-devnet-byzantine testbed::` (40/0); `cargo test -p fluentbase-node --lib` (57/0); `cargo test -p fluentbase-staking-reader` (журнал 58/0, база 60/0); `cargo test -p fluentbase-consensus --test slasher_integration` (16/0); `cargo clippy -p fluentbase-consensus -p fluentbase-node --all-targets` и с фичей (база: только чужая `large_enum_variant` в node); `cargo fmt --check`; `cargo doc -p fluentbase-consensus --no-deps 2>&1 | grep -c "unresolved link"` (9). Расхождения с журналом — назвать.
2. **Одна карта.** Есть ли в продакшн-коде `crates/**` вторая таблица «эпоха → схема» помимо `EpochEntry.scheme` (grep `Scheme>` в `BTreeMap`/`HashMap`/`Vec<(…Epoch…, …Scheme)>`, `CachedScheme`, `schemes`); сколько `impl CertProvider`; делегирует ли `EpochSchemeProvider` КАЖДЫЙ метод в модуль без собственного состояния (открой тип целиком). Ретенция: удаляется ли схема вместе с записью (пол окна), и нет ли пути, где marshal спрашивает эпоху ниже пола и получал раньше схему, а теперь `None` (сравни с `HEAD:outer.rs` ретенцией `EpochSchemeProvider` — `SCHEME_RETENTION_EPOCHS` от чего отсчитывалась там и от чего теперь).
3. **Продюсеры.** Все места, где слот `scheme` пишется (grep `.scheme =`/`scheme: Some`/`upgrade_scheme`/`resolve_scheme` в `committee/`): `install`, `resolve_scheme`, `upgrade_scheme` — и никто больше? Проследи гонки по коду: (а) `install` положил `None` (бикон ещё не доступен), `upgrade_scheme` положил signer-схему, потом отложенный `resolve_scheme` — может ли он ПЕРЕЗАПИСАТЬ signer-схему verifier-ной? (б) два потока одновременно в `resolve_scheme` — два verifier'а, один теряется — безвредно ли? (в) `upgrade_scheme` — четыре отказа (downgrade signer→verifier, чужой комитет, потеря оракула, …) — открой и сравни с `HEAD:outer.rs` `register` по одному: не потерян ли отказ. Кто зовёт `upgrade_scheme` — только `epoch_manager.rs:1362-1375` ДО `spawn_engine`? Есть ли путь, где движок эпохи E запущен, а в карте у E ещё verifier-схема (marshal примет сертификат, но собственный голос…)?
4. **Слабая ссылка на бикон (Д-25).** `BeaconSlot = Arc<OnceLock<Weak<dyn Beacon>>>`: кто и когда его заполняет на валидаторе и на follower'е (grep `beacon_slot`/`set(`); заполняется ли ровно один раз; что происходит, если бикон пересобирается (follower → signer переключение в процессе, `build_follower` повторно, стенд) — `OnceLock` уже занят старым `Weak`, `upgrade()` даёт `None` ⇒ verifier без оракула ⇒ vote-only схема на всю ретенцию? Открой код переключения ролей в `dpos.rs`/`epoch_manager.rs` и скажи, пересоздаётся ли `dyn Beacon` за время жизни процесса. Что делает verifier при `upgrade() == None`: `None` (повтор позже) или схема без оракула (запин)?
5. **Отложенный повтор (Д-26).** `Committee::scheme` при пустом слоте зовёт продюсер — сколько это стоит на пути marshal'а (`CertProvider::scoped`) на промахе: только BLS-агрегация из записи, или EVM-чтение (`committee()` при отсутствии записи)? Может ли marshal-актор заблокироваться на двух staticcall'ах (журнал §7.4) — открой `scoped` и `Committee::scheme` и скажи, в каком потоке это выполняется и что делал HEAD (поиск в карте).
6. **Пробуждение на каждом `anchor_advanced` (Д-28).** Открой `store.rs:630-645`: `watch::Sender` — `send_replace`/`send`/`send_if_modified`? Уведомляет ли `watch` при равном значении (проверь по семантике `tokio::sync::watch`: `send_replace` помечает изменение всегда; `send_if_modified` — по возврату замыкания — открой, что именно вызвано). Кто подписан (`epoch_manager.rs:832`, кто ещё) и что делает на каждое пробуждение при ~1 событии/с — есть ли там EVM-чтение или только лок стора. Класс парковки «якорь есть, состояние не исполнено» — правда ли, что до Д-28 он не имел повтора: проследи `executed_hash(anchor) == Ok(None)` → кто повторял.
7. **Громкий отказ старта → `warn!` (Д-27).** Открой `HEAD:dpos.rs:1996-2011` и дерево: что именно `bail!` защищал (свежий datadir без комитета? неверный `staking_address`? цепь без активации?) и какой из этих случаев теперь виден оператору ТОЛЬКО как `warn!` + узел-верификатор. Существует ли ребро повтора для этого пути (кто перечитает `committee(epoch(anchor))` после `NotReadable` — `subscribe`?), или узел после `warn!` реально висит до рестарта. То же для follower'а и стенда.
8. **Inlet без фатала (Д-34).** `HEAD:node/cert_inlet.rs:120-123` ронял узел на `Err` от `scheme_at_finalized_tip`; теперь `Committee::scheme` → `Option`. На `None` для эпохи с ПОСТОЯННОЙ ошибкой (`Read(permanent)`, `weights: None`, форк контракта) — что делает `CertInlet::ingest`: откладывает сертификат и повторяет бесконечно? теряет? Блокирует ли это остальную очередь (cert-follow follower'а стоит навсегда без единого `error!` со стороны inlet'а)? Где виден постоянный класс — `store.rs:192-207` `error!` один раз на эпоху + метрика — открой и подтверди, что печатается ровно один раз и что метрика инкрементируется на этом пути, а не только на пути слэшера. Вердикт: регрессия наблюдаемости (SERIOUS?) или приемлемая замена — обоснуй.
9. **Б2.3 не сделан (§7.1 журнала)** — проверь ОБЕ причины по коду, не по комментариям: (а) `node/dpos.rs:1493` `borrow_and_update()` коалесцирует; `reader/epoch_transition.rs:237`, `:495` — детекция границы требует ровно граничную высоту ⇒ на догоне плоскостной ET пропускает границу, signer-плоскостной (`dpos.rs:2293-2296`, по каждому блоку) — нет. Подтверди или опровергни; если подтверждается — это ДЕФЕКТ HEAD (плоскостной ET и сегодня пропускает границы на догоне?) или он компенсируется чем-то (`pending_boundary`, `track_and_trigger`)? (б) `cold_start` присваивает `anchor_height` безусловно (`reader/epoch_transition.rs:495-530`) и плоскостной инстанс cold-start'ится на каждом тике до заморозки — по коду `node/dpos.rs` так ли это. Оцени: разумно ли отложить Б2.3 в отдельный заход с правкой `reader/epoch_transition.rs`, и что минимально нужно (журнал предлагает (а)–(г)).
10. **Канал границ по `Epoch` (Д-33) и снимок из записи.** Валидаторский форвардер (`dpos.rs:2288`) отбрасывает снимок ET; `epoch_manager` перечитывает запись через модуль; `CommitteeRecord::snapshot_view()` — какие поля синтезирует (`tombstoned = false`? `activation_epoch`?) и кто их читает ниже (`reconcile_roles` → `signer`/`WeightedVrf`/`FluentApp::committee_index`) — повтори проверку Д-18 из ревью Б1 для НОВОГО пути. `latest_live: Option<Epoch>` — все читатели переведены; нет ли места, где раньше сравнивали снимки, а теперь — только номера эпох (потеря проверки «тот же состав»?).
11. **`register_span` останавливается на первой нечитаемой эпохе (Д-32).** HEAD `pipeline_catchup_span`/bulk-читатель — тоже останавливался? На `Read(transient)` в середине диапазона — кто повторит остаток; на `OutOfWindow` сверху — ожидаемо. Есть ли регрессия «эпохи за дырой не регистрируются никогда».
12. **`WeightedVrf` elector поднят до `upgrade_scheme` (Д-30)** — поведение то же, что на HEAD (открой `epoch_manager.rs` HEAD и дерево вокруг `spawn_engine`)? Что происходит при отказе `try_new` — раньше и теперь.
13. **`latest_scheme` может назвать будущую эпоху (§7.4 журнала)** — открой `outer.rs:915` `peers_for_finalization` и `store.rs` `latest_scheme`: какая эпоха выбирается (максимальная в карте?), может ли туда попасть `epoch(anchor)+2`, чей комитет уже закоммичен, но ещё не живой — и что это меняет для fetch'а финализаций (цели-пиры не в peer-set?).
14. **Тесты**: счёт `#[test]` HEAD vs дерево по каждому из 20 файлов (журнал: `committee/tests.rs` 22→24, `cert_inlet.rs` 28→25, `outer.rs` 5→3, `reader/epoch_transition.rs` 31→29, остальные без изменений); удалённые семь поимённо — оправдано ли (предмет исчез); особенно `corrupt_committee_read_stays_fatal` и `committee_read_fault_defers_transient_and_blocknotfound_else_corruption` — что они доказывали и есть ли это свойство теперь где-то (назови тест или скажи «нет»); два новых — что утверждают, мутация (журнал §4 — повтори одну мутацию сам); изменённый `the_wake_up_carries_the_highest_readable_epoch_as_the_anchor_grows` — усиление или ослабление. `git diff HEAD -- testbed/tests.rs` пуст?
15. **Продакшн-путь**: `unwrap`/`expect`/`#[allow]`/`todo!`/таймеры/`dbg!` — новые, с якорями (журнал: только `state.lock().unwrap()` ×5 в `store.rs`).
16. **Файлы вне списка постановки (Д-31)**: `executor.rs`, `cold_start_jump.rs`, `fault.rs`, `beacon/surface.rs` — только тесты/доки? Перечисли ханки.
17. **Где журнал вводит в заблуждение** — утверждения журнала, не подтверждённые деревом (с якорем).
18. **Что из §5.1/§5.5 неверно против кода** — журнал называет четыре места (§0.7): подтверди или опровергни каждое по коду; есть ли пятое.
19. **Граница**: что из `pub` в `committee/`, `outer.rs`, `cert_inlet.rs`, `reader/epoch_transition.rs`, `lib.rs` появилось/пропало (таблица имён); `SchemeCommittee` в `committee::testing` — `#[cfg(test)]` или `pub` в релизе?

**§1 Находки** — таблица `id | серьёзность | file:lines (дерево) | HEAD-якорь | что не так | чем пытался опровергнуть и почему не вышло | уверенность`. Серьёзность BLOCKER — только для регрессии поведения на пути голосования/финализации/границы/доставки сертификатов, которую проект не предписывал. Id — `B2-nn`.
**§2 Поведение по ханкам против HEAD** — по каждому тронутому файлу.
**§3 Граница** — таблица имён `pub`.
**§4 Ворота** — вывод команд verbatim.
**§5 Оставить как есть** — непустой.
**§6 Замечено вне рамок Б2** — для Б2.3/Б3 (один ET; стенд `FakeStaking` по ветке хэша, кольцо, тумбстоуны, модель тега reth в `StandAnchor`), 4.2 (`CommitteeSource::scheme_at`, `LiveFrontierTee.live_height`), 4.3, Э7 (голова очереди слэшера B1-03).

В финальном ответе — только §0 (кратко) и таблица §1.

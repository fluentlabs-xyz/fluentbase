# Независимая оценка закрытия этапа Э4 (строки 4.1, 4.2, 4.3)

Ты — оценщик (Opus 5) в свежем контексте; ты не видел ни одной постановки исполнителей и ни одного ревью этого этапа до этого момента. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `fca897c1`. Объект оценки — **весь `git log 1b11b61f..HEAD`** (71+ коммитов; из них ~27 — параллельная работа владельца по контракту `contracts/staking/**`, `crates/staking-abi/**`, `.dpos-study/DECISIONS.md`, `E1-*.md`, `D4-BRIEF.md` — их НЕ оценивать, только отделить) и рабочее дерево на HEAD.

## Правила

1. **Агентов не запускать.** 2. **Git только на чтение** (`git log/show/diff`). 3. **Единственный файл на запись** — `.dpos-study/history/E4-REVIEW.md`. Мутации продакшн-кода ради проверки чувствительности — только с откатом байт-в-байт и `md5sum` до/после (записать). 4. Каждое утверждение — `file:line` на HEAD (`git show HEAD:path | sed -n`) и, где есть «до», `1b11b61f:file:line`. Комментарии, доки `.claude/dpos_architecture/`, журналы и ревью — не свидетельство; свидетельство — код и прогон, который ты сделал сам. Журналы/ревью читать МОЖНО и НУЖНО, но ПОСЛЕ собственного чтения кода — чтобы найти, где оркестратор поверил на слово. 5. Читать целыми строками, никогда `cut -c`/`head -c`. 6. Не читать файлы вне git-трекинга кроме `.claude/` и `.dpos-study/`; никогда `.env*`, `*.pem`, `*.key`, `*secret*`, `*credential*`. 7. `CARGO_BUILD_JOBS=4` (параллельно идёт живой docker-прогон харнесса — не запускай `devnet/**` ничего). 8. `.claude/COMMONWARE_INTERNALS.md` — до чекаута commonware.

## Вход

- Проект: `.dpos-study/history/E4-CORE-DESIGN.md` — §0 (закрывающие ответы и «св. 1–6» — найди определение свойств: grep `св\. ` по `E4-CORE-DESIGN.md`, `history/D4-BRIEF.md`, `DECISIONS.md`), §3 инварианты I1–I5, §5 целевая архитектура (5.1 модуль комитета, 5.2 фронтир, 5.3 граница p2p, 5.4 модель отказов). `DECISIONS.md` П-1, П-4, П-5, Д-1, Д-2. `PLAN.md` §2 строки 4.1–4.3 (`:102-104`) и §1 ворота.
- Состояние оркестратора: `.dpos-study/history/E4-ORCHESTRATOR.md` — разделы «Агенты», «Квитанции», «Коммиты», «Отклонения от проекта». Журналы `history/E4-1-{A,B1,B2,B2p,B3}.md`, `E4-2-{A,B1,B2,V}.md`, `E4-3-A.md`; ревью `*-REVIEW.md`; `E4-PRECONDITIONS.md`; `REGISTER.md` статусы «2026-09-11/12».
- Ворота оркестратора (verbatim-файлы): `/tmp/claude-1000/-home-djadjka-Work-fluentbase/497904b9-e3ec-49b9-adb4-1fca928058a1/scratchpad/gates/a3v-*.txt` (финальное дерево 4.3), `v1v-*`, `b2v-*`, `b1f-*` и т.д.

## Что ответить (форма — `history/E3-REVIEW.md`: §0 состояние дерева и свои прогоны, §1… по существу, §9 три самых опасных пункта, «Прямые ответы» в конце)

1. **Держатся ли свойства 1–6 проекта §0 и инварианты I1–I5 §3 ПОСЛЕ кода** — по каждому: да/частично/нет, с file:line на HEAD и с местом, где ломается. Для I2 («фронтир только по проверенной финализации») — пройди ВСЕ вызовы `sync_to`/`sync_to_checkpoint` на HEAD и все пути к `jump_to_target`; для I3 («членство до ресурсов») — все входы каналов (BEACON, EVIDENCE, VOTE/CERT/RESOLVER/BROADCAST/MARSHAL backup, FRONTIER) и все `committee(`/`committee_for` вызовы, достижимые кадром с провода; для I1 — все носители комитета (grep `epoch_committee_snapshot`, `committee_for`, `Committee::committee`) — остался ли второй источник; для I5 — что может сделать один tracked-пир и один член.
2. **Три самых опасных места** после этапа — с механизмом, file:line и что бы ты сделал.
3. **Где оркестратор принял чужое утверждение без квитанции** — сверь раздел «Квитанции» state-файла с решениями/правками, которые он на них основал (таблицы решений в `E4-prompts/*-fix-*.md`, статусы `REGISTER.md`, строки `EXPERIMENTS.md`): назови каждое место, где действие есть, а квитанции (file:line/прогон оркестратора) нет или она проверяет не то. Отдельно: утверждения, помеченные [KNOWN] в state-файле/REGISTER/EXPERIMENTS, которые ты опроверг или не смог подтвердить.
4. **Стенд и ворота**: воспроизведи ≤ 3 мутации на самых несущих ассертах этапа (по одной на строку: 4.1 — модуль/окно; 4.2 — `deliver` или гейт живости; 4.3 — `Ingress`/tier) и запиши verbatim; сверь числа ворот из `PLAN.md` §1 с файлами `a3v-*`.
5. **Дрейф**: символы, удалённые этапом (`upstream_frontier`, `corroborate_frontier`, `highest_observed_epoch`, `pipeline_catchup_span`, `cold_start_jump_self_heal`, `verify_jump_authenticated` как стадия, `forward_vote_backup`, `JumpOutcome::{BadTarget,AuthFailed}`, `SyncReason::{AwaitingUpstream,AuthRotate}`, `qual_read_at`, `register_soft_entered`, `cold_start_register`, `soft_enter_span`, `latest_live`) — где в `.dpos-study/{PLAN,REGISTER,EXPERIMENTS}.md`, `E4-CORE-DESIGN.md` и `.claude/dpos_architecture/` они всё ещё описаны как живые без пометки REVISED/HISTORICAL.
6. **Что всплыло для Э5/Э6/Э7** — список с якорями.

«Прямые ответы» в конце — по одному на строку: (1) свойства 1–6 держатся? (2) I1–I5? (3) три опасных места; (4) сколько мест без квитанции и какие; (5) сколько [KNOWN] опровергнуто; (6) мутации verbatim; (7) числа ворот сходятся с `a3v`? (8) вердикт «этап закрыт как регрессия для П-1/П-4/П-5» — да/нет/с оговорками.

В финальном ответе — §9 и «Прямые ответы» целиком; остальное — в файле.

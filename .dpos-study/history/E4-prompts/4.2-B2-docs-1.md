# Ф7 строки 4.2, заход Б2 — правка `.claude/dpos_architecture/` под код дерева

Ты — док-исполнитель (Opus 5). Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `f8ec4939`; **код — незакоммиченное дерево Б2 после третьего прохода** (14+ файлов под `crates/`, `git status --short | grep -v devnet/`) — это истина; доки надо привести к нему. **Чужое** — `devnet/**`, `.dpos-study/history/E4-ORCHESTRATOR.md` — не трогать.

## Правила (обязательные)

1. **Агентов не запускать.** 2. **Git только на чтение.** 3. **Файлы на запись — ТОЛЬКО** `.claude/dpos_architecture/*.md`. Код — read-only. `.dpos-study/**` — read-only. 4. **Свидетельство — только код дерева** (`sed -n`, Read целыми строками; никогда `cut -c`/`head -c`); комментарии в коде — не свидетельство; каждая правка доки опирается на file:line, которую ты открыл. 5. Не читать файлы вне git-трекинга кроме `.claude/` и `.dpos-study/`; никогда `.env*`, `*.pem`, `*.key`, `*secret*`, `*credential*`. 6. **Форма правок — как в предыдущих заходах Э4** (см. `09_followers.md` §9.6 и `04_cold_start_restart_crates_dpos_consensus.md` §Э3.3 — абзацы `**[REVISED 2026-09-12, 4.2-Б1]**`): устаревший абзац НЕ удалять, а (а) переписать по факту, если он короткий, или (б) поставить перед ним `**[REVISED 2026-09-12, Э4 row 4.2-Б2]**`-абзац с новым состоянием и пометить старый как HISTORICAL/pre-Б2 record. Ничего не выдумывать: где код молчит — дока молчит. 7. Никаких `cargo`-прогонов не нужно.

## Что прочитать

- `.dpos-study/history/E4-2-B2.md` целиком (§0–§5, часть В) и `E4-2-B2-REVIEW.md` §0–§1 — что изменилось и почему; `E4-prompts/4.2-B2-impl-1.md`, `4.2-B2-fix-1.md` — постановки. Это КАРТА изменений; факты сверяй с кодом.
- Код: `dpos.rs` — `ColdStartKind` (grep `enum ColdStartKind`), `resolve_cold_start_kind`, арм `ElFinalized` в `launch`, арм #12 (`DeferToElSync`), `FreshFollowerEntry`/`fresh_follower_entry`, follower-армы `launch_follower` (grep `fresh_follower_entry(`), `refetch_verified_archive_hole`; `cold_start_jump.rs` — доки модуля, `JumpOutcome`, `ElSync` (`sync_to_checkpoint`), `verify_jump_structural`/`verify_jump_authenticated`, `jump_to_target`, `assert_l1_checkpoint`; `executor.rs` — посев `last_tip_height`/`ordering_finalized`, `maybe_re_jump`, drain (`finalized_heights_to_backfill`, арм `pending_backfill`), армы `jump_done`; `plane_upstream.rs` — доки модуля, `deliver` шаг (5) отброс, `FRONTIER_DROPPED`; `sync_metrics.rs` — `SyncReason` (какие reason остались); `cert_follow.rs` — `fetch_verified_boundary`; `node/dpos.rs` — `is_deployed_network`, `ValidatorUpstream`; `node/cert_follow/mod.rs` — проводка `deployed_network`; `testbed/fakes.rs` — `JumpElSync::sync_to_checkpoint`; `testbed/tests.rs` — переименованные тесты (`the_rejump_runs_the_production_jump_and_lands_on_its_own_archive_pair`, лестница с пином «пир без данных»).

## Инвентарь дрейфа (grep оркестратора по `.claude/dpos_architecture/`, счётчики по файлам; `00_preamble.md` verified-against — история, НЕ править кроме новой записи)

`cold_start_jump\b` — 04:13, 09:23, 13:9, 12:2, 02:2, 00a:2, 01/08/14/15 по 1; `cold_start_jump_self_heal` — 04:6; `classify_jump_outcome` — 04:4, 09:2, 13:1; `JumpDisposition` — 04:1; `AuthFailed` — 04:6, 09:7, 13:3, 15:3; `BadTarget` — 04:8, 09:7, 13:3; `awaiting_upstream` — 04:1, 08:3; `auth_rotate` — 04:2, 08:2, 09:1, 13:1; `verify_jump_authenticated` — 04:3, 09:6, 15:4; `verify_jump_structural` — 04:3, 09:1, 15:3; `unauthenticated_total` — 09:1, 13:1; `crash_recover` — 04:7, 08:4; `FreshMigration` — 04:2, 02:1; `jump_committee_reads`/`JumpCommittees` — 04:1, 15:3.

## Что сделать

1. **`04_cold_start_restart_crates_dpos_consensus.md`** — раздел холодного старта: (а) виды `ColdStartKind` — добавить `ElFinalized` (условие, якорь = свой EL-тег, кто пишет тег по коду reth — ссылка на `.claude/RETH_INTERNALS.md` строки, которые журнал §0(1) цитирует, проверь, что они там есть), FreshMigration без изменений, Restart; (б) пред-движковый прыжок, `cold_start_jump_self_heal`, `classify_jump_outcome`, `JumpDisposition`, retry-цикл `empty_archive_requires_landed_jump`, `awaiting_upstream`/`auth_rotate` — УДАЛЕНЫ: REVISED-абзацы + HISTORICAL; (в) #12 crash-survivor: закрывает стартовый backfill-drain executor'а, не прыжок (Д-99; почему прыжок структурно не может — посев одним значением и гейт drain'а; гейдж `crash_recover` снимается на последней высоте drain'а; fail-fast при reth-tip ниже пола marshal'а — B2-02, найди в коде после третьего прохода, что именно сделано); (г) L1: `Err ⇒ Stalled`, `L1Fork` остался; (д) `JumpOutcome` без `BadTarget`/`AuthFailed`; стадии `verify_jump_*` сняты из `jump_to_target`, функции остались для `refetch_verified_archive_hole`/`fetch_verified_boundary` (+ `verify_jump_structural` — шаг (2) `deliver`), `verify_jump_authenticated` без `l1_checkpoint`; (е) §Э3.3-заметка про `jump_committee_reads` — тип удалён.
2. **`09_followers.md`** — §9.6 (исходы прыжка: армы `BadTarget`/`AuthFailed` удалены; `cold_start_jump` → `jump_to_target`), cold-start follower'а: три арма (datadir `rf < activation` / `rf ≥ activation` без `sync_to`; fresh — `fresh_follower_entry`: checkpoint ⇒ `sync_to_checkpoint(hash)`, deployed без checkpoint ⇒ отказ (текст E4-05), не-deployed ⇒ devnet `get_latest ⇒ sync_to` с warn), второй прыжок follower'а удалён, `assert_l1_checkpoint` остался; §9.6.1 `dpos_frontier_unauthenticated_total` → `dpos_frontier_dropped_total`, отброс вне окна на обоих армах (A2-04 закрыт) и retry-драйвер = проба; §9.4/§9.2 упоминания `cold_start_jump`.
3. **`13_invariants_gotchas_rules.md`** — пункты с `cold_start_jump`/`BadTarget`/`AuthFailed`/`classify_jump_outcome`/`unauthenticated_total`/`auth_rotate` — REVISED; новый инвариант, если код его несёт: «`sync_to` вызывается только с (а) парой из собственного архива, (б) операторским checkpoint, (в) devnet-`Latest` за предикатом deployed-сети» — сослаться на три вызова (grep `.sync_to(`/`sync_to_checkpoint(`).
4. **`08_node_integration_crates_node_bins_fluent.md`** — таблица/список `dpos_sync_degraded{reason}`: `awaiting_upstream`, `auth_rotate` удалены (проверь `sync_metrics.rs` `SyncReason::ALL`); `crash_recover` — новый механизм снятия; `is_deployed_network` — второй потребитель (follower fresh-datadir).
5. **`15_smoke_cases_as_behavioral_spec_devnet_lo.md`** — строки тестов: `the_rejump_runs_the_production_jump_and_authenticates_at_the_landing` → `…_lands_on_its_own_archive_pair` (что пинует теперь — по коду теста), R-001 var Б ветка `AuthFailed` снята, лестница + пин «пир без данных» (Д-106), `jump_committee_reads` удалён; заметки RE-REVISED про `verify_jump_authenticated`.
6. **`12`, `02`, `01`, `00a`, `14`** — точечные упоминания `cold_start_jump`/`FreshMigration`: одна фраза REVISED там, где утверждение стало неверным; где это только имя файла `cold_start_jump.rs` — не трогать.
7. **`00_preamble.md`** — новая запись verified-against `2026-09-12 **Э4 row 4.2-Б2: …**` по форме записи 4.2-Б1 (что изменилось, Д-99…Д-1nn, ворота — возьми числа из `E4-2-B2.md` В§0(2) и из `gates`-квитанций оркестратора в постановке ниже, если даны; иначе — из журнала с пометкой «по журналу»; sections touched).
8. **Критерий приёмки (оркестратор проверит grep'ом)**: для каждого символа инвентаря `grep -n` по `.claude/dpos_architecture/*.md` даёт только (а) строки внутри абзацев с пометкой HISTORICAL/pre-Б2 при REVISED-абзаце рядом, (б) `00_preamble.md` verified-against, (в) имя файла `cold_start_jump.rs`. Всё прочее — дрейф, не закрыт.

## Отчёт (в финальном ответе)

Таблица: файл | секции/строки правок | что стало. Затем: список мест инвентаря, которые ты СОЗНАТЕЛЬНО оставил (с причиной), и список утверждений, которые не смог сверить с кодом (написал «по журналу»). Без журнала в `.dpos-study` — отчёт только в ответе.

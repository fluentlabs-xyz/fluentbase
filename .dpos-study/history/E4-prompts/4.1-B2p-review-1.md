# Ревью строки 4.1, заход Б2′ (один `EpochTransition` на валидаторе, драйвер границ — доставка блоков)

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база — `HEAD` `3a4a0fde`. Объект — **незакоммиченное рабочее дерево** захода Б2′: ровно четыре файла — `crates/dpos/staking-reader/src/epoch_transition.rs`, `crates/dpos/consensus/src/dpos.rs`, `crates/dpos/consensus/src/lib.rs`, `crates/node/src/dpos.rs`. **Всё остальное в `git status` — чужое** (`contracts/staking/**`, `crates/staking-abi/src/lib.rs` — параллельная сессия по контракту; `devnet/**`; `target-contract/`; `.dpos-study/history/E4-ORCHESTRATOR.md`) — не читать как часть захода, не трогать. Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`; `reader/` = `crates/dpos/staking-reader/src/`.

## Правила

1. **Агентов не запускать.** 2. **Git только на чтение.** Никаких правок, кроме одного файла: результат — `.dpos-study/history/E4-1-B2p-REVIEW.md`. 3. Каждое утверждение — `file:line` дерева и, где есть «до», `HEAD:file:line`. 4. Комментарии, доки, `.claude/dpos_architecture/` и журнал исполнителя — не свидетельство; свидетельство — код и прогон, который ты сделал сам. 5. **Полнота, не фильтр**: все находки с серьёзностью (BLOCKER / SERIOUS / MODERATE / MINOR / NIT) и «чем пытался опровергнуть». 6. Читать целыми строками. 7. Cargo: `CARGO_BUILD_JOBS=6`; `Blocking waiting for file lock` — ждать. 8. `.claude/COMMONWARE_INTERNALS.md` читать ДО checkout'а commonware (`~/.cargo/git/checkouts/monorepo-*`), если понадобится marshal.

## Вход

- Спека: `.dpos-study/history/E4-CORE-DESIGN.md` §5.1 абзац «Один `EpochTransition`» (`:502-505`), E4-20 (`:410`), §10 B27 (`:798`); постановка захода `.dpos-study/history/E4-prompts/4.1-B2p-impl-1.md`.
- Что было ДО: `history/E4-1-B2.md` §7.1 (две причины, план (а)–(г)), `E4-1-B2-REVIEW.md` §0.9, §6 (пункт (д)).
- Журнал исполнителя: `.dpos-study/history/E4-1-B2p.md` — читать ПОСЛЕ диффа.
- Дифф: `git diff HEAD -- crates/dpos/staking-reader/src/epoch_transition.rs crates/dpos/consensus/src/dpos.rs crates/dpos/consensus/src/lib.rs crates/node/src/dpos.rs`.
- Контекст: `reader/epoch_transition.rs` целиком (особенно `raise_anchor_height` `:295-322`, `on_finalized` `:340+`, парковка `:400-424`, `track_and_trigger` `:615-682`, `cold_start` `:689-705`, новые тесты `:2290`, `:2329`); `node/dpos.rs` — `BeaconPlane<Provider, EvmConfig>` (`:941`, `:1135`, `:1887`), `build_beacon_plane` (ET `:1377` с `Some(bridge_tx)`, поллер `:1567+`: что осталось — cold start до заморозки, тумбстоуны `:1638`), `launch_dpos_layer` (`:1950`, передача ET и `bridge_rx` вниз); `dpos.rs` — `DposLayer::launch` (`:1554`, пятый параметр), `cold_start` слоя (`:2096`), `enter_boundary` (`:2205`, `:2252`), `boundary_hook` (`:2341`), `raise_anchor_height`/`boundary_read_floor`, форвардер `bridge_rx` → `outer.boundary_sender()`; `application.rs:1022-1049` (хук на каждом `Update::Block`, `Exact`-ack); `executor.rs:2635-2643` (`boundary_read_floor` + `boundary_enter(terminal)` на приземлении); `node/dpos.rs:489-507`, `node/cert_follow/mod.rs:252` (follower без ET); `testbed/stand.rs:1555-1600`, `:2100-2107`, `:2150-2190`.

## Форма результата `.dpos-study/history/E4-1-B2p-REVIEW.md` (§0–§6 как `E4-1-B2-REVIEW.md`)

**§0 Прямые ответы** (нумерованные, каждый с [KNOWN]/[ГИПОТЕЗА]):

1. **Ворота** — прогнать САМОМУ, verbatim по одной строке: `cargo test -p fluentbase-consensus --lib` (668/0); `… --features dpos-devnet-byzantine testbed::` (40/0); `cargo test -p fluentbase-node --lib` (57/0); `cargo test -p fluentbase-staking-reader` (журнал 60/0); `cargo test -p fluentbase-consensus --test slasher_integration` (16/0); `cargo clippy -p fluentbase-consensus -p fluentbase-node --all-targets` и с фичей (одна чужая `large_enum_variant`); `cargo fmt --check` — только по четырём файлам захода (`rustfmt --check <файлы>` или отфильтровать вывод: чужие диффы в `contracts/` не считать); `cargo doc -p fluentbase-consensus --no-deps 2>&1 | grep -c "unresolved link"` (8). Внимание: в дереве чужие незакоммиченные правки `crates/staking-abi/src/lib.rs` — если сборка падает ТАМ, это не заход; назови.
2. **Один инстанс**: `git grep -n "EpochTransition::new"` по продакшн-коду — ровно один (`node/dpos.rs`)? Тот же `Arc<Mutex<…>>` доходит до `enter_boundary`/`boundary_hook`/`raise_anchor_height` в `dpos.rs` (проследи клоны, не по журналу)? `bridge_tx` у него `Some`; `bridge_rx` едет вниз и форвардер — один?
3. **Драйвер границ и шкала.** По коду: хук доставки (`application.rs:1022-1028` → `dpos.rs:2341` → `enter_boundary` → `on_finalized(number)`) — ordering-высоты, по одной, без пропусков? Что делает marshal при разрыве вне прыжка — `Update::Block` всегда контигуозен (`.claude/COMMONWARE_INTERNALS.md` marshal, при необходимости checkout `marshal/core/actor.rs`)? Шов приземления прыжка (`executor.rs:2635-2643`) — единственный источник высот, которые хук пропускает? Поллер плоскости больше НЕ зовёт `on_finalized` — подтверди по `node/dpos.rs`; что он ещё делает (`cold_start` до заморозки; тумбстоуны по `epoch_at(fin)` в EL-шкале — журнал §8.4: в окне K вокруг границы поллер спрашивает ПРЕДЫДУЩУЮ эпоху — это регресс или поведение HEAD?).
4. **Два `cold_start` на одном инстансе** (поллер плоскости на EL-высоте, слой на `latest_finalized` после прыжка, Д-42): идемпотентность по полу (`max`), по эпохе (`apply_at` write-once), по мосту (стартовая эпоха в `bridge_tx` — дважды? кто дедуплицирует у `epoch_manager`?). Гонка «поллер заморозил геометрию и трекнул эпоху ДО `DposLayer::launch`» ⇒ `cold_start` слоя уходит в граничную ветку (`:584`, парковка без `debug_assert`) — журнал §7/§8.2 называет третьего парковщика; проследи по коду `apply_at`/парковку и скажи, может ли стартовая парковка столкнуться с парковкой хука иначе, чем пережив эпоху, и что тогда (`debug_assert` `:419-424` в релизе молчит — какой наблюдаемый исход?).
5. **`raise_anchor_height` ∧ `cold_start` через `max`** (`reader/epoch_transition.rs:700`): нет ли пути, где пол ОБЯЗАН опуститься (перезапуск с более низким finalized? follower-переключение? стенд-`restart_replays`)? Тест `cold_start_after_a_raise_does_not_lower_the_floor` — что именно пинует; тест `a_coalesced_driver_skips_the_boundary_a_stepping_one_enters` — мутацию повтори САМ (какую — журнал §4.2) и приведи вывод.
6. **`track`** едет только через `track_and_trigger` (cold start + граничная ветка) — подтверди; после снятия поллерного `on_finalized` не потерян ли ни один `track` (§1в журнала: граничное ребро хука — надмножество того, что видел поллер; проверь, включая «поллер видел границу ДО того, как хук существует» на старте).
7. **Follower** (`--cert-follow`): ET нет — подтверди (`launch_follower_overlay` не зовёт `build_beacon_plane`); его `track`/peer-set — не изменились.
8. **Граница типов**: `BeaconPlane<Provider, EvmConfig>` (Д-43), `DposLayer::launch` пятый параметр (Д-41), `PlaneEpochTransition` реэкспорт — оправдано или трейт на три глагола был бы честнее; `#[allow(clippy::too_many_arguments)]` — уже стоял (`dpos.rs:1554`, `node/dpos.rs:1950`)? Новых `#[allow]`/`unwrap`/таймеров нет?
9. **Стенд**: ET один на узел, драйвер = хук по каждому блоку (`stand.rs:2150-2190`) — тот же, что у продакшна после правки? `tracked_mismatches` по-прежнему между узлами? Комментарий `stand.rs:2100-2107` «the OTHER production transition» — протух.
10. **Тесты**: счёт `#[test]` HEAD vs дерево по четырём файлам; новые — два в `epoch_transition.rs`; удалённые — нет?
11. **Где журнал вводит в заблуждение** и что из §5.1/E4-20/B27 неверно по коду (журнал §0.6 называет пять пунктов — подтверди/опровергни, добавь своё).

**§1 Находки** — `id | серьёзность | file:lines | HEAD-якорь | что не так | чем пытался опровергнуть | уверенность`; id `B2p-nn`. BLOCKER — только для регрессии на пути границы/входа в эпоху/track, не предписанной проектом. **§2 Поведение по ханкам против HEAD.** **§3 Граница** (`pub`). **§4 Ворота** verbatim. **§5 Оставить как есть** — непустой. **§6 Вне рамок** — для Б3, 4.2 (`is_live_epoch` по tip'у, `T = last_tracked_epoch` теперь один), 4.3 (`track`).

В финальном ответе — только §0 (кратко) и таблица §1.

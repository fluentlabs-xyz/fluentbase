# Ф7 строки 4.3, заход А — правка `.claude/dpos_architecture/` под код дерева

Ты — док-исполнитель (Opus 5). Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `86fe7098`; **код — незакоммиченное дерево 4.3-А после третьего прохода** (`git status --short | grep -v devnet/`, файлы под `crates/`) — это истина; доки надо привести к нему. **Чужое** — `devnet/**`, `.dpos-study/history/E4-ORCHESTRATOR.md` — не трогать.

## Правила (обязательные)

1. **Агентов не запускать.** 2. **Git только на чтение.** 3. **Файлы на запись — ТОЛЬКО** `.claude/dpos_architecture/*.md`. Код — read-only. `.dpos-study/**` — read-only. 4. **Свидетельство — только код дерева** (`sed -n`, Read целыми строками; никогда `cut -c`/`head -c`); комментарии в коде — не свидетельство; каждая правка доки опирается на file:line, которую ты открыл. 5. Не читать файлы вне git-трекинга кроме `.claude/` и `.dpos-study/`; никогда `.env*`, `*.pem`, `*.key`, `*secret*`, `*credential*`. 6. **Форма правок — как в заходах Э4** (см. `03_epoch_machinery.md` §3.2 и `04_…md` §4.1 — абзацы `**[REVISED 2026-09-12, Э4 row 4.2-В]**`): устаревший абзац НЕ удалять, а (а) переписать по факту, если он короткий, или (б) поставить перед ним `**[REVISED 2026-09-12, Э4 row 4.3-А]**`-абзац с новым состоянием и пометить старый как HISTORICAL/pre-4.3 record. Ничего не выдумывать: где код молчит — дока молчит. 7. Никаких `cargo`-прогонов (параллельно идут ворота оркестратора).

## Что прочитать

- `.dpos-study/history/E4-3-A.md` целиком (§0–§5 + часть «третий проход») и `E4-3-A-REVIEW.md` §0–§1; постановки `E4-prompts/4.3-A-impl-1.md`, `4.3-A-fix-1.md`. Это КАРТА; факты сверяй с кодом.
- Код: `crates/dpos/staking-reader/src/epoch_transition.rs` — `TrackedPeers` (grep `pub struct TrackedPeers`), `PeerSetSink::track`, `assemble_tracked_peers`, `push_neighbour_committee`; `crates/dpos/p2p/src/lib.rs` — адаптер `impl PeerSetSink for OracleHandle`, `TrackedWindow`, `Ingress`, `EpochMask`; `crates/dpos/consensus/src/dpos.rs` — `GatedReceiver`, `INGRESS_DROPPED_TOTAL`, `deque_size`; `crates/node/src/dpos.rs` — `ingress_window`, `GatedReceiver::new` (BEACON, EVIDENCE), список наблюдателей route-miss; `beacon/actor.rs` — `epoch_is_actionable`, `beacon_member`, `on_message`, `on_confirm`; `slasher/gossip.rs` — `ingest_batch`; `beacon/dkg_transport.rs` — `deque_size`; `testbed/stand.rs` — `TrackSink`, `Outcome::peer_sets`; `testbed/tests.rs` — новые тесты (grep `a_registry_only_node_is_secondary`, `a_node_outside_the_tracked_peer_set_stands_at_the_boundary`).

## Инвентарь дрейфа (grep оркестратора по `.claude/dpos_architecture/`; `00_preamble.md` verified-against — история, НЕ править кроме новой записи)

- `05_identity_crypto.md:152` — `PeerSetSink::track` с якорем `track_and_trigger:608-646` (устарел); `:148` `MAX_REGISTRY_PEER_SET` — bound теперь только на primary? (открой `check_peer_set_size`); `:100` «sits in nobody's tracked peer set». (`Ingress::Dns/Socket` `:167-180` — ЭТО commonware-тип адресов, НЕ наш `Ingress`; не трогать.)
- `06_staking_layer.md:502-509` — `track_peers`/`assemble_tracked_peers`/«TEMPORARY BRIDGE until row 4.3 moves `track` into a peer-set module» — 4.3 сделал НЕ модуль, а смену подписи на `TrackedPeers` в ET (Д-122: ET ниже модуля `committee/` в графе крейтов) — REVISED; `:526`.
- `07_slashing_end_to_end.md:211-222` — `ingest_batch` (теперь `from` + `TrackedWindow`, тир до декода, `member_of(epoch)` до `committee_for`); «Ingress provenance is a TYPE» (`slasher/ingress.rs::{Envelope, Provenance}` — это другой, существующий тип; сопоставь с новым `fluentbase_p2p::Ingress` и разведи имена в тексте).
- `08_node_integration_crates_node_bins_fluent.md:1679-1680` — «tracks `active_registry_peers ∪ committee[E] ∪ committee[E+1]`» → двухтировый `TrackedPeers`; `:1632` `OracleHandle`; `:2288-2289` («tracked peer set had to gain committee[E+1]» — теперь primary = три записи, реестр secondary; тела `buffered` только primary); `:78`, `:1075` (peer-set-only plane); новый подраздел про `GatedReceiver`/`ingress_window` (BEACON, EVIDENCE) и метрику `dpos_ingress_dropped_total{channel, reason}`; `deque_size` BROADCAST 4 (было 64), DKG bodies 2 (было `MAX_COMMITTEE_SIZE`), байтовый лимит — граница библиотеки (`buffered::Config` без байтового поля).
- `09_followers.md:377`, `:860` («tracked peer set == the committee») — REVISED-фраза.
- `03_epoch_machinery.md:42`, `:367` — точечно.
- `12_consensus_critical_constants.md` — строки `MAX_COMMITTEE_SIZE` (если там упомянут как `deque_size` DKG), новые константы: `deque_size` 4/2 — добавить строки таблицы с якорями.
- `13_invariants_gotchas_rules.md` — новые правила, если код их несёт: «primary = три записи комитетов, secondary = реестр; `check_peer_set_size` только primary», «`Ingress` без EVM: тир до декода (`GatedReceiver`), эпоха — из первых 8 байт DKG-кадра до декода тела, `Confirm` вне `[now, now+2]` — до `committee_for`», «backup-каналы — наблюдатели, потребителя нет (Д-124)»; gotcha Д-125 (симулятор тирует просмотр, не доставку) и Д-126 (`now+3` дропается). Нумерация — после последнего номера (сейчас 46), дублей нет.
- `15_…md:315`, `:328`, `:369-370` — строки стенд-тестов (переписанный 4b, новый `a_registry_only_node…`, `TrackSink` пишет `peer_sets`).
- `00_preamble.md` — новая запись verified-against `2026-09-12 **Э4 row 4.3-А: …**` по форме записи 4.2-В (что изменилось, Д-122…Д-1nn, ворота — плейсхолдер `<<a3v>>`, оркестратор впишет; по журналу lib 685/0; sections touched). Третий проход (часть «В» журнала, Д-127…Д-129): `Err` чтения соседней записи ⇒ `?` (граница ретраится контуром `PENDING_RETRY_BACKOFF`), `last_height: Option<u64>` в beacon-акторе (окно `Confirm` только после первого тика; `height_now()` пол 0 для дедлайнов), юниты `GatedReceiver`, 4b переписан над `upstream_served` (`SEVERED_AT = start(2)`), `tracked_mismatches` по тирам, clippy-алиасы, инвариант `EpochMask` ≤ 3 — отрази там, где дока это описывает.
- **Критерий приёмки**: для каждого символа инвентаря `grep -n` даёт только (а) строки внутри абзацев с маркером REVISED/HISTORICAL, (б) `00_preamble.md` verified-against, (в) живые символы, чьё описание приведено к коду.

## Отчёт (в финальном ответе)

Таблица: файл | секции/строки правок | что стало. Затем: список мест инвентаря, которые ты СОЗНАТЕЛЬНО оставил (с причиной), и список утверждений, которые не смог сверить с кодом (написал «по журналу»). Без журнала в `.dpos-study` — отчёт только в ответе.

# Ревью строки 4.2, заход Б1 (триггер прыжка по tip'у; цель прыжка из собственного архива; `upstream_frontier`/`live_height` удалены; `servable` снят)

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база — `HEAD` = `d2f215f1` (`git rev-parse HEAD`). Объект — **незакоммиченное рабочее дерево** захода Б1: `crates/dpos/consensus/src/{cert_inlet.rs, cold_start_jump.rs, dpos.rs, executor.rs, outer.rs, plane_upstream.rs, testbed/fakes.rs, testbed/preconditions.rs, testbed/stand.rs, testbed/tests.rs}`, `crates/node/src/dpos.rs`. **Всё остальное в `git status` — чужое** (`devnet/**`, `.dpos-study/history/E4-ORCHESTRATOR.md`) — не трогать. Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`.

## Правила

1. **Агентов не запускать.** 2. **Git только на чтение.** Единственный файл на запись — `.dpos-study/history/E4-2-B1-REVIEW.md`; мутации продакшн-кода ради чувствительности тестов — только с откатом и `md5sum` до/после. 3. Каждое утверждение — `file:line` дерева и, где есть «до», `HEAD:file:line`. 4. Комментарии, доки, `.claude/dpos_architecture/`, журнал исполнителя — не свидетельство; свидетельство — код и прогон, который ты сделал сам. 5. **Полнота, не фильтр**: все находки с серьёзностью (BLOCKER / SERIOUS / MODERATE / MINOR / NIT) и «чем пытался опровергнуть». 6. Читать целыми строками. 7. `CARGO_BUILD_JOBS=6`; `Blocking waiting for file lock` — ждать. 8. `.claude/COMMONWARE_INTERNALS.md` — ДО checkout'а commonware (marshal: `Update::Tip` из `store_finalization`, `Mailbox` `get_finalization`/`get_block` — как отвечает актор и что при этом делает его цикл; `HintFinalized`; `try_repair_gaps`; резолвер p2p: `Fetcher::fetch` — один запрос за вызов, порядок `pending`, ретраи/таймауты, `excluded`).

## Вход

- Спека: `.dpos-study/PLAN.md:101`; `.dpos-study/history/E4-CORE-DESIGN.md` §5.2 (`:506-533`: «Тип и единственный писатель», «Триггер и лестница», «Порядок «аутентификация до `sync_to`»», диаграмма), §5.4 (`:549-572`), §5.5; `.dpos-study/DECISIONS.md` П-4 (`:87`), Д-2 (`:18`); `history/E4-PRECONDITIONS.md` Д3.
- Постановка: `.dpos-study/history/E4-prompts/4.2-B1-impl-1.md`. Журнал: `.dpos-study/history/E4-2-B1.md` — читать ПОСЛЕ диффа (§0(1)–(8), §1 Д-84…Д-91, §2, §4, §5). Контекст А: `history/E4-2-A.md` часть В §0(4) (Д-76…Д-83) и `E4-2-A-REVIEW.md` §1 A2-01/A2-04/A2-05 (что заход Б обязан был снять).
- Дифф: `git diff HEAD --stat -- crates/` (11 файлов, +1237/−816), затем по файлам. Якоря дерева (проверены оркестратором): `executor.rs` — `BlockFetcher::pair_at` `:372-375`, impl `:425-436`, `ReJumpFn` `:544-551`, `maybe_re_jump` `:2472-2515` (сравнение `:2482-2490`, `pair_at` `:2506`), `probe_frontier` (ступень без `servable`, `:2193-2204`, единственный читатель `frontier` `:2239`), новые юниты `:10279`, `:10333`; `cold_start_jump.rs` — `cold_start_jump` (обёртка с `get_latest`, `:792-821`), `jump_to_target` `:848-857`, сверка посадки `:894-932`, `verify_jump_*` `:653`, `:705`, юниты `:1508+`; `cert_inlet.rs` — тее с одним курсором `dkg_height_tx` (`:274`), `UpstreamResolver::fetch_targeted` `:2729-2762`; `dpos.rs` — пред-движковый холодный старт (`:1319`, не тронут по смыслу), замыкания валидатора `:2435+`, follower `:3270+`; `node/dpos.rs`; `testbed/stand.rs` — `upstream_source_only_for` `:180`/`:1221-1236`, `marshal_tip_series` флаг `:203`; `testbed/tests.rs` R-004, лестница (`LADDER_REACH_TICKS`), R-008 (`:2848`); `testbed/preconditions.rs:137`, `:289` (порог), тест `:261+`.

## Форма результата `.dpos-study/history/E4-2-B1-REVIEW.md` (§0–§6 как `E4-2-A-REVIEW.md`)

**§0 Прямые ответы** (нумерованные, каждый с [KNOWN]/[ГИПОТЕЗА]):

1. **Ворота** — прогнать САМОМУ, verbatim: `cargo test -p fluentbase-consensus --lib` (журнал 685/0); `… --features dpos-devnet-byzantine testbed::` (44/0); `cargo test -p fluentbase-consensus --lib testbed::` (36/0); `cargo test -p fluentbase-node --lib` (57/0); `cargo test -p fluentbase-staking-reader` (63/0); `cargo test -p fluentbase-consensus --test slasher_integration` (16/0); `cargo clippy -p fluentbase-consensus -p fluentbase-node --all-targets` (одна чужая `large_enum_variant` `node/dpos.rs:1989`) и с фичей (0); `cargo fmt -p fluentbase-consensus -p fluentbase-node -- --check`; `cargo doc -p fluentbase-consensus --no-deps 2>&1 | grep -c "unresolved link"` (7; на HEAD 8 — назови, какая ссылка ушла). ×3 — переписанные стенд-тесты (R-004, лестница, R-008, `preconditions.rs:261`).
2. **Триггер по tip'у** (`executor.rs:2482-2490`): подтверди, что у `maybe_re_jump` нет второго входа (heartbeat-повтор — `last_tip_height`, кто его пишет); `grep` по крейту и `node/` — `upstream_frontier`/`live_height` = 0 идентификаторов. **Центральный вопрос — дедлок «`committee[E]` не закоммичен»**, ради которого `upstream_frontier` существовал (`HEAD:executor.rs:613-620`): опиши по коду HEAD, в чём он состоял (inlet принимает серты, marshal не кладёт — почему: схема эпохи неизвестна `EpochSchemeProvider`?), и достижим ли он после 4.1 (модуль читает `committee[E]` на ordering-финализированном якоре; `commit_height(E) = start(E−2)`; окно `+2`) и после Б1 (ступень `Finalized{last(T+1)}` на замёрзшем тике). Если существует состояние, где tip замёрз, ступень НЕ верифицируема marshal'ом на этом узле (схема эпохи `T+1`/`epoch(tip)+1` не в карте) и никакого другого триггера нет — это регрессия против HEAD (там прыжок шёл по `upstream_frontier`): назови её серьёзность и сценарий с file:line. Иначе — скажи прямо, что дедлок недостижим, и чем.
3. **Цель из архива в executor'е** (`executor.rs:2506`, `pair_at` `:425-436`): два `await` на `MarshalMailbox` ИЗ `select!`-цикла executor'а. Может ли это взаимно заблокироваться с актором marshal'а (marshal ждёт `ack`/отправки `Update` в почтовый ящик executor'а, executor ждёт ответа marshal'а)? Открой, как marshal отправляет `Update::Tip`/`Update::Block` (канал, ёмкость, `await` на send) и как обрабатывает `get_finalization` (в том же цикле актора?), и есть ли на HEAD уже такие ожидающие чтения из executor'а (`BlockFetcher` — `fetch_block_by_height` «fire-and-forget»; другие `await` на marshal в цикле). Вердикт с file:line: безопасно / гонка / блокировка — и серьёзность. Также: промах `pair_at` на tip'е «невозможен по построению» (журнал) — проверь по CW: `Update::Tip` отдаётся после записи ОБОИХ (финализации и блока)? `get_block(height)` по высоте — тот же архив?
4. **Сверка посадки** (`cold_start_jump.rs:894-932`): что реально проверяет `RethElSync::holds` (`:615+`) и `JumpElSync::holds` стенда; правильна ли `holds(result)` против байтового сравнения (журнал Д-88: два коротких замыкания `sync_to` `:456-474`); `result == ZERO` пропуск — законен ли (pre-K окно) и нет ли пути, где `ZERO` приходит НЕ из pre-K; `Err ⇒ Stalled` — согласуется с §5.2 («ошибка пробы ⇒ Stalled»)? Исход `InvalidTarget` в executor'е = `rotate` (`:1482-1497`) при цели из собственного архива — журнал §4.2 признаёт; серьёзность как открытого пункта Б2.
5. **`cold_start_jump` (пред-движковый) остался на `get_latest`** (`:792-821`) — подтверди, что поведение холодного старта на HEAD и в дереве одинаково (`dpos.rs:1319+`, `cold_start_jump_self_heal`); что `refetch_verified_archive_hole` (`dpos.rs:403+`) не тронут.
6. **`servable` снят** (`executor.rs:2193-2204`): подтверди; механизм Д-89 — открой CW `fetcher.rs` (`fetch` один запрос за вызов `:325`? порядок `pending` `:277-283`? как ключ, у которого никто не имеет данных, ведёт себя: `Payload::Error` ⇒ `add_retry`, таймауты, «hog» слота) и ответь: (а) верно ли, что неотвечаемый ключ `Finalized{last(T+1)}` на каждом замёрзшем тике ЗАБИРАЕТ слот у by-height тяги (`try_repair_gaps`) на ТОМ ЖЕ резолвере (на плоскостном валидаторе `Hybrid.upstream` = `PlaneUpstreamHandle`, фронтир-канал) — то есть у отставшего узла, которому ступень не обслуживается (> f молчат), тормозит контигуозный догон; (б) на каких узлах ступень неотвечаема (узел у живого кончика с замёрзшим marshal'ом; ротированный; follower `None`) и сколько запросов в секунду это даёт; (в) переписанный шов R-008 (`upstream_source_only_for = (4, 3)`) — свойство теста осталось тем же или сузилось (журнал §4.4: прямого наблюдения «кто ответил» нет); мутация: убери шов — какой ассерт краснеет и почему это «жребий», а не свойство. Серьёзность (а) — отдельная строка §1, даже если проект это принимает («ступень не обслужена ⇒ метрика, проба повторяется»).
7. **A2-05 / Д-90 RECORDED**: подтверди по коду маршрут `hint_finalization(targets)` → `Hybrid::fetch_targeted` → `UpstreamResolver::fetch_targeted` (цели роняются) → `PlaneUpstreamHandle::get_finalization` (нецелевой) и вывод «на проводе целевой ступени нет». Верно ли, что минимальная правка — цели в `CertUpstream::get_finalization` (`cert_follow.rs`), и что это НЕ было возможно в Б1 только из-за списка файлов (тогда — MODERATE «отложено по списку», не «невозможно»).
8. **Д-85 (порог `< 2·interval`)** — арифметика: потолок marshal'а = `last(epoch(fin)+2)`, где `fin` — что именно (ordering_finalized executor'а? якорь модуля?) — file:line; при проде `min(1024, interval)` условие держится; при `interval ≤ 1024`… назови ровно, при каких конфигурациях порог ≥ 2·interval достижим (только тесты?). Это находка проекта (§5.2 не знает) — подтверди/опровергни.
9. **Д-87 «дыр не бывает»**: посадка `target − K`, `target` = собственный tip. Но marshal кладёт финализации не обязательно контигуозно (out-of-order по hint'ам/ступеням): между `ordering_finalized` и `tip` могут быть высоты без пары. Общее ли утверждение «нет дыры», или свойство фикстуры `preconditions.rs:261` (ассерт `missing=[] block-only=[]`)? Если общее — чем гарантируется (floor = landing − K, `try_repair_gaps`?); если фикстурное — ассерт вводит в заблуждение (переписать как наблюдение). Следствие для §5.2 (`seed_boundary_below_floor` больше не нужен?) — подтверди/опровергни.
10. **Стенд**: (а) `marshal_tip_series` через `Metrics::encode` на тик за флагом — подтверди, что флаг выключен во всех старых тестах и что ×3 байт-в-байт с ним; (б) `LADDER_REACH_TICKS = 800` при измеренных 631/627/626 — что пинует (не вакуумно ли: узел 3 ещё и прыгает; достигается ли ступень tip'ом ДО или ПОСЛЕ прыжка — по ряду); (в) `preconditions.rs:261` при пороге `min(1024, 32) = 32`: посылки Д3 (потолок, терминалы у всех) сохранены? «≥ 2 прыжка», «цель ≤ своего потолка `last(epoch(from)+2)`» — проверь, что `from` = `JumpCall::from` = `ordering_finalized`, и что потолок считается той же формулой, что в коде; имя теста не соответствует содержанию («jumps over a hole») — MINOR? (г) R-004 по ряду tip'а — ассерты не ослаблены против дерева А; (д) два удалённых юнита — какие свойства ушли и есть ли у каждого замена (журнал §2).
11. **Граница/гигиена**: `pub` изменения (`pair_at` в трейте `BlockFetcher` — все impl'ы), `#[allow]` (перенос с HEAD `:801`), `todo!`/таймеры/`unwrap` на продакшн-пути; `dpos.rs` правки вне проводки; `outer.rs` 6 строк; `plane_upstream.rs` только доки?
12. **Где журнал вводит в заблуждение**; §5.2 расхождения (а)(б)(в) журнала §0(7) — подтверди/опровергни каждое; hard-stop 4 оркестратора (стенд опроверг проект на центральном пути?) — скажи прямо да/нет со свидетельством.

**§1 Находки** — `id | серьёзность | file:lines | HEAD-якорь | что не так | чем пытался опровергнуть | уверенность`; id `B1-nn`. BLOCKER — регрессия на пути триггера/прыжка/доставки против HEAD, не предписанная проектом (в т.ч. вопрос 2 и 3), или доказанная неверность проекта на центральном пути. **§2 Поведение по ханкам.** **§3 Граница.** **§4 Ворота** verbatim. **§5 Оставить как есть** — непустой. **§6 Вне рамок** — для Б2 (холодный старт, `Latest` вне окна, `verify_jump_*`, исходы, follower/checkpoint, адресация ступени), В, 4.3, Э7.

В финальном ответе — только §0 (кратко) и таблица §1.

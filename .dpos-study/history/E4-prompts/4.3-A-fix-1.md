# Третий проход строки 4.3, заход А — правки по ревью R10

Ты — исполнитель (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `86fe7098`. Объект — **незакоммиченное дерево захода 4.3-А** (12 файлов под `crates/` + 2 интеграционных теста, `git status --short | grep -v devnet/`). Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`; `reader/` = `crates/dpos/staking-reader/src/`; `p2p/` = `crates/dpos/p2p/src/`.

## Правила

1. **Агентов не запускать.** 2. **Git только на чтение.** 3. **Файлы на запись:** `reader/epoch_transition.rs`, `reader/error.rs` (текст ошибки), `p2p/lib.rs`, `p2p/constants.rs` и `p2p/config.rs` (только комментарии), `beacon/actor.rs`, `beacon/dkg_transport.rs`, `beacon/plane.rs`, `beacon/log_resolver.rs`, `beacon/dkg_engine.rs` (три последних — ТОЛЬКО комментарии/доки), `slasher/gossip.rs`, `dpos.rs`, `node/dpos.rs`, `testbed/{fakes,stand,tests}.rs`, `crates/dpos/p2p/tests/convergence.rs`, `crates/dpos/consensus/tests/slasher_integration.rs`, журнал `.dpos-study/history/E4-3-A.md` (дописать часть «В. Третий проход»). **НЕ трогать:** `testbed/preconditions.rs`, `committee/**`, `executor.rs`, `plane_upstream.rs`, `cold_start_jump.rs`, `cert_inlet.rs`, `beacon/**` кроме названных, любой `Cargo.toml`, `devnet/**`, `.dpos-study/history/E4-ORCHESTRATOR.md`, `.claude/**`, `contracts/**`. 4. Каждое утверждение в журнале — `file:line` дерева. Комментарии — не свидетельство. 5. Читать целыми строками. 6. Не читать файлы вне git-трекинга кроме `.claude/` и `.dpos-study/`; никогда `.env*`, `*.pem`, `*.key`, `*secret*`, `*credential*`. 7. `CARGO_BUILD_JOBS=6`. 8. **Ворота исполнителя:** ОДИН прогон `cargo test -p fluentbase-consensus --lib` и `cargo test -p fluentbase-staking-reader` и `cargo test -p fluentbase-p2p` в конце на финальном дереве (verbatim), `cargo clippy -p fluentbase-consensus -p fluentbase-node -p fluentbase-staking-reader -p fluentbase-p2p --all-targets` — ноль НОВЫХ предупреждений (допустимы только `large_enum_variant` `node/dpos.rs` и `MutexGuard` `epoch_transition.rs` — оба на HEAD), `cargo fmt` по четырём крейтам, ×3 только изменённых стенд-тестов. Полный набор — у оркестратора. 9. Мутации ≤ 2, verbatim, с откатом и `md5sum`. 10. Старые тесты не ослаблять.

## Вход

- Ревью: `.dpos-study/history/E4-3-A-REVIEW.md` (§0–§6). Журнал: `E4-3-A.md`. Постановка: `E4-prompts/4.3-A-impl-1.md`. Спека §5.3 `E4-CORE-DESIGN.md:534-548`.
- Ворота оркестратора на входном дереве: `…/scratchpad/gates/a3f-*.txt` (все зелёные; clippy 12 строк, из них новых 4 — P-13).

## Таблица решений (по каждой — FIXED / RECORDED / REJECTED в журнале, с file:line)

| id | решение | что сделать |
|---|---|---|
| **P-01** (SERIOUS, подтверждено оркестратором: `push_neighbour_committee` `:767-795` на `Err` — `warn` и пропуск тира; `track_and_trigger` `:857-895` ретраит только `Full`/`Closed` и продвигает `last_tracked_epoch`; повторный `track` на тот же индекс CW молча игнорирует — `COMMONWARE_INTERNALS.md:363`; `GatedReceiver(members_only)` `dpos.rs:110-126` роняет `Tracked`) | **FIX** | `Err` при чтении соседней записи (`E−1`, `E+1`) — `?` (фатален для этого триггера ⇒ штатный ретрай границы без продвижения `last_tracked_epoch`; ПОКАЖИ по коду `consensus/dpos.rs` (grep `PENDING_RETRY_BACKOFF`/`on_finalized`), что `Err` из `on_finalized`/`apply_at` действительно ретраится на следующем блоке, и что `track_peers` (плоскостной, `:830-846`) при `Err` тоже возвращает `Err`/`None` и повторяется). `Ok(пустая запись)` остаётся законным «не закоммичено» — но ЗАПИШИ, при каких `E` это штатно (cold start `E ≤ 2`, `commit_height(E+1) = start(E−1)` по Д-1) и что в этом окне член `C[E+1]` без реестра… — стоп: он ЕСТЬ в реестре? `secondary` = реестр, значит он `Tracked`, и на BEACON `members_only` его роняет — назови, чем это отличается от HEAD (там он был primary через реестр и его тела удерживались). Если для cold-start окна нужен `members_only = false` на BEACON до первого полного набора — сделай это по коду, а не по догадке, и запиши Д-nn. Юнит reader'а: `Err` соседней записи ⇒ `track` НЕ вызван, результат — ошибка/ретрай («красный до правки» — на дереве сейчас `track` вызывается). Дока `:783-789` (P-22) — переписать. |
| P-02 (MODERATE, подтверждено: `finalized_delivered == 0` снят без замены) | **FIX** | Тест 4b: вернуть отрицательное свойство отсечения. Если стенд даёт ряд доставок по времени/высоте (`upstream_series`/`el_events`/`marshal_tip_series` — открой `Outcome`), утверждай «после падения линий (E = 2) НИ ОДНОЙ доставки `Latest`/`Finalized` узлу 3»; если ряда нет — добавить в `FakeUpstream`-статистику узла счётчик «доставок после высоты h» ИЛИ снимок статистики на границе E=2 (минимально). Снять вакуумный `heights[3] < heights[0]`. Порог `< 15` — обосновать числом (`start(2)` при `epoch_len`?) или вернуть строгий. Журнальную фразу «фальсификатор стал строже» исправить. |
| P-03, P-04 (MODERATE) | **FIX** | Док-комментарии вернуть их константе/тесту (`actor.rs:60-119`, `gossip.rs:353-369`). |
| P-05 (MODERATE, подтверждено: `last_height: 0` `:597`, единственный писатель `on_height` `:1156`) | **FIX** | До первого `on_height` окно `[now, now+2]` не должно ронять конфирмации: либо засеять `last_height` при сборке актора известной высотой (открой `beacon/plane.rs` `ValidatorInputs`/сборку `:839` — есть ли там курсор финализации; если есть — засев, и это правка `plane.rs` СВЕРХ комментариев, разрешена одной строкой), либо хранить `last_height: Option<u64>` и при `None` пропускать оконную проверку (только членство). Выбери по коду, запиши Д-nn; юнит: конфирмация до первого тика принимается. |
| P-06 (MODERATE) | **FIX** | Юнит на `GatedReceiver` с фейковым `Receiver`: tombstoned ⇒ не доходит до `recv`, untracked ⇒ нет, `Tracked` при `members_only` ⇒ нет, `Member` ⇒ да, окно `None` ⇒ да; метрика/reason. Интеграция `TrackedWindow` в стенд — RECORDED (стенд не строит проводку узла). |
| P-07, P-08 (MODERATE) | **RECORDED** (Д-123 остаётся) | Без правки поведения: `beacon_member` через `committee_for` ограничен тремя actionable-эпохами (одно чтение на промах, запись модуля кэшируется) — запиши границу стоимости; часы ET vs актора — записать величину (≈K) и лечение ретрансмитом. |
| P-09, P-11, P-12, P-19, P-20, P-21, P-22 (MINOR/NIT доки) | **FIX** | Тексты по факту (P-12 — три файла beacon только комментарии; P-21 — оговорить асимметрию окон в доке, поведение не менять). |
| P-10 | **RECORDED** | Константа безвредна. |
| P-13 (MINOR) | **FIX** | Четыре новых clippy: `type` алиасы для кортежей стенда и `tombstoned`, `useless_conversion` в тесте. |
| P-14 | **RECORDED** | Отравление = уже паника; `None` — намеренное. |
| P-15 (MINOR) | **FIX** | `debug_assert!` на ≤ 3 в `EpochMask::of` + инвариант в доке `TrackedPeers.committees` (уникальные эпохи, ≤ 3). |
| P-16 | **RECORDED** | Д5: перепредложение не наблюдалось; хелпер `dkg_agree.rs` вне списка. |
| P-17 | **REJECTED** | Имя из §5.3; тень локальна. |
| P-18 (NIT) | **FIX** | Снять `EpochMask::iter`, `TrackedWindow::epoch`, `TrackedPeers::is_secondary_only`, если без потребителя (тесты переписать на `epochs_of`). |
| P-23 | **RECORDED** | Механические правки тестов, посылки целы. |
| P-24 (MINOR) | **RECORDED** (с P-06) | Тир-проверка в `ingest_batch` остаётся для вызывающих без обёртки; живой шов покрыт юнитом P-06. |
| P-25 (MINOR) | **FIX** | `TrackSink`: сравнение `tracked_mismatches` по тирам раздельно (primary и secondary), не по объединению; модель разрыва — как есть. |

## Журнал (`E4-3-A.md`, часть «В. Третий проход»)

§0 — таблица `id | решение | file:line | как проверено`; §1 — новые Д-nn (после Д-126); §2 — прогоны verbatim (lib, reader, p2p, clippy-итог), ×3, мутации с md5; §3 — что осталось (непустой). В финальном ответе — только таблица §0 и строки `test result` + итог clippy + `git status --short | grep -v devnet/`.

# Ревью строки 4.2, заход В (`is_live_epoch` по marshal-tip'у; корроборация фронтира удалена)

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база — `HEAD` (`git rev-parse HEAD`; ожидается `016028af`). Объект — **незакоммиченное рабочее дерево** захода В: файлы из `git status --short | grep -v devnet/` под `crates/`. **Всё остальное — чужое** (`devnet/**`, `.dpos-study/history/E4-ORCHESTRATOR.md`) — не трогать. Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`.

## Правила

1. **Агентов не запускать.** 2. **Git только на чтение.** Единственный файл на запись — `.dpos-study/history/E4-2-V-REVIEW.md`; мутации продакшн-кода ради чувствительности тестов — только с откатом и `md5sum` до/после. 3. Каждое утверждение — `file:line` дерева и, где есть «до», `HEAD:file:line`. 4. Комментарии, доки, `.claude/dpos_architecture/`, журнал исполнителя — не свидетельство; свидетельство — код и прогон, который ты сделал сам. 5. **Полнота, не фильтр**: все находки, включая мелкие и неуверенные, с серьёзностью (BLOCKER / SERIOUS / MODERATE / MINOR / NIT) и «чем пытался опровергнуть»; не объединять разные находки — фильтрует оркестратор. 6. Читать целыми строками. 7. `CARGO_BUILD_JOBS=6`. 8. `.claude/COMMONWARE_INTERNALS.md` — ДО checkout'а (marshal: как актор отвечает на `get_info`/`Update::Tip`, ёмкость почтового ящика; резолвер).

## Вход

- Спека: `.dpos-study/history/E4-CORE-DESIGN.md` §5.1 «Замена `is_live_epoch` и `latest_live`» (`:498`), §5.5 (`:576`), §10 §0.3(а)(б) (`:729-730`), B4/B21; `.dpos-study/PLAN.md:103` (фраза про `is_live_epoch` и тест-замену R-003); `REGISTER.md` R-003 (`:45+`), R-096 (`:856+`).
- Постановка: `.dpos-study/history/E4-prompts/4.2-V-impl-1.md` (якоря HEAD, проверенные оркестратором; решения (а)–(д) в файле состояния). Журнал: `.dpos-study/history/E4-2-V.md` — читать ПОСЛЕ диффа.
- Дифф: `git diff HEAD --stat -- crates/`, затем по файлам. Якоря дерева (проверены оркестратором, [KNOWN]): правило `live_epoch_of`/`is_live_epoch_at` `epoch_manager.rs:1791-1809`; `Actor::live_epoch` `:974-979`, `Actor::is_live_epoch` `:995-1000`; поле `tip: watch::Receiver<u64>` `:469`, подписка `:622`; арм `tip_wake.changed()` `:775-785` → `reconcile_live` `:1457-1465`; `reconcile_roles` читает `committee(epoch)` `:1025-1047` ДО `abort_below` `:1070`; `sweep_frontier` `:1437-1442`; `repair_keyless_schemes` `:1745-1755`; юнит правила `:2948-2975`; писатель tip'а `application.rs:1072-1080` (`ordering_tip.send_replace` в арме `Update::Tip`), поле `:368`, посев `0` `:429`, `ordering_tip()` `:463-465`; `outer.rs:1490` `epoch_manager.start(None)`; `node/dpos.rs:1266` vote-backup стал наблюдателем route-miss; `stand.rs:1814-1815` дренаж; новый стенд-тест `testbed/tests.rs:2560`; `dpos::local_tracked_epoch` `dpos.rs:1207-1219`. Diff stat: 7 файлов, +668/−1128 (`epoch_manager.rs` −1263 строк диффа). Устаревшие комментарии, которые V1 не правил (файлы вне его списка): `committee/mod.rs:674` (`register_span`), `beacon/surface.rs:2208` и `epoch_manager.rs:2484` (`vote_backup`); мёртвые pub `VoteBackupItem`/`ResettableForward` (`lib.rs:89-90`, `dpos.rs:1019`, `:1028+`) — V1 §5 п.1; проверь и включи в §1. Отдельно проверь журнал §4 п.1 (окно «движок мёртвой эпохи живёт до `committee_readable`, если `committee[live]` не читается» — может ли это быть с ЗАПУЩЕННЫМ движком: live ≤ epoch(fin)+3 при окне чтения epoch(anchor)+2), §4 п.5 (marshal `scoped(E)` ⇒ `None` при читаемой записи без схемы — какая из двух веток CW, повторный запрос есть?), §4 п.4 (follower: тот же `FluentApp`? `ordering_tip` пишется на follower'е?), Д-113 (sweep не будится по tip'у — согласен ли ты, что это отдельная работа, а не дыра этого захода) и Д-114 (route-miss метрика на vote-канале).

**Ворота v1f гонит оркестратор ПАРАЛЛЕЛЬНО твоему ревью.** Если `gates/v1f-status.txt` ещё не содержит строки `DONE` — сначала делай ревью кода, а §4 заполняй в конце; жди появления `DONE` циклом `sleep 60` (не дольше 40 мин), затем `md5sum -c v1f-tree.md5` и читай `v1f-*.txt`. Свои точечные `cargo test` запускай с `CARGO_BUILD_JOBS=4`, чтобы не мешать (ворота идут на 6).

## Форма результата `.dpos-study/history/E4-2-V-REVIEW.md` (§0–§6 как `E4-2-B2-REVIEW.md`)

**§0 Прямые ответы** (нумерованные, каждый с [KNOWN]/[ГИПОТЕЗА]):

1. **Ворота** — полный набор УЖЕ прогнан оркестратором: `…/scratchpad/gates/v1f-*.txt` (`v1f-status.txt` коды, `v1f-tree.md5` снимок — сверь `md5sum -c`). Приведи `test result` verbatim; НЕ перегоняй набор; гоняй только точечные тесты и мутации.
2. **Правило `is_live_epoch`** (центральный вопрос): по коду — что такое `live_epoch(tip)` (граница `tip == last(E)` ⇒ E+1?), совпадает ли с `local_tracked_epoch` (`dpos.rs`) и с ET-правилом (`epoch_transition.rs:558/:580`); что при незамёрзшей геометрии; на `tip` — ПРОВЕРЕННАЯ финализация (единственный писатель — `store_finalization` после `verify_delivered`; Б2 отбрасывает вне окна)? Есть ли путь, где `their_epoch` с провода (vote backup) всё ещё влияет на роль/фронтир/спавн — если да, R-003 не закрыт: серьёзность.
3. **Источник tip'а**: чтение `get_info(Latest)` из `select!` epoch_manager'а ИЛИ `watch` от `FluentApp::report`. Взаимная блокировка с marshal'ом/executor'ом (кто кого ждёт при полном почтовом ящике) — file:line; гонка «tip двинулся, реконсиляция не разбужена» — какое ребро будит (`reconcile_live` без boundary_rx); движок мёртвой эпохи живёт дольше одного `Update::Tip`?
4. **`pipeline_catchup_span`/`vote_backup`**: удалён или оставлен; если оставлен — верхняя граница span'а и что даёт сверх модуля/лестницы (исполнитель обязан был показать); `their_epoch` как триггер EVM-чтений — нагрузка от одного пира (cap 6, memo) — оцени по коду; если удалён — что потерял deep catch-up (тест `deep_catchup_pipelines_span_in_one_hint` на HEAD) и чем это покрыто теперь.
5. **`sweep_wake`/repair sweep** под новым фронтиром — исключает ту же эпоху, что раньше (HEAD `:1493`, `repair_keyless_schemes`)? Регрессия для сбора ключа живой эпохи?
6. **Стенд**: новый тест «отставший на 3 эпохи не спавнит движок…» — что пинует, невакуумность (мутация правила красит?), наблюдение ролей через `Outcome` — новый шов или существующий; переписанные ассерты «что доказывал / что доказывает».
7. **Юниты**: три точки правила, «`their_epoch = u64::MAX` не двигает роль», мутации — воспроизведи ≤ 2.
8. **Гигиена**: `pub` изменения, `#[allow]`, `todo!`, таймеры, `unwrap`; правки вне списка; `node/dpos.rs`/`application.rs` — только проводка?
9. **Где журнал вводит в заблуждение**; расхождения с §5.1/§5.2; hard-stop оркестратора: (2) Д-2/П-4/П-5 менять? (3) BLOCKER без ответа проекта? (4) стенд опроверг проект? — да/нет со свидетельством.

**§1 Находки** — `id | серьёзность | file:lines | HEAD-якорь | что не так | чем пытался опровергнуть | уверенность`; id `V-nn`. **§2 Поведение по ханкам.** **§3 Граница.** **§4 Ворота** verbatim из файлов оркестратора. **§5 Оставить как есть** — непустой. **§6 Вне рамок** — 4.3, Э5, Э7.

В финальном ответе — только §0 (кратко) и таблица §1.

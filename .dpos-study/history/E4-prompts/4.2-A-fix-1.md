# Строка 4.2, заход А, третий проход — находки ревью `E4-2-A-REVIEW.md`

Ты — исполнитель (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`; объект — незакоммиченное рабочее дерево захода 4.2-А: `crates/dpos/consensus/src/{committee/mod.rs, committee/store.rs, dpos.rs, executor.rs, plane_upstream.rs, testbed/fakes.rs, testbed/stand.rs, testbed/tests.rs}`, `crates/node/src/dpos.rs`. **Всё остальное в `git status` — чужое** (`contracts/staking/**`, `.dpos-study/**` кроме твоего журнала, `devnet/**`, `target-contract/`) — не трогать. Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`.

## Правила

1. **Агентов не запускать.** 2. **Git только на чтение.** 3. **Файлы на запись**: те же девять + `cert_inlet.rs` (ТОЛЬКО `UpstreamResolver::fetch_targeted` и диспетчер `Hybrid`, A2-05) + `outer.rs` (ТОЛЬКО `:120-132`, диспетчер резолвера, A2-05) + `.dpos-study/history/E4-2-A.md` (дописать **часть В** в конец; выше — только исправление §0(6)(в)/Д-73/§7/§8.3 с пометкой «[исправлено в части В]»). Запрещены: `cold_start_jump.rs`, `epoch_manager.rs`, `reader/**`, `beacon/**`, `p2p/**`, `.claude/**`. 4. file:line на каждое утверждение. 5. Без `#[allow]`/`todo!`/таймеров; `unwrap` на продакшн-пути — только `Mutex::lock().unwrap()`. 6. Комментарии — не свидетельство. 7. Читать целыми строками. 8. **Правила прогонов**: все правки батчем → один прогон затронутых тестов → ворота один раз в конце; «красный до правки» — только для тестов A2-01 и A2-03 (ниже); мутация — не более двух (`--exact`). 9. `CARGO_BUILD_JOBS=6`. 10. Старые ассерты не ослаблять.

## Что прочитать

1. `.dpos-study/history/E4-2-A-REVIEW.md` целиком (§0, §1 A2-01…A2-15, §5, §6) — особенно §0.3 (инструментированный прогон: 316 из 416 тиков ступень ВЫШЕ tip'а, 3 ступени положены; гасит `servable`, не потолок).
2. `.dpos-study/history/E4-2-A.md` §0, §3, §7, §8; `.dpos-study/history/E4-CORE-DESIGN.md` §5.2 (`:506-533`) — «проба … на каждом тике при замёрзшем tip'е запрашивает `Finalized{last(T+1)}` … плюс `Latest`», «`Latest` вне окна отбрасывается с метрикой», §5.4 строки «Лжец на пробе», «`Latest` вне окна», «Ступень никем не обслужена».
3. Код: `executor.rs:2090-2200` (`probe_frontier`: ранний возврат «tip сдвинулся» `:2092-2096`, `servable` `:2143-2148`, `at_or_below_the_tip` `:2150-2153`, `hint_finalization` `:2167`, `probe_step_pending`), `:530-545` (`ProbeOutcome`); `dpos.rs:2459-2520` (замыкание пробы: `frontier: latest.map(…)` `:2506`, `participants` `:2470-2476`); `plane_upstream.rs:313-420` (`deliver`; BLS `:359-366`; шаг 5 `:373-400`; ждущие `:411-418`), `:38-42` (док модуля), `:84-89` (метрики), `:966-986` (юнит pass-through); `committee/mod.rs:528-533` (схема с оракулом), `store.rs:517-524`; `scheme.rs`/`bls` — `build_verifier(ns, bimap, epoch, oracle: None)`; `cert_inlet.rs:2742-2766` (`spawn_finalized`), `:2870-2880` (`fetch_targeted` игнорирует цели); `outer.rs:120-132` (диспетчер `MarshalResolver::{Plane, Hybrid}`); `testbed/tests.rs:1243-1312` (тест лестницы: сравнение с ФИНАЛЬНЫМ `tip`), `:2947`, `:2969-3036`, `:3149-3208`, `:3349-3434`; `testbed/fakes.rs:1575-1578`, `:1818-1826`; `testbed/stand.rs:1981-1984`; `node/dpos.rs:1018-1022`, `:1797-1802`, `:2080-2082`; `dpos.rs:928-929`, `:2443-2452`.

## Решения оркестратора (исполнить ровно так; отклонение — только с причиной file:line в В§0)

| id | решение | что сделать |
|---|---|---|
| A2-01 | **FIXED** | Снять предпосылку `servable` (`executor.rs:2143-2148`): ступень кладётся на КАЖДОМ тике при замёрзшем tip'е, когда она именована и выше tip'а (`at_or_below_the_tip` остаётся — с поправкой A2-10 ниже); необслуженная ступень — метрика `unserved` + контигуозный догон, как в §5.2/§5.4. `ProbeOutcome.frontier` в решение о ступени НЕ входит (он остаётся только для `upstream_frontier` до Б). **Тест (красный до правки)**: стенд — на фикстуре теста лестницы (`tests.rs:1243`, узел 3 у потолка) ступень выше tip'а КЛАДЁТСЯ (наблюдай `hint_finalization`/`HintFinalized` через `Outcome` — добавь счётчик в стендовый marshal-обёртку или через `UpstreamCounters`, что доступно) больше, чем в 3 тиках из 416; либо ET-уровня юнит `probe_frontier` с фейком, если стенд не даёт наблюдателя. Д-nn: `servable` снят. |
| A2-02 | **FIXED** | Тест лестницы (`tests.rs:1243-1312`) сравнивать с ТЕКУЩИМ tip'ом узла на момент именования ступени (снимай tip в замыкании пробы вместе со ступенью), утверждать: (а) ≥ 1 ступень выше текущего tip'а; (б) после A2-01 — ≥ 1 ступень положена; (в) ни одной ступени с `at_or_below_the_tip`, положенной marshal'у. Переименовать по новому свойству. Журнал: §0(6)(в), Д-73, §7 «блокирующее для лестницы», §8.3 — исправить с пометкой; Д-73 переписать как «ступень выше tip'а достижима (рестарт из архива, холодный старт, узел без пуша), гасил её `servable`». |
| A2-03 | **FIXED** | BLS-гейт `deliver` (`plane_upstream.rs:359-366`) — verify-only схема БЕЗ seed-оракула, построенная из записи, которую `deliver` уже получил (`build_verifier(ns, record.bls.bimap, epoch, None)`; кэшировать по эпохе внутри `FrontierHandler`, чтобы не строить на каждый ответ); `Committee::scheme` для фронтира не использовать. **Тест (красный до правки)**: юнит `plane_upstream` — честный сертификат под комитетом, чей seed-ключ у узла «протух»/отсутствует (`SchemeCommittee` с оракулом, отвечающим `Invalid`/чужой ключ — посмотри, как cert-inlet тестирует `SeedCheck::Invalid`) ⇒ `true` (допущен), а не `false`. Д-nn: почему фронтир не делает `ensure_key` (нет бикона на этом пути; seed проверяется ниже — marshal `verify_delivered` через модуль/inlet). |
| A2-04 | **FIXED** | `Latest` вне окна / `NotReadable` / нет схемы — ОТБРАСЫВАТЬ с метрикой и `true`, как в §5.2 (`plane_upstream.rs:373-400`: ждущий `Latest` НЕ разрешается ответом; `upstream_frontier` не растёт); by-height `Finalized{h}` — оставить pass-through (Д-72 сужается до by-height с измеренной причиной — голодание `spawn_finalized`; для Б: повтор by-height по пробуждению модуля). Ассерт в тесте R-004 (`:2969-3036`): пик `upstream_frontier` жертвы = 0 при честном далёком `Latest` тоже (добавь честный контроль «пир далеко впереди, эпоха вне окна» — если фикстура позволяет; иначе юнит `plane_upstream`: `Latest` с эпохой вне окна ⇒ ждущий не разрешён). Док модуля `:38-42` (A2-09) — по факту: `Latest` — drop, by-height — pass-through. |
| A2-05 | **FIXED** | Цели ступени на `Hybrid`: открой `cert_inlet.rs:2870-2880` и `outer.rs:120-132` — `HintFinalized{targets}`/`fetch_targeted` с непустыми целями на `Hybrid` должен уходить в ПЛОСКОСТНОЙ резолвер (`PlaneUpstreamHandle`/frontier-резолвер, где `participants` из `track`), а не в WS-upstream, который цели игнорирует; если `Hybrid` без плоскости (чистый WS follower) — цели игнорируются с `debug!` (это не его путь, §5.4). Тест: юнит на диспетчер (какая ветка получает `targets`) — красный до правки не требуется, мутация одна (`--exact`). |
| A2-06 | **FIXED** | Пять комментариев о втором читателе `tracked_epoch` (`node/dpos.rs:1018-1022`, `:1797-1802`, `:2080-2082`; `dpos.rs:928-929`, `:2443-2452`) — по факту: читатель один (`executor.rs:656`). |
| A2-07 | **FIXED** | `probe_step_pending` сбрасывать на раннем возврате «tip сдвинулся» (`executor.rs:2092-2096`) — иначе `unserved` тикает за обслуженную ступень. |
| A2-08 | **FIXED (частично)** | После A2-03 честный контроль не должен давать `deliveries_rejected`; вернуть ассерт `control == 0` в R-001/R-004, где он был снят (`:3203-3208`, `:3030-3036`), если ×3 прогона стабильны; разрез по `reason` стенду недоступен — записать (Д-nn), не чинить. |
| A2-10 | **FIXED** | `at_or_below_the_tip` сравнивать с marshal floor / `last_processed_height` (то, с чем сравнивает no-op marshal'а `HintFinalized`), а не с tip'ом — ступень внутри дыры прыгнувшего узла должна класться. Проверь по `COMMONWARE_INTERNALS.md` (`core/actor.rs:633-635`), с чем именно сравнивает marshal, и возьми ту же величину, если она доступна executor'у; иначе записать. |
| A2-11 | **FIXED** | Пустой `participants` ⇒ `dpos_frontier_step_skipped_total{reason="no_participants"}` (`dpos.rs:2470-2476`, `stand.rs:1981-1984`). |
| A2-12, A2-13 | **FIXED** | Док `deliveries_rejected` (`fakes.rs:1575-1578`) по факту; ссылка на юнит (`tests.rs:2947`) — реальное имя и смысл. |
| A2-09 | **FIXED** | см. A2-04 (док модуля). |
| A2-14 | **RECORDED** | `cold_start_jump.rs` док `verify_jump_structural` — запрещён здесь; для Б (файл уходит в Б целиком). |
| A2-15 | **RECORDED** | EVM-staticcall + BLS в задаче фронтир-резолвера — класс B2-03 (замер Э7); одной строкой + Д-nn. |

## Ворота (сам, verbatim по одной строке, ОДИН раз в конце)

`cargo test -p fluentbase-consensus --lib` (679 + новые); `… --features dpos-devnet-byzantine testbed::` (44 ± переписанные); `cargo test -p fluentbase-consensus --lib testbed::` (36 ± …); `cargo test -p fluentbase-node --lib` (57/0); `cargo test -p fluentbase-staking-reader` (63/0); `cargo test -p fluentbase-consensus --test slasher_integration` (16/0); `cargo clippy -p fluentbase-consensus -p fluentbase-node --all-targets` (одна чужая `large_enum_variant`) и с фичей; `cargo fmt --check` по своим файлам; `cargo doc -p fluentbase-consensus --no-deps 2>&1 | grep -c "unresolved link"` (8). Три переписанных ролевых теста + тест лестницы — ×3.

## Журнал — часть В в конце `.dpos-study/history/E4-2-A.md`

`# Часть В — заход 4.2-А, третий проход: находки ревью E4-2-A-REVIEW.md`. В§0: (1) `FIXED n · RECORDED m · REJECTED k`; (2) ворота verbatim (+ ×3); (3) тесты — имя / что утверждает / красный до правки verbatim (A2-01, A2-03) или мутация; (4) Д-nn после Д-75, переписанная Д-73; (5) что теперь ТОЧНО делает лестница на стенде: сколько ступеней положено, сколько обслужено (tip сдвинулся после ступени?) — если ноль обслуженных, назови почему (кто отвечает на `Finalized{last(T+1)}` в стенде и доходит ли ответ до `store_finalization`); (6) что в §5.2 остаётся неверным по коду. В§1 таблица `id | исход | что сделано | file:line`. В§2 раскладка коммитов (`cargo check --all-targets` на группу). В§3 слабее всего / для Б и В.

В финальном ответе — только В§0.

# Третий проход строки 4.2, заход В — правки по ревью R9

Ты — исполнитель (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `016028af`. Объект — **незакоммиченное дерево захода В** (7 файлов под `crates/`, `git status --short | grep -v devnet/`). Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`.

## Правила

1. **Агентов не запускать.** 2. **Git только на чтение.** 3. **Файлы на запись:** `epoch_manager.rs`, `application.rs`, `dpos.rs`, `outer.rs`, `lib.rs` (только снятие реэкспортов), `beacon/surface.rs` и `committee/mod.rs` (только комментарии/тестовый двойник по V-12/V-13), `testbed/stand.rs`, `testbed/tests.rs`, `node/dpos.rs`, журнал `.dpos-study/history/E4-2-V.md` (дописать часть «В. Третий проход»). **НЕ трогать:** `testbed/preconditions.rs`, `committee/store.rs`, `executor.rs`, `plane_upstream.rs`, `cold_start_jump.rs`, `beacon/**` кроме комментария `surface.rs:2208`, `devnet/**`, `.dpos-study/history/E4-ORCHESTRATOR.md`, `.claude/**`, `contracts/**`. 4. Каждое утверждение в журнале — `file:line` дерева. Комментарии — не свидетельство. 5. Читать целыми строками. 6. Не читать файлы вне git-трекинга кроме `.claude/` и `.dpos-study/`; никогда `.env*`, `*.pem`, `*.key`, `*secret*`, `*credential*`. 7. `CARGO_BUILD_JOBS=6`. 8. **Ворота исполнителя (решение пользователя 2026-09-12):** ОДИН прогон `cargo test -p fluentbase-consensus --lib` в конце на финальном дереве (verbatim `test result`), `cargo fmt -p fluentbase-consensus -p fluentbase-node`, ×3 только своих новых/изменённых стенд-тестов. Полный набор (стенд с фичей, node, reader, slasher, clippy, doc) гонит оркестратор. По ходу — `cargo check`/точечные тесты по необходимости, не чаще, чем нужно. 9. Мутации — ≤ 2, verbatim, с откатом и `md5sum`.

## Вход

- Ревью: `.dpos-study/history/E4-2-V-REVIEW.md` (§0–§6). Журнал исполнителя: `E4-2-V.md`. Постановка захода: `E4-prompts/4.2-V-impl-1.md`. Спека: `E4-CORE-DESIGN.md` §5.1 (`:498`).
- Ворота оркестратора на входном дереве: `…/scratchpad/gates/v1f-*.txt` (все зелёные; doc `unresolved 8`, из них новая — `epoch_manager.rs:574`).

## Таблица решений (по каждой — FIXED / RECORDED / REJECTED в журнале, с file:line)

| id | решение | что сделать |
|---|---|---|
| **V-02** (SERIOUS, подтверждено оркестратором по коду: `abort_below` `:1499-1531` безусловно зовёт `prune_agreements` `:317-366` — до 8 `Storage::remove` на каждый вызов `reconcile_roles`; ребро `tip_wake` `:775-785` зовёт `reconcile_live` на КАЖДОМ `Update::Tip`, ~1/с) | **FIX, корневое** | Ребро — «live-эпоха изменилась», а не «tip изменился». В арме `tip_wake.changed()` вычислять `live_epoch()` и звать `reconcile_live` только если он отличается от последнего значения, на котором это ребро уже реконсилировало (новое поле `Actor`, например `tip_reconciled_live: Option<Epoch>`; обновлять его И в арме, И там, где `reconcile_roles(live)` уже произошёл по другому ребру — иначе первый tip после границы вызовет лишний проход; реши по коду и запиши). Проверь, что деферральный случай (комитет живой эпохи нечитаем ⇒ `deferred_reconciles`) по-прежнему дренируется ребром `committee_readable` (`:1830-1840`), а не зависел от повторных tip-пробуждений; проверь случай «геометрия ещё не замёрзла при первых tip'ах» (live `None` ⇒ ничего; после заморозки будит `committee_readable` — покажи file:line, что это ребро срабатывает на заморозке). Юнит НЕ обязателен, если нет гарнитуры `Actor`; если написать можно за приемлемую цену — один. Обновить комментарий арма `:765-771` (см. V-10) и доку `reconcile_live` `:1443-1452` (см. V-19). Это же закрывает V-03 и V-18 — отметь в журнале с якорями. |
| **V-01** (SERIOUS, подтверждено оркестратором: мутация `is_live_epoch_at → true` — стенд-тест зелёный, тот же вывод) | **FIX с таймбоксом** | (а) Одна попытка фикстуры, где гейт живости — ЕДИНСТВЕННЫЙ отказ: `StandConfig::honest(4, 1)` без ротации (доля переносится/самовыводится — проверь по коду beacon/agreement, что у узла 3 есть доля эпох, пропущенных в партиции; если нет — фикстура не годится, запиши почему) + `stand.partition(&[0,1,2], &[3]).after_height(h).for_views(n)` на ≥ 2 эпохи (образец `testbed/tests.rs:289`), затем догон. Ассерты: узел 3 после догона НЕ имеет signer-схемы (`scheme(e).me().is_none()`) для эпох, границы которых он прошёл при tip'е выше `last(e)` (сформулируй по `marshal_tip_series`/`heights`), и имеет verifier-схемы для них; контроль — узлы 0-2 signer в тех же эпохах; узел 3 signer в эпохе, где его исполнение догнало tip. **Обязательно:** мутация `is_live_epoch_at → true` должна красить этот тест (verbatim в журнале, откат, md5). Если фикстура не разделяет (мутация зелёная) — не тратить второй заход: (б) переписать доку и имя нового теста `tests.rs:2560` честно (он пинует: схемы регистрируются путём `CertProvider` без спана; реконсиляции выше исполнения нет; live-эпоха выше окна чтения), выкинуть/переписать ложный комментарий блока (3) (V-09), и записать в журнале RECORDED «стенд-пин гейта живости открыт» с механизмом, почему фикстура не разделила. |
| V-04 (MODERATE) | **FIX** | Удалить `VoteBackupItem` (`dpos.rs:1010-1019`), `ResettableForward` (`:1021-1054`) и их реэкспорт в `lib.rs:89-90`; `grep -rn` по `crates/ bins/` — ноль потребителей до и после. |
| V-05 (MODERATE) | **FIX** | Дока `repair_keyless_schemes` `:1711-1720`: снять ложный инвариант «every path that registers a scheme for E advances `entered` first»; написать по факту (путь `CertProvider` регистрирует минуя `reconcile_roles`; исключение sweep'а — по `live`/`entered`). |
| V-06 (MODERATE) | **RECORDED** | Записать Д-nn: при догоне `live` до `entered+3`, sweep получает до трёх verify-only кандидатов больше, чем на HEAD при `observed=0`; не wedge, только сеть. Без правки. |
| V-07 (MODERATE) | **FIX** | Комментарий `:2478-2485` — снять `vote_backup` из «three arms». |
| V-08 (MODERATE) | **FIX** | `:574` — ссылка на `CertProvider::scoped`/обычные кавычки; ворота `doc` должны вернуться к 7. |
| V-09 (MODERATE) | **FIX** | Комментарий блока (3) `tests.rs:2630-2636` — переписать (входит в V-01 (б); при (а) — тоже поправить). |
| V-10 (MODERATE) | **FIX** | Комментарий арма `:765-771` — с оговоркой про нечитаемый `committee[live]` (движок живёт до `committee_readable`). Поведение не менять. |
| V-11 (MINOR) | **RECORDED** | Свойство закрыто структурно (поля нет); юнит `u64::MAX` не нужен. |
| V-12 (MINOR) | **FIX** | `beacon/surface.rs:2208`, `committee/mod.rs:674` — только комментарии. |
| V-13 (MINOR) | **FIX, если чисто** | `SchemeCommittee::with_records` и `records`-половина (`committee/mod.rs:695`, `:702`) — если единственный вызывающий передаёт `\|_\| None`, снять половину и вызов; если тянет за собой больше двух мест — RECORDED. |
| V-14 (MINOR) | **RECORDED** | Частота route-miss на vote-канале при догоне — Д-nn, без правки. |
| V-15 (MINOR) | **RECORDED** | Файлы оркестратора; в журнале одна строка. |
| V-16 (NIT) | **FIX** | Дока `local_tracked_epoch` `dpos.rs:1204-1206` и `Actor::live_epoch` `epoch_manager.rs:971-974` — снять несуществующую ветку `None` «below activation». |
| V-17 (NIT) | **REJECTED** | Принятый в файле приём пина предпосылки. |
| V-19 (MINOR) | **FIX** | Дока `reconcile_live` `:1443-1452` — убрать «cannot be a stale one»; сказать, что гейт перечитывает tip после `abort_below(...).await`, и что `soft_enter` при живом движке выходит рано (`:1357-1360`). |

## Журнал (`E4-2-V.md`, часть «В. Третий проход»)

§0 — таблица `id | решение | file:line | как проверено`; §1 — новые Д-nn (после Д-115); §2 — прогон `--lib` verbatim, ×3 стенд-тестов (нового V-01 (а) и `a_node_three_epochs_behind…`), мутации verbatim с md5; §3 — что осталось (непустой). В финальном ответе — только таблица §0 и `test result` строки.

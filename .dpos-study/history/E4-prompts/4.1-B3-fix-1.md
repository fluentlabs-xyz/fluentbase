# Строка 4.1, заход Б3, третий проход — находки ревью `E4-1-B3-REVIEW.md`

Ты — исполнитель (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `17b796fa`; объект — незакоммиченное рабочее дерево захода Б3: `crates/dpos/consensus/src/testbed/{fakes,stand,mod}.rs`, новый `testbed/committee_tests.rs`, новый `e2e/src/staking_commit.rs`, `e2e/src/lib.rs`. **Всё остальное в `git status` — чужое** (`contracts/staking/**`, `crates/staking-abi/src/lib.rs`, `devnet/**`, `target-contract/`, `.dpos-study/history/E4-ORCHESTRATOR.md`) — не трогать. Пути без префикса — от `crates/dpos/consensus/src/`.

## Правила

1. **Агентов не запускать.** 2. **Git только на чтение.** 3. **Файлы на запись**: те же шесть + `.dpos-study/history/E4-1-B3.md` (дописать **часть В** в конец; выше не переписывать, кроме исправления расписки md5 B3-14 и заголовка §1 B3-10 с пометкой «[исправлено в части В]»). Продакшн-код (`committee/**`, `node/**`, `dpos.rs`, …) — ЗАПРЕЩЁН; `.claude/**` — доки правит оркестратор. 4. file:line на каждое утверждение. 5. Без `#[allow]`/`todo!`/таймеров; в тестах `unwrap` можно. 6. Комментарии — не свидетельство. 7. Читать целыми строками. 8. Каждая правка ассерта — с повтором мутации, которая её мотивировала (verbatim: красный после правки ассерта при мутации, зелёный без). 9. `CARGO_BUILD_JOBS=6`; `Blocking waiting for file lock` — ждать. 10. Старые тесты (40 с фичей / 32 без, `tests.rs`, `preconditions.rs`) не трогать.

## Что прочитать

1. `.dpos-study/history/E4-1-B3-REVIEW.md` целиком (§0, §1 B3-01…B3-16, §5, §6).
2. `.dpos-study/history/E4-1-B3.md` §0, §3 (Д-51…Д-59), §7; `.dpos-study/history/E4-1-B2p.md` часть В как образец формы части В.
3. Код: `testbed/stand.rs` — фаза collect (`:1270-1400`: `logs` `:1280`, `staking_reads` `:1314`, `committee_records` `:1361-1388`), где ставится/дренируется `DebuggingRecorder` (в тестах); `testbed/committee_tests.rs` целиком; `testbed/fakes.rs:1060-1150` (`branch_of`, `committed_at` `:1070`, `weights_at` `:1142`); `committee/store.rs:420-503` (четыре плеча `NotReadable`, `failed()`/`reported`) и `committee/mod.rs:575-590` (`commit_height`) — читать; `e2e/src/staking_commit.rs` целиком; `contracts/staking/src/consensus.rs` (`current_epoch_at_block`, горизонт) — читать.

## Решения оркестратора (исполнить ровно так; отклонение — только с причиной file:line в В§0)

| id | решение | что сделать |
|---|---|---|
| B3-01 | **FIXED** | Метрики, на которых стоят ассерты «спрашивали в прогоне», должны быть сняты ДО фазы collect, которая сама зовёт `committee(e)` для `0..=max_epoch+2` (`stand.rs:1367-1372`, оркестратор открыл). Сделай снимок рекордера частью `Outcome` (например, `Outcome::metrics_before_collect` — снятый в `run_until` ПЕРЕД collect) либо перенеси дренаж в тест до collect, если структура позволяет; тесты 2 и 3 читают счётчики из этого снимка. Проверка: с новым снимком `above` у припаркованного узла — измеренное число прогона (журнал: 72 минус вклад collect — назови число), и при мутации «прогон не спрашивает» (например, гейт, который не даёт узлу 3 спросить эпохи 5–7 — найди дешёвую) ассерт `above > 0` обязан покраснеть; если такой мутации нет — скажи прямо, что ассерт пинует только «> вклада collect», и переведи его в `above ≥ N_измеренное` с пояснением. |
| B3-02 | **FIXED** | Плечо «`NotReadable` без EVM на якоре 0» — утверждать через `module_snapshot`: у узла на якоре 0 `module_snapshot[1]` и `[2]` должны быть **0** в момент отказа (снимок счётчиков до догона — если стенд умеет промежуточный снимок; иначе — сравни финальное `module_snapshot[1] == 1` с тем, что при мутации `commit_height(e≤2) => 0` он станет ≥ 2 или отказ исчезнет). Мутация `committee/mod.rs:582` `e<=2 => 1` → `=> 0` (временно, с откатом и md5) обязана красить тест на ТОМ ассерте, который ты добавишь, а не на `:485`. Записать, какое из четырёх плеч `store.rs:429/449/470/490` тест теперь отличает, и что плечо «пусто от контракта» (`:490`, два staticcall'а) стендом не пинуется (Д-nn). |
| B3-03 | **FIXED** | Генезисная половина e2e-пина: контракт при `current = 0` (любой доактивационный блок, включая 0) принимает 0, 1, 2 и отвергает 3 — это горизонт контракта, а «1» в `commit_height(E≤2) = 1` — факт узла (первый ИСПОЛНЕННЫЙ блок), контракту неизвестный. Переписать док и ассерты честно: (а) пин на блоке 0 И на блоке 1 — оба принимают 0..2, отвергают 3 (без утверждения «единица — свойство контракта»); (б) в доке модуля/теста явно: «`commit_height(E≤2) = 1` кодирует, что первый исполненный блок узла — 1; контракт лишь не мешает». Мутация ревью (`with_block_number(0)` проходит) после правки — ожидаемо зелёная, и это теперь не противоречие. Д-nn. |
| B3-04 | **RECORDED** | Недетерминизм числа `committee(2)` (9/8/9) при побайтно одинаковом остальном — найди по коду ВЕРОЯТНЫЙ источник (кто зовёт `committee(2)` вне детерминированного рантайма: фаза collect? `DebuggingRecorder`? tokio-поток теста?) — одним абзацем, без правки; Д-nn + В§3 для EXPERIMENTS. |
| B3-05 | **FIXED (текст)** | `speculative.is_empty()` недостижимо для чтения модуля по построению (`Anchor::executed_hash` — единственный источник, канон): переписать комментарий в тесте 1 (`committee_tests.rs:304-314`) и журнал §7.2 — счётчик пинует ФЕЙК/будущих читателей 4.2, не модуль. |
| B3-06 | **FIXED** | Тест 3: добавить `committee.scheme(2).is_none()` у каждого узла (через фасад/`Arc<dyn Committee>` в `Outcome`, что доступно) к проверке через `verifier_epochs`. |
| B3-07 | **FIXED** | `branching_rotation()` строить ИЗ `rotate_four_three_four()` (канонический арм = то же расписание), `vec![0,1,2,3]` → `(0..n)`. |
| B3-08 | **FIXED** | Тест 1 ассерт (а): равенство записей утверждать только по эпохам, где якоря узлов различаются (фильтр по `CommitteeFacts.anchor`, как `read_back` в тесте 2), и отдельно посылку «таких эпох ≥ 1». |
| B3-09 | **RECORDED** | Одной строкой. |
| B3-10 | **FIXED (журнал)** | Заголовок §1: дефолт фейка ИЗМЕНИЛСЯ (арм кольца), но ненаблюдаем в старых прогонах (макс. эпоха 6). |
| B3-11 | **RECORDED** | `logs` снимается до collect ⇒ `error!` из collect невидим — одной строкой + Д-nn (для стенда). |
| B3-12 | **RECORDED** | Продакшн: постоянный отказ не мемоизируется (`store.rs:191-207`, `:480-503`) — каждый `committee(E)` снова два staticcall'а; `reported` гасит только лог. Д-nn + В§3 для 4.2/Э7 (кандидат: отрицательный кэш до следующего `anchor_advanced`). В код не лезть. |
| B3-13 | **FIXED** | Два сообщения ассертов — перенос строки. |
| B3-14 | **FIXED (журнал)** | Пересчитать md5 `e2e/src/staking_commit.rs` по итоговому дереву. |
| B3-15 | **FIXED** | `committed_at` — через `DPOS_ACTIVATION_BLOCK`. |
| B3-16 | **RECORDED** | Одной строкой. |

## Ворота (сам, verbatim по одной строке)

`cargo test -p fluentbase-consensus --lib` (671/0); `… --features dpos-devnet-byzantine testbed::` (43/0); `cargo test -p fluentbase-consensus --lib testbed::` (35/0); `cargo test -p fluentbase-consensus --lib testbed::committee_tests` ×3 (детерминизм — записать счётчики); `cargo test -p fluentbase-node --lib` (57/0); `cargo test -p fluentbase-staking-reader` (63/0); `cargo test -p fluentbase-e2e --release a_committee_is_first_committable` (1/0); `cargo clippy -p fluentbase-consensus -p fluentbase-node --all-targets` и с фичей (одна чужая `large_enum_variant`); `cargo clippy -p fluentbase-e2e --all-targets` (0); `cargo fmt --check` по своим файлам; `cargo doc -p fluentbase-consensus --no-deps 2>&1 | grep -c "unresolved link"` (8).

## Журнал — часть В в конце `.dpos-study/history/E4-1-B3.md`

`# Часть В — заход Б3, третий проход: находки ревью E4-1-B3-REVIEW.md`. В§0: (1) `FIXED n · RECORDED m · REJECTED k`; (2) ворота verbatim (+ три прогона детерминизма); (3) изменённые/новые ассерты — что утверждает / мутация verbatim; (4) новые Д-nn (после Д-59); (5) что теперь ТОЧНО пинует каждый из трёх стенд-тестов и e2e-пин (одно предложение на тест — для закрытия строки 4.1 в `REGISTER.md`: R-027, R-040, R-019, R-076, R-075, R-123 — назови, какие из них какой тест пинует, а какие — нет). В§1 таблица `id | исход | что сделано | file:line`. В§2 раскладка логических коммитов (проверить `cargo check --all-targets`). В§3 где проверка слабее всего / для 4.2, 4.3, Э7.

В финальном ответе — только В§0.

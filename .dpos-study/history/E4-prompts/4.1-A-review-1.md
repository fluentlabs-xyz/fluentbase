# Ревью строки 4.1, заход А (модуль `committee/`, фасад, тесты модуля)

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база — `HEAD` (`git rev-parse HEAD`), объект — **незакоммиченное рабочее дерево** (`git diff HEAD`, `git status --short`; файлы под `devnet/` — чужая работа, игнорировать). Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`; `reader/` = `crates/dpos/staking-reader/src/`.

## Правила

1. **Агентов не запускать** — один контекст должен держать спеку, дифф и журнал одновременно.
2. **Git только на чтение.** Никаких правок файлов вообще, кроме одного: результат — `.dpos-study/history/E4-1-A-REVIEW.md`.
3. Каждое утверждение — с `file:line` дерева и, где есть «до», `HEAD:file:line`. Утверждение без якоря не засчитывается.
4. Комментарии, док-комментарии, `.claude/dpos_architecture/` и сам журнал исполнителя — НЕ свидетельство поведения; в этом коде они дрейфуют. Свидетельство — код и прогон, который ты сделал сам.
5. **Полнота, не фильтр**: перечисляй всё найденное с серьёзностью (BLOCKER / SERIOUS / MODERATE / MINOR / NIT); отбор делает оркестратор. Каждой находке — «чем пытался опровергнуть и почему не вышло».
6. Читать файлы целыми строками (Read / `sed -n 'N,Mp'`), никогда `cut -c`/`head -c`.

## Вход

- Спека: `.dpos-study/PLAN.md:54` (строка 4.1; одна длинная строка), `.dpos-study/history/E4-CORE-DESIGN.md` §3 (`357-367`), §5.1 (`443-505`), §5.4 (`549-572`), §5.5 (`574-577`), §6 строка П-1 (`594-600`), §7 пункт «4.1» (`~683`); `.dpos-study/history/E4-PRECONDITIONS.md` §0.3, §7.
- Журнал исполнителя: `.dpos-study/history/E4-1-A.md` — читать ПОСЛЕ диффа, чтобы сверять его с деревом, а не дерево с ним.
- Дифф: `git diff HEAD --stat`, затем `git diff HEAD -- <файл>` по каждому файлу; новые файлы (`git status --short` `??`) — целиком.
- Контекст в коде: `beacon/plane.rs:100-148` (трейт `CommitteeReads`, который фасад реализует); `reader/reader.rs:179-237`, `:616-720`, `:760-800`; `reader/error.rs`; `executed.rs`; `application.rs:100-175`; `executor.rs:1080-1090`, `:1145-1152`, `:2438-2455`, `:3288-3295`, `:3560-3570`; `outer.rs:238-420`; `scheme.rs`; `crates/types/src/staking_protocol.rs`; `contracts/staking/src/consts.rs:355-380`; `testbed/fakes.rs:840-1060` (как выглядит продовый фейк контракта — для сравнения с тестовым reader'ом модуля).

## Форма результата `.dpos-study/history/E4-1-A-REVIEW.md` (как `history/E5-0-REVIEW.md` §0–§6)

**§0 Прямые ответы** (нумерованные, каждый с [KNOWN]/[ГИПОТЕЗА]):
1. Ворота — прогнать САМОМУ и вставить verbatim по одной строке: `cargo test -p fluentbase-consensus --lib` (база до захода 645/0); `cargo test -p fluentbase-consensus --lib --features dpos-devnet-byzantine testbed::` (40/0); `cargo test -p fluentbase-node --lib` (59/0); `cargo test -p fluentbase-staking-reader` (59/0); `cd contracts/staking && cargo test` (175/0); `cargo clippy -p fluentbase-consensus -p fluentbase-node --all-targets` и `cargo clippy -p fluentbase-consensus --all-targets --features dpos-devnet-byzantine` (число строк warning/error; одна чужая `large_enum_variant` в node известна); `cargo fmt --check` (число `Diff in`); `cargo doc -p fluentbase-consensus --no-deps 2>&1 | grep -c "unresolved link"` (9). Расхождения с журналом — назвать.
2. Держит ли модуль §5.1 по КАЖДОМУ пункту: якорь (`executed_state_hash(ordering_finalized)` — какой источник высоты выбран и совпадают ли его точки записи с `ordering_finalized`: проверь `executor.rs` сам); `commit_height(E) = start(E−2)`, для E ≤ 2 — блок 1; `NotReadable` без EVM (как пинуется — счётчик вызовов reader'а?); окно `[epoch(anchor)−8, epoch(anchor)+2]` и `OutOfWindow` без EVM; ровно два staticcall на одном хэше при первом чтении; пустой комитет ⇒ `NotReadable`, никогда permanent; `weights: None` ⇒ `Read(permanent)`; write-once + отказ на расхождение без `debug_assert`; ретенция `SCHEME_RETENTION_EPOCHS` через `pop_first`; `subscribe` — событийный, не таймерный; `is_member` без `tombstoned`; `ReadError::is_transient` ровно для трёх вариантов; `const_assert` против ОБЩЕЙ константы `WEIGHT_RING_EPOCHS` и то, что контракт теперь реэкспортирует её, не дублирует (значение 16 не изменилось).
3. Фасад `beacon::CommitteeReads`: `read_at` = хэш якоря; `committee`/`committee_bls` игнорируют `at` (это по проекту) — но проверь, что `committee_pair` (provided-метод, `plane.rs:143-147`) над фасадом консистентен по write-once и что `dkg_qual` отдаёт `(changed, true)`/`None`, не `(false,false)` на `NotReadable` (страж `carry.rs:238-240` ждёт `None` = «не решено»); есть ли путь, где фасад заморозит `frozen_dkg_qual` на `false` (класс Д-14 журнала 5.0).
4. Тесты: счёт `#[test]` HEAD vs дерево по каждому тронутому файлу; для каждого нового теста — утверждает ли он то, что называет (тавтологии, «вакуумные» тесты, фейк, который не может отказать); есть ли тест, который прошёл бы и на HEAD (не был красным) — назвать; расходится ли тестовый reader модуля с продовым `reader.rs` по форме ответа (пустой комитет = `Ok` с пустым `validators` и `weights: Some(vec![])`, `weights: None` = кольцо обернулось; порядок — verbatim контракта).
5. Граница: список `pub` элементов `committee/` — что из них имеет потребителя в дереве, что появится только в заходе Б, и нет ли лишнего; не тянет ли модуль beacon-типы (кроме трейта `CommitteeReads` для фасада).
6. Продакшн-путь: `unwrap`/`expect`/`#[allow]`/`todo!`/таймеры/`dbg!` — перечислить с якорями; `error!`-спам (один раз на эпоху или на каждый вызов?).
7. Где журнал вводит в заблуждение: утверждения журнала, не подтверждённые деревом (каждое — с якорем).
8. Что из §5.1 оказалось неверным против кода (если что).

**§1 Находки** — таблица `id | серьёзность | file:lines (дерево) | HEAD-якорь | что не так | чем пытался опровергнуть и почему не вышло | уверенность`.
**§2 Поведение по ханкам против HEAD** — по каждому тронутому файлу: что изменилось в поведении (не в тексте), с якорями.
**§3 Граница** — таблица имён `pub` (модуль / имя / потребитель в дереве или «заход Б» / нужно ли).
**§4 Ворота** — вывод команд verbatim.
**§5 Оставить как есть** — непустой: что ты рассматривал как находку и отклонил, с причиной.
**§6 Замечено вне рамок 4.1-А** — для заходов Б/4.2/4.3.

В финальном ответе — только §0 (кратко) и таблица §1.

## Вопросы оркестратора (проверить и ответить в §0 отдельными пунктами)

9. `committee/store.rs::install`: ретенция `pop_first` до `SCHEME_RETENTION_EPOCHS` (8) записей при окне в 11 эпох (`[epoch(anchor)−8, epoch(anchor)+2]`) — эпоха в окне ниже восьми самых новых будет перечитана заново (ещё два staticcall), и write-once на неё не действует. Это дефект против §5.1 («запись write-once», «одна ретенция») или приемлемо? Есть ли потребитель, который читает столь старые эпохи (слэшер `[cursor−1, cursor]`, marshal-схемы до 8)?
10. `CommitteeError::is_transient()` даёт `false` на `OutOfWindow` и ВЫШЕ окна (эпоха станет читаемой с ростом якоря). Кто из будущих потребителей (ET на границе трекает `E+1`; проба ступени `committee(T+1)`; `on_confirm` с `target_epoch ∈ [now, now+2]`) может спросить эпоху выше `epoch(anchor)+2`, и что он сделает с `false`?
11. Порядок шагов в `committee()`: кэш (шаг 3) проверяется ПОСЛЕ окна и readable — запись, попавшая в кэш, может ли стать «вне окна снизу» и тогда отдаваться `OutOfWindow` вместо кэша? Это ожидаемо по §5.1 или нет?

# Строка 4.1, заход Б2′, третий проход — находки ревью `E4-1-B2p-REVIEW.md`

Ты — исполнитель (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `3a4a0fde`; объект — незакоммиченное рабочее дерево захода Б2′: четыре файла `crates/dpos/staking-reader/src/epoch_transition.rs`, `crates/dpos/consensus/src/dpos.rs`, `crates/dpos/consensus/src/lib.rs`, `crates/node/src/dpos.rs`. **Всё остальное в `git status` — чужое** (`contracts/staking/**`, `crates/staking-abi/src/lib.rs` — параллельная сессия; `devnet/**`; `target-contract/`; `.dpos-study/history/E4-ORCHESTRATOR.md`) — не трогать. Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`; `reader/` = `crates/dpos/staking-reader/src/`.

## Правила

1. **Агентов не запускать.** 2. **Git только на чтение.** 3. **Файлы на запись**: те же четыре + `testbed/stand.rs` (B2p-06 и, если нужен стенд-тест, конец файла), `testbed/tests.rs` (новые тесты — в конец; старые ассерты не трогать), `.dpos-study/history/E4-1-B2p.md` (дописать **часть В** в конец; части выше не переписывать). Запрещено всё остальное (`committee/**`, `epoch_manager.rs`, `executor.rs`, `beacon/**`, `application.rs`, `.claude/**` — доки правит оркестратор). 4. file:line на каждое утверждение. 5. Без `#[allow]`/`todo!`/таймеров/поллинга-вместо-события; `unwrap` на продакшн-пути — только `Mutex::lock().unwrap()`. 6. Комментарии — не свидетельство. 7. Читать целыми строками. 8. Тест ДО правки, красный до неё (verbatim); где невозможно — мутация. 9. `CARGO_BUILD_JOBS=6`; `Blocking waiting for file lock` — ждать. 10. Старые тесты (668/40/57/60/16) не трогать по ассертам.

## Что прочитать

1. `.dpos-study/history/E4-1-B2p-REVIEW.md` целиком (§0, §1 B2p-01…B2p-08, §5, §6).
2. `.dpos-study/history/E4-1-B2p.md` §0, §1, §3 (Д-41…Д-44), §7, §8; `.dpos-study/history/E4-1-B2.md` часть В как образец формы части В.
3. `.dpos-study/history/E4-CORE-DESIGN.md` §5.1 «Один `EpochTransition`» (`:502-505`).
4. Код: `reader/epoch_transition.rs` целиком (особенно `freeze_or_warn` `:44`, поля `frozen_*` `:160-164`, `frozen_geometry` `:272`, `raise_anchor_height` `:295-322`, `on_finalized` `:340`, парковка `:398-424`, `apply_at` `:439-600` — бутстрап `:513-530`, граничная ветка `:584`, `track_and_trigger` `:615-682`, `cold_start` `:689-702`); `node/dpos.rs:1540-1600` (поллер: `frozen_before`, `cold_start`, `geometry_tx.send_replace` `:1580`, `committee_wake.anchor_advanced()`), `:1360-1400` (ET в плоскости), `:1950+` (`launch_dpos_layer`); `dpos.rs:2080-2110` (`cold_start` слоя), `:2200-2260` (`enter_boundary` → `on_finalized`), `:2320-2345` (`boundary_hook`, `raise_anchor_height`); `beacon/plane.rs:820-840` (кто ждёт `geometry_tx`/`geometry_ready` — читать, не править); `testbed/stand.rs:1550-1600`, `:2095-2110`, `:2150-2190`.

## Решения оркестратора (исполнить ровно так; отклонение — только с причиной file:line в В§0)

| id | решение | что сделать |
|---|---|---|
| B2p-01 + B2p-02 + B2p-03 + B2p-08 | **FIXED — одним движением: у инстанса ОДИН бутстрапер и ОДНО событие заморозки** | Корень (ревью, последняя строка): слиты инстансы, не события. Сделать так: (1) **поллер плоскости НЕ зовёт `cold_start`**. Ему нужна только геометрия — дай `EpochTransition` метод `freeze_geometry(&mut self, at: B256, number: u64) -> Result<bool /*frozen now*/, ReadError>` (или имя по вкусу), который делает РОВНО ту часть `apply_at`, что резолвит и замораживает `(activation, interval)` через `freeze_or_warn` (`:44`) — без бутстрапа эпохи, без `track`, без `anchor_height`, без парковки; идемпотентен (уже заморожено ⇒ `Ok(false)`). Поллер до заморозки зовёт его; после — не трогает ET вовсе (тумбстоуны остаются). (2) **публикация геометрии — по факту заморозки, независимо от вызывающего**: поллер на каждом тике до публикации проверяет `frozen_geometry()` и, если `Some` и ещё не опубликовано, делает `geometry_tx.send_replace(Some(frozen))` + `committee_wake.anchor_advanced()` — то есть если слой заморозил первым (`dpos.rs:2096`), поллер всё равно публикует на следующем тике. Проверь по `beacon/plane.rs:820-840`, что приёмник не зависит от того, КТО заморозил. (3) **`cold_start` (бутстрап эпохи + пол + мост) остаётся ТОЛЬКО у слоя** (`dpos.rs:2096`, ordering-шкала, после прыжка) — единственный писатель `last_tracked_epoch` на старте; `apply_at` на бутстрапе тогда всегда идёт в ветку `last_tracked_epoch.is_none()` ⇒ третий парковщик (B2p-03) и падение старта из граничной ветки (B2p-08) исчезают структурно — ЗАПИШИ это как следствие с якорями, а `debug_assert` парковки (`:419-424`) оставь верным по факту. (4) `cold_start` внутри всё ещё замораживает геометрию (слой может быть первым) — через тот же `freeze_geometry`, без дублирования. **Тесты (до правки, красные):** (а) ET-юнит: `freeze_geometry` замораживает и НЕ трогает `last_tracked_epoch`/`anchor_height`/мост (красный — метода нет; после правки мутация: заставить его бутстрапить ⇒ красный); (б) ET-юнит на B2p-02(i): инстанс, у которого первый вызов — на высоте `start(E)−K+1`… точнее: «бутстрап на EL-высоте в окне K после границы выбирает `E−1`, бутстрап на ordering-высоте — `E`» — пин того, ПОЧЕМУ бутстрапер должен быть один и в ordering-шкале (тест может быть зелёным на HEAD-семантике `apply_at`; тогда он документирует свойство, а красным делай стенд/поллер-тест ниже); (в) поллер-тест на B2p-01, если поллер тестируем в `node/dpos.rs` `mod tests` (посмотри, есть ли там тесты поллера; если нет — стенд-эквивалент невозможен, тогда запиши в В§3 и покрой (1)–(2) чтением с якорями + ET-юнитом (а)). Д-nn. |
| B2p-04 | **RECORDED** | Пре-движковое окно (холодный прыжок до 6 ч) без обновлений `oracle.track` — на HEAD плоскостной ET трекал по `fin` каждую границу. Смягчение — трекается активный реестр. Решение проекта — 4.3 (`TrackedPeers{primary, secondary}` с обновлением по модулю, не по ET). В В§0 одной строкой + Д-nn + В§3 для 4.3 с якорями; если после (1)–(3) поллер может дёшево звать `track` для текущей EL-эпохи БЕЗ бутстрапа и без записи `last_tracked_epoch` — не делать (второй писатель peer-set'а — тот же класс R-075), только записать. |
| B2p-05 | **RECORDED** | Доки `.claude/dpos_architecture/` — правит оркестратор в Ф7 (два абзаца 06 §6.3 + 08 §8.3 + `verified-against`). Одной строкой. |
| B2p-06 | **FIXED** | `testbed/stand.rs:2098-2107` («the OTHER production transition»), `:1556-1560`, `:1579-1581` — переписать комментарии по факту (один инстанс; драйвер = хук по каждому блоку; якоря по итоговому дереву). |
| B2p-07 | **FIXED** | `lib.rs:89-92` — убрать неиспользуемый реэкспорт `PlaneEpochTransition` (потребитель ходит через `dpos::`); если после удаления что-то не собирается — оставить с причиной. |

## Ворота (сам, verbatim по одной строке)

`cargo test -p fluentbase-consensus --lib` (668 + новые); `… --features dpos-devnet-byzantine testbed::` (40/0); `cargo test -p fluentbase-node --lib` (57 + новые); `cargo test -p fluentbase-staking-reader` (60 + новые); `cargo test -p fluentbase-consensus --test slasher_integration` (16/0); `cargo clippy -p fluentbase-consensus -p fluentbase-node --all-targets` (одна чужая `large_enum_variant`) и с `--features dpos-devnet-byzantine`; `cargo fmt --check` — по своим файлам (чужие диффы `contracts/` не считать); `cargo doc -p fluentbase-consensus --no-deps 2>&1 | grep -c "unresolved link"` (8).

## Журнал — часть В в конце `.dpos-study/history/E4-1-B2p.md`

`# Часть В — заход Б2′, третий проход: находки ревью E4-1-B2p-REVIEW.md`. В§0: (1) `FIXED n · RECORDED m · REJECTED k`; (2) ворота verbatim; (3) тесты — имя / что утверждает / красный до правки verbatim или мутация; (4) новые Д-nn (после Д-44); (5) кто теперь единственный писатель `last_tracked_epoch` на старте и единственный публикатор геометрии — цепь file:line на валидаторе; (6) что в §5.1 «Один `EpochTransition`» по-прежнему неверно/недосказано по коду. В§1 таблица `id | исход | что сделано | file:line`. В§2 раскладка логических коммитов (одна группа? проверить `cargo check --all-targets`). В§3 где проверка слабее всего / для 4.2, 4.3.

В финальном ответе — только В§0.

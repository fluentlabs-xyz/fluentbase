# Строка 4.1, заход А, третий проход — находки ревью `E4-1-A-REVIEW.md`

Ты — исполнитель (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `17a1e1ed`; объект — незакоммиченное рабочее дерево захода А (модуль `crates/dpos/consensus/src/committee/`, правки в `lib.rs`, `application.rs`, `staking-reader/src/error.rs`, `types/src/staking_protocol.rs`, `contracts/staking/src/consts.rs`). Пути без префикса — от `crates/dpos/consensus/src/`.

## Правила

1. **Агентов не запускать.** 2. **Git только на чтение.** 3. **Файлы на запись**: `committee/**`; `application.rs` (только `FinalizedCursor`, если нужно); `crates/dpos/staking-reader/src/error.rs` (только если решишь добавить варианты — см. R-НЕТ ниже: НЕ добавлять); `.dpos-study/history/E4-1-A.md` (дописать **часть В** в конец файла — не переписывать части выше). Всё остальное — запрещено (`beacon/**`, `node/**`, `outer.rs`, `executor.rs`, `testbed/**`, `Cargo.toml`, `contracts/**`, `devnet/**`). 4. Каждое утверждение — с file:line. 5. Без `#[allow]`/`todo!`/таймеров/`unwrap` на продакшн-пути (кроме `Mutex::lock().unwrap()`, уже Д-6). 6. Читать целыми строками, никогда `cut -c`.

## Что прочитать

1. `.dpos-study/history/E4-1-A-REVIEW.md` целиком (§0 п.9–11 — ответы на вопросы оркестратора; §1 таблица R-01…R-18; §5 «оставить как есть»).
2. `.dpos-study/history/E4-1-A.md` §3 (Д-1…Д-10), §4, §6, §7.
3. `.dpos-study/history/E4-CORE-DESIGN.md` §5.1 (`443-505`), §5.4 (`549-572`).
4. `committee/mod.rs`, `committee/store.rs`, `committee/facade.rs`, `committee/tests.rs` целиком.
5. Образец формы части В: `.dpos-study/history/E5-0-BOUNDARY.md:914-1000` (В§0), `:1068-1096` (В§1 таблица прослеживания), `:1135-1154` (В§4).

## Решения оркестратора по каждой находке (исполнить ровно так; отклонение — только с причиной file:line в В§0)

| id | решение | что сделать |
|---|---|---|
| R-01 | **FIXED** | Ретенция — по ПОЛУ ОКНА, не по счёту: при `anchor_advanced()` и в `install` удалять записи с `epoch < lo` (`lo = epoch(anchor) − SCHEME_RETENTION_EPOCHS`); карта тогда ≤ 11 записей и write-once держится на всём окне (детектор форка контракта работает на всех эпохах, которые модуль вообще читает). `reported` — тот же пол. Одна константа ретенции остаётся `SCHEME_RETENTION_EPOCHS` — это отклонение от буквы §5.1 («`pop_first` как `outer.rs`»), записать как Д-11 с причиной (окно 11 > 8). Тест #10 переписать: после подъёма якоря эпохи ниже `lo` выпадают, эпохи в окне остаются и НЕ стоят второго staticcall'а; плюс тест «второе, другое значение по эпохе в нижней трети окна ⇒ отказ, а не молчаливая замена». Док `mod.rs:15-17` и `:209` привести к одному утверждению. |
| R-02 | **FIXED** | `CommitteeError::is_transient()`: `OutOfWindow` ВЫШЕ окна (`epoch > hi`) ⇒ `true` (якорь дорастёт), НИЖЕ (`epoch < lo`) ⇒ `false`. Порядок проверок (окно → readable) не менять — оба без EVM; но `NotReadable{ready_at}` для эпохи выше окна не нужен: `OutOfWindow{hi}` уже говорит, до какой эпохи дорасти. Записать Д-12 (уточнение §5.1: транзиентность окна направленная). Тест: `E = hi+1` ⇒ `OutOfWindow`, `is_transient() == true`; `E = lo−1` ⇒ `false`. Док `mod.rs:193-199` переписать. |
| R-03 | **FIXED** | Фасад не принимает якорь: `Committee` получает метод `fn anchor_hash(&self) -> Option<B256>` (хэш якоря модуля или `None`, если не исполнен) — фасад строится из ОДНОГО `Arc<dyn Committee>`; `CommitteeReadsFacade::new(inner)`. Тест фасада — через тот же путь. |
| R-15 | **FIXED** | `anchor_advanced(&self)` — на трейте `Committee` (executor держит `Arc<dyn Committee>`; вызов у потребителя безвреден — перечитывает якорь). Док: кто зовёт и когда (три точки executor'а — заход Б). |
| R-07 | **FIXED** | Метрики через `metrics::counter!` как в `outer.rs` (`ORACLE_DROP_REFUSED`): `dpos_committee_read_permanent_total{reason}` (weights_none / non_unique_keys / fork / read_error / anchor_fault), `dpos_committee_out_of_window_total{side}` (above/below), `dpos_committee_not_readable_total`. Имена — `pub(crate) const`. `warn!` спустя `interval` блоков на пустом комитете (§5.4 строка 1) — НЕ делать: интервал модулю не нужен, метрика достаточна; записать в В§0. |
| R-08 | **FIXED** | `weights.len() != members.len()` ⇒ `Read(permanent)` (`AbiDecode`), тест. |
| R-09 | **FIXED** | Строить `EpochCommittee` через `crate::scheme::epoch_committee_from_snapshot(&snap)` ДО разбора `members` — одна деривация в крейте. |
| R-16 | **FIXED** | После `from_pairs` (который уже отверг дубликат) `participants` собирать `Set::from_iter` без `_dedup`-глотания либо ассертом равенства `participants.len() == members.len()` ⇒ иначе permanent; одна политика. |
| R-10 | **FIXED** | `ready_at` — одно значение: «наименьшая высота якоря, при которой ЕСТЬ смысл повторить»: для `commit_height` — `commit_height(E)`; для неисполненного якоря — `anchor_height` (повтор, когда исполнится); для пустого комитета — `anchor_height + 1`. Док варианта переписать так, чтобы три ветки объяснялись одной фразой; иначе — заменить поле на `enum` причины (по вкусу, записать). |
| R-05, R-06 | **FIXED** | Следствие R-01: `reported` режется тем же полом; доки «cannot grow»/«logged ONCE» становятся верными — проверить и поправить текст. |
| R-11 | **FIXED** | В тестовом `FakeReads` `dkg_qual` тоже ветвится по хэшу; тест #5 утверждает равенство `changed` при РАЗНЫХ хэшах не тавтологически (фейк отдаёт один бит на обеих ветках для E=5 и разные — для E=7, где и состав разный). |
| R-12 | **FIXED** | Тест: второй staticcall (`dkg_qual`) отказывает `StateNotMaterialized` ⇒ `Read(transient)`, запись не создана, `reported` пуст; `CallReverted` ⇒ permanent + `reported`. |
| R-13, R-14 | **FIXED** | Тавтологичные ассерты убрать/заменить; комментарий шага 3 в `store.rs` переписать по факту (кэш после окна — ожидаемо: эпоха ниже `lo` отвечается `OutOfWindow`, не кэшем); тест фасада покрыть `NotReadable` и `Read` ⇒ `None`, не только `OutOfWindow`. |
| R-17 | **FIXED** | `is_member` через `participants` (упорядоченное множество), если у `Set` есть `contains`; иначе RECORDED. |
| R-18 | **FIXED** | Журнал §0.6 против §3 Д-6: одно число `unwrap`. |
| R-04 | **RECORDED** | Проводка — заход Б1 (карта уже написана: `FinalizedCursor` выходит из `ProviderExecutedChain` конструктором `with_cursor`). В В§0 одной строкой. |
| Д-4/Д-5 (варианты `ReadError`) | **RECORDED** | Новые варианты `ReadError` в А не вводить; остаётся `AbiDecode` с текстом. Записать. |

Что НЕ делать: не трогать `Geometry` (ленивая геометрия — заход Б1); не расширять `EpochReads`; не менять `FinalizedCursor` (кроме как если R-03 требует — не требует).

## Ворота (сам, verbatim по одной строке)

`cargo test -p fluentbase-consensus --lib` (659 + новые); `… --features dpos-devnet-byzantine testbed::` (40/0); `cargo test -p fluentbase-node --lib` (59/0); `cargo test -p fluentbase-staking-reader` (60/0); `cd contracts/staking && cargo test` (175/0); `cargo clippy -p fluentbase-consensus -p fluentbase-node --all-targets` (3 чужие строки) и с фичей (0); `cargo fmt --check` (0); `cargo doc -p fluentbase-consensus --no-deps 2>&1 | grep -c "unresolved link"` (9).

## Журнал — часть В в конце `.dpos-study/history/E4-1-A.md`

`# Часть В — заход А, третий проход: находки ревью E4-1-A-REVIEW.md`. В§0 Прямые ответы: (1) `FIXED n · RECORDED m · REJECTED k` с перечнем id; каждая REJECTED — с опровергающим якорем; (2) ворота verbatim; (3) новые тесты (имя / что утверждает / был ли красным до правки — здесь тест пишется ДО правки, так что ответ обязан быть «да, красный: <вывод>» либо честное «нет» с причиной); (4) новые строки Д-11, Д-12 (и далее); (5) что в §5.1 после этого прохода неверно по коду (для правки проекта оркестратором). В§1 Таблица прослеживания: `id | исход | что сделано | file:line`. В§2 Обновлённая раскладка логических коммитов (группы А§6 — что добавил проход В). В§3 Где проверка слабее всего.

В финальном ответе — только В§0.

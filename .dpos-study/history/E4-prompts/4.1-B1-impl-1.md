# Строка 4.1, заход Б1 — проводка модуля `committee/` в узел, follower и стенд; снятие трёх `CommitteeReads`-реализаций и `qual_read_at`; перевод внетрейтовых читателей

Ты — исполнитель (Opus 5) одной строки плана. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`; заход А уже закоммичен (см. `git log -6`). Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`; `reader/` = `crates/dpos/staking-reader/src/`.

## Правила (обязательные)

1. **Агентов не запускать** (никаких Agent/subagent/Task): один контекст должен держать и спеку, и код.
2. **Git только на чтение.** Никаких `git add/commit/stash/checkout/restore/reset/clean`.
3. **Файлы на запись — ТОЛЬКО этот список**; правка вне списка — не делать, записать в журнал §7 с причиной file:line:
   - `committee/**` — модуль (правки API, если проводка их требует; каждая — строка Д-nn);
   - `beacon/plane.rs` — ТОЛЬКО: удаление `qual_read_at` из трейта `CommitteeReads` (`:134` и док `:121-133`) и проекция `dkg_qual_for` (`:601-608`) на `read_at`; ничего больше в `beacon/`;
   - `beacon/follower.rs` — ТОЛЬКО проекция `:125-132` на `read_at`;
   - `node/dpos.rs`, `node/ordering.rs`, `node/cert_follow/mod.rs` — проводка `CommitteeStore`/`RethAnchor`/фасада на валидаторе; удаление `PlaneCommitteeReads`, `committee_cursor`, `qual_cursor` и их тестов; `evidence_committee_for` через модуль; tombstone-поллер на `executed_state_hash(fin)`;
   - `dpos.rs` (consensus) — follower: `FollowerCommitteeReads` → фасад; `committee_at`/`enter_finalized_epoch` через модуль; watchdog (`:2335-2408`) удалить; громкий отказ старта (`:1997-2017`) через `committee(epoch(anchor))`;
   - `slasher/actor.rs` — `resolve_committee` через модуль;
   - `slasher/gossip.rs` — тип `EvidenceCommitteeFor`, если меняется;
   - `application.rs` — удалить поле `FluentApp::randomness` и `with_randomness` (§5.5 проекта: читателей нет, только `Clone` и сеттер) — проверь grep'ом, что читателей действительно нет, и запиши;
   - `testbed/stand.rs`, `testbed/fakes.rs` — `impl committee::EpochReads for FakeStaking`; `StandCommitteeReads` → фасад над `CommitteeStore<FakeStaking>` с `Anchor` над `FakeChain` (высота = ordering-финализированный tip узла стенда — найди, где стенд её держит; хэш = `FakeChain::hash_at`); `committee_read_hash` и `live_height`-курсор в чтении комитета удалить;
   - `outer.rs`, `epoch_manager.rs`, `engine.rs`, `cert_inlet.rs`, `node/cert_inlet.rs`, `epoch_transition.rs` — **НЕ в этом заходе** (заход Б2: `EpochSchemeProvider` как вид на карту, продюсеры, `CertInlet.schemes`, один ET, `latest_live`). Если проводка Б1 упирается в них — остановись на границе, оставь текущий путь и запиши в §7;
   - `.dpos-study/history/E4-1-B1.md` — журнал.
   Запрещено: `devnet/**`, `Cargo.toml`, `contracts/**`, `executor.rs` кроме ТРЁХ вызовов `anchor_advanced()` (см. Б1.1), `beacon/**` сверх названного.
4. **Каждое утверждение в журнале — с file:line** (дерево, либо `HEAD:file:line`).
5. Запрещены без строки Д-nn: `#[allow]`, `todo!`/`unimplemented!`, таймеры/поллинг вместо событий, `unwrap`/`expect` на продакшн-пути, `dbg!`.
6. Комментарии и `.claude/dpos_architecture/` — не свидетельство; свидетельство — код и прогон.
7. Читать целыми строками (Read / `sed -n`), никогда `cut -c`/`head -c`.
8. Поведение vote/cert-пути и стенда не должно меняться: стенд 40/0 обязан остаться 40/0 без правки ассертов старых тестов; если старый тест краснеет — это находка, не повод править тест (записать в §7, разобрать причину).

## Что прочитать ДО кода

1. `.dpos-study/PLAN.md:54` (строка 4.1); `.dpos-study/history/E4-CORE-DESIGN.md` §5.1 (`443-505`) — особенно таблица «Кто читает через `CommitteeReads`» и абзацы «Кто читает через модуль после 4.1», «`tombstoned`»; §5.5 (`574-577`); §7 пункт 4.1 «наполовину».
2. Журнал захода А `.dpos-study/history/E4-1-A.md` целиком, включая часть В (третий проход: Д-11 ретенция по полу окна, Д-12 направленная транзиентность `OutOfWindow`, Д-13 методы трейта `anchor_advanced`/`anchor_hash`, метрики) (особенно §3 Д-1…Д-10 и §7 «что именно ломает удаление `qual_read_at`», «что executor должен звать», «продакшн-проводки нет»); ревью `.dpos-study/history/E4-1-A-REVIEW.md` §1 и третий проход (часть В журнала А, если есть).
3. Модуль: `committee/mod.rs`, `committee/store.rs`, `committee/facade.rs` целиком.
4. Код: `beacon/plane.rs:100-148`, `:580-610`; `beacon/follower.rs:115-135`; `node/dpos.rs:1080-1110`, `:1175-1230` (`build_beacon_plane` — где строится плоскость и `geometry_tx`), `:1383-1500`, `:1520-1545`, `:1640-1760`, `:1815-1870`, `:2150-2170` (`ProviderExecutedChain::new`), `:2495-2540`; `node/ordering.rs:1-60`; `node/cert_follow/mod.rs:110-135`; `dpos.rs:1990-2060`, `:2335-2410`, `:3416-3490`, `:3655-3720`; `slasher/actor.rs:700-750`; `slasher/gossip.rs:40-100`; `application.rs:270-280`, `:350-360`, `:395-400`, `:430-442`; `testbed/stand.rs:1840-2000` и где стенд строит `executed`/ordering-финализированный курсор (grep `FinalizedCursor\|advance_finalized\|ordering_finalized` в `testbed/`); `testbed/fakes.rs:840-1060`; `executor.rs:1080-1090`, `:2445-2455`, `:3560-3570`.

## Что сделать (заход Б1)

### Б1.1. Один якорь у узла

- `FinalizedCursor` строится ОДИН раз до плоскости beacon (сегодня прячется в `ProviderExecutedChain::new`, `node/ordering.rs:29-34`, создаётся в `launch_dpos_layer` `node/dpos.rs:2160` и у follower'а `node/cert_follow/mod.rs:123`): добавь конструктор `ProviderExecutedChain::with_cursor(provider, cursor)` (или геттер `cursor()`), чтобы `RethAnchor` и `ProviderExecutedChain` держали ОДИН `FinalizedCursor`. Второго курсора быть не должно — это и есть свойство 1.
- Executor зовёт `Committee::anchor_advanced()` (метод ТРЕЙТА после третьего прохода А — `committee/mod.rs:270`; executor держит `Arc<dyn Committee>`) в трёх точках СТРОГО ПОСЛЕ `advance_finalized` (`executor.rs:1085`, `:2451`, `:3566`). Как передать в executor хэндл — на твой выбор (например, замыкание/трейт-объект в `executor::Config` рядом с тем, что там уже есть; или `FinalizedCursor` получает список подписчиков — предпочтительнее то, что не заводит второй канал). Записать выбор.
- Геометрия: `CommitteeStore` получает её ЛЕНИВО — `watch::Receiver<Option<(u64,u64)>>` от `geometry_tx` плоскости (`node/dpos.rs:1677`, `frozen_geometry()` ET) или эквивалент у follower'а (consensus `dpos.rs` знает `interval`/`dpos_activation_block` при старте); пока геометрии нет — `NotReadable{ready_at: 0}` без EVM. Если модуль А принимает `Geometry` по значению в `new` — измени API модуля (Д-nn) на ленивую геометрию; тесты модуля поправь минимально.

### Б1.2. Валидатор (`node/dpos.rs`)

- `PlaneCommitteeReads` (`:1400-1489`), `committee_cursor`/`qual_cursor` (`:1090-1110`) и их тесты (`:2502`, `:2522`) — удалить; на их место `committee::CommitteeReadsFacade::new(store)` (один `Arc<dyn Committee>`; `read_at` = `Committee::anchor_hash`); `live_height` из чтения комитета уходит (сам атомик `plane.live_height` остаётся, его пишет tee — заход Б2/4.2).
- `evidence_committee_for` (`:1815-1863`) — через модуль: `store.committee(epoch).ok().map(|r| r.bls.clone())`; memo одной эпохи не нужен (карта модуля и есть кэш) — удалить.
- Tombstone-поллер (`:1722`): курсор `provider.block_hash(fin)` → `executed_state_hash(&provider, fin)` (state-gated, §5.1 «tombstoned»); сам снимок по-прежнему читается reader'ом поллера (флаг живой, через модуль недоступен). `OracleHandle::block` остаётся.
- Один `Arc<CommitteeStore<…>>` на процесс (плоскость строится один раз); все потребители Б1 получают клоны `Arc<dyn Committee>`.

### Б1.3. Follower (consensus `dpos.rs`)

- `FollowerCommitteeReads` (`:3416-3487`) → фасад над `CommitteeStore` follower'а (якорь — тот же `FinalizedCursor`, что и у его executor'а; см. `node/cert_follow/mod.rs:123`).
- `committee_at` (`:3709-3715`) и `enter_finalized_epoch` → запись модуля (`ValidatorSetSnapshot`, который сейчас идёт в `boundary_rx`, оставь как есть в Б1 — собери снимок из записи или читай reader'ом как сейчас; в Б2 канал станет `epoch`-only; записать выбор).
- Watchdog (`:2335-2408`) — удалить целиком (проект §5.5).
- Громкий отказ старта (`:1997-2017`): условие — `store.committee(epoch(anchor))` не `Ok` ⇒ `bail!` с тем же операторским текстом; `initial_snapshot` для `cold_start_register` (`:2722-2730`) в Б1 остаётся (продюсеры — Б2), но читается из записи модуля, не вторым `epoch_committee_snapshot`.

### Б1.4. Slasher

- `resolve_committee` (`slasher/actor.rs:720-747`): через `Arc<dyn Committee>`: `Ok` ⇒ `(snap-эквивалент, record.bls)`; `NotReadable` и `Read(transient)` ⇒ `Transient` (R-027: пусто ⇒ Transient, не Permanent); `OutOfWindow` и `Read(permanent)` ⇒ `Permanent` (§5.4 «Улика слэшера об эпохе `< epoch(anchor) − 8`»). Что слэшер делает с `ValidatorSetSnapshot` дальше — проверь, нужен ли ему снимок или хватит записи; минимальная правка.
- Стенд-слэшер / `slasher_integration.rs` — только если ломается компиляция; тесты `crates/dpos/consensus/tests/` можно править под новый тип, без ослабления ассертов.

### Б1.5. Стенд

- `impl committee::EpochReads for FakeStaking` (`dkg_qual` уже inherent, `fakes.rs:991`).
- `StandCommitteeReads` (`stand.rs:1937-1998`) → `CommitteeReadsFacade` над `CommitteeStore<FakeStaking>` с `Anchor` стенда: высота — ordering-финализированный tip узла (найди, что у стенда играет роль `FinalizedCursor`; если стенд строит `executed` через свой `ExecutedChain`-фейк с курсором — используй его); хэш — `FakeChain::hash_at`. `committee_read_hash` (`:1850-1866`) удалить; `#[cfg(feature)]` roster-closure `:1878-1893` — на модуль.
- `FakeStaking` по ветке хэша, модель кольца и тумбстоуны, три новых стенд-теста — **заход Б2**, не здесь. Б1 обязан оставить 40/0.

### Б1.6. `FluentApp::randomness`

Удалить поле, `with_randomness` и ветку в `Clone`/`Default` (`application.rs:278`, `:356`, `:398`, `:438`); тесты `application.rs:1544`, `:1967` — убрать вызов сеттера. Сначала `git grep -n 'with_randomness\|\.randomness' crates/` — если читатель есть, НЕ удалять, записать.

### Б1.7. Ворота (прогнать самому, verbatim по одной строке)

`cargo test -p fluentbase-consensus --lib` (база после А: 659/0); `… --features dpos-devnet-byzantine testbed::` (40/0); `cargo test -p fluentbase-node --lib` (59/0 — минус удалённые два теста курсоров ⇒ ожидание 57/0, если ничего не добавлено); `cargo test -p fluentbase-staking-reader` (60/0); `cargo test -p fluentbase-consensus --test slasher_integration` (если тронут); `cargo clippy -p fluentbase-consensus -p fluentbase-node --all-targets` и с фичей; `cargo fmt --check`; `cargo doc -p fluentbase-consensus --no-deps 2>&1 | grep -c "unresolved link"` (9). Плюс `cargo check -p fluentbase-node --all-targets` (cert_follow тронут).

### Б1.8. Журнал `.dpos-study/history/E4-1-B1.md`

Форма — как `E4-1-A.md`: §0 Прямые ответы; §1 Инвентаризация «до» (18 вызовов `epoch_committee_snapshot` вне модуля на старте захода — таблица: сайт / что стало: «модуль» / «остаётся до Б2» / «остаётся до 4.2» / «удалён»); §2 Что сделано по файлам; §3 Отклонения Д-nn (нумерация продолжает А: Д-11…); §4 Тесты (новые/удалённые, «был ли красным»); §5 Ворота verbatim; §6 Логические коммиты (группы, компилируемость каждой — проверить `--no-run` реально, а не рассуждением); §7 Что всплыло — обязательно: список того, что Б2 обязан сделать (сайты, которые ещё читают контракт мимо модуля), и что executor теперь зовёт; §8 Слабее всего.

§0 обязан ответить: (1) сколько `FinalizedCursor` в процессе валидатора и follower'а после захода (ожидание: один); (2) сколько сайтов `epoch_committee_snapshot(` вне `committee/` осталось и какие (с якорями) — с разбивкой «Б2 / 4.2 / ET-собственные / tombstone»; (3) число `#[test]` HEAD vs дерево по файлам, удалённые тесты поимённо с причиной; (4) ворота одной строкой; (5) где §5.1 оказался неверен против кода; (6) запрещённое на продакшн-пути.

Заканчивай только когда всё из Б1.1–Б1.8 сделано и ворота зелёные; если что-то невозможно — сделай остальное полностью и назови в §7, что и почему.

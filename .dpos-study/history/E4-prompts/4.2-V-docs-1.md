# Ф7 строки 4.2, заход В — правка `.claude/dpos_architecture/` под код дерева

Ты — док-исполнитель (Opus 5). Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `016028af`; **код — незакоммиченное дерево В после третьего прохода** (`git status --short | grep -v devnet/`, файлы под `crates/`) — это истина; доки надо привести к нему. **Чужое** — `devnet/**`, `.dpos-study/history/E4-ORCHESTRATOR.md` — не трогать.

## Правила (обязательные)

1. **Агентов не запускать.** 2. **Git только на чтение.** 3. **Файлы на запись — ТОЛЬКО** `.claude/dpos_architecture/*.md`. Код — read-only. `.dpos-study/**` — read-only. 4. **Свидетельство — только код дерева** (`sed -n`, Read целыми строками; никогда `cut -c`/`head -c`); комментарии в коде — не свидетельство; каждая правка доки опирается на file:line, которую ты открыл. 5. Не читать файлы вне git-трекинга кроме `.claude/` и `.dpos-study/`; никогда `.env*`, `*.pem`, `*.key`, `*secret*`, `*credential*`. 6. **Форма правок — как в заходах Б1/Б2** (см. `04_cold_start_restart_crates_dpos_consensus.md` §4.1 и `09_followers.md` §9.6 — абзацы `**[REVISED 2026-09-12, Э4 row 4.2-Б2]**`): устаревший абзац НЕ удалять, а (а) переписать по факту, если он короткий, или (б) поставить перед ним `**[REVISED 2026-09-12, Э4 row 4.2-В]**`-абзац с новым состоянием и пометить старый как HISTORICAL/pre-В record. Ничего не выдумывать: где код молчит — дока молчит. 7. Никаких `cargo`-прогонов не нужно.

## Что прочитать

- `.dpos-study/history/E4-2-V.md` целиком (§0–§5) и `E4-2-V-REVIEW.md` §0–§1; постановки `E4-prompts/4.2-V-impl-1.md`, `4.2-V-fix-1.md`. Это КАРТА изменений; факты сверяй с кодом.
- Код: `epoch_manager.rs` — `live_epoch_of`/`is_live_epoch_at` (grep), `Actor::live_epoch`/`is_live_epoch`, поле `tip`, арм `tip_wake.changed()` в `run`, `reconcile_live`, `sweep_frontier`, `repair_keyless_schemes`, `reconcile_roles` (порядок «чтение `committee(epoch)` → `abort_below`»), сигнатуры `start`/`run` (без `vote_backup`); `application.rs` — поле `ordering_tip`, арм `Update::Tip` в `report`, метод `ordering_tip()`; `outer.rs` — `epoch_manager.start(None)`, `OuterEngine::start`/`run` без `vote_backup`; `dpos.rs` — `SharedBeaconPlane` без `vote_backup`, `local_tracked_epoch`; `node/dpos.rs` — список наблюдателей route-miss (vote backup там); `testbed/stand.rs` — дренаж vote-backup; `testbed/tests.rs` — новый тест (grep `a_node_three_epochs_behind_registers_the_schemes_and_spawns_no_engine`); юнит правила в `epoch_manager.rs` (grep `the_live_epoch_is_the_verified_tips_epoch`).

## Инвентарь дрейфа (grep оркестратора по `.claude/dpos_architecture/`, счётчики по файлам; `00_preamble.md` verified-against — история, НЕ править кроме новой записи)

`highest_observed_epoch` — 03:7, 13:1, 06:1, 00a:1; `corroborate_frontier` — 03:4; `PINS_PER_SENDER` — 03:1, 12:1; `CATCHUP_SPAN_CAP` — 03:3, 12:2, 13:1; `pipeline_catchup_span` — 03:3; `register_span` — 06:2, 03:1, 15:1; `latest_live` — 03:2, 06:1; `handle_msg_for_unregistered_epoch` — 03:1; `forward_vote_backup`/`vote_backup` — 03:4, 13:1, 08:1, 12:1; `VoteBackupItem` — 05:1, 13:1, 03:1; `ResettableForward` — 05:1; `is_live_epoch` — 03:3, 06:1; `their_epoch` — 03:1, 00a:1; `soft-enter` — 03:9, 13:1, 08:1 (не все устарели — soft-enter остался как понятие; править только фразы про корроборацию/фронтир по проводу/спан). Строки: `03_epoch_machinery.md` :29-44, :102, :298, :317, :344, :551-561, :608-659, :678; `06_staking_layer.md` :330-346, :558; `12_consensus_critical_constants.md` :57, :64-65; `13_invariants_gotchas_rules.md` :53, :859, :870; `08_…md` :2275; `05_identity_crypto.md` :112; `15_…md` :318.

## Что сделать

1. **`03_epoch_machinery.md` §3.2** — центральный раздел: (а) правило live-эпохи: `is_live_epoch(E) = E == live_epoch(tip)`, `live_epoch(tip) = epoch_of(tip)` и `E+1` при `tip == last(E)`; tip — `watch<u64>` от `FluentApp::report` (арм `Update::Tip`), посев 0, та же формула, что `dpos::local_tracked_epoch`; равенство вместо `>=`; (б) корроборация фронтира (`highest_observed_epoch`, `corroborate_frontier`, `observed_reporters`, `sender_pins`, `PINS_PER_SENDER`, `latest_live`), `pipeline_catchup_span`/`register_span`/`CATCHUP_SPAN_CAP`/`prune_resolved`/`catchup_no_progress`, `handle_msg_for_unregistered_epoch`, ребро `vote_backup` — УДАЛЕНЫ: REVISED-абзацы + HISTORICAL; (в) рёбра `select!` менеджера: новый арм `tip_wake` → `reconcile_live` → `reconcile_roles(live)` → `abort_below`; оговорка «`abort_below` после чтения `committee[live]`, при нечитаемом комитете движок мёртвой эпохи живёт до `committee_readable`» — если код это несёт; (г) `sweep_frontier = (live_epoch(tip), highest_entered)`; sweep НЕ будится по tip'у (Д-113 — отложено); (д) регистрация схем эпох впереди — по запросу marshal'а через `EpochSchemeProvider::scoped` → `CommitteeStore::scheme` (`outer.rs`, `committee/store.rs` — открой); (е) третий проход (F11, `E4-2-V.md` часть «В. Третий проход», Д-116…Д-121): ребро tip'а реконсилирует только при СМЕНЕ live-эпохи (мемо `tip_reconciled_live`, `epoch_manager.rs` grep), band-sweep `prune_agreements` стал edge-driven (мемо `agreements_swept_to`, гейт `cutoff > swept_to || aborted_one` — Д-117; это чинит стоимость, которая была и ДО захода: арм `committee_readable` реконсилирует живую эпоху на каждом дерайве); оговорка «`abort_below` после чтения `committee[live]`»; `committee_readable` НЕ срабатывает на заморозке геометрии (`committee/store.rs` `anchor_advanced` — ранний `return` без геометрии), а на первом дерайве после неё; второй стенд-тест `a_catching_up_member_takes_verify_only_at_every_boundary_below_its_own_tip` (`testbed/tests.rs`, партиция одного узла при неизменном комитете и `Beacon::Static`, `epoch_len=24`) — единственный стенд-пин гейта живости; Д-119 — ET `debug_assert` «two boundaries pending» при `epoch_len ≤ MAX_PENDING_ACKS + K` на догоне (`staking-reader/src/epoch_transition.rs:426`, по журналу; в `13` как gotcha для стенд-фикстур, если код это несёт — открой).
2. **`06_staking_layer.md`** `:330-346`, `:558` — упоминания `register_span`/`latest_live`/`highest_observed_epoch`/`is_live_epoch` — REVISED.
3. **`12_consensus_critical_constants.md`** `:57`, `:64-65` — `PINS_PER_SENDER`, `CATCHUP_SPAN_CAP`, `forward_vote_backup` — константы/механизмы удалены: строки таблицы пометить REVISED/удалено (не удалять строку).
4. **`13_invariants_gotchas_rules.md`** `:53`, `:859`, `:870` — REVISED; новое правило, если код его несёт: «live-эпоха — функция ТОЛЬКО проверенного marshal-tip'а и геометрии; ни один тег эпохи с провода не влияет на роль/фронтир/спавн» — сослаться на `live_epoch_of` и на то, что у `Actor` не осталось поля, которое пишет обработчик входящего кадра. Нумерация правил — продолжить после последнего номера (проверь, что номер не дублирован).
5. **`08_…md`** `:2275`, **`05_identity_crypto.md`** `:112` (`VoteBackupItem`/`ResettableForward` — УДАЛЕНЫ в третьем проходе, V-04; реэкспорт из `lib.rs` снят), **`15_…md`** `:318` (`register_span`), **`00a`** — точечные фразы REVISED.
6. **`00_preamble.md`** — новая запись verified-against `2026-09-12 **Э4 row 4.2-В: …**` по форме записи 4.2-Б2 (что изменилось, Д-111…Д-1nn, ворота — оркестратор гонит набор `v1v` параллельно с тобой: оставь в записи плейсхолдер `<<v1v>>` для чисел, оркестратор впишет сам; по журналу — lib 679/0; sections touched).
7. **Критерий приёмки (оркестратор проверит grep'ом)**: для каждого символа инвентаря `grep -n` по `.claude/dpos_architecture/*.md` даёт только (а) строки внутри абзацев с пометкой HISTORICAL/pre-В при REVISED-абзаце рядом, (б) `00_preamble.md` verified-against, (в) `00a` errata-история с пометкой. Всё прочее — дрейф, не закрыт.

## Отчёт (в финальном ответе)

Таблица: файл | секции/строки правок | что стало. Затем: список мест инвентаря, которые ты СОЗНАТЕЛЬНО оставил (с причиной), и список утверждений, которые не смог сверить с кодом (написал «по журналу»). Без журнала в `.dpos-study` — отчёт только в ответе.

# Строка 4.3, заход А — граница p2p: `TrackedPeers{primary, secondary}`, `Ingress` без EVM на входе, лимиты `deque_size`

Ты — исполнитель (Opus 5) одного захода строки плана. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `86fe7098` (строка 4.2 закрыта; заход В: live-эпоха по tip'у, vote-backup — наблюдатель route-miss). **В `git status` чужое** — `devnet/**`, `.dpos-study/history/E4-ORCHESTRATOR.md` — не трогать. Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`; `reader/` = `crates/dpos/staking-reader/src/`; `p2p/` = `crates/dpos/p2p/src/`.

## Правила (обязательные)

1. **Агентов не запускать.** 2. **Git только на чтение.** 3. **Файлы на запись — ТОЛЬКО**: `reader/epoch_transition.rs` (трейт `PeerSetSink`, `assemble_tracked_peers`, оба вызова `track`, тест-двойники), `p2p/lib.rs` (адаптер `OracleHandle`), `p2p/constants.rs` (только если нужен новый лимит), `beacon/actor.rs` (только вход `on_message`/`on_confirm`), `beacon/dkg_transport.rs` (`deque_size`), `slasher/gossip.rs` (вход `ingest_batch`), `outer.rs` (`deque_size`/байтовый лимит), `dpos.rs` (проводка `deque_size`, `Ingress`-фильтр), `node/dpos.rs` (проводка: evidence loop — передать `from`; список наблюдателей route-miss `:1258-1285` не трогать), `testbed/{fakes,stand,tests}.rs`, `.dpos-study/history/E4-3-A.md`. **Запрещены**: `committee/**` (читать можно), `executor.rs`, `cold_start_jump.rs`, `plane_upstream.rs`, `cert_inlet.rs`, `beacon/**` кроме двух названных файлов, `contracts/**`, `Cargo.toml`, `.claude/**`, `testbed/preconditions.rs`. 4. **file:line на каждое утверждение.** 5. Без `#[allow]`/`todo!`/таймеров/поллинга/счётчиков-штрафов; `unwrap` на продакшн-пути — только `Mutex::lock().unwrap()`. 6. Комментарии/доки/`.claude/dpos_architecture/` — не свидетельство; commonware — `.claude/COMMONWARE_INTERNALS.md` (peer sets `:363`, buffered, resolver) прежде checkout'а; checkout `~/.cargo/git/checkouts/monorepo-*` — если доки не отвечают (`tracker/directory.rs`, `record.rs`, `buffered/engine.rs`, `resolver/p2p/engine.rs`). 7. Читать целыми строками. 8. `CARGO_BUILD_JOBS=6`. 9. **Правила прогонов**: батч → один прогон затронутых тестов → в конце ОДИН `cargo test -p fluentbase-consensus --lib` и `cargo test -p fluentbase-staking-reader` (свой крейт) и `cargo fmt -p fluentbase-consensus -p fluentbase-node -p fluentbase-staking-reader -p fluentbase-p2p`; **полный набор ворот — у оркестратора**; «красный до правки» — два юнита А.5; мутаций ≤ 2; ×3 — новые стенд-тесты. 10. Старые тесты не ослаблять; покраснение — механизм. Не менять `MAX_COMMITTEE_SIZE`, `max_peer_set_size`, контракт.

## Что прочитать ДО кода

1. `.dpos-study/history/E4-CORE-DESIGN.md` §5.3 (`:534-548`) целиком; §5.4 строки про `Latest`/ступень (adressation `participants`); §10 §0.6/§0.8, A63, C.3 (grep). `DECISIONS.md` П-5 (peer-set), П-4. `PLAN.md:104` (строка 4.3). `REGISTER.md` R-054 (`:556`), R-029 (`:292`), R-013 (`:121`), R-037 (`:340`), R-023 (`:249`), R-096 (`:856`); `E4-CORE-DESIGN.md` E4-04 (`:378`), E4-12 (`:394`), E4-13 (`:396`), E4-14 (`:398`), E4-15 (`:400`).
2. `history/E4-PRECONDITIONS.md` Д5 (замер тел DKG) и Д-секция про secondary; `testbed/preconditions.rs:511` (`dkg_bodies_per_peer_…`), `:566` (`a_secondary_peer_…` — `TrackedPeers::new(primary, secondary)` на authenticated-транспорте — образец типа).
3. Код (якоря на HEAD `86fe7098`, открыты оркестратором 2026-09-12): `reader/epoch_transition.rs` — трейт `PeerSetSink` `:129-130`, `assemble_tracked_peers` `:655-697` (реестр ∪ `snap.validators` ∪ `committee[E+1]` через `reader.epoch_committee_snapshot`, `check_peer_set_size` `:696`), `track_peers` `:730-751` (TEMPORARY BRIDGE — дока `:724-729` уже называет целевую подпись), `track_and_trigger` `:759-766`, тест-синки `KeySink` `:1014`, `RecordingSink` `:1026`; `reader/reader.rs:729` `active_registry_peers`; `p2p/lib.rs:296-309` адаптер `OracleHandle`, `:374` `Provider::peer_set`; стенд `stand.rs:1041-1078` `TrackSink` (пишет `per_node`/`by_epoch`, форвардит в `oracle.manager().track`), посев peer-set (grep `manager().track`); BEACON — `beacon/actor.rs:1832` `on_message` (декод до проверки; `Confirm` ⇒ `on_confirm` `:1409` — `committee_for(target_epoch)` до членства), ceremony `handle(from, body)`; EVIDENCE — `node/dpos.rs:1792-1830` (`evidence_committee_for` через модуль `:1792-1800`, приём `Ok((_from, buf))` `:1812`), `slasher/gossip.rs:93` `ingest_batch` (`retains` → `committee_for`), `EvidenceCommitteeFor`; VOTE/CERT backup — после захода В все пять backup-приёмников Muxer'ов — наблюдатели route-miss (`node/dpos.rs:1258-1285`, `observe_route_misses`): кадр без маршрута считается и выбрасывается, `forward_vote_backup`/`handle_msg_for_unregistered_epoch` удалены (`48bf62ed`); лимиты — `outer.rs` `deque_size` (grep), `dpos.rs:2467`/`:3315` = 64, `stand.rs:2324` = 64, `beacon/dkg_transport.rs:123` `deque_size: MAX_COMMITTEE_SIZE`, `dkg_agree.rs:2227` `MAX_SET_LEN`, `order_block.rs:31` `MAX_ORDER_BLOCK_SIZE = 4 MiB`; модуль — `committee/mod.rs` (`Committee::committee(E)` → запись с `participants`; `geometry()`); стенд-сеть — `commonware_p2p::simulated` (тиры `network.rs:263-313`, `:602-620` по PLAN).

## Что сделать

### А.1 `TrackedPeers` одним коммитом в четырёх местах

`PeerSetSink::track(epoch, TrackedPeers<PeerPubkey>)`: primary = `C[E−1] ∪ C[E] ∪ C[E+1]` ИЗ ЗАПИСЕЙ МОДУЛЯ (не из `epoch_committee_snapshot` reader'а — покажи, откуда ET берёт модуль или почему пока читает reader; если ET не имеет `Arc<dyn Committee>` — назови проводку в `node/dpos.rs` и сделай), secondary = `active_registry_peers(at)`; `C[E−1]` — новый член союза; `C[E+1]` не закоммичен ⇒ как сегодня (debug, без него). Адаптер `p2p/lib.rs:306` — `Manager::track(index, TrackedPeers)`; стенд `TrackSink` — записывать primary/secondary раздельно (`Outcome` — новое поле `peer_sets[i]: Vec<(epoch, primary, secondary)>`), форвардить в `oracle.manager().track`; тест-двойники в `epoch_transition.rs`. `check_peer_set_size` — только primary (CW паникует на primary > `max_peer_set_size`; secondary не проверяется — `tracker/actor.rs:157-164` по докам). Запиши в §0: что изменилось в составе primary на живом узле (реестр ушёл в secondary — R-013/R-037/E4-14 закрываются этим), и что secondary НЕ набирается исходящим (кто дозванивается до кого — по CW `record.rs`).

### А.2 `Ingress` на BEACON и EVIDENCE (без EVM на входе)

Тип `Ingress<'a> { Member{peer, epochs: EpochMask}, Tracked(peer), Dropped }` — классификация по ПОСЛЕДНЕМУ `TrackedPeers` окна (источник: тот же набор, что ушёл в `track`, — держи его в `Arc<RwLock<…>>`/`watch` рядом с синком; НЕ читать `Provider::peer_set` у CW на каждый кадр); `epochs` — маска эпох `[E−1, E+1]`, в чьих записях peer состоит; tombstoned ⇒ `Dropped` до декода (источник — `TombstoneSet`, проверь, где он доступен). BEACON (`on_message`): `Member` для `msg.ceremony_epoch` ДО декода тела (`DkgMsg` заголовок — прочитай, что декодируется первым; если эпоха внутри тела — декод заголовка минимальный, тело — только для `Member`); `Confirm`: `target_epoch ∉ [now, now+2]` ⇒ drop ДО `committee_for` (`now` = текущая эпоха ceremony/геометрии — назови источник), затем `Member` по маске. EVIDENCE (`ingest_batch`): передать `from` из `node/dpos.rs:1845`; `Member` в маске ∧ эпоха в окне (`retains`) до `committee_for`. Своего штрафа/счётчика на пира не заводить; метрика отброса одна на канал (`dpos_ingress_dropped_total{channel, reason}`).

### А.3 VOTE/CERT backup — закрыто структурно заходом В

После `48bf62ed` у backup-кадров VOTE/CERT нет потребителя: приёмники — наблюдатели route-miss (`node/dpos.rs:1258-1285`), кадр для эпохи без движка считается метрикой и выбрасывается, EVM не трогается. `Ingress` на backup ставить НЕ НАДО — это подтверди по коду (что `observe_route_misses` не декодирует тело и не читает контракт; `epoch_from_subchannel` — только метка `kind`) и запиши в §0(3) как Д-nn «E4-04 закрыт удалением потребителя, не фильтром»; если найдёшь другой путь, где кадр backup-канала доходит до состояния/EVM — назови file:line и поставь `Ingress` там.

### А.4 Лимиты

BROADCAST `deque_size = 4` (`dpos.rs:2516`, `:3365`, стенд `:2329` — стенд тоже 4, иначе он не модель) + байтовый лимит на пира `4 × MAX_ORDER_BLOCK_SIZE` — найди, есть ли у CW `buffered::Config` байтовый лимит (доки/`buffered/engine.rs`); если нет — запиши как границу библиотеки, лимит по количеству остаётся; DKG bodies `deque_size = 2` (`dkg_transport.rs:123`; замер Д5 — 1 тело на (инстанс, отправитель) + 1 запас). Прогони `dkg_bodies_per_peer_…` (preconditions) и все стенд-тесты DKG (B1/B2/B3/C9, разрез сети) — покраснение = механизм.

### А.5 Юниты и стенд

(а) `epoch_transition` юнит: `track` получает `TrackedPeers` с primary = три комитета, secondary = реестр, реестровый не-член НЕ в primary (**красный до правки**: на HEAD реестр в primary); (б) `Ingress` классификация: не-член на BEACON/EVIDENCE ⇒ `Dropped`/`Tracked`, `committee_for` НЕ вызван (**красный до правки**: на HEAD `on_confirm` зовёт `committee_for` для любого отправителя — покажи счётчиком чтений); (в) стенд: «реестровый узел (secondary) получает сертификаты, его тела не в `buffered`» — `commonware_p2p::simulated` тиры (PLAN: `network.rs:263-313`, `:602-620`) — если стенд-сеть тиры не моделирует, запиши с file:line и поставь тест на authenticated-транспорте по образцу `preconditions.rs:566`; «кадр не-члена на backup/BEACON/EVIDENCE не вызывает `FakeStaking`-чтения и не меняет состояние» (`StakingReads`); (г) мутаций ≤ 2.

### А.6 Журнал `.dpos-study/history/E4-3-A.md` (короткая форма)

§0 Прямые ответы: (1) подпись и состав `TrackedPeers` (откуда C[E−1], модуль vs reader, что ушло в secondary); (2) `Ingress`: источник окна, маска, где стоит на каждом канале, что декодируется до проверки; (3) VOTE/CERT backup — подтверждение «потребителя нет» с file:line; (4) лимиты — числа, байтовый лимит есть/нет у CW, результат DKG-тестов; (5) `cargo test --lib` + reader verbatim, ×3; (6) стенд: тиры моделируются или нет, что доказывают два теста; (7) что в §5.3 оказалось неверно по коду; (8) запрещённое (ноль). §1 Д-nn (нумерация продолжается после Д-121). §2 тесты. §3 логические коммиты (четыре места подписи — ОДИН коммит). §4 где проверка слабее всего. §5 что осталось (4.3-Б если есть, Э5, Э7).

Заканчивай только когда всё сделано и lib/reader зелёные; невозможное — сделать остальное, назвать в §5. В финальном ответе — только §0.

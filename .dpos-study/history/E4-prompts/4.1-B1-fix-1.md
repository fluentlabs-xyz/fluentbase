# Строка 4.1, заход Б1, третий проход — находки ревью `E4-1-B1-REVIEW.md`

Ты — исполнитель (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `bf6d9f5e`; объект — незакоммиченное рабочее дерево захода Б1 (19 файлов под `crates/`; `.dpos-study/history/E4-ORCHESTRATOR.md` в диффе — чужое, не трогать). Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`; `reader/` = `crates/dpos/staking-reader/src/`.

## Правила

1. **Агентов не запускать.** 2. **Git только на чтение.** 3. **Файлы на запись**: `committee/**`; `executed.rs`; `reader/reader.rs` (ТОЛЬКО видимость `classify_transient_provider_error` — `pub`, док) и `reader/lib.rs` (реэкспорт); `slasher/actor.rs`; `crates/dpos/consensus/tests/slasher_integration.rs`; `node/dpos.rs`; `dpos.rs` (consensus); `lib.rs`; `cert_inlet.rs` (ТОЛЬКО док-комментарий `:322-333`); `beacon/surface.rs` (ТОЛЬКО док-комментарий `:630-634`); `testbed/stand.rs`, `testbed/tests.rs`; `.dpos-study/history/E4-1-B1.md` (дописать **часть В** в конец, части выше не переписывать). Всё остальное — запрещено (`beacon/**` сверх одного дока, `outer.rs`, `epoch_manager.rs`, `executor.rs`, `devnet/**`, `Cargo.toml`, `contracts/**`, `.claude/**` — доки правит оркестратор). 4. Каждое утверждение — file:line. 5. Без `#[allow]`/`todo!`/таймеров/`unwrap` на продакшн-пути (кроме `Mutex::lock().unwrap()`). 6. Читать целыми строками.

## Что прочитать

1. `.dpos-study/history/E4-1-B1-REVIEW.md` целиком (§0, §1 B1-01…B1-15, §5).
2. `.dpos-study/history/E4-1-B1.md` §3 (Д-15…Д-20), §7; `.dpos-study/history/E4-1-A.md` часть В (В§0, Д-11…Д-14).
3. `.dpos-study/history/E4-CORE-DESIGN.md` §5.1 (`443-505`), §5.4 (`549-572`).
4. Код: `committee/{mod,store,facade}.rs` целиком; `executed.rs` целиком; `reader/reader.rs:28-70` (`map_state_provider_err`, `classify_transient_provider_error`); `reader/error.rs:40-130`, `:150-170`; `slasher/actor.rs:600-730`; `tests/slasher_integration.rs:190-220`, `:750-780`; `node/dpos.rs:1380-1600` (курсор, стор, `geometry_tx`/заморозка `:1569-1571`); `dpos.rs:3100-3180` (follower: стор, курсор); `testbed/stand.rs:1660-1690`, `:1850-1880`; `testbed/fakes.rs:600-625` (`FakeChain::executed_state_hash`); `testbed/tests.rs:1850-1880`.
5. Форма части В: `.dpos-study/history/E4-1-A.md` — часть В (В§0…В§3) как образец.

## Решения оркестратора по каждой находке (исполнить ровно так; отклонение — только с причиной file:line в В§0)

| id | решение | что сделать |
|---|---|---|
| B1-01 | **FIXED** | Зонд якоря не должен превращать транзиентную ошибку провайдера в постоянную. В `executed.rs::executed_state_hash` ветка `Err(e)` от `block_hash` (и от `best_block_number`) классифицируется через `fluentbase_staking_reader::reader::classify_transient_provider_error(&e)` (сделать `pub` с доком; сегодня приватная, `reader/reader.rs:~50`): `Some(transient)` ⇒ этот транзиентный вариант, `None` ⇒ `Backend` как сейчас. `Ok(None)` при `height <= best` остаётся `Backend` (header-index fault). Тест в `executed.rs`: провайдер, отдающий `ProviderError::Database(DatabaseError::Decode)` и `StateForHashNotFound` ⇒ `Err(TransientStorage)`/`Err(StateNotMaterialized)`, `is_transient() == true`; прочая ошибка ⇒ `Backend`. Проверь grep'ом всех потребителей `executed_state_hash` (ET через `executed_hash`, inlet-пробы, `RethAnchor`) — кто матчит на `Backend` по имени и изменит ли поведение (ET считает любой `Err` — проверь `epoch_transition.rs`); записать. Д-nn. |
| B1-02 | **FIXED** | `RethAnchor::height()` = `max(cursor.height(), EL-finalized тег)`: у валидатора `provider.finalized_block_number().ok().flatten().unwrap_or(0)`, у follower'а — тот же провайдер (проверь, что `RethAnchor` follower'а строится над провайдером с `BlockNumReader::finalized_block_number`; если follower читает `CanonicalInMemoryState::get_finalized_num_hash`, дай `RethAnchor` второй конструктор/замыкание `fallback: Arc<dyn Fn() -> Option<u64>>`). Обоснование в доке: тег finalized reth — высота, финализированная этим же узлом раньше (ставится `executor.rs` из сертифицированных данных, `= ordering_finalized − K`), поэтому как якорь чтения комитета он безопасен и монотонен; курсор `FinalizedCursor` НЕ трогать (его Tier-F семантика — executor'а). Тест модуля: `Anchor` с курсором 0 и тегом N ⇒ окно от `epoch(N)`. Д-nn. |
| B1-03 | **RECORDED** | Голова очереди слэшера блокируется на транзиентном повторе (`slasher/actor.rs:640-668`) — структура повторов до Б1, теперь достижима чаще (пусто ⇒ транзиент, R-027). В код не лезть; в В§0 одной строкой + в В§3 «для Э7/4.3: повтор через отдельную очередь отложенных улик, не sleep в цикле продюсера». |
| B1-04 | **FIXED** | Заморозка геометрии обязана будить: там, где плоскость делает `geometry_tx.send_replace(Some(frozen))` (`node/dpos.rs:1569-1571`), сразу после — `committee.anchor_advanced()`; у follower'а — аналогично в точке, где его геометрия становится известна (найди; если у follower'а геометрия известна с рождения стора — записать, что окна нет). Тест модуля: стор с `GeometryRx = None` → подписчик молчит; после `send_replace(Some(..))` + `anchor_advanced()` приёмник получает `epoch(anchor)+2`. Док `subscribe` — назвать оба события (якорь, заморозка). |
| B1-05 | **RECORDED** | Доковый дрейф `.claude/dpos_architecture/` — правит оркестратор в Ф7 этого же захода (`.claude/**` тебе запрещён). В В§0 одной строкой. |
| B1-06 | **RECORDED** | Состояние «наполовину» между Б1 и Б2 — сознательное разбиение строки; Б2 запускается сразу после коммита Б1. Одной строкой. |
| B1-07 | **RECORDED** | Проекции `reads.committee(epoch, reads.read_at()?)` гейтят кэш модуля на неисполненном якоре; уходит с формой трейта в Э5 5.1; `beacon/**` не трогать. Одной строкой + якоря. |
| B1-08 | **FIXED (журнал)** | В части В — явная строка про follower-inlet-источник `dpos.rs:3678-3730` как оставшийся носитель для Б2. |
| B1-09 | **RECORDED** | Утрата операторской диагностики watchdog'а («finalized стоит, ключ не в комитете») — §5.5 предписывает удаление; замена (метрика/лог «я не член текущей эпохи» у `epoch_manager` при `is_member == false` и стоящем finalized) — кандидат для Э7; одной строкой. |
| B1-10 | **FIXED** | `testbed/stand.rs:1670-1685` — переписать оба комментария по факту (якорь стенда = `chain.tip()` как у продакшн-курсора; «qual reads … teed cursor» — удалить). |
| B1-11 | **FIXED** | Три протухших дока: `cert_inlet.rs:322-333` (`live_height` — курсор только follower'а до Б2), `node/dpos.rs:806-813` (tee больше не питает `committee_for`), `beacon/surface.rs:630-634` (`FluentApp` без `with_randomness`). |
| B1-12 | **FIXED** | `testbed/tests.rs:1875` — активация и длина эпохи из тех же констант/конфига, что у стенда (`DPOS_ACTIVATION_BLOCK`, `cfg.epoch_len`/`EPOCH_LEN` — одна величина). |
| B1-13 | **FIXED** | `Geometry` убрать из `pub use` в `lib.rs:82` (в модуле остаётся `pub` для тестов/фасада как нужно). |
| B1-14 | **FIXED** | `StandAnchor::executed_hash` — через `FakeChain::executed_state_hash` (`fakes.rs:609-619`), чтобы ветка `Err` была достижима; комментарий переписать. |
| B1-15 | **FIXED** | `tests/slasher_integration.rs`: `StubAnchor` умеет отдавать `Ok(None)`, `Err(transient)`, `Err(permanent)` и высоту по выбору теста; три новых теста: (а) транзиентный отказ зонда якоря ⇒ `Transient` (улика повторяется, не выбрасывается) — красный ДО правки B1-01 (проверить и записать вывод); (б) `OutOfWindow` НИЖЕ окна ⇒ `Permanent`; (в) `OutOfWindow` ВЫШЕ окна ⇒ `Transient`. Утверждать по наблюдаемому исходу (улика отправлена/дропнута/повторена), не по строке лога. |

## Ворота (сам, verbatim по одной строке)

`cargo test -p fluentbase-consensus --lib` (665 + новые); `… --features dpos-devnet-byzantine testbed::` (40/0); `cargo test -p fluentbase-node --lib` (57/0); `cargo test -p fluentbase-staking-reader` (60/0); `cargo test -p fluentbase-consensus --test slasher_integration` (13 + 3); `cargo clippy -p fluentbase-consensus -p fluentbase-node --all-targets` (3 чужие строки) и `cargo clippy -p fluentbase-consensus --all-targets --features dpos-devnet-byzantine` (0); `cargo fmt --check` (0); `cargo doc -p fluentbase-consensus --no-deps 2>&1 | grep -c "unresolved link"` (9).

## Журнал — часть В в конце `.dpos-study/history/E4-1-B1.md`

`# Часть В — заход Б1, третий проход: находки ревью E4-1-B1-REVIEW.md`. В§0: (1) `FIXED n · RECORDED m · REJECTED k` с id; (2) ворота verbatim; (3) новые тесты — имя / что утверждает / был ли красным до правки (тест пишется ДО правки; для B1-01 обязателен красный вывод); (4) новые строки Д-nn (нумерация после Д-20); (5) кто из потребителей `executed_state_hash` изменил поведение после B1-01 (с якорями); (6) что в §5.1/§5.4 после прохода неверно по коду. В§1 таблица прослеживания `id | исход | что сделано | file:line`. В§2 обновлённая раскладка логических коммитов Б1 (группы §6 журнала + что добавил проход В; проверить компилируемость групп реально, `--no-run`). В§3 где проверка слабее всего.

В финальном ответе — только В§0.

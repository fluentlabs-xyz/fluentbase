# Третий проход строки 4.2, заход Б2 — по ревью `E4-2-B2-REVIEW.md`

Ты — исполнитель (Opus 5) третьего прохода. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `f8ec4939`; объект — **незакоммиченное дерево Б2** (14 файлов под `crates/`, `git status --short | grep -v devnet/`). **Чужое** — `devnet/**`, `.dpos-study/history/E4-ORCHESTRATOR.md` — не трогать. Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`.

## Правила (обязательные)

1. **Агентов не запускать.** 2. **Git только на чтение.** 3. **Файлы на запись — ТОЛЬКО**: `dpos.rs`, `cold_start_jump.rs`, `executor.rs`, `plane_upstream.rs`, `outer.rs`, `testbed/{fakes,stand,tests,mod}.rs`, `.dpos-study/history/E4-2-B2.md` (часть В — дописать в конец; §0(6) — исправить на месте, B2-20). Остальное — запрещено (в т.ч. `.claude/**`, `node/**`, `committee/**`, `epoch_manager.rs`). 4. **file:line на каждое утверждение.** 5. Без `#[allow]`/`todo!`/таймеров/поллинга; `unwrap` на продакшн-пути — только `Mutex::lock().unwrap()`. 6. Комментарии/доки/журналы — не свидетельство. 7. Читать целыми строками. 8. `CARGO_BUILD_JOBS=6`. 9. **Правила прогонов**: все правки батчем → один прогон затронутых тестов → в конце ОДИН `cargo test -p fluentbase-consensus --lib` (682 ± изменения) и `cargo fmt -p fluentbase-consensus -p fluentbase-node`; **полный набор ворот НЕ гонять — он у оркестратора**; «красный до правки» — только для B2-02 и B2-12 (мутацией/точечным юнитом), мутаций ≤ 2 (`--exact`); ×3 — только если тронут стенд-тест. 10. Ничего не ослаблять; покраснение старого теста — находка (механизм), не подгонка.

## Вход

`.dpos-study/history/E4-2-B2-REVIEW.md` — читать целиком (§0–§6); `E4-2-B2.md` §0–§5 (свой контекст); `E4-prompts/4.2-B2-impl-1.md` (постановка Б2). Решения оркестратора — таблица ниже; спорить можно только фактом с file:line (тогда REJECTED с причиной в журнале, правку не делать).

## Решения по находкам

| id | решение | что сделать |
|---|---|---|
| B2-01 SERIOUS | **RECORDED** | Не чинить. В журнале В§0(1): механизм точно — тупик только «узел вне текущего реестра (его нет в чужих peer-set'ах) И все члены его старых `committee(T±1)` выключены»; ступень обслуживает любой ЖИВОЙ член (архив не прунится), а ротированный-но-живой узел принимает соединение от того, кто есть в его текущем peer-set'е. Выход по коду: `--dpos.follower-upstream` ⇒ `ValidatorUpstream::Ws` (`node/dpos.rs:2222-2237`) — ступень и by-height идут по WS мимо peer-set'ов; для follower'а WS — единственный источник и так. **Сделать**: в `info!` арма `ElFinalized` (`dpos.rs:1755-1763`) и в `ensure!(has_upstream)` (`:1320-1327`) назвать этот выход одной фразой. Д-nn |
| B2-02 MODERATE | **FIXED (минимально) + RECORDED** | Fail-fast с именованной причиной вместо `Fault::corruption` посреди drain'а. Проверь, читается ли пол marshal'а ПРЕД-движково в `launch` (что возвращает `MarshalActor::init` `outer.rs:762` и есть ли у standalone-архива в `dpos.rs` пол/`last_processed`). (а) Если читается — в #12-арме (`dpos.rs:1810-1840`): `best < floor` ⇒ `Err` с текстом «reth's tip N is below the marshal floor F — the EL is older than a range this node jumped over and the consensus archive holds no blocks there; restore an EL snapshot at or above F, or delete the consensus archive so the node re-enters as ElFinalized»; (б) если нет — тот же текст в арме `None` drain'а (`executor.rs:1438-1457`) по признаку `height < self.marshal_floor` (найди поле/геттер пола у executor'а; если пола у executor'а нет — запиши, оставь текст без числа пола). Перепиши комментарий «inside its own floor..=last_finalized» (`:1439`) — диапазон начинается с reth-tip'а, не с пола. **Красный до правки**: юнит на арм `None` с `height < floor` — сообщение содержит «below the marshal floor» (на дереве до правки — «inconsistent archives»); если пол executor'у недоступен — запиши, юнит не ставить. Перестройка (EL-sync к полу) — RECORDED в В§3 |
| B2-03 MODERATE | **RECORDED** | Не чинить: `gap = target − best` (`dpos.rs:361-364`), диапазон drain от `last_block_number()` (`:1864-1866`), `last ≤ best` ⇒ при `gap > 0` диапазон непуст по построению; fault на последней высоте ⇒ shutdown. Записать арифметику в В§0 |
| B2-04 MODERATE | **RECORDED + текст** | В сообщении об ошибке инварианта чистой остановки FreshMigration (найди, где `FreshMigration` при EL далеко впереди даёт `Err`, `dpos.rs:1885-1893` по ревью) назвать класс: «an EL synced by devp2p with no FCU ever issued has no finalized tag and reads as height 0 here». Д-nn |
| B2-16 MINOR | **FIXED** | Доки `cold_start_jump.rs:26-28`, `:676-683`, `:706-712`: `verify_jump_structural` — три продакшн-вызывающих, включая шаг (2) `deliver` (`plane_upstream.rs:419`) |
| B2-05 MINOR | **FIXED** | `plane_upstream.rs:481-482`, `:625-646`, `:68-73`: «cancels the in-flight fetch» — неверно; описать по факту (`waiters.remove` ⇒ `fetch_one` получает `None`, `cancel` не зовётся; CW закрывает fetch по `true`) |
| B2-07 MINOR | **FIXED** | Переименовать `empty_archive_with_the_el_past_epoch_zero_anchors_at_the_el_finalized_tag` в имя про KIND (`…_resolves_to_el_finalized`), дока — что проводка `launch:1739-1766` не покрыта (В§4 уже говорит) |
| B2-12 NIT | **FIXED** | Follower fresh-арм: после `sync_to_checkpoint`/`sync_to` и `read_geometry` — если `h < activation`, ранний `Err` «checkpoint N is below the DPoS activation block A» вместо 10-с пояса с общей ошибкой (`dpos.rs:2937-2948`). **Красный до правки**: если логика выделяема в чистую функцию — юнит; иначе записать |
| B2-09 NIT | **FIXED** | `testbed/mod.rs:53-58` — снять описание удалённой конструкции и ссылку `[fakes::JumpCommittees]` |
| B2-10 NIT | **FIXED** | `executor.rs:4918` докстринг про гейдж `auth_rotate` |
| B2-11 NIT | **FIXED** | `cold_start_jump.rs:2045-2049` строка `expect_err` |
| B2-19 NIT | **FIXED** | `tests.rs:3416-3419` дублирующий фальсификатор |
| B2-20 NIT | **FIXED (журнал)** | `E4-2-B2.md` §0(6): фраза «пин отброса держится зелёным: `preconditions::…`, `committee_tests::…`» — заменить на правду (эти тесты отброс не пинуют; инверсия — три юнита) |
| B2-21 MINOR | **RECORDED** | Переименование метрики `dpos_frontier_unauthenticated_total` → `dpos_frontier_dropped_total` — Д-nn (ломающее для дашбордов; потребителей в `crates`/`bins` нет — проверь grep и запиши) |
| B2-06, B2-08, B2-17, B2-18, B2-14 | **RECORDED** | Одной строкой каждая в В§1 с file:line |

## Журнал — часть В (`E4-2-B2.md`, дописать)

В§0 Прямые ответы: (1) итог по находкам (FIXED/RECORDED/REJECTED с числом); (2) `cargo test --lib` verbatim (один прогон, финальное дерево) + «остальные ворота — у оркестратора»; ×3 если трогал стенд; (3) тесты прохода: имя / что утверждает / красный до правки verbatim или мутация (≤ 2, откат с `md5sum`); (4) Д-nn (после Д-107); (5) B2-02 — что именно сделано (арм (а) или (б), число пола доступно/нет), B2-12 — форма; (6) что в §5.2/§5.4 остаётся неверным по коду (добавить к §0(7)). В§1 Находки — исход и что сделано (таблица по всем 19). В§2 Раскладка коммитов (уточнить §3 с учётом правок). В§3 Где проверка слабее всего; что уходит в В/Э7.

Заканчивай только когда всё сделано и lib зелёный; невозможное — сделать остальное, назвать в В§3. В финальном ответе — только В§0.

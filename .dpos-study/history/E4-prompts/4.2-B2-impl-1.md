# Строка 4.2, заход Б2 — холодный старт по «Правилу единому»: `sync_to` только с проверенным входом; отброс `Latest` вне окна; стадии `verify_jump_*` и `JumpOutcome::{BadTarget, AuthFailed}` сняты

Ты — исполнитель (Opus 5) одного захода строки плана. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `f8ec4939` (заход Б1 закрыт: триггер steady-state прыжка — только `tip − ordering_finalized > threshold`; цель — пара из СОБСТВЕННОГО архива на tip'е (`pair_at`) → `jump_to_target`; сверка посадки `holds(result)` ⇒ `InvalidTarget` ⇒ в steady state `Fault::corruption`; follower получил лестницу; `upstream_frontier`/`live_height`/`servable` удалены). **В `git status` чужое** — `devnet/**` (не трогать); `.dpos-study/history/E4-ORCHESTRATOR.md` — файл оркестратора, не трогать. Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`.

## Правила (обязательные)

1. **Агентов не запускать.** 2. **Git только на чтение.** 3. **Файлы на запись — ТОЛЬКО**: `dpos.rs`, `cold_start_jump.rs`, `executor.rs`, `plane_upstream.rs`, `cert_follow.rs`, `cert_inlet.rs`, `outer.rs`, `sync_metrics.rs`, `lib.rs`, `node/dpos.rs`, `node/cert_follow/mod.rs`, `testbed/{fakes,stand,tests,byzantine_roles,preconditions}.rs` (тесты `preconditions.rs` НЕ удалять), `.dpos-study/history/E4-2-B2.md`. **Запрещены**: `epoch_manager.rs` (заход В), `committee/**` (читать можно), `crates/dpos/staking-reader/**`, `beacon/**`, `p2p/**`, `bins/**`, `devnet/**`, `contracts/**`, `Cargo.toml`, `.claude/**`. Если правка вне списка неизбежна — §5 с причиной, не делать. 4. **file:line на каждое утверждение.** 5. Без `#[allow]`/`todo!`/таймеров/поллинга; `unwrap` на продакшн-пути — только `Mutex::lock().unwrap()`. 6. Комментарии, доки, `.claude/dpos_architecture/` — не свидетельство; коммонвер — `.claude/COMMONWARE_INTERNALS.md`, checkout только если доки не отвечают. 7. Читать целыми строками. 8. `CARGO_BUILD_JOBS=6`; `Blocking waiting for file lock` — ждать. 9. **Правила прогонов**: все правки батчем → один прогон затронутых тестов → ворота ОДИН раз в конце; «красный до правки» — только для двух юнитов из Б2.7; мутация — не более двух, по одному тесту (`--exact`); детерминизм ×3 — только переписанные/новые стенд-тесты, один раз в конце. 10. Старые тесты (consensus lib 688, стенд с фичей 44 / без 36, node 57, reader 63, `slasher_integration` 16) — не ослаблять; покраснение = находка: либо ожидаемое (свойство сменилось — переписать ассерт и записать «что доказывал / что доказывает»), либо дефект (§0). Не чинить покраснение подгонкой чисел без объяснения механизма. Не менять значения `WEIGHT_RING_EPOCHS`/`SCHEME_RETENTION_EPOCHS`/`MAX_COMMITTEE_LOOKAHEAD_EPOCHS`, контракт.

## Что прочитать ДО кода

1. `.dpos-study/history/E4-CORE-DESIGN.md` §5.2 `:521-533` («Порядок «аутентификация до `sync_to`»», «Правило единое», «Д-2 — что сейчас и что для прода», диаграмма); §5.4 `:565-568`; §5.5 `:576`. `DECISIONS.md` Д-2 (`:18`), П-4 (`:87`).
2. `.dpos-study/history/E4-2-B1.md` — §0(2), §5, часть В §0(4) (Д-92…Д-98, особенно Д-96 и Д-97), В§3 п.6.
3. Код (якоря на HEAD `f8ec4939`, открыты оркестратором): **пред-движковый холодный старт валидатора** `dpos.rs:1840-2066`: `resolve_cold_start_kind` `:1285-1331`, `cold_start_jump_eligible` `:1333`, `empty_archive_requires_landed_jump` `:1847-1853`, crash-survivor `:1874-1950` (`DeferToElSync` #12 `:1930-1950`), прыжок `:1959-2035`, retry-цикл `:2021-2029`, остаточный fatal `:2059-2066`; `JumpDisposition` `:1340-1360`, `classify_jump_outcome` `:1371-1403`, `cold_start_jump_self_heal` `:1423-1501`; `derive_cold_start_heights` `:161-176`. **Follower** `dpos.rs:3057-3185`: `read_geometry` на `rf_hash` `:3070`, datadir-арм `get_latest ⇒ sync_to` `:3077-3078`, fresh-арм `:3106-3143`, `assert_l1_checkpoint` `:3149-3151`, второй прыжок `:3157-3182`, `l1_checkpoint_hash` в `FollowConfig` `:2901`, проводка `node/cert_follow/mod.rs:105-117`, `:232-240`; `cfg.l1` `node/dpos.rs:389-390`. **`cold_start_jump.rs`**: `JUMP_THRESHOLD` `:77`, `JumpOutcome` `:82-170` (`BadTarget` `:115`, `InvalidTarget` `:127-137`, `AuthFailed` `:154`, `L1Fork` `:155-170`), `verify_jump_structural` `:655`, `verify_jump_authenticated` `:707-758` (арм «нечитаемый комитет + L1 ⇒ Ok» `:733-757`), обёртка `cold_start_jump` `:795-826`, `jump_to_target` `:851-980` (need-gate `:869`, структурная стадия `:875-876`, `sync_to`, сверка посадки `:912-932`, auth-стадия `:938-940`, L1 `:943-963`, `Err ⇒ AuthFailed` `:962`), `assert_l1_checkpoint` (grep; строки verbatim — smoke). **Не-стадийные потребители `verify_jump_*` (ОСТАЮТСЯ)**: `refetch_verified_archive_hole` `dpos.rs:403-460`, `fetch_verified_boundary` `cert_follow.rs:178-234`. **Executor** `executor.rs:1450-1600`: армы `BadTarget` `:1475-1484`, `InvalidTarget` `:1486-1520`, `Stalled` `:1522-1541`, `AuthFailed` `:1565-1585`, `L1Fork` `:1586+`; `maybe_re_jump` `:2508-2560`; `Scripted` `:9865-9895`. **A2-04** `plane_upstream.rs`: причины `:145-146`, `:451-452`, PASSED ON `:470-493`, юниты `:1144`, `:1177`, доки модуля `:60-130`. **Стенд**: `stand.rs:2109-2205` (`jump_to_target`, имена вариантов `:2178-2185`), `tests.rs:3389`, `fakes.rs` (`JumpElSync`, `JumpCommittees`, `FakeUpstream`), `byzantine_roles.rs` (роли `LyingUpstream`/`InflatedProbe`/`WrongHeightFinalized` — образец). Тесты под снос/переписку: `cold_start_jump::tests` `:1442-2030` (13 тестов зовут `cold_start_jump(`; `no_latest_does_not_jump` `:1642`, `unverifiable_jump_target_is_bad_target_before_sync` `:1699`, `forged_far_ahead_target_is_rejected` `:1749`), `executor::tests` `re_jump_bad_target_rotates` `:11420`, `re_jump_auth_failed_rotates_and_stays_up_degraded` `:11560`, `re_jump_bad_target_resets_cross_url_streak` `:11736`, `dpos::tests::classify_jump_outcome_dispositions` `:3922`, `dpos::cold_start_kind_tests` (grep). Предикат deployed-сети (devnet/testnet/mainnet chain_id) в `node/dpos.rs` — тест `:2521` `plaintext_bls_rejected_on_deployed_networks` называет функцию; используй тот же предикат, не второй.

## Что сделать

### Б2.1 Пред-движковый холодный старт валидатора — без прыжка по `get_latest`

Удалить: обёртку `cold_start_jump` (`cold_start_jump.rs:795-826`), `cold_start_jump_self_heal`, `classify_jump_outcome`, `JumpDisposition`, `cold_start_jump_eligible`, блок прыжка `dpos.rs:1959-2035` вместе с retry-циклом `empty_archive_requires_landed_jump` и остаточным fatal `:2059-2066`, `SyncReason::AwaitingUpstream`/`AuthRotate` если читателей не останется (проверь `sync_metrics.rs` и `executor.rs:1466`, `:1473`, `:1580`). **Якорь пустого архива при EL за эпохой 0** (`resolve_cold_start_kind` арм `cs_finalized >= activation + interval` `:1310-1329`): не генезис (`archive_finalized`, ChainConfig там codeless — `:1847-1852`) и не `Latest`, а СОБСТВЕННЫЙ EL-финализированный тег: `(cs_finalized, cs_finalized_hash)` из `derive_cold_start_heights` — тот же источник, что follower-`rf_hash` в `:3070`, и тот же писатель (`sync_to`/FCU этого узла или пред-DPoS секвенсер). Если `cs_finalized < activation` — FreshMigration как сегодня. Прочитай `derive_cold_start_heights` `:161-176` и `CanonicalInMemoryState::get_finalized_num_hash` (`.claude/RETH_INTERNALS.md` → checkout только при необходимости): что именно там лежит после (а) graceful restart, (б) devp2p-синк без единого FCU, (в) снапшот; запиши в §0(1) для каждого случая, какой якорь получится и почему это не «состояние неизвестного происхождения» (или почему это оно — тогда §0 с предложением, не делать самовольно). Условие `has_upstream` для этого арма (`:1319-1327`) — пересмотри: без прыжка upstream нужен лестнице (плоскость или WS); проверь, что `upstream.is_some()` у плоскостного валидатора всегда (`node/dpos.rs:2220-2237`), и перепиши текст `ensure!`. Дальше движок стартует на этом якоре, лестница Б1 + tip-only прыжок ведут узел (Д-2(а)). **#12** (`DeferToElSync` `:1930-1950`): якорь на reth-tip остаётся; закрытие EL-разрыва — steady-state прыжок с heartbeat (`executor.rs` — `last_tip_height` сеется из `last_consensus_finalized_height` `:1219`, `pair_at(tip)` из архива `:2551`); проверь по коду, что при populated-архиве и `tip − fin > threshold` первый heartbeat действительно спавнит прыжок (назови арм и условие `pending_backfill`/`finalized_heights_to_backfill` `:2529-2532` — при старте backfill-drain может быть непуст: что происходит и когда прыжок всё же уйдёт), и запиши. Гейдж `crash_recover` — что с ним.

### Б2.2 Follower: два `sync_to` без проверки

(а) datadir-арм `dpos.rs:3077-3078` (`get_latest ⇒ sync_to`) снять: якорь — `rf_hash` (геометрия и модуль читаются локально), как проект пишет; `wait_for_activation_block` — оставить для случая `rf < activation`. (б) fresh-арм `:3106-3143`: `sync_to` ТОЛЬКО к операторскому checkpoint. Checkpoint уже есть в форме L1 (`l1_checkpoint_hash`, `node/cert_follow/mod.rs:114-117`) — hash без высоты; проект просит `(height, hash)`. Реши форму: либо `ElSync::sync_to_checkpoint(hash)` (FCU head=hash, высота — `block_number(hash)` после посадки), либо расширить `L1CheckpointConfig`/`FollowConfig` полем высоты — выбери меньшее и обоснуй; `assert_l1_checkpoint` `:3149-3151` с verbatim-строками остаётся. Без checkpoint: на deployed-сети — `Err` при старте громко (E4-05, текст с командой оператору); на devnet (предикат узла) — единственное место, где `get_latest ⇒ sync_to` остаётся, с `warn!` «devnet-only trust-on-first-use». (в) второй прыжок follower'а `:3157-3182` — удаляется вместе с Б2.1 (`cold_start_jump_self_heal`); follower стартует на якоре и идёт лестницей.

### Б2.3 Отброс вне окна (A2-04)

`plane_upstream.rs:470-493`: `OutOfWindow`/`NotReadable` на ОБОИХ армах ⇒ ответ отброшен, `deliver` возвращает `true` (пир не наказан), метрика — переименуй `dpos_frontier_unauthenticated_total` в то, что происходит (`…_dropped_total{reason}`), или обоснуй сохранение имени. Условия Д-72 (by-height теряет retry-драйвер) и Д-78 (`Latest` — вход триггера) измерялись при `get_latest`-цели и `max`-триггере; после Б1/Б2.1 у `Latest` потребителей как цели нет (проверь: `grep -n get_latest` — должны остаться проба `dpos.rs:1136-1150` и devnet-арм Б2.2(б)). Инвертировать юниты `:1144`, `:1177`. Стенд: прогнать `a_node_more_than_two_epochs_behind_…`, `the_ladder_names_…`, `every_node_serves_…`, R-004/R-001/R-009/R-008, `a_zero_overlap_…`; покраснение — механизм с file:line (в т.ч. CW `fetcher`/`engine`), не подгонка. Доки модуля `plane_upstream.rs:60-130` переписать по факту.

### Б2.4 Стадии и варианты

`jump_to_target`: снять `verify_jump_structural` `:875-876` (структура проверена marshal'ом при `handle_deliver` — CW `core/actor.rs:987-993` по докам — и `deliver` шагом (2); пара из архива) и `verify_jump_authenticated` `:938-940` (BLS — в `deliver`/`verify_delivered`; пара из архива уже под 2f+1); удалить `JumpOutcome::{BadTarget, AuthFailed}` и их армы в executor'е (`:1475-1484`, `:1565-1585`), `Scripted::{BadTarget, AuthFailed}`, имена в `stand.rs:2178-2200`, `tests.rs:3389`; L1-проба `Err ⇒ Stalled` (`:962`); `L1Fork` и `holds(l1)` остаются. Функции `verify_jump_structural`/`verify_jump_authenticated` остаются для `refetch_verified_archive_hole` и `fetch_verified_boundary` (не стадии) — перепиши их доки (`cold_start_jump.rs:20-40`, `:640-706`) и арм «нечитаемый комитет + L1 ⇒ Ok» `:733-757`: у обоих оставшихся потребителей `l1_checkpoint` = `None`/явный — проверь, достижим ли арм вообще; если нет — снять. `JUMP_THRESHOLD` остаётся константой прод-порога (`dpos.rs:2557`, `:3307`); Д-97 закрывается удалением обёртки — запиши.

### Б2.5 Что в Б2 НЕ делается (записать в §5, не делать)

`is_live_epoch`/корроборация (`epoch_manager.rs`) — В. Роль «EL сел не туда» (`InvalidTarget ⇒ corruption` вживую) — Э7. `dkg_height_tx` — Э5.

### Б2.6 Стенд

1. Роль «пир без данных отвечает `Error`» (`byzantine_roles.rs`): честный пир, у которого нет запрошенной высоты, отвечает отказом (в CW — `Payload::Error` ⇒ `add_retry` без бана, §5.2; найди, как `FakeUpstream`/плоскостной `Producer` стенда выражает «нет данных» сегодня — `None`?) — пин: `deliveries_rejected == 0` у запросившего, пир НЕ исключён (следующий пулл к нему уходит), высота приходит от другого. Если стенд не различает «нет данных» и «ложь» на проводе — запиши с file:line, роль не писать.
2. Пин отброса вне окна: узел, стоящий > 2 эпох позади (фикстура `a_node_below_the_chain_refuses_what_it_cannot_see_without_an_evm_call` или лестницы), получает `Latest` выше потолка: счётчик отброса > 0, tip не выше `last(epoch(fin)+2)`, `deliveries_rejected == 0`, узел поднимается лестницей (уже пиновано — держать зелёным).
3. Холодный старт стенд НЕ моделирует (`dpos.rs::launch` не в стенде) — юниты Б2.7; запиши в §4.

### Б2.7 Юниты

(а) `dpos::cold_start_kind_tests`/новый: пустой архив + EL за эпохой 0 + upstream ⇒ якорь = `(cs_finalized, hash)`, не генезис и не `Latest` (**красный до правки**: на HEAD якорь = `archive_finalized`, а `cold_start_jump_eligible == true`); `cs_finalized < activation` ⇒ activation. (б) `cold_start_jump::tests`: `jump_to_target` с парой, чей multisig сломан, при `holds(result) == true` ⇒ `Landed` (стадии нет; **красный до правки**: на HEAD `AuthFailed`) — и рядом положительный контроль, что та же пара НЕ проходит `deliver` (`plane_upstream::tests`, юнит есть — сослаться, не дублировать); L1 `holds(l1) == Err` ⇒ `Stalled`. (в) follower fresh-datadir без checkpoint на deployed-сети ⇒ `Err` (если логика вынесена в чистую функцию — юнит; иначе записать, почему нет). (г) `plane_upstream::tests`: вне окна ⇒ отброшен + `true`, оба арма (инверсия `:1144`, `:1177`). (д) мутация ≤ 2 (`--exact`).

### Б2.8 Ворота (сам, verbatim по одной строке, ОДИН раз в конце)

`cargo test -p fluentbase-consensus --lib` (688 ± …); `… --features dpos-devnet-byzantine testbed::` (44 ± …); `cargo test -p fluentbase-consensus --lib testbed::` (36 ± …); `cargo test -p fluentbase-node --lib` (57/0); `cargo test -p fluentbase-staking-reader` (63/0); `cargo test -p fluentbase-consensus --test slasher_integration` (16/0); `cargo clippy -p fluentbase-consensus -p fluentbase-node --all-targets` (одна чужая `large_enum_variant` в `node/dpos.rs`) и `cargo clippy -p fluentbase-consensus --all-targets --features dpos-devnet-byzantine` (0); `cargo fmt -p fluentbase-consensus -p fluentbase-node -- --check` (0); `cargo doc -p fluentbase-consensus --no-deps 2>&1 | grep -c "unresolved link"` (7). Новые/переписанные стенд-тесты — ×3 (`--test-threads=1 --nocapture`, вывод байт-в-байт).

### Б2.9 Журнал `.dpos-study/history/E4-2-B2.md` (короткая форма)

§0 Прямые ответы: (1) холодный старт: якорь в каждом случае (graceful / devp2p без FCU / снапшот / пустой архив + EL за эпохой 0 / crash-survivor #12) с file:line, что удалено (каждый символ), как #12 закрывается heartbeat-прыжком (арм, условие, что при непустом drain); (2) follower: три арма после правки, форма checkpoint, текст отказа, devnet-предикат (file:line функции узла); (3) A2-04: что отброшено, метрика, результат стенда, механизм покраснений; (4) стадии/варианты: что снято, что оставлено и для кого, арм «L1 ⇒ Ok» — достижим или снят; (5) ворота verbatim + ×3; (6) стенд: роль сделана/не сделана и почему, пин отброса; (7) что в §5.2/§5.4 оказалось неверно по коду; (8) запрещённое (ноль); (9) риск проекта: лестница требует живого члена `committee(T+1)` на каждую ступень (§5.4) — при полной ротации комитетов за время отставания узел с пустым архивом не поднимется; назови, какой путь у него остаётся по коду после Б2 (checkpoint? рестарт с архивом?) — не чинить, записать. §1 Д-nn (после Д-98). §2 тесты (имя / что утверждает / красный до правки verbatim или мутация). §3 логические коммиты (`feat(consensus)!: …`; стенд зависит от кода — назвать группы, которые не собираются отдельно). §4 где проверка слабее всего. §5 что осталось В и дальше.

Заканчивай только когда всё сделано и ворота зелёные; невозможное — сделать остальное, назвать в §5. В финальном ответе — только §0.

# Строка 4.2, заход Б1 — триггер прыжка по tip'у; цель прыжка из собственного архива; `upstream_frontier` удалён; `servable` снят

Ты — исполнитель (Opus 5) одного захода строки плана. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `d2f215f1` (заход 4.2-А закрыт: `FrontierHandler::deliver` — пять шагов, `false` только на лжи; ступень `Finalized{last(T+1)}` через `marshal.hint_finalization`; `Committee::geometry()`/`epoch_of`). **В `git status` чужое** — `devnet/**` (не трогать); `.dpos-study/history/E4-ORCHESTRATOR.md` — файл оркестратора, не трогать. Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`; `reader/` = `crates/dpos/staking-reader/src/`.

## Правила (обязательные)

1. **Агентов не запускать.** 2. **Git только на чтение.** 3. **Файлы на запись — ТОЛЬКО**: `executor.rs`, `cold_start_jump.rs`, `cert_inlet.rs`, `dpos.rs`, `outer.rs`, `plane_upstream.rs` (только доки модуля `:60-79` и то, что требует смена читателей `Latest`; сами пять шагов `deliver` не трогать), `lib.rs`, `node/dpos.rs`, `testbed/{fakes,stand,tests,byzantine_roles,preconditions}.rs` (тесты `preconditions.rs` НЕ удалять — переписывать ассерты можно, с записью «что доказывал / что доказывает»), `.dpos-study/history/E4-2-B1.md`. **Запрещены**: `epoch_manager.rs` (заход В), `committee/**` (читать можно), `reader/**`, `beacon/**`, `p2p/**`, `devnet/**`, `contracts/**`, `Cargo.toml`, `.claude/**`. Если правка вне списка неизбежна — §5 с причиной, не делать. 4. **file:line на каждое утверждение.** 5. Без `#[allow]`/`todo!`/таймеров/поллинга; `unwrap` на продакшн-пути — только `Mutex::lock().unwrap()`. 6. Комментарии, доки, `.claude/dpos_architecture/` — не свидетельство. 7. Читать целыми строками. 8. `CARGO_BUILD_JOBS=6`; `Blocking waiting for file lock` — ждать. 9. **Правила прогонов**: все правки батчем → один прогон затронутых тестов → ворота ОДИН раз в конце; «красный до правки» — только для двух юнитов из Б1.7 (триггер по tip'у; сверка посадки); мутация — не более двух, по одному тесту (`--exact`); детерминизм ×3 — только переписанные стенд-тесты, один раз в конце. 10. Старые тесты (consensus lib 683, стенд с фичей 44 / без 36, node 57, reader 63, `slasher_integration` 16) — не ослаблять; покраснение = находка: либо ожидаемое (свойство сменилось — переписать ассерт и записать), либо дефект (§0). Не чинить покраснение подгонкой чисел без объяснения механизма.

## Что прочитать ДО кода

1. `.dpos-study/PLAN.md:101` (строка 4.2); `.dpos-study/history/E4-CORE-DESIGN.md` §5.2 (`:506-533`): «Тип и единственный писатель» (`Frontier` = marshal tip, тело из архива, `upstream_frontier`/`live_height` удаляются), «Триггер и лестница — один шаг», «Порядок «аутентификация до `sync_to`»», «Правило единое», диаграмма состояний; §5.4 (`:549-572`) строки про `Latest` вне окна, посадку не на заверенную ветку, L1; §5.5 (`:574-577`); §10 A31, A46, A47, A51, B16, B17 (`grep -n "A31\|A46\|A47\|A51\|B16\|B17"`). `DECISIONS.md` П-4 (`:87`), Д-2 (`:18`).
2. `.dpos-study/history/E4-2-A.md` — §0(6), §7, часть В §0(4) (Д-76…Д-83), В§3 «Заходу Б» (пять пунктов), и `E4-2-A-REVIEW.md` §6 п.2-4. Это твой вход: три отклонённых решения А (A2-01 `servable`, A2-04 отброс `Latest`, A2-05 цели на `Hybrid`) и их измерения.
3. `.claude/COMMONWARE_INTERNALS.md` — marshal (`Update::Tip`, `get_info`/`get_finalization`/`get_block`, `HintFinalized`, `try_repair_gaps`, потолок `last(epoch(fin)+2)`), resolver p2p (`fetch_targeted` только участникам). Checkout commonware — только если доки не отвечают.
4. Код (якоря на HEAD `d2f215f1`, проверены оркестратором): `executor.rs` — `ReJumpFn` `:508`, `FrontierProbeFn` `:566`, `TrackedEpochFn` `:578`, `ReJump` `:606-663` (`upstream_frontier` `:625`, `threshold` `:641`, `rotate` `:648`, `probe` `:659`, `tracked_epoch` `:663`), `last_tip_height` `:954`, арм-ы `JumpOutcome` `:1417-1540`, `probe_frontier` `:2105-2240` (`servable` `:2191-2197`, писатель `upstream_frontier` `:2224-2226`, `hint_finalization(frontier)` `:2233-2235`), `Update::Tip` `:2381-2392`, `maybe_re_jump` `:2468-2510` (`max(tip, upstream_frontier)` `:2479-2482`, спавн `:2509`); `cold_start_jump.rs` — `JUMP_THRESHOLD` `:77`, `JumpOutcome` `:82-156`, `ElSync` `:365-375`, `RethElSync::sync_to` `:442`, `holds` `:615`, `verify_jump_structural` `:633`, `verify_jump_authenticated` `:674`, `cold_start_jump` `:761`, `cold_start_jump_with_threshold` `:802-900` (`get_latest` `:816`, need-gate `:821`, `sync_to` `:837-842`, «назад» `:845`, POST-sync auth `:856`, L1 `:861-882`, floor `:886`); `cert_inlet.rs` — `LiveFrontierTee` `:255-292` (`live_height` `:277`, `upstream_frontier` `:288`), писатель `upstream_frontier` `:580-587`, писатель `live_height` `:737-743`, `UpstreamResolver::fetch_targeted` `:2872-2894`; `dpos.rs` — `refetch_verified_archive_hole` `:403-460` (зовёт `verify_jump_*` `:443-448` — это НЕ стадия прыжка, оставить), пред-движковый cold-start прыжок `:1849-1900` (`cold_start_jump_self_heal` по `get_latest` — в Б1 НЕ трогать, см. Б1.5), `DposLayerConfig.upstream_frontier` `:983-987`, `:1629`, замыкания пробы/прыжка валидатора `:2440-2560` (`frontier: latest.map(...)` `:2526`, `ReJumpFn` `:2537`), `:2584-2600`, follower `:3276-3332`, `:3787-3803`, follower `sync_to` `:3044-3045`, `:3083-3089` (Б2, не трогать); `node/dpos.rs` — `upstream_frontier` `:743-794`, `live_height` `:1016`, `:1347`, `:1999`, `:2077`, `:2309`; `outer.rs:295` `MarshalMailbox`; `plane_upstream.rs:247-276` `FrontierMarshal for MarshalMailbox` — готовое чтение пары из архива (`get_info(Latest)` → `get_finalization(h)` → `get_block(h)`), доки `:60-79` («`Latest` — единственный вход триггера»). Стенд: `stand.rs` `upstream_frontier` `:1043-1046`, семплирование `:1230-1241`, `upstream_frontier_series` `:603-610`, `:1534`, `live_height` `:1932-1952`, `:2132`, `:2510`; `tests.rs` R-004 `:2943-3130` (`upstream_frontier_series` `:3025`, `:3122-3126`), `the_rejump_runs_the_production_jump_and_authenticates_at_the_landing` `:2255`, `a_rotated_out_node_without_the_rejump_parks` `:2165`, `three_boundaries_…` `:1443` (`rejump_calls >= 1`), лестница `:1287`; `preconditions.rs:134`, `:261` (порог 80 `:264`, ассерты посадки/потолка/floor `:309-336`); `testbed/fakes.rs` (`FakeUpstream`, `UpstreamCounters`, `FakeElSync`/`JumpCalls`).

## Что сделать

### Б1.1 Триггер прыжка — только по tip'у; `upstream_frontier` удалён

`maybe_re_jump(height)` сравнивает `height − ordering_finalized` с `threshold` — без `max(..., upstream_frontier)`. Удалить `ReJump::upstream_frontier`, `LiveFrontierTee::upstream_frontier` и его писателя в `ingest` (`cert_inlet.rs:580-587`), писателя в `probe_frontier` (`executor.rs:2224-2226`), `DposLayerConfig.upstream_frontier` и всю проводку (`dpos.rs`, `node/dpos.rs`), стендовые `upstream_frontier`/`upstream_frontier_series`. Доки `ReJump` (`:613-625`), `probe` (`:651-660`), `probe_frontier` (`:2086-2100`) переписать по факту. **`live_height` (`cert_inlet.rs:277`)**: найди всех читателей (`grep -n live_height` по крейту и `node/`); если после 4.1 читателя нет — удалить в этом заходе (тее остаётся ради `dkg_height_tx` до Э5); если читатель есть — назвать его и оставить (Б2). Замёрзший tip при «committee[E] не закоммичен» (причина, по которой `upstream_frontier` был введён — `executor.rs:613-620`) больше не маскирует глубокий разрыв, потому что лестница двигает tip проверенной ступенью (§5.2 «Триггер и лестница»); запиши это в §0 и проверь стендом (Б1.6).

### Б1.2 Цель steady-state прыжка — пара на marshal-tip'е из СОБСТВЕННОГО архива

Сегодня `cold_start_jump_with_threshold` берёт цель из `upstream.get_latest()` (`cold_start_jump.rs:816`) — непроверенный вход (R-001). Для steady-state прыжка (`ReJumpFn`, `executor.rs:2509`) цель — `(finalization, block)` на текущем marshal-tip'е, прочитанная из локального архива marshal'а (то, что `store_finalization` положил после `verify_delivered` — единственный писатель, §5.2): используй `FrontierMarshal`-чтение (`plane_upstream.rs:261-276`) или его аналог на `MarshalMailbox`. Форма: `ReJumpFn` получает `(from, tip)` (tip — `Height` из `Update::Tip`, `executor.rs:2381`), замыкание в `dpos.rs:2537` читает пару из архива и зовёт `cold_start_jump_with_threshold` с ГОТОВОЙ целью (сигнатура: цель — аргумент `UpstreamFinalized`/`Option<UpstreamFinalized>` вместо `&U`; или отдельный `re_jump_to_target`). Need-gate `latest.block.height <= anchor + jump_threshold` и «назад» (`:845`) остаются. **Пред-движковый cold-start прыжок** (`dpos.rs:1849-1900`, `cold_start_jump` `:761`) в Б1 НЕ меняется — у него на пустом архиве цели нет; это Б2 (перестройка холодного старта по §5.2 «Правило единое»). Запиши в §5, что после Б1 единственный потребитель непроверенного `Latest` как ЦЕЛИ — пред-движковый холодный старт.

**Сверка посадки.** После `sync_to` вернул `Valid` `(landing_h, landing_hash)`: сверить, что EL сел на заверенную ветку — `el.holds(block.result)` (или `block_hash(landing_h) == block.result`; посмотри, что `RethElSync::sync_to` реально возвращает как `landing_hash` `:442-600` и что такое `block.result` относительно `landing_h` — `tip − K`) ⇒ иначе `JumpOutcome::InvalidTarget`. Исход в executor'е (`:1447-1460`, «rotate immediately») в Б1 не меняется — запиши, что при цели из собственного архива `rotate` бессмыслен и по проекту исход — `Fault::corruption` (Б2 вместе с удалением `BadTarget`/`AuthFailed`). `verify_jump_structural`/`verify_jump_authenticated` при цели из архива становятся избыточными — в Б1 оставить (их удаление как стадий — Б2), но в §0 назвать, что именно каждая проверка теперь дублирует (file:line писателя архива).

### Б1.3 `servable` снят (A2-01)

Удалить предикат `servable` и арм `above_the_frontier` (`executor.rs:2191-2197`, метка `reason` тоже): ступень кладётся на каждом замёрзшем тике, если она выше marshal-FLOOR (арм `at_or_below_the_floor` остаётся). `ProbeOutcome.frontier` после этого нужен только для `hint_finalization(frontier)` (`:2233-2235`) — проверь, что это единственный оставшийся читатель, и перепиши доки `probe_frontier` (`:2093-2100`). **Измерение А (Д-79)**: без `servable` `a_forged_seed_slot_is_admitted_with_no_key_and_refused_when_the_key_lands` терял архивный релей («мёртвый целевой фетч на каждом тике теснит фронтир-канал»). Повтори прогон этого теста после батча. Если красный — найди МЕХАНИЗМ (file:line в commonware `fetcher`/`engine` или в стенде: дедуп hint'ов одной высоты, cadence ретраев, квота канала) и реши по нему: (а) тест пинует своё свойство через тайминг — переписать шов теста (не свойство), с расписком; (б) реальная нагрузка на канал — назвать её числом (fetch'ей на тик) и записать в §5 для Э7; таймеры/rate-limit не вводить. «Оставить `servable`» — не вариант: проект §5.2 его не содержит, и после Б1.1 у триггера нет другого непроверенного входа, который бы его оправдывал.

### Б1.4 Цели ступени на `Hybrid` (A2-05, Д-77)

`UpstreamResolver::fetch_targeted` (`cert_inlet.rs:2872-2894`) роняет цели. Проверь по коду, кто на плоскостном валидаторе является `upstream` в `MarshalResolver::Hybrid` (`outer.rs:121-146`, `node/dpos.rs`), и почему в А маршрут «targeted `Finalized` → `plane`» унёс запрос с канала, на котором ротированный узел ещё обслуживался (три теста красные, Д-77): назови канал/резолвер/peer-set с file:line. Затем либо (а) сделай так, чтобы целевой `Finalized{last(T+1)}` реально уходил `committee[T+1]` (§5.2 «Почему ступень отправляется»), либо (б) RECORDED с механизмом и числом, почему в Б1 нельзя. Ассерт `requests_created{Dropped} = 0` (§5.4) — если метрика резолвера достижима стенду, поставить; иначе записать.

### Б1.5 Что в Б1 НЕ делается (записать в §5, не делать)

Отброс `Latest` вне окна (A2-04, Д-78) — только в Б2 вместе с перестройкой холодного старта (единственный оставшийся потребитель); удаление `verify_jump_*`-стадий и `JumpOutcome::{BadTarget, AuthFailed}`, L1 `Err ⇒ Stalled`, follower `sync_to`/checkpoint (`dpos.rs:3044`, `:3083`) — Б2; `is_live_epoch`/корроборация — В. Доки `plane_upstream.rs:60-79` — переписать под Б1 (после Б1 `Latest` кормит только `hint_finalization(frontier)` и пред-движковый холодный старт; триггер прыжка на него не смотрит).

### Б1.6 Стенд

1. **Ряд tip'а вместо `upstream_frontier_series`**: в семплировании (`stand.rs:1230-1241`) писать за тик marshal-tip узла (`Outcome::marshal_tip_series[i]`) — найди шов БЕЗ возмущения прогона (Д-81: чтение tip'а из замыкания пробы роняло `a_zero_overlap_boundary_halts_the_chain_verify_only`; семплирование в драйвере стенда — другой шов; метрика `outer_marshal_finalized_height` или атомик, который стенд уже держит). Проверь на том же тесте ×3.
2. **R-004** (`an_inflated_latest_probe_is_refused_at_the_frontier_and_spawns_no_re_jump`, `tests.rs:2993`): ассерты по `upstream_frontier_series` → по `marshal_tip_series` (tip жертвы не растёт от лжеца) и `jump_calls` пуст; свойство то же.
3. **Положительный пин лестницы** (В§3 п.3): в `the_ladder_names_successive_rungs_…` (`tests.rs:1287`) добавить утверждение, связывающее ступень с ростом tip'а: для каждой названной ступени `last(T+1)` marshal-tip узла 3 достигает её в пределах N тиков после первого именования (N — измерь и запиши; ×3). Если стенд НЕ показывает связи (tip растёт до именования, или прыжок опережает) — записать с числами, не подгонять.
4. **`preconditions.rs:261`** (`a_node_more_than_two_epochs_behind_freezes_its_marshal_at_the_ceiling_and_jumps_over_a_hole`): после Б1 цель прыжка — пара на потолке (159), а не `Latest` (180); при пороге 80 разрыв 159−95 = 64 ≤ 80 — прыжка не будет, догон контигуозный + ступени. Прогони и запиши, что происходит; перепиши ассерты под то, что тест ДОЛЖЕН доказывать теперь (marshal замерзает на потолке; терминалы обслуживаются; узел проходит потолок — прыжком при проектном пороге `min(1024, interval) = 32` или ступенями при 80 — выбери ОДИН вариант и обоснуй; посылки Д3 сохранить). Тест не удалять. То же для `every_node_serves_the_terminal_pair_of_every_passed_epoch_after_re_jumps` (`:134`), если краснеет.
5. `the_rejump_runs_the_production_jump_and_authenticates_at_the_landing` (`:2255`), `a_rotated_out_node_without_the_rejump_parks` (`:2165`), `three_boundaries_…` (`:1443`), R-001 (`:3179`), R-009 (`:3380`) — прогнать; покраснение объяснить механизмом.

### Б1.7 Юниты

(а) `executor::tests`: триггер по tip'у — при `tip − ordering_finalized ≤ threshold` прыжок НЕ спавнится, что бы ни кричал внешний источник (**красный до правки**: на HEAD атомик `upstream_frontier` поднимает триггер); (б) `cold_start_jump::tests` с `FakeElSync`: цель из архива → `sync_to` → `holds(result) == false` ⇒ `InvalidTarget` (**красный до правки**: на HEAD сверки нет — покажи, что на HEAD прыжок с несовпавшей посадкой возвращает `Landed`); `holds == true` ⇒ `Landed`; (в) мутация ≤ 2 (`--exact`): например, вернуть `max` в триггер — (а) красный; убрать сверку посадки — (б) красный.

### Б1.8 Ворота (сам, verbatim по одной строке, ОДИН раз в конце)

`cargo test -p fluentbase-consensus --lib` (683 ± новые); `… --features dpos-devnet-byzantine testbed::` (44 ± переписанные); `cargo test -p fluentbase-consensus --lib testbed::` (36 ± …); `cargo test -p fluentbase-node --lib` (57/0); `cargo test -p fluentbase-staking-reader` (63/0); `cargo test -p fluentbase-consensus --test slasher_integration` (16/0); `cargo clippy -p fluentbase-consensus -p fluentbase-node --all-targets` (одна чужая `large_enum_variant` в `node/dpos.rs`) и с фичей (0); `cargo fmt -p fluentbase-consensus -p fluentbase-node -- --check` (0); `cargo doc -p fluentbase-consensus --no-deps 2>&1 | grep -c "unresolved link"` (8). Переписанные стенд-тесты и лестница — ×3 (`--test-threads=1 --nocapture`, вывод байт-в-байт).

### Б1.9 Журнал `.dpos-study/history/E4-2-B1.md` (короткая форма)

§0 Прямые ответы: (1) триггер: строка сравнения, что удалено (file:line каждого удалённого символа/писателя), судьба `live_height`; (2) цель прыжка: откуда пара, сигнатура, сверка посадки, что дублируют `verify_jump_*` теперь; (3) `servable`: результат повторного измерения, механизм, решение; (4) A2-05: канал/резолвер/peer-set с file:line, сделано или RECORDED; (5) ворота verbatim + ×3; (6) стенд: что каждый переписанный тест доказывал и доказывает теперь, N тиков лестницы; (7) что в §5.2 оказалось неверно по коду; (8) запрещённое (ноль). §1 Д-nn (после Д-83). §2 тесты (имя / что утверждает / красный до правки verbatim или мутация). §3 логические коммиты (`feat(consensus)!: …`; `cargo check --all-targets` на группу — учти, что стенд зависит от кода: группы, которые не собираются по отдельности, назвать). §4 где проверка слабее всего. §5 что осталось Б2 и В.

Заканчивай только когда всё сделано и ворота зелёные; невозможное — сделать остальное, назвать в §5. В финальном ответе — только §0.

# Ревью строки 4.2, заход А (`FrontierHandler::deliver` — точка доверия фронтира; проба-лестница `Finalized{last(T+1)}`)

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база — `HEAD` (`git rev-parse HEAD`; чужие коммиты по контракту после `4ef23313` не трогают `crates/`). Объект — **незакоммиченное рабочее дерево** захода А: `crates/dpos/consensus/src/{committee/mod.rs, committee/store.rs, dpos.rs, executor.rs, plane_upstream.rs, testbed/fakes.rs, testbed/stand.rs, testbed/tests.rs}`, `crates/node/src/dpos.rs`. **Всё остальное в `git status` — чужое** (`contracts/staking/**`, `.dpos-study/**` кроме твоего файла, `devnet/**`, `target-contract/`) — не трогать. Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`; `reader/` = `crates/dpos/staking-reader/src/`.

## Правила

1. **Агентов не запускать.** 2. **Git только на чтение.** Единственный файл на запись — `.dpos-study/history/E4-2-A-REVIEW.md`; мутации продакшн-кода ради чувствительности тестов — только с откатом и `md5sum` до/после. 3. Каждое утверждение — `file:line` дерева и, где есть «до», `HEAD:file:line`. 4. Комментарии, доки, `.claude/dpos_architecture/`, журнал исполнителя — не свидетельство; свидетельство — код и прогон, который ты сделал сам. 5. **Полнота, не фильтр**: все находки с серьёзностью (BLOCKER / SERIOUS / MODERATE / MINOR / NIT) и «чем пытался опровергнуть». 6. Читать целыми строками. 7. `CARGO_BUILD_JOBS=6`; `Blocking waiting for file lock` — ждать. 8. `.claude/COMMONWARE_INTERNALS.md` — ДО checkout'а commonware (resolver p2p: `deliver` `false` ⇒ `block` + `excluded`; `fetch_targeted`/`participants`; marshal `HintFinalized`, `handle_produce`, `verify_delivered`/`store_finalization`, `last_processed_height`).

## Вход

- Спека: `.dpos-study/PLAN.md:101`; `.dpos-study/history/E4-CORE-DESIGN.md` §5.2 (`:506-533`), §5.4 (`:549-572`), §5.5, §10 A36/A38/A45/A48/A49/A50/A64/A74/B5/B6; `.dpos-study/DECISIONS.md` П-4 (`:87`) и Д-2 (лестница = «один шаг по проверенной финализации в окне `epoch(fin)+2`, повторяемый»); `history/E4-PRECONDITIONS.md` Д3.
- Постановка: `.dpos-study/history/E4-prompts/4.2-A-impl-1.md`. Журнал: `.dpos-study/history/E4-2-A.md` — читать ПОСЛЕ диффа (§0(6) — четыре расхождения с §5.2, §3 Д-70…Д-75, §7, §8).
- Дифф: `git diff HEAD --stat -- crates/`, затем по файлам. Контекст: `plane_upstream.rs` целиком (`deliver` `:313-420`, `decode_frontier` `:203`, метрики `:84-89`, `new_bridge` `:455`); `committee/mod.rs` (`geometry()` `:398`, `epoch_of` `:403`), `committee/store.rs:603`, `:146`; `executor.rs:500-660` (`ReJump`, `TrackedEpochFn` `:568`, `ProbeOutcome.step` `:536`, метрики `:517-523`), `:1000-1010` (`probe_step_pending`), `:2100-2200` (`probe_frontier`, предпосылки `:2143-2154`, `hint_finalization` `:2167`); `dpos.rs:1097` (`step_skip_reason`), `:2116`, `:2459` (замыкание пробы), `:2581`, `:2750-2755` (писатель `T`), `:3322`; `node/dpos.rs:1025`, `:1788`, `:1826`; `cert_inlet.rs::UpstreamResolver::spawn_finalized`, `fetch_targeted` (`:2872-2881`) — читать; `cold_start_jump.rs:633` (`verify_jump_structural`); `testbed/tests.rs` — переименованные тесты (`:2969`, `:3149`, `:3349`) и новый `every_ladder_step_a_lagging_node_can_name_is_at_or_below_its_own_marshal_tip` (`:1275`); `testbed/preconditions.rs:261`; `testbed/stand.rs` (`frontier_plane`, проба, `UpstreamCounters`); `testbed/byzantine_roles.rs` (не тронут).

## Форма результата `.dpos-study/history/E4-2-A-REVIEW.md` (§0–§6 как `E4-1-B3-REVIEW.md`)

**§0 Прямые ответы** (нумерованные, каждый с [KNOWN]/[ГИПОТЕЗА]):

1. **Ворота** — прогнать САМОМУ, verbatim: `cargo test -p fluentbase-consensus --lib` (журнал 679/0); `… --features dpos-devnet-byzantine testbed::` (44/0); `cargo test -p fluentbase-consensus --lib testbed::` (36/0); `cargo test -p fluentbase-node --lib` (57/0); `cargo test -p fluentbase-staking-reader` (63/0); `cargo test -p fluentbase-consensus --test slasher_integration` (16/0); `cargo clippy -p fluentbase-consensus -p fluentbase-node --all-targets` и с фичей (одна чужая `large_enum_variant`); `cargo fmt --check` по файлам захода; `cargo doc -p fluentbase-consensus --no-deps 2>&1 | grep -c "unresolved link"` (8). Три переименованных стенд-теста + новый — прогнать ×3 (дрожание `UpstreamCounters`? журнал §8.5 говорит, что видел 1 и 3 отказа в одном контроле).
2. **`deliver` — пять шагов по коду** (`plane_upstream.rs:313-420`): подтверди таблицу исходов журнала §0(1) строка за строкой; `false` ТОЛЬКО на лжи? Есть ли путь, где честный пир получает `false` (см. п. 4 — seed-оракул)? Cap декода — какой и достаточен ли (`MAX_COMMITTEE_SIZE`, размер тела блока?).
3. **Д-73 / §0(6)(в) — центральный вопрос.** Исполнитель утверждает: ступень `last(T+1) ≤ last(epoch(fin)+2)` = потолок marshal'а отставшего узла ⇒ «лестница не поднимает никого». Проверь по коду и по стенду: (а) где и как marshal отставшего узла ЗАМЕРЗАЕТ на `last(epoch(fin)+2)` — это его tip ≤ потолка или tip = потолку? В прогоне журнала tip узла 3 = 264 при fin 95 — то есть потолка в стенде НЕТ? Открой `preconditions.rs:261` (`…freezes_its_marshal_at_the_ceiling…`) — что там «потолок» и совпадает ли с утверждением; (б) сценарий проекта: tip замёрз НИЖЕ потолка (никто не пушит финализации — узел вне peer-set'ов, > f молчат, старт с пустым архивом) — тогда ступень `last(T+1)` ВЫШЕ tip'а и поднимает его; достижим ли этот сценарий на стенде/в проде, и верно ли тогда «ступень ≤ tip всегда»? (в) если tip = потолку, срабатывает ли обычный триггер `tip − ordering_finalized > threshold` (прыжок к tip'у) — и не есть ли ЭТО лестница проекта (Д-2(а): шаг по проверенной финализации в окне +2, повторяемый), а проба-ступень — только fallback для голодающего узла? Вердикт: Д-73 — арифметическая истина, артефакт стенда, или подмена понятия «лестница»? Если проект в §5.2 действительно неверен на центральном пути — скажи это прямо со свидетельством прогона (hard-stop 4 оркестратора).
4. **Д-75 / §8.1 — SERIOUS-кандидат.** BLS в `deliver` идёт через схему модуля с seed-оракулом: протухший/чужой ключ эпохи роняет `verify` ЧЕСТНОГО сертификата ⇒ `false` ⇒ честный пир исключён из фронтир-канала до рестарта (наблюдено: 3 отказа в честном контроле R-001). Подтверди по коду (`plane_upstream.rs:348-371`, `CombinedScheme::verify_*`, оракул в схеме модуля `committee/store.rs` `EpochVerifier`); есть ли путь проверить ТОЛЬКО multisig-половину 2f+1 (`is_attributable`/`verify_certificate` без seed; или схема без оракула из записи) — что делает cert-inlet (`ensure_key` до `verify`) и почему фронтиру это недоступно. Серьёзность и минимальная правка.
5. **Д-72 — непроверяемый ответ передаётся дальше.** До захода Б `upstream_frontier` жив: что именно теперь попадает в `upstream_frontier` (`plane_upstream.rs:373-401`, `:411-418`) — только проверенные ответы, или и `out_of_window`/`not_readable`? Если и они — R-004 остаётся открытым в А ровно в той форме, что на HEAD (честный далёкий `Latest` ⇒ глубокий прыжок ⇒ `Stalled`), и §7 журнала это признаёт; подтверди/опровергни и назови, что Б обязан снять. Сравни с §5.2 «отбрасывается с метрикой»: измерение исполнителя (узел 3 садится на 180 и больше не тянет — два пулла на 181–182 отброшены) — воспроизведи или проверь по коду `spawn_finalized`/`try_repair_gaps`.
6. **Д-71 — `deliver` не пишет в marshal.** Подтверди по коду, что ответ, допущенный `deliver`, доходит до `store_finalization` через `UpstreamResolver::spawn_finalized` → резолвер marshal'а (второй BLS там же); измерение «10 из 35 краснеют при записи из `deliver`» — правдоподобно (двойная доставка ⇒ `debug_assert` «two boundaries pending»)? Есть ли путь, где допущенный ответ НЕ доходит до marshal'а (потеря)?
7. **Лестница по проводке**: `T` = ячейка `AtomicU64`, писатель — форвардер моста (`dpos.rs:2750-2755`), `u64::MAX` = нет; `hint_finalization(height, targets)` (`executor.rs:2167`) — что делает marshal с `HintFinalized{targets}` (`COMMONWARE_INTERNALS.md`: `targets` уходят в `fetch_targeted` резолвера marshal'а? на `Hybrid` `UpstreamResolver::fetch_targeted` игнорирует цели `cert_inlet.rs:2872-2881` — подтверди; на каких узлах ступень реально целевая); две предпосылки `:2143-2154`; `probe_step_pending`/`unserved` — когда инкрементируется, не ложно ли; follower `None` (`:3322`).
8. **Трейт `Committee::geometry()` (Д-70)** — обязательный метод: все реализации (стор, `SchemeCommittee` тестов, стенд) обновлены; `epoch_of` = та же формула, что `Geometry`/`epoch_at_block`; `None` до заморозки.
9. **Переименованные тесты R-004/R-009/R-001**: открой каждый на HEAD и в дереве — что доказывал, что доказывает; ассерты не ослаблены (лжец исключён — как наблюдается; «подмена ровно одна»; «дыра закрыта контролем»); есть ли вакуумные ассерты. Новый тест лестницы — что пинует (журнал: «ступень именуется и ≤ tip»); не пинует ли он ровно артефакт Д-73?
10. **Девять «неожиданно покрасневших и закрытых переделкой хода» тестов** (§0(4)) — подтверди, что все зелёные на исходных ассертах (`git diff HEAD -- testbed/tests.rs` по `^-.*assert` для них — 0).
11. **Граница/гигиена**: `pub` изменения; `#[allow]`/`todo!`/таймеры/`unwrap` на продакшн-пути; правки `dpos.rs` вне диапазона постановки (`:2116`, `:2459`, `:2750`) — проводка или поведение; `node/dpos.rs`.
12. **Где журнал вводит в заблуждение**; что в §5.2 неверно/недосказано (журнал: четыре пункта + два) — подтверди/опровергни каждый.

**§1 Находки** — `id | серьёзность | file:lines | HEAD-якорь | что не так | чем пытался опровергнуть | уверенность`; id `A2-nn`. BLOCKER — регрессия на пути фронтира/прыжка/доставки, не предписанная проектом, или доказанная неверность проекта на центральном пути. **§2 Поведение по ханкам.** **§3 Граница.** **§4 Ворота** verbatim. **§5 Оставить как есть** — непустой. **§6 Вне рамок** — для Б (потолок против ступени, `upstream_frontier`, `fetch_targeted` на `Hybrid`, роль «пир без данных»), В, 4.3.

В финальном ответе — только §0 (кратко) и таблица §1.

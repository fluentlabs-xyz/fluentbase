# Строка 5.3, заход В+Г1 — вход p2p бикона (один источник, одно окно) и малая гигиена

Ты — исполнитель (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `a45c458d`. Пути без префикса — от `crates/dpos/consensus/src/`. Рабочее дерево чисто, кроме чужого `.dpos-study/history/E4-ORCHESTRATOR.md` — не читать, не трогать.

## Рамка

Это заход В+Г1 строки 5.3 (`.dpos-study/PLAN.md`, `| [ ] 5.3 |`). Карта захода — `.dpos-study/history/E5-ORCHESTRATOR.md`, раздел «Карта захода В+Г1» (пять решений; исполняются как написано, отклонение — только с записью Д-nn в журнале и причиной). Инвентарь модуля — `history/E5-3-MAP-RESEARCH.md` (§5, §6.3, §6.4; реляция, перепроверяй). Это НЕ автомат состояний (заход А), НЕ `Option`-швы → конфиг (Г2, идёт с А), НЕ идентичность лога (Б закрыт `cee2e037`). Если увидишь существенное вне рамки — одна строка в конце журнала.

## Что установлено оркестратором по коду (опирайся; опровергать — с file:line)

- Три копии правила окна: `is_bufferable` `beacon/actor.rs:2063-2065` (`(now, now+2]`), `epoch_is_actionable` `:2098-2099` (`[now, now+2]` ∪ живые церемонии), `on_confirm` `:1571` (окно перед `:1613`). `beacon_member` `:2114-2116` — `committee_for(epoch).position(from)`. `on_message` `:2118-2150`: пик эпохи → окно (`"epoch"`) → членство (`"not_member"`) → декод. Три `record_ingress_drop` `:1613`, `:2139`, `:2149`.
- `TrackedWindow` (`crates/dpos/p2p/src/lib.rs:338-398`): `classify(peer) → Option<Ingress>` (`None` до первого `track`), `Ingress::member_of(epoch)` (`:415`) — в биконе НИКЕМ не читается. Записи `{E−1, E, E+1}` от эпохи перехода E (`staking-reader/src/epoch_transition.rs:767-790`; сосед без закоммиченной записи пропускается).
- Узел: `GatedReceiver(beacon_receiver, ingress_window, "beacon", members_only=true)` `crates/node/src/dpos.rs:1892-1897`; `ValidatorInputs` (`beacon/plane.rs:504-541`) окна не несёт. **Стенд окна не строит вовсе** (`testbed/stand.rs`: ни `TrackedWindow`, ни `GatedReceiver`) — пред-декодный гейт стендом сегодня не исполняется.
- Арм «резолвер умер»: `actor.rs:877-878` (`resolver_rx = None; self.resolver = None`, цикл продолжается). Движок `beacon_log_resolver` — supervised-ребёнок (`plane.rs:998-1006`), узел держит один хэндл (`node/dpos.rs:797`): «gossip-only» — состояние, которого узел не переживает.
- `deque_size: 2` — `beacon/dkg_transport.rs:135`, обоснование `:123-134`; юниты `dkg_agree.rs:2248` строят движок с `MAX_SET_LEN`, свойство `=2` не проверяется. Перепредлагает ли лидер ДРУГОЕ тело после nullify — не установлено; установи по `dkg_agree.rs::propose`.

## Что сделать (по решениям карты 1–5)

1. **Одно окно.** Константа `INGRESS_LOOKAHEAD_EPOCHS = 2` и одна чистая функция `(now, epoch) → bool` (или `RangeInclusive`), три сайта на неё; `is_bufferable` сохраняет своё `epoch > now` как своё правило. Кадр `now+3` НЕ добавлять — обоснование из карты в доккомментарий к константе (R-126 закрывается записью).
2. **Один источник классификации.** `ValidatorInputs.ingress_window: TrackedWindow` — обязательное поле; `DkgActor` держит окно; `beacon_member(from, epoch)` = `window.classify(from).is_some_and(|i| i.member_of(epoch))`; `None` ⇒ отказ с меткой `window_unset`. `committee_for` — только для позиций ростера внутри церемонии. Узел передаёт `ingress_window.clone()` (`node/src/dpos.rs:1879`). **Стенд**: построить окно как ET (`{E−1,E,E+1}` из комитетов стенда, `record` на каждом переходе) и надеть `GatedReceiver(.., "beacon", true)` на beacon-канал каждого узла — так стенд впервые исполняет пред-декодный гейт. Обязательство: C7/C8 (`a_zero_overlap_boundary_…` и сосед), keyed/keyless, оба теста R-002 — зелёные; новый стенд-тест: узел на эпоху впереди шлёт дилинг для `now+2` → отказ `not_member`, ретрансмит доставляет после продвижения, ключ эпохи минтится всеми. **Если стенд краснеет по живости — СТОП: запись в журнал с механизмом (какой кадр, какой узел, почему ретрансмит не спас), без обходов; это решение оркестратора.**
3. **Одно место отказа.** `fn refuse(&self, reason: &'static str)` в акторе (метрика через `record_ingress_drop` + одна debug-строка с `from`/`epoch`/`reason`), три вызова → один. Реестры не сливать (R-070).
4. **resolver-exit ⇒ fatal.** Арм `None =>` → `error!` с цепочкой (supervised-ребёнок, узел падает) и `break` из `run`. Юнит: актор с `resolver_rx = Some`, отправитель дропнут → `run` возвращается. `resolver: None` с рождения — не трогать.
5. **`deque_size = 2`.** Установи по коду, перепредлагает ли лидер другое тело после nullify (`dkg_agree.rs::propose`, `certified`/nullify-путь); если да — юнит: два тела от одного отправителя проходят через `build_body_engine` (`dkg_engine.rs:312`) с продакшн-`deque_size`; если нет — константу не менять, обоснование `dkg_transport.rs:123-134` привести к факту, записать.
6. **Доки в том же изменении**: `.claude/dpos_architecture/{03_epoch_machinery,08_node_integration…,13_invariants_gotchas_rules,00_preamble}.md` — ingress-правило бикона (один источник, окно, метки `window_unset`/`epoch`/`not_member`), `verified-against`; `git grep` по `beacon_member`/`committee_for` в доках — ни одного устаревшего утверждения.

## Правила

- **Агентов не запускать** — ни для работы, ни для проверки себя (спека и код — в одном контексте). Git — только чтение.
- Записываемые файлы: `beacon/{actor,plane,dkg_transport,dkg_engine}.rs` и их тесты, `testbed/{stand,tests}.rs`, `crates/node/src/dpos.rs` (только передача окна), `.claude/dpos_architecture/*.md`, журнал `.dpos-study/history/E5-3-VG1.md`. `crates/dpos/p2p/src/lib.rs` — только если `TrackedWindow` нужен новый читающий метод (не менять семантику `classify`). Остальное — не трогать; нужно — остановись и запиши.
- Каждое утверждение в журнале — `file:line`; комментарии и доки — не свидетельство; числа считать.
- Никаких `#[allow]`, новых `unwrap`/`expect` на продакшн-пути, новых `pub` наружу без нужды. `CARGO_BUILD_JOBS=6`; стенд не гонять параллельно с другой сборкой.

## Ворота (все, verbatim; база на HEAD `a45c458d`)

`--lib` 681/0 · `--lib --features dpos-devnet-byzantine testbed::` 56/0 · `--lib testbed::` 47/0 (634 filtered) · node 57/0 · staking-reader 64/0 · slasher 16/0 · clippy три крейта — два чужих (`node/src/dpos.rs:1989`, `staking-reader/.../epoch_transition.rs:3017`), ноль своих · consensus с фичей 0 · fmt exit 0 · doc unresolved 6 · harness 2 failed / 2086 / 10. Прирост тестов по именам.

**Мутации (с откатом и md5):** (M1) вернуть `beacon_member` на `committee_for` — новый стенд-тест или юнит на `window_unset` красный; (M2) сломать окно (`+3`) — тест на окно красный; (M3) вернуть арм `None =>` к продолжению цикла — юнит п.4 красный.

## Журнал `history/E5-3-VG1.md` — §0

1. Ворота verbatim. 2. Таблица «решение карты → что сделано → file:line → чем доказано», пять строк + доки. 3. Что изменилось в поведении ingress по сравнению с HEAD (таблица: кадр/отправитель/эпоха → было → стало) — в том числе кадры для `now+2`. 4. Стенд с окном: что показал новый тест (числа), C7/C8 до/после. 5. `deque_size`: ответ по коду с якорем. 6. Мутации verbatim. 7. Д-nn (отклонения от карты) — каждое с причиной. 8. Вне рамки. 9. Слабее всего — ранжируй. §1 «оставил как есть» непустой.

Финальный ответ — §0 сжато и таблица п.2. Не останавливайся между шагами; не спрашивай — решай и записывай допущение; СТОП только по п.2.

<tone_preference>Кратко и по существу.</tone_preference>

# Строка 5.3, заход Б — идентичность DKG-лога `(dealer, hash)`, refetch по хэшу, две подписи одного дилера

Ты — исполнитель (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `18573e76`. Пути без префикса — от `crates/dpos/consensus/src/`. Рабочее дерево чисто, кроме чужого `.dpos-study/history/E4-ORCHESTRATOR.md` — его не читать и не трогать.

## Рамка — что это и чего это НЕ

Это заход Б строки 5.3 (`.dpos-study/PLAN.md`, `| [ ] 5.3 |`; решение П-9 `DECISIONS.md:92`; дизайн `history/E5-BEACON-DESIGN.md` — §5.2 автомат `:596-616`, находка E5-14 `:486`, приёмка `:729`). Объём — ТОЛЬКО идентичность лога и её следствия. Это НЕ введение автомата состояний (заход А, позже), НЕ вход p2p (заход В), НЕ гигиена (Г). Терминал `Conflict` как состояние не вводить — здесь только ДАННЫЕ, которые А превратит в состояние. Не переписывать `actor.rs` сверх названных функций. Если увидишь существенное вне рамки — одна строка в конце журнала, не развивая.

Инвентарь неявного автомата — `history/E5-3-MAP-RESEARCH.md` (§4 — таблица ключей по дилеру: это твоя карта затрагиваемых мест; реляция, каждую строку перепроверь по коду перед правкой).

## Что установлено оркестратором по коду (опирайся; опровергать — с file:line)

- `ceremony.rs:412-414` `record_checked_log`: `!self.recorded.insert(pk) ⇒ Step::default()` — first-wins по дилеру, второй лог без следа. `:642` `ingest_signed_log` возвращает `(true, record_checked_log(..))` — второй валидный лог того же дилера с другим хэшем засчитан как «дубль», fetch помечен выполненным (R-002/E5-14, BLOCKER по I3 дизайна).
- `log_resolver.rs:52-55` `DkgLogKey {epoch, dealer}` — хэша в wire-ключе нет ⇒ refetch «по хэшу» невозможен по протоколу. Кодек `:73-96`, `BeaconFetchKey::Log :113-116`.
- Единственные места, где хэш уже сравнивается: `scoped_pinned_logs` `ceremony.rs:878-898` (`:884`). Пришпиленный набор — `AgreedSet.pinned: BTreeMap<u8, B256>` (`actor.rs:542-544`, idx→hash) — это ИСТИНА о том, какой лог дилера действителен для эпохи; его не менять (П-3/Д-1 — hard-stop).
- Журнал `JournalRecord::PeerLog` (`share_state.rs:361-377`) append-only без ключа; при replay `log_map.insert(pk, …)` — last-wins (`ceremony.rs:718-724`, `:216-219`, `:1035-1040`), а live-путь — first-wins. Две семантики одного ключа.
- Стенд: `testbed/tests.rs:3117` `a_dealer_with_two_logs_leaves_the_addressed_victim_without_a_share` (под фичей `dpos-devnet-byzantine`) воспроизводит R-002: жертва без share на эпоху. `:3293` `a_two_log_dealer_that_also_withholds_its_partial_stops_the_chain_silently` — остановка (граница I5), должна ОСТАТЬСЯ остановкой.

## Что сделать

1. **Ключ `(dealer, hash)`** там, где сегодня ключ — дилер: `recorded`, `signed_logs`, `want`, `nondurable_logs`, `scoped_pinned_logs`, `DealerLogStore`/`ServeMap`, и всё, что §4 инвентаря перечисляет (перепроверь). Хэш — тот же, что в `AgreedSet.pinned` (`signed_log_hash`, `ceremony.rs:844-846`) — одна функция хэша, не две.
2. **Refetch по хэшу.** `DkgLogKey` получает `hash` (wire-формат меняется — сети переподнимаются с генезиса, миграции нет; кодек фиксированной раскладки, byte-identical, тест кодека обязателен). `fetch_missing_logs` (`actor.rs:2224-2244`, `:2247-2270`) и `fetch_bodies` (`dkg_agree.rs:1366-1390`) просят `(epoch, dealer, hash)` — хэш из пришпиленного набора; дилер, чей записанный лог не совпадает с пришпиленным хэшем, ТЯНЕТСЯ заново (сегодня — нет, `:2224-2237`). Сервер (`serve_log`, `:2681-2690`) отдаёт лог по ключу с хэшем или ничего.
3. **Две валидные подписи одного дилера с разными хэшами** (обе прошли `check`): (а) сохранить ПАРУ как evidence — в RAM у церемонии до sweep и в журнале (новый `JournalRecord`, чтобы пережить рестарт); (б) локальный бан: дилер помечен для эпохи, дальнейшие gossip-логи от него не записываются; церемония использует тот лог, чей хэш пришпилен (через refetch п.2); он-чейн ничего не шлётся (Д-6 defer — `DECISIONS.md:41`). Метрика-счётчик + одна WARN-строка на (эпоха, дилер). Если моя формулировка бана невыполнима без изменения кворума или пришпиливания — СТОП, запись в журнал, не обходить.
4. **Одна семантика журнала**: replay `PeerLog` ключуется `(dealer, hash)` так же, как live; после рестарта `recorded`/`signed_logs` равны тому, что было до (тест на replay обязателен).
5. **Приёмка** (дизайн `:729`): тест `:3117` меняет вердикт — жертва ПОЛУЧАЕТ share по refetch; переименовать по новому вердикту, витнесс подмены (`reveals_swapped`, `ShareConfirm` с чужим хэшем) сохранить — зелёная ветка без подмены вакуумна. Тест `:3293` — остаётся остановкой, число в число сверить до/после. Плюс юнит на пару подписей (evidence записан, бан действует, пришпиленный лог принят) и на кодек ключа.
6. **Доки в том же изменении** (правило проекта — doc-drift блокер ревью): `.claude/dpos_architecture/05_identity_crypto.md:74` (строка канала BEACON_RESOLVER — ключ `{epoch, dealer}` → с хэшем), `13_invariants_gotchas_rules.md` (правило идентичности лога, §~502 про `recorded_dkg_logs`), `00_preamble.md` блок `verified-against`; `git grep` по старому символу `DkgLogKey` и по словам «first-wins»/«by dealer» в `.claude/dpos_architecture/` и в доккомментариях кода — ни одного устаревшего.

## Правила

- **Агентов не запускать** — ни для работы, ни для проверки себя: спека и код должны сидеть в одном контексте. Git — только чтение (`git diff/show/log/grep`); никаких `add/commit/stash/checkout/restore`.
- Записываемые файлы: `beacon/{ceremony,actor,log_resolver,log_store,share_state,dkg_agree}.rs` (последний — только `fetch_bodies` и то, что нужно для ключа), их `*_tests.rs`, `testbed/tests.rs` (только два названных теста + при нужде роль в `testbed/stand.rs`/`roles`), `.claude/dpos_architecture/*.md`, журнал `.dpos-study/history/E5-3-B.md`. Всё остальное — не трогать; если нужно, остановись и запиши почему.
- Каждое утверждение в журнале — `file:line`. Комментарии и доки — не свидетельство. Числа считай, не оценивай.
- Никаких `#[allow]`, новых `unwrap`/`expect` на продакшн-пути, новых `pub` наружу модуля без нужды.
- `CARGO_BUILD_JOBS=6`; стенд не гонять параллельно с другой сборкой.

## Ворота (все, verbatim в журнал; база на HEAD `18573e76`)

`cargo test -p fluentbase-consensus --lib` 676/0 · `--lib --features dpos-devnet-byzantine testbed::` 56/0 · `--lib testbed::` 47/0 (629 filtered) · `-p fluentbase-node --lib` 57/0 · `-p fluentbase-staking-reader` 64/0 · `--test slasher_integration` 16/0 · `cargo clippy -p fluentbase-consensus -p fluentbase-node -p fluentbase-staking-reader --all-targets` — два чужих (`node/src/dpos.rs:1989`, `staking-reader/.../epoch_transition.rs:3017`), ноль своих · то же с фичей 0 · `cargo fmt --check` exit 0 · `cargo doc -p fluentbase-consensus --no-deps` unresolved 6 · `(cd devnet/local-dpos-smoke && make harness-test)` 2 failed / 2086 / 10 (две красные предсуществуют). Прирост тестов назвать по именам.

**Мутации (обязательны, с откатом и md5 до/после):** (M1) вернуть first-wins в `record_checked_log` — приёмочный тест `:3117` должен покраснеть; (M2) refetch без хэша (ключ с нулевым хэшем) — тест на refetch красный; (M3) журнал без новой записи evidence — тест на replay пары красный.

## Журнал `history/E5-3-B.md` — §0 прямые ответы

1. Ворота verbatim. 2. Полный список мест, где ключ сменён (таблица: место → было → стало → file:line). 3. Точная семантика «локального бана» как реализована, и что происходит с логом дилера, который пришпилен: принят? 4. Что делает узел, если ОБА лога дилера не совпадают с пришпиленным хэшем (третий хэш). 5. Wire: старая/новая раскладка `DkgLogKey`, тест кодека. 6. Replay: чем доказано равенство до/после рестарта. 7. Приёмка: вердикт `:3117` до и после, числа `:3293` до и после. 8. Мутации M1–M3 verbatim. 9. Что вне рамки заметил (по строке). 10. Где проверка слабее всего — ранжируй. Затем §1 «оставил как есть» (непустой), §2 «вне рамок».

Финальный ответ — §0 сжато и таблица п.2. Не останавливайся между шагами; не спрашивай — решай и записывай допущение.

<tone_preference>Кратко и по существу.</tone_preference>

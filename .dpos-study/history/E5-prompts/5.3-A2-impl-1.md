# Строка 5.3, заход А2 — `Option`-швы → `Wiring`, и хвосты автомата

Ты — исполнитель (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `f71f4d52`. Пути без префикса — от `crates/dpos/consensus/src/beacon/`. Рабочее дерево чисто, кроме чужого `.dpos-study/history/E4-ORCHESTRATOR.md` — не читать, не трогать.

## Рамка

Заход А2 строки 5.3 — последний заход строки. Карта — `.dpos-study/history/E5-ORCHESTRATOR.md`, «Карта захода А2» (пять пунктов + три мелких). А1 закоммичен `ffec54b2`: автомат `EpochState`, `recover(E)`, один гейт `share_on_artifact`, durable `Conflict`, стор-владелец; журнал `history/E5-3-A1.md`, финальное ревью `history/E5-3-A1-REVIEW.md` (§2 «оставить», §3 «вне рамок» — источники хвостов). Дизайн — `history/E5-BEACON-DESIGN.md` §5.2–5.4, находка E5-23 (`:504`, семь `Option`-швов). НЕ этот заход: новые состояния/переходы автомата, `dkg_agree`, резолверные каналы (R-067), контракт.

## Что установлено оркестратором по коду

- `Option`-швы `DkgActor`: `resolver :726`, `resolver_rx :732`, `changed :743`, `share_dir :764`, `plane_clock :829`, `outcome_at :833`, `pull_artifact :841`, `recorded_dkg_logs :846`, `pinned_rx :857`, `agreement_tx :867`, `artifacts_rx :876`, `body_lost_rx :881` — двенадцать. `commitment`/`share` `:709-710`, `last_height :819` — данные, не швы, не трогать. Конструктор `new :908-923` (15 параметров, 5 `Option`), builders `with_* :983-1110` (9), продакшн-сборка `plane.rs:859-884`; тестовые `DkgActor::new(` — 15 вызовов + `standalone_actor{,_cf,_at}` `:6455/6474/6490`.
- `restart_replay` `artifact.rs:1462` — избыточен после стор-владельца (стор читается первым шагом каждого тика, `reconcile_with_store`; push до первого тика отбрасывается) — финал А1 §2.
- `validate_share_on_poly` `outcome.rs:100-113` — сверяет `partial_public(my_share.index)` с точкой доли, но не `my_share.index == committee.position(me)`: чужая доля с правильной точкой на своём индексе проходит — финал А1 §3.
- F-01 (финал А1): маркер конфликта fsync'ится до write-behind записи артефакта; смерть в окне ⇒ рестарт в `Conflict` без артефакта в сторе; `Conflict` не `needs_artifact()` ⇒ `PK_E` не приобретается ⇒ σ эпохи `Pending`.

## Что сделать

1. **`Wiring`** — одна структура рёбер актора, все поля обязательные: `resolver` (+`resolver_rx`), `share_dir`, `outcome_at`, `pull_artifact`, `recorded_dkg_logs`, `pinned_rx`, `agreement_tx`, `artifacts_rx`, `body_lost_rx`, `plane_clock`, `changed`. `DkgActor::new(core…, wiring)` без `Option`; builders `with_*` удалить или свести к `Wiring`. В акторе — ни одного `if let Some(шов)`/`recv_or_never(None)` на пути `run`/тика: каналы всегда есть (в тестах — припаркованные приёмники и no-op отправители из `Wiring::standalone()`/`Wiring::for_test(..)` под `#[cfg(test)]`), `outcome_at` в тестах — над пустым/фейковым стором, `share_dir` — `TempDir` (как сейчас в тестах, где он `Some`). Продакшн-сборка `plane.rs` собирает `Wiring` целиком. 15 тестовых вызовов + 3 хелпера — переписать ОДИН раз через хелперы, без дублирования. Семантика `resolver: None ⇒ recovery gossip-only` (актор `:338`-era док) исчезает: резолвер обязателен; тесты, которые проверяли поведение «без резолвера», переводятся на «резолвер с припаркованным движком» — если какой-то тест держал именно отсутствие резолвера как свойство, запиши что он проверял и чем это покрыто теперь.
2. **F-01**: `Conflict{key: None}` входит в `needs_artifact()` — актор тянет артефакт и в терминале (для `PK_E`, I4), подписание остаётся остановленным (`Conflict` не становится `Keyed`); при приходе артефакта — `Conflict{key: Some}`. Тест: рестарт с маркером и без артефакта в сторе ⇒ `Conflict` ⇒ pull ⇒ `key` заполнен, доля не появляется.
3. **`restart_replay` удалить** вместе с проводкой (`plane.rs`), докстроки `artifact.rs:50/67/1653` — поправить. Тест на рестарт с артефактом на диске должен держаться на `reconcile_with_store` (переписать тот, что вызывал `restart_replay`, если есть).
4. **E-10 — бан эквивокатора на дилинги**: `Commitment`/`Share`/`Ack` от дилера, для которого у эпохи есть evidence (`Conflict{evidence}`/пара из Б), отвергаются потребителем через `refuse(from, epoch, "equivocator")`; никакого он-чейн действия. Д-nn: расширение локального бана Б (логи) на дилинги — обоснование: never-acked игрок иначе берёт dealing второго полинома и теряет долю. Тест: пара evidence есть → Commitment от того же дилера → отказ, счётчик.
5. **`validate_share_on_poly(outcome, committee, me, my_share)`**: дополнительно `committee.position(me) == Some(my_share.index)`; иначе `false`. Все вызывающие передают `me`. Тест: чужая доля с правильной точкой на чужом индексе — отказ.
6. Мелкое: **DB-08** — провал записи `ReceivedDealing` ставится в ту же очередь ретрая, что `nondurable_logs` (или отдельную), с повтором на следующем тике; **F-02** — body-lost для эпохи в `Dealing{agreed: Some}` не сбрасывать, а запомнить в слоте и применить на печати (`Sealed → Acquiring`); **F-03** — `BodyMissing` и `QuorumMissing` — отдельные условия снятия (`carries`), gauge не завышен.
7. **Доки в том же изменении**: `.claude/dpos_architecture/{08,13,00_preamble}.md`, `DPOS_ARCHITECTURE_CHANGELOG.md` — `Wiring`, удаление `restart_replay`, бан на дилинги, `validate_share_on_poly` с `me`; `git grep` по `with_pinned_requests|with_artifact_pull|with_plane_clock|with_agreement_plane|with_body_lost|with_changed_bit|with_recorded_logs|with_share_confirms|restart_replay|gossip-only` в доках и коде — ничего живого.

## Правила

- **Агентов не запускать** — ни для работы, ни для проверки себя. Git — только чтение.
- Записываемые файлы: `beacon/{actor,artifact,ceremony,plane,outcome,metrics,share_state,mod}.rs` и их тесты, `testbed/{stand,tests}.rs`, `crates/node/src/dpos.rs` (только если `Wiring` меняет сборку узла), `.claude/dpos_architecture/*.md`, `.claude/DPOS_ARCHITECTURE_CHANGELOG.md`, журнал `.dpos-study/history/E5-3-A2.md`. Остальное — не трогать; нужно — стоп и запись.
- Не переформатировать нетронутое; дифф должен читаться как «швы → `Wiring`, плюс пять точечных правок». Никаких `#[allow]`, новых `unwrap`/`expect` на продакшн-пути, новых `pub` наружу модуля. `CARGO_BUILD_JOBS=6`; стенд не гонять параллельно.
- **СТОП** (запись, не обход): `Wiring` невозможен без изменения `dkg_agree`/резолверных каналов; какой-либо стенд краснеет по живости; нужно менять `DECISIONS.md`.

## Ворота (все, verbatim; база на HEAD `f71f4d52`)

`--lib` 717/0 · стенд с фичей 58/0 · без фичи 49/0 (668 filtered) · node 57/0 · staking-reader 64/0 · slasher 16/0 · clippy три крейта — два чужих (`node/src/dpos.rs:1991`, `staking-reader/.../epoch_transition.rs:3024`), ноль своих · consensus с фичей 0 · fmt 0 · doc unresolved 6 · harness 2 failed / 2086 / 10. Прирост/убыль тестов по именам.

**Мутации (с откатом и md5):** (M1) вернуть `if let Some` на одном шве (например, `pull_artifact`) — компиляция/тест `Wiring` красный (или `debug_assert`); (M2) `Conflict` не в `needs_artifact()` — тест F-01 красный; (M3) бан на дилинги снят — тест E-10 красный; (M4) `validate_share_on_poly` без проверки индекса — тест чужой доли красный.

## Журнал `history/E5-3-A2.md` — §0

1. Ворота verbatim. 2. Таблица швов: шов → было (`Option`, где гейтил) → стало (поле `Wiring`, что в тестах) → file:line. 3. Что удалено (`restart_replay`, builders) и чем покрыто. 4. F-01, E-10, `validate_share_on_poly` — что именно сделано и мутации. 5. DB-08/F-02/F-03. 6. Д-nn. 7. Вне рамки. 8. Слабее всего — ранжируй. §1 «оставил как есть» — непустой.

Финальный ответ — §0 сжато и таблица п.2. Не останавливайся между шагами; не спрашивай — решай и записывай допущение; СТОП только по названным условиям.

<tone_preference>Кратко и по существу.</tone_preference>

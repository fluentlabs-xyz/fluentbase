# Строка 5.4, заход Б — круг 3 (пост-финальная правка ≤10 строк продакшн-кода)

Ты — исполнитель (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `79dd18f9`; рабочее дерево = HEAD + заход Б (10 файлов под `crates/`, два круга, финальное ревью «КОММИТИТЬ» — `.dpos-study/history/E5-4-B-REVIEW.md`; dsh круг 2 — `E5-4-B-DSH-2.md`). Пути без префикса — от `crates/dpos/consensus/src/`. Чужое `.dpos-study/history/E4-ORCHESTRATOR.md` — не читать.

Это точечная правка четырёх принятых остатков. Ничего сверх них.

## Что сделать

1. **E-01 dsh / F-01 финала** (`beacon/dkg_engine.rs`, цикл `spawn_agreement_launcher` ~`:757-783` и `on_request` ~`:928-951`): арм халта должен выигрывать всегда, когда готов — `biased;` в `select!` лаунчера, арм `halt_edge` первым; перечитывание латча после `start_one` — для ВСЕХ исходов (`Running`/`NotAMember`/`Failed`), т.е. один раз после `match`, а не только в ветке `Running`. Итого ≤ 10 строк продакшн-кода. Дважды `on_halt` (E-08) при этом допустимо — он идемпотентен; если хочешь снять двойной вызов — только если это не выходит за 10 строк.
2. **F-03 финала / E-06 dsh** (`epoch_manager.rs` ~`:1460-1484`, док ~`:1410-1411`): `abort_below` остался `async fn` без единого `await` после снятия прунинга — сделать синхронным (все вызовы — без `.await`), док про «file I/O» переписать по факту.
3. **F-02 финала** (мутация M1′: band `..cutoff` → `..=cutoff` выживает 16/0): добавить в `beacon::dkg_engine::tests` тест (или расширить существующий `pruning_reclaims_the_journal_partitions_an_abort_left_behind`), который краснеет от M1′: партиция cutoff-эпохи БЕЗ живого инстанса не сносится. Воспроизведи M1′ до и после (verbatim, с откатом и md5).
4. **F-04 финала**: доккомментарий перенесённого теста, говорящий «every finalized height» — по факту часы публикуют только на смене эпохи; поправить текст.

## Правила

- Агентов не запускать (спека и код в одном контексте). Git — только чтение.
- Записываемые файлы: `beacon/dkg_engine.rs`, `epoch_manager.rs` (и `beacon/actor.rs`, `dpos.rs` только если там вызовы `abort_below(...).await`), журнал `.dpos-study/history/E5-4-B.md` (раздел «## Круг 3», коротко), `.claude/dpos_architecture/13_invariants_gotchas_rules.md`/`08_…md` только если там описан порядок армов лаунчера или `abort_below` как async (проверь `grep -n "abort_below\|halt_edge\|biased" .claude/dpos_architecture/*.md`), CHANGELOG — одна строка.
- Не переформатировать нетронутое; никаких `#[allow]`, новых `unwrap`/`expect` на продакшн-пути.
- Cargo: **только** `cargo test -p fluentbase-consensus --lib beacon::dkg_engine::tests` и `cargo test -p fluentbase-consensus --lib epoch_manager` + `cargo clippy -p fluentbase-consensus --all-targets` + `cargo fmt --check`; `CARGO_BUILD_JOBS=12`. Полный набор ворот гоняет оркестратор.
- СТОП (запись, не обход): правка п.1 не укладывается в 10 строк продакшн-кода; `abort_below` синхронный ломает вызывающий код нетривиально.

## Журнал — «## Круг 3»: (1) что изменено → file:line, число строк продакшн-кода в п.1; (2) M1′ до/после verbatim + md5; (3) результаты названных cargo-команд verbatim; (4) что не сделал и почему.

Заверши ответ в ≤ 15 строк.

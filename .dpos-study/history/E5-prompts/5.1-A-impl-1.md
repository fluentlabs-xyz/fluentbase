# Строка 5.1, заход А — факт `PK_E`: артефакт становится единственным владельцем

Ты — исполнитель (Opus 5) одного захода строки плана, в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `3ead5c6c` (строка 5.0а закрыта: код `7790d1bc`, доки `3ead5c6c`). Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`; `beacon/` = `crates/dpos/consensus/src/beacon/`. **В `git status` чужое**: `.dpos-study/history/E4-ORCHESTRATOR.md` (файл другой сессии) — не трогать.

## Правила (обязательные)

1. **Агентов не запускать.** Причина: спека и код обязаны сидеть в одном контексте; делегирование разрывает связь «утверждение ↔ якорь», а этот заход переписывает ядро модуля.
2. **Git только на чтение.** Не коммитить, не `add`, не `stash`/`checkout`/`restore`/`reset`.
3. **Файлы на запись — ТОЛЬКО**: `beacon/{actor,surface,share_state,artifact,oracle,outcome,plane,keys,key_journal,resolve,metrics,mod}.rs`, `testbed/tests.rs`, `.dpos-study/history/E5-1-A.md`. **Запрещено**: `beacon/{carry,follower}.rs` (заход Б), `beacon/{certify,seed_journal,verified_seed,seed}.rs` (строка 5.2), `committee/**` (читать можно), `epoch_manager.rs`, `executor.rs`, `cert_inlet.rs`, `plane_upstream.rs`, `node/**`, `p2p/**`, `testbed/{stand,fakes,cert_inlet_tests,byzantine_roles,preconditions,committee_tests,mod}.rs`, `bins/**`, `devnet/**`, `contracts/**`, `Cargo.toml`, `.claude/**`. Правка вне списка неизбежна ⇒ §5 с причиной и file:line, НЕ делать.
4. **file:line на каждое утверждение** в журнале.
5. Без `#[allow]`, `todo!`, таймеров вместо событий, поллинга вместо пробуждений; `unwrap` на продакшн-пути — только `Mutex::lock().unwrap()`. Любое из этого — только строкой Д-nn с причиной.
6. Комментарии, доки и `.claude/dpos_architecture/` — **не свидетельство**: в этом репозитории они дрейфуют, и последние пять проходов каждый нашёл устаревший. Свидетельство — код и твой прогон. Для commonware — `.claude/COMMONWARE_INTERNALS.md` ДО чекаута.
7. Читать целыми строками: `Read` или `sed -n 'N,Mp'`; никогда `cut -c`/`head -c`. Поиск по коду — **ТОЛЬКО `git grep`**: под `crates/dpos/consensus/src/.claude/session-reads/` лежат gitignored журналы чужих сессий, и `grep -rn` по `crates/` выдаёт из них мусор.
8. `CARGO_BUILD_JOBS=6`; `Blocking waiting for file lock` — ждать. Стенд не гонять параллельно с другой сборкой.
9. **Правила прогонов** (решение пользователя 2026-09-12): полный набор ворот НЕ гонять — он у оркестратора. Твои прогоны: один `cargo test -p fluentbase-consensus --lib` в конце, `cargo clippy -p fluentbase-consensus --all-targets` **БЕЗ фичи** И **с фичей** (рост clippy — дефект; в 5.0а это уже стоило возврата на воротах), `cargo fmt --check`, и ×3 по своим новым/переписанным стенд-тестам. Правки батчем, ОДИН прогон затронутых тестов. «Красный до правки» — только для теста на найденный дефект поведения; мутации ≤ 2 (`--exact`), с откатом и `md5sum`.
10. Существующие числа на HEAD: consensus lib **692**/0; стенд с фичей **55**/0, без фичи **46**/0; node 55/0; reader 64/0; slasher 16/0; clippy ровно два ЧУЖИХ предупреждения (`large_enum_variant` в node, `MutexGuard` в staking-reader), с фичей 0; `fmt --check` чисто; `cargo doc` unresolved 6. Не ослаблять ни один тест. Покраснение = находка с механизмом, не подгонка. **Исключение, предписанное проектом:** тест C7 (ниже) ОБЯЗАН упасть и быть переписан.

## Что прочитать ДО кода

1. `.dpos-study/PLAN.md`, блок «### Э5.», строка `5.1` — задание дословно.
2. `.dpos-study/history/E5-BEACON-DESIGN.md`: §3 инварианты (`:443-455`, особенно I2, I4, I6, I8); §5.1 граница (`:556-595`); §5.2 автомат (`:596-617`); §5.3 владельцы фактов (`:618-628`) — строки `PK_E и полином` и `share`; §5.4 модель отказов (`:629-646`) — строки про persist и «Частичный успех»; §5.5 файлы (`:647-649`); §5.6 сценарии (`:650-666`); §7 строка 5.1 и её абзац «наполовину / проверка / риск» (`:711-733`).
3. `.dpos-study/DECISIONS.md`: П-3 (`:86`) и Д-7 (`:44-47`) — оба «принято»; **отклонение от принятого решения — hard-stop, не твоя правка**: если реализация требует изменить П-3 или Д-7, остановись и запиши предложение в §0.
4. `.dpos-study/history/E5-PRECONDITIONS.md` §0 п.4 (Д-7 после 4.1), §2 (а) (фасад `CommitteeReads`).
5. `.dpos-study/history/E5-0-BOUNDARY.md` А§2 «Отклонения от §5.1» (Д-1…Д-8) и В§2 (Д-9…Д-14) — форма трейта как она есть; В§5 «Что осталось для 5.1».

## Якоря, открытые оркестратором в этой сессии [KNOWN] — опирайся, не перепроверяй

- Граница модуля: `beacon/mod.rs:56-86` (подмодули приватны), `:118-124` (`pub use`), `:127-129` (`pub(crate) use`), `:131-171` (тестовая дверь, 25 имён + 6 под фичей).
- Трейт `Beacon` — `surface.rs:126-219`: `seed` `:133`, `terminal_seed` `:138`, `mandatory_at` `:143`, `can_participate` `:151`, `signer` `:154`, `oracle_for` `:163`, `ensure_key` `:167`, `observe_certificate` `:175`, `artifact_bytes` `:182`, `observe_epoch` `:190`, `observe_cert` `:196`, `subscribe` `:210`, `faults` `:218`. Внутренний `pub(super) trait Randomness` `:475`.
- **Главная ловушка захода, разобрана оркестратором по телам.** `observe_epoch` (`surface.rs:2674-2688`) и `observe_cert` (`:2690-2695`) ведут ТРИ ретенции: `w3_backfill(&self.keys, &self.resolver, reconciled)` (`:2215`) и `self.keys.retain_from(oldest)` — умирают с `keys.rs`/W3 здесь; `self.seeds.retain_quarantine_from(oldest)` и `self.seeds.retain_terminal_from(oldest)` — умирают только с `SeedIndex` в 5.2. Значит заход А снимает из обоих методов ключевые ноги и **ОСТАВЛЯЕТ сами методы** с seed-ногами. Удаление методов с трейта — строка 5.2. Это исполнение проекта §5.1 («после 5.1 … и 5.2 …»), а не отклонение.
- `CeremonyStore = Arc<RwLock<BTreeMap<u64, (CeremonyOutput, Share)>>>` — `actor.rs:173`; `adopt_share` `:1014`; вызовы `:1624` (основной путь) и `:2392` (recompute).
- Гейт `validate_share_on_poly` (`outcome.rs:99`) стоит ТОЛЬКО на recompute-пути — `actor.rs:2358`, дока `:2314`.
- `DkgOutcome = Output` с полем `public: Sharing` — `outcome.rs:29-30`.
- `oracle.rs`: `BeaconOracle` `:63`, `minted_at_for` `:115` (читает held-карту `CeremonyStore`), `with_material` `:153`.
- Копия артефакта в share-файле: v2 `(output, share, artifact)` — `share_state.rs:10`, `:22-23`, `:74`, `:92-94`; `inner_frame` `:94`; разбор `:128-149`; `encode` `:221-234`; `persist` `:300-316`; загрузка `:322-355`; окно `:689-690`.
- `keys.rs`: `InvalidSeed` `:56`, `KeySource` `:73`, `BeaconKeys` `:148` (`with_persistence` `:189`, `cached_only` `:216`, `cached_at_least` `:231`, `attested` `:250`, `set_pk` `:272`, `retain_from` `:365`, `on_invalid_seed` `:397`, `subscribe` `:414`), `AgreedKeyAt` `:433`, `AgreedKeys` `:447`, `KeySources` `:496`, `memoise_carry` `:615`; W1/W3 названы писателями стора в доках `:69`, `:74`, `:143`, `:246`, `:340`, `:515`.
- `resolve.rs`: `mint_diverges_from_attested` `:41`, `beacon_share_resolver` `:69`; тест `a_stable_committees_attested_mint_outlives_the_retention_window` `:304`.
- `artifact.rs`: `ArtifactStore` `:418` (`insert` `:452`, `get` `:477`, `has` `:483`, `epochs` `:488`, `with_persistence` `:429`), `verify_artifact` `:246`, `verify_artifact_for_epoch` `:301`, `encode/decode_artifact` `:313`/`:318`, `restart_replay` `:537`, `ArtifactJournal` `:565`, `spawn_writer` `:655`, `PULL_MIN_INTERVAL = 5 s` `:171`, `PULL_TIMEOUT = 8 s` `:178`, `CommitteeSource` `:189`, `MAX_ARTIFACT_SIZE` `:161`.
- `LiveBeacon::signer_scheme` — `surface.rs:2478`; гейт `mandatory_at` УЖЕ стоит с 5.0 (комментарий `:2484-2490`).
- `for_keys` — `surface.rs:1009`, потребители только два теста в том же файле (`:1864`, `:1881`). `for_seeds` `:984` — это 5.2, НЕ трогать.

## Что сделать

### А.1 `Sharing` из артефакта, а не из копии `Output`

Проект §5.3 (строка `PK_E и полином`) требует: `Sharing` строится из `artifact.group_key`, и копия `Output` в `CeremonyStore` уходит. §0.7(г) проекта помечает достаточность этого как [ГИПОТЕЗА], проверяемую ТИПОМ (`DkgOutcome = Output` с полем `public: Sharing`, `outcome.rs:29-30`) и стендом B1–B5. Проверь типом ПЕРВЫМ делом и запиши вывод: хватает ли `artifact.group_key` для `verify_partial`/`sign_partial` (`oracle.rs:153-170`) и для `minted_at_for` (`:115`). Если НЕ хватает — назови, чего именно не хватает, с file:line, и НЕ ломай `CeremonyStore` молча: это либо отклонение Д-nn с ценой, либо hard-stop по П-3.

### А.2 `validate_share_on_poly` перед ЛЮБЫМ `adopt_share`

Сейчас гейт стоит только на recompute-пути (`actor.rs:2358`). Требование П-3 (`DECISIONS.md:86`) и §5.5 — обязательный перед каждым `adopt_share` (`actor.rs:1014`, вызовы `:1624` и `:2392`). Сделай гейт свойством самой `adopt_share`, а не её вызывающих, чтобы «забыть его» стало невозможно. Что делать при провале гейта на ОСНОВНОМ пути (до сих пор недостижимом): исход должен быть явным — назови его и обоснуй по §5.4.

### А.3 Правила persist и частичный успех

По §5.4, три строки дословно: (1) **ошибка persist share** ⇒ share НЕ принимается, узел verify-only, состояние не `Keyed`, событие `Stalled{PersistFailed}` + gauge, повтор по часам; (2) **ошибка persist артефакта** ⇒ артефакт ПРИНЯТ в RAM (иначе локальная ошибка диска останавливает верификацию и исполнение), запись повторяется, то же событие + gauge; порядок: артефакт в RAM и в очередь записи РАНЬШЕ share; (3) **частичный успех** (share на диске, артефакта нет; рестарт) ⇒ `Acquiring{artifact}` с сохранённым share, и это НАЗВАННОЕ изменение живучести — до прихода артефакта от пиров σ эпохи `Pending`, исполнение паркуется.
Оговорка: вариант события `Stalled{reason}` проект отдаёт строке 5.3 (Д-5 журнала 5.0 — «производителя нет до 5.3»). Реши и запиши: либо ввести вариант здесь (тогда это Д-nn с причиной «производитель появился раньше потребителя»), либо выразить исход тем, что есть (gauge + `ParticipationChanged`), и оставить имя 5.3. Мёртвую поверхность не вводить.

### А.4 Удаления

`keys.rs`, `key_journal.rs`, `resolve.rs`, W1/W3 (писатели стора ключей), копия артефакта в share-файле (формат v2 теряет третье поле), `Output` в `CeremonyStore`, `for_keys` (`surface.rs:1009`) вместе с двумя его тестами, `Carried`/`LocalDkg` как провенанс. ОДНА ретенция — `SCHEME_RETENTION_EPOCHS`; `JOURNAL_RETENTION_EPOCHS = 1` (`mod.rs`) по §5.3 уходит — проверь, есть ли у него ещё читатели, и если есть, назови их.
**Размен W1 назван проектом и обязан быть в журнале:** сегодня share без артефакта недостижим (finalize берёт pinned-набор только из `agreed_pinned`, который пишет только `on_artifact`; recompute читает `outcome_at` = `ArtifactStore`), поэтому «член доделал DKG, а артефакт потерял» не существует; единственный реальный случай — рестарт после провала sync артефакта при живом share-файле, где сегодня ключ даёт копия в share-файле или W1, а в проекте — `Acquiring{artifact}` у пиров. Проверь ОБА утверждения по коду и запиши результат: если «share без артефакта» ДОСТИЖИМ каким-то путём, который проект не назвал, это находка §0, и удаление W1 нужно пересмотреть.
**`share_state.rs` v1:** по §8 формат v2 и AEAD с AAD `(tag, version, epoch)` остаются (минус копия артефакта); v1 удаляется. Что делать с файлом v1 на диске при старте — назови исход явно.

### А.5 Что в заходе А НЕ делается (записать в §5, не делать)

`carry.rs` и `Д-7` (бит `changed`, `maybe_start`, `committee_pair_for`, страж `!(bit || committed)`), форма `CommitteeReads`, артефактная половина `follower.rs`, хвосты 5.0 Д-10/Д-11/Д-12 (`absent_unregistered`, `ArtifactFetch` → трейт, `PinEffort` внутрь) — всё это **заход Б** той же строки. Удаление `observe_epoch`/`observe_cert` с трейта, `for_seeds`, `certify.rs`, `follower.rs` целиком — строка 5.2. `Stalled{reason}` как полноценный набор состояний — 5.3.
**Порядок-риск, который заход А обязан разрешить и записать:** `keys.rs` умирает здесь, а `carry.rs` живёт до Б, при этом `AgreedKeys::new(at, dkg_qual)` берёт `DkgQualFor` из `carry.rs`, а `signer`-резолвер зависит от `chain_key_epoch_memoised`. Если удалить `keys.rs` без `carry.rs` не получается — скажи это прямо с file:line и предложи: либо перенести границу заходов (тогда Д-nn), либо оставить в А тонкий шов. Не «сделать заодно и Б» молча.

### А.6 Тесты захода

1. **C7 ОБЯЗАН упасть и быть переписан.** `testbed::tests::a_zero_overlap_boundary_halts_the_chain_verify_only` (`testbed/tests.rs:2298`, доккоммент `:2276-2297`) сегодня пинует ФАКТ «нулевое пересечение комитетов на границе останавливает цепь молча» (R-121/R-122), и его фальсификатор — «any node crossing its park height». Заход А закрывает R-121/R-122 приобретением артефакта для НЕ-члена живой эпохи, значит утверждение инвертируется: обе половины переходят границу (выходящая добирает артефакт эпохи 3 «вперёд», входящая — эпохи 2 «назад»), `prev_randao` всех узлов побайтно равны. Перепиши тест под новое утверждение, СОХРАНИВ его фальсификатор в новой форме (что упадёт, если свойство не держится), и назови в журнале, что старый тест доказывал и что доказывает новый. Фейк не врёт: артефакт проверяется `verify_artifact` против `committee[minted_at]` из `FakeStaking`.
   **Если C7 после захода остаётся ЗЕЛЁНЫМ в старой форме — заход не сделан.** Это единственное живое свидетельство закрытия R-121/R-122.
2. Тест persist-ошибки share: `share_dir` read-only ⇒ узел verify-only + наблюдаемое событие, НЕ подписант.
3. Тест persist-ошибки артефакта: узел продолжает верифицировать, отказ записи виден.
4. Тест частичного успеха: share-файл есть, партиция артефактов пуста ⇒ после рестарта `Acquiring` → ключ от пиров.
5. `a_stable_committees_attested_mint_outlives_the_retention_window` (`resolve.rs:304`) пишется заново над новым владельцем — файл удаляется, свойство остаётся.
6. Мутация ≤ 2 по несущим ассертам новых тестов.

Детерминизм ×3 по каждому новому и переписанному стенд-тесту.

## Журнал `.dpos-study/history/E5-1-A.md` (короткая форма, без «инвентаризации до»)

§0 Прямые ответы, нумерованные, каждый с [KNOWN]/[ГИПОТЕЗА] и file:line: (1) хватает ли `artifact.group_key` для всего, что читало `Output` — по типу и по прогону; (2) где теперь стоит `validate_share_on_poly` и почему его нельзя забыть; (3) три правила persist — как выражен каждый исход и что наблюдаемо; (4) что удалено, посписочно, и сколько строк продакшна ушло (`wc -l` до/после по `beacon/`); (5) размен W1: достижим ли «share без артефакта» иным путём — да/нет со свидетельством; (6) порядок-риск `keys.rs` без `carry.rs`: как разрешён; (7) C7: старое утверждение, новое, новый фальсификатор, вывод прогона verbatim; (8) остальные тесты: имя / что утверждает / «красный до правки» verbatim или мутация; (9) прогоны verbatim: `--lib`, clippy БЕЗ фичи и с фичей, `fmt --check`, ×3; (10) что в проекте §5 оказалось неверным по коду; (11) запрещённого сделано — ноль или список; (12) слабое место: что НЕ проверил.
§1 Отклонения Д-nn (нумерация строки: 5.1а-Д-1, 5.1а-Д-2, …): что иначе / причина file:line / меняет ли П или Д (если меняет — это hard-stop, и он идёт в §0). §2 Тесты (таблица). §3 Логические коммиты: заголовки Conventional Commits (`refactor(beacon)!:` / `feat(beacon):` / `test(testbed):`) и какие группы НЕ собираются по отдельности. §4 Где проверка слабее всего. §5 Что осталось заходу Б и строкам 5.2/5.3/5.4.

Ты НЕ коммитишь. Заканчивай, когда всё сделано и твои прогоны зелёные (кроме C7 в старой форме — он обязан быть переписан, а не зелёным); невозможное — сделать остальное и назвать в §5. В финальном ответе — только §0.

# Ревью строки 5.2, заход А (факт σ: `SeedIndex` — единственный владелец; Д-3 = (в); crash-replay; R-016)

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база `HEAD` = `37691136` (`git rev-parse HEAD`; строки 5.0а и 5.1 закрыты). Объект — **незакоммиченное рабочее дерево**: `git status --short -- crates` = одно удаление (`beacon/certify.rs`), один новый файл (`beacon/seed_index.rs`, untracked — он НЕ попадёт в `git diff`, читай его целиком с диска) и 15 изменённых. **Чужое в `git status`** — `.dpos-study/history/E4-ORCHESTRATOR.md` — не трогать. Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`; `beacon/` = `crates/dpos/consensus/src/beacon/`.

## Правила

1. **Агентов не запускать.** Спека и код обязаны сидеть в одном контексте.
2. **Git только на чтение.** Единственный файл на запись — `.dpos-study/history/E5-2-A-REVIEW.md`. Мутации продакшн-кода ради чувствительности тестов — только с откатом и `md5sum` до/после.
3. Каждое утверждение — `file:line` дерева и, где есть «до», `HEAD:file:line` (`git show HEAD:crates/…`).
4. Комментарии, доки, `.claude/dpos_architecture/` и журнал исполнителя — **не свидетельство**; свидетельство — код и прогон, который ты сделал сам. Для commonware — `.claude/COMMONWARE_INTERNALS.md` ДО чекаута.
5. **Полнота, не фильтр**: все находки, включая мелкие и неуверенные, с серьёзностью (BLOCKER / SERIOUS / MODERATE / MINOR / NIT) и «чем пытался опровергнуть»; не объединять разные находки ради краткости — фильтрует оркестратор.
6. Читать целыми строками (`Read` / `sed -n 'N,Mp'`); никогда `cut -c`/`head -c`. Поиск — **ТОЛЬКО `git grep`** по `crates`: под `crates/dpos/consensus/src/.claude/session-reads/` и под `.claude/dpos_architecture/.claude/` лежат gitignored журналы чужих сессий. Для `.claude/dpos_architecture/` `git grep` пуст — там нужен `grep -rn`, и пустой вывод `git grep` за отсутствие не принимать.
7. `CARGO_BUILD_JOBS=6`. Стенд не гонять параллельно с другой сборкой. Если классификатор среды запретит тебе `cargo test`/`sed -i` — **скажи это в §0(1) прямо** и опирайся на файлы ворот оркестратора; молча не подменяй прогон чтением. Это уже случалось в этом этапе.

## Вход

- **Строка плана**: `.dpos-study/PLAN.md:114` (строка `5.2`) — читай её целиком, это перечень обязательств.
- **Проект**: `history/E5-BEACON-DESIGN.md` §3 (`:443-455`), §5.1 (`:556-595`), §5.3 (`:618-628`), §5.4 (`:629-646`), §5.5 (`:647-649`), §7 строка 5.2.
- **Решения**: `DECISIONS.md` — П-2, Д-3 (= вариант (в)), Д-9, все «принято». Отклонение от них — hard-stop этапа; если найдёшь такое, это BLOCKER.
- **Постановка**: `history/E5-prompts/5.2-A-impl-1.md`.
- **Журнал исполнителя**: `history/E5-2-A.md` — читать **ПОСЛЕ** диффа.
- **Ворота**: прогон оркестратора `c1-*` в `/tmp/claude-1000/-home-djadjka-Work-fluentbase/f6f42c91-6188-4d9c-ae64-a1e9faed9028/scratchpad/gates/` (`c1-status.txt` — коды выхода). База на `37691136`: lib 656/0, стенд с фичей 55/0, без фичи 46/0, node 55/0, reader 64/0, `--test slasher_integration` 16/0, clippy ровно два ЧУЖИХ предупреждения, clippy с фичей 0, `fmt --check` чисто, doc unresolved 6.
- **Найдено оркестратором ДО тебя** (подтверди или опровергни, и найди ОСТАЛЬНОЕ того же класса — это твоя работа, не пересказ):
  - О-5: `dpos.rs:3252-3254` — комментарий «Epoch-relative re-jump gate … `min(serving-window, 1 epoch)`» стоит прямо над `:3255-3257` «R-016: ABSOLUTE» + `JUMP_THRESHOLD`. Верхний описывает удалённый код.
  - О-6: `observe_cert` оставлен пустым дефолтом (`surface.rs:218`), но продакшн-вызов `cert_inlet.rs:758` остался — и `cert_inlet.rs` СТОИТ в списке записываемых файлов захода. Плюс комментарии на снятое поведение: `cert_inlet.rs:2865`, `dpos.rs:3735-3736`, `testbed/stand.rs:354`, `:2673`, `crates/node/src/{cert_inlet.rs:50,dpos.rs:784}`.
  - О-7: `beacon::certify` (удалённый модуль) назван в `beacon/actor.rs:1410` и `engine.rs:44`.

## Форма результата `.dpos-study/history/E5-2-A-REVIEW.md` (§0–§6)

**§0 Прямые ответы** — нумерованные, каждый с [KNOWN]/[ГИПОТЕЗА] и file:line:

1. **Ворота** verbatim из файлов `c1`; расхождения с числами журнала назвать. **Счёт тестов — отдельный вопрос.** База 656, дерево 659 заявлено. Исполнитель приводит баланс: `certify → seed_index` −1, `follower.rs` +1, `cert_inlet.rs` +2, `dpos.rs` +1. Пересчитай сам (`grep -c '#\[test\]'` на `HEAD:` и на дереве, по всем изменённым файлам — не только по этим четырём). Внутри `−1` исполнитель называет ЧЕТЫРЕ снятых теста и ТРИ новых. По каждому из четырёх снятых назови по `HEAD:`-телу: что он доказывал, и что стало со свойством — ушло классом вместе с удалённым кодом, переехало в другой тест (назови его и проверь, что новый тест краснеет от того же дефекта), или осталось БЕЗ теста. Последнее — находка.
2. **Строка плана, обязательство за обязательством.** Перечисли всё, что `PLAN.md:114` требует, и по каждому: сделано / сделано иначе (какое отклонение, держится ли причина по коду) / не сделано. Не пропусти: `hint_finalized` как выход из hold НЕ вводить; «ОСТАТОК: три транзиентные копии σ в executor (`executor.rs:134,239,724`)» — они разрешены до 6.1, проверь, что их ровно три и не прибавилось; `on_invalid_seed` (оркестратор утверждает, что он ушёл вместе с `keys.rs` в 5.1 — проверь по `HEAD:` и назови, остались ли доккомментные упоминания).
3. **`follower.rs` НЕ УДАЛЁН — главный вопрос захода.** Строка требует «удалить … seed-половину и сам файл `follower.rs`». Исполнитель удалил seed-половину и оставил файл, аргумент — §5 журнала: остаток принадлежит П-3 (тип оракула, `share_probe`/`signer`, ступень `ensure_key`), слияние меняет тип оракула и провайдера на пути, который покрыт только `make smoke-cert-follow`. **Проверь аргумент по коду, а не по журналу**: (а) что ФАКТИЧЕСКИ осталось в файле — перечисли по функциям с `file:line`, и по каждой скажи, σ это или ключ; (б) правда ли слияние в `plane.rs` требует смены типа оракула — назови типы обеих сторон; (в) есть ли остаток, который НЕ ключевой и всё-таки остался; (г) твой вердикт: это законное отклонение или невыполненный пункт строки. Если (г) = «невыполненный» — это как минимум SERIOUS.
4. **`SeedIndex` — проверь, что владелец ЕДИНСТВЕННЫЙ.** По `git grep` перечисли всё в дереве, что хранит, кэширует или проносит σ (`BlsSignature` в роли seed, `Seed`, `seed()`, `terminal_seed`): что владеет, что транзитно, и сколько владельцев осталось ФАКТИЧЕСКИ. Отдельно: `seed_journal.rs` — он теперь «только для рестарта»; проверь, что ни один продакшн-читатель не спрашивает журнал в горячем пути, и что его запись не стала вторым источником правды при расхождении с индексом.
5. **Правило вытеснения против удалённой карты.** `oldest_evictable` (`seed_index.rs:392`) + `evict` (`:355`). Проверь: (а) свойства старой terminal-карты, перечисленные по `HEAD:beacon/certify.rs` (`pin_terminal`, `terminal_at`, `retain_terminal_from`) — каждое: несёт правило / не несёт / несёт ИНАЧЕ; (б) есть ли раунд, на который старая карта отвечала, а новый индекс молчит — в частности `epoch_manager.rs:163` (`boundary_base` через `Beacon::terminal_seed`): чем он теперь получает терминал `E-1` и что будет, если индекс держит не тот раунд; (в) может ли правило защитить НЕ ТОТ раунд (док `:379-391` сам это признаёт — оцени последствие, а не наличие признания); (г) `floor = top_epoch − SCHEME_RETENTION_EPOCHS` от старшей эпохи В ИНДЕКСЕ: что происходит, когда узел принял σ эпохи далеко впереди (форджённой или из будущего окна) — не сдвинет ли она floor и не выметет ли защиту у актуальных эпох. (г) — приоритет.
6. **Д-3 = (в), три сайта вердикта.** `spec_exec.rs:92`, `cert_inlet.rs:731`, `cert_inlet.rs:2924`. Третий получает только `warn!`, потому что у `UpstreamResolver` нет ручки `RotateUpstream`. Проверь: (а) это соответствует Д-3 или это дыра — лгущий by-height апстрим не платит ничего; (б) есть ли у этой двери ДРУГАЯ цена (исключение пира, метрика, отказ `deliver`), и если нет — назови, что должно было быть; (в) `Pending` на by-height двери: поднимает ли она want, или σ «зависает» без того, чтобы кто-то попросил ключ.
7. **Поздний `DataFault`.** `Beacon::faults()` → `cert_inlet.rs:472` → `drain_late_verdicts` (`:829`) в голове `ingest`. Проверь: (а) «потребитель ровно один» — структурно ли это (`git grep '\.faults()'`), и что происходит с сообщениями, если приёмник не взят; (б) дренаж на чужом ребре: при остановленном потоке сертификатов поздний фолт не отрабатывается — безвреден ли этот случай на самом деле, или есть сценарий, где ротация нужна именно при остановленном потоке; (в) `charges += fault.refused.min(MAX_UPSTREAM_FAULTS)` и сброс `consecutive_faults = 0` на чистом ingest (`:747`) — разбери порядок: может ли заряд быть стёрт сертификатом, который прибыл ПОСЛЕ него в той же итерации; (г) переполнение канала `faults` — что теряется и заметно ли это.
8. **Автомат `admit`** (`seed_index.rs:187-215`). Проверь каждый переход против свойств, которые несли ТРИ карты на `HEAD` (served / quarantine / terminal): I1 (две разные проверенные σ на раунд), «проверенное не перезаписывается непроверенным», «`Pending ⇒ Pending` last-wins», «отказанная удаляется, а не удерживается». Отдельно: `persist` — что именно уходит в журнал и может ли туда уйти `Pending`; и порядок «промотировали ⇒ записали» против «записали ⇒ промотировали» при падении между ними.
9. **`settle_pending` синхронно в мосте** (`plane.rs:977`, KEY-ветка, ПЕРЕД публикацией `KeyAvailable`). Исполнитель называет это своим слабым местом. Проверь: (а) правда ли это снимает гонку «потребитель разбужен `KeyAvailable` раньше промоутера» — назови, чем гонка была наблюдаема на `HEAD`; (б) цена: сколько BLS-проверок максимум под одним `KeyAvailable` и что на это время не публикуется; (в) дешёв ли `NoKey`, как утверждает журнал — по коду оракула, а не по доку; (г) может ли `settle_pending` запаниковать или заблокироваться на отравленном мьютексе и утащить мост.
10. **crash-replay (E5-03).** `seed_via_beacon` (`dpos.rs:741`), `CertSeed` (`:708`), расщепление `crash_recover_defer_or_fatal` → `crash_recover_defer` (`:466`). Проверь: (а) обе ветки прежней функции сохранены дословно — сверь с `HEAD:dpos.rs:438`; (б) `Absent` на ОТКАЗАННОЙ σ и `Absent` на «сертификат чужого раунда» — один исход на два разных факта: приводит ли это к тому, что узел молча берёт σ из другого источника после доказанной лжи; (в) не может ли `Pending ⇒ Defer` стать вечным (ключ не придёт никогда — какая эпоха, какой выход); (г) пин раунда сохранён и означает то же, что на `HEAD`.
11. **R-016.** `dpos.rs:2480` и `:3257`: `JUMP_THRESHOLD.min(interval)` → `JUMP_THRESHOLD`. Это изменение продакшн-ЖИВОСТИ, и предусловие строки — «только после того, как σ-hold перестанет зависеть от re-jump». Проверь предусловие по коду: (а) у hold действительно появился собственный выход (`Pending` + settle по `KeyAvailable`) и он НЕ зависит от того, придёт ли артефакт — назови, кто его тащит и что будет, если не придёт; (б) какие ещё пути опирались на re-jump как на выход — `git grep` по `re_jump`/`ReJump`/`JUMP_THRESHOLD`, каждый разобрать; (в) devnet: `JUMP_THRESHOLD` абсолютный при коротком `interval` — какой сценарий девнета ломается и есть ли в `devnet/` кейс, который это поймает (читать `devnet/` можно, ПРАВИТЬ нельзя); (г) стендовые тесты задают порог сами (`testbed/stand.rs:2476`) — проверь, что ни один из них не потерял покрытие продакшн-формулы.
12. **Дедуп ERROR-защёлки.** `follower.rs:379` (`first_seed_refusal`) и парная у плоскости. Проверь: защёлка на эпоху, а не глобальная; сбрасывается ли она и когда; не съедает ли она ПЕРВУЮ строку другой эпохи; и что происходит с метрикой (счёт должен остаться полным, глушится только строка лога).
13. **Пустые дефолты `observe_epoch`/`observe_cert`** (отклонение 5.2а-Д-1, `surface.rs:206`, `:218`). Проверь аргумент по коду: держатель `observe_epoch` — `epoch_manager.rs:1225` (файл запрещён, аргумент держится). Держатель `observe_cert` — по журналу `byzantine_roles.rs:404`, но это ДЕЛЕГИРУЮЩИЙ враппер, существующий ради пересылки, и вызов `cert_inlet.rs:758` стоит в РАЗРЕШЁННОМ файле. Твой вердикт: аргумент держится или пункт не выполнен. Плюс: вызов пустого тела в продакшне — это мёртвый код или он что-то значит.
14. **Граница и гигиена.** `pub use`/`pub(crate) use` в `beacon/mod.rs` — `HEAD` против дерева, имя за именем (включая алиас `SeedStore`, `mod.rs:164-170`). Новые `pub`, `#[allow]`, `todo!`, таймеры вместо событий, поллинг, `unwrap`/`expect` на продакшн-пути. Правки вне списка: `executor.rs` заявлен как тронутый ТОЛЬКО в `#[cfg(test)]` — проверь семантически (нет ли правки, формально тестовой, но меняющей продакшн-поведение через фикстуру); `lib.rs` заявлен как «только доккоммент константы» — сверь.
15. **Дрейф доков.** По `git grep` в `crates` и `grep -rn` в `.claude/dpos_architecture/` найди все места, называющие удалённые символы: `certify.rs`/`beacon::certify`, `SeedStore` (как тип, не алиас), `pin_terminal`, `terminal_at`, `retain_terminal_from`, `retain_quarantine_from`, `promote_epoch`, `quarantined_epochs`, `quarantine`, `waiters`, `wait_for`, `prune_waiters`, `terminal_seed_at`, `for_seeds`, `promote_quarantined`, `on_invalid_seed`, `observe_cert`, `observe_epoch`, «промоутер». Список с `file:line`; править НЕ надо. `.claude/dpos_architecture/` — отдельной таблицей, это вход Ф7.
16. **Где журнал вводит в заблуждение** — по пунктам §0 журнала (особенно §0(10): исполнитель заявляет ТРИ опровержения чужих якорей — проверь каждое сам) и §1 (четыре отклонения). Каждый пункт подтвердить или опровергнуть.
17. **Слабые места, названные исполнителем** (§0 п. 12 и §4): follower без девнет-прогона; дренаж на чужом ребре; общий бюджет `SEED_RETENTION`; `settle_pending` синхронно. По каждому — твоя независимая оценка серьёзности, и есть ли ЧЕТВЁРТОЕ, которое он не назвал.
18. **Hard-stop оркестратора**: (2) требуется ли менять П-2, Д-3 или Д-9 — да/нет со свидетельством; (3) есть ли BLOCKER, на который проект не отвечает; (4) опроверг ли стенд проект на центральном пути.
19. **Где твоя проверка была слабее всего.**

**§1 Находки** — таблица `id | серьёзность | file:lines дерева | HEAD-якорь | что не так | чем пытался опровергнуть | уверенность`; id `C-nn`. BLOCKER — потеря свойства, которую проект не предписывал; отклонение от П-2/Д-3/Д-9; тест, зелёный при сломанном свойстве. **§2 Поведение по ханкам** против `HEAD` (группируй по файлам, начни с `seed_index.rs` целиком — он untracked и в дифф не попадает — затем `surface.rs`, `cert_inlet.rs`, `dpos.rs`, `plane.rs`, `follower.rs`). **§3 Граница** — таблица имён. **§4 Ворота** verbatim + твои точечные прогоны. **§5 Оставить как есть** — непустой и конкретный. **§6 Замечено вне рамок 5.2** — для 5.3/5.4.

В финальном ответе — только §0 (кратко) и таблица §1.

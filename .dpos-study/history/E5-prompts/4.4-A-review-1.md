# Ревью строки 4.4 (регрессия 4.2-Б2 / R-131): вход follower'а ниже блока активации

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база `HEAD` = `0dd39f60` (проверь `git rev-parse HEAD`). Объект — **незакоммиченное рабочее дерево**: три файла Rust (`crates/dpos/consensus/src/{dpos.rs, cold_start_jump.rs, beacon/artifact.rs}`) и три файла харнесса (`devnet/local-dpos-smoke/dpos_harness/{cases/smoke/verdicts_follow.py, cases/smoke/asserts_follow.py, tests/test_smoke_follow_cases.py}`). Новых файлов нет — весь объект виден в `git diff`. **Чужое в `git status`** — `.dpos-study/history/E4-ORCHESTRATOR.md` — не читать и не трогать. Пути без префикса — от `crates/dpos/consensus/src/`.

Это строка ЗАКРЫТОГО этапа Э4, вынесенная в Э5 как регрессия. Рамка: один дефект (R-131, follower-половина) и его правка. **Валидаторская половина (`FreshMigration`) в границу НЕ входит** и правиться здесь не должна — если найдёшь её правку в дереве, это находка.

## Правила

1. **Агентов не запускать.** Решение, проект и код обязаны сидеть в одном контексте: агент, читающий одну сторону, не увидит, где другая её перевирает. Не запускать агента и для проверки собственной работы.
2. **Git только на чтение.** Никаких `git add`/`commit`/`checkout`/`stash`/`restore`/`reset`. Единственный файл на запись — `.dpos-study/history/E4-4-A-REVIEW.md`. Мутации продакшн-кода ради проверки чувствительности тестов — только с откатом и `md5sum` до/после в тексте.
3. Каждое утверждение — `file:line` дерева и, где есть «до», `HEAD:file:line` (`git show HEAD:crates/…`).
4. Комментарии, доккомментарии, `.claude/dpos_architecture/` и журнал исполнителя — **не свидетельство**. Свидетельство — код и прогон, который ты сделал сам. В этой кодовой базе доки дрейфуют: последние пять проходов каждый нашёл устаревший. Для commonware — `.claude/COMMONWARE_INTERNALS.md` ДО чекаута, для reth — `.claude/RETH_INTERNALS.md`.
5. **Полнота, не фильтр**: все находки, включая мелкие и неуверенные, с серьёзностью (BLOCKER / SERIOUS / MODERATE / MINOR / NIT), с полем «чем пытался опровергнуть и почему не вышло»; не объединять разные находки ради краткости. Фильтрует оркестратор — ты поставляешь полноту.
6. Читать целыми строками (`Read` / `sed -n 'N,Mp'`); никогда `cut -c`/`head -c`. Поиск по коду — **ТОЛЬКО `git grep`**: под `crates/dpos/consensus/src/.claude/session-reads/` лежат gitignored журналы чужих сессий, и `grep -rn` по `crates/` выдаёт из них мусор. Для `.claude/dpos_architecture/` наоборот — `git grep` там пуст, нужен `grep -rn`, и пустой вывод `git grep` за отсутствие не принимать.
7. Разрешённые формы команд: `cargo test`, `cargo clippy`, `cargo fmt --check`, `git grep`, `git show`, `git diff`, `sed -n`, `sed -i` (с откатом), `md5sum`, `python3`, `make harness-test` (из `devnet/local-dpos-smoke`), `cp`. `CARGO_BUILD_JOBS=6`; стенд не гонять параллельно с другой сборкой. **Если классификатор среды запретит тебе какую-то команду — скажи это в §0(1) прямо** и опирайся на файлы ворот оркестратора; молча подменять прогон чтением нельзя. Это уже случалось на этом этапе.
8. `make smoke-*` НЕ запускать — живая приёмка за оркестратором.

## Вход

- **Строка плана**: `.dpos-study/PLAN.md:106` (строка `4.4`) — читай целиком, это перечень обязательств, включая половину про `devnet/`.
- **Проект**: `history/E4-CORE-DESIGN.md` §5.2, особенно `:514` («`sync_to` — единственный писатель EL-тегов на этом пути, и это не меняется; меняется только его вход») и `:516` («Первые два идут через `Frontier`»). Именно это требование 4.2-Б2 нарушил.
- **Решение**: `history/E5-COLDSTART-DECISION.md` (шесть решений, принято Fable 5.1). Исследование `history/E5-COLDSTART-RESEARCH.md` (вариант V2), контр-разбор `history/E5-COLDSTART-COUNTER.md` (13 атак, 5 ландят, включая A10). Отклонение от решения допустимо, но должно быть НАЗВАНО как `4.4а-Д-nn` в журнале и обосновано кодом.
- **Дефект**: `.dpos-study/REGISTER.md:1363` (R-131).
- **Постановка исполнителю**: `history/E5-prompts/4.4-A-impl-1.md`.
- **Журнал исполнителя**: `history/E4-4-A.md` — читать **ПОСЛЕ** того, как сам разобрал дифф.
- **Ворота**: прогон оркестратора тегом `d4` в `/tmp/claude-1000/-home-djadjka-Work-fluentbase/f6f42c91-6188-4d9c-ae64-a1e9faed9028/scratchpad/gates/` (`d4-status.txt` — коды выхода; `d1` того же каталога ОТРАВЛЕН — его `--lib` мерился по дереву до последней правки, не опирайся на него). База на `0dd39f60`: `--lib` 665/0, стенд с фичей 55/0, без фичи 46/0, node 55/0, reader 64/0, `--test slasher_integration` 16/0, clippy ровно два ЧУЖИХ предупреждения, clippy с фичей 0, `fmt --check` чисто, doc unresolved 6, `make harness-test` 2 красных предсуществующих.
- **Установлено оркестратором ДО тебя** (подтверди или опровергни; и найди ОСТАЛЬНОЕ того же класса — это твоя работа, а не пересказ):
  - К-81: `sync_to_checkpoint` (`cold_start_jump.rs:639`) берёт только `&self` и хэш — `CertUpstream` ему не нужен, поэтому checkpoint-ветвь обязана стоять ВЫШЕ теста на апстрим. Первая версия правки ставила её ниже и парковала узел с исправным входом. Переставлено; красное воспроизведено оркестратором (`left: WaitLocal / right: Checkpoint`).
  - К-86/К-87: две красные юниты `make harness-test` (`test_smoke_boundary_cases.py:491` и `:528`) предсуществуют этой строке — текст кода на `bec013a7`, на `HEAD` и в дереве байт-в-байт одинаков. Не приписывай их этой правке; но если найдёшь ТРЕТЬЮ красную — это находка.

## Что ответить — `.dpos-study/history/E4-4-A-REVIEW.md`, секции §0–§6

**§0 Прямые ответы** — нумерованные, каждый с `[KNOWN]`/`[ГИПОТЕЗА]` и `file:line`:

1. **Ворота** verbatim из своих прогонов (все одиннадцать, включая `make harness-test`); расхождения с числами журнала и с файлами `d4` назвать. **Счёт тестов — отдельный вопрос**: база `--lib` 665, дерево заявлено 672. Пересчитай сам (`grep -c '#\[test\]'` на `HEAD:` и на дереве по каждому изменённому файлу) и скажи, весь ли прирост в `dpos::cold_start_kind_tests` и не ушёл ли попутно какой-нибудь тест. `filtered out` в стендовых воротах должен вырасти ровно на число новых lib-тестов — сверь.
2. **Строка плана `PLAN.md:106`, обязательство за обязательством**: сделано / сделано иначе (какое отклонение, держится ли его причина по коду) / не сделано. Не пропусти половину про `devnet/`: `CF_KEY_LINE` → `beacon:`, `beacon/artifact.rs` `debug!` → `info!`, фаза 1 `smoke-cert-follow` пинует строку привода, **и — отдельно — `smoke-cert-cascade` фаза 3 (пара строк отказа и `CC_REJECT_S = 420`), которую оркестратор из захода ИСКЛЮЧИЛ намеренно**: её бюджет нельзя измерить, пока тир-1 красный. Твой вердикт: оставляет ли это исключение строку невыполненной, или это законная открытая приёмка. Также: первый пункт карты был стоп-условием (юнит на `epoch_committee_snapshot(0, genesis_hash)` против генезиса девнета) — проверь, чем он кончился и что из этого следует для решения.
3. **Марш из пяти входов — главный вопрос захода.** `follower_entry` (`dpos.rs:1571-1617` примерно) и `FollowerEntry` (`:1517-1560`). Проверь: (а) ПОЛНОТА — существует ли достижимая конфигурация входов, которой марш не отвечает или отвечает не тем армом; перечисли конфигурации как таблицу истинности по `(holds_activation, rf_num≥activation, has_checkpoint, has_upstream, probes)` и назови арм на каждой строке; (б) ПОРЯДОК — для каждой соседней пары скажи, что именно ломается при перестановке, и есть ли юнит, который от этой перестановки краснеет (у пары checkpoint/upstream такой юнит есть — проверь ОСТАЛЬНЫЕ пары тем же способом, мутацией с откатом); (в) двойное вычисление через `PeerProbes`: каллер зовёт функцию дважды — сначала с `None`, потом с заполненными пробами — могут ли два вызова разойтись так, что узел зацикливается между армами, и что удерживает peer-free префикс от расхождения со вторым вызовом.
4. **Арифметика `Certificate { target }`** — приоритетный пункт. `floor = activation + K`; `last_readable = activation + (e_max+1)·interval − 1`; `target = latest.min(last_readable).max(floor)`. Проверь по коду `order_block::result_target` и `cert_follow::fetch_verified_boundary`: (а) может ли `.max(floor)` поднять `target` ВЫШЕ `last_readable` или выше `latest` — то есть попросить высоту, которую апстрим не обслуживает или чей комитет не читается, — и что тогда происходит (отказ с повтором или фатал); (б) верно ли, что ниже `activation + K` сертификат несёт `B256::ZERO`, и что цель ровно `activation + K` даёт EVM-хэш блока `activation`; (в) `saturating_*` на каждом шаге — есть ли вход, на котором сатурация даёт тихо неверную цель вместо отказа; (г) `e_max` пробуется по `0..=MAX_COMMITTEE_LOOKAHEAD_EPOCHS` — что если читаемая эпоха НЕ нулевая и не непрерывна (дырка в середине окна).
5. **Шов между чистой функцией и армом.** Исполнитель сам называет его непроверенным (§2 журнала: у `FakeElSync` нет `CertUpstream`, `cold_start_jump.rs:1271-1285`). Проверь ПО КОДУ арма (`dpos.rs:3141-3359` примерно), что каждый из пяти вердиктов действительно приводит к тому приводу, который обещает его доккомментарий: назови для каждого арма конкретный вызов (`sync_to`, `sync_to_checkpoint`, `wait_for_activation_block`, повтор) и скажи, совпадает ли он с вердиктом. Отдельно: (а) `ChainBelowActivation` — это «спроси снова»: есть ли сон между попытками (не busy-spin) и не фатал ли он; (б) отказ сертификатного входа — повтор или фатал, и правильно ли это; (в) есть ли путь, на котором арм ИГНОРИРУЕТ вердикт функции.
6. **`assert_l1_checkpoint` после match, считаемый ОТ ПОСАДКИ** (`dpos.rs`, около `:3120` на `HEAD`). Аргумент К-73: сертификатный вход садится на самую низкую законную высоту, поэтому обратный порядок превратил бы выживаемый парк в фатальный отказ. Проверь этот аргумент для КАЖДОГО из пяти армов по очереди: после какого арма ассерт срабатывает, на какой высоте он считается, и есть ли арм, после которого он теперь отказывает там, где на `HEAD` не отказывал. Это изменение продакшн-живости — самая дорогая ошибка в заходе.
7. **`beacon/artifact.rs`** — изменена одна строка (`debug!` → `info!` около `:941`). Проверь: (а) что оверлей харнесса читает ИМЕННО эту строку и текст совпадает символ в символ; (б) частоту — строка стоит под `if first`, а метрика `follower_artifact_adopted.inc()` (`:939`) безусловна: правильно ли это распределение и не потерял ли кто-нибудь счёт; (в) строка 5.1 изменила ЧЕТЫРЕ вещи (уровень, префикс, хвост текста, частоту) — восстановлены ли все четыре в том виде, в каком их ждёт вердикт, или только уровень.
8. **Харнесс: проверки, которые могут быть зелёными при сломанном свойстве** — приоритетный пункт, у оркестратора он записан как К-82. Исполнитель (а) добавил `CF_ENTRY_LINE` + `evaluate_entered_by_el_drive` (`verdicts_follow.py:344-374`) и `ctx.check` в `asserts_follow.py:181-197`; (б) добавил driven-негатив `test_cert_follow_FAILS_when_the_follower_was_CARRIED_rather_than_entering`; (в) **переписал три негативных мира** (`_no_key`, «драйвер не логировал отказ», offline-pip SKIP), потому что они глушили лог честного follower'а целиком и их ассерты проходили бы на чужом отказе. Пункт (в) он проверил ЧТЕНИЕМ ассертов, а не мутацией — сделай мутацию сам: для каждого из трёх миров убедись, что он отрицает ровно своё свойство и что его тест краснеет от поломки именно этого свойства, а не от любой другой. Плюс: новая проверка фазы 1 — покажи её доказанный путь отказа.
9. **Форма вместо `#[allow]`**: вместо восьми скаляров заведена структура `PeerProbes`. Проверь, что запрет на `#[allow]` не обойдён иначе где-нибудь в дереве (`git grep '#\[allow'` на изменённых файлах против `HEAD`), и что `PeerProbes` не завела лишней публичности.
10. **Граница и гигиена.** Новые `pub`/`pub(crate)`; `todo!`/`unimplemented!`; `unwrap`/`expect` на продакшн-пути; таймеры и поллинг вместо событий (для `ChainBelowActivation` поллинг может быть законным — тогда скажи, почему именно здесь); правки вне трёх Rust-файлов; не тронут ли валидаторский `resolve_cold_start_kind` (`:1421` на `HEAD`) и его `ColdStartKind`.
11. **Дрейф доков.** По `git grep` в `crates` и `grep -rn` в `.claude/dpos_architecture/` найди всё, что теперь описывает не тот код: упоминания того, что арм `Some` не имеет привода EL; `mk_el_sync` как «только для `None`»; `wait_for_activation_block` как единственный путь ниже активации; `CF_KEY_LINE`/`cert-follow:` как строку-свидетеля. Список с `file:line`, править НЕ надо — это вход Ф7. `.claude/dpos_architecture/` отдельной таблицей.
12. **Где журнал `E4-4-A.md` вводит в заблуждение.** По каждому пункту его §1 (отклонения `4.4а-Д-nn`), §2 (остатки) и §3 — подтвердить или опровергнуть. Особое внимание: он заявляет, что мутация 1 не задета перестановкой ветвей, и что вывод мутации оставлен от первого прогона — проверь оба утверждения сам.
13. **Hard-stop оркестратора**: (2) требуется ли менять решение в `DECISIONS.md` — да/нет со свидетельством; (3) есть ли BLOCKER, на который проект и решение не отвечают; (4) опроверг ли стенд решение на центральном пути.
14. **Где твоя проверка была слабее всего** — ранжируй, не исповедуйся.

**§1 Находки** — таблица `id | серьёзность | file:lines дерева | HEAD-якорь | что не так | чем пытался опровергнуть | уверенность`; id `D-nn`. BLOCKER — потеря живости, которой решение не предписывало; правка валидаторской половины; тест, зелёный при сломанном свойстве. **§2 Поведение по ханкам** против `HEAD`, по файлам, начиная с `dpos.rs`. **§3 Достижимость** — таблица истинности марша из §0(3а). **§4 Ворота** verbatim + твои точечные прогоны и мутации с `md5sum`. **§5 Оставить как есть** — непустой и конкретный. **§6 Замечено вне рамок 4.4** — одной строкой на пункт, не развивая.

Если заметишь что-то существенное вне рамок строки — одна строка в §6, не гонись за этим. Рамка: один дефект и его правка, не пересмотр холодного старта.

<tone_preference>Отвечай кратко и по существу. В финальном ответе — только §0 сжато и таблица §1.</tone_preference>

# Строка 5.4, заход А — один клок: бикон на `ordering_tip`, три фидера и tee сняты

Ты — исполнитель (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `08764870` (код 5.4-Б — `a1337ab2`). Пути без префикса — от `crates/dpos/consensus/src/`. Рабочее дерево чисто, кроме чужого `.dpos-study/history/E4-ORCHESTRATOR.md` — не читать, не трогать.

## Рамка

Строка 5.4 (`.dpos-study/PLAN.md`, `| [ ] 5.4 |` — блок «Заход Б сделано» прочитай), заход А. Карта — `.dpos-study/history/E5-ORCHESTRATOR.md`, «Карта захода А строки 5.4» (якоря перепинованы оркестратором, опирайся). Дизайн — `history/E5-BEACON-DESIGN.md` §5.3 и находки E5-06 (три фидера снаружи), E5-40 (имена партиций). Журнал Б — `history/E5-4-B.md` (§0.3: доказательство sweep, Д-5.4Б-3 — tee опережает tip; этот заход его закрывает).

## Что сделать

1. **Один клок.** `ValidatorInputs.heights: mpsc::Receiver<u64>` (`beacon/plane.rs:530`) → `clock: tokio::sync::watch::Receiver<u64>`; актор (`beacon/actor.rs:1235-1241`) слушает `clock.changed()` и берёт `*clock.borrow_and_update()`; clamp `max` в `on_height` остаётся. Источник — `FluentApp.ordering_tip` (`application.rs:368/:429/:1080/:463`), тот самый watch, что читает `epoch_manager.rs:531`. Watch создаётся в узле и в стенде ДО `beacon::build` и отдаётся в `FluentApp` (`with_ordering_tip(sender)` на месте `with_dkg_heights`, `application.rs:452`; `outer.rs:448/:998`) — `epoch_manager` продолжает брать `app.ordering_tip()`. Seed после рестарта — marshal сам шлёт `Update::Tip` из старшей сохранённой финализации на старте (`.claude/COMMONWARE_INTERNALS.md:167,197` → `marshal/core/actor.rs:397-402` в pinned checkout — открой и подтверди).
2. **Снять фидеры.** (а) Поллер `fin + K` (`crates/node/src/dpos.rs:1416-1426`, `:1469`, `:1493-1510`) — целиком. Его комментарий `:1426` говорит, что он «drives the transition's GEOMETRY FREEZE until it takes» — ДО удаления установи по коду, кто выполняет эту работу без поллера (что именно замораживает геометрию и от какого события); если никто — **СТОП**, запись в журнал, поллер не трогать, остальное сделать. (б) `LiveFrontierTee` (`cert_inlet.rs:254-283`, посылка `:782`), поле `tee` инлета, узел `node/dpos.rs:778-779`. (в) `dkg_height_tx` везде (`git grep dkg_height_tx -- crates` — 31 строка: application/outer/dpos/cert_inlet/stand/node). (г) `PlaneClock::note_height_drop` (`sync_metrics.rs:366`) — watch без потерь; `record_ordering_tip`/`record_dkg_clock` остаются (два гейджа = видимость вставшего актора).
3. **E5-40.** Четыре имени партиций бикона (`dpos.rs` ~`:218-230`: `SEED_JOURNAL_PARTITION`, `MINT_MEMO_PARTITION`, `ARTIFACT_JOURNAL_PARTITION`, `AGREEMENT_JOURNAL_PARTITION_PREFIX`) → `beacon/mod.rs`; из `dpos.rs` — только реэкспорт, и только для реального внешнего читателя (`git grep` каждого имени).
4. **Тесты.** `testbed/cert_inlet_tests.rs` (14 fn) стоит на списке teed-высот и drop-счётчике (`TeeWiring::{Observed,Production}`, `stand.rs:263`): КАЖДАЯ tee-ассерция — либо в форму «клок бикона = marshal tip после ingest» (наблюдение через `PlaneClock`/гейдж или через probe актора), либо удаляется с названной причиной (свойство было свойством tee, а не узла). Таблица «ассерция → новая форма / удалена: почему» в журнале; молчаливого ослабления нет. Стенд `Beacon::Live` — клок из `ordering_tip` стенда. **Обязательный стенд-тест** «догоняющий валидатор дилит на живом фронтире без tee»: валидатор, чей marshal догоняет через инлет/plane-upstream (та работа, ради которой tee существовал — `cert_inlet.rs:258-261`), дилит свою первую эпоху до дедлайна. **Красный по живости = СТОП** (запись, не обход). Рестарт-тесты стенда (`Ex-4.1c`, `preconditions.rs`) остаются зелёными — клок после рестарта = tip marshal-а. Тест `a_drain_finishes_only_once_the_last_sender_is_gone` (node) остаётся.
5. **Доки в том же изменении**: `.claude/dpos_architecture/{00_preamble,03,08,13,06_staking_layer(:575-590 про три фидера)}.md`, CHANGELOG, `verified-against`; `grep -rn "dkg_height_tx\|LiveFrontierTee\|fin + K\|three feeders\|три фидера\|note_height_drop" .claude/dpos_architecture` — только исторические записи CHANGELOG.

## Правила

- **Агентов не запускать** — ни для работы, ни для проверки себя: спека и код должны сидеть в одном контексте. Git — только чтение.
- Записываемые файлы: `beacon/{plane,actor,mod}.rs`, `application.rs`, `cert_inlet.rs`, `dpos.rs`, `outer.rs`, `sync_metrics.rs`, `epoch_manager.rs` (только если `ordering_tip` меняет форму), `testbed/{stand,cert_inlet_tests,tests,preconditions}.rs`, `crates/node/src/dpos.rs`, их тесты, `.claude/dpos_architecture/*.md`, CHANGELOG, журнал `.dpos-study/history/E5-4-A.md`. Остальное — только чтение.
- Не переформатировать нетронутое; никаких `#[allow]`, новых `unwrap`/`expect` на продакшн-пути, новых `pub` наружу `beacon/` (кроме реэкспорта имён по п. 3).
- Cargo (`CARGO_BUILD_JOBS=12`): `cargo test -p fluentbase-consensus --lib beacon::`, `… testbed::cert_inlet_tests`, свои стенд-тесты по имени, `… epoch_manager`, `cargo test -p fluentbase-node --lib`, `cargo clippy -p fluentbase-consensus -p fluentbase-node --all-targets`, с фичей `--features dpos-devnet-byzantine` для стенда, `cargo fmt --check`. **Полный набор ворот гоняет оркестратор** — не гоняй `--lib` целиком без нужды; стенд не гонять параллельно с другим cargo.
- Каждое утверждение в журнале — с `file:line`, который открыл. [KNOWN] только по прочитанному/прогнанному в этой сессии.
- **СТОП** (запись, не обход): geometry freeze без хозяина; стенд-тест догоняющего валидатора красный по живости; рестарт-тест красный; `DECISIONS.md`; нужен файл вне списка.

## Мутации (с откатом и md5)

(M1) актор игнорирует `changed()` (клок стоит) — тест догоняющего валидатора красный; (M2) в `FluentApp` передан другой watch (два канала) — тот же или рестарт-тест красный; (M3) в `report(Update::Tip)` `send_replace` заменён на публикацию, которая пропускает первую высоту после старта, — рестарт-тест или тест клока актора красный (если ни один не краснит — напиши, не сочиняй тест ради мутации).

## Журнал `history/E5-4-A.md` — §0

1. Результаты названных cargo-команд verbatim. 2. Geometry freeze: кто выполняет без поллера (file:line) — или СТОП. 3. Seed после рестарта: цепочка marshal → `Update::Tip` → watch (file:line в checkout). 4. Таблица tee-ассерций (п. 4). 5. Обязательный стенд-тест: что именно утверждает, чем был бы красным с tee-логикой снятой без замены. 6. Мутации verbatim. 7. E5-40: куда переехали имена, кто читает. 8. Вне рамки. 9. Слабее всего — ранжируй.

Заверши ответ кратко (≤ 25 строк): cargo, что снято/куда переехало, стенд-тест, мутации, слабее всего.

# Строка 5.1, Ф7 — дрейф `.claude/dpos_architecture/` после удаления владельцев `PK_E`

Ты — док-исполнитель (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `129f2754` (`refactor(beacon)!: make the agreed artifact the only owner of the epoch key` — строка 5.1, код уже закоммичен). Пути без префикса — от `crates/dpos/consensus/src/`.

## Правила (обязательные)

1. **Агентов не запускать.** Причина: секция и код обязаны сидеть в одном контексте — иначе доки разъезжаются повторно.
2. **Git только на чтение.** Не коммитить, не `add`. `.claude/` вообще не трекается git'ом, поэтому коммита по нему и не будет.
3. **Файлы на запись — ТОЛЬКО** `.claude/dpos_architecture/*.md`. **Ни одной строки кода.** Ни `.dpos-study/`, ни `crates/`.
4. **Каждое утверждение, которое ты пишешь в доку, — с `file:line` живого кода**, открытого тобой на `129f2754`. Дока без якоря в этом репозитории превращается в следующий дрейф.
5. Читать целыми строками (`Read` / `sed -n 'N,Mp'`); никогда `cut -c`/`head -c`.
6. Поиск по коду — **ТОЛЬКО `git grep`** (под `crates/dpos/consensus/src/.claude/session-reads/` лежат gitignored журналы чужих сессий). Поиск по докам — `grep -rn` по `.claude/dpos_architecture/`: **этот каталог в `.gitignore`, поэтому `git grep` по нему МОЛЧИТ, и пустой вывод за отсутствие принимать нельзя.**
7. Текстовые правила файлов: строки не заканчиваются многоточием; число обратных кавычек в строке чётное (разорванный переносом код-спан — это ПАРА строк, по одной нечётной в каждой, так можно); нет `,,`; нет пустых пар обратных кавычек; пустые круглые скобки — только внутри код-спанов. Проверь в конце своими командами и приведи счётчики.

## Что случилось в коде (и почему доки поехали)

Строка 5.1 реализовала П-3: артефакт стал единственным владельцем `PK_E`. **Удалены четыре файла** — `beacon/keys.rs`, `beacon/key_journal.rs`, `beacon/carry.rs`, `beacon/resolve.rs` (2301 строка) — и вместе с ними: `BeaconKeys` со тремя тирами провенанса (`KeySource::{LocalDkg, Carried, Agreed}`), лестница `BeaconKeys::get_pk` и `KeySources`/`store_floor`, `AgreedKeys`, `InvalidSeed`, `DkgQualFor`/`frozen_dkg_qual` и страж `!(bit || committed)`, `select_carry_scheme`, `chain_key_epoch`/`chain_key_epoch_memoised`, `mint_diverges_from_attested`, `beacon_share_resolver`, `BeaconResolve`, `CommitteePairFor`, `for_keys`, писатели W1 и W3, метрика `seed_material_refused_divergent`.

**Что стоит на их месте** (открой сам, якоря — мои, проверены на `129f2754`): всё в `beacon/artifact.rs` — `ArtifactStore` (`:418`, insert-only, не вытесняется) как единственный долговечный владелец ЗНАЧЕНИЯ; `MintIndex` (`:596`, `minted_at` `:635`) — какая эпоха минтила ключ для целевой; `KeyIndex` (`:725`, `key_at`/`sharing_at`/`holds_mint_of`) — проекция значения; `ChangedAt` — бит `changed` без ноги `committed`; `open_mint_memo` (`:1126`) — ДОЛГОВЕЧНОЕ мемо `epoch → minted_at`, write-once, недекодированный ответ не записывается. Полином читается из `artifact.group_key`, поэтому `CeremonyStore` больше не несёт `Output` (`actor.rs:173`), а share-файл — артефакта (`share_state.rs`). Гейт `validate_share_on_poly` стал свойством `adopt_share` (`actor.rs:1055`). Решение «дилю ли я» читает бит с якоря модуля (`maybe_start`, `actor.rs`), ростерной пары нет. Ретенция одна: `JOURNAL_RETENTION_EPOCHS` — алиас `SCHEME_RETENTION_EPOCHS = 8` (`beacon/mod.rs:126`, `lib.rs:26`), окно recompute-heal расширилось с 1 до 8 эпох (≈ +3 МБ диска на узел). Член с долей и без артефакта просит артефакт каждый тик (`actor.rs:2348-2355`). Нулевое пересечение комитетов на границе теперь переживаемо — каждая половина добирает чужой ключ.

## Инвентарь дрейфа (мой `grep -rn` на 2026-09-13; пересчитай сам, числа могли сдвинуться)

Строк с попаданиями удалённых имён, по файлам: `03_epoch_machinery.md` **41**, `00_preamble.md` **35**, `08_node_integration_crates_node_bins_fluent.md` **27**, `09_followers.md` **13**, `13_invariants_gotchas_rules.md` **13**, `00a_errata_2026_08_10_full_audit.md` 4, `02_ordering_core_orderblock_deferred_execut.md` 3, `01_system_map.md` 1, `05_identity_crypto.md` 1, `06_staking_layer.md` 1, `12_consensus_critical_constants.md` 1 — всего **140** в 11 файлах.
По именам (вхождений): `BeaconKeys` 46, `keys.rs` 22, `carry.rs` 15, `JOURNAL_RETENTION_EPOCHS` 12, `chain_key_epoch` 10, `resolve.rs` 10, `KeySources` 8, `beacon_share_resolver` 8, `BeaconResolve` 8, `for_keys` 8, `AgreedKeys` 6, `key_journal` 5, `mint_diverges_from_attested` 4, `frozen_dkg_qual` 3, `DkgQualFor` 2, `select_carry_scheme` 1.

## Правило разбора — применяй его к каждому попаданию

Три класса, и класс определяет правку:

1. **Секция описывает ТЕКУЩЕЕ поведение удалённым именем** — переписать по факту, с якорем живого кода. Если удалена не строка, а МЕХАНИЗМ (лестница `get_pk`, тиры провенанса, carry-резолвер), то и переписывать надо ПОДРАЗДЕЛ целиком над новым владельцем, а не подменять имена: подмена имён в тексте, описывающем трёхтировую лестницу, которой больше нет, — это новый дрейф, а не его устранение. Самый тяжёлый случай — `03_epoch_machinery.md` §3.3.
2. **Историческая проза «раньше здесь было X»** — оставить, но убедиться, что она ЧИТАЕТСЯ как история (есть маркер даты/прохода или явное «до»/«was»). Если не читается — добавить маркер, текст не переписывать.
3. **`00_preamble.md`** — это журнал `verified-against`, и по правилу самого файла его СТАРЫЕ записи остаются историей: их не трогать вовсе. Твоя работа там — ОДНА новая запись сверху, по форме соседних (дата `2026-09-13`, жирный заголовок, тело с якорями, гейты, «Sections touched», ссылка на журнал `.dpos-study/history/E5-1-A.md`). Гейты на `129f2754` (мои прогоны, тег `b5`): consensus lib **656/0**, стенд с фичей **55/0** и без фичи **46/0**, node 55/0, staking-reader 64/0, `slasher_integration` 16/0, clippy ровно два предупреждения (оба чужие и оба вне `fluentbase-consensus`), clippy с фичей 0, `cargo fmt --check` чисто, `cargo doc` unresolved link 6.

Отдельно: `13_invariants_gotchas_rules.md` держит инварианты и правила — там проверь, не стало ли ПРАВИЛО неверным, а не только его якорь. Например правило про ретенцию `BeaconKeys::retain_from` тир-за-тиром и правило про `JOURNAL_RETENTION_EPOCHS = 1`. И `12_consensus_critical_constants.md` — окно heal изменилось с 1 на 8, это константа, за которой следят.

## Что НЕ делать

Не описывать то, чего в коде нет. Не «улучшать» секции, которых дрейф не касается. Не переносить в доки содержимое журнала `.dpos-study/` — доки описывают КОД. Не вводить новых обозначений. Если какое-то попадание правильно оставить как есть — скажи это в отчёте с причиной, а не молча.

## Отчёт `.dpos-study/history/E5-1-DOCS.md` (новый файл; это единственное исключение из п. 3 списка файлов)

§0 Прямые ответы: (1) пересчитанный инвентарь — сколько попаданий было и сколько осталось, по файлам, своей командой verbatim; (2) какие ПОДРАЗДЕЛЫ переписаны целиком и почему именно они (класс 1 «удалён механизм»); (3) какие попадания оставлены как история и по какому признаку они читаются историей; (4) новая запись `verified-against` — привести её целиком; (5) что в доках оказалось неверным СВЕРХ списка удалённых имён (то есть дрейф, который ты нашёл по ходу, а не по grep'у) — с `file:line` доки и кода; (6) какие ПРАВИЛА в `13_` и константы в `12_` изменились по существу; (7) текстовые счётчики (многоточия, чётность кавычек, `,,`, пустые кавычки, пустые скобки) своими командами; (8) что осталось недоделанным и почему.
§1 Таблица: файл / что было / что стало / якорь живого кода.

В финальном ответе — только §0.

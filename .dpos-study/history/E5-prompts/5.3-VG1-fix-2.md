# Второй круг захода В+Г1 строки 5.3 — ПЕРЕСМОТР решения 2 карты после dsh-ревью круга 1

Ты — исполнитель (Opus 5) в СВЕЖЕМ контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `a45c458d`; рабочее дерево несёт незакоммиченный заход В+Г1 (6 файлов: `beacon/{actor,dkg_transport,plane}.rs`, `testbed/{stand,tests}.rs`, `crates/node/src/dpos.rs`). Пути без префикса — от `crates/dpos/consensus/src/beacon/`. `.dpos-study/history/E4-ORCHESTRATOR.md` не читать, не трогать.

Вход, в этом порядке: (1) `git diff HEAD` — объект; (2) постановка `.dpos-study/history/E5-prompts/5.3-VG1-impl-1.md`; (3) журнал `.dpos-study/history/E5-3-VG1.md`; (4) ревью `.dpos-study/history/E5-3-VG1-DSH-1.md` (D-01…D-21; три SERIOUS). Всё в (3)–(4) — реляция; перед правкой проверь по коду; REJECTED допустим с file:line.

## Что оркестратор проверил сам и почему решение 2 пересматривается

- D-01 [KNOWN]: `confirmations.rs:186-188` — `mint` пропускает, если `previous >= confirmed.len()`: полноширинный `ShareConfirm`, отправленный один раз и отвергнутый отстающим соседом как `not_member` (его окно не несёт `now+2`), никогда не переиздаётся. На HEAD такой confirm принимался (`committee_for(now+2)`).
- D-05 [KNOWN]: часы актора — max по трём фидерам, включая НЕфинализированный ordering-tip маршала (`actor.rs:1322-1327` комментарий и код); окно ET ключуется эпохой перехода по `fin`. Значит `now` актора может опережать `E_w` окна, и кадры текущей церемонии `now+1` могут резаться как `not_member`.
- D-02/D-03 [KNOWN по отчёту dsh, ordering проверен мной по `node/dpos.rs:1560-1600`]: в проде геометрия публикуется раньше `track_peers` в том же полле — актор может жить с пустым окном; стенд заполняет окно в `cold_start` ДО актора, поэтому `window_unset == 0` в стенде структурен.

**Вывод: связывать ПО-ЭПОШНОЕ членство с окном ET — ошибка карты (моя, не исполнителя).** Окно отвечает на вопрос «КТО этот отправитель на последнем зарегистрированном наборе» и не обязано знать эпоху кадра; эпоху к отправителю привязывает ПОТРЕБИТЕЛЬ кадра: для дилингов/ack — commonware `Player`/`Dealer` по `Info` эпохи (чужой дилер отвергается там), для `ShareConfirm` — ростер `committee_for(target_epoch)` в `on_confirm`. Это и есть «один источник» R-127 в правильной форме: классификация — ТОЛЬКО пред-декодный `GatedReceiver` по окну (в проде уже так; в стенде теперь тоже), а в акторе по-эпошной проверки членства НЕТ.

## Что сделать (решение 2 — ПЕРЕСМОТРЕННОЕ; Д-nn в журнале с этой причиной)

1. **Убрать `beacon_member` из актора** и поле `DkgActor.ingress_window`; `ValidatorInputs.ingress_window` убрать, если больше ничего в биконе окно не читает (стендовый `GatedReceiver` строится в `stand.rs`, ему поле актора не нужно; узел уже строит гейт сам). `on_message`: пик эпохи → `epoch_is_actionable` (одно окно, решение 1 остаётся) → декод → потребитель. Метки `window_unset`/`not_member` из актора уходят.
2. **Отказ потребителя — видимый.** Где кадр отвергается по «нет такого сиденья в эпохе»: дилинг/ack от не-дилера эпохи (commonware `dealer_message`/`receive_player_ack` Err — установи по чекауту, какая ошибка) и confirm от не-члена ростера (`on_confirm`: `roster.position(from)` — сейчас проверяется? установи) — через `refuse(from, epoch, "no_seat")`. Одно место отказа (решение 3) сохраняется: `refuse` остаётся единственным сайтом метрики.
3. **Стенд**: `GatedReceiver` + окно остаются (это главный выигрыш захода — пред-декодный гейт впервые исполняется в стенде); D-20 — окно стенда без тумбстоун-предиката, а продакшн с ним: подключить `with_tombstones` от стендового `TombstoneSet` (он есть, `stand.rs:2912-2919` по dsh), чтобы форма совпадала. Новый стенд-тест `a_dealing_for_now_plus_two_…` переписать под новую семантику: кадр для `now+2` от узла впереди теперь принимается в `pending` как на HEAD — тест становится тестом ГЕЙТА: сосед из registry-tier (не член ни одной из трёх записей) шлёт дилинг → отказ на гейте (`dpos_ingress_dropped_total{channel="beacon", reason="untracked"|"secondary"}`), ключ минтится; атрибуция — по логу `refuse`/гейта с узлом, не по общепроцессной сумме (D-11/D-12).
4. **Минорные dsh (правь):** D-07 (`refuse` печатает `now` тоже), D-08 (док `within_ingress_window` про нулевой floor — привести к коду), D-09/D-10 (доки константы/`on_confirm` — переписать под пересмотренное решение), D-15 (тестовый хелпер окна — под новую форму или удалить, если окно из актора ушло), D-16 (имя теста), D-21 (устаревший комментарий `epoch_transition.rs:735-746` — только комментарий, файл вне списка, разрешаю), D-13 (юнит deque — если возможно, гнать через реальный `propose`/nullify; если >40 строк — записать).
5. **Записать (не править):** D-04/D-06 (снимаются пересмотром — сказать, чем), D-14 (третье перепредложение вытесняет первое — граница `deque_size`, кандидат в реестр), D-17, D-18 (арм `pinned_rx`/`artifacts_rx` — не fatal по дизайну: «resolver-exit ⇒ fatal» — единственный случай), D-19 (снимается).
6. **Доки** `.claude/dpos_architecture/` — переписать под пересмотренное правило (RULE 48(a)/50, §8.12, `verified-against`): «классификация отправителя — только пред-декодный гейт по окну; эпоху привязывает потребитель; актор членства не проверяет».

Правила: агентов не запускать; git только чтение; файлы — те же 6 + `ceremony.rs` при нужде п.2 + `epoch_transition.rs` (только комментарий) + доки + журнал `E5-3-VG1.md` (раздел «Второй круг»); никаких `#[allow]`/`unwrap` на продакшн-пути; каждое утверждение — file:line; `CARGO_BUILD_JOBS=6`.

Ворота — все одиннадцать verbatim (база круга 1: `--lib` 685/0, стенд 57/0 и 48/0 @637, node 57/0, reader 64/0, slasher 16/0, clippy 2 чужих / 0 своих, с фичей 0, fmt 0, doc 6, harness 2/2086/10); прирост/убыль тестов по именам. Мутации с md5: (M1) снять гейт в стенде — переписанный стенд-тест красный; (M2) `no_seat` не считать — юнит красный; (M3, повтор) арм резолвера → продолжение цикла — юнит красный. В журнале: таблица «кадр × смещение эпохи отправителя → HEAD / круг 1 / круг 2 → кто доставит повторно» (ответ на вопрос (1) dsh — ни одной клетки «nothing» для нужного кадра). Ответ — таблица D → решение → file:line, ворота, md5.

<tone_preference>Кратко и по существу.</tone_preference>

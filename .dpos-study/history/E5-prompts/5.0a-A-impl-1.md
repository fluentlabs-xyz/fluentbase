# Строка 5.0а, единственный заход — стендовый `CertInlet`: вторая продюсерская дверь в marshal узла стенда

Ты — исполнитель (Opus 5) одного захода строки плана. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `bec013a7`. Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`. **В `git status` чужое**: `.dpos-study/history/E4-ORCHESTRATOR.md` (файл другой сессии) и `.dpos-study/PLAN.md` (правка оркестратора) — не трогать.

## Правила (обязательные)

1. **Агентов не запускать.** Причина: спека и код обязаны сидеть в одном контексте; делегирование разрывает связь «утверждение ↔ якорь».
2. **Git только на чтение.** Не коммитить, не `add`, не `stash`, не `checkout`.
3. **Файлы на запись — ТОЛЬКО**: `testbed/stand.rs`, `testbed/fakes.rs`, `testbed/mod.rs` (если нужен `pub(super)`-реэкспорт), `.dpos-study/history/E5-0a-A.md`. **Запрещено**: `testbed/tests.rs` (его держит строка 5.1 — параллельная работа), `testbed/byzantine_roles.rs`, `testbed/preconditions.rs`, `testbed/committee_tests.rs`, `beacon/**`, `cert_inlet.rs`, `plane_upstream.rs`, `executor.rs`, `epoch_manager.rs`, `committee/**`, `node/**`, `p2p/**`, `bins/**`, `devnet/**`, `contracts/**`, `Cargo.toml`, `.claude/**`. Правка вне списка неизбежна ⇒ §5 с причиной и file:line, НЕ делать.
4. **file:line на каждое утверждение** в журнале.
5. Без `#[allow]`, `todo!`, таймеров вместо событий, поллинга вместо пробуждений; `unwrap` на продакшн-пути — только `Mutex::lock().unwrap()`. Любое из этого — только строкой Д-nn с причиной.
6. Комментарии, доки и `.claude/dpos_architecture/` — **не свидетельство**: в этом репозитории они дрейфуют. Свидетельство — код и прогон, который ты сделал сам. Для commonware — `.claude/COMMONWARE_INTERNALS.md` ДО чекаута.
7. Читать целыми строками: `Read` или `sed -n 'N,Mp'`; никогда `cut -c`/`head -c`.
8. `CARGO_BUILD_JOBS=6`; `Blocking waiting for file lock` — ждать. Стенд не гонять параллельно с другой сборкой.
9. **Правила прогонов** (решение пользователя 2026-09-12): полный набор ворот НЕ гонять — он у оркестратора. Твои прогоны: один `cargo test -p fluentbase-consensus --lib` в конце и ×3 по своим новым/переписанным стенд-тестам (`--test-threads=1 --nocapture`, вывод байт-в-байт). Правки батчем, ОДИН прогон затронутых тестов. «Красный до правки» — только для теста на найденный дефект поведения; мутации ≤ 2, по одному тесту (`--exact`).
10. Существующие тесты не ослаблять: база `bec013a7` — consensus lib **686**/0, стенд с фичей **48**/0. Покраснение = находка: либо свойство сменилось (переписать ассерт и записать «что доказывал / что доказывает»), либо дефект (§0). Не подгонять числа без механизма.

## Что прочитать ДО кода

1. `.dpos-study/PLAN.md` строка 5.0а (строка 111 файла, блок «### Э5.») — задание дословно.
2. `.dpos-study/history/E5-BEACON-DESIGN.md` §5.1 (`:556-595`) — граница `Beacon`; §5.4 (`:629-646`) — модель отказов; §7 строка 5.2 и 5.4 (`:711-733`) — зачем нужен этот стенд; §0.7(а) и §0.7(б) — две [ГИПОТЕЗА], которые эта строка делает проверяемыми.
3. `.dpos-study/history/E5-PRECONDITIONS.md` §0 п.5 (оценка и все входы), §2 (е) (R-128/R-129).
4. `.dpos-study/EXPERIMENTS.md` §3 п.1 (Ex-21) и §4 (ловушки стенда).

## Якоря, открытые оркестратором в этой сессии [KNOWN]

- `CertInlet` — `cert_inlet.rs:290` (поля), `new(marshal, committee, ctx)` `:404` (`M: MarshalSink`, `E: CryptoRngCore + Send`); билдеры `with_randomness` `:446`, `with_epoch_math` `:456`, `with_rotate` `:466`, `with_connection_token` `:479`, `with_window` `:488`, `with_tee` `:500`; `ingest` `:520` (инфаллибельна — каждый исход это skip); `record_data_fault` `:758`; `MAX_UPSTREAM_FAULTS = 3` `:91`; `RotateUpstream` `:106`; `LiveFrontierTee{dkg_height_tx, plane_clock}` `:274`; `MarshalSink` `:213`, `impl MarshalSink for MarshalMailbox` `:231`.
- Три исхода-фолта в `ingest`: height↔epoch bind `:545-568` (на валидаторском inlet-е `epoch_bind == None` ⇒ no-op), `payload != digest` `:573-586`, BLS verify FAILED `:648-673`. «Комитет нечитаем» ⇒ **НЕ** фолт `:619-640`.
- `ensure_key(epoch, PinEffort::Local)` на каждый серт — `:596`. Тик tee — `:713-717`, ПОСЛЕ `observe_certificate` `:690` и `observe_cert` `:706`, только на чистом ingest.
- Вердикт `observe_certificate` сейчас НЕ читается (`let _observed`, `:690`, комментарий `:691-692` называет это работой 5.2) — **5.0а потребителя вердикта не добавляет**.
- Продакшн-сборка — `node/cert_inlet.rs:57-113`: `.with_tee(tee).with_rotate(rotate).with_randomness(beacon).with_connection_token(conn_gen)`; петля `finalized_rx.recv()`, `None ⇒ error! + break` (фатально).
- `UpstreamFinalized` — `cert_follow.rs:44`. Трейт `CertUpstream`: `get_finalization(Height) -> Option<UpstreamFinalized>`, `get_latest()`, `rotate()` (реализация обёртки — `fakes.rs:1625-1646`).
- **`deliver` плоскости σ НЕ проверяет** — `plane_upstream.rs:455-461` строит схему через `verifier_for` `:371-390`, а тот зовёт `build_verifier(..., None)` `:382-387` — **без оракула эпохи**. Поэтому серт с подменённым σ проходит `deliver` (multisig цел) и доходит до вызывающего; первая проверка σ — `ingest` через `self.committee.scheme(epoch)` `cert_inlet.rs:620` (схема с оракулом, `committee/mod.rs:326`). Это и есть механизм стендовой половины Ex-21; на нём строится тест.
- Стенд: `frontier_plane` `stand.rs:1751-1790` (`new_bridge` `:1766`, `CountingHandler`, под фичей `ForgedSeedProducer` `byzantine_roles.rs:574`, режим `ForgeMode::SeedSlot` для `Role::ForgedSeedUpstream` `stand.rs:2026`); `CountingUpstream::new` `fakes.rs:1608`, сборка `stand.rs:2012-2041`; `marshal_slot: Arc<OnceLock<MarshalMailbox>>` `stand.rs:2011`, заполняется `stand.rs:2418`; `upstream` ПЕРЕМЕЩАЕТСЯ в `outer.start(..., Some(upstream))` `stand.rs:2584` (нужен клон — проба уже клонирует `:2078`); `(dkg_height_tx, dkg_height_rx)` `stand.rs:2233`, `dkg_height_tx` уходит в `OuterBuilder.dkg_height_tx` `stand.rs:2402` (нужен клон); `plane_clock` там же; `randomness: Arc<dyn Beacon>` `stand.rs:2333-2345`.
- `StandConfig` `stand.rs:90-205`; `Beacon{Static, Live}` `:209-222`; `PeerSet` `:243-260`; `Role` `:318-380`; `Outcome` `:552-660`; `NodeHandles` `:1121-1144`; `drive` `:1146`; сбор `Outcome` `:1489-1640`; `FakeChain` `fakes.rs:326-359`.
- `PlaneClock::record_dkg_clock` вызывается в одной точке — `beacon/actor.rs:1171` (`on_height`, `height_now().max(height)` `:1165`); счётчик дропов `dpos_dkg_height_drops_total` `sync_metrics.rs:337`, `note_height_drop` `:366`, тест-геттер `:392`.
- `git grep CertInlet -- crates/dpos/consensus/src/testbed` на HEAD — **пусто**.

## Что сделать

### 1. Переключатель в `StandConfig` — РОВНО одно новое поле

Добавить `pub cert_inlet: Option<CertInletCfg>` (`None` по умолчанию во всех конструкторах — ни один существующий тест не меняет поведение). **Никаких изменений существующих полей `StandConfig`**: строка 5.1 идёт параллельно и на этот тип опирается; любое переименование/изменение существующего поля — конфликт. Это условие жёсткое.

`CertInletCfg` — минимальная форма, которая покрывает три потребителя ниже. Предложение оркестратора (обосновать или заменить с file:line): `{ nodes: Vec<usize> }` — индексы узлов, которые поднимают inlet. Источник сертификатов узлу НЕ конфигурируется: им служит собственная плоскость фронтира узла (`CountingUpstream`), которая уже тянет из архивов ДРУГИХ узлов через `FRONTIER_CHANNEL`; адресность, если тест её требует, задаётся существующими `upstream_only_link` / `upstream_source_only_for`.

### 2. Задача inlet-а в `build_node`

Опциональная задача (спавнить только когда `cfg.cert_inlet` называет этот индекс), по форме `node/cert_inlet.rs:74-113`, с производственными билдерами:

- `CertInlet::new(marshal, committee.clone(), ctx)` — `marshal` берётся из `marshal_slot` ПОСЛЕ `outer.build` (слот заполняется `stand.rs:2418`); задача должна дождаться слота, а не читать его до заполнения.
- `.with_tee(LiveFrontierTee { dkg_height_tx: <см. ниже>, plane_clock: plane_clock.clone() })`
- `.with_randomness(randomness.clone())` — ТОТ ЖЕ `Arc<dyn Beacon>`, что у узла (а не свежий: иначе `observe_cert` пруни́т стор, которого никто не читает, и ключ из DKG плоскости до этого inlet-а не дойдёт — та же причина, что в `node/cert_inlet.rs:44-49`).
- `.with_rotate(rotate)` — `RotateUpstream`, который инкрементирует счётчик стенда и зовёт `upstream_clone.rotate()`.
- `with_epoch_math` НЕ ставить (валидаторский inlet его не ставит — `node/cert_inlet.rs:86-89`), `with_window`/`with_connection_token` НЕ ставить.

Фидер: цикл, который берёт следующую высоту над собственным тиром-F (`FakeChain` — без сообщений в marshal) и зовёт `upstream_clone.get_finalization(Height::new(h))`; `Some(uf) ⇒ inlet.ingest(uf).await`, `None ⇒ уступить такт` (событием/`ctx.sleep` детерминированного рантайма — назови, что выбрал и почему это не поллинг ради поллинга). Никакого WS-слоя (`cert_follow::upstream::init`, `node/cert_inlet.rs:57-64`) — он не нужен.

**Тик tee — решение оркестратора, не изобретать**: стенд создаёт СВОЙ канал `(tee_tx, tee_rx)`, отдаёт `tee_tx` в `LiveFrontierTee`, а **сама задача inlet-а** сразу после возврата `ingest` дренирует `tee_rx` через `try_recv`, записывает высоты в наблюдаемое и пересылает их в настоящий `dkg_height_tx` (клон `stand.rs:2233`). Отдельной задачи-реле НЕ создавать: лишняя задача — лишний такт планировщика детерминированного рантайма, а строка 5.4 будет сравнивать «с tee» против «без tee» и не должна мерить реле. Если по коду этот приём невозможен — §0 с file:line, не самовольная замена.

### 3. Наблюдаемые в `Outcome`

По строке плана — `record_data_fault`, `consecutive_faults`, тик tee. Каждое должно быть пиновано ассертом, а не просто существовать:

- сколько сертификатов задача отдала в `ingest` (попытки);
- сколько раз сработал `rotate()` (единственный внешне видимый эффект `record_data_fault` при `consecutive_faults >= MAX_UPSTREAM_FAULTS`, `cert_inlet.rs:758-772`);
- сам `consecutive_faults` **приватен** (`cert_inlet.rs:343`) и `cert_inlet.rs` править запрещено. Наблюдай его косвенно: число `rotate()` при известном числе поданных плохих сертов даёт `consecutive_faults` арифметически (порог 3). Если считаешь, что без геттера строка не закрывается, — §0 с обоснованием, поля не добавлять.
- высоты, которые прошли через tee (п. 2), и `PlaneClock` dkg-часы после прогона.

### 4. Роль «валидатор с отстающим EL и живым inlet-ом»

**Нового варианта `Role` не вводить, если существующая комбинация конфигурации даёт то же**: узел вне `committee[E]` при `PeerSet::Committee { upstream_link: true }` (`stand.rs:252-256`) теряет консенсус-плоскость, сохраняет плоскость фронтира и следует за цепью только через upstream — это и есть валидатор с отстающим EL и живым inlet-ом. Проверь по коду, что так, и запиши вывод; если комбинации не хватает (например, отставание нужно УДЕРЖИВАТЬ, а не получить однократно), назови ровно то, чего не хватает, и добавь минимум в `StandConfig`/`fakes.rs` — не в `Role`.

### 5. Тесты захода

Тесты живут **не** в `testbed/tests.rs` (он занят 5.1). Помести их в модуль `#[cfg(test)] mod tests` внутри `testbed/stand.rs` либо в новый файл `testbed/cert_inlet_tests.rs`, подключённый из `testbed/mod.rs` — выбери и обоснуй одной строкой.

(а) **Стендовая половина Ex-21** — «до» для 5.2. Узел с inlet-ом тянет сертификаты у пира с `Role::ForgedSeedUpstream` (`ForgeMode::SeedSlot`), `Beacon::Live`. Утверждения: (1) пока `PK_E` у узла нет, подменённый серт ДОПУЩЕН (`ensure_key` вернул `false`, multisig цел, `deliver` σ не смотрел — `plane_upstream.rs:455-461`) — узел продвинулся; (2) когда ключ эпохи пришёл, тот же класс серта проваливает BLS в `ingest` (`cert_inlet.rs:648`), и после `MAX_UPSTREAM_FAULTS` подряд срабатывает `rotate()` — счётчик ротаций ≥ 1. Тест обязан **самопроверить, что подмена состоялась**: роль уже это делает (`byzantine_roles.rs:684-700` ассертит на байтах), сошлись на её `ByzFacts` (`certs_forged`, `forged_seed_differs`, `forged_vote_half_intact`) — фальсификатор теста должен падать, если подмена не случилась ни разу.
(б) **Фикстура R-129** — узел получает финализацию эпохи, схем которой у него нет. Сам тест R-129 ставит 5.2; от 5.0а нужна ФИКСТУРА и утверждение, что путь достигается: `Committee::scheme(epoch)` отвечает `None` ⇒ `ingest` делает НЕ-фолтовый defer (`cert_inlet.rs:619-640`, счётчик `dpos_cert_inlet_committee_read_deferred_total`) — пин на счётчик defer > 0 и `rotate()` == 0 (лаг узла не должен стоить ротации, `:635-639`).
(в) **Отрицательный контроль**: при `cert_inlet: None` ни один узел inlet-а не поднимает — ни один существующий тест не меняет числа. Это доказывается зелёным полным набором (у оркестратора), в журнале только назвать.

Детерминизм ×3 по каждому новому тесту.

### 6. Что в 5.0а НЕ делается (записать в §5, не делать)

Чтение вердикта `observe_certificate` и потребитель `faults()` — строка 5.2. Снятие tee и переход на `ordering_tip` watch — 5.4. Правки `cert_inlet.rs`/`beacon/**` любого вида. Тест «догоняющий валидатор дилит на живом фронтире без tee» — 5.4 (5.0а только даёт ему роль). Сам тест R-129 — 5.2.

## Журнал `.dpos-study/history/E5-0a-A.md` (короткая форма, без «инвентаризации до»)

§0 Прямые ответы, нумерованные, каждый с [KNOWN]/[ГИПОТЕЗА] и file:line: (1) форма `CertInletCfg` и почему именно она; (2) откуда задача берёт marshal и как дождалась слота; (3) фидер: какая высота, какой источник, чем уступает такт и почему это не поллинг; (4) тик tee: как реализован дренаж `try_recv`, где именно в цикле, и чем доказано, что высота доходит до `DkgActor` (`actor.rs:1171`); (5) наблюдаемые: имена полей `Outcome`, что каждое пинует, и как `consecutive_faults` наблюдается без геттера; (6) роль отстающего EL: комбинация конфигурации или чего не хватило; (7) тесты: имя / что утверждает / его фальсификатор / «красный до правки» verbatim или мутация; (8) `cargo test -p fluentbase-consensus --lib` verbatim + ×3 по новым тестам; остальные ворота — у оркестратора; (9) что в проекте §5 / строке плана оказалось неверным по коду; (10) запрещённого сделано — ноль или список; (11) слабое место: что ты НЕ проверил.
§1 Отклонения Д-nn (нумерация Э5 начинается с Д-1 этой строки — 5.0а-Д-1, 5.0а-Д-2, …): что иначе / причина file:line / меняет ли П или Д.
§2 Тесты (таблица). §3 Логические коммиты: заголовки Conventional Commits и какие группы НЕ собираются по отдельности. §4 Где проверка слабее всего. §5 Что осталось для 5.1/5.2/5.4.

Ты НЕ коммитишь. Заканчивай, когда всё сделано и `--lib` зелёный; невозможное — сделать остальное и назвать в §5. В финальном ответе — только §0.

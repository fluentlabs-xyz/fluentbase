# Финальное ревью захода А строки 5.4 — один клок бикона на `watch` marshal-tip, три фидера и tee сняты (Opus, свежий контекст)

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база `HEAD` = `08764870`. Объект — незакоммиченное рабочее дерево: 14 файлов под `crates/` (`git diff HEAD --stat -- crates`). Пути без префикса — от `crates/dpos/consensus/src/`. Чужое `.dpos-study/history/E4-ORCHESTRATOR.md` — не читать. Корневой `CLAUDE.md` изменён не этим заходом — не ревьюить.

Это ФИНАЛЬНЫЙ круг: три круга исполнителей (свежие агенты; журнал `history/E5-4-A.md` — §0 круга 1, «## Круг 2», «## Круг 3»), одно dsh-ревью (`history/E5-4-A-DSH-1.md`, D-nn — если файла нет, dsh не завершилось: ревьюй без него и скажи об этом). Постановки — `history/E5-prompts/5.4-A-{impl-1,fix-2,fix-3}.md`. Карта — `history/E5-ORCHESTRATOR.md`, «Карта захода А строки 5.4»; дизайн — `history/E5-BEACON-DESIGN.md` §5.3, находки E5-06, E5-40.

## Установлено оркестратором (опирайся; опровергать — с file:line)
- Заход: `ValidatorInputs.clock: watch::Receiver<u64>` (было `heights: mpsc`); актор `clock.changed()` + `borrow_and_update()`, clamp `max` в `on_height` остался; сняты `LiveFrontierTee`, `dkg_height_tx` (31 строка), клок-часть поллера узла (`fin+K`, seed `cs_fin_num`), `PlaneClock::note_height_drop` + `dpos_dkg_height_drops_total`; четыре имени партиций — в `beacon/mod.rs` (E5-40).
- Marshal шлёт `Update::Tip` на старте из сохранённой финализации (pinned checkout `marshal/core/actor.rs:396-402`) — seed после рестарта не нужен.
- Принятое отклонение **Д-5.4А-1**: поллер узла остаётся задачей (geometry freeze — `geometry_tx.send_replace` `crates/node/src/dpos.rs:~1569` единственный писатель, актор паркуется на первом `Some` `beacon/plane.rs:~860-863`; первая `track_peers`; tombstone-watch), клок-часть снята. Не переоткрывать как BLOCKER; сказать, если остаток поллера ещё кормит клок бикона хоть одним путём.
- Круг 2 объяснил красный `committee_tests::five_nodes…` сдвигом фикстуры и подвинул `TOMBSTONE_FROM` 64→88; **я это отверг** — стенд стал недетерминированным от процесса к процессу (база 5/5 md5 одинаковы, дерево захода — 5 разных). Круг 3 нашёл: `tokio::sync::watch` выбирает шард `big_notify` через `thread_rng_n(8)` (`tokio-1.52.3/src/sync/watch.rs:422`, открыл сам), два запаркованных приёмника на `ordering_tip` (epoch_manager + актор) просыпались в процессно-случайном порядке. Правка: второй канал `FluentApp.beacon_tip`, оба `send_replace` подряд в арме `Update::Tip` (`application.rs:~1105-1111`). Мой замер: 5 прогонов `five_nodes…` md5 = база; `TOMBSTONE_FROM` снова 64, `committee_tests.rs` из диффа ушёл.
- Записанный СТОП круга 3 (§3.7): остаточный ~2–3% разброс ОДНОГО печатного поля `in_window` в новом тесте — класс `tokio::select!` (замер с прибитым RNG), сайт не локализован, ни одна ассерция не зависит. Не переоткрывать как BLOCKER; сказать, если видишь ассерцию, которая от него зависит, или сайт, который можно назвать по коду.
- Ворота — МОИ, прогнаны на этом дереве (тег `x2`): логи `/tmp/claude-1000/-home-djadjka-Work-fluentbase/f6f42c91-6188-4d9c-ae64-a1e9faed9028/scratchpad/gates/x2-*.txt`, статус `x2-status.txt` (`DONE` в конце), md5-снимок `x2.md5`. **Cargo не гоняй** (дерево общее, ворота мои); `md5sum -c x2.md5` — можно. Единственное исключение — мутации (п. 6): точечный `cargo test -p fluentbase-consensus --lib <тест>` (стенд — с `--features dpos-devnet-byzantine`) с откатом и md5 до/после, `CARGO_BUILD_JOBS=12`, не более трёх, не параллельно.

## Правила
1. Агентов не запускать — ни для работы, ни для проверки себя: спека, ревью и код должны сидеть в одном контексте. 2. Git только на чтение; единственный файл на запись — `.dpos-study/history/E5-4-A-REVIEW.md`; `E4-ORCHESTRATOR.md` не читать. 3. file:line на всё; доки, журналы и dsh-отчёты — не свидетельство. 4. Полнота, не фильтр: всё, от BLOCKER до NIT, с уверенностью. 5. Не расширяй в редизайн; существенное вне рамки — одной строкой в конце.

## Вход
Дифф `git diff HEAD -- crates` первым, потом журнал, потом dsh-отчёт.

## §0 — прямые ответы ([KNOWN]/[ГИПОТЕЗА], file:line)
1. Ворота: прочитай `x2-*` и выпиши числа verbatim (ожидаю `--lib` 730/0, стенд с фичей 57/0, node 57/0, reader 64/0, slasher 16/0, clippy два чужих / ноль своих, fmt 0, doc unresolved 6); дельту тестов против базы (`--lib` 732 → 730, стенд 58 → 57) объясни ПО ИМЕНАМ (`git diff` по `fn ` в тестовых модулях) — сходится ли счёт.
2. **Живость клока**: каждый путь постройки валидатора (узел `crates/node/src/dpos.rs`, стенд `testbed/stand.rs` `Beacon::Live`, рестарт, промоушен follower→validator, verifier-режим) — `beacon_tip` создан, передан в `FluentApp` через `with_beacon_tip`, и его receiver — тот, что у актора? Есть ли путь, где актор получает receiver, в чей sender никто не пишет (клок стоит на 0)? `git grep -n 'beacon_tip\|with_beacon_tip' -- crates`.
3. **Коалесцирование**: `watch` теряет промежуточные высоты. Перечисли всё, что `on_height` приводит в движение (ребро эпохи, дедлайны, sweep-cutoff лаунчера, гейдж), и для каждого — может ли пропуск высот в одном прыжке пропустить действие, которое `mpsc`-фид делал. Особенно: два ребра эпох внутри одного прыжка (узел, догоняющий через jump).
4. **Tee снят**: tee слал высоту фронтира ДО `marshal.verify_block`; теперь клок движется после `report(Update::Tip)`. Где считается дедлайн первой сделки догоняющего валидатора и до какой высоты доходит клок к нему; обязательный стенд-тест `a_catching_up_validator…` — действительно ли его узел догоняет ЧЕРЕЗ инлет/plane-upstream (строки setup), и чем он был бы красным без замены tee (§0.5 журнала — проверь по ассерциям).
5. **Два канала**: другие писатели `ordering_tip` (`git grep -n ordering_tip -- crates`), не пишущие `beacon_tip` (расхождение значений); `beacon_tip == None` на валидаторном пути; второй `subscribe()` на любом из двух каналов с паркующимся `changed()`/`wait_for` (`git grep -n 'changed()\|wait_for' -- crates/dpos/consensus/src crates/node/src`) — инвариант «один паркующийся приёмник на канал» держится?
6. **Тесты и мутации**: таблица §0.4 журнала против фактического `cert_inlet_tests.rs` — каждое снятое утверждение либо переизложено, либо действительно свойство tee (назови, какое свойство при этом потеряно, если потеряно); тавтологии; чтения `PlaneClock`/гейджа — тот же клок, что потребляет актор. Мутации (до трёх, с откатом и md5): M1 (актор игнорирует `changed()`), M2 (актору отдан receiver третьего, неписаного канала), M3 (`beacon_tip.send_replace` снят) — воспроизведи verbatim; если какая-то зелёная — скажи, какое свойство не покрыто.
7. **Остаток поллера** (`crates/node/src/dpos.rs`): что осталось, пишет ли что-то в клок/`PlaneClock`/актор; `git grep -n 'dkg_height\|note_height_drop\|LiveFrontierTee\|fin + K' -- crates devnet .claude` — только исторические записи CHANGELOG (`devnet/.../verdicts_fault.py` — я знаю о stale-атрибуции `dkg_height` поллеру; подтверди или опровергни).
8. **E5-40**: имена партиций в `beacon/mod.rs` — видимость, читатели, нужен ли реэкспорт из `dpos.rs`.
9. **Гигиена**: `#[allow]`, новые `unwrap`/`expect` вне тестов, новые `pub` наружу `beacon/`, мёртвые импорты, уровни логов, комментарии-нарратив/ссылки на ревью, док `ValidatorInputs.clock`.
10. **Доки** `.claude/dpos_architecture/{00,03,06,08,13,15}.md` + `DPOS_ARCHITECTURE_CHANGELOG.md` — по коду: якоря `fn`/строки существуют (`grep -n`), `verified-against` в `00_preamble.md` обновлён; `grep -rn "dkg_height_tx\|LiveFrontierTee\|three feeders\|три фидера\|note_height_drop\|with_ordering_tip" .claude/dpos_architecture` — только исторические записи CHANGELOG.
11. Hard-stop: требует ли что-то правки `DECISIONS.md`; BLOCKER, на который дизайн §5.3 не отвечает.
12. **Вердикт: КОММИТИТЬ / НЕ КОММИТИТЬ**; что не прочитал.
13. Слабее всего — ранжируй.

**§1 Находки** `id | серьёзность | file:lines | что не так | чем пытался опровергнуть | уверенность`, `F-nn`. BLOCKER — путь постройки с неписаным клоком; коалесцированный прыжок, пропускающий действие, которое делал `mpsc`; догоняющий валидатор, доказуемо пропускающий первый дедлайн без tee; второй паркующийся приёмник на одном `watch`; продакшн-поведение против HEAD вне названного снятия/замены. Затем непустое «Оставить как есть».

В финальном ответе — §0 сжато (вердикт, ворота, непрочитанное) и таблица §1.

<tone_preference>Кратко и по существу.</tone_preference>

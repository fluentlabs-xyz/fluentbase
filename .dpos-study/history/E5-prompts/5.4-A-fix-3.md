# Строка 5.4, заход А — круг 3: найти источник недетерминизма стенда и убрать его

Ты — исполнитель (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `08764870` (код базы — `a1337ab2`). Пути без префикса — от `crates/dpos/consensus/src/`. Рабочее дерево = база + заход А (круги 1 и 2, 14 файлов) — объект. Чужое `.dpos-study/history/E4-ORCHESTRATOR.md` — не читать.

## Установлено оркестратором (проверено им лично, опровергать — только замером)

Заход А сделал стенд недетерминированным **от процесса к процессу**. Замер: один и тот же тестовый бинарник, пять запусков подряд, сравнение печатаемого набора якорей (`anchors=…`) в `testbed::committee_tests::five_nodes_at_five_heights_hold_one_committee_record_per_epoch`:

- база `a1337ab2` (worktree `/tmp/wt-base-a1337ab2`, `CARGO_TARGET_DIR=/tmp/tgt-base`, бинарник `/tmp/tgt-base/debug/deps/fluentbase_consensus-16374378e027c0d8`): md5 `1a2c67e5c6` × 5 — **идентично**;
- дерево захода А: пять РАЗНЫХ md5 (`8a2b138cc8`, `8356217310`, `ac29a258ab`, `eb10633c74`, `c630187ede`).

Другой стендовый тест (`testbed::tests::the_ladder_names_successive_rungs_and_the_lagging_node_reaches_every_one`, печатает `steps(T, last(T+1), ticks)` и `UpstreamStats`) на дереве захода А печатает ОДНО И ТО ЖЕ 4/4 — значит недетерминизм точечный, а не тотальный.

Круг 2 объяснил красный тест «сдвигом фикстуры» и подвинул `TOMBSTONE_FROM` 64 → 88. Это объяснение неполное: сама изменчивость от процесса к процессу — новая, и её источник не назван. Правку фикстуры не откатывай (она может остаться и после), но помни: её обоснование опирается на регрессию, которую ты ищешь.

Уже исключено (не переделывай): ASLR (`setarch -R`), порядок армов `select!` в лаунчере (`biased;`), новые `HashMap`/`HashSet` в диффе (`git diff -- crates | grep '^+' | grep HashMap` — пусто).

## Задача

Найти, ЧТО в заходе А вносит зависимость от процесса, и убрать её так, чтобы стенд снова давал побитово один и тот же прогон.

Метод — бисекция диффа, не рассуждение:

1. Сделай себе площадку: `git worktree add --detach /tmp/wt-bisect <base>` + свой `CARGO_TARGET_DIR`; переноси туда части `git diff HEAD -- crates` по одному файлу/ханку (`git diff … > patch; git apply --include=…` или ручной перенос) и после каждого шага мери детерминизм — 5 запусков бинарника, md5 печатаемых якорей. Первый шаг, на котором md5 перестают совпадать, — виновник; дальше сузь до ханка.
2. Назови МЕХАНИЗМ с `file:line`: какая величина приходит из процесса, а не из семени. Типовые источники (проверь их прямо, а не по памяти): итерация `HashMap`/`HashSet`/`Set`/`IndexMap` где-то на пути, куда заход А добавил новое чтение; `Instant::now`/`SystemTime` вместо часов рантайма; адреса/указатели в ключе сортировки; `watch` + `borrow_and_update` там, где число коалесцированных тиков зависит от порядка планирования; любой `tokio` примитив, созданный ВНЕ детерминированного рантайма (например, канал, созданный в тестовом потоке до `runner.start`, и разбуженный его waker-ом).
3. Почини причину, а не симптом. Если причина — примитив, созданный вне рантайма, то создавать его надо внутри; если итерация неупорядоченного множества — упорядочить; если коалесцирование watch — показать, почему оно не может менять результат, и подтвердить замером.
4. Подтверди: 5 запусков → один md5; **и на тесте `five_nodes…`, и на `the_ladder…`, и на новом `a_catching_up_validator_deals_its_first_epoch_on_the_live_frontier_without_a_tee`**. Плюс: верни `TOMBSTONE_FROM` обратно на 64 и прогони 5 раз — если при детерминизме он снова стабильно зелёный, оставь 64 (меньше правки фикстуры); если стабильно красный — оставь 88 и объясни разницу механизмом (какой якорь сдвинулся и почему это законно).

## Правила

- Агентов не запускать. Git — только чтение; `git worktree add --detach` в `/tmp` разрешён как чтение базы; `stash`/`checkout`/`restore`/`reset` в рабочем дереве — запрещены.
- Записываемые файлы: те же 14, что уже в диффе, + `testbed/*.rs`, `.claude/dpos_architecture/*.md`, CHANGELOG, журнал `.dpos-study/history/E5-4-A.md` (раздел «## Круг 3»).
- Cargo (`CARGO_BUILD_JOBS=12`): тесты по имени; `--lib` целиком — один раз в конце. Стенд не гонять параллельно с другим cargo.
- **СТОП** (запись, не обход): источник найден, но его устранение требует правки за пределами списка файлов или меняет продакшн-семантику клока; источник — в базе (`a1337ab2`) и заход А лишь сделал его видимым (тогда покажи замером на базе, что он там уже есть, и опиши).

## Журнал — «## Круг 3»
1. Протокол бисекции: шаги, что применял, md5 по 5 запусков на каждом шаге (таблица). 2. Виновник: file:line + механизм. 3. Правка и почему она корневая. 4. Подтверждение: три теста × 5 запусков, md5. 5. Судьба `TOMBSTONE_FROM`. 6. Результаты cargo verbatim. 7. Слабее всего.

Заверши ответ в ≤ 20 строк.

# Финальное ревью захода Б строки 5.3 — идентичность DKG-лога `(dealer, hash)` (Opus, свежий контекст)

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база `HEAD` = `18573e76`. Объект — незакоммиченное рабочее дерево: 13 файлов под `crates/dpos/consensus/src/` (`beacon/{actor,artifact,ceremony,confirmations,dkg_agree,dkg_engine,dkg_msg,log_resolver,log_store,metrics,plane,share_state}.rs`, `testbed/tests.rs`) плюс доки `.claude/dpos_architecture/*.md` (gitignored, в дереве). Пути без префикса — от `crates/dpos/consensus/src/beacon/`. md5 на входе: `actor.rs 47760d6e…`, `artifact.rs 231da641…`, `ceremony.rs e74979f2…`, `log_resolver.rs e119b3a6…` — сверь первые 8 символов; не совпало — остановись и скажи.

Это ЧЕТВЁРТЫЙ и последний круг: до тебя — dsh-ревью круга 1 (`history/E5-3-B-DSH-1.md`, D-01…D-18), правка, dsh круга 2 (`history/E5-3-B-DSH-2.md`: часть (А) — вердикты по D-nn, часть (Б) — E-01…E-14), правка свежим исполнителем. Журнал исполнителей — `history/E5-3-B.md` (три круга). Постановка — `history/E5-prompts/5.3-B-impl-1.md`. Задача — **вердикт: можно ли коммитить**; не пятый круг полировки. Рамка — идентичность лога и её следствия; автомат состояний, вход p2p, гигиена — другие заходы. Существенное вне рамки — одна строка в конце.

## Установлено оркестратором (опирайся; опровергать — с file:line)

- На HEAD идентичность была по дилеру: `record_checked_log` first-wins (`ceremony.rs:412-414` на HEAD), `ingest_signed_log` `(true, ..)` на второй лог, replay last-wins; `DkgLogKey {epoch, dealer}` без хэша; commonware `Logs` write-only. `AgreedSet.pinned` (idx→hash) — истина о том, какой лог в силе; постановка запрещала его менять.
- **Два принятых отклонения (Д-nn оркестратора):** (1) приёмочный тест `…stops_the_chain_silently` больше НЕ останавливает цепь — жертва с share ⇒ 3 честных = quorum(4); переписан на новый вердикт. (2) `artifact.rs`/`plane.rs` вне списка постановки: `ArtifactPull::minter_to_ask` — целевой round-robin фетч по `committee[epoch]`; причина по журналу — снятие pre-agreement фетча логов обнажило, что нетаргетный `fetch` вечно переспрашивал быстрого `NotYet`-соседа, и стенд C7 (`a_zero_overlap_boundary_is_crossed_by_acquiring_the_other_halfs_key`, приёмка строки 5.1) держался на побочном эффекте. Оцени РЕАЛИЗАЦИЮ обоих, не факт отклонения; по (2) — подтверди или опровергни причинную цепочку своим прогоном (тест C7 на дереве с откаченным `minter_to_ask` → нетаргетный `fetch`).
- Записанные остатки (не поднимать заново как находки, только если запись неверна): D-09 (стенд лечится и через `fetch_bodies`, live-путь `fetch_missing_logs` держит юнит), D-10 (resume берёт first-recorded тело, не пришпиленное — остаток для А), D-11, D-18, E-04 (present-but-not-held после `cpk == pk` — недостижимо по построению), E-09, E-10 (never-acked игрок и dealing второго полинома — share теряется, форка нет), E-14.

## Правила

1. **Агентов не запускать** — ни для работы, ни для проверки себя.
2. **Git только на чтение.** Единственный файл на запись — `.dpos-study/history/E5-3-B-REVIEW.md`. Мутации — только с откатом и `md5sum` до/после в тексте. `.dpos-study/history/E4-ORCHESTRATOR.md` не читать.
3. Каждое утверждение — `file:line`. Комментарии, доки, журналы и dsh-отчёты — **не свидетельство**; свидетельство — код и твой прогон.
4. **Полнота, не фильтр**: все находки с серьёзностью (BLOCKER / SERIOUS / MODERATE / MINOR / NIT) и полем «чем пытался опровергнуть». Фильтрует оркестратор.
5. Поиск — только `git grep` (под `crates/dpos/consensus/src/.claude/session-reads/` чужие логи). Читать целыми строками.
6. **Cargo — только после ворот оркестратора**: перед первой cargo-командой `until grep -q DONE /tmp/claude-1000/-home-djadjka-Work-fluentbase/f6f42c91-6188-4d9c-ae64-a1e9faed9028/scratchpad/gates/m1-status.txt; do sleep 30; done`. До этого — чтение. `CARGO_BUILD_JOBS=6`; стенд не гонять параллельно с другой сборкой.
7. **Стенд — ДВА прогона**: `testbed::` с `--features dpos-devnet-byzantine` и без.

## Вход

- Дифф: `git diff HEAD -- crates` — читать ПЕРВЫМ. База ворот исполнителя: `--lib` 681/0, стенд с фичей 56/0, без фичи 47/0 (634 filtered), node 57/0, reader 64/0, slasher 16/0, clippy два чужих / ноль своих, с фичей 0, fmt 0, doc 6, harness 2/2086/10.
- Ключевые места: `ceremony.rs` — `LogId`, `signed_logs`/`first_log`/`equivocations`, `insert_log`, `record_checked_log`, `handle(Reveal)` бан, `checked_logs_in` (одно правило валидности пары), `resume`, `scoped_pinned_logs` (`cpk == pk`); `actor.rs` — `DkgActor::equivocations` + sweep, `fetch_missing_logs` (только при артефакте), `publish_recorded_logs` (`Entry::Vacant` first-wins по seat), `note_equivocation(.., durable)`; `log_resolver.rs` — `DkgLogKey` 72 байта, `BeaconFetchKey::Log` boxed (конверт 73); `share_state.rs` — `JournalRecord::DealerEquivocation`; `artifact.rs` — `slots`/`PullSlot`, `minter_to_ask`, перепроверка стора после throttle; `dkg_agree.rs::fetch_bodies` с хэшем; `testbed/tests.rs:~3117` и `~3293` — два приёмочных теста.

## §0 — прямые ответы, каждый с [KNOWN]/[ГИПОТЕЗА] и file:line

1. **Ворота** verbatim из ТВОИХ прогонов (все одиннадцать).
2. **Безопасность ключа.** Есть ли путь, где лог выбирается по дилеру без хэша и это влияет на то, над чем финализируется share (не на диагностику)? Разбери `first_log`, `recorded_dkg_logs`/`publish_recorded_logs`, `recompute_scoped`, `checked_serve_map`, `Player::resume`. Может ли честный узел финализировать share над НЕпришпиленным логом?
3. **Бан.** Что именно отвергается и что принимается для эквивокатора; может ли один поддельный лог (не подписанный дилером) запустить бан; может ли пришпиленный лог банённого дилера не дойти до церемонии жертвы. Поставь мутацию: бан распространить на целевой фетч — приёмочный тест должен покраснеть.
4. **Журнал.** Пара как один рекорд, whole-or-nothing; журнал старого формата (`PeerLog` второго хэша без пары) — что происходит; crash между записью первого `PeerLog` и пары. Replay ≡ live (`checked_logs_in` одна функция) — подтверди прогоном юнита.
5. **Wire.** `DkgLogKey` 72 байта, конверт 73, тег на месте; декод тотален на коротком входе — мутацией (обрезать вход) или чтением кодека.
6. **`ArtifactPull`.** Ротация: индекс, `wrapping_add`, `forget` на трёх выходах, рост `slots`, нечитаемый комитет, пустой `others`, не-член (никого не пропускает — ×n); перепроверка стора после throttle (E-07). Причинная цепочка C7 — твоим прогоном (п. выше).
7. **Приёмка.** Оба стенд-теста: витнесс подмены (`reveals_seen == reveals_swapped`, `ShareConfirm` с чужим хэшем), вердикт до/после (`[63;4]` → `[72;4]` для второго), `dpos_dkg_dealer_equivocation_total` `[1,0,0,0]`. Может ли любой из них быть зелёным при сломанном свойстве? Минимум ДВЕ свои мутации, не совпадающие с M1–M3 исполнителя и мутациями кругов 2–3.
8. **Доки.** `.claude/dpos_architecture/{00_preamble,05,08,12,13,15}.md` — соответствуют коду? `git grep` по старым формулировкам («by dealer», «first-wins», `{epoch, dealer}`) в доках и доккомментариях — ничего устаревшего.
9. **Граница и гигиена.** Продакшн вне 13 файлов не тронут (`git status`); `#[allow]` нет; `unwrap`/`expect` на продакшн-пути нет новых; новых `pub` наружу нет; мёртвый код после удаления `Logs`/`recorded` не остался.
10. **Hard-stop.** Нужно ли менять решение в `DECISIONS.md` (П-3, Д-1 — пришпиленный набор; Д-6 defer); есть ли BLOCKER.
11. **Вердикт: КОММИТИТЬ / НЕ КОММИТИТЬ**, одной фразой с причиной.
12. **Где твоя проверка была слабее всего** — ранжируй.

**§1 Находки** — таблица `id | серьёзность | file:lines | что не так | чем пытался опровергнуть | уверенность`; id `F-nn`. BLOCKER — share над непришпиленным логом, изменение пришпиливания, паника декода, регрессия живости на пути DKG. **§2 Оставить как есть** — непустой. **§3 Вне рамок** — по строке.

В финальном ответе — §0 сжато (включая вердикт) и таблица §1.

<tone_preference>Кратко и по существу.</tone_preference>

# Строка 5.2, заход А — факт σ: `SeedIndex` становится единственным владельцем

Ты — исполнитель (Opus 5) одного захода строки плана, в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `37691136` (строка 5.1 закрыта: `129f2754`, `fca59634`, `15e59c17`, `37691136`). Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`; `beacon/` = `crates/dpos/consensus/src/beacon/`. **Чужое в `git status`** — `.dpos-study/history/E4-ORCHESTRATOR.md` — не трогать.

## Правила (обязательные)

1. **Агентов не запускать.** Причина: спека и код обязаны сидеть в одном контексте; заход переписывает владельца факта, и делегирование разрывает связь «утверждение ↔ якорь».
2. **Git только на чтение.** Не коммитить, не `add`, не `stash`/`checkout`/`restore`/`reset`.
3. **Файлы на запись — ТОЛЬКО**: всё `beacon/**`; `spec_exec.rs`; `cert_inlet.rs`; `consensus/dpos.rs`; `testbed/{tests,cert_inlet_tests,committee_tests,preconditions,stand,fakes}.rs`; ТОЛЬКО `#[cfg(test)]`-модуль `executor.rs` (фикстура `for_seeds`, `:5012` и `:5051` — продакшн-тело `executor.rs` ЗАПРЕЩЕНО: три транзиентные копии σ — это строка 6.1); `.dpos-study/history/E5-2-A.md`. **Запрещено**: продакшн-тело `executor.rs`, `committee/**`, `plane_upstream.rs`, `epoch_manager.rs`, `node/**`, `p2p/**`, `bins/**`, `devnet/**`, `contracts/**`, `Cargo.toml`, `.claude/**` (доки — отдельный проход).
4. **file:line на каждое утверждение** в журнале.
5. Без `#[allow]`, `todo!`, таймеров вместо событий, поллинга вместо пробуждений; `unwrap` на продакшн-пути — только `Mutex::lock().unwrap()`. Любое — только строкой Д-nn с причиной.
6. Комментарии, доки и `.claude/dpos_architecture/` — **не свидетельство**; в этом репозитории они дрейфуют, и каждый проход Э5 находил устаревший. Свидетельство — код и твой прогон. Для commonware — `.claude/COMMONWARE_INTERNALS.md` ДО чекаута.
7. Читать целыми строками; никогда `cut -c`/`head -c`. Поиск — **ТОЛЬКО `git grep`** (под `crates/dpos/consensus/src/.claude/session-reads/` лежат gitignored журналы чужих сессий).
8. **Ворота — ТРИ прогона тестов, это условие приёма**: `cargo test -p fluentbase-consensus --lib`, `… --lib --features dpos-devnet-byzantine testbed::` (сейчас **55/0**), `… --lib testbed::` без фичи (**46/0**). Плюс clippy БЕЗ фичи и с фичей (рост — дефект; сейчас по два ЧУЖИХ предупреждения вне `fluentbase-consensus`), `cargo fmt --check`. Полный набор остальных крейтов — у оркестратора. «`--lib` зелёный» доводом не будет: в 5.1 это стоило возврата.
9. Правки батчем, один прогон затронутых тестов, ×3 по новым и переписанным стенд-тестам, мутации ≤ 2 с откатом и `md5sum`.
10. Текущие числа на HEAD: consensus lib **656**/0; стенд с фичей **55**/0, без фичи **46**/0; node 55/0; reader 64/0; slasher 16/0; clippy два чужих, с фичей 0; `fmt` чисто; doc unresolved 6. Существующие тесты не ослаблять; покраснение — находка с механизмом, не подгонка.

## Что прочитать ДО кода

1. `.dpos-study/PLAN.md`, блок «### Э5.», строка `5.2` — задание дословно.
2. `history/E5-BEACON-DESIGN.md`: §3 инварианты `:443-455` (особенно I1, I5, I7); §5.1 граница `:556-595` (строки `seed`, `observe_certificate`, `faults`, и абзац «Уходит внутрь»); §5.2 автомат `:596-617` (`recover(E)`, последний абзац про σ); §5.3 `:618-628` — строки «σ(round)» и «σ ВНЕ beacon»; §5.4 `:629-646` — строки «Ошибка persist σ» и «Рестарт без ключа эпохи»; §5.6 `:650-666`; §7 строка 5.2 `:711-733`.
3. `DECISIONS.md`: **Д-3** (`:23-26`) и **Д-9** (`:54-57`) — оба «принято 2026-09-12». Отклонение от них — hard-stop этапа, не твоя правка: если реализация требует изменить Д-3 или Д-9, останови этот пункт, сделай остальное и запиши предложение в §0.
4. `history/E5-0-BOUNDARY.md` В§5 «Что осталось для 5.2»; `history/E5-1-A.md` §5 и Д§5 — что 5.1 оставила именно тебе.

## Якоря, открытые оркестратором [KNOWN] — опирайся, не перепроверяй

- `certify.rs` (933 строки): `SEED_RETENTION = 4096` `:43`; `SeedStore` `:76`, `new` `:126`, `with_persistence` `:159`, `record` `:189`, `insert` `:201`, `lookup` `:256`; карантин — `quarantine` `:265`, `promote_epoch` `:298`, `quarantined_epochs` `:341`, `retain_quarantine_from` `:354`; terminal-карта — `pin_terminal` `:376`, `terminal_at` `:400`, `retain_terminal_from` `:409`; `events` `:418`; waiters — `prune_waiters` `:436`, `wait_for` `:450`.
- **Промоутер-задача** — `plane.rs:960-985`: `context.with_label("seed_promoter").spawn`, ждёт `promoter_edge.notified()`, гоняет `quarantined_epochs` × `promote_epoch`, и при `refused > 0` зовёт `report_fault(DataFault{epoch, refused})` (`:982`). После 5.1 её ребро — `ArtifactStore::subscribe`.
- **Сайтов вердикта в ПРОДАКШНЕ два, а не три**: `spec_exec.rs:92` и `cert_inlet.rs:689`; третий (`cert_inlet.rs:2641`) — в тестовом модуле. Строка плана называет три по состоянию до 5.0 — это уточнение, а не отклонение.
- `certificate_verdict` — `surface.rs:321`. `faults()` на трейте `:215`; реализации `:464`, `:618`, `:878`, `:1070`, `:1222`; защёлка `faults_armed` `:2009`, инициализация `:2057`.
- `for_seeds` — `surface.rs:985`; потребители ТОЛЬКО тестовые: `executor.rs:5012`, `:5051`; упоминание в доке `certify.rs:426`.
- `follower.rs` seed-половина: `promote_quarantined` `:332-340`, вызов из фетчера `:260`, спавн `:209`.
- **`on_invalid_seed` УЖЕ УДАЛЁН** строкой 5.1 (жил в `keys.rs`). Осталось два доккоммента — `surface.rs:170` и `:226`; это дрейф, который свод 5.1 пропустил. Поправь попутно, в журнал одной строкой.
- `observe_epoch` и `observe_cert` на трейте — `surface.rs:190`, `:196`; их seed-ноги в `LiveBeacon` — `retain_quarantine_from` и `retain_terminal_from`. 5.1 сняла ключевые ноги; **удаление самих методов с трейта — ТВОЯ работа**, и она возможна ровно тогда, когда уйдут обе карты.

## Что сделать

### 1. `SeedIndex` — единственный владелец σ

RAM-индекс с ЕДИНСТВЕННЫМ писателем; `seed(round)` синхронен и отвечает только по ТОЧНОМУ раунду (`executor.rs` и `epoch_manager` зовут его синхронно — проверь оба сайта и назови их); журнал — только для рестарта (чтение `Ordinal` асинхронно, поэтому `seed` обязан оставаться синхронным). Ретенция `SEED_RETENTION` + исключение терминального раунда как **правило вытеснения**, не отдельная карта: счёт глобальный, а спрашивает следующая эпоха. **`SEED_RETENTION` не менять.**

### 2. Карантин — состояние, а не карта и не диск

`Pending` внутри индекса. Промоутер-задача (`plane.rs:960-985`) удаляется целиком: её работу берёт ребро `KeyAvailable`, которое уже есть. `waiters`/`wait_for`/`prune_waiters` удаляются (`record` сегодня обходит карту на каждом вызове).

### 3. Вердикт: синхронный `Refused` + поздний `DataFault` с ПОТРЕБИТЕЛЕМ

Д-3 = вариант (в). `observe_certificate` возвращает вердикт СИНХРОННО; два продакшн-сайта `let _observed` получают читателей: `Refused` ⇒ `record_data_fault` inlet-а (`cert_inlet.rs:758`), `Pending` ⇒ `ReplaySeed::Defer`. `faults()` получает потребителя (Д-8 журнала 5.0) — сегодня канал гейтится `faults_armed` именно потому, что потребителя нет. Дедуп ERROR-строки — защёлка на эпоху (правило приехало из удалённого `keys.rs::reported_invalid_seed`).
**Наблюдаемая для стенда уже есть:** 5.0а дала `Outcome::cert_inlet[i].rotations`, и второе плечо `a_forged_seed_slot_costs_the_upstream_a_rotation_once_the_epoch_key_is_held` после твоей правки обязано поменять ПРИЧИНУ (было «BLS verify FAILED», станет «синхронный `Refused` ⇒ `record_data_fault`») при том же числе ротаций. Перепиши его доккоммент по факту и скажи в журнале, что изменилось в механизме, а что в числах.

### 4. Удаления

Промоутер-задача, `waiters`, terminal-карта КАК КАРТА (правило остаётся), seed-половина `follower.rs` и **сам файл `follower.rs`** (артефактную половину 5.1 уже слила — проверь и назови, что осталось), `for_seeds` вместе с фикстурой executor-а (`executor.rs:5012`, `:5051` — тестовый модуль, он тебе разрешён), `observe_epoch`/`observe_cert` с трейта. `hint_finalized` НЕ вводить (Д-9 это прямо запрещает).

### 5. Crash-replay и R-016

Crash-replay идёт через `observe_certificate`: `Pending ⇒ ReplaySeed::Defer` по существующей петле, возобновление по `KeyAvailable`. Сегодня архив читается БЕЗ проверки, «потому что ключа может не быть» — это E5-03, и оно закрывается проверкой под `PK_E`. Найди сайт сам (`consensus/dpos.rs`, crash-replay), назови его.
**Затем** R-016: порог re-jump валидатора равен `min(1024, interval)`; сделать абсолютным. Это «затем» из строки плана — делай ПОСЛЕ `SeedIndex`, и если порог окажется вне твоего списка файлов, скажи это с file:line и оставь строкой в §5.

### 6. Что в заходе А НЕ делается (записать в §5, не делать)

Стенд-тест R-129 — заход В. Три транзиентные копии σ в `executor.rs` (`:134`, `:239`, `:724`) — строка 6.1, продакшн-тело `executor.rs` запрещено. Правки `.claude/` — отдельный проход. Хвосты 5.1, заблокированные запретом на продакшн вне `beacon/` (Д-10, Д-11 буквально, Д-12, форма `CommitteeReads`, пин равенства окон) — не твои.

### 7. Тесты

Существующие тесты `certify.rs` (их там больше двадцати) — не удалять: каждый пинует свойство, которое новый владелец обязан нести. По каждому, который переписываешь, в журнал: что доказывал, что доказывает теперь, остаётся ли фальсификатор. Особое внимание: `the_epochs_terminal_round_outlives_the_retention_window` (`certify.rs:827`) — это ПРАВИЛО вытеснения, и оно обязано пережить исчезновение карты; `a_second_differing_seed_for_one_round_is_refused` (`:731`) — инвариант I1; `quarantined_seeds_are_never_served_and_promote_when_the_key_lands` (`:667`) — то самое, что становится состоянием `Pending`.
Новое: тест на СИНХРОННЫЙ `Refused` у потребителя (не у `certificate_verdict`); тест на поздний `DataFault` с потребителем; тест дедупа ERROR-защёлки. «Красный до правки» — для теста на дефект; назови ОДНУ строку-мутацию, которой оркестратор воспроизведёт его сам.

## Журнал `.dpos-study/history/E5-2-A.md` (короткая форма)

§0 Прямые ответы с [KNOWN]/[ГИПОТЕЗА] и file:line: (1) форма `SeedIndex` и кто его единственный писатель; (2) как терминальное правило пережило карту; (3) два сайта вердикта — что каждый теперь делает с `Refused` и с `Pending`; (4) потребитель `faults()` — кто, и что делает с поздним `DataFault`; (5) что удалено, посписочно, и `wc -l beacon/` до/после; (6) crash-replay: где сайт и чем теперь проверяется σ; (7) R-016 — сделано или заблокировано, с file:line; (8) тесты: имя / что утверждает / «красный до правки» verbatim или мутация + строка-мутация для оркестратора; (9) прогоны verbatim — `--lib`, ОБА набора стенда, clippy ×2, `fmt`, ×3; (10) что в проекте §5 оказалось неверным по коду; (11) запрещённого сделано — ноль или список; (12) слабое место.
§1 Отклонения Д-nn (нумерация 5.2а-Д-1, …): что иначе / причина file:line / меняет ли П или Д (если меняет — hard-stop, в §0). §2 Тесты. §3 Логические коммиты. §4 Где проверка слабее всего. §5 Что осталось заходу В и строкам 5.3/5.4.

Ты НЕ коммитишь. В финальном ответе — только §0.

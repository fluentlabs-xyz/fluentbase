# Row 5.3, заход Д — round 3: dsh findings D-05..D-08, D-12 (fresh implementer, Opus 5)

You are the implementer (Opus 5) in a fresh context. Repository `/home/djadjka/Work/fluentbase`, branch `djadjka/dpos-reth-2.2-squashed`, HEAD `d8747086`. Paths without a prefix are relative to `crates/dpos/consensus/src/`. The working tree carries round 1 (3 files under `crates/`, journal `.dpos-study/history/E5-3-D.md` §0) and round 2 (harness, `devnet/`, journal `## Round 2`). Do not read `.dpos-study/history/E4-ORCHESTRATOR.md`.

## What the change is (read the journal §0.2–0.4 first, then the code)
`recover`'s two arms `Torn if past_seal` / `NoFile if past_seal` (`beacon/actor.rs:~3256-3273`) now heal the share as a player (`heal_or_acquire` `:~2896`) instead of sitting out; `EpochState::SatOut` is deleted. The dsh review is `.dpos-study/history/E5-3-D-DSH-1.md`; the orchestrator opened every anchor below. Fix exactly these, nothing else:

1. **D-05** — `beacon/metrics.rs:52-53,65-76,87`: `StallReason::SatOut` and the `sat_out` label of `dpos_dkg_stalled` have no producer (`git grep -n 'SatOut\|sat_out' -- crates devnet` — only `metrics.rs` and `actor.rs` `carries` `:~504`). Delete the variant, its label arm and the `carries` mention; every `match` stays exhaustive. Then fix the two doc lines that say it stays registered: `.claude/dpos_architecture/00_preamble.md:~26` and `08_node_integration_crates_node_bins_fluent.md:~1107` (gitignored, edit in place; `grep -n 'SatOut\|sat_out' .claude/dpos_architecture/*.md` afterwards — only lines marked DELETED/historical may remain).
2. **D-06** — prose in the touched crate that now describes a state that cannot happen: `actor.rs:~2963` («Not dealing (sat out, …)»), `:~3794` («a sat-out / unrecoverable member»), `:~4461-4463` (`INTERVAL` doc naming the renamed test `…_sits_out`), and `beacon/share_state.rs:~560-566,584-586,629-632` (`JournalLoad`/`load_journal` docs «the caller SITS OUT» / «must SIT OUT» for `Torn`). Rewrite each to what the code does now (`Torn` at/after the seal ⇒ evicted and healed as a player; before the seal ⇒ evicted and dealt fresh — verify both arms in `recover` before writing). One line where one line does; no history («was», «before 5.3-Д», dates), no finding ids.
3. **D-07** — `testbed/tests.rs:~2064-2068`: the assertion message is inverted (`dealers_of(&artifact1) == 3` means the absentee's log is NOT in the set; the message says it is). Fix the message.
4. **D-08** — same lines: assert that the dealer absent from `artifact1`'s pinned set is node 3's key, not merely that the count is 3 (use the stand's key accessor — find how other tests in `tests.rs` name a node's BLS/peer key).
5. **D-12** — new test doc-comments in `actor.rs:~12005,12257-12259,13794-13800` cite design cells and change history («Cell (NoFile, h ≥ seal)», «§5.2 restart table», «Before 5.3-А1», «(B4″)»). Apply the comment rule to every comment line ADDED by round 1 (`git diff HEAD -- crates | grep -nE '^\+\s*//'`): keep only a one-line *why* the code cannot show; delete narration, history, references to briefs/designs/findings/dates. Existing comments not in the diff are not yours to touch.

## Rules
- Do not launch agents (the finding, the code and the test must sit in one context). Git read-only. Writable files: `beacon/actor.rs`, `beacon/metrics.rs`, `beacon/share_state.rs`, `testbed/tests.rs`, `.claude/dpos_architecture/00_preamble.md`, `.claude/dpos_architecture/08_node_integration_crates_node_bins_fluent.md`, `.claude/DPOS_ARCHITECTURE_CHANGELOG.md`, and the journal (append `## Round 3 — dsh D-05..D-08, D-12`). Nothing under `devnet/`.
- Open every `.rs` file with the `Read` tool before editing it (not `cat`/`sed`); the rules file for Rust is injected on that read — follow its «New code» section.
- Comments: default none; a kept comment is one line of *why*. Before the first cargo run: `git diff HEAD -- crates | grep -nE '^\+\s*//'` — every line must pass the rule; quote the count before/after in the journal.
- Cargo, `CARGO_BUILD_JOBS=12`, sequential, never in parallel with each other: `cargo check -p fluentbase-consensus --all-targets`, then the tests by name — `cargo test -p fluentbase-consensus --lib beacon::actor`, `cargo test -p fluentbase-consensus --lib beacon::metrics`, `cargo test -p fluentbase-consensus --lib --features dpos-devnet-byzantine an_absentee_restarted_after_the_seal_heals_its_share_and_signs`; then `cargo clippy -p fluentbase-consensus --all-targets` (both with and without `--features dpos-devnet-byzantine`), `cargo fmt --check`. Full gates are the orchestrator's. Quote every result verbatim.
- STOP (record, do not work around): a fix needs a file outside the list; D-08 cannot be asserted without a new stand accessor of more than ~5 lines.

## Journal section
1. Per item: hunk `file:line`, what changed. 2. Comment-line count before/after and the remaining added comment lines quoted. 3. Cargo verbatim. 4. `grep` results for `SatOut|sat_out` over `crates devnet .claude/dpos_architecture`. 5. Weakest point.

Finish the reply in ≤ 15 lines.

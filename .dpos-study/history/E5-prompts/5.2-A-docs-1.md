# Строка 5.2, заход А — фаза Ф7: свод `.claude/dpos_architecture/` с кодом

Ты — документатор (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база `HEAD` = `37691136`. Строка 5.2 (заход А) закончена и зелена в рабочем дереве, но НЕ закоммичена. Пути без префикса — от `crates/dpos/consensus/src/`.

## Правила

1. **Агентов не запускать.** Код и документ обязаны сидеть в одном контексте.
2. **Git только на чтение.** Не коммить, не индексировать.
3. **Продакшн-код и тесты НЕ ПРАВИТЬ вообще.** Твои файлы — только `.claude/dpos_architecture/*.md` и отчёт `.dpos-study/history/E5-2-DOCS.md`. Если найдёшь дефект В КОДЕ — запиши его в §3 отчёта, не правь.
4. **Каталог `.claude/` в `.gitignore`**, поэтому `git grep` по нему даёт ноль ВСЕГДА. Пользуйся `grep -rn … .claude/dpos_architecture/ --include='*.md'`. Пустой вывод `git grep` за отсутствие не принимать — на этом уже спотыкались.
5. Не читай `**/.claude/session-reads/*.jsonl` — журналы чужих сессий.
6. Каждая правка опирается на `file:line` КОДА, который ты открыл. Комментарий в коде — не свидетельство о поведении: в этом репозитории они дрейфуют, и последние пять проходов каждый нашли устаревший. Свидетельство — тело функции.
7. Правило проекта (`.claude/CLAUDE.md`): после изменения поведения DPoS секции `dpos_architecture/` и блок `verified-against` в `00_preamble.md` правятся В ТОМ ЖЕ изменении. Дрейф доков — блокер коммита, не следующая задача.

## Что изменила строка 5.2 (перечень для сверки; каждый пункт проверь по коду сам)

- `beacon/certify.rs` **удалён**; его место занял `beacon/seed_index.rs`. Тип `SeedStore` → `SeedIndex`; старое имя живёт ТОЛЬКО как алиас в `beacon/mod.rs` для тестового модуля `epoch_manager.rs`.
- **Трёх карт больше нет** (served / quarantine / terminal) — одна `BTreeMap<Round, Entry>` с состояниями `Entry::{Verified, Pending}` (`seed_index.rs:76`). «Карантин» как слово уходит: это состояние `Pending`.
- Терминальная экзёмпция — **правило вытеснения**, не карта: `oldest_evictable` (`seed_index.rs:512`) + `evict` (`:391`), защиту получают только `Verified`. Удалены `pin_terminal`, `terminal_at`, `retain_terminal_from`, `retain_quarantine_from`, `promote_epoch`, `quarantined_epochs`, `promote_quarantined`, `waiters`/`wait_for`/`prune_waiters`, `terminal_seed_at`, `for_seeds`, `lookup` (→ `seed`).
- `Pending` ограничен **поэпошно** (`retire_closed_pending_epochs`) и **своим** бюджетом (`bound_pending`), отдельным от `Verified` (`bound_verified`). Пик — `2 · SEED_RETENTION`.
- **Задача `seed_promoter` удалена**: settle едет на KEY-ветке моста (`plane.rs`), ПЕРЕД публикацией `KeyAvailable`. На `HEAD` это были две подписки на одно ребро — гонка.
- **Вердикт σ читается на трёх продакшн-сайтах** (Д-3 = вариант (в)): `spec_exec.rs` (пропуск спекуляции), `cert_inlet.rs` live-поток (`record_data_fault` + skip), `cert_inlet::UpstreamResolver` by-height (`warn!`, рычага у двери нет — это записанный остаток). Плюс crash-replay в `dpos.rs` (`Pending ⇒ Defer`). Поздняя половина — `Beacon::faults()` → `LateFaults` → дренаж в голове `CertInlet::ingest`.
- `Randomness::observe_epoch`/`observe_cert` — **ноги и реализации удалены**, на трейте пустые дефолты; продакшн-вызывающих в крейте больше нет.
- **`beacon/follower.rs` УДАЛЁН ЦЕЛИКОМ.** `FollowerRandomness` и `KeyOnlyOracle` удалены; follower строит тот же `LiveBeacon` с ПУСТЫМ `CeremonyStore`, и `build_follower`/`FollowerInputs`/`run_fetcher` живут в `beacon/plane.rs`. **В крейте ровно один `impl Randomness`** — `surface.rs` для `LiveBeacon`. Проверь это сам (`git grep 'impl Randomness for' -- crates`) прежде чем писать.
- **R-016 НЕ сделан**: порог остался `JUMP_THRESHOLD.min(interval)`, откачен решением оркестратора 5.2а-Д-5 — абсолютный порог заклинивает узел с остановленным исполнением (воспроизведено живым прогоном; свидетельство — `testbed/preconditions.rs:303-315`). Если документ где-то обещает абсолютный порог — это ошибка документа.

## Мой инвентарь дрейфа (перепроверь, он мог сдвинуться)

**179 попаданий в 10 файлах.** `SeedStore` 44, `quarantin*` 35, `follower.rs` 33, `observe_cert` 32, `FollowerRandomness` 18, `certify.rs` 13, `for_seeds` 8, `pin_terminal`/`terminal_at`/`observe_epoch` по 6, `promote_epoch`/`waiters`/`wait_for`/`on_invalid_seed` по 5, `retain_terminal_from` 3, `KeyOnlyOracle`/`retain_quarantine_from` по 2, `beacon::certify`/`quarantined_epochs`/`terminal_seed_at`/`promote_quarantined`/`seed_promoter` по 1.

По файлам: `09_followers.md` 56, `00_preamble.md` 35, `08_node_integration…md` 33, `13_invariants_gotchas_rules.md` 21, `02_ordering_core…md` 14, `03_epoch_machinery.md` 11, `06_staking_layer.md` 3, `15_smoke_cases…md` 2, `12_consensus_critical_constants.md` 2, `01_system_map.md` 2.

## Задание

1. **`09_followers.md` переписывается целиком** — весь раздел построен вокруг файла, которого больше нет. Не переименовывай символы: перепиши то, ЧЕМ follower является теперь (`LiveBeacon` с пустым ceremony, ключ через артефакт, σ через тот же индекс, want на `Pending`). Сохрани всё, что осталось верным, и назови в отчёте, что выбросил.
2. **`13_invariants_gotchas_rules.md`** — здесь не переименование, а РЕШЕНИЕ: заход снял инвариант про follower-а как отдельную реализацию. Найди его, скажи, держится ли он ещё в какой-то форме, и либо перепиши, либо удали с объяснением. То же для инвариантов про карантин и про терминальную карту.
3. **Остальные восемь файлов** — свод по якорям. Каждое попадание: либо правка по коду, либо строка в отчёте «оставляю, потому что…».
4. **`00_preamble.md`** — добавь запись `verified-against` для строки 5.2 в том же формате, что уже используют 5.0а и 5.1 (посмотри, как они сделаны, и повтори форму).
5. **`15_smoke_cases…md`** — если девнетные ожидания разошлись с кодом, назови это; ПРАВИТЬ `devnet/` нельзя и не надо, но документ обязан говорить правду. В частности проверь: отчёт третьего прохода сообщает, что `devnet/…/verdicts_follow.py` ждёт строку `"cert-follow: PK_epoch obtained…"`, а код пишет `"beacon: PK_epoch obtained…"` — и что это расхождение ПРЕДСУЩЕСТВУЕТ `HEAD`. Проверь сам и запиши как есть.
6. **Ищи дефекты, которые ревью пропустило.** В строке 5.1 доковый проход нашёл два, которых не было в отчёте ревью: мёртвый счётчик и восемь комментариев на удалённые символы. Смотри в ту же сторону: метрики без потребителя, доккомментарии в КОДЕ на удалённые имена (их правит не ты — ты их называешь), обещания документа, которых код не выполняет.

## Отчёт `.dpos-study/history/E5-2-DOCS.md`

**§0** — сколько попаданий было, сколько осталось, и по каждому оставшемуся — почему.
**§1** — что переписано, по файлам и подразделам; для `09_followers.md` отдельно: что выброшено и почему.
**§2** — решения по инвариантам `13_…md`: какой снят, какой переписан, какой держится.
**§3** — **дефекты в КОДЕ, которые ты нашёл, а ревью пропустило** (не правь, назови file:line и почему это дефект).
**§4** — расхождения документа с девнетом.
**§5** — где твоя проверка слабее всего.

В финальном ответе — только §0 (кратко) и §3 целиком.

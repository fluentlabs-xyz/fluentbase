# Финальное ревью захода В строки 5.2 — стенд-пин R-129 (круг 4, Opus)

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база `HEAD` = `128e5224`. Объект — незакоммиченное рабочее дерево: изменены ТОЛЬКО `crates/dpos/consensus/src/testbed/{stand.rs, cert_inlet_tests.rs, tests.rs}`. Продакшн не тронут — это условие захода. Пути без префикса — от `crates/dpos/consensus/src/`. md5 объекта: `cert_inlet_tests.rs 68361290ae5298fe453f14a6df65e0d9`, `stand.rs d347a051d7beed72d477a310ed399f52`, `tests.rs c09a704f4a7939a8db9f35f9b13eac9b` — сверь до начала; если не совпало, остановись и скажи.

Это ЧЕТВЁРТЫЙ и последний круг: до тебя были Opus-ревью (`history/E5-2-V-REVIEW.md`, V-01 BLOCKER исправлен), два круга DeepSeek (`history/E5-2-V-DSH-2.md`, `E5-2-V-DSH-3.md`) и четыре круга правок исполнителя (`history/E5-2-V.md`). Твоя задача — финальный вердикт: **можно ли коммитить**. Это не повод открыть пятый круг полировки: рамка — один стенд-тест и его наблюдаемые. Если заметишь существенное вне рамки — одна строка в конце.

## Установлено оркестратором по коду (опирайся; опровергать — только с file:line)

- На `deliver == false` резолвер commonware делает ДВА действия (`~/.cargo/git/checkouts/monorepo-27b478c9bb41d208/3c4e02c/resolver/src/p2p/engine.rs:437-438`): `block!` (транспортный бан — no-op со стендовым шпионом и с продакшновым `NoopBlocker`) и `fetcher.block(peer)` → `excluded`, откуда ничего не удаляется. Гейдж `peers_blocked = excluded.len()` пишется ТОЛЬКО в арме `on_start` (`engine.rs:177`). Макрос `block!` (`p2p/src/lib.rs:435-442`) сначала `tracing::warn!("invalid data received")`, потом `block().await`.
- Маршальский арм `verify_delivered ⇒ false` в этой фикстуре недостижим: при отсутствии схемы маршал отвечает `true` ДО декодирования (`marshal/core/actor.rs:964-972`). Тест это документирует и НЕ закрывает R-129 целиком.
- Мутация M7 (вторая метка `brand_new_arm` на шаге (5) `plane_upstream.rs:487`) воспроизведена оркестратором: красный на `cert_inlet_tests.rs:1350`.
- Записанные и принятые остатки (не поднимать заново как находки, только если найдёшь, что запись неверна): D-04 (шпион доказывает раздачу, не использование), D-05/E-03 (рельс `dropped <= plane_calls` слабый), D-06 (дроп виден только по счётчику), E-01 (ветвь `else { eprintln! }` при неживом захвате — конвенция модуля, `tests.rs:195`), E-02 (строка `BLOCK_WARN` зависит от текста commonware), E-08 (консенсусный шпион стоит и у batcher/voter — латентный ложный красный).

## Правила

1. **Агентов не запускать** — ни для работы, ни для проверки себя.
2. **Git только на чтение.** Единственный файл на запись — `.dpos-study/history/E5-2-V-REVIEW-2.md`. Мутации — только с откатом и `md5sum` до/после в тексте. `.dpos-study/history/E4-ORCHESTRATOR.md` не читать.
3. Каждое утверждение — `file:line`. Комментарии, доки, журнал исполнителя и отчёты dsh — **не свидетельство**; свидетельство — код и твой прогон.
4. **Полнота, не фильтр**: все находки с серьёзностью (BLOCKER / SERIOUS / MODERATE / MINOR / NIT) и полем «чем пытался опровергнуть». Фильтрует оркестратор.
5. Поиск — только `git grep`. Читать целыми строками.
6. **Cargo — только после того, как закончатся ворота оркестратора**: перед первой cargo-командой выполни `until grep -q DONE /tmp/claude-1000/-home-djadjka-Work-fluentbase/f6f42c91-6188-4d9c-ae64-a1e9faed9028/scratchpad/gates/i1-status.txt; do sleep 30; done`. До этого — чтение. `CARGO_BUILD_JOBS=6`.
7. **Стенд — ДВА прогона**: `testbed::` с `--features dpos-devnet-byzantine` и без.

## Вход

- Дифф: `git diff HEAD -- crates/dpos/consensus/src/testbed/`. Читать ПЕРВЫМ, до журналов.
- Тест: `testbed/cert_inlet_tests.rs` `an_epoch_outside_this_nodes_read_window_costs_no_peer_its_channel` (около `:1050-1400`); общая фикстура `run_held_lag_over_donor_archive` / `assert_the_lag_is_held` (около `:840-914`), сосед на ней (около `:962-973`). Наблюдаемые: `BlockerSpy` (`stand.rs:419-500`), `Outcome::blocked`, `Outcome::peers_blocked()` (`stand.rs:~1231`), `BLOCK_WARN` через `Outcome::logs`.
- Ворота исполнителя (база): `--lib` 676/0, стенд с фичей 56/0, без фичи 47/0 (`filtered out` 629), node 57/0, reader 64/0, slasher 16/0, clippy два чужих / ноль своих, clippy с фичей 0, fmt exit 0, doc 6, harness 2 failed / 2086 / 10.

## §0 — прямые ответы, каждый с [KNOWN]/[ГИПОТЕЗА] и file:line

1. **Ворота** verbatim из ТВОИХ прогонов (все одиннадцать); расхождения назвать.
2. **Может ли тест быть ЗЕЛЁНЫМ при сломанном свойстве** — по каждому ассерту: `blocks.is_empty()`, `excluded.is_empty()`, `block_warns.is_empty()`, `rejected.is_empty()`, `fired == covered`, набор семейств `peers_blocked`, `sites`, `out.blocked.len()`. Назови конфигурацию, где ассерт держится, а свойство нет, или скажи, что такой нет. Поставь **минимум две свои мутации**, не совпадающие с M1/M7/M8 исполнителя (например: `return true` → `return false` только для `not_readable`; удалить `fetcher.block` эквивалент невозможно — тогда мутируй тест-сторону: подмени шпион фронтира на `NoopBlocker` при сохранённом `at(...)` и покажи, что ловит).
3. **Честность покрытия**: имя теста, докстрока и `COVERED` — согласованы ли с тем, что фикстура реально производит; проверь своим прогоном, что `fired == {"out_of_window"}` и `defers > 0`.
4. **D-12 рефакторинг**: сосед `…_over_a_donor_archive…` ведёт себя байт в байт как до выноса — сверь по `git show HEAD:` и по своему прогону (числа `ingests/teed/defers`).
5. **`peers_blocked()` и семейства**: правильно ли парсится экспозиция, все ли пять узлов дают три названных семейства, что с `dkg_simplex_resolver` — премисса «хотя бы один» устойчива?
6. **Граница и гигиена**: продакшн не тронут (`git status`), новых `pub` наружу нет, `#[allow]` нет, `unwrap`/`expect` вне `#[cfg(test)]` нет.
7. **Докстрока против кода**: каждое фактическое утверждение в докстроке теста (`:1050-1150`) — верно ли по коду; неверное — находка.
8. **Hard-stop**: нужно ли менять решение в `DECISIONS.md`; есть ли BLOCKER.
9. **Вердикт**: КОММИТИТЬ / НЕ КОММИТИТЬ, одной фразой с причиной.
10. **Где твоя проверка была слабее всего** — ранжируй.

**§1 Находки** — таблица `id | серьёзность | file:lines | что не так | чем пытался опровергнуть | уверенность`; id `F-nn`. **§2 Оставить как есть** — непустой. **§3 Вне рамок** — по строке.

В финальном ответе — §0 сжато (в том числе вердикт п.9) и таблица §1.

<tone_preference>Кратко и по существу.</tone_preference>

# Строка 5.4, заход А — круг 2: красные ворота (`committee_tests`) и разбор СТОП-а по поллеру

Ты — исполнитель (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `08764870`. Пути без префикса — от `crates/dpos/consensus/src/`. Рабочее дерево = HEAD + заход А круга 1 (14 файлов) — объект. Чужое `.dpos-study/history/E4-ORCHESTRATOR.md` — не читать.

## Входы

1. `.dpos-study/history/E5-prompts/5.4-A-impl-1.md` — постановка круга 1.
2. `.dpos-study/history/E5-4-A.md` — журнал круга 1 (§0.2 СТОП по поллеру, §0.3 рестарт, §0.4 таблица tee-ассерций, §0.5 стенд-тест, §0.6 мутации, §0.9 слабее всего).

## Задача 1 (главная) — красные ворота оркестратора

`cargo test -p fluentbase-consensus --lib` на твоём дереве: **729 passed, 1 failed**.

```
testbed::committee_tests::five_nodes_at_five_heights_hold_one_committee_record_per_epoch ... FAILED
panicked at testbed/committee_tests.rs:328: no epoch was read on both sides of the tombstone height 64,
so the live flag was the same for every node:
[[(0,0),(1,1),(2,32),(3,64),(4,95),(5,127),(6,159),(7,168)],
 [(0,0),(1,2),(2,31),(3,64),(4,96),(5,128),(6,159),(7,168)],
 [(0,0),(1,1),(2,32),(3,64),(4,95),(5,127),(6,159),(7,168)],
 [(0,0),(1,1),(2,32),(3,64),(4,68),(5,116),(6,159),(7,168)],
 [(0,0),(1,2),(2,32),(3,64),(4,95),(5,127),(6,159),(7,168)]]
```

На дереве до твоего изменения (`a1337ab2`, ворота `w3`) этот тест зелёный — **проверь это сам** (`git stash` НЕЛЬЗЯ; сравнивай через `git show HEAD:<путь>` в отдельный каталог scratchpad + собственный `CARGO_TARGET_DIR`, либо через `git worktree add --detach` в `/tmp` и прогон там; ветку и дерево не трогать).

Падает ПРЕДПОСЫЛКА теста (`:317-331`), а не его свойство: ни одна эпоха больше не читается по обе стороны `TOMBSTONE_FROM = 64`. Требуется РАЗДЕЛИТЬ два объяснения, и ответ обязан опираться на код, а не на правдоподобие:

- **(а) сдвиг фикстуры**: смена клока с `mpsc` на `watch` изменила траекторию детерминированного рантайма (прецедент — инцидент круга 1 захода Б, `history/E5-4-B.md` §0.9: лишняя runnable-задача на тик тасует очередь готовых задач общим RNG, `runtime/src/deterministic.rs:577-578`), и якоря чтения комитета разъехались; свойство теста цело, фикстура перестала его создавать;
- **(б) настоящее изменение поведения**: узел стал читать запись комитета на другом якоре систематически (например, потому что ET/менеджер теперь просыпается от другого события), и это регрессия, которую тест поймал по касательной.

Что сделать: назвать причину с `file:line`, показать МЕХАНИЗМ (какая задача, какое событие, какой якорь), и только потом чинить. Если (а) — восстанови предпосылку минимальной правкой фикстуры (высота тумбстоуна / момент разреза / целевая высота), не ослабляя ни одного утверждения теста; в журнале — что именно подвинул и почему это не подгонка под зелёный (чем тест краснел бы, если бы свойство сломалось). Если (б) — **СТОП**: запиши диагноз и не чини.

## Задача 2 — СТОП по поллеру: зафиксировать границу

Оркестратор проверил и принял твой СТОП (`geometry_tx.send_replace` `node/dpos.rs:1569` — единственный писатель; актор паркуется на первом `Some`, `beacon/plane.rs:860-863`). Нужно, чтобы остаток был описан как отклонение, а не как недоделка:

- Заведи **Д-5.4А-1** в журнале: поллер остаётся задачей (freeze + публикация геометрии, первая `track_peers`, tombstone-watch), его клок-часть снята; условие закрытия — кто и когда станет писателем `geometry_tx` (назови кандидата по коду: `EpochTransition::cold_start`/`apply_at`, что мешает сегодня — порядок постройки и зависимость от `track_peers`).
- Проверь и напиши прямо: после снятия клок-части поллер больше НЕ влияет на клок бикона ни одним путём (`git grep` по его функциям), и удаление его клок-части не осиротило ни один счётчик/гейдж.

## Задача 3 — остатки

- `sync_metrics`: убедись, что `record_dkg_clock`/`record_ordering_tip` по-прежнему пишутся (два гейджа — единственная видимость вставшего актора) и что гейдж клока пишется из одного места; `dpos_dkg_height_drops_total` удалён — проверь, что его нет ни в доках, ни в харнессе (`git grep dkg_height_drops -- crates devnet .claude`).
- `cargo test -p fluentbase-node --lib` и `cargo clippy -p fluentbase-consensus -p fluentbase-node --all-targets` — verbatim в журнал (у оркестратора чужой варнинг сдвинулся на `node/dpos.rs:1982`, своих нет).

## Правила

- Агентов не запускать. Git — только чтение (`git show`, `git worktree add --detach` в `/tmp` допустим как ЧТЕНИЕ базы; `stash`/`checkout`/`restore`/`reset` — нет).
- Записываемые файлы: `testbed/{committee_tests,stand,tests,cert_inlet_tests,preconditions}.rs`, `beacon/*.rs`, `application.rs`, `cert_inlet.rs`, `dpos.rs`, `outer.rs`, `sync_metrics.rs`, `crates/node/src/dpos.rs`, `.claude/dpos_architecture/*.md`, CHANGELOG, журнал `.dpos-study/history/E5-4-A.md` (раздел «## Круг 2»).
- Cargo (`CARGO_BUILD_JOBS=12`): нужные тесты по имени, `--lib` целиком — один раз в конце. Стенд не гонять параллельно с другим cargo.
- **СТОП**: объяснение (б); фикстуру нельзя восстановить, не ослабив утверждение.

## Журнал — «## Круг 2»
1. Диагноз красного теста: (а) или (б), механизм с file:line, как отделил одно от другого (включая прогон на базе). 2. Правка фикстуры: что подвинул, почему не подгонка. 3. Д-5.4А-1. 4. Остатки (задача 3). 5. Результаты cargo verbatim. 6. Слабее всего.

Заверши ответ в ≤ 20 строк.

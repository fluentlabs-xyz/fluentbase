# Финальное ревью захода В+Г1 строки 5.3 — вход p2p бикона и малая гигиена (Opus, свежий контекст)

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база `HEAD` = `a45c458d`. Объект — незакоммиченное рабочее дерево: 10 файлов под `crates/` (`dpos/consensus/src/beacon/{actor,ceremony,dkg_transport,mod}.rs`, `dpos/consensus/src/testbed/{stand,tests,committee_tests}.rs`, `dpos/consensus/src/dpos.rs` (комментарий), `dpos/staking-reader/src/epoch_transition.rs` (комментарий), `node/src/dpos.rs`) плюс доки `.claude/dpos_architecture/*.md` (gitignored, в дереве). Пути без префикса — от `crates/dpos/consensus/src/beacon/`. Сверь md5 до начала (первые 8): `actor.rs 77f2b43d`, `ceremony.rs edfb78c0`, `testbed/stand.rs fc7e9627`, `testbed/tests.rs 56a995ff`, `testbed/committee_tests.rs 4a33669f`; не совпало — остановись и скажи.

Это ПЯТЫЙ и последний круг: dsh круг 1 (`history/E5-3-VG1-DSH-1.md`, три SERIOUS) → **пересмотр решения 2 карты оркестратором** → свежая правка → dsh круг 2 (`history/E5-3-VG1-DSH-2.md`) → правка → СТОП на фикстуре → правка фикстуры. Журнал исполнителей — `history/E5-3-VG1.md` (четыре круга). Постановки — `history/E5-prompts/5.3-VG1-{impl-1,fix-2,fix-3,fix-4}.md`. Твоя задача — **вердикт: можно ли коммитить**. Рамка — вход p2p бикона и три пункта гигиены; автомат состояний, идентичность лога (Б, закоммичен `cee2e037`), `Option`-швы — другие заходы.

## Установлено оркестратором (опирайся; опровергать — с file:line)

- **Пересмотр решения 2 (Д-nn оркестратора):** изначальная карта вешала по-эпошное членство на `TrackedWindow` в акторе — dsh круг 1 доказал (проверено оркестратором по коду), что это ломает живость: `mint` edge-triggered (`confirmations.rs:186-188`), отвергнутый confirm не переиздаётся; часы актора включают нефинализированный ordering-tip (`actor.rs:1322-1327`) и могут опережать окно ET. Итоговое правило: **классификация отправителя — только пред-декодный `GatedReceiver` (в проде так с 4.3-А; в стенде — впервые с этого захода), актор членства не проверяет, эпоху привязывает потребитель** (commonware `Info` для дилингов/ack; ростер `committee_for` для confirm и на диспатче/буферизации — `DkgCeremony::has_seat`, `refuse("no_seat")`).
- Одно окно: `INGRESS_LOOKAHEAD_EPOCHS = 2`, `within_ingress_window`, три сайта; `now+3` (R-126) не расширяется — записью. Одно место отказа: `refuse(from, epoch: Option<u64>, reason)` — метки `epoch`, `no_seat`, `undecodable`; реестры не сливаются (R-070). resolver-exit ⇒ `error!` + `break`. `deque_size = 2` — лидер перепредлагает другое тело после nullify (`dkg_agree.rs:1455-1460`, `:1105-1130`), константа не менялась, юнит через `build_body_engine`.
- Стенд: окно на узел через `TrackSink` продакшн-`assemble_tracked_peers`, `with_tombstones` над стендовым `TombstoneSet`, который заполняется прод-формой `observe(&snapshot)`; `GatedReceiver(.., "beacon", true)` на beacon-канале каждого узла; роль `Role::StrayDealer` (registry-tier отправитель) и тест на тумбстоуненного члена (`untracked`).
- **СТОП третьего круга и его снятие:** заполнение `TombstoneSet` покрасило пред-существующую фикстуру `committee_tests::four_nodes_at_four_heights…` (узел 0 тумбстоунен с 64 и в `C[3]={0,1,2}` ⇒ `covering=1 < quorum=3`): фикстура была BFT-неисполнима под прод-семантикой и держалась на том, что стенд тумбстоун к транспорту не применял. Решение оркестратора: пять узлов, `C[3]=C[4]={0,1,2,4}`, тест переименован в `five_nodes_at_five_heights…`, все премиссы непустые (`straddled=[3]`, узел 0 ∈ `C[3]`, `tree_only[3]=Some`, два re-jump). Оцени, не потерял ли тест то, что доказывал (докстрока `committee_tests.rs:150-195`).
- Пред-существующие остатки (не поднимать как находки, только если запись неверна): E-01 (confirm НОВОГО члена, ещё не в трёх записях окна, режется гейтом и не переиздаётся — с 4.3-А), E-02 (гейт по окну и потребитель по `committee_for` могут разойтись на пропущенной несовершённой записи), E-06 (`has_seat` на `Reveal` — как HEAD), D-14 (третье перепредложение вытесняет первое из deque), D-18 (арм `pinned_rx`/`artifacts_rx` не fatal — по дизайну).

## Правила

1. **Агентов не запускать** — ни для работы, ни для проверки себя.
2. **Git только на чтение.** Единственный файл на запись — `.dpos-study/history/E5-3-VG1-REVIEW.md`. Мутации — с откатом и `md5sum` до/после. `.dpos-study/history/E4-ORCHESTRATOR.md` не читать.
3. Каждое утверждение — `file:line`. Комментарии, доки, журналы и dsh-отчёты — не свидетельство.
4. **Полнота, не фильтр**: все находки с серьёзностью и «чем пытался опровергнуть». Фильтрует оркестратор.
5. Поиск — только `git grep`. Читать целыми строками.
6. **Cargo — только после ворот оркестратора**: `until grep -q DONE /tmp/claude-1000/-home-djadjka-Work-fluentbase/f6f42c91-6188-4d9c-ae64-a1e9faed9028/scratchpad/gates/p1-status.txt; do sleep 30; done`. `CARGO_BUILD_JOBS=6`; стенд не гонять параллельно с другой сборкой.
7. Стенд — ДВА прогона (с фичей `dpos-devnet-byzantine` и без).

## Вход

- Дифф `git diff HEAD -- crates` — ПЕРВЫМ. База ворот исполнителя: `--lib` 688/0, стенд с фичей 58/0, без 49/0 (639 filtered), node 57/0, reader 64/0, slasher 16/0, clippy два чужих / ноль своих, с фичей 0, fmt 0, doc 6, harness 2/2086/10.

## §0 — прямые ответы, каждый с [KNOWN]/[ГИПОТЕЗА] и file:line

1. **Ворота** verbatim из ТВОИХ прогонов (все одиннадцать).
2. **Таблица кадров — построй САМ по коду**: тип кадра (`Commitment`, `Share`, `Ack`, `Reveal`, `Confirm`) × смещение эпохи отправителя относительно `now` получателя (−2…+2) × где режется (гейт / `epoch` / `no_seat` / принят / буфер) → кто доставит повторно. Сравни с HEAD (`git show HEAD:…actor.rs`). Любая клетка «нужный кадр, нет повторной доставки», которой не было на HEAD, — находка по последствию.
3. **`no_seat`**: все три сайта (буферизация, дрейн, диспатч, `on_confirm`) — одно правило? может ли честный кадр получить `no_seat` (церемония до читаемого ростера; resume player-only; relayed `Reveal`)? Мутация: снять проверку на одном сайте — какой тест краснеет.
4. **Стенд-гейт**: окно заполняется в те же моменты, что в проде (`track` на переходе)? `TombstoneSet` — тот же экземпляр у окна и у `OuterBuilder`? Каждая роль (включая византийские под фичей) идёт через гейт? Тесты `StrayDealer` и «тумбстоуненный член» — атрибуция по `channel="beacon"` И `reason`; могут ли быть зелёными без гейта (мутация: снять гейт).
5. **Фикстура пяти узлов**: докстрока `committee_tests.rs:150-195` против кода — те же четыре премиссы и три наблюдения; `straddled` непуст и тумбстоуненный узел ∈ страддленной эпохе (иначе премисса 4 вакуумна); `rotate_four_three_four` остался для backfill-тестов — они не задеты.
6. **`refuse` и `undecodable`**: единственный сайт `record_ingress_drop` в биконе; `undecodable` не считает то, что не отказ; `refuse` с `epoch: None` — где.
7. **resolver-exit `break`**: что остаётся не остановленным в `run` после `break`; достижим ли `None` штатно (тестовый харнесс, ранний дроп `LogHandler`).
8. **`deque_size` юнит**: через `build_body_engine` с продакшн-конфигом; проверяет ли именно «два тела одного отправителя живут, третье вытесняет первое».
9. **Доки** `.claude/dpos_architecture/{00,08,12,13,15}.md` — под пересмотренное правило; `git grep beacon_member|window_unset|not_member` в доках и коде — устаревших утверждений нет.
10. **Граница и гигиена**: продакшн вне 10 файлов не тронут; `#[allow]` нет; новых `unwrap`/`expect` вне тестов нет; реэкспорты в `beacon/mod.rs` для `StrayDealer` — `cfg(test)`-скоуп или расширение публичной поверхности; мёртвого кода после удаления окна из актора/`ValidatorInputs` нет.
11. **Hard-stop**: нужно ли менять решение в `DECISIONS.md`; есть ли BLOCKER.
12. **Вердикт: КОММИТИТЬ / НЕ КОММИТИТЬ**, одной фразой с причиной.
13. **Слабее всего** — ранжируй.

**§1 Находки** — таблица `id | серьёзность | file:lines | что не так | чем пытался опровергнуть | уверенность`; id `F-nn`. BLOCKER — нужный честный кадр без повторной доставки (которого не было на HEAD), стенд зелёный там, где прод режет, `break` в штатной работе, изменение пришпиливания. **§2 Оставить как есть** — непустой. **§3 Вне рамок** — по строке.

В финальном ответе — §0 сжато (включая вердикт) и таблица §1.

<tone_preference>Кратко и по существу.</tone_preference>

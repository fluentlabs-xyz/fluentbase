# Ревью захода В строки 5.2 — стенд-пин R-129

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база `HEAD` = `128e5224`. Объект — незакоммиченное рабочее дерево: изменены ТОЛЬКО `crates/dpos/consensus/src/testbed/{stand.rs, cert_inlet_tests.rs, tests.rs}`. Продакшн не тронут, и это условие захода, а не случайность. Пути без префикса — от `crates/dpos/consensus/src/`.

Заход маленький и рамка узкая: **один стенд-тест и его наблюдаемая**. Это не ревью beacon-а, не ревью R-129 как дефекта и не повод предлагать правку продакшна. Если заметишь существенное вне рамки — одна строка в конце, не развивая.

## Что уже установлено оркестратором ПО КОДУ (не перепроверяй, опирайся)

R-129 говорит: «`deliver == false` на marshal-резолвере исключает честного пира навсегда». **Половина «исключает» в нашей разводке не достигается:** blocker резолвера маршала берётся из `OuterBuilder.blocker` (`outer.rs:1267` — `Hybrid`, `:1302` — `Plane`), а все три конструкции `OuterBuilder` дают `NoopBlocker` (`dpos.rs:2746`, `dpos.rs:3837`, `testbed/stand.rs`). Единственный настоящий `block()` в продакшне — тумбстоун за эквивокацию (`crates/node/src/dpos.rs:1715`). Обоснование Noop'а написано про симплекс-батчер, не про маршал — защита побочная. **Поэтому предмет захода — ПИН, а не изменение поведения.**

Также закрыто: `CertProvider` — чужой трейт (`outer.rs:39`), причину по типу не несёт; пред-гейт как на фронтире на арме `Plane` невозможен, потому что потребителем у резолвера маршала является сам актор маршала (`.claude/COMMONWARE_INTERNALS.md:166`).

## Правила

1. **Агентов не запускать** — ни для работы, ни для проверки себя.
2. **Git только на чтение.** Единственный файл на запись — `.dpos-study/history/E5-2-V-REVIEW.md`. Мутации — только с откатом и `md5sum` до/после в тексте. `.dpos-study/history/E4-ORCHESTRATOR.md` не читать.
3. Каждое утверждение — `file:line`. Комментарии, доки и журнал исполнителя — **не свидетельство**; свидетельство — код и твой прогон.
4. **Полнота, не фильтр**: все находки с серьёзностью (BLOCKER / SERIOUS / MODERATE / MINOR / NIT) и полем «чем пытался опровергнуть». Фильтрует оркестратор.
5. Поиск — только `git grep` (под `crates/dpos/consensus/src/.claude/session-reads/` лежат чужие журналы). Читать целыми строками.
6. **Стенд — ДВА прогона**: `testbed::` с `--features dpos-devnet-byzantine` и без. Без фичи тесты под `cfg(feature)` не компилируются.
7. `CARGO_BUILD_JOBS=6`; стенд не гонять параллельно с другой сборкой. Если среда запретит команду — скажи прямо.

## Вход

- Постановка: `history/E5-prompts/5.2-V-impl-1.md`. Журнал: `history/E5-2-V.md` — читать ПОСЛЕ диффа.
- Тест: `testbed/cert_inlet_tests.rs:1078` `an_epoch_whose_scheme_cannot_be_built_costs_no_peer_its_channel`. Наблюдаемая: `BlockerSpy` (`testbed/stand.rs:419-442`), роздан в `stand.rs:2439` (фронтир) и `:2810` (`OuterBuilder.blocker`).
- Ворота исполнителя: `--lib` 676/0, стенд с фичей 56/0, без фичи 47/0 (`filtered out` 629), node 57/0, reader 64/0, slasher 16/0, clippy два чужих и ноль своих, clippy с фичей 0, `fmt` чисто, doc 6, `make harness-test` 2 failed / 2086 passed / 10 skipped (две красные предсуществуют, не трогать).

## §0 — прямые ответы, каждый с [KNOWN]/[ГИПОТЕЗА] и file:line

1. **Ворота** verbatim из ТВОИХ прогонов; расхождения назвать. Прирост тестов пересчитай сам.
2. **Может ли тест быть ЗЕЛЁНЫМ при сломанном свойстве?** Это главный вопрос. Разбери все три ассерта-премиссы (`defers > 0`, `dropped > 0`, `sites == [consensus, frontier]`) и скажи по каждому: что именно он исключает, и остаётся ли конфигурация, в которой он выполняется, а свойство при этом не проверяется. Мутации ставь сам.
3. **Мутация 2 исполнителя дала ОТРИЦАТЕЛЬНЫЙ результат** — `EpochSchemeProvider::scoped ⇒ None` оставил тест зелёным, то есть арм `verify_delivered ⇒ false` резолвера маршала в этой фикстуре не достигается. Подтверди или опровергни СВОИМ прогоном. И главное: **можно ли вообще построить стендовую фикстуру, достающую до этого арма?** Назови, что для этого нужно по коду (кто кормит резолвер маршала в стенде, есть ли путь, на котором ответ доходит до `verify_delivered` с нечитаемой эпохой), или скажи, что пути нет и почему. Это решает, закрывается ли остаток R-129 тестом в принципе.
4. **Что пин доказывает и чего НЕ доказывает.** Сформулируй границу сам, по коду: какое правило исполняется в этом прогоне (фронтирное или маршальское), и что произойдёт с тестом, если в ПРОДАКШНЕ `OuterBuilder.blocker` заменят на настоящий blocker. Если ответ «ничего», скажи это прямо.
5. **`BlockerSpy` как тип.** Считает и не блокирует — проверь, что он не меняет поведения стенда иначе (тайминги, порядок, `Clone`-семантика счётчиков при раздаче в два слота), и что `sites` собирается детерминированно.
6. **В-3, цена повтора**: 31 дроп за 159.916 с и ассерт `dropped <= plane_calls`. Проверь, что эта граница что-то запрещает, а не выполняется тождественно.
7. **Граница и гигиена**: продакшн действительно не тронут (`git status`), новых `pub` наружу нет, `#[allow]` нет, `unwrap`/`expect` на не-тестовом пути нет; правки в `tests.rs` — что это и зачем.
8. **Hard-stop**: требуется ли менять решение в `DECISIONS.md`; есть ли BLOCKER.
9. **Где твоя проверка была слабее всего.**

**§1 Находки** — таблица `id | серьёзность | file:lines | что не так | чем пытался опровергнуть | уверенность`; id `V-nn`. BLOCKER — тест, зелёный при сломанном свойстве; правка продакшна. **§2 Оставить как есть** — непустой. **§3 Вне рамок** — по строке.

В финальном ответе — §0 сжато и таблица §1.

<tone_preference>Кратко и по существу.</tone_preference>

# Строка 5.2, заход А — третий проход (правки по ревью `E5-2-A-REVIEW.md`)

Ты — исполнитель третьего прохода (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база `HEAD` = `37691136`. Объект — **незакоммиченное рабочее дерево** захода А (15 изменённых + `beacon/seed_index.rs` untracked + `beacon/certify.rs` удалён). Пути без префикса — от `crates/dpos/consensus/src/`.

## Правила

1. **Агентов не запускать.** Спека и код обязаны сидеть в одном контексте.
2. **Git только на чтение.** Не коммить, не индексировать, не `checkout`/`stash`/`restore`/`reset`.
3. Каждое утверждение в отчёте — `file:line`; для «было» — `HEAD:file:line`.
4. Комментарии и доки — не свидетельство. Свидетельство — код и прогон, который ты сделал сам.
5. Поиск — **только `git grep`** по `crates` (под `**/.claude/session-reads/` лежат gitignored журналы чужих сессий). Для `.claude/dpos_architecture/` нужен `grep -rn`, и пустой `git grep` там за отсутствие не принимать — каталог в `.gitignore`. **`.claude/dpos_architecture/` в этом проходе НЕ ПРАВИТЬ** — это отдельная фаза.
6. `CARGO_BUILD_JOBS=6`. Стенд не гонять параллельно с другой сборкой.
7. **Ворота — ТРИ прогона тестов, это условие приёма**, не формальность: `cargo test -p fluentbase-consensus --lib` (сейчас 659/0), `… --lib --features dpos-devnet-byzantine testbed::` (сейчас **55/0**), `… --lib testbed::` БЕЗ фичи (сейчас **46/0**). Плюс `cargo test -p fluentbase-node --lib` (55/0), `-p fluentbase-staking-reader` (64/0), `--test slasher_integration` (16/0), clippy БЕЗ фичи (ровно два ЧУЖИХ предупреждения: `MutexGuard held across an await` в staking-reader lib-test, `large size difference between variants` в node; в consensus ноль) И `cargo clippy -p fluentbase-consensus --all-targets --features dpos-devnet-byzantine` (ноль), `cargo fmt --check`, `cargo doc -p fluentbase-consensus --no-deps` (unresolved 6). **«`--lib` зелёный» доводом не будет**: в строке 5.1 это стоило трёх возвратов, потому что набор «стенд с фичей» не гонялся ни разу.

## Файлы на запись

`beacon/{seed_index.rs, surface.rs, plane.rs, follower.rs, seed_journal.rs, verified_seed.rs, artifact.rs, mod.rs, metrics.rs, actor.rs (ТОЛЬКО доккомментарии), oracle.rs}`, `spec_exec.rs`, `cert_inlet.rs`, `dpos.rs`, `executor.rs` (продакшн-тело — ТОЛЬКО доккомментарии; код — только `#[cfg(test)]`), `engine.rs` (ТОЛЬКО доккомментарий `:44`), `cold_start_jump.rs` (ТОЛЬКО доккомментарий `:774`), `lib.rs`, `testbed/{tests.rs, cert_inlet_tests.rs, preconditions.rs, stand.rs}`, и отчёт `.dpos-study/history/E5-2-A-FIX.md`.

**ЗАПРЕЩЕНО**: `committee/**`, `plane_upstream.rs`, `epoch_manager.rs`, `crates/node/**`, `crates/dpos/p2p/**`, `bins/**`, `devnet/**`, `contracts/**`, `Cargo.toml`, `.claude/**`, `testbed/{byzantine_roles.rs, committee_tests.rs, fakes.rs}`.

## Вход

- Ревью: `.dpos-study/history/E5-2-A-REVIEW.md` — читай целиком, это перечень находок `C-01…C-23`.
- Журнал захода: `.dpos-study/history/E5-2-A.md`.
- Строка плана `.dpos-study/PLAN.md:114`; проект `history/E5-BEACON-DESIGN.md` §5.1/§5.3/§5.4; `DECISIONS.md` П-2, Д-3, Д-9 (все «принято», менять их — hard-stop).

## Задание

Порядок ниже — порядок исполнения. Пункт 6 идёт ПОСЛЕДНИМ и только после зелёных ворот на 1-5.

### 1. C-01 (BLOCKER) — R-016 ОТКАТИТЬ. Решение оркестратора 5.2а-Д-5, не предмет обсуждения.

`dpos.rs:2494` и `:3257`: вернуть `crate::cold_start_jump::JUMP_THRESHOLD.min(interval)` на обоих путях. Новые доккомментарии «R-016: ABSOLUTE» снять; восстановить смысл прежних.

**Почему** (воспроизведено оркестратором, не гипотеза): `testbed/preconditions.rs:303-315` содержит меренный вывод прошлого этапа — «A gate at or above `2·interval` wedges an execution-stalled node PERMANENTLY after §5.2. Production cannot configure one: it computes `JUMP_THRESHOLD.min(interval)`». Оркестратор поставил мутацию `preconditions.rs:353` → `Some(JUMP_THRESHOLD)` и получил verbatim: `heights=[264, 264, 264, 95] jumps=[] marshal_fin=[…, Some(159.0)]`, `missing=[160…264]`, паника `preconditions.rs:411: node 3 did not climb out by repeated re-jumps: []`. Предусловие строки («σ-hold перестал зависеть от re-jump») выполнено, но НЕПОЛНО: re-jump нёс вторую работу — выход для узла с остановленным ИСПОЛНЕНИЕМ, — и строка 5.2 её ничем не заменила.

Заодно C-20: пройди по девяти сайтам дрейфа, названным в ревью (`dpos.rs:2477-2478`, `:3252-3254`; `executor.rs:663-671`; `cold_start_jump.rs:774`; `stand.rs:156`; `tests.rs:1343`, `:1591`; `preconditions.rs:304`, `:314`) и приведи каждый в соответствие с откатанным кодом. `executor.rs:663-671` ревью называет инвертированным — проверь и почини. C-21 (продакшн-формула без покрытия) откатом снимается; **скажи в отчёте, снимается ли она полностью, и если нет — что осталось**.

### 2. C-03 + C-04 + C-05 (три SERIOUS) — правило вытеснения должно различать состояние. Это ОДНА правка, делай её как одну.

Оркестратор открыл все три якоря сам, находки подтверждены:

- **C-03**: `HEAD:certify.rs:76-93` держал `seeds` и `quarantined` как ДВЕ карты, и `quarantine` (`HEAD:certify.rs:265-288`) бил `SEED_RETENTION` по своей. Вытеснение проверенной σ неподтверждённой было структурно невозможно. `evict` (`seed_index.rs:355-371`) состояние не различает — теперь возможно.
- **C-04**: `HEAD` пиновал терминал только из проверенного пути (`pin_terminal` зовётся из `certify.rs:203` внутри `insert(VerifiedSeed)` и `:178` — регидратация журнала; карантин не пинует никогда). `oldest_evictable` (`seed_index.rs:392-405`) читает только `round` и не смотрит в `Entry` — значит `Pending` выше настоящего терминала получает защиту, а `Verified`-терминал под ним вытесняется, и `boundary_base` (`epoch_manager.rs:163`) получает `None`.
- **C-05**: `HEAD:certify.rs:350-359` (`retain_quarantine_from`) имел док, называющий причину поэпошности: «one `PK_e` settles every round of its epoch at once, **so evicting by round would tear an epoch in half and leave a remainder that can never resolve meaningfully**». Новое правило рвёт эпоху пополам ровно так. Тест `quarantine_eviction_is_per_epoch` (`HEAD:certify.rs:714`) снят без замены.

Требования к правке: (а) защиту терминала получает только `Verified`; (б) `Pending` не может вытеснить `Verified` — раздельные бюджеты или приоритет вытеснения, выбери и назови цену; (в) `Pending` уходит ПОЭПОШНО, а не по одному раунду. **Не восстанавливай вторую карту** — строка требует одного владельца; неси свойства правилом. На каждое из трёх — тест с названным фальсификатором и однострочной мутацией, которая его красит.

### 3. C-06 (SERIOUS) — у follower-а поздней половины Д-3 нет вообще.

`grep -n 'fn faults\|report_fault\|DataFault' beacon/follower.rs` даёт пусто: `FollowerRandomness` наследует дефолт `surface.rs:603 → None`, а `settle_pending` (`follower.rs:332-334`) кладёт `refused` в `info!`. И это на классе, чей собственный док (`follower.rs:321-325`) говорит «the keyless window is the ORDINARY state… `Pending` is where most of it first lands». Дай ему тот же канал, что у плоскости, целиком внутри `follower.rs`/`surface.rs`. Тест: поздний отказ у follower-а доходит до потребителя.

### 4. C-10, C-11, C-07, C-13 — по одной правке каждая.

- **C-10**: `observe_cert` — пустой дефолт (`surface.rs:218`), а продакшн-вызов `cert_inlet.rs:758` стоит в РАЗРЕШЁННОМ файле. Отклонение 5.2а-Д-1 оправдывает метод НА ТРЕЙТЕ (держатель `epoch_manager.rs:1225` для `observe_epoch`, `byzantine_roles.rs:404` для `observe_cert` — оба вне списка), но не вызов пустого тела в продакшне. Вызов снять; доки на снятое поведение поправить в разрешённых файлах: `cert_inlet.rs:752-757`, `:2865-2869`, `dpos.rs:3735-3738` (ревью говорит, что там две ложные утверждения подряд — проверь), `stand.rs:354`, `:2673`, `cert_inlet_tests.rs:99`. Сайты в `crates/node/**` НЕ трогать — перечисли их в отчёте.
- **C-11**: у синхронного `Refused` нет счётчика вообще; на follower-е новая защёлка плюс отсутствие фолта дают одну строку на эпоху и ноль метрик. Утверждение задания «глушится только строка, счёт остаётся полным» не выполнено. Дай синхронному отказу счётчик (`beacon/metrics.rs` открыт на запись).
- **C-07**: поздний заряд ниже порога стирается тем же чистым сертификатом, потому что дренаж в голове `ingest` (`cert_inlet.rs:829`), а сброс серии — в хвосте (`:747`). Синхронный фолт переживает (`return` до сброса). Сделай их симметричными.
- **C-13**: на отравленном локе `admit` (`seed_index.rs:189-195`) σ дропается, а вердикт остаётся `Recorded` — вызывающий думает, что подал. На `HEAD` от вердикта ничего не зависело, теперь зависит crash-replay.

### 5. Мелкое: C-12, C-14, C-15, C-17, C-18, C-19, C-22, C-23.

По каждой — либо правка, либо строка «оставляю, потому что…» с file:line. C-23 (`beacon/actor.rs:1410`, `engine.rs:44` называют удалённый `beacon::certify`) — правка доккомментария, оба файла открыты на запись под это. C-16 (замечание про `fluentbase-slasher`) — только поправка в отчёте: ворота гоняют не пакет, а таргет `--test slasher_integration`, он есть и зелёный 16/0.

### 6. C-02 (SERIOUS) — ПОСЛЕДНИМ, и только на зелёных воротах по 1-5.

Строка плана требует «удалить … seed-половину и сам файл `follower.rs`». Ни то, ни другое не сделано: файл вырос `HEAD` 1042 → 1166, и в `impl Randomness for FollowerRandomness` живы `record_seed`, `hold_seed`, `first_seed_refusal`, `seed_for`, `settle_pending`.

Аргумент прошлого прохода (§5 журнала — «слияние меняет тип оракула») ревью опровергает кодом: `BeaconOracle::verify_seed` (`oracle.rs:218-227`) и `KeyOnlyOracle::verify_seed` (`:281-292`) — одинаковые тела, `with_material` (`oracle.rs:117-122`) на пустом ceremony даёт те же негативы, конструкторы пустых сторов уже есть. **Сначала проверь это опровержение сам** и скажи в отчёте, держится ли оно.

- Держится ⇒ слей: follower использует общую реализацию, второй `Randomness`-impl исчезает, файл удаляется.
- Не держится ⇒ **остановись на этом пункте**, оформи как нумерованное отклонение `5.2а-Д-n` с file:line, который блокирует, и назови цену. Не оставляй дерево слитым наполовину.
- Если слияние требует правки файла ВНЕ списка — то же: остановись, назови файл и строку.

`make smoke-cert-follow` не гоняй (`devnet/` держит другая сессия); приёмку берёт на себя оркестратор. Назови в отчёте, что именно в этом слиянии покрыто только девнетом.

## Отчёт `.dpos-study/history/E5-2-A-FIX.md`

**§0** — таблица `C-nn | FIXED / RECORDED / REJECTED | file:lines | что сделано или почему нет`. По **каждой** находке ревью, включая NIT. REJECTED требует свидетельства, что находка неверна, а не что она мелкая.

**§1** — по каждой FIXED на SERIOUS-или-выше: ханк, тест, который её ловит, и **однострочная мутация, которая его красит** (оркестратор воспроизведёт одну из них сам).

**§2** — ворота verbatim, все десять, с балансом счёта тестов по файлам.

**§3** — отклонения `5.2а-Д-n`, если появились.

**§4** — что осталось незакрытым и почему; отдельно — что покрыто только девнетом.

**§5** — где твоя работа слабее всего.

В финальном ответе — только §0 и §4.

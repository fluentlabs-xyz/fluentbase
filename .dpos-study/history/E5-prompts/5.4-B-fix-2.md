# Строка 5.4, заход Б — круг 2 (правка по dsh-ревью)

Ты — исполнитель (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `79dd18f9`. Пути без префикса — от `crates/dpos/consensus/src/`. Рабочее дерево = HEAD + незакоммиченный заход Б (9 файлов под `crates/`) — это объект. Чужое `.dpos-study/history/E4-ORCHESTRATOR.md` — не читать, не трогать.

## Входы (читать первыми, в этом порядке)

1. `.dpos-study/history/E5-prompts/5.4-B-impl-1.md` — постановка круга 1 (рамка, правила, ворота).
2. `.dpos-study/history/E5-4-B.md` — журнал круга 1 (§0.2 таблица переезда, §0.3 доказательство sweep, §0.4 защёлка, §0.9 инцидент per-tick).
3. `.dpos-study/history/E5-4-B-DSH-1.md` — ревью DeepSeek: D-01…D-13. Реляция, не факт: каждое утверждение сверяй с кодом. Что из него делать — ниже; остальное не делать.

## Решения оркестратора (проверены по коду)

- **D-02 — принят как регрессия политики халта, правится в этом круге.** У HEAD менеджер abort-ил инстансы согласования НЕМЕДЛЕННО с ребра латча (`epoch_manager.rs:604-619`, `engaged_edge` = `Notify::notify_one`, `sync_metrics.rs:536-541`, `:607-609`). В дереве abort приходит только с ребра часов актора (`dkg_engine.rs:905-917`), а часы тикают только с `heights.recv()` (`actor.rs:1257-1259`). Утверждение dsh «после халта тиков нет вообще» НЕ подтверждено (ordering tip верифицирующего узла продолжает идти до потолка `last(epoch(EL_fin − K) + 2)` — `06_staking_layer.md:582`), но задержка «до следующего финализированного блока» вместо «сейчас» — регрессия относительно HEAD, и окно `start_one` (четыре `register_dkg_subchannel(...).await` до спавна, `:965-983`) — тоже. Правило халта — «stop signing NOW» (`13_…md`, halt policy).
- **D-01 — НЕ правится в этом круге.** Опережение через tee `cert_inlet.rs:781-785` (tee до `marshal.verify_block :805`) реально только с устаревшим `--dpos.follower-upstream` на валидаторе и пропущенным граничным сертом; tee целиком уходит в заходе 5.4-А (один клок = `ordering_tip`). Записать как отклонение Д-5.4Б-3 в журнале с условием закрытия «5.4-А». Код не трогать. D-06/D-12/D-13 — та же судьба (упомянуть в Д-5.4Б-3 одной строкой).
- **D-05 — правится**: `spawn_agreement_launcher` (реальная задача с `select!`) не покрыт ни одним тестом.
- **D-03 — правится** попутно (retain на пути отказа).
- D-04, D-07…D-10 — не правятся (D-07 исчезает вместе с `halted`, см. ниже); D-11 — среда dsh (`.claude/` в gitignore, в worktree его нет), доки уже правлены в круге 1 — их надо ОБНОВИТЬ под новый механизм.

## Что сделать

1. **Латч становится многоадресным ребром без потери.** В `sync_metrics.rs` заменить `notify: Arc<Notify>` на `tokio::sync::watch` (`Arc<watch::Sender<bool>>` или `watch::Sender<bool>`, tokio 1.52 — `Sender: Clone`); `latch()` → `send_if_modified` (0→1 ровно один раз); `engaged_edge()` → `subscribe()` + `wait_for(|e| *e)` — резолвится немедленно, если латч уже взведён (свойство теста `safety_halt_edge_fires_on_first_engage :816-826` — сохранить, тест зелёный), и для ЛЮБОГО числа ждущих. `is_engaged()` — как был (атомик оставить либо читать `borrow()`; выбери одно, обоснуй). `#[derive(Default)]` на `SafetyHalt` (`:431`) — `watch::Sender` не `Default`: ручной `impl Default`. Доккомментарии `:409`, `:605-606`, `actor.rs:826` — переписать под новый механизм (слово `notify_one` не должно остаться нигде: `git grep notify_one -- crates`).
   **Ловушка**: `wait_for` на уже-истинном значении резолвится на КАЖДОМ вызове. Оба цикла, где ребро стоит в `select!` (`epoch_manager.rs:604-606` и лаунчер), обязаны обезоружить арм после первого срабатывания (`_ = &mut halt_n, if !halt_seen`), иначе цикл крутится вхолостую. Тест на это — обязателен (см. п. 4).
2. **Лаунчер ждёт ребро сам.** В `spawn_agreement_launcher` третий арм `select!`: `safety_halt.engaged_edge()` → abort + JOIN всех живых (перенос тела `on_tick :905-917`), затем арм обезоружен. `on_tick(cutoff)` остаётся только прунингом. `on_request`: проверка `is_engaged()` до спавна остаётся; после `start_one` (после четырёх `await`) — если латч взведён, немедленно abort+join только что запущенного (закрывает окно `:965-983`), либо покажи, что ребро-арм это окно уже закрывает без гонки (тогда объясни почему в §0). `started.retain(floor)` — и на пути отказа (D-03).
3. **Часы актора — только эпоха.** `AgreementClock { epoch, halted }` → `watch<u64>` (или структура с одним полем — выбери проще); `Wiring.safety_halt` (`actor.rs:830`) и чтение латча в `on_height :2364-2376` — удалить: актор латч не читает. `send_if_modified` при смене `now` остаётся (per-tick публикации нет — §0.9 журнала, правило 45). `ValidatorInputs.safety_halt` остаётся (лаунчер). Тест `the_agreement_clock_moves_on_the_epoch_edge_and_on_the_halt_and_never_per_tick` → переименовать/переписать под «ребро эпохи и никогда per tick».
4. **Тесты** (в `dkg_engine::tests` и `sync_metrics::tests`; deterministic runtime, никаких реальных sleep):
   - через РЕАЛЬНУЮ задачу `spawn_agreement_launcher`: запрос → инстанс в карте; ребро часов → прунинг; закрытие `requests` → задача выходит и инстансы abort-нуты (D-05);
   - халт с ребра латча БЕЗ единого тика часов: инстанс abort-нут и join-ен, задача жива и дальше отвечает на запросы отказом (это и есть «не крутится вхолостую»: после халта задача обязана ещё принять запрос и ответить отказом, а не висеть в горячем цикле — покажи это утверждением, не описанием);
   - латч взведён ДО постройки лаунчера → первый запрос отказан, ни одного спавна;
   - `sync_metrics`: два ждущих `engaged_edge()` оба резолвятся с одного `engage`; ждущий, вставший ПОСЛЕ `engage`, резолвится немедленно;
   - `epoch_manager`: существующий тест halt-арма менеджера (найди его: `git grep -n "engaged" crates/dpos/consensus/src/epoch_manager.rs`) остаётся зелёным; если его нет — не сочинять.
5. **Доки в том же изменении**: `.claude/dpos_architecture/{13_invariants_gotchas_rules,00_preamble,03_epoch_machinery,08_…}.md` — механизм латча (`watch`, многоадресное ребро, кто ждёт: менеджер + лаунчер), `AgreementClock` без `halted`, `Wiring` без `safety_halt`; `grep -rn "notify_one\|AgreementClock.*halted\|Wiring.safety_halt" .claude/dpos_architecture` — пусто; CHANGELOG — одна запись круга 2; `verified-against` в `00_preamble.md`.

## Правила

- **Агентов не запускать** — ни для работы, ни для проверки себя: спека, ревью и код должны сидеть в одном контексте. Git — только чтение (`git diff`, `git show`, `git grep`); никаких `git add/commit/stash/checkout/restore`.
- Записываемые файлы: `sync_metrics.rs`, `epoch_manager.rs`, `beacon/{dkg_engine,actor,plane}.rs` (+ `dpos.rs`, `testbed/stand.rs`, `crates/node/src/dpos.rs` ТОЛЬКО если сборка требует из-за снятого `Wiring.safety_halt`), их тесты, `.claude/dpos_architecture/*.md`, CHANGELOG, журнал `.dpos-study/history/E5-4-B.md` (добавить раздел «## Круг 2» — не переписывать §0 круга 1, кроме исправления таблиц, которые круг 2 делает ложными; такие правки пометить «[круг 2]»). Остальное — только чтение.
- Не переформатировать нетронутое; никаких `#[allow]`, новых `unwrap`/`expect` на продакшн-пути, новых `pub` наружу. `CARGO_BUILD_JOBS=6`; стенд не гонять параллельно с другим cargo.
- Каждое утверждение в журнале — с `file:line`, который ты открыл. [KNOWN] только по прочитанному/прогнанному в этой сессии.
- **СТОП** (запись в журнал, не обход): для правки нужен файл вне списка; `DECISIONS.md`; стенд краснеет по живости; менеджеру нужна семантика латча сверх «то же ребро, но многоадресное».

## Ворота (все, verbatim; база — дерево круга 1)

`--lib` 727/0 · стенд с фичей 58/0 (678 filtered) · без фичи входит в `--lib` (`testbed::` 52 строки) · node 57/0 · staking-reader 64/0 · slasher 16/0 · clippy три крейта — два чужих (`node/src/dpos.rs:2007`, `epoch_transition.rs:3024`), ноль своих · consensus с фичей 0 · fmt 0 · doc unresolved 6 · harness 2 failed / 2086 / 10 (те же две). Прирост тестов — объяснить по именам.

**Мутации (с откатом и md5):** (M4) арм латча в лаунчере снят — тест «халт без тика» красный; (M5) обезоруживание арма снято (`if !halt_seen` убран) — тест «после халта ещё отвечает на запрос» красный или зависает по таймауту рантайма (укажи что именно); (M6) `send_if_modified` в `latch()` заменён на `send_replace` — тест «0→1 ровно один раз» красный (если такого свойства нет у тестов — напиши, что мутация зелёная и почему это приемлемо, не сочиняй тест ради мутации).

## Журнал — раздел «## Круг 2»

1. Ворота verbatim. 2. Что изменено → file:line (таблица). 3. Латч: старый и новый механизм, кто ждёт, почему нет потери и нет горячего цикла — с file:line. 4. Окно `start_one` — закрыто чем. 5. Д-5.4Б-3 (D-01/D-06/D-12/D-13 → 5.4-А). 6. Мутации verbatim. 7. Что из D-03…D-13 сделано / не сделано и почему. 8. Слабее всего — ранжируй. 9. Вне рамки.

Заверши ответ кратко (≤ 25 строк): ворота, что изменено, мутации, слабее всего.

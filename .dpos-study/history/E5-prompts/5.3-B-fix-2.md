Второй круг захода Б строки 5.3 — после dsh-ревью круга 1 (`.dpos-study/history/E5-3-B-DSH-1.md`, D-01…D-18, ни одного BLOCKER/SERIOUS). Те же правила: только файлы постановки (`E5-prompts/5.3-B-impl-1.md`) плюс `artifact.rs`/`plane.rs`, которые ты уже тронул; продакшн вне них не трогать; git только чтение; агентов не запускать (спека и код — в одном контексте; себя агентом не проверять); каждое утверждение — file:line. Все находки dsh — реляция; перед правкой проверь по коду, REJECTED допустим с file:line.

Что я проверил сам: D-03 — `attempts` (`artifact.rs:1674`) пишется в `minter_to_ask` `:1720-1726`, снимается только на `Have` `:1775`; `next_allowed` чистится `:1819`; ранний локальный хит `:1751-1753` карту не трогает — подтверждено. D-15 — `note_equivocation` (`actor.rs:2145`) вызывается ДО `append_journal` `:2146` — подтверждено. D-06 — артефакт метода (`.claude/` не в worktree dsh), не дефект: доки на месте в рабочем дереве, ревью их проверит Opus.

ПРАВИТЬ:
- D-03: чистить `attempts[epoch]` там же, где `next_allowed` (`:1819`), и на раннем локальном хите; либо один `HashMap<u64, PullState{next_allowed, cursor}>` — одна карта, одна очистка.
- D-07: пара evidence должна жить до sweep, не до finalize: поле актора `equivocations: BTreeMap<u64 /*epoch*/, BTreeMap<PeerPubkey, DealerEquivocation>>`, заполняется из `Step.equivocation` (и при replay), снимается в `sweep_epoch_state` вместе с остальными картами; читатель — тест + будущий заход А (`Conflict`). Не дублировать тела — только пара хэшей + дилер.
- D-15: порядок — сначала `append_journal`, потом `note_equivocation` (WARN + счётчик только для записанной пары); если запись не легла — та же WARN с пометкой `nondurable`, счётчик один раз.
- D-14: `seal_dealings` `ceremony.rs:667-674` — не отбрасывать `Inserted::Equivocation`; собственная эквивокация невозможна по построению (дилер детерминирован от seed) — сделай это явным: `debug_assert!`/`unreachable`-ветка с объяснением, не `let _`.
- D-16: `scoped_pinned_logs` `:1049-1058` — сверять `cpk == pk`, при расхождении — не записывать и считать это повреждением карты (ошибка, не молчание).
- D-17: второй приёмочный тест — `dpos_dkg_dealer_equivocation_total == 0` на узлах 1–3, как в первом.
- D-13: комментарий про `publish_recorded_logs` «never overwrites» (`actor.rs:1505`) — привести к коду (`or_default().insert(idx, hash)` перезаписывает) или сделать код таким, как комментарий, если по смыслу должен быть first-wins по seat; обоснуй выбор по `confirmations.rs:16-21`.
- D-05, D-08: устаревшие/завышенные доккомментарии (`metrics.rs:56`, `ceremony.rs:1033`, `:1467-1470`, `confirmations.rs:37`, `share_state.rs:377-385`, `:492-498`, `ceremony.rs:898-905`) — по коду.
- D-10: `Player::resume` берёт first-recorded тело дилера (`ceremony.rs:868,893,901`), которое может быть не пришпиленным: если на момент resume пришпиленный набор известен (`agreed_pinned`), предпочитать пришпиленный хэш, иначе first. Если это тянет за собой больше 30 строк — НЕ делать, записать как остаток для А с точным описанием.

ЗАПИСАТЬ (в журнал, раздел «Второй круг»): D-01 и D-02 — мои Д-nn (отклонения от постановки, приняты оркестратором: тест остановки меняет вердикт по факту; `ArtifactPull` — корневая правка); D-04 (латентность ротации `PULL_TIMEOUT × |committee|` — граница, не дефект; назвать число для n=4 и n=100); D-09 (стенд не пришпиливает путь `fetch_missing_logs` — юнит `a_held_body_…` его держит, стенд лечится и через `fetch_bodies`); D-11; D-12; D-18.

Ворота после правок — все одиннадцать verbatim, база из твоего первого круга (`--lib` 679/0 и т.д.). Мутация для D-07: убрать очистку в sweep — тест на «пара живёт до sweep и исчезает после» красный. Журнал `E5-3-B.md` — раздел «Второй круг»: таблица D → решение → file:line → чем доказано; md5 всех тронутых файлов на выходе. Ответ — таблица и ворота, без пересказа.

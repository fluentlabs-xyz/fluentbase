# Финальное ревью захода Б строки 5.4 — бикон владеет инстансами согласования и партициями; латч SafetyHalt многоадресный (Opus, свежий контекст)

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база `HEAD` = `79dd18f9`. Объект — незакоммиченное рабочее дерево: 10 файлов под `crates/` (`git diff HEAD --stat -- crates`). Пути без префикса — от `crates/dpos/consensus/src/`. Чужое `.dpos-study/history/E4-ORCHESTRATOR.md` — не читать.

Это ФИНАЛЬНЫЙ круг: два круга исполнителей (свежие агенты; журнал `history/E5-4-B.md` — §0 круга 1 и «## Круг 2»), два dsh-ревью (`history/E5-4-B-DSH-1.md` D-01…D-13; `history/E5-4-B-DSH-2.md` E-nn — если файла ещё нет, значит второе dsh не завершилось: ревьюй без него и скажи об этом). Постановки — `history/E5-prompts/5.4-B-{impl-1,fix-2}.md`. Карта — `history/E5-ORCHESTRATOR.md`, «Карта строки 5.4»; дизайн — `history/E5-BEACON-DESIGN.md` §5.3.

## Установлено оркестратором (опирайся; опровергать — с file:line)
- Заход: лаунчер (`beacon/dkg_engine.rs`) владеет инстансами (`Launcher.instances`), `prune_agreements` + `AGREEMENT_SWEEP_SPAN` переехали из `epoch_manager.rs` дословно (мой `diff` тел: два слова в комментариях); канал `agreement_intake` и вся его проводка (manager/plane/outer/dpos/node/stand) удалены; часы лаунчера — `watch<u64>` эпохи актора, публикуемые `send_if_modified` только при смене эпохи; cutoff `E` наступает на `first(E)`, у HEAD — на `last(E−1)` (+1 высота; доказательство §0.3 журнала подтверждено мной для фидеров tip/poller; для tee — отклонение Д-5.4Б-3, ниже).
- Круг 2 (по D-02 dsh, принято мной как регрессия политики халта): `SafetyHalt` (`sync_metrics.rs`) хранит бит и ребро в одном `watch::Sender<bool>` (`latch` = `send_if_modified` 0→1; `is_engaged` = `borrow()`; `engaged_edge` = `subscribe()+wait_for`); менеджер (`epoch_manager.rs` ~`:601-622`) и лаунчер (~`:757-783`) армят ребро один раз и обезоруживают `if !halt_seen`; лаунчер `on_halt` = abort+join всех инстансов немедленно; после `start_one → Running` латч перечитывается (окно четырёх `await`); `on_tick` — только прунинг; `AgreementClock` и `Wiring.safety_halt` удалены; `#[cfg(test)]` probe в цикле лаунчера; +5 тестов.
- Принятые отклонения: Д-5.4Б-1 (халт при `on_request` = не спавнить, вместо «принять и abort-нуть»), Д-5.4Б-2 (follower латча не получает — у него нет плоскости согласования), Д-5.4Б-3 (опережение sweep через tee `cert_inlet.rs:781-785` при устаревшем `--dpos.follower-upstream` на валидаторе + пропущенном граничном серте — уходит вместе с tee в 5.4-А; D-01/D-06/D-12/D-13 dsh). Не переоткрывать как BLOCKER; сказать, если круг 2 сделал хуже.
- Ворота — МОИ, уже прогнаны на этом дереве (тег `w2`): логи `/tmp/claude-1000/-home-djadjka-Work-fluentbase/f6f42c91-6188-4d9c-ae64-a1e9faed9028/scratchpad/gates/w2-*.txt`, статус `w2-status.txt` (строка `DONE` в конце), md5-снимок дерева `w2.md5`. **Cargo не гоняй** (ни тесты, ни clippy — дерево общее, ворота мои); `md5sum -c w2.md5` — можно, чтобы убедиться, что читаешь то дерево, на котором они прогнаны. Единственное исключение — мутации (п. 5 ниже): точечный `cargo test -p fluentbase-consensus --lib <module>` с откатом и md5 до/после, `CARGO_BUILD_JOBS=12`, не более трёх.

## Правила
1. Агентов не запускать — ни для работы, ни для проверки себя: спека, ревью и код должны сидеть в одном контексте. 2. Git только на чтение; единственный файл на запись — `.dpos-study/history/E5-4-B-REVIEW.md`; `E4-ORCHESTRATOR.md` не читать. 3. file:line на всё; доки, журналы и dsh-отчёты — не свидетельство. 4. Полнота, не фильтр: всё, от BLOCKER до NIT, с уверенностью. 5. Не расширяй в редизайн; существенное вне рамки — одной строкой в конце.

## Вход
Дифф `git diff HEAD -- crates` первым, потом журнал, потом dsh-отчёты.

## §0 — прямые ответы ([KNOWN]/[ГИПОТЕЗА], file:line)
1. Ворота: прочитай мои логи `w2-*` и выпиши числа verbatim (ожидаю `--lib` 732/0, стенд с фичей 58/0, node 57/0, reader 64/0, slasher 16/0, clippy два чужих / ноль своих, с фичей 0, fmt 0, doc unresolved 6); прирост тестов 725 → 732 объясни по именам (`git diff` по `fn ` в тестовых модулях).
2. **Перенос как есть**: `prune_agreements` — тело против HEAD; `on_tick` только прунинг; momenты sweep — согласен ли с «+1 высота» для фидеров tip/poller; что с частотой (HEAD per reconcile, новое per ребро эпохи) — есть ли инстанс/партиция, доживающая дольше, чем на HEAD, более чем на эпоху.
3. **Латч**: все свойства HEAD `SafetyHalt` сохранены (идемпотентность, первый reason, синхронный restore из маркера, gauge); `borrow()` нигде не держится через `await`; клоны делят один канал; оба цикла не крутятся вхолостую и не поллят завершённый future (семантика precondition в `tokio::select!` — из исходника tokio в `~/.cargo/registry/src/`); `engaged_edge` при взведённом до постройки латче; окно `start_one` закрыто перечитыванием — инстанс в карте на момент перечитывания?; после `on_halt` новый запрос не спавнит; все выходы лаунчера abort-ят инстансы.
4. **Проводка**: ни одного остатка `agreement_intake`/`AgreementClock`/`Wiring.safety_halt`/`Notify`/`AtomicBool` в коде (`git grep`); `SyncMetrics::register` — ровно один вызов на процесс, тот же `ctx`; `SharedBeaconPlane.{sync_metrics, safety_halt}` — один экземпляр до исполнителя, менеджера, супервизора и лаунчера; follower-путь не изменён; стенд: `ValidatorInputs.safety_halt` — тот же латч, что у исполнителя стенда?
5. **Тесты и мутации**: пять новых тестов — может ли какой-то быть зелёным при сломанном свойстве (`runtime timeout` как режим падения — ограничен ли он виртуальным временем); `#[cfg(test)]` probe — не попадает ли в feature-сборку стенда (как гейтится `testbed`); перенесённые три теста — клетка в клетку против HEAD. Мутации (до трёх, с откатом и md5): выбери сам среди M4 (арм латча в лаунчере снят), M7 (перечитывание после спавна снято), M1+M1′ (пропуск живых + граница band-а) — воспроизведи verbatim.
6. **Гигиена**: `#[allow]` (кроме бывшего `too_many_arguments`), новые `unwrap`/`expect` вне тестов, новые `pub` наружу `beacon/`, `cfg(test)`-поля в продакшн-структурах, мёртвый код, уровни логов.
7. **Доки** `.claude/dpos_architecture/{00,03,08,09,13}.md` + `DPOS_ARCHITECTURE_CHANGELOG.md` — по коду: якоря `fn`/строки существуют (`grep -n`), `verified-against` в `00_preamble.md` обновлён; `grep -rn "agreement_intake\|AgreementClock\|notify_one\|Wiring.safety_halt" .claude/dpos_architecture` — только исторические записи CHANGELOG.
8. Hard-stop: требует ли что-то правки `DECISIONS.md`; BLOCKER, на который дизайн не отвечает.
9. **Вердикт: КОММИТИТЬ / НЕ КОММИТИТЬ**; что не прочитал.
10. Слабее всего — ранжируй.

**§1 Находки** `id | серьёзность | file:lines | что не так | чем пытался опровергнуть | уверенность`, `F-nn`. BLOCKER — потерянное ребро латча; паника (`resumed after completion`, `borrow` через `await`); инстанс, живущий под взведённым латчем дольше одной итерации своей задачи; партиция, снесённая раньше HEAD не через tee; свойство `SafetyHalt` с HEAD потеряно; продакшн-поведение против HEAD вне названного переноса и правки. Затем непустое «Оставить как есть».

В финальном ответе — §0 сжато (вердикт, ворота, непрочитанное) и таблица §1.

<tone_preference>Кратко и по существу.</tone_preference>

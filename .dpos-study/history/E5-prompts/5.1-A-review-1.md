# Ревью строки 5.1 (факт `PK_E`: артефакт — единственный владелец; Д-7; удаление `keys.rs`/`carry.rs`/`key_journal.rs`/`resolve.rs`)

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база `HEAD` = `3ead5c6c` (`git rev-parse HEAD`; строка 5.0а закрыта коммитами `7790d1bc` + `3ead5c6c`). Объект — **незакоммиченное рабочее дерево** строки 5.1: `git status --short -- crates` = четыре удаления (`beacon/{carry,key_journal,keys,resolve}.rs`) и ~20 изменённых. **Чужое в `git status`** — `.dpos-study/history/E4-ORCHESTRATOR.md` — не трогать. Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`; `beacon/` = `crates/dpos/consensus/src/beacon/`.

## Правила

1. **Агентов не запускать.** Спека и код обязаны сидеть в одном контексте.
2. **Git только на чтение.** Единственный файл на запись — `.dpos-study/history/E5-1-A-REVIEW.md`. Мутации продакшн-кода ради чувствительности тестов — только с откатом и `md5sum` до/после.
3. Каждое утверждение — `file:line` дерева и, где есть «до», `HEAD:file:line` (`git show HEAD:crates/…`).
4. Комментарии, доки, `.claude/dpos_architecture/` и журнал исполнителя — **не свидетельство**; свидетельство — код и прогон, который ты сделал сам. Для commonware — `.claude/COMMONWARE_INTERNALS.md` ДО чекаута.
5. **Полнота, не фильтр**: все находки, включая мелкие и неуверенные, с серьёзностью (BLOCKER / SERIOUS / MODERATE / MINOR / NIT) и «чем пытался опровергнуть»; не объединять разные находки ради краткости — фильтрует оркестратор.
6. Читать целыми строками (`Read` / `sed -n 'N,Mp'`); никогда `cut -c`/`head -c`. Поиск — **ТОЛЬКО `git grep`**: под `crates/dpos/consensus/src/.claude/session-reads/` лежат gitignored журналы чужих сессий.
7. `CARGO_BUILD_JOBS=6`. Стенд не гонять параллельно с другой сборкой. Если классификатор среды запретит тебе `cargo test` — скажи это в §0(1) прямо и опирайся на файлы ворот оркестратора; молча не подменяй прогон чтением.

## Вход

- **Строка плана**: `.dpos-study/PLAN.md`, блок «### Э5.», строка `5.1`.
- **Проект**: `history/E5-BEACON-DESIGN.md` §3 (`:443-455`), §5.1 (`:556-595`), §5.2 (`:596-617`), §5.3 (`:618-628`), §5.4 (`:629-646`), §5.5 (`:647-649`), §5.6 (`:650-666`), §7 строка 5.1 (`:711-733`).
- **Решения**: `DECISIONS.md` П-3 (`:86`), Д-7 (`:44-47`) — оба «принято». Отклонение от них — hard-stop этапа; если найдёшь такое, это BLOCKER.
- **Постановка**: `history/E5-prompts/5.1-A-impl-1.md`; плюс ДВА возврата оркестратора, чьи решения О-2/О-3/О-4 пересказаны в журнале (части Б и В).
- **Журнал**: `history/E5-1-A.md` — части А, Б, В, Г; читать **ПОСЛЕ** диффа. Часть Г — третий возврат: три репина feature-gated тестов и починка `Stand::partition` (разрез теперь запоминает снятые пары и залечивание восстанавливает ровно их — оркестратор ханк открыл).
- **Ворота**: набор прогнан оркестратором, четыре прогона. **`b4-*` — текущее дерево и ЗЕЛЁНЫЙ набор**, на него и опирайся (`b4-status.txt` — коды выхода, `b4-tree.md5` — снимок дерева, сверь `md5sum -c`). Прогоны `b1-*`, `b2-lib.txt`, `b3-*` — история трёх возвратов на красных воротах: `b1` = 685/6 (разбиение заходов было неверным), `b2-lib` = 643/10 (репины О-4 не сделаны), `b3` = стенд С ФИЧЕЙ 52/3 при зелёном `--lib` (исполнитель не гонял вторую конфигурацию). Все в `/tmp/claude-1000/-home-djadjka-Work-fluentbase/f6f42c91-6188-4d9c-ae64-a1e9faed9028/scratchpad/gates/`. То, что строка прошла ТРИ возврата, — вход для твоего §0(13): смотри, не осталось ли следов подгонки под зелёное. База на `3ead5c6c`: lib 692/0, стенд с фичей 55/0, без фичи 46/0, node 55/0, reader 64/0, slasher 16/0, clippy ровно два ЧУЖИХ предупреждения, с фичей 0, `fmt --check` чисто, doc unresolved 6.

## Форма результата `.dpos-study/history/E5-1-A-REVIEW.md` (§0–§6)

**§0 Прямые ответы** — нумерованные, каждый с [KNOWN]/[ГИПОТЕЗА] и file:line:

1. **Ворота** verbatim из файлов `b3`; расхождения с числами журнала назвать.
   **Счёт тестов — и это отдельный вопрос, а не формальность.** База `3ead5c6c` даёт lib 692/0, дерево — 654/0, то есть **минус 38**. Оркестратор посчитал дельту по файлам сам, сходится ровно: `actor.rs` +1, `artifact.rs` +1, `carry.rs` −13, `key_journal.rs` −3, `keys.rs` −14, `oracle.rs` −2, `resolve.rs` −4, `share_state.rs` −2, `surface.rs` −2. Тридцать четыре ушли вместе с четырьмя удалёнными файлами; **шесть ушли из файлов, которые ОСТАЛИСЬ** — `oracle.rs` (2), `share_state.rs` (2), `surface.rs` (2). По каждому из этих ШЕСТИ назови по `HEAD:`-телу: как назывался, что доказывал, и что стало со свойством — ушло классом вместе с удалённым кодом, переехало в другой тест (назови его), или осталось БЕЗ теста. Последнее — находка, и её серьёзность зависит от свойства. То же и для тридцати четырёх, но там достаточно классификации группами; шесть — поштучно.
2. **П-3 выполнен или нет, по пунктам.** П-3 (`DECISIONS.md:86`) требует: `BeaconKeys = epoch → pk` только из `ArtifactStore`; `key_epoch(E)` по записям П-1 с двумя бутстрап-краями; удалить `key_journal.rs`, `carry.rs`, W1/W3, `Carried`; `validate_share_on_poly` перед любым `adopt_share`; один ретеншн. По каждому: сделано / сделано иначе (какое Д и держится ли причина по коду) / не сделано.
3. **Единственный владелец — проверь, что он ЕДИНСТВЕННЫЙ.** Проект §5.3 и §0.1 говорят: после Э5 носителей `PK_E` остаётся ноль вне beacon (кроме `Arc<dyn SeedOracle>` в схемах по замыслу). Перечисли по `git grep`, что в дереве хранит или кэширует `PK_E`/полином/`Sharing`/минт-эпоху, и скажи, сколько владельцев осталось ФАКТИЧЕСКИ. Особое внимание: `MintIndex`/`KeyIndex`/`ChangedAt`/долговечное мемо — это один владелец с несколькими проекциями или несколько владельцев? Ответь по коду, не по названию.
4. **Долговечное мемо (решение О-3 оркестратора).** Оно введено как замена `key_journal.rs`. Проверь: (а) оба правила мемо сохранены — `None` никогда не мемоизируется и запись write-once (`HEAD:beacon/carry.rs:83-95`, `:110-157` — что было); (б) что именно попадает на диск и может ли туда попасть НЕрешённый или РАСХОДЯЩИЙСЯ ответ; (в) что происходит при порванной записи мемо на старте (аналог `Torn` у других журналов) — назван ли исход; (г) закрывает ли мемо ту щель, которую `HEAD:beacon/key_journal.rs:9-18` объявлял своей, и что из неё остаётся. Остаток «свежий datadir + минт старше `SCHEME_RETENTION_EPOCHS`» оркестратор уже разобрал и записал как остаток I4, существовавший ДО строки — не перепроверяй его существование, но скажи, СУЖЕН он строкой или нет.
5. **Продакшн-регрессия, которую нашёл исполнитель, и её правка.** `material()` (`surface.rs:2078-2086`) отдаёт `Some((sharing, None, ns))` узлу с артефактом и без доли; гейты `share_probe` (`:2148`, `signable` `:2167-2169`) и `signer_scheme` (`:2216`, `Withheld(NoUsableShare)`) переведены на проверку ДОЛИ. Оркестратор оба ханка открыл. Твоя задача — не подтвердить их, а найти ТРЕТИЙ такой сайт: перечисли по `git grep` ВСЕ места дерева, где решение зависит от `material(..)`/`sharing_at(..)`/`Option<Share>`, и по каждому скажи, проверяет оно полином или долю, и верно ли это для его вопроса. `promote_gates` и `oracle_at`/`BeaconOracle::with_material` — обязательно.
6. **Д-7.** `maybe_start` теперь решает по биту `changed`. Проверь: (а) бит читается с якоря модуля и нигде больше; (б) что стало с ростерной парой и стражем `!(bit || committed)` — удалены или переехали; (в) свойство, которое страж несёт сегодня («неопределённый бит не мемоизируется»), — чем оно несётся теперь; (г) `ChangedAt` без ноги `committed` — может ли `changed == false` при незакоммиченной эпохе быть прочитан как «комитет не менялся», и что тогда произойдёт.
7. **Пред-beacon эпоха.** `HEAD:beacon/surface.rs:2484-2490` предупреждает: оракул, привязанный к пред-beacon эпохе, отвергает КАЖДЫЙ легальный seedless-сертификат этой эпохи, и сегодня это свойство несёт ветвь `Some(None)` из `carry.rs`. Ветвь удалена. Проверь по коду, что свойство несёт явный `mandatory_at`, и назови тест, который упадёт, если это перестанет держаться (исполнитель называет `a_beacon_active_epoch_with_no_material_is_withheld_and_never_signs_seedless` — проверь, действительно ли он это пинует, или только как предпосылку).
8. **Десять репинов (решение О-4).** По каждому из десяти: что тест доказывал на `HEAD`, что доказывает в дереве, чем сделано отставание, и — главное — **не ослаблен ли его фальсификатор**. Отдельно: `CutPlanes::ConsensusOnly` и `heal_above(height)` добавлены в `StandConfig`/стенд — это новая поверхность стенда; проверь, что она не делает вакуумным ни один из репиненных тестов (в частности, что «разрез сработал» и «залечился» пиновано, а не предполагается). И скажи, не потерял ли смысл `four_nodes_at_four_heights_hold_one_committee_record_per_epoch` при переходе на физический разрез.
9. **Слияние ретенции 1 → 8.** Поведенческое изменение: окно recompute-heal расширяется. Проверь: (а) направление действительно безопасно по коду, а не только по доку константы; (б) цена диска — пересчитай сам и сверь с журналом (`≈ +3.0 MB` на узел заявлено); (в) четыре пересчитанных теста — выводят числа из константы или из литералов; (г) есть ли в дереве место, где `SCHEME_RETENTION_EPOCHS` теперь означает ДВЕ разные вещи (окно чтения комитета и окно heal) и может разъехаться.
10. **Воссозданный тест** `beacon::artifact::tests::a_stable_committees_mint_outlives_the_retention_window`. Исполнитель приводит перечень из четырёх свойств старого теста (`HEAD:beacon/resolve.rs:304`) и утверждает, что два ушли классом, одно несёт новый тест, одно уже запинено юнитом. Проверь перечень по `HEAD:`-телу старого теста: полон ли он, и правда ли каждое «ушло классом».
11. **Граница и гигиена.** Что вышло из `beacon/` наружу и что вошло: `pub use`/`pub(crate) use` в `mod.rs` — `HEAD` против дерева, имя за именем. Новые `pub`, `#[allow]`, `todo!`, таймеры вместо событий, поллинг, `unwrap` на продакшн-пути. Правки вне списка: продакшн-тела `epoch_manager.rs`/`cert_inlet.rs`/`dpos.rs` обязаны быть НЕ тронуты (оркестратор проверил по номерам строк — проверь семантически: нет ли правки, которая формально в тестовом модуле, а меняет продакшн-поведение через фикстуру).
12. **Дрейф `.claude/dpos_architecture/`**: по `git grep` найди все секции, называющие удалённые символы (`BeaconKeys`, `AgreedKeys`, `KeySources`, `chain_key_epoch`, `frozen_dkg_qual`, `DkgQualFor`, `select_carry_scheme`, `beacon_share_resolver`, `mint_diverges_from_attested`, `BeaconResolve`, `CommitteePairFor`, `for_keys`, `InvalidSeed`, `KeySource`, `carry.rs`, `keys.rs`, `key_journal.rs`, `resolve.rs`, W1, W3, `JOURNAL_RETENTION_EPOCHS`) — список с `file:line`; править НЕ надо.
13. **Где журнал вводит в заблуждение** — по пунктам §0 всех трёх частей, каждый подтвердить или опровергнуть.
14. **Hard-stop оркестратора**: (2) требуется ли менять П-3 или Д-7 — да/нет со свидетельством; (3) есть ли BLOCKER, на который проект не отвечает; (4) опроверг ли стенд проект на центральном пути.
15. **Где твоя проверка была слабее всего.**

**§1 Находки** — таблица `id | серьёзность | file:lines дерева | HEAD-якорь | что не так | чем пытался опровергнуть | уверенность`; id `B-nn`. BLOCKER — потеря свойства, которую проект не предписывал; отклонение от П-3/Д-7; тест, зелёный при сломанном свойстве. **§2 Поведение по ханкам** против `HEAD` (группируй по файлам, начни с `surface.rs` и `artifact.rs`). **§3 Граница** — таблица имён. **§4 Ворота** verbatim + твои точечные прогоны. **§5 Оставить как есть** — непустой и конкретный. **§6 Замечено вне рамок 5.1** — для 5.2/5.3/5.4.

В финальном ответе — только §0 (кратко) и таблица §1.

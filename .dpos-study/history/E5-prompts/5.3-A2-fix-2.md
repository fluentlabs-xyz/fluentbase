# Второй круг захода А2 строки 5.3 — после dsh-ревью круга 1

Ты — исполнитель (Opus 5) в СВЕЖЕМ контексте. Репозиторий `/home/djadjka/Work/fluentbase`, HEAD `f71f4d52`; рабочее дерево несёт незакоммиченный заход А2 (6 файлов под `crates/dpos/consensus/src/beacon/`). Пути без префикса — от него. `.dpos-study/history/E4-ORCHESTRATOR.md` не читать, не трогать.

Вход: (1) `git diff HEAD`; (2) постановка `.dpos-study/history/E5-prompts/5.3-A2-impl-1.md`; (3) журнал `.dpos-study/history/E5-3-A2.md`; (4) ревью `history/E5-3-A2-DSH-1.md` (D-01…D-10; два MODERATE). Реляция; проверяй по коду; REJECTED с file:line допустим.

Что оркестратор проверил сам: D-01 — drain start-race (`actor.rs:2900-2912`) проверяет только `has_seat`, бан `equivocator` из `on_message` здесь не повторён; D-02 — на drain-пути (`:2919-2927`) провал `append_journal` только `withhold_acks`, `ReceivedDealing` в `nondurable_dealings` не кладётся — DB-08 закрыт лишь на live-пути.

ПРАВИТЬ:
- D-01: одна функция «принять дилинг/ack от `from` для эпохи E» (seat + evidence-бан), вызываемая и с live-пути, и на drain; тест: рестарт с журналом, где есть `DealerEquivocation`, буферизованный `Commitment` эквивокатора до первого тика ⇒ отказ на drain, `refuse("equivocator")`.
- D-02: drain-путь кладёт провалившиеся `ReceivedDealing` в ту же очередь ретрая, что live (одна функция «append или отложить»); тест на drain с `journal_failures`.
- D-03: `Wiring::standalone()` — `share_dir` через `TempDir`, который живёт со сборкой (не `std::env::temp_dir()` без удаления); если сетевые фикстуры теперь реально шлют `ShareConfirm` — добавить один ассерт, фиксирующий это как факт (число confirm > 0 в одной фикстуре), чтобы поведение не было молчаливым.
- D-05: F-03 — обоснование/логика: при `!all_held && !ready` защёлка `QuorumMissing` не должна сниматься; поправить условие или комментарий по коду (реши по смыслу: две независимые причины, две защёлки, снятие каждой по своему условию).
- D-06: `mem::forget` — не забывать `agreement_rx` (держать в структуре сборки); докстрока «четыре хендла» — по факту.
- D-10: `standalone_actor_at` не перезаписывает `wiring.changed`, если передан явно.
- D-04: записать (Д-А2-2 принято оркестратором — внутренние `Option` `log_store`/`confirmations` вне списка); D-07/D-08/D-09: записать.

Правила: агентов не запускать; git только чтение; файлы — те же 6 + журнал (`E5-3-A2.md`, «Второй круг») + доки при необходимости; никаких `#[allow]`/`unwrap` на продакшн-пути; file:line; `CARGO_BUILD_JOBS=6`.

Ворота — все одиннадцать verbatim (база круга 1: `--lib` 723/0, стенд 58/0 и 49/0 @674, node 57/0, reader 64/0, slasher 16/0, clippy 2 чужих / 0 своих, с фичей 0, fmt 0, doc 6, harness 2/2086/10). Мутации с md5: (M1) drain без бана — тест D-01 красный; (M2) drain без ретрая — тест D-02 красный. Ответ — таблица D → решение → file:line → доказательство, ворота, md5.

<tone_preference>Кратко и по существу.</tone_preference>

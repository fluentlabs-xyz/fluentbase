# Четвёртый круг захода В+Г1 строки 5.3 — снять СТОП: фикстура `committee_tests` под прод-семантикой тумбстоуна

Ты — исполнитель (Opus 5) в СВЕЖЕМ контексте. Репозиторий `/home/djadjka/Work/fluentbase`, HEAD `a45c458d`; рабочее дерево несёт незакоммиченный заход В+Г1 (три круга). `.dpos-study/history/E4-ORCHESTRATOR.md` не читать, не трогать. Вход: `git diff HEAD`; журнал `.dpos-study/history/E5-3-VG1.md` §«Третий круг», особенно Т.2 (СТОП-запись); постановки `E5-prompts/5.3-VG1-impl-1.md`, `-fix-2.md`, `-fix-3.md`.

## Ситуация (проверено оркестратором по коду)

Третий круг завёл стендовый `TombstoneSet` прод-формой (`observe(&snapshot)` на фиде финализированных высот) — теперь тумбстоуненный узел стенда режется пред-декодным гейтом бикона как `untracked`, как в проде. От этого покраснела пред-существующая фикстура `testbed/committee_tests.rs` `four_nodes_at_four_heights_hold_one_committee_record_per_epoch` (`:196-200`): `live(4,1)`, `rotate_four_three_four` (`C[3]=C[4]={0,1,2}`, `:67-74`), `tombstoned=[(0, 64)]`, узел 3 разрезан в эпохе 2. Под прод-семантикой узел 0 (тумбстоунен с 64, но остаётся в `C[3]`) не участвует в DKG эпохи 3 ⇒ `covering=1 < quorum=3` ⇒ парковка на 95. Фикстура была зелёной ТОЛЬКО потому, что стенд свой тумбстоун к транспорту не применял. Тест НЕ удалять (вне объёма строки) — фикстуру ПРИВЕСТИ к BFT-исполнимой под прод-семантикой, сохранив каждую премиссу непустой.

Что тест доказывает и что нельзя потерять (докстрока `:150-195`): (1) две ветки контракта дают разные комитеты (`branching_rotation`); (2) есть догоняющий узел с tree-only хэшем (узел 3, разрез + re-jump); (3) записи читаются на разных якорях; (4) якоря страддлят `TOMBSTONE_FROM=64` — та же эпоха прочитана с флагом и без; флаг ДОСТИГАЕТ чтений — тумбстоуненный узел сидит в каждом комитете (`:198-199`: «Node 0 sits in every epoch's committee, so the flag reaches every read above the height»). Значит тумбстоуненный ДОЛЖЕН оставаться членом всех комитетов, а DKG эпох 3 и 4 обязан собирать кворум БЕЗ него и БЕЗ разрезанного узла 3.

## Решение оркестратора (исполнить; отклонение — с Д-nn и причиной)

Пятый узел: `StandConfig::live(5, 1)`, расписание `C[3]=C[4]={0,1,2,4}` (узел 3 по-прежнему ротирован из 3 и 4, узел 0 в каждом комитете, `n=4`, `quorum(4)=3` = узлы 1, 2, 4), `branching_rotation` на тот же ростер; разрез `[0,1,2,4] | [3]`; имя теста и докстрока — под пять узлов (`five_nodes_…`), с абзацем ПОЧЕМУ пять: тумбстоуненный член не участвует в плане (прод-семантика с 5.3-В+Г1), и `n−1−1 ≥ quorum(n)` нужен, чтобы DKG собрался без него и без разрезанного. Все четыре премиссы и три наблюдения — те же, непустые (проверь числами: `straddled` непуст, `tombstoned_seen` непуст, `jump_calls[3]` непуст, `tree_only` есть). Если пять узлов ломают что-то другое в этом тесте (тайминги разреза/re-jump) — подстрой `after_height`/`for_views` минимально и запиши; если премисса 2 (tree-only хэш) не воспроизводится на пяти — СТОП с числами, не подгонять.

Заодно: юнит `dkg agree: too few members confirm` как ОЖИДАЕМЫЙ исход для «тумбстоуненный член остаётся в комитете, n−1 < quorum» — НЕ писать (это известная дыра слэшинга, REGISTER; вне рамки), только одна строка в журнале.

Правила: агентов не запускать; git только чтение; файлы — `testbed/committee_tests.rs` (только этот тест и его расписание; Д-nn — файл вне списка захода, причина — СТОП третьего круга) + журнал `E5-3-VG1.md` §«Четвёртый круг» + доки `.claude/dpos_architecture/{15_smoke…:428, 13_…:1250, 08_…:2915, 00_preamble:24}.md` — имя теста упомянуто там (grep сделан оркестратором), переименовать и в одну фразу сказать, почему пять узлов; `.dpos-study/EXPERIMENTS.md:107` (Ex-4.1c) и `REGISTER.md:728` — оставить оркестратору, не трогать; ничего больше. file:line; `CARGO_BUILD_JOBS=6`.

Ворота — все одиннадцать verbatim; ожидаемая база: `--lib` 688/0 (третий круг: 687/1 с этой фикстурой), стенд с фичей 58/0 и без 49/0 @639, node 57/0, reader 64/0, slasher 16/0, clippy 2 чужих / 0 своих, с фичей 0, fmt 0, doc 6, harness 2/2086/10. Мутация с md5: вернуть `live(4,1)`/`{0,1,2}` — тест красный на парковке (verbatim строку `too few members confirm`), откат. md5 `committee_tests.rs` на выходе. Ответ — что изменено, числа премисс, ворота, мутация, md5.

<tone_preference>Кратко и по существу.</tone_preference>

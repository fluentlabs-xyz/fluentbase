# Третий круг захода Б строки 5.3 — точечные правки после dsh-ревью круга 2

Ты — исполнитель (Opus 5) в СВЕЖЕМ контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `18573e76`; рабочее дерево несёт незакоммиченный заход Б (13 файлов под `crates/dpos/consensus/src/`). Пути без префикса — от `crates/dpos/consensus/src/beacon/`. `.dpos-study/history/E4-ORCHESTRATOR.md` не читать и не трогать.

Вход, в этом порядке: (1) `git diff HEAD --stat` и сам дифф — объект; (2) постановка захода `.dpos-study/history/E5-prompts/5.3-B-impl-1.md`; (3) журнал исполнителя `.dpos-study/history/E5-3-B.md` (два круга); (4) ревью `.dpos-study/history/E5-3-B-DSH-1.md` и `E5-3-B-DSH-2.md` — круг 2 подтвердил все правки круга 1 и дал E-01…E-14, все MINOR/NIT. Всё в (3)–(4) — реляция; перед правкой проверь по коду, REJECTED допустим с file:line.

Правила: агентов не запускать (спека и код — в одном контексте; себя агентом не проверять); git только чтение; писать только в файлы диффа + журнал `.dpos-study/history/E5-3-B.md` (раздел «Третий круг»); продакшн вне названных мест не трогать; никаких `#[allow]`, новых `unwrap`/`expect` на продакшн-пути; каждое утверждение — file:line; `CARGO_BUILD_JOBS=6`.

Что я проверил сам: E-07 — `artifact.rs:1772-1785`: локальный хит проверяется ДО `throttle`, waiter регистрируется ПОСЛЕ; `Have`, доставленный во время сна, текущим вызовом теряется — подтверждено по коду.

ПРАВИТЬ (каждое — минимально, без переписывания вокруг):
- E-01: два уцелевших доккомментария про per-dealer (`actor.rs:754`, `:2213-2214`) — по коду.
- E-02: `checked_serve_map` (`ceremony.rs:289-293`) должен применять ТО ЖЕ правило валидности пары, что и replay (`:923-940`): один дилер, два разных хэша, иначе пара не служится целиком. Одна функция валидности, два вызова.
- E-03: число в записи D-04 (журнал и `.claude/dpos_architecture/08_…`) — худший случай для не-члена `×n`, не `×(n−1)`; поправить текст, код не трогать.
- E-05: юнит ротации `artifact.rs:~2746` — добавить прогон с `me = Some(член комитета)` и ассерт, что себя не спросили.
- E-06: второй стенд-тест `testbed/tests.rs:3334` — `reveals_seen == reveals_swapped`, как в первом (`:3138`).
- E-07: после `throttle` перепроверить локальный стор до регистрации waiter (три строки), с `forget` как на первом хите.
- E-08: `or_default()` только при наличии seat (`actor.rs:1523-1524`).
- E-11: `actor.rs:2014-2019` — `extend` перезаписывает `(epoch, dealer)`; использовать `entry().or_insert` по дилеру, чтобы первая пара оставалась (согласовано с `insert_log`, где пара фиксируется один раз).
- E-12: порядок `first`/`second` в паре при replay — либо сверять с порядком записи, либо явно задокументировать, что порядок не несёт смысла (выбери по тому, читает ли порядок кто-нибудь; если никто — второе).
- E-13: тест кодека — закрепить и полный размер арма `BeaconFetchKey::Log` (тег + 72 = 73) и положение тега.

ЗАПИСАТЬ (журнал, не править): E-04 (латентный «present but not held» после D-16 — недостижимо по построению, остаток для А), E-09, E-10 (окно never-acked игрока при эквивокации — остаток D-12, share теряется, форка нет), E-14.

Ворота после правок — все одиннадцать verbatim (база из круга 2: `--lib` 680/0, стенд с фичей 56/0, без фичи 47/0 @633, node 57/0, reader 64/0, slasher 16/0, clippy 2 чужих / 0 своих, с фичей 0, fmt 0, doc 6, harness 2/2086/10). Мутация для E-02: вернуть старый `checked_serve_map` — тест на «пара с двумя дилерами не служится» красный. md5 всех тронутых файлов на выходе. Ответ — таблица E → решение → file:line → доказательство, ворота, md5.

<tone_preference>Кратко и по существу.</tone_preference>

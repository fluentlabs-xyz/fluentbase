# Строка 5.2, заход А — малый проход (доккомментарии в КОДЕ, найденные фазой Ф7)

Ты — исполнитель малого прохода (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база `HEAD` = `37691136`. Рабочее дерево строки 5.2 закончено и **ворота зелёные** — это не возврат, а добор. Пути без префикса — от `crates/dpos/consensus/src/`.

## Правила

1. **Агентов не запускать.**
2. **Git только на чтение.** Не коммить, не индексировать.
3. **Правки только в доккомментариях и комментариях.** Ни одной строки исполняемого кода, ни одного теста. Если для какого-то пункта нужна правка кода — не делай её, напиши это в отчёте.
4. `.claude/dpos_architecture/` **не трогать** — фаза Ф7 закрыта, там уже сведено.
5. Каждая правка опирается на `file:line` тела, которое ты открыл. Комментарий — не свидетельство о поведении.
6. Поиск — `git grep` по `crates`; не читай `**/.claude/session-reads/*.jsonl`.
7. `CARGO_BUILD_JOBS=6`.

## Файлы на запись

`beacon/{mod.rs, seed_journal.rs, log_resolver.rs, plane.rs}`, `executor.rs`, `dpos.rs` — **во всех только комментарии**; отчёт `.dpos-study/history/E5-2-A-FIX2.md`.

## Задание — восемь пунктов, все проверены оркестратором по коду

Фаза Ф7 нашла их ПОСЛЕ ревью — ревью их пропустило. В строке 5.1 доковый проход дал ровно такие же две находки, поэтому класс известный: текст, который строка редактировала, и в котором оставила мёртвое утверждение.

1. **`beacon/mod.rs:40-41`** — «PLAN rows 5.1-5.2 give the last two their readers» стоит в будущем времени. Строка 5.2 И ЕСТЬ та строка: `Observed` читается на трёх продакшн-сайтах (`spec_exec.rs`, `cert_inlet.rs` live-поток, `cert_inlet::UpstreamResolver`) плюс crash-replay в `dpos.rs`; `DataFault` — в `cert_inlet.rs` (`self.faults`, `drain_late_verdicts`). Перепиши в настоящем времени и назови читателей по `file:line`, которые проверишь сам.
2. **`beacon/mod.rs:45-46`** — «the two PRODUCTION implementations still speak». Реализация теперь ОДНА: `git grep 'impl Randomness for' -- crates` даёт единственную строку, `beacon/surface.rs`. Это утверждение о количестве, опровергаемое однострочным грепом.
3. **`beacon/seed_journal.rs:7-10`** — обоснование ORDERING-CRITICAL ссылается на «the certify gate's `false`-on-missing-seed verdict», а certify-гейта нет. `spec_exec.rs:56-84` этот же контракт УЖЕ перевыведен заново и там прямо сказано, что прежнее обоснование устарело и что половина «precede the executor send» не несущая. Приведи `seed_journal.rs` в соответствие с `spec_exec.rs`; если они противоречат — верен `spec_exec.rs`, но проверь оба тела сам. Строка 5.2 редактировала здесь слово `SeedStore` → `SeedIndex` в том же предложении и мёртвое обоснование не заметила.
4. **`beacon/log_resolver.rs:128-131`** — «`SeedStore::insert` writes the per-epoch terminal pin from that same call». Ни `insert`, ни `pin_terminal`, ни карты пинов больше нет; выживание терминального раунда обеспечивает ПРАВИЛО вытеснения (`seed_index.rs::oldest_evictable`), ограниченное окном `SCHEME_RETENTION_EPOCHS` — чем пин ограничен не был. Это единственное место, где обоснован отказ от переиспользования проводного тега `TAG_SEED_RETIRED`, поэтому перепиши ВЫВОД, а не только имя: скажи, по какой причине он держится теперь и что эта причина СЛАБЕЕ прежней. Заодно `capture_certificate_seed` в том же абзаце не существует — это предсуществующий дрейф, поправь его тоже.
5. **`executor.rs:9475-9476`** — ссылка на `certify.rs`'s `seed_store_record_notifies_without_a_lost_wakeup`: файла нет, теста с таким именем нет. Преемник — `beacon/seed_index.rs::seed_index_record_notifies_without_a_lost_wakeup`. Проверь имя по коду и перепиши ссылку.
6. **`executor.rs:1689-1690`, `:2479-2482`, `:5219`** — механизм назван «`Notify`» / «per-record `Notify`», а на деле это `broadcast::Sender<BeaconEvent>` с вариантом `SeedRecorded` (`beacon/seed_index.rs`). Дрейф предсуществует 5.2 (broadcast появился в 5.0), но эти строки — В ЕЁ ДИФФЕ: в них менялось `SeedStore` → `SeedIndex` в том же предложении. Разница несущая: пермит `notify_one` переживает отсутствие ждущего, а broadcast буферизует ОТ МОМЕНТА ПОДПИСКИ и требует подписаться до первого чтения — `beacon/surface.rs:236-243` описывает это как ловушку. Назови механизм правильно во всех трёх.
7. **`dpos.rs:773`** — док-ссылка `[`seed_from_cert`]` на несуществующий символ; функция называется `seed_via_beacon`. Второе вхождение (`dpos.rs:4689`) корректно — там сказано «предшественник», его не трогай.
8. **`beacon/plane.rs:1041`** — буллет «no DKG, no agreement plane, no muxes, no geometry watch» отрицает поле, которое конструктор ставит намеренно: `build_resolved` передаёт `geometry: watch::channel(Some((0, 1))).1` и объясняет это как несущее решение (follower не замораживает своей геометрии, поэтому уточнение `GeometryUnfrozen` в `share_probe` не должно срабатывать). Последствие ограничено меткой `WithheldReason`, не поведением — это нит, но буллет читается как инструкция снять поле. Проверь оба тела и поправь буллет.
9. **`beacon/mod.rs:177`** — «unlike the `SeedStore` alias **below** it», а алиас на `:170`, то есть ВЫШЕ. Нит.

## Ворота

Правки чисто комментарные, поэтому достаточно: `cargo test -p fluentbase-consensus --lib` (сейчас **665/0**), `cargo clippy -p fluentbase-consensus --all-targets` (ноль в consensus), `cargo fmt --check`, `cargo doc -p fluentbase-consensus --no-deps` (**unresolved 6** — и проверь, не стало ли их МЕНЬШЕ после п. 7: если стало, скажи это, это хорошая новость). Полный набор стенда оркестратор перегонит сам.

## Отчёт `.dpos-study/history/E5-2-A-FIX2.md`

**§0** — таблица `пункт | сделано / не сделано | file:lines | чем проверено тело`. **§1** — если какой-то пункт требовал правки КОДА, а не комментария: что именно и почему ты её не сделал. **§2** — ворота verbatim. **§3** — что ещё того же класса нашёл по дороге (ищи: доккомментарии на удалённые символы, утверждения о количестве, обещания в будущем времени, которые строка уже выполнила).

В финальном ответе — только §0 и §3.

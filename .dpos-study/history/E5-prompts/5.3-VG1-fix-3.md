# Третий круг захода В+Г1 строки 5.3 — точечные правки после dsh-ревью круга 2

Ты — исполнитель (Opus 5) в СВЕЖЕМ контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `a45c458d`; рабочее дерево несёт незакоммиченный заход В+Г1 (8 файлов под `crates/`). Пути без префикса — от `crates/dpos/consensus/src/beacon/`. `.dpos-study/history/E4-ORCHESTRATOR.md` не читать, не трогать.

Вход, по порядку: (1) `git diff HEAD`; (2) `.dpos-study/history/E5-prompts/5.3-VG1-impl-1.md` и `5.3-VG1-fix-2.md` (решение 2 карты ПЕРЕСМОТРЕНО — классификация только пред-декодным гейтом, эпоху привязывает потребитель); (3) журнал `.dpos-study/history/E5-3-VG1.md` (два круга); (4) ревью `E5-3-VG1-DSH-1.md` и `E5-3-VG1-DSH-2.md` — круг 2 дал часть (А): D-01…D-06/D-19 сняты пересмотром, D-07…D-12/D-15/D-16/D-21 FIXED, D-20 PARTIALLY, и часть (Б): E-01…E-11. Всё в (3)–(4) — реляция; проверяй по коду; REJECTED с file:line допустим.

Что оркестратор проверил сам: E-03 — `stand.rs:2440` `TombstoneSet::default()`, ничем не заполняется; `cfg.tombstoned` уходит только в `FakeStaking::with_schedule` — «тумбстоуненный» стендовый узел остаётся `Member` окна. E-09 — `actor.rs:1989-2011`: `has_seat` проверяется на дрейне `pending`, то есть не-член может занимать слот `pending` до `maybe_start`; на HEAD `beacon_member` резал его на входе.

ПРАВИТЬ:
- E-03: `cfg.tombstoned` заводить в стендовый `TombstoneSet` (тот же, что и в окне и в `OuterBuilder`), чтобы тумбстоуненный узел в стенде резался гейтом как `untracked`, как в проде. Если у `TombstoneSet` нет публичного `insert` — установить, как его заполняет прод (`node/dpos.rs`, слэшер), и повторить ту же форму; не добавлять тестовых лазеек в продакшн-тип.
- E-09: при буферизации в `pending` (`is_bufferable`/сайт вставки) — если `committee_for(epoch)` читаем и `position(from)` нет ⇒ `refuse(.., "no_seat")` сразу, слот не занимать; если ростер ещё не читаем — буферизовать (дрейн проверит, как сейчас).
- E-04: `beacon(reason)` в стенд-тесте — фильтр по `channel="beacon"` И `reason`, не только `reason` (`counter_of` с двумя метками или новый хелпер).
- E-05: отбросы вне `refuse` в ingress актора (внешний декод `BeaconMessage::read` Err, пик эпохи Err, `DkgMsg::read_cfg` Err) — через `refuse(.., "undecodable")` (одна метка), чтобы «одно место отказа» было правдой; не считать только то, что вообще не отказ.
- E-07: доккомментарий `consensus/src/dpos.rs:73-78` (`GatedReceiver`) — под пересмотренное правило (только комментарий; файл вне списка — разрешаю комментарий).
- E-08: журнал §В.8 п.3 — утверждение «`no_seat` в проде всегда 0» неверно (window-`Member` любой из трёх эпох может подписать confirm за чужую эпоху); переписать.
- E-10: комментарий `epoch_transition.rs:746-754` — убрать «no second reading can disagree».
- E-11: явная премисса в стенд-тесте: узел 4 в active registry ⇒ `Tracked`⇒`secondary` (ассерт на предусловие, не молчаливая связь).

ЗАПИСАТЬ (журнал, «Третий круг», не править): E-01 (confirm нового члена отвергнут гейтом, не переиздаётся — пред-существует с 4.3-А; кандидат в реестр, фикс — переиздание `mint` при росте окна, вне рамки), E-02 (гейт по окну и потребитель по `committee_for` могут разойтись на пропущенной несовершённой записи/опережении часов — пред-существует; текст константы `actor.rs:131-138` про «one source» — сузить до «одна классификация, одна привязка»), E-06 (`has_seat` на `Reveal` — как на HEAD).

Правила: агентов не запускать; git только чтение; файлы — те же 8 + `consensus/src/dpos.rs` (только комментарий) + доки `.claude/dpos_architecture/` (если правило меняется в тексте — E-02 сузить) + журнал; никаких `#[allow]`/`unwrap` на продакшн-пути; file:line; `CARGO_BUILD_JOBS=6`.

Ворота — все одиннадцать verbatim (база круга 2: `--lib` 686/0, стенд 57/0 и 48/0 @638, node 57/0, reader 64/0, slasher 16/0, clippy 2 чужих / 0 своих, с фичей 0, fmt 0, doc 6, harness 2/2086/10). Мутации с md5: (M1) E-03 — не заводить `tombstoned` в набор — тест на тумбстоун в стенде красный (нужен тест: тумбстоуненный узел шлёт дилинг ⇒ `untracked`); (M2) E-09 — вернуть буферизацию без проверки — юнит красный; (M3) E-05 — `undecodable` не считать — юнит красный. md5 всех тронутых файлов на выходе. Ответ — таблица E → решение → file:line → доказательство, ворота, md5.

<tone_preference>Кратко и по существу.</tone_preference>

# Третий проход строки 5.0а, заход А — по ревью `E5-0a-A-REVIEW.md`

Ты — исполнитель (Opus 5) третьего прохода в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `bec013a7`; объект — **незакоммиченное дерево захода А**: `testbed/stand.rs` (+244), `testbed/mod.rs` (+4), новый `testbed/cert_inlet_tests.rs`. **Чужое** — `.dpos-study/PLAN.md`, `.dpos-study/history/E4-ORCHESTRATOR.md` — не трогать. Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`.

## Правила (обязательные)

1. **Агентов не запускать.** Спека и код обязаны сидеть в одном контексте.
2. **Git только на чтение.** Не коммитить, не `add`.
3. **Файлы на запись — ТОЛЬКО**: `testbed/stand.rs`, `testbed/cert_inlet_tests.rs`, `testbed/mod.rs`, `testbed/fakes.rs` (если и только если правка A-04/A-05 требует — тогда назови, что именно и почему), `.dpos-study/history/E5-0a-A.md` (дописать часть В в конец; §0/§2/§5 править на месте там, где сказано ниже). **Запрещено**: `testbed/tests.rs`, `testbed/byzantine_roles.rs`, `testbed/preconditions.rs`, `testbed/committee_tests.rs`, `cert_inlet.rs`, `plane_upstream.rs`, `beacon/**`, `committee/**`, `epoch_manager.rs`, `executor.rs`, `node/**`, `devnet/**`, `contracts/**`, `Cargo.toml`, `.claude/**`.
4. **file:line на каждое утверждение.**
5. Без `#[allow]`, `todo!`, таймеров вместо событий, поллинга вместо пробуждений; `unwrap` на не-тестовом пути — только `Mutex::lock().unwrap()`. Любое из этого — только строкой Д-nn с причиной.
6. Комментарии, доки и журналы — **не свидетельство**. Свидетельство — код и твой прогон.
7. Читать целыми строками. Поиск — ТОЛЬКО `git grep` (под `crates/dpos/consensus/src/.claude/session-reads/` лежат gitignored журналы чужих сессий).
8. `CARGO_BUILD_JOBS=6`. Стенд не гонять параллельно с другой сборкой.
9. **Правила прогонов**: правки БАТЧЕМ → один прогон затронутых тестов → в конце ОДИН `cargo test -p fluentbase-consensus --lib`, `cargo clippy -p fluentbase-consensus --all-targets` **БЕЗ фичи** и **с фичей**, `cargo fmt --check`. Полный набор НЕ гонять — он у оркестратора. «Красный до правки» — только для A-05 (ниже сказано, как). Мутаций ≤ 2, по одному тесту (`--exact`), с откатом и `md5sum` до/после. Детерминизм ×3 — один раз в конце, по всем тестам файла.
10. Ничего не ослаблять. Покраснение существующего теста — находка с механизмом, не подгонка чисел. Существующие числа: lib 688/0, стенд с фичей 51/0, стенд без фичи 42/0, clippy ровно два чужих (`large_enum_variant` в node, `MutexGuard` в staking-reader).

## Вход

`.dpos-study/history/E5-0a-A-REVIEW.md` — читать ЦЕЛИКОМ (§0–§6). `.dpos-study/history/E5-0a-A.md` — свой контекст (§0–§5 + часть Б). `.dpos-study/history/E5-prompts/5.0a-A-impl-1.md` — постановка захода. Строка плана — `.dpos-study/PLAN.md` строка 111 (блок «### Э5.»). Проект — `history/E5-BEACON-DESIGN.md` §5.1 `:556-595`, §0.7(а)/(б), §7 строки 5.2/5.4 `:711-733`.

Решения оркестратора — таблица ниже. Спорить можно только фактом с `file:line`: тогда REJECTED с причиной в журнале и правку НЕ делать.

**Что оркестратор проверил САМ** (не перепроверяй, опирайся): tee `try_send` стоит в `cert_inlet.rs:712-717`, то есть ПЕРЕД `verify_block` (`:735`/`:743`) и `report_finalization` (`:739`/`:744`); `PlaneUpstreamHandle::rotate` = `mailbox.cancel(FrontierKey::Latest)` (`plane_upstream.rs:684-692`); `pub trait FrontierMarshal { latest_pair(), pair_at(height) -> Option<(Cert, OrderBlock)> }` (`plane_upstream.rs:249-257`) и `impl FrontierMarshal for MarshalMailbox` (`:259-268`); `FakeChain` не умеет удерживать тир-F (`fakes.rs:326-359`); `Partition{a, b, after_height, duration}` + `PartitionCfg::for_views` (`stand.rs:489-510`), наблюдаемая `PartitionObservation{heights_at_cut, heights_at_heal}` (`:639-644`), и `PeerSet::Committee` с `Stand::partition` несовместим (`stand.rs:363-365`) — но дефолт `PeerSet::AllNodes` совместим; `StandConfig::honest` уже даёт `committees: Committees::All` (`stand.rs:374-400`), `live` наследует (`:402-414`); `inlet_inputs` захватывает `dkg_height_tx.clone()` ДО строки `let dkg_height_tx = matches!(cfg.beacon, Beacon::Live).then_some(dkg_height_tx)` (`stand.rs:2474-2488`). Плюс оркестратор сам воспроизвёл «красный до правки»: замена `assert_eq!(f.defers, 0, …)` на `assert!(f.defers > 0, …)` (`cert_inlet_tests.rs:485`) даёт `FAILED. 0 passed; 1 failed` с `CertInletFacts { ingests: 3333, rotations: 0, tee_heights: […127], defers: 0 }` — ветка defer через плоскость не берётся ни разу, и `tee_heights` содержит ~3333 записи, каждая высота повторена ~26 раз.

## Решения по находкам

| id | серьёзность | решение | что сделать |
|---|---|---|---|
| A-03 | SERIOUS | **FIXED** | Пересылка теенной высоты в настоящий `dkg_height_tx` обязана происходить в ПРОДАКШН-ПОРЯДКЕ — до `verify_block`/`report_finalization`, а не после возврата `ingest`. Сделать без отдельной задачи-реле: в том же цикле держать `ingest` как future и дренировать `tee_rx` КОНКУРЕНТНО (`tokio::select!` или `futures::select!` с biased-порядком в пользу `tee_rx`, в петле до завершения ingest-future; затем финальный `try_recv`-дренаж на остаток). Обосновать выбранную форму и доказать по коду, что порядок теперь тот же, что в продакшне. Если конкурентный дренаж невозможен из-за `&mut self` в `ingest` — назови точную причину с file:line и предложи альтернативу (например отдать tee НАСТОЯЩИЙ `dkg_height_tx` и наблюдать тик через гейдж `dpos_dkg_clock_height` + `dpos_dkg_height_drops_total`, потеряв список высот) — но не выбирай альтернативу молча. **Это центральная правка прохода**: без неё эксперимент §0.7(а) проекта построить нельзя, потому что стенд не отличает «tee пришёл раньше marshal-Tip» от «позже» |
| A-04 | MODERATE | **FIXED** | Строка плана требует роль «валидатор с отстающим EL и живым inlet-ом». Сегодня её изображает узел, НАВСЕГДА выкинутый из `committee[2]` — не валидатор и не догоняющий. Построить настоящую: ЧЛЕН комитета, временно отрезанный `Stand::partition` (`PeerSet::AllNodes`, `after_height`/`for_views`), с живым inlet-ом; после заживления он догоняет, оставаясь членом. Новый тест: назвать его так, чтобы имя говорило «догоняющий ЧЛЕН», утверждения — лаг реально был (`PartitionObservation`), узел остался членом (дилит: артефакт своей эпохи есть), inlet кормил его в это время (`ingests > 0`), и он догнал (lockstep на конце). Существующий keyless-тест НЕ удалять и НЕ переписывать: он пинует другое (не-член + гейт плоскости) — только уточнить его доккоммент, что это НЕ-ЧЛЕН, а не «отстающий валидатор» |
| A-05 | MODERATE | **FIXED** | Добавить вход, названный строкой плана ПЕРВЫМ и до сих пор не построенный: поток `UpstreamFinalized`, собранный из архива ДРУГОГО узла напрямую — `FrontierMarshal::pair_at(h)` по `MarshalMailbox` узла-источника, БЕЗ плоскости и без её гейта `deliver`. Третий вариант `CertInletSource` (например `PeerArchive { from: usize }`); хэндл источника — поздней привязкой, тем же приёмом, что `marshal_slot` (`stand.rs:2011`): в `drive` создать вектор слотов на все узлы и передать его в `build_node`, задача ждёт слот источника. Что этот вход обязан дать: (1) достижимость ветки defer inlet-а (`cert_inlet.rs:618-640`) — **и это твой «красный до правки»**: сначала поставь ассерт `defers > 0` на новом тесте, покажи, что он КРАСНЫЙ на текущем дереве (плоскостной фидер, `defers == 0` — вывод оркестратора выше), затем сделай его зелёным новым входом; (2) фикстуру для R-129 из колонки «Закрывает» строки 5.0а — но САМ тест R-129 не писать (он у 5.2: R-129 про marshal-резолвер, `REGISTER.md:997-1006`), а записать в §5 журнала, чего ему не хватает; (3) положительный контроль для счётчика `defers` (A-16). Фидер обязан идти ВВЕРХ без повторов — на нём же измерь, сколько ingest-ов нужно, и сравни с ~3333 плоскостного (A-02) |
| A-06 | MODERATE | **FIXED** | Подключить `with_carry_forward_fail_metric` (`cert_inlet.rs:~432`; форма — follower-ская, `consensus/dpos.rs:3680`) и добавить его в наблюдаемые. В keyed-тесте это ТОЧНЫЙ свидетель того, что ключ эпохи был известен в момент verify: счётчик растёт только на «verify провалился ПРИ resolvable ключе» (`cert_inlet.rs:668-674`). Ассерт «счётчик == числу взятых подделок» ставит премиссу keyed-арма на прямое свидетельство вместо артефакта на конец прогона |
| A-01 | MODERATE | **FIXED (комментарий + журнал)** | Комментарий `stand.rs:2616-2623` и журнал §0(3) утверждают, что `None` от `fetch_one` всегда стоит `FRONTIER_FETCH_TIMEOUT = 8 s`. Это неверно: на пути дропа `deliver` (`plane_upstream.rs:488`) oneshot-sender уничтожается и `rx` резолвится `Err` мгновенно (комментарий `:641-647` этот путь называет). Переписать оба по факту: цикл ограничен сетевым round-trip'ом (латентность стенда 10 мс), а не таймаутом; назвать измеренную частоту из keyless-прогона |
| A-11 | MINOR | **FIXED (текст)** | Назвать в доккомментах keyed-теста и в журнале: `PlaneUpstreamHandle::rotate` = `mailbox.cancel(FrontierKey::Latest)` (`plane_upstream.rs:684-692`), то есть для by-height фидера полный no-op, и равенство `rotations == faults / MAX_UPSTREAM_FAULTS` держится ИМЕННО на её бездействии. Не молчать об этом: иначе читатель верит, что стенд моделирует продакшн-failover |
| A-17 | MINOR | **FIXED** | Неназванная премисса точности `faults = ingests − tee.len()` — «tee.try_send ни разу не упал». Поставить `dpos_dkg_height_drops_total == 0` ассертом и в keyed-тесте (в clean-тесте он уже есть) |
| A-08 | MINOR | **FIXED** | `Beacon::Static` + `cert_inlet` достижимы (`honest` = `Static`), приёмник настоящего `dkg_height_tx` дропнут, и каждая пересылка тикает `dpos_dkg_height_drops_total`, чей смысл — «канал ПОЛОН». Закрыть: либо громкий отказ конфигурации на сборке, либо inlet поднимается только при `Beacon::Live`. Выбрать и обосновать |
| A-09 | MINOR | **FIXED (текст)** | Назвать в keyless-тесте, что `dpos_frontier_dropped_total` — процессно-суммарный (`counter_of`, `stand.rs:594-598`) и в него попадает и узел 3; сказать, почему вывод от этого не рушится (или усилить ассерт, если есть чем) |
| A-10 | MINOR | **FIXED (журнал)** | Якоря журнала §0(2),(3),(4) указывают не на то, что названо (2578→2589, 2576→2588, 2613-2616→2630-2633, 2626-2636→2648-2653, 2396→2433). Перепинить ВСЕ якоря журнала на финальное дерево в самом конце прохода, после последней правки |
| A-14 | NIT | **FIXED** | Литералы заменить существующими константами: `EPOCH_LEN`/`32` (`stand.rs:376`), активация `0` (`fakes.rs:51` `DPOS_ACTIVATION_BLOCK`), эпоха `2` (`beacon::testing::DETERMINISTIC_BOOTSTRAP_EPOCH`, `beacon/actor.rs:127`). Если константа из `testbed` недоступна — назови почему, литерал оставь с комментарием-ссылкой |
| A-18 | NIT | **FIXED** | keyed-тест обещает «комитет цел и цепь одна», но не проверяет ни `errors()`, ни `assert_lockstep_except` — добавить оба (как в тестах (1) и (3)) |
| A-19 | NIT | **FIXED** | Отрицательный контроль поставить ассертом, а не только текстом: в каждом тесте — `cert_inlet[j].is_none()` для всех узлов, которые inlet не поднимают |
| A-16 | MINOR | **FIXED через A-05** | Положительный контроль «счётчик `defers` умеет расти» даёт новый архивный вход. Записать связь |
| A-15 | NIT | **REJECTED** | Не убирать `cfg.committees = Committees::All;`. Причина (проверено оркестратором: `stand.rs:374-400`, `:402-414` — это действительно дефолт): явное присваивание делает предпосылку фикстуры независимой от дефолта `honest` и читается на месте. Записать как REJECTED с этой причиной |
| A-02 | MODERATE | **RECORDED** | Не чинить арм `Frontier`: его переингест (3333 на ≤127 высот, каждая высота ~26 раз — измерено оркестратором) — цена того, что этот тест пинует ГЕЙТ ПЛОСКОСТИ, а другого способа получить серт выше своего окна через плоскость нет. Записать цену цифрами в §4 журнала и сказать, что новый архивный вход (A-05) этой цены не платит |
| A-07 | MODERATE | **RECORDED** | Не менять ассерт `tee == (1..=ingests)`: набор {первый == 1, строго возрастает, длина == ingests, последний == ingests} ему эквивалентен, так что «ослабить» нечего. Записать в доккоммент, что это пин СООТНОШЕНИЯ СКОРОСТЕЙ (executor успевает продвинуть тир-F до следующего `chain.tip()`), и что на одном сиде он наблюдался ×3 |
| A-12 | MINOR | **RECORDED** | Второй общий с 5.1 тип — `Outcome`. Одна строка в §5 журнала |
| A-13 | MINOR | **RECORDED** | `UpstreamCounters` смешивают трафик inlet-а и зонда (`served_heights` растёт до тысяч). Одна строка в §5 для 5.2/5.4 |

## Журнал — часть В (`.dpos-study/history/E5-0a-A.md`, дописать в конец)

В§0 Прямые ответы: (1) итог по 19 находкам — FIXED/RECORDED/REJECTED с числами, каждая ровно раз; (2) A-03: выбранная форма конкурентного дренажа, доказательство продакшн-порядка по коду, и что именно теперь МОЖЕТ измерить 5.4, чего не могла до; (3) A-04: форма догоняющего члена, что наблюдается, чем отличается от keyless-теста; (4) A-05: форма архивного входа, «красный до правки» verbatim (ассерт `defers > 0` красный на плоскостном фидере, зелёный на архивном), сколько ingest-ов против 3333, и чего не хватает тесту R-129; (5) A-06: что теперь свидетельствует «ключ был»; (6) прогоны verbatim: `--lib`, clippy без фичи и с фичей, `fmt --check`, детерминизм ×3 по всем тестам файла; (7) что оказалось неверным в моих решениях (если что-то REJECTED — здесь); (8) запрещённого сделано — ноль или список.
В§1 Таблица по всем 19 находкам: исход и что сделано. В§2 Новые Д-nn (после 5.0а-Д-4). В§3 Раскладка коммитов (уточнить §3 с учётом правок). В§4 Где проверка слабее всего + цена арма `Frontier` цифрами. В§5 Что уходит в 5.1/5.2/5.4 (дополнить существующий §5).

Плюс правки на месте: §0(3) и комментарий кода по A-01; §0(2),(3),(4) якоря по A-10; §2 — цена `Frontier`.

Ты НЕ коммитишь. Заканчивай, когда всё сделано и твои четыре прогона зелёные; невозможное — сделать остальное и назвать в В§0(7). В финальном ответе — только В§0.

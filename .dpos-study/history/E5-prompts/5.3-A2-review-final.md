# Финальное ревью захода А2 строки 5.3 — `Wiring` и хвосты автомата (Opus, свежий контекст)

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база `HEAD` = `f71f4d52`. Объект — незакоммиченное рабочее дерево: 6 файлов `crates/dpos/consensus/src/beacon/{actor,artifact,ceremony,outcome,plane,share_state}.rs` плюс доки `.claude/dpos_architecture/*.md`, `.claude/DPOS_ARCHITECTURE_CHANGELOG.md`. Пути без префикса — от `beacon/`. md5 (первые 8): `actor.rs 9cbd4715`, `artifact.rs 85a74d45`, `ceremony.rs ad67f2f6`, `outcome.rs aed633bd`, `share_state.rs a46d50c8`, `plane.rs bcad0998` — сверь; не совпало — остановись.

Это ФИНАЛЬНЫЙ круг: два круга исполнителей (свежие агенты; журнал `history/E5-3-A2.md`), одно dsh-ревью (`history/E5-3-A2-DSH-1.md`, D-01…D-10, два MODERATE закрыты). Постановки — `history/E5-prompts/5.3-A2-{impl-1,fix-2}.md`. А1 — HEAD (`ffec54b2`), его финал `history/E5-3-A1-REVIEW.md`. Задача — **вердикт: коммитить ли**, и это последний заход строки 5.3.

## Установлено оркестратором (опирайся; опровергать — с file:line)
- Заход: двенадцать `Option`-швов → `Wiring<R>` (13 обязательных рёбер), builders и `recv_or_never` удалены (мой grep: `if let Some(` по швам — 0; `Option<` в полях — только `commitment`/`share`/`last_height`); `Conflict{key: Option}` в `needs_artifact()` (F-01); `restart_replay` удалён; `refuse("equivocator")` на дилинги/ack от дилера с evidence (E-10, Д-А2-1) — после круга 2 через одну `ceremony_refusal` на live и drain; `validate_share_on_poly(.., me, ..)` с `position(me) == share.index`; `journal_or_defer` на live и drain (DB-08); body-lost в `Dealing` запоминается (F-02); две защёлки `BodyMissing`/`QuorumMissing` с раздельным снятием (F-03); тестовая `Wiring::fixture` с `Drop`-зачисткой каталога и удержанными дальними концами каналов вместо `mem::forget`.
- Принятые остатки: Д-А2-2 (внутренние `Option` `log_store`/`confirmations` — за актором всегда `Some`), Д-А2-3 (`standalone().changed = |_| None`), Д-А2-11 (без `tempfile`, свой `Fixture`), HEAD-era каталоги `nondurable-*`/`append-*` в `/tmp` (вне рамок).

## Правила
1. Агентов не запускать. 2. Git только на чтение; единственный файл на запись — `.dpos-study/history/E5-3-A2-REVIEW.md`; мутации с откатом и md5; `E4-ORCHESTRATOR.md` не читать. 3. file:line на всё; доки/журналы — не свидетельство. 4. Полнота, не фильтр. 5. `git grep`. 6. **Cargo — после `until grep -q DONE /tmp/claude-1000/-home-djadjka-Work-fluentbase/f6f42c91-6188-4d9c-ae64-a1e9faed9028/scratchpad/gates/v1-status.txt; do sleep 30; done`**; `CARGO_BUILD_JOBS=6`; стенд не параллельно. 7. Стенд — два прогона.

## Вход
Дифф `git diff HEAD -- crates` первым. База ворот исполнителя: `--lib` 725/0, стенд 58/0 и 49/0 (676 filtered), node 57/0, reader 64/0, slasher 16/0, clippy два чужих / ноль своих, с фичей 0, fmt 0, doc 6, harness 2/2086/10.

## §0 — прямые ответы ([KNOWN]/[ГИПОТЕЗА], file:line)
1. Ворота verbatim (одиннадцать).
2. **Эквивалентность `Wiring` продакшну**: для каждого из 13 полей — что передаёт `plane.rs` и что HEAD передавал; любое отличие поведения продакшна против HEAD, кроме пяти названных правок, — находка по последствию. Закрытый plane-канал ⇒ `recv_or_park`, закрытый `resolver_rx` ⇒ `break` — обосновано?
3. **Тесты после `Wiring`**: 15 сайтов + 15 цепочек через 4 хелпера — назови тесты, чья фикстура изменилась по существу (`share_dir` реальный, `ConfirmPool` реальный, `recorded_dkg_logs` реальный) и есть ли среди них тест, чей ассерт стал слабее; `Fixture`/`Drop` — каталоги реально удаляются (проверь `ls` после прогона).
4. **Пять правок**: F-01 (подписание стоит после заполнения `key` на всех путях; `needs_artifact()` перестаёт просить); E-10 (`ceremony_refusal` на live и drain; честный дилер не отвергается; кворум дилингов достижим при одном забаненном); `validate_share_on_poly` (все вызывающие передают `me` узла; типы индекса); `restart_replay` (рестарт с артефактом на диске — шаги; что доказывает переписанный тест); DB-08/F-02/F-03 (`journal_or_defer` на обоих путях; body-lost применён ровно раз; условия снятия защёлок). Минимум ДВЕ свои мутации сверх M1–M6 исполнителя.
5. **Гигиена**: `#[allow]` нет; новых `unwrap`/`expect` вне тестов нет; `pub struct Wiring` — не наружу модуля; мёртвый код (`recv_or_never`, builders, `restart_replay`, `journal_epochs`, `ArtifactStore::epochs` под `cfg(test)`); `#[cfg(test)] fixture: Option<Fixture>` в продакшн-типе — приемлемо?
6. **Доки** `00/08/13/12` + CHANGELOG — по коду; `git grep with_pinned_requests|with_artifact_pull|with_plane_clock|with_agreement_plane|with_body_lost|with_changed_bit|with_recorded_logs|with_share_confirms|restart_replay|recv_or_never|gossip-only` — ничего живого.
7. Hard-stop: `DECISIONS.md`; BLOCKER.
8. **Вердикт: КОММИТИТЬ / НЕ КОММИТИТЬ**; что не прочитал.
9. Слабее всего — ранжируй.

**§1 Находки** `id | серьёзность | file:lines | что не так | чем пытался опровергнуть | уверенность`, `F-nn`. BLOCKER — продакшн-поведение против HEAD не из пяти правок; подпись под `Conflict`; честный дилер как `equivocator`; доля с чужим индексом; регрессия живости на стенде. **§2 Оставить как есть** — непустой. **§3 Вне рамок** — по строке.

В финальном ответе — §0 сжато (вердикт, непрочитанное) и таблица §1.

<tone_preference>Кратко и по существу.</tone_preference>

# Инвентарь для карты строки 5.3 (DKG-автомат, П-9) — исследовательский проход, только чтение

Ты — Opus 5 в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `18573e76`. Объект — модуль `crates/dpos/consensus/src/beacon/` (пути ниже — от него): `actor.rs` (9055 строк, `DkgActor`), `share_state.rs` (1255, `ShareState`), `ceremony.rs` (2088), `dkg_agree.rs`, `dkg_engine.rs`, `dkg_transport.rs`, `plane.rs`, `log_resolver.rs`. Это НЕ реализация и НЕ ревью: **результат — инвентарь с file:line**, по которому оркестратор построит карту и разбиение строки на заходы. Ничего не менять, ничего не предлагать переписать сверх того, что спрашивается. Агентов не запускать — ни для работы, ни для проверки себя: спека строки и код должны сидеть в одном контексте. Git только на чтение. Единственный файл на запись — `.dpos-study/history/E5-3-MAP-RESEARCH.md`. `.dpos-study/history/E4-ORCHESTRATOR.md` не читать. Поиск — `git grep`. Читать целыми строками.

## Строка плана (объект исследования)

`.dpos-study/PLAN.md`, строка `5.3` (найди по `| [ ] 5.3 |`), и решение П-9 в `.dpos-study/DECISIONS.md:92`. Дизайн — `.dpos-study/history/E5-BEACON-DESIGN.md` (строки про П-9: `:673`, `:645`, `:806`). Регистры, которые строка закрывает: R-002, R-036, R-038, R-026, R-051, R-072, R-067, R-070, R-071, R-126, R-127 — прочитай их в `.dpos-study/REGISTER.md` (по `R-nnn`), чтобы знать, какие дефекты автомат должен закрыть.

Уже установлено оркестратором (не перепроверяй, опирайся): `SatOut`, `Unrecoverable`, `Conflict` как варианты в дереве НЕ существуют; `Stalled` — только в доккомментарии `actor.rs:6734`; `Unfrozen` есть в `plane.rs`/`surface.rs`; ближайшее к состоянию церемонии — `ShareState` (`share_state.rs:97`). `beacon_member` — `actor.rs:2015`, `epoch_is_actionable` — `:1995`, порядок гейтов в `on_message`: окно (`:2039`), потом членство (`:2043`). Арм «резолвер умер» — `actor.rs:836-848`; движок — supervised-ребёнок (`plane.rs:998-1006`), узел держит один хэндл (`node/dpos.rs:797`) — снятие «gossip-only» живость не меняет.

## Что нужно, по разделам (каждое утверждение — file:line; комментарии и доки — не свидетельство, код — свидетельство; там, где вывод, а не чтение — пометка [ГИПОТЕЗА])

§1 **Неявный автомат сегодня.** Для ОДНОЙ эпохи E: какие поля `DkgActor` / `ShareState` / структур церемонии кодируют её фазу (каждое `Option`, флаг, карта с ключом эпохи, `deque`); какие сочетания значений реально встречаются; таблица «состояние по факту → какими полями выражено → где читается → где пишется». Отдельно: где фаза читается по НЕСКОЛЬКИМ полям сразу (это места, где явный enum что-то меняет).

§2 **Входы.** Четыре входа по дизайну: `clock` (высоты — `on_height`), артефакт (`on_artifact`), p2p (`on_message` — перечисли ТИПЫ сообщений и что каждый двигает), resolver (`on_resolver_message`). Для каждого — какие переходы он вызывает и с какими гейтами. Плюс любые ДРУГИЕ входы, которых дизайн не называет (таймеры, каналы, `pinned_rx`, что-то ещё).

§3 **Терминалы и отказы.** Где сегодня церемония «застревает», «падает», «переигрывается»: каждый `return`/`break`/fatal/`SafetyHalt`/логирование-и-молчание, который завершает или замораживает эпоху. Для каждого: восстановимо ли по коду (что его снимает) или это тупик. Сопоставь с терминалами из строки плана (`Unfrozen`, `SatOut`, `Unrecoverable`, `Conflict`, `Stalled{reason}`, `Acquiring{artifact|logs}`): что уже есть по сути под другим именем, чего нет.

§4 **Идентичность лога.** Ключи `recorded`, `signed_logs`, `scoped_pinned_logs`, `want` (и всё, что ещё держит логи по дилеру) — сегодня по чему ключ, где вторая подпись того же дилера перезаписывает первую или отбрасывается; где refetch и по какому ключу. Пометь каждое место, которое смена ключа на `(dealer, hash)` затронет.

§5 **Вход p2p и классификация отправителя.** Два источника (гейт окна до декода и `beacon_member`): где каждый, что каждый исключает, чем различаются множества; `TrackedWindow`/`ValidatorInputs` (`consensus/src/dpos.rs:89` и рядом) — как маска добирается до бикона сегодня, если добирается. Кадр `now+3` (R-126): где окно `[now, now+2]` задаётся и что упирается в `+3`.

§6 **`recover(E)`, `Option`-швы, метрики, `deque_size`.** Все функции восстановления эпохи (по имени и вызовам); каждый `Option` в конфигурации/конструкторе актора с тем, что означает `None`; все места регистрации метрик (сколько «реестров»); `deque_size` в `dkg_transport.rs` и перепредложение после nullify (P-16).

§7 **Связанность для разбиения.** Строка предположительно делится на заходы: А — сам автомат (состояния+терминалы+`recover`), Б — идентичность лога, В — вход p2p (один источник + окно как правило + `now+3`), Г — гигиена (resolver-exit ⇒ fatal, `Option`→конфиг, реестр метрик, юнит `deque_size`). Для каждой пары заходов: какие функции/поля общие; можно ли сделать Б, В, Г ДО А, не переделывая их потом; какой заход обязан идти первым и почему. Если разбиение по коду плохое — скажи, какое лучше, с якорями.

§8 **Тесты.** Какие тесты сегодня держат этот автомат (`beacon/*_tests.rs`, `testbed/`, `preconditions.rs`): по разделам §1–§6 — что покрыто, что нет.

§9 **Числа.** Функции `DkgActor` по группам (входы / переходы / восстановление / p2p / служебные) с диапазонами строк — чтобы оркестратор мог оценить объём каждого захода. Пересчитай, не оценивай на глаз.

§10 **Где твоё чтение слабее всего** — ранжируй.

Формат отчёта: разделы §1–§10, таблицы там, где таблица; в конце — прямые ответы: (а) сколько состояний ты насчитал по факту и сколько полей их кодируют; (б) три самых опасных места, одной строкой каждое; (в) какое разбиение ты рекомендуешь и что должно идти первым; (г) что осталось непрочитанным. В финальном ответе — только (а)–(г) и путь к файлу.

<tone_preference>Кратко и по существу. Каждая строка — либо факт с якорем, либо помеченная гипотеза.</tone_preference>

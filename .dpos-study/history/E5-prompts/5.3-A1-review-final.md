# Финальное ревью захода А1 строки 5.3 — явный автомат DKG-церемонии (Opus, свежий контекст)

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база `HEAD` = `60d6c3e3`. Объект — незакоммиченное рабочее дерево: 15 файлов под `crates/` (`dpos/consensus/src/beacon/{actor,artifact,ceremony,dkg_engine,metrics,plane,share_state,oracle,epoch_manager,log_store,dkg_oracle,log_resolver,dkg_agree}.rs` — последние пять/шесть лишь комментарии или одно тестовое выражение; `dpos/consensus/src/testbed/tests.rs`; `node/src/dpos.rs` комментарий) плюс доки `.claude/dpos_architecture/*.md` (gitignored, в дереве). Пути без префикса — от `crates/dpos/consensus/src/beacon/`. md5 на входе (первые 8): `actor.rs 23beb851`, `artifact.rs 478413f0`, `share_state.rs b3585037`, `ceremony.rs 56bb0ffd` — сверь; не совпало — остановись и скажи.

Это ФИНАЛЬНЫЙ круг после трёх кругов исполнителей (все свежие агенты) и четырёх dsh-ревью по фокусам (`history/E5-3-A1-DSH-{1A,1B,2A,2B}.md`). Журнал — `history/E5-3-A1.md` (три круга, трассируемость S0–S17, таблица переходов, Д-А1-1…24). Постановки — `history/E5-prompts/5.3-A1-{impl-1,fix-2,fix-3}.md`. Дизайн — `history/E5-BEACON-DESIGN.md` §5.2–5.4 (`:596-646`), приёмка `:729`. Инвентарь до изменения — `history/E5-3-MAP-RESEARCH.md` §1.2/§3. Задача — **вердикт: можно ли коммитить**. Объект большой (`actor.rs` продакшн ≈ `:1-4050`); если не удерживаешь целиком — скажи это прямо в §0.13 и назови, что не прочитал.

## Установлено оркестратором (опирайся; опровергать — с file:line)

- Что закрывалось по кругам (все проверены оркестратором по коду до правки): круг 1 — 25 носителей → `EpochSlot{state: EpochState, ..}`, `recover(E)`, гейт adopt против артефакта (F-02/R-039), body-lost канал, `Stalled{reason}`; **найден и закрыт попутный дефект Д-А1-12** — кэш ack ре-эмитился на ретрансмите после провала записи журнала. Круг 2 — DB-01 BLOCKER (третий путь в `Keyed` без `validate_share_on_poly` — теперь один `share_on_artifact`), DB-25 (`Conflict` RAM-only — маркер `beacon-conflict-e<E>.bin`), DA-01 (стор — владелец факта артефакта, `reconcile_with_store`), DA-02 (`Conflict` по `value_digest` без confirms), DB-03 (off-poly без спина). Круг 3 — EA-01/EB-04 BLOCKER (свидетель расхождения в сторе был RAM — теперь стор персистит маркер сам в `note_divergent`), EB-01 (порядок «маркер → доля» в `stop_signing`), EB-02 (`insert → Result<(), Box<AgreedArtifact>>`, проигравший гонку не теряется), EB-03 (`attempted` после загрузки, `Stalled{HealFailed}`).
- Пришпиливание (`AgreedSet.pinned`, `dkg_agree`) не менялось; `Conflict` только от значений, прошедших кворум-проверку моста. `DECISIONS.md` не трогался.
- Стенд-тесты не менялись и зелёные (58/49); юниты актора на `INTERVAL = 30` + возвращённая фикстура нулевого окна (R-036 ⇒ `SatOut`). Исполнитель 3-го круга отверг EA-07 (`resolve_artifact` сверяет payload сертификата, `value_digest` вычислить нечем — `artifact.rs:269-271`).
- Принятые остатки (не поднимать как находки, если запись верна): DA-08/09/10 (строки §5.2 не буквально — паритет с HEAD), DA-11, DA-13, DB-08 (провал записи `ReceivedDealing` не ретраится — А2), DB-28/DA-05 (body-lost `try_send` — актор сам тянет за границей), DB-29, DB-33, EA-06.

## Правила

1. **Агентов не запускать** — ни для работы, ни для проверки себя.
2. **Git только на чтение.** Единственный файл на запись — `.dpos-study/history/E5-3-A1-REVIEW.md`. Мутации — с откатом и `md5sum` до/после. `.dpos-study/history/E4-ORCHESTRATOR.md` не читать.
3. Каждое утверждение — `file:line`. Комментарии, доки, журналы, dsh-отчёты — не свидетельство.
4. **Полнота, не фильтр**; фильтрует оркестратор.
5. Поиск — только `git grep`. Читать целыми строками.
6. **Cargo — только после ворот оркестратора**: `until grep -q DONE /tmp/claude-1000/-home-djadjka-Work-fluentbase/f6f42c91-6188-4d9c-ae64-a1e9faed9028/scratchpad/gates/t1-status.txt; do sleep 30; done`. `CARGO_BUILD_JOBS=6`; стенд не гонять параллельно с другой сборкой.
7. Стенд — ДВА прогона (с фичей `dpos-devnet-byzantine` и без).

## Вход
Дифф `git diff HEAD -- crates` — ПЕРВЫМ, `actor.rs` продакшн целиком. База ворот исполнителя: `--lib` 717/0, стенд с фичей 58/0, без 49/0 (668 filtered), node 57/0, reader 64/0, slasher 16/0, clippy два чужих / ноль своих, с фичей 0, fmt 0, doc 6, harness 2/2086/10.

## §0 — прямые ответы, каждый с [KNOWN]/[ГИПОТЕЗА] и file:line

1. **Ворота** verbatim из ТВОИХ прогонов (все одиннадцать).
2. **Автомат**: перечисли варианты `EpochState` и для каждого — входы и выходы по коду; вариант без выхода до sweep, достижимый штатно, — находка (S11 `Unrecoverable` — намеренно). Сверь с §5.2 построчно; расхождения, не записанные как Д-А1-nn, — находки.
3. **Единственный путь в `Keyed`**: все сайты, где состояние становится `Keyed`, и что каждый вызывает перед этим; против чего проверяется доля (провенанс `group_key`). Мутация: снять `share_on_artifact` на ОДНОМ пути — какой тест краснеет.
4. **`Conflict` durable и по значению**: цепочка «второе значение → `note_divergent` (стор пишет маркер) → `stop_signing` (маркер → доля) → рестарт → `recover` читает маркер/стор → `Conflict`»; точки смерти между шагами — что на рестарте; `value_digest` — одинаково ли считается везде (мост, инстанс, актор, `resolve_artifact` — тут исполнитель отверг EA-07: прав ли он); маркер испорчен — fail-closed. Мутация: `note_divergent` без персиста.
5. **Стор — владелец**: `reconcile_with_store` до каждого сетевого pull; актор `Keyed` на X при сторе Y — достижимо?; `insert → Result` — все вызывающие обрабатывают `Err` (grep).
6. **`recover(E)`**: таблица рестарта — сверь с дизайном; окно `decidable_epochs`; вызывается ровно раз на эпоху; ветка частичного успеха.
7. **Живость**: `Stalled{*}` — защёлки, gauge, снятие при смене состояния; спин каждый тик где-либо (`HealFailed`, off-poly, unmappable); body-lost и `try_send`.
8. **Д-А1-12 (`withhold_ack`)**: трассируй гейт durable-before-ack на live-пути, дрейне и ретрансмите; может ли удержанный ack уйти без durable-журнала.
9. **Тесты**: 29 новых юнитов (699→717 минус/плюс) и фикстура нулевого окна — могут ли быть зелёными при сломанном свойстве; минимум ДВЕ свои мутации, не совпадающие с M1–M4 трёх кругов.
10. **Доки** `.claude/dpos_architecture/{00,03,08,13}.md` + `DPOS_ARCHITECTURE_CHANGELOG.md` — по коду; `git grep` по удалённым носителям (`torn_warned|eval_logged|deferred_reported|terminal_recompute|recompute_pending|agreement_announced|agreed_pinned`) в доках и коде — ничего живого.
11. **Граница и гигиена**: продакшн вне 15 файлов не тронут; `#[allow]` нет; новых `unwrap`/`expect` вне тестов нет (журнал называет замены на тех же сайтах — проверь); новых `pub` наружу нет; `#[cfg(test)]`-точка смерти в продакшн-типе (`actor.rs:897-898`) — приемлемо ли; мёртвый код.
12. **Hard-stop**: нужно ли менять решение в `DECISIONS.md`; есть ли BLOCKER.
13. **Вердикт: КОММИТИТЬ / НЕ КОММИТИТЬ**, одной фразой; и что НЕ прочитал.
14. **Слабее всего** — ранжируй.

**§1 Находки** — `id | серьёзность | file:lines | что не так | чем пытался опровергнуть | уверенность`; id `F-nn`. BLOCKER — доля над непроверенным/непришпиленным полиномом; `Conflict` от непроверенного значения или забытый рестартом; состояние без выхода, из которого HEAD выходил; изменение пришпиливания; регрессия живости на стенде. **§2 Оставить как есть** — непустой. **§3 Вне рамок** — по строке.

В финальном ответе — §0 сжато (включая вердикт и что не прочитано) и таблица §1.

<tone_preference>Кратко и по существу.</tone_preference>

# Row 5.3, заход Д — an absentee restarted after the seal deadline heals its share instead of sitting out

You are the implementer (Opus 5) in a fresh context. Repository `/home/djadjka/Work/fluentbase`, branch `djadjka/dpos-reth-2.2-squashed`, HEAD `d8747086`. Paths without a prefix are relative to `crates/dpos/consensus/src/`. The working tree is clean under `crates/`; `.dpos-study/{PLAN,EXPERIMENTS}.md`, `history/E5-ORCHESTRATOR.md`, `history/E5-REVIEW.md` and the root `CLAUDE.md` are modified by others — do not touch them. Never read `.dpos-study/history/E4-ORCHESTRATOR.md`.

## Why this заход exists (established by the orchestrator; contradict only with file:line)

`make smoke-vrf-dkg-live-heal` on `d8747086` FAILS on behaviour: `validator-3`, stopped before the epoch-2 deal window and restarted after `finalized ≥ 262 > seal(2) = 236` with no epoch-2 journal, is `verify-only (share-gate) … reason=NoUsableShare` four epochs later (log tail heights 511–522; the committee is stable, so epochs 3+ carry epoch 2's key forward). The harness expected one of two roads: `live DKG: ceremony started` on the restarted process (road A — `start_fresh` after the deadline; removed by design, R-036: re-dealing self-equivocates) or `live DKG: demoted committee member detected — starting share recompute-heal` (road B — `heal_over`, `beacon/actor.rs:2877-2893`, still logged). Neither was taken.

Cause, by code: `recover` (`actor.rs:3137`) computes `past_seal` (`:3232-3233`) and returns `SatOut { key }` for `JournalLoad::Torn if past_seal` (`:3244-3252`) and `JournalLoad::NoFile if past_seal` (`:3253-3260`). `SatOut` is terminal: an arriving artifact only records the digest (`:1771-1773`). The heal (`Acquiring(Acquire::Logs(heal_over(..)))`) is reachable only from `key_held_share` (`:1806-1836`, share `None` → heal at `:1831`) and from a failed finalize/adoption (`:2818`, `:2857`). The design cell (`history/E5-BEACON-DESIGN.md:612,616,637`: `(NoFile|Torn, h ≥ seal) ⇒ SatOut`) was justified by R-036 — which forbids re-DEALING, not a player-only recompute over the pinned logs: a dealer reveals the point of a player whose ack it never got (`actor.rs:1209`, `:3579`), so an absentee's share is derivable from the agreed set's pinned bodies without any broadcast. The cell is pinned by `recover_no_journal_at_the_seal_deadline_sits_out` (`:11888`), `recover_torn_journal_at_the_seal_deadline_sits_out` (`:11968`), `at_a_zero_width_deal_window_a_missing_journal_at_the_deadline_sits_out` (`:13484`), all from `ffec54b2` (5.3-А1). The case was PASS on `fca897c1` (Э4, `E4-CLOSEOUT.md` §5).

The owner decided (2026-09-15) to amend the cell. This is deviation **Д-5.3Д-1** from the design §5.2/§5.4 (record it in the journal; `DECISIONS.md` П-9 is untouched — no dealing is added).

## What to do

1. **Amend `recover`**: for `Torn if past_seal` and `NoFile if past_seal`, do not sit out. With the artifact already stored → enter the heal directly (the same state `key_held_share` produces for share `None`: `Acquiring(Acquire::Logs(Box::new(self.heal_over(epoch, &committee, set))))`); without an artifact → `Acquiring(Acquire::ArtifactForShare)`, which on artifact arrival (`:1750-1760`) goes through `key_held_share` → share `None` → heal. Establish by reading `key_held_share`, `drive_acquisition` (`:3803`), `try_recompute` (`:3941+`) and `recompute_scoped` (`ceremony.rs`) that (a) the heal fetches the pinned bodies by exact `(dealer, hash)` and (b) the recompute derives this node's share from a dealer's REVEAL of its point when the node never acked — cite the exact lines; if (b) is not what the primitive does, **STOP** (record, do not work around). Torn: decide by code whether the torn file must be removed before the heal (`parse_journal`, `load_journal`, `share_state.rs`) — a torn file must not make `held`/`want` lie; say what you chose and why. The `past_boundary` / `Acquiring(ArtifactForCeremony)` doc rule at `:3130-3133` and the `Sealed`-entered drive at `:3807-3830` must stay consistent — read them.
2. **Failure terminal**: a player whose point was NOT revealed (it acked before dying and its journal is gone) cannot heal. Establish what `try_recompute` does then (`MissingPlayerDealing ⇒ Unrecoverable`, retryable ⇒ stay in `Acquiring(Logs)` fetching forever?). The absentee heal must end in a terminal (`Unrecoverable{key}` or `SatOut{key}`) once every pinned body is held and the recompute still yields no share — no unbounded refetch loop, no per-tick log spam. If that requires a new small rule, it belongs in this заход; keep it minimal.
3. **Tests**: invert the three unit tests (`:11888`, `:11968`, `:13484`) to the new cell and rename them to what they now prove; add a unit for the failure terminal (point not revealed → terminal, not a loop). **Mandatory stand test** in `testbed/tests.rs` or `testbed/preconditions.rs`: a member absent for the whole deal window of a mint epoch, restarted after the seal with NO journal for it (use the stand's restart machinery — `Snapshotter`/`run_held_lag_over_donor_archive`-style, see `Ex-4.1c`/`restart_replays` tests for the pattern), reaches `Keyed` for that epoch and signs σ in the carry-forward epochs; assert on the actor's phase (`keyed`) AND on a σ-participation fact of that node, not on a log line. Red-by-liveness = STOP (record).
4. **Mutations** (with rollback and md5 before/after, verbatim in the journal): (M1) restore `SatOut` in the `NoFile if past_seal` arm — the stand test must go red; (M2) the heal enters but `want` is computed as empty — the stand test or the unit must go red.
5. **Docs in the same change**: `.claude/dpos_architecture/00_preamble.md:259-264`, `08_node_integration_crates_node_bins_fluent.md:1028,1092-1107,2154-2160` (state table, `recover` table, the "sits out" prose), `13_*` if it names the cell (`grep -rn "SatOut\|sits out\|sit out" .claude/dpos_architecture`), `DPOS_ARCHITECTURE_CHANGELOG.md`, `verified-against`. Do NOT edit `history/E5-BEACON-DESIGN.md`.

## Rules

- **Spawn no agents** — not for the work, not to check yourself: the spec and the code must sit in one context. Git read-only.
- Writable files: `beacon/actor.rs`, `beacon/ceremony.rs` and `beacon/share_state.rs` ONLY if step 1/2 proves a change there is unavoidable (say so), `testbed/{tests,preconditions,stand}.rs`, `.claude/dpos_architecture/*.md`, `DPOS_ARCHITECTURE_CHANGELOG.md`, journal `.dpos-study/history/E5-3-D.md`. Nothing else; no `devnet/` changes.
- No reformatting of untouched code; no `#[allow]`; no new `unwrap`/`expect` on production paths; no new `pub` out of `beacon/`. Comments: only a *why* the code cannot show, one line; no change notes, no references to this brief.
- Cargo (`CARGO_BUILD_JOBS=12`): `cargo test -p fluentbase-consensus --lib beacon::actor`, your stand tests by name (`--features dpos-devnet-byzantine` for the stand), `cargo test -p fluentbase-consensus --lib testbed::preconditions`, `cargo clippy -p fluentbase-consensus --all-targets`, `cargo fmt --check`. The full gate set is run by the orchestrator — do not run `--lib` whole; never run the stand in parallel with other cargo.
- Every claim in the journal carries a `file:line` you opened. `[KNOWN]` only for what you read or ran in this session.
- **STOP** (record, do not work around): the recompute primitive cannot derive a share from a reveal; the stand test is red by liveness; the change needs a file outside the list; the heal cannot be given a terminal without redesigning `try_recompute`.

## Journal `history/E5-3-D.md` — §0

1. Cargo results verbatim. 2. The heal path for an absentee, line by line (`recover` → `Acquiring` → artifact → `key_held_share` → `heal_over` → fetch → `try_recompute` → `Keyed`), with the reveal rule cited. 3. Torn handling and why. 4. Failure terminal: what happens when the point was not revealed, and how the loop is bounded. 5. The stand test: what it asserts, what it would look like with the old cell (the red-before-fix run, verbatim). 6. Mutations verbatim. 7. Д-5.3Д-1: what differs from the design §5.2/§5.4, why, П/Д unchanged. 8. Out of scope. 9. Weakest — ranked.

End your reply in ≤ 20 lines: cargo, the new cell, the stand test, mutations, weakest.

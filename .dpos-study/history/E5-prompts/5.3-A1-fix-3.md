# Третий круг захода А1 строки 5.3 — после dsh-ревью круга 2 (2A: стор/окно/переходы; 2B: гейт/durable Conflict/digest/off-poly/тесты)

Ты — исполнитель (Opus 5) в СВЕЖЕМ контексте. Репозиторий `/home/djadjka/Work/fluentbase`, HEAD `60d6c3e3`; рабочее дерево несёт незакоммиченный заход А1 (два круга, 13 файлов под `crates/`). Пути без префикса — от `crates/dpos/consensus/src/beacon/`. `.dpos-study/history/E4-ORCHESTRATOR.md` не читать, не трогать.

Вход, по порядку: (1) `git diff HEAD` (≈8000 строк; `actor.rs` продакшн `:1-3980` — автомат, читай целиком); (2) постановки `.dpos-study/history/E5-prompts/5.3-A1-impl-1.md`, `5.3-A1-fix-2.md`; (3) журнал `.dpos-study/history/E5-3-A1.md` (два круга); (4) ревью круга 2: `history/E5-3-A1-DSH-2A.md` (DA-верdicts + EA-01…EA-10) и `history/E5-3-A1-DSH-2B.md` (DB-верdicts + EB-01…EB-10) — часть (А) обоих подтвердила закрытие всех находок круга 1. Всё в (3)–(4) — реляция; проверяй по коду; REJECTED с file:line допустим.

## Что оркестратор проверил сам

- **EA-01/EB-04 BLOCKER** [KNOWN]: `ArtifactStore::note_divergent` (`artifact.rs:566-582`) пишет второй артефакт только в RAM-карту `divergent`; маркер `persist_conflict` кладёт актор в `stop_signing` на следующем тике (`actor.rs:1769-1786`). Рестарт в этом окне теряет свидетеля ⇒ `recover` ⇒ `Keyed` по первому значению.
- **EB-01 SERIOUS** [KNOWN]: `stop_signing` сначала `drop_share` (файл доли удалён), потом `persist_conflict`; смерть между ними или `Err` записи маркера (только warn) ⇒ ни маркера, ни доли ⇒ на рестарте журнал `Present` ⇒ resume ⇒ finalize ⇒ `Keyed` — терминал забыт.
- **EB-02 SERIOUS** [KNOWN]: мост `deliver` (`artifact.rs:1650-1678`): `store.get` → `None` → `store.insert(epoch, artifact)` (move); проигрыш гонки с инстансом (`insert` вернул `false`) — ни `note_divergent`, ни adopt: второе кворум-сертифицированное значение исчезает, `Conflict` не достижим асимметрично.
- **EB-03/EA-04 MODERATE** [по отчёту, механизм читаем]: `attempted = true` до `load_journal`; `NoFile`/`Torn`/`Err(other)` при пустом `want` ⇒ вечная парковка в `Acquiring(Logs)` без `Stalled` и без gauge.

## ПРАВИТЬ

**Блок I:**
- EA-01/EB-04 + EB-01: **стор — владелец и durable-свидетель конфликта.** `note_divergent` персистит маркер конфликта САМ (у стора есть `with_persistence`/каталог; если маркер живёт в `share_dir` актора, а не в каталоге стора — дай стору путь или перенеси маркер к артефактам; выбери одно место и запиши Д-nn). `stop_signing`: порядок — сначала маркер (если ещё не записан стором), затем `drop_share`; `Err` записи маркера ⇒ `error!` + gauge, долю всё равно снять, но на рестарте `recover` при `divergent` из стора ⇒ `Conflict` (стор перечитывает свои маркеры при `with_persistence`). Тест: конфликт отмечен стором → рестарт ДО тика актора → `recover` ⇒ `Conflict`; тест: смерть между маркером и `drop_share` — рестарт ⇒ `Conflict`, доля снята.
- EB-02: в `deliver` при проигрыше гонки `insert` — `note_divergent` с проигравшим значением (не терять артефакт: `insert` возвращает его назад или сравнивать по клону); симметрично инстансу. Тест на гонку (мок стора, `insert` возвращает `false`).
- EB-03/EA-04: `attempted` ставить ПОСЛЕ успешной загрузки входов; `Err(other)`/`NoFile`/`Torn` при пустом `want` ⇒ `Stalled{reason}` (защёлка + gauge), не молчание; повторная попытка при любом новом входе (лог/артефакт/тик после `Torn`?) — определи и запиши.

**Блок II:**
- EA-02/EB-06: `debug_assert_settled` на выходе из `on_message` и `on_resolver_message` тоже; три `let Some(EpochState::X) = self.take_state(..) else { continue }` — не терять слот (вернуть состояние на `else`).
- EA-03: маркеры конфликта и слоты-терминалы — чистить и в sweep (не только в одноразовом `reconcile_journals`).
- EA-05/EB-05: одна функция границы окна для `decide_window` и `decidable`.
- EA-07: `dkg_engine.rs:494-499` — `resolve_artifact` сравнивает по полному digest (с confirms) — перевести на `value_digest`.
- EA-08: follower hand-off `let _ = adopt.try_send` (`artifact.rs:1021-1027`) — warn + счётчик как у `ArtifactBridge::adopt`.
- EA-10: испорченный маркер (≠64 байта) — fail-closed: считать конфликтом (`Conflict`) с ERROR, не игнорировать.
- EB-07: тест `a_conflict_survives_a_restart` — с файлом доли на диске (проверить, что доля не переживает терминал).
- EB-08: HELP-текст `dpos_dkg_stalled` — все девять причин.
- EB-09/EB-10: текст warn `Full`; паритет guard `logs.is_empty()` между `on_artifact` и `reconcile_with_store`.
- EA-09: `enter` на существующем слоте — не терять `stalled`.

**ЗАПИСАТЬ:** EA-06 (conflict без слота — снимается тем, что стор персистит и `recover` читает), остальное из части (А), что RECORDED.

Правила: агентов не запускать; git только чтение; файлы — те же 13 + `artifact.rs`/`share_state.rs`/`dkg_engine.rs` (уже в диффе) + доки `.claude/dpos_architecture/` (durable-свидетель конфликта — у стора) + журнал `E5-3-A1.md` («Третий круг»); никаких `#[allow]`/`unwrap` на продакшн-пути; file:line; `CARGO_BUILD_JOBS=6`; стенд не гонять параллельно.

Ворота — все одиннадцать verbatim (база круга 2: `--lib` 711/0, стенд 58/0 и 49/0 @662, node 57/0, reader 64/0, slasher 16/0, clippy 2 чужих / 0 своих, с фичей 0, fmt 0, doc 6, harness 2/2086/10). Мутации с md5: (M1) `note_divergent` без персиста — тест «рестарт до тика» красный; (M2) `stop_signing` в старом порядке — тест «смерть между» красный; (M3) `deliver` без `note_divergent` на проигрыше — тест гонки красный; (M4) `attempted` до загрузки — тест `Stalled` при `Torn` красный. Ответ — таблица EA/EB → решение → file:line → доказательство, ворота, md5.

<tone_preference>Кратко и по существу.</tone_preference>

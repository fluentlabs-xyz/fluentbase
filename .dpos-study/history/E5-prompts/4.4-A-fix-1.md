# Строка 4.4, третий проход: 15 находок ревью

Ты — исполнитель правок (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база `HEAD` = `0dd39f60`. Объект — незакоммиченное рабочее дерево строки 4.4. Ревью — `.dpos-study/history/E4-4-A-REVIEW.md` (15 находок `D-01…D-15`), журнал захода — `.dpos-study/history/E4-4-A.md`, постановка первого круга — `.dpos-study/history/E5-prompts/4.4-A-impl-1.md`, строка плана — `.dpos-study/PLAN.md:106`.

**Три находки оркестратор открыл и подтвердил САМ** (`E5-ORCHESTRATOR.md`, квитанции К-92…К-95) — их механизм не оспаривается, оспаривать можно только предложенную форму правки, и тогда с кодом в руках:

* **D-01 (BLOCKER).** `get_latest` (`crates/node/src/cert_follow/upstream.rs:100-107`) — `tx.send` плюс голый `rx.await` без таймаута; `.ok()?` ловит только закрытый канал, а живой-но-нечитающий актор `None` не даёт. Единственный `self.mailbox_rx.recv()` стоит на `:258`, внутри ВНУТРЕННЕГО цикла, достижимого только после успешного `subscribe_events`; при неудачном коннекте внешний цикл `:206-217` делает `warn!` + `sleep(backoff)` + `continue`, мейлбокс не трогая. Блок `:300-336` — коалесcирование `Rotate`, не путь разрыва. Итог: `up.get_latest().await` (`dpos.rs:3189`) висит вечно при настроенном-но-мёртвом апстриме; арм-цикл не проворачивается, `block_hash(activation)` не перечитывается. На `HEAD` тот же узел опрашивал активацию раз в 2 с и её замечал.
* **D-02 (SERIOUS).** `rf_num`/`rf_hash` выводятся один раз на `dpos.rs:3116`, вне цикла; внутри перечитывается только `activation_hash`. Половина «ни одна эпоха не читается на `rf_hash`» поллингом не лечится никогда — поздний `commitEpochCommittee` появляется на позднем блоке, не на этом пинованном. А `warn!` `:3318-3326` обещает «Polling (no give-up)» и называет `commitEpochCommittee has not run`.
* **D-08.** Арм `:3153-3402` не покрыт ни одним тестом: `launch_follower` вне `dpos.rs` зовёт только продакшн (`crates/node/src/cert_follow/mod.rs:258`), и `testbed/fakes.rs:1401` признаёт «The stand never builds a fresh-datadir follower».

## Правила

1. **Агентов не запускать** — ни для работы, ни для проверки себя. Правка и её обоснование обязаны сидеть в одном контексте.
2. **Git только на чтение**: никаких `add`/`commit`/`checkout`/`stash`/`restore`/`reset`. Коммиты делает оркестратор.
3. `.dpos-study/history/E4-ORCHESTRATOR.md` не читать и не трогать — файл чужой сессии.
4. Каждое утверждение в журнале — `file:line`. Комментарии и доки — не свидетельство; свидетельство — код и твой прогон.
5. **`#[allow]` запрещён.** Если он кажется нужным — меняй форму (в первом круге так появилась `PeerProbes`, и это была верная реакция).
6. **Никаких таймеров, флагов и особых случаев там, где есть событие.** Таймаут поверх висящего вызова — крутль, а не правка: чини причину.
7. `devnet/` менять МОЖНО, но только полноценным решением. Правка, единственный смысл которой — сделать тест зелёным, запрещена. Правка, которая приводит утверждение кейса в соответствие с тем отказом, который на новом пути действительно происходит, — законна.
8. Читать целыми строками; поиск по коду — только `git grep` (под `crates/dpos/consensus/src/.claude/session-reads/` лежат gitignored журналы чужих сессий).
9. `make smoke-*` НЕ запускать — живая приёмка за оркестратором. `make harness-test` запускать обязательно.
10. `CARGO_BUILD_JOBS=6`; стенд не гонять параллельно с другой сборкой.
11. Если среда запретит команду — скажи это прямо в §0, не подменяй прогон чтением.

## Файлы на запись

`crates/dpos/consensus/src/dpos.rs`, `crates/dpos/consensus/src/cold_start_jump.rs`, `crates/dpos/consensus/src/beacon/artifact.rs`, **`crates/node/src/cert_follow/upstream.rs` (расширение границы под D-01, см. ниже)**, `devnet/local-dpos-smoke/dpos_harness/cases/smoke/verdicts_follow.py`, `.../cases/smoke/asserts_follow.py`, `.../tests/test_smoke_follow_cases.py`, `.../tests/test_smoke_follow_verdicts.py`, и журнал `.dpos-study/history/E4-4-A.md` (дописывай раздел «третий проход», прежнее не переписывай).

`.claude/dpos_architecture/**` НЕ трогать — это Ф7 оркестратора. Валидаторскую половину (`resolve_cold_start_kind`, `ColdStartKind`, арм `FreshMigration`) не трогать вовсе.

## Что делать — по находкам

### Исправить

* **D-01.** Корень — в акторе, а не в вызове: запрос, который нельзя обслужить, обязан получить `None`, а не тишину. Сделай мейлбокс обслуживаемым и в состоянии «соединения нет»: во внешнем цикле (путь неудачного коннекта/подписки) сообщения должны вычитываться и получать отрицательный ответ, а не копиться. Проверь при этом ВСЕХ вызывающих (`get_finalization`, `get_finalization_everywhere`, `get_epoch_artifact`, `get_latest`, `rotate`) и скажи в журнале по каждому, что меняется: для gap-repair `None` означает «повторю на следующем подметании», и это уже документированное поведение — но убедись по коду, а не по этой фразе. Таймаут на стороне вызова — НЕ решение. Границу списка файлов расширяю ровно на этот файл и ровно под эту причину.
* **D-02.** Перечитывай якорь внутри цикла, а не пинуй его до цикла: тогда поздний `commitEpochCommittee` становится видимым и обещание в `warn!` становится правдой. Проверь всех потребителей `rf_hash` в арме (в т.ч. чтение комитета и `fetch_verified_boundary`) и скажи, что перечитывание для каждого из них безопасно. Если для какого-то потребителя пин был НУЖЕН — назови его и оставь пин именно там, объяснив разделение.
* **D-03.** `.max(floor)` может поднять цель выше читаемого окна. Правильная форма: если читаемое окно ниже `floor`, входа ещё НЕТ (это `ChainBelowActivation`), а не «попросим недостижимую высоту». Плюс юнит, который краснеет на старой форме.
* **D-04.** Своя поверхность отказа вместо чужой: собственное сообщение и собственная метрика вместо заимствованных у другого пути; текст не должен обещать того, чего лончер не делает.
* **D-06 + D-07.** Фаза 3 каскада: свидетель отказа СМЕНИЛСЯ из-за перестановки ветвей (негодный checkpoint теперь останавливается внутри `sync_to_checkpoint` и до `assert_l1_checkpoint` не доходит). Приведи утверждение кейса к отказу, который на новом пути действительно происходит, — и назови в журнале, какой именно это отказ, по коду. `CC_REJECT_S`: строка плана называет 420, в дереве 240 и юнит пинует 240; бюджет измерить нельзя, пока тир-1 красный, поэтому **число не выдумывай** — приведи в согласие текст, юнит и то, что кейс реально проверяет, а расхождение с планом оставь явной открытой приёмкой в журнале. Парк⇒фатал ниже активации при необслуживаемом checkpoint-е (D-07) — назови отклонением `4.4а-Д-nn` с механизмом.
* **D-09, D-10, D-15.** Недостижимый фатал назвать внутренним инвариантом так же, как названы два других; вторую `const POLL` с той же каденцией свести к одной; транзиентный промах чтения посадки — ретрай (это гонка видимости #17), не фатал запуска.
* **D-11.** Свидетель фазы 1 должен быть специфичен для ВХОДА, а не опираться на позицию и на недостижимость другого арма.
* **D-12.** Либо восстанови хвост текста и частоту, либо назови отклонением явно — сейчас восстановлен только уровень, и это не названо.
* **D-13.** Числа в журнале: «625 → 626» не измерено (база `filtered out` 619, рост +7), и в той же таблице подменена база ворот 1. Исправь журнал на измеренное.

### Записать, не исправлять

* **D-05** — один запрос на одну высоту и предположение монотонности: у валидатора, вошедшего прыжком после отставания, средний диапазон отсутствует. Запиши механизм и адрес в §3 журнала; правка здесь не делается.
* **D-08** — арм без тестов. Стенд `launch_follower` не строит; завести его — отдельная работа. Запиши как остаток с адресом, НЕ пытайся построить фикстуру в этом проходе.
* **D-14** — правило 43 в `.claude/dpos_architecture/` объявило себя греп-проверяемым и счёт разошёлся: это Ф7 оркестратора, только упомяни.

## Форма ответа и журнал

Дописать в `E4-4-A.md` раздел «третий проход»: по каждой из 15 находок строка `id | FIXED / RECORDED / REJECTED | что сделано | file:line | чем доказано`. `REJECTED` допустим, но только с кодом против кода — и для D-01/D-02/D-08 порог доказательства выше, их оркестратор проверил сам.

**Чувствительность.** Для D-01, D-02, D-03 и D-11 покажи мутацией, что новый тест краснеет на СТАРОЙ форме: `md5sum` до / с мутацией / после откат плюс verbatim строка падения. Для D-01 назови, каким наблюдаемым доказано, что висящий вызов больше не висит.

**Ворота — все одиннадцать**, база: `--lib` 672/0, стенд с фичей 55/0, без фичи 46/0 (`filtered out` 626), node 55/0, reader 64/0, slasher 16/0, clippy два ЧУЖИХ и ноль своих, clippy с фичей 0, `fmt` чисто, doc unresolved 6, `make harness-test` `2 failed, 2084 passed, 10 skipped` (две красные предсуществуют — их НЕ трогать; если станет три, разбирайся). Прирост тестов назови поимённо.

В финальном ответе — таблица 15 находок и числа ворот. Коротко.

<tone_preference>Кратко и по существу.</tone_preference>

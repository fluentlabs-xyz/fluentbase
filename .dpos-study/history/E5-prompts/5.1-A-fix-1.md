# Третий проход строки 5.1 — по ревью `E5-1-A-REVIEW.md` (23 находки)

Ты — исполнитель (Opus 5) третьего прохода в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база `HEAD` = `3ead5c6c`; объект — **незакоммиченное дерево строки 5.1** (четыре удаления в `beacon/`, ~20 изменённых). **Чужое** — `.dpos-study/history/E4-ORCHESTRATOR.md` — не трогать. Пути без префикса — от `crates/dpos/consensus/src/`.

## Правила (обязательные)

1. **Агентов не запускать.** Спека и код обязаны сидеть в одном контексте.
2. **Git только на чтение.** Не коммитить, не `add`.
3. **Файлы на запись**: всё `beacon/**`; `testbed/{tests,cert_inlet_tests,committee_tests,preconditions,stand,fakes}.rs`; ТОЛЬКО `#[cfg(test)]`-модули и комментарные сайты в `epoch_manager.rs`, `cert_inlet.rs`, `dpos.rs`; `.dpos-study/history/E5-1-A.md` (часть Д). **Запрещено**: продакшн-тела `epoch_manager.rs`/`cert_inlet.rs`/`dpos.rs`, `committee/**`, `plane_upstream.rs`, `node/**`, `p2p/**`, `bins/**`, `devnet/**`, `contracts/**`, `Cargo.toml`, **`.claude/**` (доки правит оркестратор)**.
4. **file:line на каждое утверждение.**
5. Без `#[allow]`, `todo!`, таймеров вместо событий, поллинга; `unwrap` на продакшн-пути — только `Mutex::lock().unwrap()`.
6. Комментарии и доки — не свидетельство. Свидетельство — код и твой прогон.
7. Читать целыми строками. Поиск — ТОЛЬКО `git grep`.
8. **Ворота — ОБА набора стенда, это условие приёма прохода**: `cargo test -p fluentbase-consensus --lib` (654 ± дельта), `… --lib --features dpos-devnet-byzantine testbed::` (**55/0**), `… --lib testbed::` без фичи (**46/0**), clippy БЕЗ фичи и с фичей, `cargo fmt --check`. Полный набор остальных крейтов — у оркестратора. «`--lib` зелёный» доводом не будет — это уже стоило строке возврата №3.
9. Правки батчем, один прогон затронутых тестов, ×3 по новым и переписанным стенд-тестам, мутации ≤ 2 с откатом и `md5sum`.

## Вход

`.dpos-study/history/E5-1-A-REVIEW.md` — читать ЦЕЛИКОМ (§0–§6). `.dpos-study/history/E5-1-A.md` — свой контекст (части А, Б, В, Г). Проект — `history/E5-BEACON-DESIGN.md` §5.3 `:618-628`, §5.4 `:629-646`, §7 строка 5.1 `:711-733`.

**Что оркестратор проверил САМ — опирайся, не перепроверяй.** B-17: `actor.rs:2329-2331` — `drive_recompute` делает `continue` на «Already holds E's share (qualified)», поэтому член С долей и БЕЗ артефакта выпадает из петли до всякого `pull`; `:2466-2474` — `acquire_mint_artifacts` явно пропускает членов. B-07: на HEAD был `consults: Arc<AtomicU32>` (`HEAD:cert_inlet.rs:2349-2350`) и ассерт на его значении (`:2374`); в дереве вместо него `mints.artifacts.epochs() == vec![2]`, истинное при любом числе обращений. B-20: тест persist-ошибки ДОЛИ существует (`actor.rs:6715`), тестов на persist АРТЕФАКТА и на ЧАСТИЧНЫЙ УСПЕХ нет ни под каким именем.

## Решения по находкам

| id | серьёзность | решение | что сделать |
|---|---|---|---|
| B-17 | SERIOUS | **FIXED** | Реализовать исход, который §5.4 называет: член живой эпохи, держащий ДОЛЮ и не держащий артефакта, обязан просить артефакт у пиров (`Acquiring{artifact}`, throttle `PULL_MIN_INTERVAL` сохраняется), а не сидеть verify-only до следующей границы эпохи. Сделай это ОДНИМ владельцем решения, а не третьей петлёй: либо ранний `continue` в `drive_recompute` (`:2329-2331`) перестаёт пропускать «доля есть, артефакта нет», либо `acquire_mint_artifacts` перестаёт исключать членов — выбери и обоснуй по коду, почему выбранное место единственное. И поправь продакшн-док `share_state.rs:26-29`, который сегодня утверждает обратное. **Порядок обязателен:** СНАЧАЛА тест из B-20 (частичный успех) и его КРАСНЫЙ verbatim, ПОТОМ правка |
| B-20 | SERIOUS | **FIXED** | Написать две отсутствующие проверки проекта §7: (а) **частичный успех** — доля на диске, партиция артефактов пуста ⇒ узел просит артефакт и становится `Keyed` по его приходу от пиров. Это твой «красный до правки», и его красный обязан быть verbatim в журнале. (б) **persist-ошибка артефакта** — артефакт ПРИНЯТ в RAM, узел продолжает верифицировать, отказ записи наблюдаем. Плюс: в журнале назови ОДНУ строку-мутацию, которая снова роняет тест (а) — оркестратор воспроизведёт красный сам, и без этой строки он не сможет |
| B-07 | SERIOUS | **FIXED** | Вернуть фальсификатор половины «rung consulted once»: пин обязан падать, если обращений к рунгу стало больше одного. Счётчик — законный способ (он и был на HEAD); если выберешь другой механизм, докажи по коду, что он падает именно на втором обращении. Доккоммент `:2313-2314` переписать по факту |
| B-03 | MODERATE | **FIXED** | `seed_material_refused_divergent` (`metrics.rs:183-189`, `:340-346`) объявлен и зарегистрирован, но не инкрементируется нигде — мёртвая поверхность, которую постановка запрещала, и оператору вечный 0 читается как «расхождений нет». Удалить вместе с регистрацией; три дока (`oracle.rs:20-23`, `:461-465`, `artifact.rs:2038-2042`), которые уже говорят «удалён», станут верными |
| B-09 | MODERATE | **FIXED** | §5.4 и постановка А.3(2) требуют «запись артефакта ПОВТОРЯЕТСЯ». Сегодня на провале `sync` — `continue` (`artifact.rs:1355-1369`), и повтор только со следующим `insert`, а артефакт приходит раз на смену комитета; свой же `warn!` говорит «lost on a hard kill, must be re-fetched». Сделать повтор (по существующему ребру часов, НЕ таймером) либо, если по коду это невозможно без выхода за список файлов, — Д-nn с точной причиной и с тестом на наблюдаемость отказа |
| B-21 | MINOR | **FIXED** | Мемо открывает партицию `beacon-key-ordinal` (`plane.rs:682-688`, `dpos.rs:198`) — имя УДАЛЁННОГО key-журнала и при этом другой тип стора (`Ordinal` → `Metadata`). Переименовать. Причина не гипотетическая: `.claude/COMMONWARE_INTERNALS.md:520` называет «wrong codec on an existing partition» паникой на init, и «всегда свежий генезис» — политика развёртывания, а не свойство кода. `dpos.rs:198` — имя партиции, разрешённый сайт |
| B-12 | MINOR | **FIXED** | Назвать исход аналога `Torn` для мемо явно: сегодня «все записи отклонены» поведенчески равно «мемо пусто» и различимо только счётчиком (`artifact.rs:1150-1169`). Либо явное состояние/лог, либо строка Д-nn с причиной, почему неразличимость безопасна |
| B-05, B-18, B-19, B-22, B-23 | MINOR/NIT | **FIXED (текст)** | Комментарии, разошедшиеся с кодом: `surface.rs:2226-2230` («the belt is gone and this gate is the whole of the property» — ремень переехал в `MintIndex::minted_at`, `artifact.rs:638-640`, свойство держат ДВА места); `committee/facade.rs:84-87` ссылается на страж в удалённом `beacon::carry` — **`committee/**` тебе запрещён, поэтому это строка в §5 для меня**; `share_state.rs:1` (первая строка модуля опровергается его же `:10`); `artifact.rs:1098-1099` (осиротевшая док-строка про параметры, которых у `key_index_over` нет); `artifact.rs:1105-1108` (`MINT_MEMO_PARTITION_SUFFIX = ""` с доком не про эту константу) |
| B-16 | NIT | **FIXED** | `artifact.rs:558,787,792,801,813,946,1128` — новые `pub` в приватном модуле рядом с `pub(crate)`; привести к однородности (`pub(crate)`), наружу крейта они и так не выходят |
| B-13 | MINOR | **FIXED (док)** | Код делает `return None` на первом нечитаемом бите и до мемо НИЖЕ дыры не доходит (`artifact.rs:645-663`), а док `:583-586` обещает «closes that for every epoch this node ever resolved». Привести док к коду; достижимость низкая — саму логику не менять |
| B-06 | MODERATE | **RECORDED + пин** | `SCHEME_RETENTION_EPOCHS` означает четыре политики, и две связаны жёстко: окно приобретения артефактов обязано совпадать с окном, в котором фасад отвечает битом. Записать все четыре смысла одной таблицей в журнал. Плюс, если это дёшево, поставить ПИН равенства двух окон (`const_assert!` либо юнит) — если дорого или требует `committee/**`, скажи и оставь записью |
| B-10 | MODERATE | **RECORDED** | Долговечное write-once мемо делает неверный `minted_at` необратимым (до строки рестарт его чистил). Записать ЦЕНУ прямо — журнал назвал долговечность «усилением правил» и цену не назвал. Не чинить: источник ошибки узок (якорь финализированный) |
| B-04 | MODERATE | **RECORDED** | `with_material` (`oracle.rs:117-124`) требует И полином, И долю, поэтому верификаторный оракул не-члена отвечает `false`/`None` там, где П-3 сделал полином отдельно доступным. Не регрессия; строка в §5 для 5.2/6.1 |
| B-14 | MINOR | **RECORDED** | `promote_gates` вакуумен при `share == None` и безопасен только гейтом выше (`surface.rs:2262-2269`, оркестратор его открыл). Записать связку одной строкой |
| B-15 | MINOR | **RECORDED** | `four_nodes_at_four_heights_…` сменил генератор отставания: смысла не потерял, но режим, который он назывался проверять (ротация), на стенде больше не воспроизводится. Строка в §5 |
| B-11 | MINOR | **RECORDED как Д-nn** | `JOURNAL_RETENTION_EPOCHS` оставлен алиасом вместо удаления (§5.3 требовал удалить). Обосновано в доке, но это «сделано иначе» — оформить отдельной строкой Д-nn |
| B-02 | NIT | **RECORDED** | «Шесть тестов» — нетто: удалено 11, добавлено 4. Поправить §0(4) журнала по факту |
| B-08, B-01 | — | **НЕ ТВОЁ** | B-08 (118 попаданий удалённых символов в `.claude/dpos_architecture/`) правит оркестратор в Ф7. B-01 — замечание про бухгалтерию ворот оркестратора, принято им. В журнал не вносить |

## Журнал — часть Д (`.dpos-study/history/E5-1-A.md`, дописать)

Д§0 Прямые ответы: (1) итог по 23 находкам — FIXED/RECORDED/REJECTED с числами, каждая ровно раз; (2) B-17: где стоит решение теперь и почему это место единственное; (3) B-20(а): красный до правки verbatim + ОДНА строка-мутация, которой оркестратор его воспроизведёт; (4) B-20(б) и B-09: что наблюдаемо при отказе записи артефакта; (5) B-07: чем падает пин теперь; (6) четыре смысла `SCHEME_RETENTION_EPOCHS` таблицей и поставлен ли пин равенства окон; (7) прогоны verbatim — `--lib`, ОБА набора стенда с числами, clippy ×2, `fmt`, ×3; (8) что из моих решений оказалось неверным по коду (REJECTED — здесь); (9) запрещённого сделано — ноль или список.
Д§1 Таблица по всем 23. Д§2 Новые Д-nn. Д§3 Раскладка коммитов. Д§4 Где проверка слабее всего. Д§5 Что уходит в 5.2/5.3/5.4 и что остаётся оркестратору (в т.ч. `committee/facade.rs:84-87`).

Ты НЕ коммитишь. В финальном ответе — только Д§0.

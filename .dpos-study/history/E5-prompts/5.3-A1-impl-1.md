# Строка 5.3, заход А1 — явный автомат DKG-церемонии (П-9, ядро)

Ты — исполнитель (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `60d6c3e3`. Пути без префикса — от `crates/dpos/consensus/src/beacon/`. Рабочее дерево чисто, кроме чужого `.dpos-study/history/E4-ORCHESTRATOR.md` — не читать, не трогать.

## Рамка

Это заход А1 строки 5.3 (`.dpos-study/PLAN.md`, `| [ ] 5.3 |`; П-9 `DECISIONS.md:92`). Карта — `.dpos-study/history/E5-ORCHESTRATOR.md`, раздел «Карта захода А». Дизайн — `history/E5-BEACON-DESIGN.md`: §5.2 автомат (`:596-616`, включая таблицу рестарта `recover(E)`), §5.3 «единственный владелец каждого факта» (`:618`), §5.4 модель отказов (`:629-646`), приёмка `:729`. Инвентарь — `history/E5-3-MAP-RESEARCH.md`: §1.2 (S0–S17 на 25 носителях), §1.3 (14 мест мульти-чтения), §3 (T1–T19 терминалы). Регистры, которые заход закрывает — прочитай в `REGISTER.md`: R-036, R-038, R-026, R-051, R-072, R-067, R-071, R-039 (переоткрыта наполовину), E5-11, E5-12, E5-15, E5-24, E5-35, E5-36, E5-43 (в дизайне §4). Уже сделано и не трогается: идентичность лога (Б, `cee2e037`), вход p2p и `refuse` (В+Г1, `6403e7b6`). НЕ этот заход: `Option`-швы → конфиг (А2), `dkg_agree.rs` внутренности, плоскость согласования, контракт.

## Что установлено оркестратором по коду (опирайся; опровергать — с file:line)

- `actor.rs` продакшн `:1-3086`; носители состояния эпохи — поля `DkgActor` `:455-607`: `ceremonies`, `deferred_reported`, `pending`, `eval_logged`, `torn_warned`, `recompute_pending`, `terminal_recompute`, `equivocations`, `agreement_announced`, `agreed_pinned`, `nondurable_logs`. Входы: `run`, `on_artifact :998`, `on_height :1327`, `on_message`, `on_resolver_message :2898`. Переходы: `drive_finalization :1732`, `maybe_start :1878`, `start_fresh :2045`, `resume_from_journal :2069`, `adopt_share :1140`, `sweep_epoch_state :1239`. Восстановление: `drive_recompute :2547`, `try_recompute_pending :2799`, `acquire_mint_artifacts :2739`.
- Дефекты, которые автомат обязан закрыть по построению: T2 — `JournalLoad::NoFile ⇒ start_fresh` без гейта по дедлайну, тогда как `Present` гейт имеет (R-036); T5 — finalize Err оставляет `player = None` до sweep (E5-12); T3 — resume Err без памяти, повтор каждый тик (S11); T11 — потеря тела предложения ⇒ инстанс не перезапускается, heal только при `now == E` (E5-11/R-026); T14 — второй артефакт за эпоху молча отбрасывается (`on_artifact`); T16 — `below_quorum`/`missing_body` — только одноразовый warn, события нет.
- **F-02/R-039 (проверено оркестратором):** live-путь `drive_finalization` вызывает `adopt_share(e, &committee, out, share)` с ЛОКАЛЬНЫМ `out` (`:1783` на предыдущем HEAD; найди текущую строку), heal — с пришпиленным outcome; под мутацией «`scoped_pinned_logs` по дилеру» жертва приняла share над непришпиленным набором с `ok=1`. Гейт adopt обязан сверять с `group_key` АРТЕФАКТА на обоих путях.
- **D-10:** `Player::resume` берёт first-recorded тело дилера, не пришпиленное (`ceremony.rs` resume); при известном `agreed_pinned` предпочитать пришпиленный хэш.
- **F-01 (В+Г1):** док `refuse` не перечисляет тихие дропы (confirm без пула, envelope≠signed epoch, отказ `is_bufferable`) — при переписывании буфера учесть или посчитать.

## Что сделать

1. **`EpochState`** — один enum на target-эпоху, `epochs: BTreeMap<u64, EpochState>` (или эквивалент), состояния по дизайну §5.2: `Idle`, `Dealing{ceremony}`, `Sealed{ceremony}`, `Agreed{ceremony, pinned}`, `Acquiring{Artifact | Logs}`, `Finalizing`, `Keyed{share}`, `KeyOnly`, терминалы `SatOut`, `Unrecoverable`, `Conflict{evidence}`; событие `Stalled{reason}` (`QuorumMissing`, `PersistFailed`, `Conflict`, `BodyLost`, …) — WARN один раз на (эпоха, причина) + gauge, защёлка. Имена и состав — твои, но **каждое из S0–S17 инвентаря отображается ровно в одно состояние или удаляется с причиной** — таблица трассируемости обязательна. Носители из списка выше либо уходят внутрь состояния, либо остаются с обоснованием «не состояние эпохи» (например, `pending` — буфер входа). Фаза эпохи читается в ОДНОМ месте (`match`), а не по сочетанию полей.
2. **Переходы** — по таблице §5.2 дизайна, включая: `Torn`/`NoFile` одним гейтом по дедлайну (`h < seal ⇒ Dealing с reconstruct`, `h ≥ seal ⇒ SatOut`); finalize `Err(MissingPlayerDealing) ⇒ Unrecoverable`, `Err(other) ⇒ Acquiring{logs}`; body-lost ⇒ `Acquiring{artifact}` немедленно (границу с `dkg_engine` — только сигнал актору; если сигнал требует нового канала — Д-nn); второй quorum-артефакт за эпоху ⇒ `Conflict` (проверенные значения только: артефакт прошёл кворум-проверку); evidence эквивокации (Б, `equivocations`) ⇒ данные `Conflict{evidence}` БЕЗ остановки подписания (дизайн: остановка — только на двух quorum-артефактах/двух σ; для пары логов — бан и evidence, как в Б); persist Err share ⇒ остаёмся не-`Keyed`, `Stalled{PersistFailed}`, повтор по часам; persist Err артефакта ⇒ артефакт принят в RAM, запись повторяется.
3. **`recover(E)`** — одна функция по таблице рестарта §5.2 (share есть/нет × артефакт есть/нет × журнал Present/Torn/NoFile × `h` до/после seal), с веткой частичного успеха (S14: share есть, артефакта нет ⇒ `Acquiring{artifact}` с сохранённым share). D-10 внутри: resume над пришпиленным телом, если `agreed_pinned` известен.
4. **Гейт adopt (F-02):** `validate_share_on_poly` на ОБОИХ путях против `group_key`/полинома ПРИШПИЛЕННОГО артефакта; локальный `out` — только как источник share. Тест: мутация `scoped_pinned_logs` по дилеру ⇒ live-путь отказывает (demote/`Refused`), не `ok=1`.
5. **Тесты**: четыре клетки {Torn, NoFile}×{до seal, после seal} (юниты актора); `Stalled{QuorumMissing}` + ERROR-строка на фикстуре ниже кворума (стенд: `>f` торн-журналов или удержание — выбери воспроизводимую); `Conflict` от двух quorum-артефактов (юнит с двумя валидными артефактами разных наборов) и НЕ от одного поддельного; `recover(E)` — таблица клеток юнитом на каждую; existing 11 тестовых сборок актора не меняются (конструктор тот же); стенд с фичей и без — зелёные число в число. Прирост тестов по именам.
6. **Доки в том же изменении**: `.claude/dpos_architecture/{03_epoch_machinery,08_…,13_…,00_preamble}.md` — автомат (состояния, переходы, терминалы, `recover`), `verified-against`; `git grep` по удалённым носителям (`torn_warned`, `eval_logged`, `deferred_reported`, `terminal_recompute`, `recompute_pending`, `agreement_announced`) в доках и коде — ни одного устаревшего.

## Правила

- **Агентов не запускать** — ни для работы, ни для проверки себя: спека и код — в одном контексте. Git — только чтение.
- Записываемые файлы: `beacon/{actor,ceremony,share_state,artifact,dkg_engine,metrics,plane}.rs` (последние три — только в части сигнала body-lost / события `Stalled` / проводки), их тесты, `testbed/{stand,tests}.rs`, `.claude/dpos_architecture/*.md`, журнал `.dpos-study/history/E5-3-A1.md`. `dkg_agree.rs`, `log_resolver.rs`, `confirmations.rs`, `crates/node/**` — не трогать; нужно — стоп и запись.
- **Ревьюируемость — условие приёмки:** не переформатировать и не переименовывать нетронутое; не менять порядок функций без нужды; дифф должен читаться как «носители → enum, переходы → таблица»; если объём выходит за ~1500 строк продакшна — остановись после п.1–3 и запиши, что осталось (п.4–5 тогда уйдут во второй круг).
- Каждое утверждение в журнале — `file:line`; комментарии и доки — не свидетельство; числа считать. Никаких `#[allow]`, новых `unwrap`/`expect` на продакшн-пути, новых `pub` наружу модуля. `CARGO_BUILD_JOBS=6`; стенд не гонять параллельно с другой сборкой.
- **СТОП** (запись в журнал, не обходить): нужно менять решение `DECISIONS.md` (П-3, Д-1 — пришпиливание; Д-6); `Conflict` достижим от непроверенного значения; enum невозможен без изменения `dkg_agree`; стенд краснеет по живости на центральном пути (keyed/keyless, C7/C8, оба R-002-теста, `five_nodes_…`).

## Ворота (все, verbatim; база на HEAD `60d6c3e3`)

`--lib` 688/0 · `--lib --features dpos-devnet-byzantine testbed::` 58/0 · `--lib testbed::` 49/0 (639 filtered) · node 57/0 · staking-reader 64/0 · slasher 16/0 · clippy три крейта — два чужих (`node/src/dpos.rs:1991`, `staking-reader/.../epoch_transition.rs:3024`), ноль своих · consensus с фичей 0 · fmt exit 0 · doc unresolved 6 · harness 2 failed / 2086 / 10.

**Мутации (с откатом и md5):** (M1) вернуть `NoFile ⇒ start_fresh` без гейта — клетка «NoFile после seal» красная; (M2) finalize Err ⇒ старое поведение (`player=None`, без перехода) — тест `Acquiring{logs}` красный; (M3) гейт adopt против локального `out` — тест F-02 красный; (M4) `Conflict` от одного артефакта без кворум-проверки — тест «не от поддельного» красный.

## Журнал `history/E5-3-A1.md` — §0

1. Ворота verbatim. 2. Таблица трассируемости S0–S17 → состояние (и T1–T19 → переход/терминал). 3. Носители: что удалено, что осталось и почему. 4. Таблица переходов как реализована (вход × состояние → состояние/событие) — сверка с §5.2 построчно, расхождения — Д-nn. 5. `recover(E)`: клетки таблицы рестарта и тест на каждую. 6. F-02: что именно сверяется на обоих путях; результат мутации M3. 7. D-10. 8. Мутации M1–M4 verbatim. 9. Что не вошло (для А2) и почему. 10. Вне рамки (по строке). 11. Слабее всего — ранжируй. §1 «оставил как есть» — непустой.

Финальный ответ — §0 сжато и таблицы п.2 и п.4. Не останавливайся между шагами; не спрашивай — решай и записывай допущение; СТОП только по названным условиям.

<tone_preference>Кратко и по существу.</tone_preference>

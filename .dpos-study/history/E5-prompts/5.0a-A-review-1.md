# Ревью строки 5.0а, заход А (стендовый `CertInlet`: вторая продюсерская дверь в marshal узла стенда)

Ты — ревьюер (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, база `HEAD` = `bec013a7` (`git rev-parse HEAD`). Объект — **незакоммиченное рабочее дерево**: `crates/dpos/consensus/src/testbed/stand.rs` (+244), `crates/dpos/consensus/src/testbed/mod.rs` (+4), новый `crates/dpos/consensus/src/testbed/cert_inlet_tests.rs` (495 строк). **Всё остальное в `git status` — чужое**: `.dpos-study/PLAN.md` (правка оркестратора), `.dpos-study/history/E4-ORCHESTRATOR.md` (файл другой сессии) — не трогать. Пути без префикса — от `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`.

## Правила

1. **Агентов не запускать.** Спека и код обязаны сидеть в одном контексте.
2. **Git только на чтение.** Единственный файл на запись — `.dpos-study/history/E5-0a-A-REVIEW.md`. Мутации кода ради проверки чувствительности теста — только с откатом и `md5sum` до/после.
3. Каждое утверждение — `file:line` дерева и, где есть «до», `HEAD:file:line`.
4. Комментарии, доки, `.claude/dpos_architecture/` и журнал исполнителя — **не свидетельство**; свидетельство — код и прогон, который ты сделал сам. Для commonware — `.claude/COMMONWARE_INTERNALS.md` ДО чекаута.
5. **Полнота, не фильтр**: все находки, включая мелкие и неуверенные, с серьёзностью (BLOCKER / SERIOUS / MODERATE / MINOR / NIT) и «чем пытался опровергнуть»; не объединять разные находки ради краткости — фильтрует оркестратор.
6. Читать целыми строками (`Read` / `sed -n 'N,Mp'`); никогда `cut -c`/`head -c`. Поиск по коду — ТОЛЬКО `git grep`: под `crates/dpos/consensus/src/.claude/session-reads/` лежат gitignored журналы чужих сессий.
7. `CARGO_BUILD_JOBS=6`; `Blocking waiting for file lock` — ждать. Стенд не гонять параллельно с другой сборкой.

## Вход

- **Строка плана**: `.dpos-study/PLAN.md`, блок «### Э5.», строка `5.0а` (строка 111 файла) — задание дословно.
- **Проект**: `.dpos-study/history/E5-BEACON-DESIGN.md` §5.1 (`:556-595`), §5.4 (`:629-646`), §7 строки 5.2 и 5.4 (`:711-733`), §0.7(а)/(б).
- **Постановка**: `.dpos-study/history/E5-prompts/5.0a-A-impl-1.md` (в ней — якоря, проверенные оркестратором, и решения оркестратора по форме).
- **Журнал**: `.dpos-study/history/E5-0a-A.md` — читать **ПОСЛЕ** диффа; §0–§5 — заход, часть Б — возврат на красных воротах.
- **Ворота**: набор УЖЕ прогнан оркестратором на этом дереве (решение пользователя 2026-09-12: один прогон набора на заход). Вывод — в `/tmp/claude-1000/-home-djadjka-Work-fluentbase/f6f42c91-6188-4d9c-ae64-a1e9faed9028/scratchpad/gates/`: прогон **`a1-*`** — первый, на нём ворота были КРАСНЫЕ по одному пункту (clippy вырос 2 ⇒ 3, `unused import: Role`), и заход вернулся исполнителю (возврат Ф3→Ф2 №1, часть Б журнала); прогон **`a2-*`** — после правки, ЗЕЛЁНЫЙ, и это текущее дерево (`a2-tree.md5`, сверь `md5sum -c`). `a2` — частичный набор (clippy ×2, `fmt`, lib, стенд с фичей и без): правка коснулась только `testbed/cert_inlet_tests.rs` (файл под `#[cfg(test)]` внутри `fluentbase-consensus`), поэтому `node`/`reader`/`slasher`/`doc` перенесены из `a1`. Прочитай оба и приведи `test result` verbatim; **НЕ перегоняй набор**. Оцени заодно, законно ли это сокращение — если нет, это находка. Сам гоняй ТОЛЬКО точечные тесты (`--exact`), мутации и то, что нужно под конкретную находку. База на `bec013a7` (мои прогоны): lib 686/0, стенд с фичей 48/0, стенд без фичи 40/0, node 55/0, reader 64/0, slasher 16/0, clippy ровно два чужих (`large_enum_variant` в node, `MutexGuard` в staking-reader), clippy с фичей 0, `fmt --check` чисто, doc unresolved 6.

## Форма результата `.dpos-study/history/E5-0a-A-REVIEW.md` (§0–§6 как `E5-0-REVIEW.md`)

**§0 Прямые ответы** — нумерованные, каждый с [KNOWN]/[ГИПОТЕЗА] и file:line:

1. **Ворота** verbatim из файлов оркестратора; расхождения с числами журнала назвать. Счёт `#[test]` + `#[tokio::test]` по каждому изменённому файлу: `HEAD` против дерева.
2. **Строка плана закрыта или нет, по пунктам.** Строка требует: опциональную задачу над продакшн-`CertInlet::new(marshal, committee, ctx)` в `build_node`; `.with_tee(LiveFrontierTee{dkg_height_tx, plane_clock})`, `.with_randomness(beacon)`, `.with_rotate(счётчик)`; вход — поток `UpstreamFinalized`, собранный из архива ДРУГОГО узла либо через `CountingUpstream`/`plane_upstream::new_bridge`; наблюдаемые `record_data_fault`, `consecutive_faults`, тик tee; роль «валидатор с отстающим EL и живым inlet-ом». По каждому: сделано / сделано иначе (тогда какое Д и держится ли причина по коду) / не сделано.
3. **Цикл задачи inlet-а** (`stand.rs`, спавн после `outer.build`). Центральный вопрос: **может ли этот цикл крутиться без продвижения виртуального времени?** Пройди по коду: `PlaneUpstreamHandle::fetch_one` (`plane_upstream.rs:610-662`) — какие пути возвращают `None` и сколько виртуального времени каждый стоит; в частности путь, где `deliver` снял запись `waiters` (`:486-490`) и oneshot-sender дропнут (комментарий `:641-647`). Журнал §0(3) утверждает, что «`None` уже стоил `FRONTIER_FETCH_TIMEOUT` = 8 с» — проверь это утверждение прямо и скажи, верно ли оно на КАЖДОМ пути. Если есть путь, на котором `None` приходит без расхода виртуального времени, — назови, что тогда ограничивает цикл, и является ли это дефектом или свойством (keyless-тест печатает `plane_dropped = 840` при `ingests = 3332` — сколько виртуального времени ушло на прогон и сходится ли это с твоим ответом).
4. **Тик tee.** Дренаж `tee_rx` через `try_recv` после `ingest` — корректен ли он по коду `cert_inlet.rs` (tee дёргается синхронно и только на чистом исходе? может ли в канале лежать высота от ПРЕДЫДУЩЕГО серта?); теряется ли что-нибудь при `try_recv` (ёмкость канала против числа тиков за один ingest); что происходит при `Beacon::Static` + `cert_inlet` (кто держит приёмник настоящего `dkg_height_tx` в этом случае — посмотри порядок строк вокруг `let dkg_height_tx = matches!(cfg.beacon, Beacon::Live).then_some(dkg_height_tx)`), и достижима ли эта комбинация из конфигурации.
5. **Наблюдаемые и их нетавтологичность.** `consecutive_faults` приватен и геттера не добавляли; журнал утверждает `faults = ingests − tee.len()` точно при `defers == 0` и `rotations == faults / MAX_UPSTREAM_FAULTS` точно. Проверь каждую посылку по коду `cert_inlet.rs::ingest`: исчерпывают ли «чистый ingest (теет)» и «data-фолт» все исходы при `defers == 0` — в частности, есть ли исход, который не теет, не фолт и не defer (посмотри ВСЕ `return` в `ingest`, включая арм `epoch_bind` и любой ранний выход). Если такой исход есть — арифметика теста неверна, это SERIOUS.
6. **Три теста: что каждый доказывает и что его фальсифицирует.** По каждому — назови ассерт, который упал бы, если бы свойство не держалось, и проверь, что он НЕ вакуумен. Отдельно: (а) в `an_inlet_on_a_healthy_member_…` ассерт `tee_heights == (1..=ingests)` пинует непрерывность — зависит ли она от ПОРЯДКА (успел ли executor продвинуть тир-F до следующего `chain.tip()`), и если да, то это пин планировщика или свойство? (б) в `a_forged_seed_slot_…` — действительно ли `ingested_forged` считается корректно (высоты, которые форжер подделал И которые жертва не тее-нула: может ли высота попасть в `forged_heights`, но не быть подана ЭТОЙ жертве?); (в) в `a_keyless_admission_…` — верно ли, что `teed_top <= window_top` и `teed_top > …` вместе пинуют именно окно, и не пройдут ли они при другом механизме останова.
7. **Находка журнала §0(9)** («пин `defers > 0` в стенде недостижим», две независимые причины). Оркестратор ПОДТВЕРДИЛ обе по коду (`committee/store.rs:218-224`, `committee/mod.rs:616-622`, `testbed/fakes.rs:621-623`; `plane_upstream.rs:445-449`, `:486-490`). Твоя задача — не перепроверять их, а ответить: (а) есть ли ТРЕТИЙ путь подать inlet-у серт эпохи вне окна чтения этого узла (например, минуя плоскость), и стоит ли его строить в 5.0а; (б) правильно ли переписанный тест ПИНУЕТ механизм, а не замалчивает нуль.
8. **Граница и гигиена.** Новое поле `StandConfig` — ровно одно? Изменено ли хоть одно СУЩЕСТВУЮЩЕЕ поле (условие разделения с параллельной строкой 5.1)? Тронут ли `testbed/tests.rs` (запрещён), `fakes.rs`, `byzantine_roles.rs`, `preconditions.rs`, `committee_tests.rs`? Новые `pub`, `#[allow]`, `todo!`, таймеры вместо событий, поллинг, `unwrap` на не-тестовом пути? Задача inlet-а: её `Handle` не связан — соответствует ли это идиоме стенда (`stand.rs` про «detached on drop») и что будет при завершении рантайма? Литералы вместо констант (например активация `0` против `fakes.rs::DPOS_ACTIVATION_BLOCK`, `2` против `DETERMINISTIC_BOOTSTRAP_EPOCH`).
9. **Возврат №1.** Часть Б журнала описывает правку `Role` и урок. Проверь: (а) правка ровно одна и `stand.rs`/`mod.rs` ею не тронуты; (б) остальные имена того же `use` действительно нужны без фичи; (в) идиома in-body `use` в feature-gated тесте — та, что журнал называет (`testbed/tests.rs:3561`, `:3758`, `:4102`); (г) есть ли в диффе ЕЩЁ имена/элементы, которые компилируются только под фичей, но объявлены безусловно (тот же класс дефекта в другом месте).
10. **Дрейф `.claude/dpos_architecture/`**: заход добавил стендовую поверхность; называет ли какая-нибудь секция (`15_smoke_cases…`, `09_followers`, `08_node_integration…`) состав стенда или продюсеров marshal-а так, что теперь это неверно? Список мест с `file:line`; править НЕ надо.
11. **Где журнал вводит в заблуждение** — по пунктам §0 журнала, каждый подтвердить или опровергнуть.
12. **Hard-stop оркестратора**: (2) требуется ли менять решение в `DECISIONS.md` — да/нет со свидетельством; (3) есть ли BLOCKER, на который проект не отвечает; (4) опроверг ли стенд проект на центральном пути.
13. **Где твоя проверка была слабее всего.**

**§1 Находки** — таблица `id | серьёзность | file:lines дерева | HEAD-якорь | что не так | чем пытался опровергнуть | уверенность`; id `A-nn`. BLOCKER — регрессия против HEAD, не предписанная планом/проектом, либо доказанная неверность проекта на центральном пути, либо тест, зелёный при сломанном свойстве. **§2 Поведение по ханкам** против `HEAD`. **§3 Граница** — таблица имён, что вышло из `testbed/stand.rs` наружу. **§4 Ворота** verbatim + твои точечные прогоны. **§5 Оставить как есть** — непустой и конкретный. **§6 Замечено вне рамок 5.0а** — для 5.1/5.2/5.4.

В финальном ответе — только §0 (кратко) и таблица §1.

# Второй круг захода А1 строки 5.3 — после двух dsh-ревью круга 1 (A: состояния/переходы/входы; B: adopt/heal/sweep/церемония/тесты)

Ты — исполнитель (Opus 5) в СВЕЖЕМ контексте. Репозиторий `/home/djadjka/Work/fluentbase`, HEAD `60d6c3e3`; рабочее дерево несёт незакоммиченный заход А1 (8 файлов под `crates/dpos/consensus/src/`). Пути без префикса — от `crates/dpos/consensus/src/beacon/`. `.dpos-study/history/E4-ORCHESTRATOR.md` не читать, не трогать.

Вход, по порядку: (1) `git diff HEAD --stat` и дифф — объект (≈6200 строк; читай `actor.rs` продакшн `:1-3570` целиком, это автомат); (2) постановка `.dpos-study/history/E5-prompts/5.3-A1-impl-1.md`; (3) журнал `.dpos-study/history/E5-3-A1.md` (трассируемость S0–S17, переходы, Д-А1-1…12); (4) ревью `history/E5-3-A1-DSH-1A.md` (DA-01…DA-18) и `history/E5-3-A1-DSH-1B.md` (DB-01…DB-33, не все номера заняты); дизайн §5.2–5.4 `history/E5-BEACON-DESIGN.md:596-646`. Всё в (3)–(4) — реляция; перед правкой проверь по коду; REJECTED допустим с file:line.

## Что оркестратор проверил сам (можешь опираться)

- **DB-01 BLOCKER** [KNOWN]: `apply_artifact` `actor.rs:1480-1482` — `Acquiring(ArtifactForShare) ⇒ Keyed{digest}` без проверки доли; `recover` `:2586-2595` — share ∧ artifact ⇒ `Keyed` без проверки; `validate_share_on_poly` вызывается только из `adopt_share`. Третий путь в `Keyed` обходит гейт F-02: доля, записанная над артефактом X, становится «ключевой» при приходе любого X′.
- **DB-25 SERIOUS** [KNOWN]: `conflict()` `:1530-1540` снимает долю только из RAM (`store.remove`), `share_state::evict_share` (`share_state.rs:647`) вызывается лишь из `reconcile_journals` (`:691`); на рестарте `load_all` вернёт долю, `recover` даст `Keyed` — терминал забыт.
- **DA-01 SERIOUS** [KNOWN]: `ArtifactBridge::adopt` = `try_send`, `Full`/`Closed` ⇒ warn и всё (`artifact.rs:1640-1656`); `apply_artifact :1446-1451` сравнивает только с собственным `held_digest`; `drive_acquisition :3208-3243` на `needs_artifact()` лишь дёргает `pull(e)`, стор (`outcome_at`) читается только в `recover :2582`. Потерянный hand-off ⇒ актор без артефакта, который стор держит (парковка `NoArtifact`), и слепое пятно `Conflict`.

## ПРАВИТЬ — по приоритету; если правка выходит за ~700 строк продакшна, остановись после блока I и запиши остаток

**Блок I (BLOCKER/SERIOUS):**
- DB-01: `validate_share_on_poly` против полинома/`group_key` артефакта на ВСЕХ путях в `Keyed`: `ArtifactForShare + артефакт` и `recover(share ∧ artifact)`; провал ⇒ не `Keyed` — `Refused` как на live-пути (`Acquiring(Logs)` + `Stalled{…}` + счётчик), доля из RAM снимается. Одна функция «принять долю над артефактом» для всех трёх путей. Тесты: доля над X, приходит X′ ⇒ отказ; `recover` с долей над X и артефактом X′ на диске ⇒ отказ. Мутация: снять проверку на пути `ArtifactForShare` — тест красный.
- DB-25: `Conflict` — durable: на входе в `Conflict` вызвать `evict_share` (файл доли) И записать факт конфликта так, чтобы `recover` его увидел (маркер в журнале эпохи или отдельный файл рядом с долей — выбери то, что уже есть в `share_state`, без нового формата, если возможно); `recover` при маркере ⇒ `Conflict{evidence}`, не `Keyed`. Тест: конфликт → рестарт (`load_all` + `recover`) → `Conflict`.
- DA-01 (+DB-26, DB-27, DA-05/DB-28 частично): **стор — владелец факта «артефакт эпохи»**: в `drive_acquisition` для каждого `needs_artifact()` сначала `outcome_at(e)`; если стор держит — применить через `apply_artifact` (без сети); `apply_artifact` при отсутствии собственного digest сверяет с digest стора — расхождение ⇒ `Conflict` (стор first-wins и кворум-проверен). Сигнал конфликта из моста: раз актор читает стор, потеря `try_send` больше не теряет ФАКТ — но мост при расхождении должен оставить наблюдаемый след, который актор прочтёт (например, стор хранит второй digest/флаг конфликта эпохи) — иначе `Conflict` от расходящегося B, отвергнутого мостом, не достижим. Реши и запиши Д-nn. Тесты: (а) потерянный hand-off (мок `adopt_tx` полон) → следующий тик → `Keyed`/`Agreed` из стора; (б) стор держит A, актор без digest, приходит B ≠ A → `Conflict`.

**Блок II (MODERATE):**
- DA-02: `Conflict` ключевать не по `DkgProposal::digest()` (включает `confirms`), а по ЗНАЧЕНИЮ — пришпиленный набор (`logs`) + `group_key`; два сертификата над одним значением с разной метаданной confirms — не конфликт (дедуп). Д-nn с обоснованием по `dkg_agree.rs:455-461`.
- DB-03: off-poly на live-пути → `Acquiring(Logs)` не должен крутиться каждый тик с ERROR: защёлка + повтор только при изменении входов (новый лог/артефакт), после провала recompute тоже off-poly ⇒ `Unrecoverable{key}`. Событие `Stalled{OffPolynomial}` (DA-06).
- DB-11/DA-12: вернуть хотя бы одну фикстуру старой геометрии (`INTERVAL=20`, нулевое окно дилинга) — тест, что NoFile на дедлайне теперь `SatOut`, а не старт (это и есть R-036 в старой геометрии); стёртые комментарии геометрии (DB-21) поправить.
- DB-09: прямой тест D-10 (`resume(.., preferred)` берёт пришпиленное тело, когда first-recorded ≠ pinned).
- DA-07/DB-31: `on_artifact`/`decide` — ограничить окном `recover` (`[max(2,now−8), now] ∪ {now+1}`), артефакт вне окна — отложить/отбросить с меткой, не стартовать церемонию.

**Блок III (MINOR, если остался бюджет):** DA-03/DB-16 (unmappable ERROR — под защёлку), DA-04/DB-17 (`stall` при плейсхолдере — логировать реальное состояние), DA-14/DB-06 (защёлки снимать при смене состояния, не только на `Keyed`), DA-15/DB-24 (плейсхолдер `take_state` — отдельный вариант `Transit` или `Option`, чтобы забытая запись ловилась типом/`debug_assert` на выходе из каждого входа), DB-05 (`announced` — снимать вместе с выходом из `Agreed`), DB-13 (вакуум-проверка теста слотов — вернуть смысл), DA-16/DA-17/DB-20 (комментарии; пять файлов вне списка — только комментарии, разрешаю), DA-18.

**ЗАПИСАТЬ (журнал «Второй круг», не править):** DA-08/DA-09/DA-10 (строки §5.2 не буквально — Д-nn с обоснованием паритета), DA-11 (`first_log` для publish/confirm — намеренно: узел подтверждает то, что записал первым), DA-13, DB-08 (провал записи `ReceivedDealing` не ретраится — для А2), DB-29, DB-33, DB-22 (объём).

Правила: агентов не запускать; git только чтение; файлы — те же 8 + комментарии в пяти названных + доки `.claude/dpos_architecture/` (автомат: durable `Conflict`, стор-владелец, третий путь в `Keyed`) + журнал `E5-3-A1.md` («Второй круг»); никаких `#[allow]`/`unwrap` на продакшн-пути; file:line; `CARGO_BUILD_JOBS=6`; стенд не гонять параллельно.

Ворота — все одиннадцать verbatim (база круга 1: `--lib` 699/0, стенд 58/0 и 49/0 @650, node 57/0, reader 64/0, slasher 16/0, clippy 2 чужих / 0 своих, с фичей 0, fmt 0, doc 6, harness 2/2086/10). Мутации с md5: DB-01 (снять проверку на `ArtifactForShare`), DB-25 (не вызывать `evict_share`), DA-01 (не читать стор в `drive_acquisition`), DA-02 (вернуть ключ по полному digest — тест «одно значение, разные confirms — не конфликт» красный). Ответ — таблица DA/DB → решение → file:line → доказательство, ворота, md5.

<tone_preference>Кратко и по существу.</tone_preference>

# Final review of row 5.3, заход Д — an absentee restarted after the seal deadline heals its share instead of sitting out (Opus 5, fresh context)

You are the reviewer (Opus 5) in a fresh context. Repository `/home/djadjka/Work/fluentbase`, branch `djadjka/dpos-reth-2.2-squashed`, base `HEAD` = `d8747086`. The object is the uncommitted working tree: 5 files under `crates/dpos/consensus/src/` (`git diff HEAD --stat -- crates`: `beacon/actor.rs`, `beacon/metrics.rs`, `beacon/share_state.rs`, `testbed/stand.rs`, `testbed/tests.rs`, +599/−116) and 4 harness files under `devnet/local-dpos-smoke/dpos_harness/` (`git diff HEAD --stat -- devnet`). Paths without a prefix are relative to `crates/dpos/consensus/src/`. Do not read `.dpos-study/history/E4-ORCHESTRATOR.md` (another session's file). The root `CLAUDE.md` is modified by someone else — not under review.

This is the FINAL round: three implementer rounds, each a fresh agent (journal `history/E5-3-D.md`: §0.1–0.10 round 1, `## Round 2 — harness reader` — the smoke reader followed the log field renamed `dealers=`→`pinned=` by `cee2e037`, `## Round 3` — dsh D-05..D-08, D-12; briefs `history/E5-prompts/5.3-D-{impl-1,harness-2,fix-3}.md`), one dsh review (`history/E5-3-D-DSH-1.md`, findings `D-nn` — if the file is missing, the dsh run did not complete: review without it and say so). The brief: `history/E5-prompts/5.3-D-impl-1.md`. The design: `history/E5-BEACON-DESIGN.md` §5.2/§5.4 (`:612`, `:616`, `:637` — the cells being changed), R-036. The cause: `history/E5-ORCHESTRATOR.md`, section «## Hard-stop» (item (4): the live smoke `smoke-vrf-dkg-live-heal` failed on `d8747086` because a validator restarted after the seal deadline without a journal landed in `SatOut` and stayed `NoUsableShare`).

## Established by the orchestrator (rely on it; contradict only with file:line)
- The change: the two `recover` arms `JournalLoad::Torn if past_seal` (`actor.rs:~3256`) and `JournalLoad::NoFile if past_seal` (`:~3266`) now return `heal_or_acquire` (`:~2896`): artifact already stored ⇒ `Acquiring(Acquire::Logs(heal_over(..)))`, the player-only recompute over the artifact's pinned dealer logs; no artifact ⇒ `Acquiring(Acquire::ArtifactForShare)`, which on artifact arrival goes through `key_held_share` with share `None` into the same heal. A torn journal is evicted before the heal. `EpochState::SatOut` is deleted. Owner-ratified deviation **Д-5.3Д-1** (journal §0.7): the design cells `(Torn|NoFile, h ≥ seal) ⇒ SatOut` become `⇒ heal`; re-DEALING after the deadline stays forbidden (R-036 unchanged); П-9 untouched. Do not reopen the deviation as a BLOCKER.
- The reveal rule, verified by the orchestrator in the pinned commonware checkout: `~/.cargo/git/checkouts/monorepo-*/3c4e02c/cryptography/src/bls12381/dkg.rs:1836-1846` — a player's share per dealer is its own received dealing OR the dealer's public reveal of its point (`priv_msg` or `log.get_reveal(&self.me_pub)`); a dealer reveals every player whose ack it did not receive. `Player::resume` `:1741-1754` returns `MissingPlayerDealing` when some log carries a valid ack of this node that the replay did not provide.
- Gates are MINE, run on this tree (tag `y2`): logs `/tmp/claude-1000/-home-djadjka-Work-fluentbase/f6f42c91-6188-4d9c-ae64-a1e9faed9028/scratchpad/gates/y2-*.txt`, status `y2-status.txt`, md5 snapshot `y2.md5` (`md5sum -c` is allowed). Expected: consensus `--lib` 733/0, stand with `--features dpos-devnet-byzantine` 58/0, node 57/0, reader 64/0, slasher 16/0, clippy two foreign warnings / zero own, fmt 0, doc unresolved 6; harness `make harness-test` 2 failed (pre-existing `test_smoke_boundary_cases.py:491,528`) / 2086 passed / 10 skipped (orchestrator's run). **Do not run cargo** (shared tree, gates are mine). The only exception is mutations (question 7): a targeted `cargo test -p fluentbase-consensus --lib <test>` (stand tests with `--features dpos-devnet-byzantine`), rollback by `cp` of a copy you made first, md5 before/after, `CARGO_BUILD_JOBS=12`, at most three, never in parallel.
- The live smoke on this tree (rounds 1+2, before round 3 which touched only prose, a dead metric variant and test assertions): `.../scratchpad/smoke/live-heal-5.3D-y2.log` — `OK (smoke-vrf-dkg-live-heal)`, `exit=0`, the three validator-3 lines are inside it (heal entry `want=4 pinned=4`, `recomputed its share … promoting to Signer epoch=2`, `promoted to Signer in-process`), plus `PRODUCED in epoch 2`. The run before the harness fix: `.../smoke/live-heal-5.3D-y1-reader-fail.log` (FAIL in the reader, line 132). The original FAIL on `d8747086`: `.../smoke/live-heal-d8747086.log`. No container logs survived (the case tears the stack down).
- The implementer had no LSP tool and did not see `~/.claude/rules/rust.md`; it wrote 121 comment lines first and reduced them to 54 after correction (journal §0.10). Judge what is in the diff now.

## Rules
1. Do not launch agents — not for the work, not to verify yourself: the design cell, the dsh report and the code must sit in one context; an agent that reads one side cannot see where the other misreads it. 2. Git read-only; the only file you write is `.dpos-study/history/E5-3-D-REVIEW.md`. 3. Open every `.rs` file you cite with the `Read` tool (not `cat`/`sed`) — file:line on every claim; journals, the dsh report, comments and docs are not evidence. 4. Completeness, not a filter: report everything from BLOCKER to NIT, with confidence; the orchestrator filters. 5. This is a review, not a redesign; something substantial outside the row goes as one line at the end.

## Input order
`git diff HEAD -- crates` first, then the journal, then the dsh report, then the smoke log.

## §0 — direct answers ([KNOWN]/[HYPOTHESIS], file:line)
1. **Gates**: read `y2-*` and quote the numbers verbatim; explain the test-count delta against the base (`--lib` 730 → 733, stand 57 → 58) BY NAME (`git diff` on `fn ` in test modules) — does the count add up.
2. **Smoke**: verdict line of `live-heal-5.3D-y2.log` verbatim; which of the three validator-3 lines prove which step (entry with an empty journal, share adopted, signer); does `PRODUCED in epoch 2` prove signing (what the harness reads for it — `asserts_fault.py`); does round 3's diff touch any line executed on that live path (if yes, say the smoke must be rerun). The harness diff (`git diff HEAD -- devnet`): does the reader now match exactly what `heal_over` logs (`actor.rs:~2879-2884`), and does anything else in the case still read a renamed field.
3. **Safety of the new arms**: trace `heal_or_acquire` → `Acquiring(Logs)` → `drive_acquisition`/`fetch_missing_logs` → `try_recompute` → `adopt_share` and every outgoing sink on that path: can anything be BROADCAST for the epoch (a dealing, an ack, a seal)? A node that DID seal before crashing (journal torn) — can it re-seal or re-broadcast via this path; does `evict_journal` on a torn file destroy anything still needed (own sealed log body served to peers, a share).
4. **Termination**: for an absentee whose point was NOT revealed (it acked, then lost the journal) — `Unrecoverable` exactly once (`try_recompute` `MissingPlayerDealing` arm), no refetch loop, no per-tick log; every pinned body fetched at most once per change of inputs (`attempted`); a body nobody holds — bounded by what (`fetch_missing_logs` back-off vs epoch sweep). `Err(other)` from `recompute_scoped` with empty `want` — non-terminal by the journal (§0.4): reachable for an absentee or not, by code.
5. **Artifact-less entry**: `Acquiring(ArtifactForShare)` was designed for a HELD share; with share `None` the `apply_artifact` arm → `key_held_share` → `store.get == None` → `Acquiring(Logs(heal_over))` — identical to the artifact-present entry? Is the artifact pulled every tick in that state (`needs_artifact`); a committee read failure there — silent stall or a latch?
6. **The other cells**: `(Torn|NoFile, h < seal)` and `(Present, h ≥ seal)` unchanged; `decide_window`/`decidable_epochs` still decide an epoch jumped past; a NON-member restarting (`me ∉ committee`) still takes `KeyOnly`/`ArtifactForKey`, never the heal; the `past_boundary` → `ArtifactForCeremony` rule only for `Present`.
7. **Tests and mutations**: (a) the stand test `testbed::tests::an_absentee_restarted_after_the_seal_heals_its_share_and_signs` (`tests.rs:~2033`) — the journal admits (§0.9 п. 1) that node 3 HAD a share and a journal at the checkpoint and the test deletes both by hand before the replay: is the deletion asserted to have removed something; do the `recover(2)` inputs then equal the smoke's validator-3 (no journal, no share, clock ≥ seal, artifact PRESENT in the store) — which of the two entry arms does the stand cover and which only the unit; is «signs» proven by a fact false for a verify-only node (node 1 cut, 28 blocks need node 3's partial — check the arithmetic: committee 4, f = 1, threshold; quote the line in `combined_scheme.rs` or wherever a verify-only node is shown not to sign — the journal took it from role docs, not code); could the test pass with the heal broken. (b) the three inverted units assert the new cell, not merely «not sat_out»; the two new units — does `node0_absent_epoch2_artifact` build a real pinned set with reveals for node 0 (fixture asserts `reveals`), does the acked→`Unrecoverable` unit really withhold the reveal. (c) mutations, at most three, with rollback and md5, verbatim: M1 — restore a terminal in the `NoFile if past_seal` arm (`Unrecoverable { key }` in place of `heal_or_acquire`) → expect the stand test and 4 units red; M2 — `want = BTreeSet::new()` in `heal_over` → expect 2 units + stand red; M3 — your own choice (e.g. drop `evict_journal` in the Torn arm) → name what is covered. A green mutation names the uncovered property.
8. **SatOut removal**: every `match` on `EpochState` exhaustive; `StallReason::SatOut` and the `sat_out` label (`beacon/metrics.rs:53,73,87`), `carries` at `actor.rs:~509` — dead now (the implementer records it as a follow-up, `metrics.rs` was outside its list; confirm no producer remains: `git grep -n 'SatOut\|sat_out' -- crates devnet .claude`).
9. **Hygiene**: `#[allow]`, new `unwrap`/`expect`/`panic!` outside tests, new `pub` outside `beacon/`, dead code, the two new `warn!` — per tick or once; comments: `git diff HEAD -- crates | grep -nE '^\+\s*//'` — list every added comment line that narrates, cites a document/finding/brief, or records history (the rule: a comment only for a *why* the code cannot show, one line); test doc-comments count too.
10. **Docs**: `.claude/dpos_architecture/00_preamble.md`, `08_*.md`, `DPOS_ARCHITECTURE_CHANGELOG.md` — anchors exist (`grep -n`), `verified-against` updated; `grep -rn 'SatOut\|sat out\|sits out' .claude/dpos_architecture` — only historical CHANGELOG lines or an explicit «no longer» note.
11. **dsh report**: for each D-nn — CONFIRMED / REJECTED (with file:line) / NOT CHECKED.
12. **Hard-stop**: does anything require a change in `DECISIONS.md` (П-2, П-3, П-9, Д-1, Д-3, Д-6, Д-7, Д-9); a BLOCKER the design does not answer.
13. **Verdict: COMMIT / DO NOT COMMIT**; what you did not read.
14. Where your check was weakest — ranked.

**§1 Findings** `id | severity | file:lines | what is wrong | refutation attempt | confidence`, ids `F-nn`. BLOCKER = any broadcast from the heal path; a sealed node re-sealing; an unbounded loop or per-tick log without a terminal; the stand test green with the heal broken; a non-member entering the heal; the smoke FAIL caused by the code. Then a non-empty «Leave as is».

Write the file, then reply with §0 compressed (verdict, gates, smoke, unread) and the §1 table.

<tone_preference>Brief and to the point.</tone_preference>

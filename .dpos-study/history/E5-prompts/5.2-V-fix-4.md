Четвёртый круг захода В (после dsh-ревью круга 3, `.dpos-study/history/E5-2-V-DSH-3.md`). Тот же объём: только `testbed/{stand,cert_inlet_tests,tests}.rs`, продакшн не трогать, git не трогать, агентов не запускать (причина прежняя: спецификация и код должны сидеть в одном контексте; агента для проверки своей работы тоже не запускать). Каждое утверждение — file:line.

Прочитай `E5-2-V-DSH-3.md` целиком. Часть (А) — вердикты по D-01…D-12: 8 FIXED, D-04 PARTIALLY, D-05/D-06 RECORDED correctly, D-09 RECORDED but fixable. Часть (Б) — E-01…E-10, все MINOR/NIT. Всё это — реляция dsh; перед каждой правкой проверь находку по коду сам, REJECTED допустим с file:line.

Что я проверил сам (можешь опираться): D-09 — `git ls-files .dpos-study` = 152 файла, `.gitignore:49` игнорирует, но каталог force-tracked (`git add -f`), пять `E5-2-*` в HEAD; журнал `E5-2-V.md` уйдёт в коммит той же операцией — слово «untracked» в `cert_inlet_tests.rs:1100` неверно. E-03 — стендовый `timeout: 5s` (`stand.rs:2248`) < `FRONTIER_FETCH_TIMEOUT = 8s` (`plane_upstream.rs:169`), маршрут повтора по таймауту реален. E-06 — `.parse().ok()?` в `stand.rs:1236` молча роняет семейство с нечисловым узлом. E-01 — ветвь `else { eprintln! }` реальна.

ПРАВИТЬ:
- D-09: убрать «untracked» и всё, что утверждает статус трекинга; оставить ссылку на журнал `.dpos-study/history/E5-2-V.md` §0(4), мутация 2, «beside the other E5-* records» — без утверждений о git.
- E-03: переписать обоснование рельса `dropped <= plane_calls` (`cert_inlet_tests.rs:1359-1366`) по факту: повтор по таймауту резолвера (5 с в стенде < 8 с клиентского ожидания) МОЖЕТ дать дроп без нового `latest_calls`/`finalized_calls`; рельс остаётся «WEAK, sanity rail, printed number», но причина «не может умножиться» убирается. Не менять сам ассерт и не менять таймауты.
- E-04: докстрока хелпера `run_held_lag_over_donor_archive` — сказать точно: поле присваивается безусловно, `None` = отсутствие снапшоттера.
- E-05: фраза «its frontier plane keeps answering, and step (5) runs» (`:1086-1088`) — переписать так, чтобы не утверждалось, что арм берёт именно жертва; единственная по-узловая наблюдаемая — `defers` инлета; если хочешь — печатать `upstream[VICTIM].deliveries_decoded` с оговоркой, но не ассертить.
- E-06: `peers_blocked()` — семейство, оканчивающееся на `_peers_blocked`, но не парсящееся как `node{i}_…`, должно `panic!` с именем ключа, а не молча отбрасываться (сначала проверка суффикса, потом парсинг узла с паникой).
- E-07: «at DECODE time» → «before decode» (арм отсутствия схемы `actor.rs:964-972` отвечает `true` ДО декодирования на `:974`).
- E-09: сообщение падения `rejected` (`:1253-1259`) — не называть таблицу «the four self-inflicted arms read …» единственным содержанием; сказать, что наблюдённые метки — `rejected`, а таблица — только нули по самопричинённым армам.
- E-10: фраза о «STOPS as well as STARTS» (`:1057-1070`) — оговорить: для меток семейства `dpos_frontier_dropped_total`, не для путей кода.

ЗАПИСАТЬ (в журнал, не править): E-01 — конвенция модуля (`tests.rs:195` тот же `if out.log_capture_live`), в одном тесте не меняем; E-02 — строка `BLOCK_WARN` зависит от текста в commonware, при смене — молчаливая потеря витнесса; E-08 — консенсусный шпион стоит и у batcher/voter, честный прогон, споткнувшийся о их `block!`, даст ложный красный — латентно.

Ворота после правок — все одиннадцать, verbatim, база: `--lib` 676/0, стенд с фичей 56/0, без фичи 47/0 @629 filtered, node 57/0, reader 64/0, slasher 16/0, clippy 2 чужих / 0 своих, с фичей 0, fmt exit 0, doc 6, harness 2 failed / 2086 / 10. Мутационное доказательство для E-06: временно подсунуть в экспозицию строку `nodeX_frontier_resolver_peers_blocked 0` (или эквивалент в юнит-вызове `peers_blocked()`) и показать панику; откатить. Журнал `E5-2-V.md` — раздел «Четвёртый круг»: таблица D/E → решение → file:line → чем доказано; md5 трёх файлов на выходе. Ответ — таблица и числа ворот, без пересказа.

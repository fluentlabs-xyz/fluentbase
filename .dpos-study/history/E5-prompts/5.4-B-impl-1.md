# Строка 5.4, заход Б — бикон владеет жизненным циклом инстансов согласования и их партициями

Ты — исполнитель (Opus 5) в свежем контексте. Репозиторий `/home/djadjka/Work/fluentbase`, ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `79dd18f9`. Пути без префикса — от `crates/dpos/consensus/src/`. Рабочее дерево чисто, кроме чужого `.dpos-study/history/E4-ORCHESTRATOR.md` — не читать, не трогать.

## Рамка

Строка 5.4 (`.dpos-study/PLAN.md`, `| [ ] 5.4 |`), заход Б. Карта — `.dpos-study/history/E5-ORCHESTRATOR.md`, «Карта строки 5.4». Дизайн — `history/E5-BEACON-DESIGN.md` §5.3 (`:618-627`: «инстанс agreement — beacon (supervisor + band-sweep партиций ПО НОМЕРУ ЭПОХИ в окне `SCHEME_RETENTION_EPOCHS`, перенесённый как есть)»), `:578` (`Tasks{supervised, drain}` — ровно два хэндла), находки E5-04/E5-05/E5-06/E5-40 (§4). НЕ этот заход: клок (5.4-А: `heights`/`dkg_height_tx`/tee), автомат (5.3 — HEAD), контракт, `dkg_agree.rs` внутренности.

## Что установлено оркестратором по коду

- `epoch_manager.rs`: `prune_agreements :339-396` — abort + join живых инстансов ниже cutoff, band-sweep `[cutoff − AGREEMENT_SWEEP_SPAN, cutoff)` с пропуском живых, edge-driven memo `swept_to` (`AGREEMENT_SWEEP_SPAN = SCHEME_RETENTION_EPOCHS`, `:321`); поля `dkg_agreements :472`, `agreements_swept_to :478`, `agreement_intake :482`, `with_agreement_intake :694`; приём инстанса из intake `:980-1016` с защёлкой SafetyHalt (`:1000-1013`: `safety_halt.is_engaged() ⇒ abort`, иначе `insert`, замена живого — abort предыдущего); вызов из `abort_below :1686-1692`, cutoff = `current.get()` (`:1671`); юниты `:3051-3191` (четыре: прунинг, band-sweep с живыми, memo edge-driven, fresh process).
- Проводка intake: `Tasks.agreement_intake` `beacon/plane.rs:192` (канал `:675`, `:920`, `:1007`, follower-заглушка `:1155`), `OuterBuilder.agreement_intake` `outer.rs:551`, `:1080-1081`, `dpos.rs:1282/2092/2952/4117`, узел `crates/node/src/dpos.rs:750/978/1958/2051/2280`, стенд `testbed/stand.rs:2810/2968/3068`. Лаунчер `dkg_engine.rs` держит `started` (`:402`), отдаёт хэндл в `adopted` (`:576`).
- `agreement_partition` `dkg_engine.rs:260`, реэкспорт `pub(crate) use` `beacon/mod.rs:149`, читатель вне бикона — только `epoch_manager.rs:17`. Имена партиций бикона (`SEED_JOURNAL_PARTITION`, mint memo) — `consensus/dpos.rs:~190-206`.
- `SafetyHalt` — `sync_metrics.rs:432`, `is_engaged :600`; у бикона его нет (`OuterBuilder.safety_halt` `outer.rs:434`).

## Что сделать

1. **Лаунчер владеет инстансами.** `dkg_engine.rs` (`AgreementLauncher`/супервизор) держит `BTreeMap<Epoch, Handle<()>>` запущенных инстансов вместо отдачи в intake; замена живого — abort предыдущего с warn (как `:1011-1013`). Защёлка SafetyHalt — ДО спавна: `SafetyHalt` приходит в бикон обязательным ребром (`ValidatorInputs`/`Wiring` — выбери то, где живут остальные обязательные входы; follower-сборка тоже получает его), `is_engaged() ⇒` инстанс не спавнится, warn один раз на эпоху; семантика HEAD (abort после спавна) заменяется на «не спавнить» — Д-nn с обоснованием (тот же исход, без лишнего спавна).
2. **`prune_agreements` переезжает в бикон КАК ЕСТЬ** (тело, порядок abort→join→remove, пропуск живых, `swept_to`, `AGREEMENT_SWEEP_SPAN`): вызывается биконом на своём cutoff. Cutoff у HEAD — `current` эпоха в `abort_below` (переход ET). В биконе установи эквивалентный момент (переход эпохи по клоку актора / `sweep_epoch_state`) и ДОКАЖИ, что sweep не наступает РАНЬШЕ, чем у HEAD, ни для одной эпохи (иначе партиция живого инстанса может быть снесена): таблица «эпоха → высота sweep HEAD vs новое» в журнале; если раньше — сдвинь, не обходи.
3. **Удалить** из `epoch_manager.rs`: `dkg_agreements`, `agreements_swept_to`, `agreement_intake`, `with_agreement_intake`, `prune_agreements`, `recv_agreement`, арм intake в цикле, вызов в `abort_below`; юниты `:3051-3191` — перенести в бикон без потери клеток (четыре теста + тест защёлки SafetyHalt). Удалить `Tasks.agreement_intake` (в `Tasks` — ровно `supervised` и `drain`), `OuterBuilder.agreement_intake`, `dpos.rs`/узел/стенд проводку, `Option`-поле `epoch_manager.agreement_intake` (не оставлять `Option`).
4. **Имена партиций** — `agreement_partition` приватно в биконе; префикс `dkg_epoch_` — в модуль констант рядом с `SEED_JOURNAL_PARTITION` (`consensus/dpos.rs`) или в `beacon/mod.rs` — одно место, доккомментарий «кто владеет, кто подметает, окно». `pub(crate) use dkg_engine::agreement_partition` снять (`mod.rs:149`); `git grep agreement_partition` вне `beacon/` — пусто.
5. **Тесты**: перенесённые четыре + SafetyHalt; стенд с фичей и без — число в число; узел `cargo test -p fluentbase-node --lib` — `a_drain_finishes_only_once_the_last_sender_is_gone` остаётся зелёным.
6. **Доки в том же изменении**: `.claude/dpos_architecture/{03_epoch_machinery,08_…,13_…,00_preamble}.md`, CHANGELOG — владение инстансами/партициями, `Tasks` два хэндла, защёлка; `git grep agreement_intake|dkg_agreements|prune_agreements|with_agreement_intake` в доках и коде — ничего живого.

## Правила

- **Агентов не запускать** — ни для работы, ни для проверки себя. Git — только чтение.
- Записываемые файлы: `beacon/{dkg_engine,plane,mod,actor}.rs` (актор — только если cutoff берётся из него), `epoch_manager.rs`, `outer.rs`, `dpos.rs`, `testbed/stand.rs`, `crates/node/src/dpos.rs`, их тесты, `.claude/dpos_architecture/*.md`, CHANGELOG, журнал `.dpos-study/history/E5-4-B.md`. Остальное — не трогать; нужно — стоп и запись.
- Не переформатировать нетронутое; никаких `#[allow]`, новых `unwrap`/`expect` на продакшн-пути, новых `pub` наружу. `CARGO_BUILD_JOBS=6`; стенд не гонять параллельно.
- **СТОП** (запись, не обход): sweep наступает раньше HEAD и это не сдвигается без изменения окна; изменение семантики SafetyHalt сверх «не спавнить вместо abort»; `DECISIONS.md`; стенд краснеет по живости.

## Ворота (все, verbatim; база на HEAD `79dd18f9`)

`--lib` 725/0 · стенд с фичей 58/0 · без фичи 49/0 (676 filtered) · node 57/0 · staking-reader 64/0 · slasher 16/0 · clippy три крейта — два чужих (`node/src/dpos.rs:1991`, `epoch_transition.rs:3024`), ноль своих · consensus с фичей 0 · fmt 0 · doc unresolved 6 · harness 2 failed / 2086 / 10. Прирост/убыль тестов по именам (перенос ≠ убыль).

**Мутации (с откатом и md5):** (M1) sweep без пропуска живых — тест band-sweep красный; (M2) защёлка снята — тест SafetyHalt красный; (M3) memo `swept_to` не edge-driven — тест memo красный.

## Журнал `history/E5-4-B.md` — §0

1. Ворота verbatim. 2. Таблица «что удалено → куда переехало → file:line». 3. Таблица «эпоха → момент sweep HEAD vs новое» и доказательство «не раньше». 4. Защёлка SafetyHalt — где, семантика, Д-nn. 5. Партиции — где имена, кто владеет. 6. Мутации verbatim. 7. Вне рамки. 8. Слабее всего — ранжируй. §1 «оставил как есть» — непустой.

Финальный ответ — §0 сжато и таблица п.2. Не останавливайся между шагами; не спрашивай — решай и записывай допущение; СТОП только по названным условиям.

<tone_preference>Кратко и по существу.</tone_preference>

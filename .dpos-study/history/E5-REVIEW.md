<!-- Независимая оценка закрытия Э5 (строки 5.0а, 5.1, 5.2, 5.3, 5.4 + 4.4). Дерево /home/djadjka/Work/fluentbase,
ветка djadjka/dpos-reth-2.2-squashed, HEAD d8747086, диапазон git log bec013a7..HEAD (28 коммитов, 14 кодовых).
Написано 2026-09-15 одной сессией Opus 5 в свежем контексте. Агентов не запускал. Cargo не гонял (запрет
постановки). Git только на чтение. Всё, помеченное [KNOWN], открыто или запущено в ЭТОЙ сессии. -->

# Э5 — оценка закрытия: одна граница beacon, один владелец у каждого факта, DKG-автомат, один клок

## 0. Состояние дерева и мои прогоны

- [KNOWN] `git rev-parse HEAD` = `d8747086ddbe1005a9d74b649ad4613a7726431d`; `git log --oneline bec013a7..HEAD` — 28 коммитов, из них кодовые ровно те 14, что названы в постановке. `git diff --stat c4543510 HEAD -- crates` пуст: по `crates/` HEAD = `c4543510`, дерево ворот `x2`.
- [KNOWN] `git status --short` на входе: ` M .dpos-study/EXPERIMENTS.md`, ` M .dpos-study/PLAN.md`, ` M .dpos-study/history/E4-ORCHESTRATOR.md` (чужой, не открывал), ` M CLAUDE.md` (не этапа, не открывал). Под `crates/` дерево чисто. На выходе — те же четыре плюс этот файл.
- [KNOWN] `md5sum -c scratchpad/gates/x2.md5` — 14/14 `ЦЕЛ` против рабочего дерева ⇒ логи `x2-*` сняты ровно с того `crates/`, что на HEAD.
- [KNOWN] Объём `beacon/` на HEAD: `ls | wc -l` = 23, `cat *.rs | wc -l` = 38909 — совпадает с ответом 9 оркестратора (`E5-ORCHESTRATOR.md:360`). Против `bec013a7` (28 / 31313): −5 файлов, +7596 строк, из них `actor.rs` 14463 (продакшн до `:4282`).
- Ничего не запускал из cargo. Числа ворот беру из `x2-*.txt` и сверяю их содержимое построчно (§4). Мутаций не ставил (каждая требует прогона).
- Чекауты: tokio `~/.cargo/registry/src/*/tokio-1.52.3/src/sync/watch.rs:412-425` открыл (К-185); commonware не открывал.
- Прочитано целиком в этой сессии: `beacon/{mod,surface (продакшн 1-1100, 2001-2507),seed_index (1-380),artifact (440-940, 1489-1560, 1836-2014),plane (115-200, 289-460, 596-1160, 1261-1284),share_state (1-120, 651-741),metrics (20-110),seed_journal (425-470)}.rs`, `actor.rs` продакшн выборочно (`:123-190`, `:225-470`, `:536-560`, `:628-633`, `:1159-1306`, `:1535-1590`, `:1610-1980`, `:2074-2124`, `:2160-2455`, `:2913-3060`, `:3137-3300`, `:3425-3490`, `:3803-3920`), `dpos.rs:745-960`, `cert_inlet.rs:689-690,798-835,2905-2940`, `application.rs:1080-1115`, `epoch_manager.rs:159-163,601-683,1005-1030,1600-1762`, `committee/facade.rs:40-95`, `outer.rs:283-290`, `sync_metrics.rs:414-602` (grep), `dkg_engine.rs` (grep по символам), `executor.rs:14-45,740-760` + grep. Все журналы строк и ревью читал через `grep`/выборочно, `E5-ORCHESTRATOR.md` — целиком, `E5-BEACON-DESIGN.md` §0–§3, §5–§7, §9.

---

## 1. Свойства проекта после кода

### 1.1 Восемь мест вне `beacon/` (проект §0.1)

| # | место по проекту | что на HEAD [KNOWN] | статус |
|---|---|---|---|
| 1–3 | три транзиентные копии σ в executor (`Notarized.seed`, `ParkedSpec.seed`, `Deferred.seed`) | `executor.rs:134`, `:239`, `:724` — на месте, тип `crate::beacon::Seed` | **как по проекту** (остаются до 6.1) |
| 4 | архив marshal читается crash-replay-ем как источник σ мимо beacon (`dpos.rs:657-706`) | `seed_via_beacon` `dpos.rs:815-833`: σ из ЛЮБОГО сертификата (локальный архив `:890-893`, upstream `:907-917`) идёт через `beacon.observe_certificate(Finalization)` и читается обратно `beacon.seed(round)`; `Pending ⇒ Defer` (`:896`, `:915`). Второго носителя σ нет — архив стал ВХОДОМ | **ДА** (E5-03 закрыт). Дрейф: док `recover_replay_seed` `dpos.rs:838-848` всё ещё говорит «archive are read WITHOUT re-verification … the epoch key … is also the one thing a restart may legitimately not have» — противоречит `seed_via_beacon` на 20 строк выше |
| 5 | `outer.rs:258` `EpochSchemeProvider` держит схемы с `Arc<dyn SeedOracle>` | `outer.rs:283-290` — вид на `committee.scheme(scope)`; оракул кладёт `committee/mod.rs:550` через `beacon.oracle_for(record.epoch)`; оракул читает `KeyIndex` живьём (`surface.rs:2178-2190`), ключа не копирует | **как по проекту** (§8 «остаётся по замыслу») |
| 6 | `cert_inlet.rs:403` `CertInlet.schemes` | снято ещё Э4 (`65a23db7`); на HEAD inlet берёт `Committee::scheme` | ДА (до этапа) |
| 7 | `engine.rs:225` `register_scheme` | снято Э4 | ДА (до этапа) |
| 8 | `engine.rs:80` `fallback_seed` | снято Э4; остаются чистые функции `witness_fallback_seed`/`constant_fallback_seed` (`beacon/mod.rs:194`) | ДА (до этапа) |

Внутренних типов beacon снаружи: подмодули приватны (`mod.rs:68-93`), парадная дверь — `mod.rs:192-198` + `pub(crate) absent_unregistered` `:202` + `#[cfg(test)] testing` `:219-267`. `git grep 'impl Randomness for' -- crates` — одно попадание, `surface.rs:2194` [KNOWN]. Свойство 2 («один владелец, три транзиентных читателя») — **ДА**.

### 1.2 Инварианты I1–I8 (§3; столбец «держит ли код сейчас» писался до кода)

| # | было по проекту | на HEAD [KNOWN] | статус |
|---|---|---|---|
| I1 | Да при ≤ f; конфликт σ — `error!`, первая остаётся; при > f молчаливая расходимость | `seed_index.rs:216-221`: `(Verified(held), true) if held != seed ⇒ error! + return` — то же правило. §5.2 требовал «вторая σ за раунд, ПРОВЕРЕННАЯ под PK_E ⇒ Conflict(E)» — **не реализовано**, Д-nn не записано (grep `Conflict` в `seed_index.rs` пуст). Недостижимо при ≤ f (порог уникален), так что статус тот же | **ДА при ≤ f, без изменения**; терминал для > f — только у артефактов, не у σ |
| I2 | Да при ≤ f; два `Agreed` — `debug_assert!`; два quorum-артефакта — `warn` | Два quorum-артефакта ⇒ `EpochState::Conflict` (терминал, `actor.rs:352-365`), `stop_signing` `:1933-1974` (маркер → доля), стор — владелец свидетеля `artifact.rs:614-647` + durable маркер, `recover` читает маркер первым `:3143-3168`; `KeyIndex` — один тир (`artifact.rs:880-919`), `LocalDkg` нет | **ДА**, статус улучшен: > f получил явный терминал вместо `warn` |
| I3 | Нет: R-002 (идентичность по ключу дилера), R-026 (потеря тела не перезапрашивается), R-036 (`NoFile` после дедлайна переизлагает) | R-002: `LogId = (dealer, hash)`, вторая подпись = evidence + бан (`ceremony.rs` по К-132, мной открыт только `actor.rs:3035-3060` `ceremony_refusal` `equivocator`); R-026: `on_body_lost` `:1535-1580` ⇒ `Acquiring(ArtifactForCeremony)` + немедленный `pull_artifact`; R-036: `recover` `:3247-3262` — `Torn`/`NoFile` при `past_seal` ⇒ `SatOut`. Гейт `share_on_artifact` на всех трёх входах в `Keyed` (`:1842-1869`, `key_held_share :1806-1840`, `adopt_share :2081-2083`) | **ДА при ≤ f** (было НЕТ). Остатки: тумбстоуненный член остаётся в `committee[E]` ⇒ `n−1 < quorum(n)` — контракт (Всплыло оркестратора, стендом воспроизведено); `ShareConfirm` нового члена вне окна {T−1,T,T+1} режется гейтом и не переиздаётся — `confirmations.rs:184-186` edge-triggered (`previous >= confirmed.len() ⇒ continue`), R-126/E-01 |
| I4 | Нет: не-член живой эпохи — вызывающего кода нет; follower — только RPC | `recover` `:3212-3223`: не-член ⇒ `KeyOnly`/`Acquiring(ArtifactForKey)`; `drive_acquisition` `:3803-3851` пулит каждый тик (`needs_artifact` `:425-443`); `ensure_key(Thorough)` `surface.rs:2467-2496` ⇒ `acquire.fetch(minted_at)`; follower — тот же `LiveBeacon` + want из `hold_seed` `:2215-2220`. Приёмка C7 — прогон оркестратора (К-42, реляция для меня; в `x2-standf` тест `a_zero_overlap_boundary_is_crossed_by_acquiring_the_other_halfs_key` зелёный [KNOWN]) | **ЧАСТИЧНО** (было НЕТ): держится для окна `[epoch(anchor)−8, +2]`. **НЕТ** для свежего datadir, чей минт старше `SCHEME_RETENTION_EPOCHS`: `MintIndex::minted_at` `artifact.rs:790-819` обрывает обход на первом `None` бите (`:810-812`), бит вне окна = `None` (`facade.rs:47-54` сворачивает `OutOfWindow`), `ensure_key` ⇒ `false` на `minted_at == None` (`surface.rs:2483-2485`), fetch по эпохе минта невозможен — см. §9.2 |
| I5 | Граница протокола (`quorum(n)`) | Не менялось; `Stalled{QuorumMissing}` теперь явный (`metrics.rs:37`, `actor.rs:2742-2745`) | **граница**, наблюдаемость добавлена |
| I6 | Нет: пять сторов warn-and-continue; replay неоднороден | `recover(E)` — одна функция с перечислимыми ветками `:3137-3294`; share persist Err ⇒ ОТКАЗ (`adopt_share :2083-2104`, `Stalled{PersistFailed}`); артефакт persist Err ⇒ RAM + счётчик (`artifact.rs:572-590`); маркер конфликта ⇒ fsync до доли (`:1933-1974`); `Torn`/`NoFile` — четыре клетки по таймингу; init журналов — `?` (`plane.rs:705-713`, `:743-749`, `:766-771`), replay share/artifact — warn+skip (по замыслу) | **ДА** в основном. Остаток: σ-persist Err — только `warn!`, без повтора и без счётчика на append (`seed_journal.rs:446`; счётчик только на `sync` `:455`) против §5.4 «повтор на каждом observe_certificate, error! + gauge» — не сделано и Д-nn не записано |
| I7 | Да для записи; нет для crash-replay и журнала | Все три двери (`spec_exec`, `cert_inlet:689`, `UpstreamResolver :2922`) и crash-replay идут через `certificate_verdict` `surface.rs:405-493` ⇒ `VerifiedSeed::check`; `VerifiedSeed` — приватный тип (`mod.rs:92`, дверь только `testing::VerifiedSeed` `:254`); журнал реплеится без перепроверки — по замыслу (`seed_index.rs:206-207` `persist=false`) | **ДА** (было ЧАСТИЧНО) |
| I8 | Нет: PK_E — 5 носителей; σ — 3; очистка партиций — два модуля | PK_E: `ArtifactStore` + производный `MintIndex` (`artifact.rs:448-696`, `:751-877`); `CeremonyStore = BTreeMap<u64, Share>` — только доля (`actor.rs:536`); share-файл v2 = только доля (`share_state.rs:10-24`); транзиентные копии полинома в `AgreedSet.group_key`/`RecomputeState.outcome` — из артефакта. σ: `SeedIndex` + журнал-для-рестарта; архив — вход. Партиции: все четыре имени в `beacon/mod.rs:160-190`, sweep `dkg_engine::prune_agreements` `:332`, в `epoch_manager.rs` `git grep prune_agreements|dkg_agreements|agreement_intake` — пусто [KNOWN] | **ДА** (было НЕТ) |

Итого: ДА — 6 (I1 без изменения, I2, I3, I6, I7, I8), ЧАСТИЧНО — 1 (I4), граница — 1 (I5). Сменили статус против столбца проекта: I2 (Да→Да+терминал), I3 (Нет→Да), I4 (Нет→Частично), I6 (Нет→Да), I7 (Частично→Да), I8 (Нет→Да). Не сменили: I1, I5.

### 1.3 §5.1 граница — ЧАСТИЧНО

[KNOWN] `pub trait Beacon` `surface.rs:124-254`: `seed`, `terminal_seed` (default → `seed`, `:153`), `mandatory_at`, `can_participate`, `signer`, `oracle_for`, `ensure_key` (`:184`), `observe_certificate`, `artifact_bytes`, `observe_epoch` (`:216`, пустой дефолт), `observe_cert` (`:231`, пустой дефолт), `subscribe`, `faults`. Проект: 9 запросов/приёмов + `subscribe` + `faults`; `ensure_key`/`PinEffort` уходят внутрь; `observe_epoch`/`observe_cert` удаляются; `terminal_seed_at` в `seed`. На HEAD — 11 + 2. Живые продакшн-вызовы снаружи: `ensure_key` — `cert_inlet.rs:579`, `epoch_manager.rs:1733-1735`; `observe_epoch` — `epoch_manager.rs:1015` (зовёт пустое тело); `terminal_seed` — `epoch_manager.rs:162`, `:2658`; `observe_cert` — только тестовая обёртка `byzantine_roles.rs:405`. Док самого трейта обещает: «5.4 deletes both» (`surface.rs:229-231`) и «collapsing … belongs to whoever may write that file» (`:145-147`) — 5.4 закрыта, ни одно не сделано, в PLAN 5.4 и в Д-nn не записано. `BeaconEvent` — три варианта (`:378-387`), `Stalled{reason}` из §5.1 на шине НЕТ: он реализован как gauge + латч-лог внутри актора (`metrics.rs:34-56`, `actor.rs:1489-1533`) — снаружи неподписываем. Две сборки `build`/`build_follower` (`plane.rs:596`, `:1153`) над одной парой `(Arc<dyn Beacon>, Tasks{supervised, drain})` (`:173-183`) — ДА.

### 1.4 §5.3 владельцы фактов — ДА

[KNOWN] σ — `SeedIndex` (`seed_index.rs:115`), синхронный `seed` по точному раунду (`:275-280`), ретенция правилом от старшего раунда индекса; PK_E и полином — `ArtifactStore` + durable memo `MintIndex` (`artifact.rs:751-877`; `None` не мемоизируется `:810-812`, write-once `:835-876`); доля — `CeremonyStore`/`ShareFile` v2; dealer-логи — DKG-журнал с окном `JOURNAL_RETENTION_EPOCHS = SCHEME_RETENTION_EPOCHS` (`mod.rs:137`); инстанс agreement — лаунчер beacon (`plane.rs:917-948`, `dkg_engine.rs:332`); часы — `ValidatorInputs.clock: watch::Receiver<u64>` (`plane.rs:548`), один писатель `FluentApp::report` `application.rs:1096-1111` (`ordering_tip` + `beacon_tip` одним оператором), актор паркуется один (`actor.rs:1238-1248`), `git grep dkg_height_tx|note_height_drop|LiveFrontierTee|with_tee -- crates bins` — только три комментария-истории [KNOWN].

### 1.5 §5.4 модель отказов — ЧАСТИЧНО (14 строк: 12 ДА, 2 нет)

ДА (файл:строка выше): persist доли ⇒ отказ; persist артефакта ⇒ RAM+счётчик+повтор на следующем `sync`; частичный успех ⇒ `Acquiring(ArtifactForShare)` (`recover :3182-3195`); нет артефакта ⇒ `Stalled{NoArtifact}` + pull без предела (`:3844-3849`); `Torn`/`NoFile` одно правило; рестарт до/после дедлайна; crash-replay без ключа ⇒ `Pending ⇒ Defer` (`dpos.rs:896`); > f ⇒ `QuorumMissing`; два артефакта ⇒ `Conflict`; `geometry` не заморожена ⇒ `GeometryUnfrozen` (`surface.rs:2291-2292`, `plane.rs:859-869`); resolver умер ⇒ `break` + супервизор (`actor.rs:1262-1283`, `plane.rs:1043-1052`).
НЕТ: (а) «Ошибка persist σ ⇒ повтор + error! + gauge» — `warn!` без повтора (`seed_journal.rs:446`); (б) «две проверенные σ за раунд ⇒ Refused + Stalled{Conflict}» — `error!` + первая остаётся (`seed_index.rs:216-221`). Обе — без Д-nn.

### 1.6 П-2 / П-3 / П-9 как регрессия

- **П-2 (в форме Д-9)** — **ДА**: один владелец σ, вход — `observe_certificate` на всех трёх дверях и в crash-replay; `hint_finalized` не введён (`git grep hint_finalized -- crates` пуст [KNOWN]); `SeedStore`/`certify.rs`/промоутер/`follower.rs` удалены. Оговорка: поздний `Refused` не ведёт к повторному запросу высоты (§9.1).
- **П-3** — **ДА**: `KeyIndex` один тир, W1/W3/`Carried`/`keys.rs`/`key_journal.rs`/`carry.rs`/`resolve.rs` удалены (`git ls-files beacon/` [KNOWN]), `validate_share_on_poly` перед КАЖДЫМ входом в `Keyed` (три сайта через `share_on_artifact`), один ретеншн. Оговорка: I4 держится только в окне (§9.2); `ensure_key`/`PinEffort` остались на границе (Д-12 открыт).
- **П-9** — **ДА**: `(dealer, hash)`, автомат `EpochState` из 13 состояний с четырьмя входами `run` (`:1238-1300`: clock / p2p / resolver / pinned / artifact / body-lost — шесть армов, входов по проекту четыре), `recover(E)` одной функцией, терминалы `SatOut`/`Unrecoverable`/`Conflict`, `NoFile` после дедлайна ⇒ не стартовать. Оговорка: `Stalled` не событие шины; σ-половина `Conflict` не реализована.

---

## 2. Три самых опасных места — см. §9

---

## 3. Где оркестратор принял утверждение без квитанции

Правило сессии (`E5-ORCHESTRATOR.md:7`): «каждая находка SERIOUS+, каждый REJECTED и каждый FIXED на SERIOUS+ — ханк открываю сам». Дисциплина в целом выше, чем в Э4: почти каждый SERIOUS+ имеет свой `file:line`, красные воспроизводились самим (К-16, К-37, К-51, К-63, К-84, К-110, К-125, К-176, К-181, К-188). Места, где ДЕЙСТВИЕ опирается на реляцию:

1. **Квитанции К-8…К-24 отсутствуют в объекте.** [KNOWN] На HEAD таблица идёт К-1…К-7, затем К-44 (`:204-211`); К-25…К-43 стоят внутри карты 5.1 (`:401-429`). `git show 3ead5c6c:…/E5-ORCHESTRATOR.md` содержит К-8…К-20 (`:127-139`), коммит `15e59c17` (закрытие 5.1) их СНЯЛ; К-21…К-24 нет ни в одной закоммиченной версии, хотя на них ссылаются О-2/О-3 (`:415`, `:417`) и `E5-1-A.md:588` («Оркестратор проверил К-22/К-23/К-24 сам»). Решения объединить заходы 5.1 (О-2) и сделать мемо долговечным (О-3) — предусловие удаления `key_journal.rs` — стоят на квитанциях, которых в дереве нет.
2. **REJECTED-вердикты приняты по реляции**: К-136 «D-12 REJECTED с обоснованием» (5.3-Б круг 2) — ханк не открывался; К-138 «E-08 REJECTED (`or_default` … `actor.rs:1509-1517`)» — якорь взят из отчёта; К-163/К-165 «EA-07 REJECTED … исполнитель отверг правильно» — подтверждение от финального ревьюера, то есть вторая реляция. Три нарушения правила 7 в одной строке 5.3.
3. **Закрытие R-036/R-038/R-026/R-051/R-072 в REGISTER (К-165)** — собственные ханки оркестратора по А1 (К-154…К-163) покрывают гейт `share_on_artifact`, маркер конфликта, стор-владельца и `insert → Result`; механизмы R-036 (DB-11 «фикстура нулевого окна»), R-038 (DB-03 `attempted`), R-026 (Д-А1-1 канал body-lost) названы в квитанциях как реляция. На HEAD я их открыл (§1.2 I3) — они верны, но закрытие было сделано до этого.
4. **Закрытие R-126/R-127 (К-152)** — на «своей таблице кадров» финального ревьюера (реляция) и dsh (реляция); собственные чтения оркестратора относятся к кругу 1 (К-143) и двум строкам круга 2 (К-146 `:2247-2253`).
5. **Приёмка 5.3-Б `:3293` сменила вердикт** («остаётся остановкой» по проекту §7 → «не остановка», К-132) — принято по прогону исполнителя; финальный ревьюер гонял C7, не `:3293`. Проектное утверждение опровергнуто прогоном, который оркестратор не повторял.
6. **Строка 5.3 закрыта `[x]` (К-172)** с остатками F-01/F-02 «зонды MR4/MR5 зелёные при снятии» — существование остатков установлено реляцией; в PLAN они записаны как факт.
7. **К-40** — восемь комментариев на удалённые символы отправлены в правку по списку D1 без открытия («НЕ проверял»); правка сделана.
8. **К-64** — из девяти доковых дефектов открыты шесть, три ушли в правку по реляции.
9. **К-31/К-38** — реляции помечены и НЕ действовали (в «Всплыло» и в CLOSEOUT как реляция) — правильно, не считаю.

Итого мест с действием на реляции: **8** (1–8). Три самых весомых: п. 1 (несуществующие квитанции под О-2/О-3), п. 2 (три REJECTED без своего ханка), п. 3 (пять R-nnn закрыты до собственного чтения).

**[KNOWN]-якоря оркестратора, проверенные мной на HEAD.** Опровергнутых — **ноль**. Проверил и подтвердил: `plane.rs:548`/`:562` (К-188), `node/dpos.rs:1572` единственный `geometry_tx.send_replace` (К-183/К-188; `git grep geometry_tx -- crates bins` — три строки, писатель один), `dpos.rs:2477` `cold_start`, `node/dpos.rs:1618` `track_peers`, `epoch_manager.rs:683` + `actor.rs:1244` — по одному паркующемуся приёмнику на канал (К-188), `application.rs:1108-1110` `beacon_tip.send_replace` (К-185), tokio `watch.rs:422` `thread_rng_n(8)` (К-185), `outer.rs:283-290` только `scoped` (К-117), `NoopBlocker` во всех трёх `OuterBuilder`-конструкциях `dpos.rs:2921`, `:4081` + стенд (К-89, в части «все NoopBlocker»), clippy `epoch_transition.rs:3024` + `node/dpos.rs:1985` (К-186), `beacon/` 23/38909 (К-189), `dkg_engine.rs:766` `biased;`, `:771` арм халта первым, `:955-957` перечитывание (К-181), `epoch_manager.rs:1462` `abort_below` sync, `:1020` единственный вызов (К-181). Дрейф якорей после последующих коммитов (не опровержение): К-175 `on_height :2227` → `:2217`, `epoch_of :1177` → `:1159`, `send_if_modified :2364-2376` → `:2350-2357`; К-178 `sync_metrics.rs:442/:547/:609/:621` → `:419/:524/:586/:599-602` (сдвиг −23 после 5.4-А); К-181 `on_halt :973` → `:978`, `on_tick :957` → `:962`.

**Самопоправки оркестратора, засчитываю ему:** К-89 → К-119 (исключение честного пира РЕАЛЬНО на уровне `fetcher.excluded`, а не транспорта), К-66 → К-76 (5.1 понизила уровень лог-строки), К-67 (моя ошибка про окно пина), К-85/К-100 (отравленные ворота), К-109 (бюджет 240 < 300), К-111, К-176 (`git checkout --` без путей).

---

## 4. Ворота

### 4.1 `x2-*` против `PLAN.md` §1 (`:34-37`, `:45`) [KNOWN, прочитано построчно]

`x2-status.txt`: девять шагов, все `exit=0`, `DONE`. `x2.md5` 14/14 `ЦЕЛ` против рабочего дерева = HEAD по `crates/`.

| ворота | `PLAN.md` §1 (на `c4543510`) | `x2-*.txt` | сходится |
|---|---|---|---|
| `fluentbase-consensus --lib` | **730**/0 | `ok. 730 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; 45.64s` | да |
| стенд с фичей | **57**/0 | `ok. 57 passed; 0 failed; 682 filtered out; 52.53s` | да |
| стенд без фичи | не назван (К-174: берётся из lib) | внутри `x2-lib.txt` — 48 строк `test testbed::` | — (числа в PLAN нет) |
| `fluentbase-node` | **57**/0 | `ok. 57 passed; 0 failed` | да |
| `fluentbase-staking-reader` | **64**/0 (+1 ignored) | `ok. 64 passed` + `0 passed; 1 ignored` | да |
| `slasher_integration` | 16/0 | `ok. 16 passed` | да |
| clippy 3 крейта | 0 своих, 2 чужих (`epoch_transition.rs:3024`, `node/dpos.rs:1985`) | ровно два предупреждения: `MutexGuard held across an await` → `epoch_transition.rs:3024`, `large size difference between variants` → `node/dpos.rs:1985` | да |
| clippy с фичей | 0 | 0 строк `warning`/`error` | да |
| `cargo doc` unresolved | 6 | `grep -c 'unresolved link'` = 6 | да |
| `cargo fmt --check` | (строка §1 «чисто» на `7790d1bc`) | 0 `Diff in` | да |
| `make harness-test` | 2 failed / 2086 / 10 | `2 failed, 2086 passed, 10 skipped`; красные — `test_every_grepped_token…[EL-sync fast-forwarded the anchor]` и `test_the_two_jump_gates…` (К-86/К-87) | да |

**Сходятся полностью.** Расхождений внутри `PLAN.md` §1 два, оба косметические: (а) строка `:26` «`cargo fmt --check` в корне красный на шести чужих файлах» стоит в «Сломано и наблюдалось», а таблица ворот `:45` говорит «чисто» — противоречие живёт с Э4; (б) строка «(было 5.0а)» `:37` оставлена как история — читается нормально.

### 4.2 Что ворота НЕ измеряют

- `cbd2fb68` (`Cargo.toml` +8: `[profile.test.package."*"] opt-level = 1`) — с него все числа ворот сняты на оптимизированных зависимостях (commonware, tokio); `debug-assertions` профиля `test` не тронуты. Ворота до/после (w2 → w3) совпали число в число (К-181), так что семантического сдвига не видно; но «красный до правки» для всех последующих мутаций мерился уже на новом профиле.
- Живые приёмки: `smoke-cert-follow`/`-cascade`/`-keyless` — зелёные по К-108/К-112/К-113 (реляция для меня, логов не открывал); `smoke-vrf-dkg-live-heal` на `d8747086` запущен (К-189), результата в объекте нет. Строки 5.3 и 5.4 живого свидетеля не имеют вовсе (стенд только).

---

## 5. Дрейф: удалённые символы в `.claude/dpos_architecture/`

[KNOWN] `grep -rn` по 19 файлам (каталог в `.gitignore`, `git grep` там пуст — учтено) для 18 символов постановки. Попаданий всего 209; исключены: журнал `verified-against` в `00_preamble.md` (`:6-2860`, по правилу файла — история), `00a_errata…` (историческая по имени), `.claude/session-reads/*.jsonl` (журналы чужих сессий, не доки). По остатку каждое попадание открыто:

| символ | попаданий | живых | где |
|---|---|---|---|
| `keys.rs` | 18 | 0 | все под `[REVISED/DELETED 2026-09-13]`; `05:23`, `08:1734` — `bls/keys.rs` (другой файл, живой); `08:980` — `genesis-bootstrap` `keys.rs` (удалён, так и написано) |
| `key_journal` / `carry.rs` / `resolve.rs` | 10 / 16 / 14 | 0 | `03:179,199,252-254`, `06:796`, `08:328,405-412,807,1491,1510`, `12:72`, `13:679-680`, `02:282-289` — все «DELETED»/«is deleted»/`[REWRITTEN]` |
| `follower.rs` / `certify.rs` | 16 / 8 | 0 | `09:126,197,202`, `03:345,422`, `13:837,1004`, `08:327,813,820,2368`, `15:430` — все «DELETED» |
| `waiters` | 15 | 0 | все про `plane_upstream` waiters (живой символ) и `notify_waiters` tokio — не тот символ |
| `on_invalid_seed` | 12 | 0 | `02:284`, `13:677`, `08:1203` — «GONE»/«DELETED» |
| `dkg_height_tx` / `LiveFrontierTee` / `note_height_drop` | 19 / 12 / 4 | 0 | `03:88`, `08:1331-1346,2306-2314`, `09:646-656,811-834`, `13:275-276` — «deleted»/«HISTORICAL» |
| `dkg_agreements` / `prune_agreements` / `agreement_intake` | 3 / 9 / 12 | 0 | `08:1237,2679-2683` — «DELETED»; `08:2690,2757,2795` — про живой `dkg_engine::prune_agreements` |
| `committee_pair_for` / `with_ordering_tip` | 0 / 0 | 0 | — |
| `for_keys` / `for_seeds` | 18 / 16 | 0 | `09:357,364`, `08:1231-1232`, `01:36` — «DELETED» |

**Живых упоминаний удалённых символов — 0.** `01_system_map.md:36` — одна строка-абзац, в которой каждое имя стоит с «DELETED» и датой.

`verified-against` (`00_preamble.md:6-60`): верхняя запись — 5.4-А круг 3, «base `08764870`», журнал `E5-4-A.md`; sha `c4543510`/`d8747086` в блоке нет ни разу (`grep` пуст) — блок ведётся по строкам, не по HEAD; содержательно = HEAD (описывает один клок, два канала, снятый tee). **Не равен HEAD по имени, равен по содержанию.**

**Дрейф, которого постановка не спрашивала, но он есть [KNOWN]:**
- Якоря `sync_metrics.rs` в доках сдвинуты 5.4-А (файл потерял `note_height_drop`/`drops`, −23 строки): `13_invariants…:632-635` (`:442/:547/:609/:621` → на HEAD `:419/:524/:586/:599-602`), `09_followers.md:1595` (`:442`), `08:2226` (`FinalizeApply :54` → `:50`). Ф7 5.4-А (К-188) перепинила `plane.rs`/`node/dpos.rs`/`dpos.rs`, этот блок пропустила.
- В КОДЕ: `beacon/mod.rs:40-51` — из девяти якорей верны три (`spec_exec.rs:123`, `surface.rs:2194`, `epoch_manager.rs:163`); `cert_inlet.rs:733-736` (сейчас комментарий про клок), `:3030-3032`, `:406/:472/:562/:847`, `dpos.rs:750-758` (это `replay_seed_source`, а `seed_via_beacon` — `:815-833`) — не туда. `surface.rs:229` «caller `epoch_manager.rs:1225`» → `:1015`. `mod.rs:239-241` алиас `SeedStore` «`epoch_manager.rs:2076,:2080,:2129,:2836,:2848`» → `:1859,:1863,:1913,:2620,:2632`, и обещание «Dropping the alias is one rename» строка 5.4 не исполнила. `committee/facade.rs:85-87`: «The `!(bit || committed) ⇒ None` guard in `beacon::carry` stays where it is» — `carry.rs` удалён (файл вне списка всех заходов). `epoch_manager.rs:1727-1730`: «`Thorough` carries the provenance floor… the pin» — тира и пина нет с 5.1. `dpos.rs:838-848` — см. §1.1 п. 4. `artifact.rs:1548-1553`: «PLAN row 5.4 is where the beacon's clock becomes a `watch` …; a clock-driven re-`sync` belongs there» — 5.4 закрыта, re-sync по часам не добавлен.
- `.dpos-study/REGISTER.md` R-121/R-122 статусы 09-13 называют `acquire_mint_artifacts`, `drive_recompute`, `beacon/actor.rs:2348-2355` — символов на HEAD нет (`git grep` пуст), заменены А1 (`drive_acquisition`, `try_recompute`); это датированные статус-строки, дрейфом по правилу реестра не считаю, но читатель по ним код не найдёт.

---

## 6. Всплыло для Э6/Э7 (требует строки плана)

Из моего чтения (нет в «Всплыло» оркестратора):
1. **Поздний `Refused` без повторного запроса высоты** (§9.1) — Д-3(в) реализована наполовину; нужна либо инвалидация высоты в marshal, либо re-observe архива на `KeyAvailable` для раундов, оставшихся без σ. Э6 (executor seed intake, 6.1) — естественное место.
2. **Свежий узел не входит в сеть, чей последний минт старше 8 эпох** (§9.2) — I4 «Частично»; адрес Д-2 (weak-subjectivity checkpoint с артефактом) или расширение окна `changed`-битов. Отдельная строка, не остаток.
3. **§5.4 «persist σ ⇒ повтор + error + gauge»** — не сделано, Д-nn нет (`seed_journal.rs:446`). 
4. **§5.2 «две проверенные σ за раунд ⇒ Conflict»** — не сделано, Д-nn нет; недостижимо при ≤ f, но проект обещал терминал и для > f.
5. **Хвосты границы**: `ensure_key`/`PinEffort` (Д-12), `observe_epoch`/`observe_cert`, `terminal_seed`, алиас `testing::SeedStore` — четыре «5.4 снимет» в доках кода, 5.4 закрыта. Одна строка Э6 «граница beacon: финальная форма».
6. `Stalled{reason}` — только gauge; если Э6 захочет реагировать на `Conflict`/`Unrecoverable` из executor/epoch_manager, события на шине нет.
7. Дрейф якорей `sync_metrics.rs` в `13`/`09` и якорей в `beacon/mod.rs:40-51`, `facade.rs:85-87`, `epoch_manager.rs:1727-1730`, `dpos.rs:838-848`, `artifact.rs:1548-1553` — один доковый проход.
8. `E5-ORCHESTRATOR.md`: восстановить К-8…К-20 из `3ead5c6c` и дописать К-21…К-24 или пометить О-2/О-3 как «квитанции утрачены».

Из «Всплыло» оркестратора (`:538-569`), что заслуживает строки, а не абзаца: R-016 (второй выход для execution-stalled узла); crash-recovery ложный деструктивный фатал в гонке (трёхзначный ответ на `CertUpstream`); тумбстоуненный член в `committee[E]` ⇒ `n−1 < quorum` (контракт, Э2/Э7); `ShareConfirm` нового члена вне окна (R-126 E-01); band-sweep при прыжке > 8 эпох (D-02); два `engaged_edge()` на одном `watch<bool>` (`epoch_manager.rs:601`, `dkg_engine.rs:757` — открыл оба) против инварианта «один приёмник на канал», который этап сам установил; три стендовых теста > 60 с; харнесс-сторож `min(interval as u64)` красный.

---

## 7. Где моя проверка была слабее всего (ранжировано)

1. **Ни одного прогона и ни одной мутации** — запрет постановки. Все «красные до правки» и «мутация различает» этапа — реляция оркестратора/агентов; я подтвердил только, что финальные числа `x2` = PLAN и что дерево = md5.
2. **`actor.rs` прочитан на ~40 % продакшна**: `on_message` (`:3490-3675`), `fetch_missing_logs`, `drive_finalization` (`:2676-2876`), `try_recompute` (`:3941-4086`), `resume_from_journal`, `ingest_log` — не читал. `ceremony.rs`, `dkg_agree.rs`, `dkg_engine.rs` (кроме grep по `prune_agreements`/`biased`/`on_halt`), `log_resolver.rs` — не читал. R-002 (хэш-идентичность) подтверждён по `ceremony_refusal` и реляции К-132, не по `ceremony.rs`.
3. **Стенд не читал** (`testbed/{tests,cert_inlet_tests,stand}.rs`) — что именно пинуют C7/тест 5.4-А/тест R-129, знаю из отчётов.
4. **Журналы строк и ревью читал через grep**, не целиком: `E5-2-A.md` (отклонения), `E5-4-A-REVIEW.md` (§0/F-01); `E5-1-A.md`, `E5-3-*.md`, `E5-4-B.md`, dsh-отчёты — не открывал. §3 построен по «Квитанциям» оркестратора, не по первоисточникам агентов.
5. **§9.2 (окно 8 эпох)** — по коду `MintIndex`/`facade`/`ensure_key`; что это ПРЕДСУЩЕСТВУЕТ 5.1, беру из К-27 [LIKELY] — старый обход `chain_key_epoch` не открывал.
6. Commonware чекаут не открывал: утверждения про `Update::Tip` на старте, `fetcher.excluded`, `send_lossy(true)` — реляция К-117/К-119/К-183.

## 8. Оставлено как есть

- `Pending ⇒ Pending` last-wins в `SeedIndex::admit` (`seed_index.rs:228-231`) — названо в коде и в ревью C-03…C-05; сама по себе не дыра, дыра — отсутствие повторного запроса (§9.1).
- `DataFault` на позднем `Refused` списывается на ТЕКУЩИЙ upstream независимо от двери, через которую σ вошла (`cert_inlet.rs:798-813`) — грубо, но ротация дешева.
- Артефакты при рехидрации не перепроверяются против комитета (`artifact.rs:1489-1507`) — доверие к собственному диску, как у share-файла.
- Durable-сторы beacon не привязаны к chain_id/genesis (партиции `mod.rs:160-190` без префикса, AAD share-файла = `(tag, version, epoch)`): datadir от прежнего генезиса на том же хосте молча переключает `recover` в `Acquiring(ArtifactForShare)`/`Keyed` по чужому минту (`:3182-3195`). Политика «всегда свежий генезис» + `down -v` в харнессе; записываю как известную границу, не как дефект.
- `persist_conflict` пишет `std::fs::write` без tmp+rename (`share_state.rs:686`) — рваный файл читается как `Malformed` ⇒ fail-closed `Conflict` без пары (`:705-720`) — безопасное направление.
- `ensure_key(Thorough)` ремонтного sweep-а спит в `throttle` до 5 с + `PULL_TIMEOUT` 8 с — на своей задаче (`epoch_manager.rs:1616-1645`), не в цикле менеджера. Не блокирует.
- `epoch_of` актора без ветки `last(e) ⇒ e+1` при `live_epoch_of` с ней (`actor.rs:1159-1163` vs `epoch_manager.rs:1757-1762`): cutoff лаунчера на высоту позже, дил-решение не задето (`decide_window` целит `now+1` в любом случае).
- Отклонения Д-5.4А-1, Д-5.4Б-1..3, Д-А1-*, Д-А2-* — по описаниям в PLAN; ни одно не меняет П-2/П-3/П-9 по существу (согласен с оркестратором: hard-stop (2) не наступал; «Hard-stop» пуст `E5-ORCHESTRATOR.md:699`).

---

## 9. Три самых опасных места

1. **Поздний `Refused` оставляет раунд без σ, и живого пути получить честную σ заново нет — Д-3(в) реализована наполовину.** [KNOWN по коду] Цепочка: на keyless-окне (у follower-а — штатное состояние, `surface.rs:319-320`; у валидатора — не-член/догоняющий) σ каждой финализации ложится `Pending` (`certificate_verdict :445-448`); второй сертификат той же высоты от другого источника (by-height дверь `UpstreamResolver`, второй upstream) перезаписывает её last-wins (`seed_index.rs:228-231`); ключ приходит, `settle_epoch` отвергает подделку и УДАЛЯЕТ запись (`:358-367`) ⇒ `DataFault` ⇒ только `record_data_fault`/ротация (`cert_inlet.rs:798-835`; `git grep DataFault -- crates` — потребитель один); честная σ лежит в сертификате, который marshal уже хранит, но никто его не пере-наблюдает: executor «never reads a certificate on the derive path… No park, no re-poke, no by-height re-fetch, no timer» (`executor.rs:24-26`), `seed_via_beacon` над архивом — только crash-replay (`dpos.rs:815-833`). Исход: `awaiting_seed` до re-jump (`JUMP_THRESHOLD.min(interval)` = R-016) или до рестарта; детектор через 60 с только пишет (`executor.rs:746-760`). Проект Д-3(в) обещал «marshal перезапрашивает высоту» (`DECISIONS.md:25`, `E5-BEACON-DESIGN.md:674`) — этого кода нет. Чем опровергал: искал любого потребителя `Observed::Refused`/`DataFault`, инвалидирующего высоту (нет); искал re-observe на `KeyAvailable` вне `settle_pending` (нет — `settle_pending` работает только над `Pending`-записями, удалённой записи нечего перепроверять). Один лгущий источник при ≤ f ⇒ парк исполнения на горизонте re-jump; стенд `a_forged_seed_slot…` это и показал («re-jump carried them forward», `15_smoke_cases…:430`), то есть свойство принято молча, без Д-nn. **Что сделал бы:** на `settle_epoch`-отказе класть раунд в `wanted` и на `KeyAvailable` пере-наблюдать локальную финализацию высоты (`seed_via_beacon` уже умеет) — одна функция, Э6.1.

2. **Свежий узел не может войти в сеть, чей последний минт старше `SCHEME_RETENTION_EPOCHS = 8` эпох.** [KNOWN по коду, предсуществование — LIKELY по К-27] `MintIndex::minted_at(E)` идёт вниз по битам `changed[e]` и на первом `None` возвращает `None`, ничего не записав (`artifact.rs:806-812`); бит вне окна модуля комитета — `None` (`facade.rs:47-54`, `OutOfWindow` сворачивается); `ensure_key` на `minted_at == None` — `false` (`surface.rs:2483-2485`); `KeyIndex::key_at` — `None` ⇒ оракул `NoKey` ⇒ каждая σ `Pending` ⇒ executor паркуется на первой обязательной высоте. Артефакт спросить нельзя — неизвестно, у кого и за какую эпоху (`AcquireArtifact::fetch(minted_at)`). При эпохе ≈ сутки и стабильном комитете это ОБЫЧНАЯ сеть через неделю после ротации. PLAN 5.1 записал это как «остаток I4, сужено, не закрыто» — по тяжести это не остаток, а условие входа в сеть. Чем опровергал: искал второй маршрут к минту (RPC `consensus_getEpochArtifact` отдаёт по ЭПОХЕ МИНТА — `artifact_bytes(epoch)` `:2498-2502`, эпоху надо знать); искал чтение бита мимо окна (`changed_bit` `plane.rs:1261-1266` — только через `read_at()` фасада). **Что сделал бы:** Д-2 (checkpoint с `(minted_at, artifact)`) либо `changed`-биты читать не из окна комитета, а как отдельный фактор без окна (биты не требуют весов).

3. **Инвариант «один паркующийся приёмник на `watch`», введённый этапом против процессного RNG tokio, уже нарушен внутри этапа на `SafetyHalt.engaged`.** [KNOWN] `sync_metrics.rs:419` `engaged: watch::Sender<bool>`; `engaged_edge()` `:599-602` = `subscribe() + wait_for`; два паркующихся: `epoch_manager.rs:601` и `dkg_engine.rs:757` (арм халта, `biased;` первым `:766-771`). Механизм тот же, что дал пять разных md5 на пять прогонов в 5.4-А (`watch.rs:422` `thread_rng_n(8)` — открыл): порядок пробуждения менеджера (abort движков) и лаунчера (abort инстансов) при латче — процессно-случайный. Сегодня оба действия идемпотентны, стенд халта не поплыл; но это ровно та «случайная защита», которую этап трижды называл дефектом (R-129 К-90, tee К-177, `NoopBlocker` К-89), и её никто не пинует. Чем опровергал: искал третий приёмник (`git grep engaged_edge` — два), проверял, что оба обработчика не зависят от порядка (abort+join у лаунчера `on_halt :978`, `abort_below` у менеджера — по чтению, не прогоном). **Что сделал бы:** второй канал (`watch<bool>` на лаунчер, записываемый тем же `latch()`), как сделано для tip; либо тест «латч при живых инстансах ×N прогонов — одна трасса».

---

## Прямые ответы

1. **П-2/П-3/П-9 как регрессия:** П-2 (Д-9) — **ДА**, оговорка: поздний `Refused` не ведёт к повторному запросу высоты (§9.1). П-3 — **ДА**, оговорка: I4 держится только в окне 8 эпох (§9.2), `ensure_key`/`PinEffort` остались на границе. П-9 — **ДА**, оговорка: `Stalled` не событие шины, σ-половина `Conflict` не реализована.
2. **I1–I8:** ДА — 6 (I1, I2, I3, I6, I7, I8), ЧАСТИЧНО — 1 (I4), граница протокола — 1 (I5). Сменили статус: I2 (+терминал > f), I3 (Нет→Да), I4 (Нет→Частично), I6 (Нет→Да), I7 (Частично→Да), I8 (Нет→Да); не сменили I1, I5.
3. **Три опасных места:** (1) поздний `Refused` удаляет σ раунда, повторного запроса высоты нет — парк до re-jump (`seed_index.rs:358-367`, `cert_inlet.rs:798-835`, `executor.rs:24-26`); (2) свежий узел с минтом старше 8 эпох не разрешает `PK_E` никогда (`artifact.rs:806-812`, `facade.rs:47-54`, `surface.rs:2483-2485`); (3) два паркующихся приёмника на `SafetyHalt.engaged` (`epoch_manager.rs:601`, `dkg_engine.rs:757`) — класс нондетерминизма, который этап сам открыл.
4. **Мест без квитанции — 8**; три самых весомых: К-21…К-24 не существуют ни в одной версии (О-2/О-3 без квитанций), а К-8…К-20 сняты коммитом `15e59c17`; три REJECTED приняты по реляции (К-136 D-12, К-138 E-08, К-165 EA-07); R-036/R-038/R-026/R-051/R-072 закрыты в REGISTER до собственного чтения (К-165).
5. **Опровергнутых [KNOWN] — 0.** Проверено 20 якорей (§3), все держатся; шесть сдвинуты последующими коммитами (`sync_metrics.rs:442/547/609/621`, `actor.rs:2227/1177`), свойства на месте.
6. **Ворота PLAN §1 против `x2`:** сходятся по всем одиннадцати строкам (lib 730, стенд 57, node 57, reader 64+1, slasher 16, clippy 0/2, с фичей 0, doc 6, fmt 0, harness 2/2086/10); md5 дерева 14/14. Несоответствие только внутри PLAN §1: `:26` «fmt красный на шести файлах» против `:45` «чисто».
7. **Дрейф доков:** живых упоминаний удалённых символов — **0** из 209 попаданий (все под DELETED/REVISED/HISTORICAL или про другой символ). Побочно: 6 якорей `sync_metrics.rs` в `13:632-635`, `09:1595`, `08:2226` сдвинуты; в коде — `beacon/mod.rs:40-51` (6 из 9 якорей не туда), `facade.rs:85-87` (`beacon::carry`), `epoch_manager.rs:1727-1730`, `dpos.rs:838-848`, четыре обещания «5.4 сделает» (`surface.rs:229-231`, `mod.rs:239-243`, `artifact.rs:1548-1553`).
8. **Поверхностные проходы — да, три:** (а) 5.3-В+Г1 круг 1 — карта оркестратора (решение 2: членство через окно ET) и стенд-тест исполнителя с `window_unset == 0` структурно; опрокинуты dsh тремя SERIOUS (К-145); (б) 5.1 исполнитель — «стенд с фичей» не гонялся ни разу до третьего возврата (К-45); 5.0а ревьюер R1 — ноль команд (К-20); (в) Ф7 оркестратора по 5.4-А — перепинены три файла, пропущены якоря `sync_metrics.rs` в `13`/`09` и обещания «5.4 сделает» в коде. Финальное Opus-ревью 5.4-А поверхностным НЕ считаю — F-01 (мутации краснят только по таймауту) оно нашло само.
9. **Не прочитал:** ~60 % продакшна `actor.rs` (`on_message`, `drive_finalization`, `try_recompute`, `resume_from_journal`, `ingest_log`), `ceremony.rs`, `dkg_agree.rs`, `dkg_engine.rs` (кроме grep), `log_resolver.rs`, весь стенд (`testbed/*`), журналы строк `E5-1-A/3-*/4-B.md` и dsh-отчёты целиком, чекаут commonware; cargo не запускал — «красный до правки» и мутации этапа для меня реляция.

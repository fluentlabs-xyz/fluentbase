# E4E5-FIX-A — правки по `э4э5-review.md` (F-4, F-2 ограниченная половина, §4 п.19, MINOR п.5/6/9/10/11/13/17, §3.3)

Исполнитель Opus 5, одна сессия, 2026-09-16. База `HEAD = c7828f65`
(`djadjka/dpos-reth-2.2-squashed`), дерево не закоммичено. Без субагентов. Пути без
префикса — от `crates/dpos/consensus/src/`; номера строк — по дереву ПОСЛЕ правок
(собраны `grep -n` в конце сессии). Теги: `[KNOWN]` — файл/строка открыт или команда
запущена в этой сессии; `[LIKELY]` — вывод из прочитанного; `[GUESS]` — не проверено.

## §0 Outcome

Сделано всё из списка scope: F-4 (a–d), F-2 (адаптер над модулем, `RethCommitteeSource`
удалён, все пять мест переведены), §4 п.19 (ретенция артефактов в RAM и в журнале), п.5,
п.6, п.9, п.10, п.11, п.13, п.17 и три дубля §3.3. Не сделано (и не должно было): F-1 —
только записка §A; F-5; п.7/8/12/15/16/18 — §2. Документация `.claude/dpos_architecture/`
поправлена в том же дереве (разделы 03, 04, 06, 08, 09, 15 + `verified-against` в
`00_preamble.md`); `.claude/` в `.gitignore`, так что в `git status` этих правок нет
[KNOWN: `git check-ignore -v`].

Ворота (все запущены после `cargo fmt --all`, вывод дословно):

1. `cargo fmt --all -- --check` — exit 0 [KNOWN].
2. `cargo clippy --workspace --all-targets --all-features -- -D warnings` — exit 101,
   НЕ из-за моего кода: `error[E0599]: the method par_iter_mut exists for struct HashMap<...>`
   и два `E0282` в `~/.cargo/git/checkouts/reth-9084db4313ec21c5/8cc96a3/crates/trie/sparse/src/state.rs:900-903`
   (объединение фич reth) плюс `error: use of deprecated method ... gas_used` в
   `crates/testing/src/evm.rs:277` — оба вне тронутых крейтов. Замена:
   `cargo clippy -p fluentbase-consensus -p fluentbase-node -p fluentbase-staking-reader -p fluent --all-targets --all-features -- -D warnings`
   — ровно две известные ошибки: `crates/dpos/staking-reader/src/epoch_transition.rs:2520:17`
   (`MutexGuard held across await`) и `crates/node/src/dpos.rs:1985:1` (`large size
   difference between variants`, `ValidatorUpstream`); `cargo clippy --workspace --all-targets`
   без `-D` — те же две, больше ни одной [KNOWN].
3. `cargo nextest run -p fluentbase-consensus` —
   `Summary [  42.983s] 769 tests run: 769 passed, 1 skipped`;
   `cargo nextest run -p fluentbase-consensus --features dpos-devnet-byzantine testbed::` —
   `Summary [  54.264s] 58 tests run: 58 passed, 721 skipped` [KNOWN].
4. `cargo nextest run -p fluentbase-node -p fluentbase-staking-reader` —
   `Summary [   2.601s] 121 tests run: 121 passed, 0 skipped`; в 2 прогонах из 5 падал
   `fluentbase-node dpos::tests::a_drain_finishes_only_once_the_last_sender_is_gone`
   («the writer task never ran», `crates/node/src/dpos.rs:2633`) — тест на реальном tokio с
   ограниченным циклом yield, в одиночку 5/5 зелёный; мой diff в `node/dpos.rs` — только
   делегирование `get_finalization_everywhere` на `ValidatorUpstream`, этот тест его не
   трогает. Флейк доправочный [LIKELY — на чистом дереве не гонял].
5. `cargo test --workspace --no-default-features` — exit 101: 17 падений, все в
   `fluentbase-e2e` (`e2e/src/builtins.rs` ×9, `staking_reserve.rs` ×6, `staking_cost.rs`,
   `staking.rs`), газовые ассерты rWasm (`left: 23607 right: 23095`); в первом прогоне
   их было 9, во втором 17 — недетерминированно. `cargo tree -p fluentbase-e2e
   --no-default-features -e normal,dev | grep -c 'fluentbase-consensus\|fluentbase-node\|fluentbase-staking-reader'`
   = 0: пути зависимости от тронутых крейтов нет [KNOWN]. Остальные бинарники зелёные, в
   т.ч. `745 passed` для consensus.
6. `RUSTDOCFLAGS="-D warnings" cargo doc -p fluentbase-consensus --no-deps` — exit 101,
   34 `error:` (5 `unresolved link`/`redundant`, остальные `private_intra_doc_links`); ни
   одна не на строке, добавленной мной — проверил все `-->` в тронутых файлах:
   `cert_inlet.rs:83, :318`, `dpos.rs:1870, :2692`, `epoch_manager.rs:256, :344, :346` —
   это доправочный текст [KNOWN]. Спецификация обещала «6 известных», в дереве их 34 до
   и после.

## §1 Per finding

| finding | files:lines changed | test added | red before / green after | если применить наполовину | чем пытался опровергнуть до правки |
|---|---|---|---|---|---|
| F-4a `failed()` текст | `committee/store.rs:190-197` | — (текст лога) | нет: строка лога, стендовый `a_reverting_committee_read_inside_the_window_refuses_the_epoch_and_leaves_the_node_up` грепает только префикс `committee read failed PERMANENTLY` и остался зелёным | безопасно само по себе | открыл `store.rs::committee` — `Read(Permanent)` не яд, `poisoned` только для `Impossible` (`:166-172`), значит следующий вызов действительно перечитывает; «no retry can fix it» было ложью |
| F-4b `reconcile_roles` | `epoch_manager.rs:652-693` (арм), `:1270-1293` (`CommitteeMiss`, `committee_miss`), `:309-312` (doc поля) | `a_permanent_read_fault_is_deferred_and_the_epoch_is_entered_on_the_next_wake` (`:2383`) | да: с армом `Read(_) => Refuse` — `assertion left == right failed: a permanent-but-not-impossible read fault must be parked for the wake-up / left: Refuse / right: Defer` | если `committee_miss` вернёт `Defer`, а вызывающий арм не вставит в `deferred_reconciles` — эпоха молча теряется до следующего boundary (ровно старое поведение) | искал, где ещё живая эпоха переспрашивается: `committee_wake_targets` (`:1364`) всегда добавляет `live` — верно, но НЕ живая (deferred) эпоха после `Permanent` терялась; опровергнуть не удалось |
| F-4c cold-start retry | `dpos.rs:218-284` (`COLD_START_READ_*`, `ColdStartRead`, `ColdStartRefusal`, `cold_start_committee_read`), `:2220-2264` (validator), `:3448-3486` (follower) | `dpos::cold_start_read_tests::{a_permanent_fault_is_re_asked_and_the_third_answer_is_taken, a_fault_that_survives_every_attempt_is_fatal_after_the_last, a_transient_miss_is_deferred_and_an_impossible_answer_refused_without_a_pause}` (`:5524`) | да по построению: функции не существовало, оба сайта были `Err(e) if !e.is_transient() => bail`; тесты проверяют счётчик попыток и виртуальное время на `deterministic::Runner` (2 паузы по 2 s, 4 паузы при 5 попытках, 0 при transient/impossible/below-window) | если хелпер есть, а сайт всё ещё матчит `!is_transient()` — retry не работает, тесты хелпера зелёные, сайт не покрыт (см. §4) | проверил, что `OutOfWindow`-below не `Permanent` и не `Impossible` (`committee/mod.rs:172-183`) — его нельзя ретраить, выделил в `BelowWindow` |
| F-4d watch = высота | `committee/store.rs:95-97, :112-119, :589-591, :606-616`; `committee/mod.rs:256-272` (trait, doc), `:679` (double); `committee/tests.rs:289-305, :746-772`; `epoch_manager.rs:445`; `crates/node/src/dpos.rs:1470` (комментарий) | `the_wake_up_fires_on_every_advance_and_carries_the_anchor_height` (переписан из `the_wake_up_carries_the_highest_readable_epoch_as_the_anchor_grows`) | нет: поведение «будит на каждый advance» уже было (`send_if_modified(\|_\| true)`); изменился ТИП (значение = высота, `send_replace`) и имя; старый тест по значению `epoch+2` теперь не компилируется | если переименовать трейт, но оставить `send_if_modified` с `max` — тесты на высоту падают; если `send_replace` без переименования — компилируется, семантика прежняя | `send_replace` у tokio `watch` = `send_modify` (`tokio-1.52.3/src/sync/watch.rs:1229-1234`), а `send_modify` уведомляет всех получателей безусловно, даже при равном значении [KNOWN: файл открыт] — и старый тест «advance без роста — wake-up» зелёный |
| F-2 адаптер | `cert_inlet.rs:87-135` (`ModuleCommitteeSource`, `RethCommitteeSource` удалён, импорты reth/reader сняты), `lib.rs:75-76`; `dpos.rs` `boundary_fetch_seam`, `build_committee` follower + адаптер в марше входа, старая сборка стора удалена; Restart recovery — ПОСЛЕ контр-ревью (CR-1) оставлен приватный прямой ридер `dpos.rs::RecoveryCommitteeSource` по гуляющему `parent_hash` (см. §5) | `cert_inlet::tests::the_module_adapter_answers_at_the_anchor_and_refuses_any_other_hash` (`:692`) | да по построению (типа не было); тест: `None` anchor ⇒ `Err("anchor is not executed")`, чужой хэш ⇒ `Err("reads only at its own anchor")`, свой ⇒ схема, которой реальный 2f+1 серт верифицируется | если адаптер стоит, а сайт передаёт НЕ якорный хэш — все чтения этого сайта отказывают (fail-closed): для `boundary_fetch` = verify-only на эпоху, для recovery = фатал на дыре; поэтому Restart-arm переведён на `anchor_hash()` явно | для каждого сайта проверил, чей хэш: `reseed_forward` (`executor.rs:2221-2378`) двигает курсор и зовёт `anchor_advanced` ДО `seed_boundary_below_floor(floor, landing_hash)` ⇒ `landing_hash == anchor_hash()` [KNOWN]; follower `rf_hash` = `canonical_state.get_finalized_num_hash()`, а `RethAnchor::height()` читает `provider.finalized_block_number()` = тот же `canonical_in_memory_state.get_finalized_num_hash()` (`reth .../blockchain_provider.rs:280-282`, `storage-api/src/block_id.rs:121-123`) [KNOWN]; recovery читал по `parent_hash` (гуляющий) — единственный сайт с не-якорным хэшем, переведён на якорь |
| §4 п.19 ретенция артефактов | `beacon/artifact.rs` (doc, `Durable`, `epochs` без `cfg(test)`, `retain`, `retain_mints_for_window`, `ArtifactJournal::retain`, writer); владелец вызова — ПОСЛЕ CR-5 `beacon/surface.rs` `LiveBeacon::observe_epoch` (см. §5), не актор | `artifact::tests::{retention_keeps_the_mints_the_window_resolves_to, retention_keeps_a_stable_committees_only_mint_far_below_the_floor, retention_reaches_the_journal}` (`:2181, :2212, :2225`) | да по построению (`retain` не существовал); минты {2,5,55}, now 60, окно 8 ⇒ {5,55}; {2,5,40} ⇒ {40} (52 разрешается в 40, не в 5); один минт 2 при now 60 и 10 000 ⇒ жив; журнал после reopen = {5,55} | если RAM `retain` есть, а `Durable::Retain` не применён в writer — после рестарта журнал вернёт удалённые минты (рост диска остаётся) | перечитал `ceremony_retain_floor` (тогда `actor.rs:475`): правило долей ровно «новейший минт ≤ now − окно и всё выше» — то, что просит spec; пример spec «5 stays if minted_at(52)» при {2,5,40} ложен: minted_at(52) = 40 |
| §4 п.5 `RotatedKey` | `epoch_manager.rs:878-910` (арм), `:1295-1330` (`SoftEnterReason`, `SignerDecision`, `signer_decision`) | `a_rotated_key_soft_enters_and_spawns_no_engine` (`:2473`) | да: с `RotatedKey(s) => Spawn(s)` — `panicked at epoch_manager.rs:2486: RotatedKey must soft-enter, got Spawn(..)` | если `signer_decision` даёт `SoftEnter`, а арм всё равно зовёт `upgrade_scheme` — модулю подсунут verify-only схему поверх своей (upgrade откажет как downgrade только при occupied signer; на вакантном слоте примет) | искал причину, по которой спавн был нужен: комментарий «reconciler aborts this engine on its next reconcile» — движок с `me() == None` бесполезен; `soft_enter` берёт `committee.scheme()` (`:929`), т.е. модульную схему с оракулом, что и требуют 03 §3 |
| §4 п.6 `Dropped(reason)` | `plane_upstream.rs:102-115` (`Delivery`, `Waiters`), `:400-419` (deliver), `:515-575` (`fetch_one`), тесты `:1055, :1085, :1122` + `:976, :1225` | три существующих drop-теста теперь `matches!(rx.await, Ok(Delivery::Dropped(REASON_OUT_OF_WINDOW / REASON_NOT_READABLE)))` | да: старый код закрывал sender ⇒ `rx.await` = `Err` ⇒ `matches!` ложно | если тип `Waiters` сменён, а `deliver` по-прежнему `remove` без send — компилируется, waiter получает `Err`, `fetch_one` пишет «abandoned (the serve side is gone)» — диагностика снова врёт, но иначе | проверил, что `deliver` при drop возвращает `true` и что `cancel` не нужен (`:404-411`) — оставил |
| §4 п.10 `WalkOutcome` | `cert_follow.rs:43-59` (enum), `:72-97` (trait default), `:191-199` (`fetch_verified_boundary`); `dpos.rs:440-455` (`refetch_verified_archive_hole`, `get_latest`-проба удалена), `:1735-1747` (`fetch_verified_entry`); `plane_upstream.rs:588-606`; `lib.rs:73`; `crates/node/src/cert_follow/upstream.rs:22, :49-52, :87-102, :311-331, :345-390` (`dispatch`), `:405-410`, enum + `into_option` удалены, тест `:1062`; `crates/node/src/dpos.rs:2005-2016` (делегирование) | — новых нет; переведены доубли `FakeUpstream`/`FlakyUpstream` (`dpos.rs:5020-5100`), 5 тестов `refetch_hole_tests` и 4 node `walk_tests` остались зелёными | нет (поведенческий инвариант тот же, изменился носитель); `upstream_missing_height_is_fatal` и `an_unreachable_upstream_is_not_a_data_loss_verdict` были бы красными, если бы default трейта отдавал `MissedEverywhere` | если трейт сменён, а `refetch_verified_archive_hole` матчит `Option` — не компилируется (типы); опаснее: если `ValidatorUpstream` НЕ делегирует — Ws-валидатор в crash recovery получает default (`NoneAnswered`) и на «reachable-but-missing» ждёт вечно вместо вердикта потери данных | открыл `node/dpos.rs:1990-2028` — `ValidatorUpstream` не делегировал `get_finalization_everywhere` и ДО правки (доправочный пробел, теперь закрыт — поведенческое изменение для Ws-валидатора: walk по всем URL вместо одного pull) |
| §4 п.11 `inflight` handles | `cert_inlet.rs:2409-2426` (`Inflight`), `:2447-2450`, `:2519-2523, :2578-2592` (`spawn_finalized`, `abort_pulls`), `:2648-2676` (`cancel`/`clear`/`retain`) | `a_cancelled_fetch_aborts_the_pull_in_flight` (`:1222`) | да: с `drop(handle)` вместо `abort()` — `assertion left == right failed: the cancelled pull must not return from the upstream / left: 1 / right: 0` | если слот `None` вставлен, а handle не записан — dedup работает, abort нет (старое поведение); если handle записан ПОСЛЕ того как задача уже удалила слот — утечка handle завершённой задачи (закрыто `get_mut` только на occupied slot, `:2582-2584`) | `Handle` drop ⇒ detach, не abort (`COMMONWARE_INTERNALS.md:409-411`) — поэтому в тесте `drop(handle)` действительно «не отменяет»; первая версия теста была вакуумной (`notify_waiters` не будил парк) — поймал тем, что тест прошёл и без cancel, заменил на `notify_one` |
| §4 п.13 `finalize_over_pinned` | `beacon/ceremony.rs:1052-1090` (`FinalizeError`, `?` вместо `expect`), `beacon/actor.rs:43-46, :2313` | `a_second_finalize_over_a_consumed_player_is_an_error_not_a_panic` (`:1291`) | да: второй вызов на старом коде паникует `expect("Agreed holds an unconsumed player")` | если сигнатура сменена, а `actor.rs` матчит `Err(DkgError::MissingPlayerDealing)` — не компилируется; `PlayerConsumed` идёт в общий `Err(err)` ⇒ `heal_over` (`:2338-2352`), как просил spec | искал второй вызов на одной церемонии в проде: `take_state(e)` забирает `Agreed{ceremony}` и после finalize кладёт `Keyed`/`Acquiring` (`:2287-2296, :2355`) — второго вызова нет, паника была недостижима, но `expect` в актёре всё равно не место |
| §4 п.17 архивы `Result` | `outer.rs:37` (import), `:258-263, :293-296, :301-307, :337-340` (сигнатуры + `map_err`), `:620-627` (`wrap_err`); `dpos.rs:964-978` (`wrap_err` в recovery); `executor.rs:11325-11336, :11477-11488` (тесты: `.expect`) | — | нет: сама ошибка init недостижима в тесте без ломаного storage | если init вернул `Result`, а вызывающий забыл `?` — не компилируется | — |
| §4 п.9 одна ретенция долей | `beacon/share_state.rs:617-676` (`ceremony_retain_floor` переехал сюда, `reconcile_journals` через него), `beacon/actor.rs:54-56` (import), удалена копия `actor.rs:458-478`; тест актора `:13139` импортирует новую | `share_state::tests::{reconcile_prunes_shares_by_the_same_floor_as_the_ram_store, reconcile_keeps_a_recently_superseded_share_inside_the_window, reconcile_keeps_all_when_no_share_is_old_enough_to_be_a_floor}` (`:1235, :1263, :1282`; два первых заменили старые `reconcile_prunes_superseded_shares_keeps_active_and_future` / `reconcile_keeps_all_when_every_share_is_future`) | да: со старым правилом `max{e ≤ now}` — `reconcile_keeps_a_recently_superseded_share_inside_the_window` FAIL (share 10 удалён при {10,15}, now 20) | если функция переехала, а `reconcile_journals` не переведён — компилируется, диск и RAM по-прежнему расходятся | старый doc `reconcile_journals` («keeping only the max would drop the active carry-forward whenever a future share exists») описывает частный случай нового правила — новое покрывает его (floor ≤ max ≤ now), опровержения нет |
| §3.3 `re_jump_seam` / `boundary_fetch_seam` | `dpos.rs:1287-1400` (две функции), `:2468-2495` (validator), `:3276-3300` (follower); удалены две пары замыканий ~60 строк ×2 | — | нет (без изменения поведения); guard — `executor::tests::a_follower_shaped_re_jump_*`, стенд | если одна из двух копий переведена, а вторая нет — дубль остаётся, поведение то же | diff двух копий: различия только `l1_checkpoint` (None/Some) и `tracked_epoch` (cell/`local_tracked_epoch`) — оба стали параметрами |
| §3.3 `live_epoch_of` | `epoch_manager.rs:1339` (`pub(crate)`), `dpos.rs:1402-1418` (`local_tracked_epoch` зовёт его) | — | нет; guard `dpos::local_tracked_epoch_tests::*` зелёный | — | формулы были байт-идентичны (`if last(e) == fin { e+1 } else { e }`) |
| §3.3 WS `dispatch` | `crates/node/src/cert_follow/upstream.rs:311-331, :345-390` | — | нет; guard — 4 `walk_tests` + `a_disconnected_actor_answers_every_pull` (`:1062`) зелёные | если `dispatch` возвращает `Some(response)` для `Rotate`, а вызывающий не обрабатывает — rotate-ACK потерян, `rotate().await` виснет; в главном цикле `Rotate` матчится ДО `dispatch` | `answer_while_disconnected` остался отдельным — у него нет `client` |

## §2 Leave as is

- F-1 — только §A, по условию.
- F-5 (два `OnceLock`, keepalive на `pending()`, фиктивная геометрия `Some((0,1))`, dummy `FluentP2P`, два tip-watch) — отдельный проход по условию.
- §4 п.7 (`sync_to` ⇒ `local_landing()` без сертификата) — вне scope; безопасность держится на `ResultDivergence`, не трогал.
- §4 п.8 (N re-poke задач на парк, `dpos.rs::enter_boundary`) — вне scope.
- §4 п.12 (`GatedReceiver` открыт до первого `track`) — задокументированное решение, покрыто юнитом.
- §4 п.15 (пустой снапшот после `commit_height` ⇒ вечный `NotReadable`) — вне scope; заметка: с F-4b такой эпохе теперь гарантирован retry на каждом wake, что и раньше для live.
- §4 п.16 (двойная классификация EVIDENCE) — намеренно, покрыто.
- §4 п.18 (`reseed_forward` → `boundary_fetch` через reader) — закрыто item 2 (сайт `dpos.rs:2338` старой нумерации теперь `boundary_fetch_seam` над адаптером), отдельно не трогал.
- `ReadError::Backend` остаётся `Permanent` (`staking-reader/src/error.rs` не менялся) — по условию 1a.
- Два startup-чтения геометрии через `RethStakingStateReader::new` (`dpos.rs:1953, :2722`) и `bins/fluent/src/main.rs:306` `ActivationProbe` — вне item 2 (не `RethCommitteeSource`).
- `EpochTransition` (Д-122) и тумбстоун-поллер — вне scope по условию.
- `ValidatorUpstream::get_epoch_artifact` НЕ делегируется (Ws-валидатор не тянет артефакт по WS) — доправочный пробел, не трогал (§3).
- `ArtifactStore.divergent` при `retain` не чистится — владелец маркеров конфликта — журнальный sweep актора; оставил.
- `RethCommitteeSource` как тип удалён; единственный сайт с не-якорным хэшем — Restart-arm recovery — получил приватный `dpos.rs::RecoveryCommitteeSource` (§5, CR-1).
- Комментарии `09_followers.md` «Pinned by `plane_upstream::tests::*`» перечисляют старые имена (`*_is_passed_on_unauthenticated`) — доправочный дрейф, не мой; не трогал.
- 03 §3 абзацы про «`RotatedKey` scheme is the epoch's FIRST registration and lands in a vacant slot» (`03_epoch_machinery.md:650-656, :944-950`) — исторические `[REVISED 2026-08-25]` блоки про закрытый путь; добавил примечание в таблицу продюсеров (`:581`), сами блоки не переписывал.

## §3 Adjacent

- `ValidatorUpstream` (`crates/node/src/dpos.rs:1990`) не делегировал `get_finalization_everywhere` до правки — Ws-валидатор в crash recovery делал один pull + `get_latest`-пробу вместо walk по всем URL; теперь делегирует (поведенческое изменение, отмечено в 09 §9.5).
- `fluentbase-node dpos::tests::a_drain_finishes_only_once_the_last_sender_is_gone` — флейк под параллельной нагрузкой nextest (2/5 полных прогонов), `crates/node/src/dpos.rs:2627-2633` цикл на 1024 yield.
- `cargo test --workspace --no-default-features`: `fluentbase-e2e` падает недетерминированно (9 или 17 тестов, газовые ассерты и staking e2e) — не связано с consensus/node (`cargo tree` = 0 путей).
- `cargo clippy --workspace --all-features` не компилируется из-за `reth-trie-sparse` в чекауте reth (`state.rs:900`) и deprecated `gas_used` в `crates/testing/src/evm.rs:277` — ворота 2 в буквальной форме недостижимы на этом дереве.
- Rustdoc: 34 ошибки при `-D warnings`, не 6; все на доправочных строках.
- Пример spec для п.19 «5 stays if it is minted_at(52)» при {2, 5, 40}: minted_at(52) = 40, 5 уходит; тест построен на {2, 5, 55}, где 5 действительно minted_at(52).
- Restart-arm recovery: первая версия читала через модуль по якорю; CR-1 показал две причины фатала (окно при devnet-интервале 32, неопубликованная геометрия) — откачено на `parent_hash` через `RecoveryCommitteeSource` (§5). Тестов на Restart-arm с дырой по-прежнему нет.
- `SchemeCommittee::faults`/`set_anchor_hash` — новые ручки двойника (`committee/mod.rs:599-612`), используются тремя тестами.

## §4 Weakest check

1. **F-4c на сайтах.** Хелпер покрыт юнитами, но оба `match cold_start_committee_read(..)` в `launch`/`launch_follower` (`dpos.rs:2220, :3448`) никаким тестом не гоняются (стенд `launch` не строит, как и до правки). Что арм `Permanent` печатает правильный текст — только чтение.
2. **F-2 на Restart-arm.** После CR-1 поведение прежнее (чтение по `parent_hash`), но через новый тип `RecoveryCommitteeSource` — сам тип покрыт только компиляцией (юниты `refetch_hole_tests` работают на `CannedCommittees`, стенд Restart с дырой не моделирует).
3. **F-2 на follower-марше.** Равенство `rf_hash == anchor_hash()` доказано чтением reth (`blockchain_provider.rs:280`), но живого follower-старта в тестах нет (стенд follower не строит — известно из 09 §9.6.1). Несовпадение = отказ и повтор через `ACTIVATION_POLL`, не фатал.
4. **п.10 для Ws-валидатора.** Делегирование `get_finalization_everywhere` — единственный тест `a_disconnected_actor_answers_every_pull` проверяет тип ответа, не маршрут через `ValidatorUpstream`.
5. **п.19 на ребре `observe_epoch`.** Покрыто юнитом `the_epoch_edge_retains_…` на `LiveBeacon`; что `reconcile_roles` действительно доводит `highest_entered_epoch` до этого вызова на follower — только чтение `epoch_manager.rs:706`.
6. **Ворота 2/5/6** в буквальной форме красные по доправочным причинам (см. §0) — доказано `cargo tree` и списком `-->`, но не прогоном на чистом дереве (stash/worktree запрещены условием).

## §A F-1 — записка (ключ PK_E для узла, вошедшего прыжком/чекпоинтом в сеть со стабильным комитетом)

**Факт [KNOWN по цепочке].** `MintIndex::minted_at(E)` (`beacon/artifact.rs:652-680`) идёт от `E` вниз до `DETERMINISTIC_BOOTSTRAP_EPOCH+1`, читая `bit(e)` (`:682-691`) через `ChangedAt` = `plane.rs::changed_bit` (`:1108-1113`) = `CommitteeReadsFacade::dkg_qual` (`committee/facade.rs:56-58`) = `record(epoch)?.changed`, а `record` (`:26-33`) схлопывает `NotReadable | OutOfWindow | Read` в `None`. Модуль отвечает только в окне `[epoch(anchor) − 8, epoch(anchor) + 2]` (`committee/store.rs:146-152`). Значит для узла с пустым мемо, чей якорь на > 8 эпох выше последнего минта, первый же `bit(e)` ниже окна даёт `None` ⇒ `minted_at` = `None` ⇒ `ensure_key` ⇒ `false` ⇒ `seed_at_own_round` = `Missing` ⇒ executor паркует блок (`executor.rs:1692-1704` по ревью, не открывал). Контракт пишет бит один раз при коммите: `contracts/staking/src/consensus.rs:598-602` (`dkg_qual_accessor().entry(target).set_checked(sdk, changed)`), читается `get_dkg_qual` (`:616-627`), reader `RethStakingStateReader::dkg_qual` (`staking-reader/src/reader.rs:480-482`). Негативный контроль в `artifact.rs` (тест `a_stable_committees_mint_outlives_the_retention_window`, ветка «fresh memo over a blind chain») утверждает именно этот отказ.

**Вариант 1 — контракт: `getLastDkgMintEpoch(at)`.** Одна ячейка `last_dkg_mint_epoch`, пишется в `commit_epoch_committee` рядом с `dkg_qual` (`consensus.rs:598-602`): `if changed { last_mint = target }`; handler по образцу `get_dkg_qual` (`:616-627`); ABI в `staking-reader/src/abi` + метод `RethStakingStateReader::last_dkg_mint_epoch(at)` рядом с `dkg_qual` (`reader.rs:480`); в consensus — новый метод `beacon::CommitteeReads::last_mint(at)` и реализация в `CommitteeReadsFacade` (`facade.rs`), которая НЕ идёт через `committee()` (одна staticcall по `anchor_hash()`, без окна); `MintIndex::minted_at` (`artifact.rs:652`): при `None` от walk — спросить `last_mint`; если `last_mint ≤ E` — это и есть ответ (мемоизировать). Плюсы: O(1), не зависит от окна, ответ — факт цепи. Минусы: миграция контракта (сети перезапускаются с genesis — память `dpos-deploy-always-fresh-genesis`), новый ABI-метод, инвариант «минт ⇔ changed» переезжает в контракт. Тест: `artifact.rs` — стабильная цепь (`changed = Some(false)` в окне, `None` ниже), пустое мемо, `frontier = BOOTSTRAP + 10·8`, `CommitteeReads::last_mint` = `Some(BOOTSTRAP)` ⇒ `keys.minted_at(frontier) == Some(BOOTSTRAP)` и `key_at(frontier) == Some(pk)` — переворот нынешнего негативного контроля; стенд: follower/валидатор с потерянным datadir входит прыжком в сеть с `changed=false` на 20 эпох и деривирует beacon-active блок (стенд follower не строит — нужен валидатор с `--dpos.l1-checkpoint`-подобным входом; сегодня такого сценария в `testbed/tests.rs` нет).

**Вариант 2 — узел: читать `dkg_qual` вне окна комитета.** Бит не требует весов и не привязан к кольцу (`dkg_qual` — `entry(epoch)`, не ring; `consensus.rs:598-602`), так что `OutOfWindow`-below для него не обоснован. Правка: `committee::Committee::changed(epoch)` (`committee/mod.rs:212-214`) перестаёт быть «same record, same read»: для эпохи НИЖЕ окна модуль делает ОДИН staticcall `dkg_qual(epoch, anchor_hash)` без записи в `records` (новая ветка в `CommitteeStore::committee`/`changed`, `store.rs:411-470`; свой счётчик, кэш битов — уже есть в `MintIndex::bits`); `CommitteeReadsFacade::dkg_qual` (`facade.rs:56`) зовёт `changed`, а не `record`. Плюсы: без контракта, без ABI; walk остаётся тем же. Минусы: walk длиной до `E − BOOTSTRAP` staticcall'ов при пустом мемо (по одному на эпоху, кэшируется в `bits`; на длинной стабильной цепи — сотни вызовов один раз); ломается доктрина «модуль читает только в окне» (06 §6.2b «predicate on the request») — нужно явное исключение в трейте (`changed` документируется как «окно не применяется»); риск, что чужой p2p-фрейм заставит узел делать staticcall по произвольной эпохе (сегодня `deliver` зовёт `committee`, не `changed` — проверить всех вызывающих `changed`). Тест: тот же `artifact.rs`-переворот негативного контроля, но с `SchemeCommittee::with_window` (`committee/mod.rs:560-570`), где `changed(e)` ниже окна отвечает `Ok(false)`, а `committee(e)` — `OutOfWindow`; плюс юнит в `committee/tests.rs`: `changed(e_below_window)` = 1 staticcall `dkg_qual`, 0 `epoch_committee_snapshot`, запись в `records` не появилась.

**Не решаю здесь.** Выбор — между стоимостью миграции контракта (1) и отступлением от «одно окно, один предикат» (2); оба закрывают F-1 полностью только вместе со стендовым сценарием входа прыжком на стабильной цепи, которого нет.

## §5 Контр-ревью (Opus 5, `reviewer`, свежий контекст, read-only) и что принято

13 AGREE / 1 DISAGREE_CONCERN по 17 пунктам; 5 находок.

| # | находка | моя проверка [KNOWN] | решение |
|---|---|---|---|
| CR-1 🟠 | Restart-arm recovery через модуль по якорю = reth finalized tag фатален на дыре, если (i) `EPOCH_BLOCK_INTERVAL = 32` (devnet, `bootstrap.rs:222`) и дыра выше `epoch(tag)+2`; (ii) поллер плана (`node/dpos.rs:1572`, единственный писатель geometry-watch) ещё не опубликовал геометрию — в `launch` до этой точки ожидания нет (grep `geometry` в 1940–2110 пуст) | оба открыл; (ii) я пропустил | ПРИНЯТО: recovery возвращён на чтение по `parent_hash` через приватный `dpos.rs::RecoveryCommitteeSource` (`:893-940`), параметр `committee_at` снят; `ModuleCommitteeSource` остался на четырёх сайтах; 06 §6.2b, 09 §9.1, preamble поправлены; consensus 769/769 после правки |
| CR-2 🟡 | тест F-4b не гоняет арм `Defer => deferred_reconciles.insert` | верно | ЗАКРЫТО: арм вынесен в `epoch_manager::park_committee_miss(deferred, safety_halt, epoch, e)` (`:1256-1305`), `reconcile_roles` зовёт его; тест `a_permanent_read_fault_is_deferred_…` теперь гоняет сам арм: Backend ⇒ в `deferred`, latch не взведён; PeerKey ⇒ latch взведён, `deferred` пуст |
| CR-3 🟡 | гонка RAM-`retain` / `Durable::Retain` vs `insert` минта ниже пола — расхождение на один sweep | верно | ЗАКРЫТО: `insert` и `retain` отправляют `Durable::{Append, Retain}` ПОД write-lock RAM (`artifact.rs`), журнал применяет их в порядке RAM |
| CR-4 🟡 | `unreachable!` в post-rotate drain (`upstream.rs:322`) | доправочный, сохранён | не мой |
| CR-5 🟡 | follower без `DkgActor` ⇒ п.19 на нём не бежит, RAM-стор растёт на артефакт за смену состава | верно | ЗАКРЫТО: единственный владелец ретенции — `LiveBeacon::observe_epoch(_, entered_frontier)` (`surface.rs`, новый default `Randomness::observe_epoch`, проброс из blanket-impl `Beacon`), который `reconcile_roles` зовёт на ОБОИХ классах (`epoch_manager.rs:706`); вызов из `DkgActor::sweep_epoch_state` и `Wiring.artifacts` удалены (отклонение от spec «call it from sweep_epoch_state» — ради одного владельца). Тест `surface::tests::the_epoch_edge_retains_the_artifacts_the_window_resolves_to`: {2,5,55} @60 ⇒ {5,55}; @10 000 ⇒ {55} |

Дополнительно от агента: `CommitteeStore::new` теперь читает `anchor.height()` при конструировании (`store.rs:112`) — одно провайдерное чтение; принято к сведению.

Ворота после закрытия CR-1/2/3/5: fmt 0; clippy тронутых крейтов — только известная `node/dpos.rs:1985`; consensus `770 passed`; testbed `58 passed`; node+staking-reader `121 passed` (тот же флейк `a_drain_finishes_…` в одном прогоне из двух).

## §Z Closing answers

- **Счёт.** Findings fixed: 13 (F-4a, F-4b, F-4c, F-4d, F-2, п.19, п.5, п.6, п.9, п.10, п.11, п.13, п.17) + 3 дубля §3.3 = 16 позиций; deferred (по условию): F-1, F-5, п.7, п.8, п.12, п.15, п.16, п.18 = 8; refuted-by-me: 0 (пример spec для п.19 уточнён, не опровергнут). Tests added: 15 новых (`epoch_manager` 2, `dpos::cold_start_read_tests` 3, `cert_inlet` 2, `plane_upstream` 0 новых/3 усилены, `ceremony` 1, `share_state` 3 (2 замены + 1), `artifact` 3, `committee/tests` 1 переписан). Files touched: 18 кода (`git diff --stat HEAD -- crates/`: 16 consensus + 2 node, +1704/−797) + 7 docs в `.claude/dpos_architecture/` (не под git) + этот журнал.
- **Три правки, которые вероятнее всего сломали то, чего тест не покрывает.** (1) (снято после CR-1: recovery снова читает по `parent_hash`.) (2) Follower: стор строится внутри марша входа; `e_max`-проба и `fetch_verified_entry` теперь требуют `rf_hash == anchor_hash()` — при расхождении in-memory finalized и якоря вход крутится на `ACTIVATION_POLL` без фатала, но и без входа. (3) Ws-валидатор: `ValidatorUpstream` теперь делегирует walk — в crash recovery Ws-валидатор посещает все URL и может получить вердикт `MissedEverywhere` (фатал «re-sync the EL disk») там, где раньше делал один pull + пробу.
- **Отклонения от spec.** (а) Тесты (b) и п.5 «через `reconcile_roles`»: построить `epoch_manager::Actor` в юните нельзя — `MarshalMailbox::new` в commonware `pub(crate)` (`marshal/core/mailbox.rs:150-152`, чекаут `3c4e02c`), `FluentApp`/`SlasherMailbox`/`spec_exec` тоже нужны; вместо этого арм вынесен в `park_committee_miss`/`signer_decision`, которые `reconcile_roles` зовёт, и тест бьёт по ним + `committee_wake_targets` + двойнику с `fault_reads`. (д) п.19: вызов ретенции не из `DkgActor::sweep_epoch_state`, а из `LiveBeacon::observe_epoch` — единственный владелец на обоих классах (CR-5). (е) F-2: Restart-arm оставлен на прямом ридере `RecoveryCommitteeSource` (CR-1). (б) `Delivery::Admitted` и `WalkOutcome::Got` — `Box` (clippy `large_enum_variant`), spec тип не задавал. (в) `SignerDecision::Spawn(Box<BlsScheme>)` — то же. (г) п.9: старые тесты `reconcile_prunes_superseded_shares_keeps_active_and_future`/`reconcile_keeps_all_when_every_share_is_future` кодировали старое правило и заменены (не «добавлен тест» поверх).
- **Неверные якоря ревью при открытии.** `dpos.rs:2026–2047` / `:3379–3393` — совпали; `epoch_manager.rs:645–700` — совпал (`:625` начало fn); `:885–893` RotatedKey — совпал; `store.rs:605–629` — совпал; `plane_upstream.rs:509–546` — совпал; `ceremony.rs:1073–1076` — совпал; `dpos.rs:891–901` — совпал; `share_state.rs:644–650` — совпал; `cert_inlet.rs:2552–2556` — совпал; `dpos.rs:419–479` — совпал; `upstream.rs:304–339 / :350–387` — совпали; `RethCommitteeSource` сайты `:1900, :2338, :2732, :2881, :3211` — совпали. Не совпало: утверждение spec «6 pre-existing unresolved links» (в дереве 34 rustdoc-ошибки при `-D warnings`); пример spec п.19 «{2,5,40}: 5 stays if minted_at(52)» (5 не minted_at(52)); `node/dpos.rs:306` в spec — это `bins/fluent/src/main.rs:306` (не трогал ни то, ни другое).
- **Чтение длинных строк.** `sed -n A,Bp` и `grep -n` через Bash; `cut -c1-N` для таблиц доков. `grep -n '…$' .dpos-study/history/E4E5-FIX-A.md | wc -l` = 0 (единственное «…» в файле — внутри этой строки, не в конце строки).

# Э4 — проход предусловий (Д1–Д10). Журнал 2026-09-11

Ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `1b11b61f`, рабочее дерево под `crates/` = HEAD до правок этого прохода. Проект — `history/E4-CORE-DESIGN.md` (писался против `1e5f394e`; между ними `777f7916` переписал 30 файлов под `crates/`). Commonware — чекаут `~/.cargo/git/checkouts/monorepo-27b478c9bb41d208/3c4e02c` (v2026.4.0; второй чекаут `monorepo-9732103c47eb4665/3c4e02c` из шапки `.claude/COMMONWARE_INTERNALS.md` — тот же коммит); reth — `~/.cargo/git/checkouts/reth-9084db4313ec21c5/8cc96a3`. Пути без префикса — `crates/dpos/consensus/src/`; `node/` = `crates/node/src/`; `CW:` — чекаут commonware; `RETH:` — чекаут reth. Теги: [KNOWN] — прочитано/прогнано в этой сессии; [ГИПОТЕЗА] — вывод.

## §0. Прямые ответы

1. **Д1–Д10.** Закрыто экспериментом (стенд-тест с посылкой) — 4: Д3, Д4, Д5, Д8. Закрыто кодом (чтение чекаута/HEAD, без прогона) — 5: Д1, Д2, Д6, Д7, Д9. Открыто — 0; при этом у Д7 половина «что делает НАШ узел» — [ГИПОТЕЗА по коду] с приёмкой в строке 4.2 (живой прогон E4-28), у Д8 девнет-дозвон — приёмка 4.3, у Д4 замер на девнете под нагрузкой — остаётся [ГИПОТЕЗА] о проде (§0.10 (7) проекта). Д10 — пересчёт: суммарная оценка 9–11 нед не изменилась, состав оценок записан.
2. **Д3.** Ступень обслуживается, глубина — БЕЗ СРОКА для всего, что узел когда-либо сохранил: fluentbase держит финализации и блоки marshal'а в `immutable::Archive` (`outer.rs:422-423`, `:439`, `:483`), чей `prune` — no-op (CW `consensus/src/marshal/store.rs:223-226`, `:261-264`); `SetFloor` зовёт `prune_finalized_archives` (CW `core/actor.rs:698-702`) впустую, а чтения по высоте, которыми отвечают на `Finalized{h}` (`core/actor.rs:808-818` → `get_finalization_by_height`/`get_finalized_block` `:1369-1393`; `plane_upstream.rs:147-166`), floor не смотрят. `last(E)` финализируется ЯВНО: на стенде ни одной высоты «блок без сертификата» на честных прогонах (см. ниже, исключение — дыра прыгнувшего узла). Тесты (`testbed/preconditions.rs`): `every_node_serves_the_terminal_pair_of_every_passed_epoch_after_re_jumps` — утверждение: у каждого из 4 узлов (узел 3 — с двумя посадками 125/157 и floor 170 > `last(3)` = 127) `archive[i][last(E)] = {finalization: true, block: true}` для E = 0..4 и `block-only = []`, `missing = []`; `a_node_more_than_two_epochs_behind_freezes_its_marshal_at_the_ceiling_and_jumps_over_a_hole` — утверждение: marshal узла 3 замерзает ровно на `last(epoch(95)+2)` = 159 (160 отсутствует), прыжок 95 → 180 (потреблён сертификат 183) оставляет дыру 160..177 и «блок без сертификата» 178..182, терминалы у всех узлов на месте. Лестница §5.2 стартует как написана — **да**: `committee(T+1)` финализировало `last(T+1)` само и хранит пару без срока.
3. **Д2.** `qual_read_at` УХОДИТ в 4.1. Причина: он введён в 5.0 (Д-14) ради окна `fin = None ∧ live > 0`, где `read_at` откатывается на генезис (`node/dpos.rs:1090-1096`, `:1107-1110`) и write-once мемо `frozen_dkg_qual` заморозило бы `false`; у модуля этого окна нет — при `anchor < commit_height(E)` он отвечает `NotReadable` без EVM, при `anchor ≥ commit_height(E)` бит окончателен, потому что пишется в том же `commit_epoch_committee`, что и запись (`contracts/staking/src/consensus.rs:632-635`); второй страж `!(bit || committed) ⇒ None` (`beacon/carry.rs:238-240`) остаётся до 5.1. Подшаг 4.1: существующий `beacon::CommitteeReads` (`beacon/plane.rs:108-148`) — фасад над модулем `committee::Committee`, методы те же, курсор — якорь модуля, параметр `at` в окне на значение не влияет (write-once), три реализации и два курсора удаляются (§7 4.1 проекта, таблица §5.1 «Кто читает»). Оценка 4.1 — 4–5 нед, не изменилась: модуль + окно + ошибки ~1.5 нед; фасад, снятие трёх impl'ов и `qual_read_at` ~3 дн; перевод `EpochSchemeProvider`/ET×1/`CertInlet.schemes`/slasher/follower-держателей ~1.5 нед; `FakeStaking` по ветке хэша, четыре стенд-теста, e2e-пин ~1 нед.
4. **Д1.** Якорей в §5/§6/§7/§10, указывающих в 30 переписанных файлов, — 229 (из 517 якорей диапазона); перепинено 206, без изменений 22, потерял предмет 1 (`node/dpos.rs:1408-1432`, closure `committee_pair_for` — стал provided-методом трейта `beacon/plane.rs:143-147`). Из перепинутых 21 сменил не номер, а носителя (closure'ы → `PlaneCommitteeReads`/`FollowerCommitteeReads`, рёбра `share_n`/`key_n` → одно `next_reconcile_wake`) — записаны в §6 «Следствия 5.0 для Э4». Строки плана меняет по составу только 4.1 (удаляемое: один impl + два курсора вместо пяти closure'ов; follower-держателей четыре, не три); 4.2/4.3 — только номера.
5. **Д7.** reth реорг под чужим `finalized` ДОПУСКАЕТ: `apply_chain_update` применяет реорг до проверки тегов (RETH `crates/engine/tree/src/tree/mod.rs:1294-1299`), `update_finalized_block` переставляет `finalized` на тег локального FCU, если тот на новой канонической цепи, иначе `invalid_state` (`:3109-3140`, отказ `:3117-3122`); проверки «голова — потомок текущего finalized» в `on_new_head` (`:862-941`) нет.
6. **Ворота** (verbatim в §4): `cargo test -p fluentbase-consensus --lib testbed::` — `32 passed` (27 + 5); с `--features dpos-devnet-byzantine` — `40 passed` (35 + 5); `cargo clippy … --all-targets --features dpos-devnet-byzantine` — 0 строк; `cargo fmt --check` — 0 строк. Новые тесты: `every_node_serves_the_terminal_pair_of_every_passed_epoch_after_re_jumps`, `a_node_more_than_two_epochs_behind_freezes_its_marshal_at_the_ceiling_and_jumps_over_a_hole`, `the_head_gap_band_is_measured_on_fcu_events`, `dkg_bodies_per_peer_are_measured_under_a_partition_in_the_agreement_window`, `a_secondary_peer_on_the_authenticated_transport_is_accepted_and_heard`. Красным на посылке был ОДИН — первый: моя формулировка «каждый узел на две эпохи дальше `last(E)`» для E = 4 требовала высоты 191 при остановке фикстуры на 168; посылка переписана на то, что фикстура даёт (E ≤ 3 — две эпохи, E = 4 — одна), стенд не трогался. Ни один тест не был красным из-за того, что стенд не дошёл до сценария.
7. **Начинать 4.1 в понедельник — да.**
8. **Где подогнал/прочитал вместо прогона.** (а) Ядро ответа Д3 («не прунится») — чтение `store.rs`, стенд это лишь подтвердил; прогон не мог бы показать прунинг, которого нет в коде. (б) Д6 и Д7 — только чтение чекаута, прогонов нет (по условию задачи). (в) Д8: secondary C дозвонился первым (bootstrapper A), так что «C никого не набирают» показано в мире, где соединение уже есть; «не набирают по политике» — по-прежнему `record.rs`, не прогон. (г) Д4: параметры второго прогона (линк 250 мс, потери 5 %) выбраны мной, не сняты с девнета; полоса 1 — свойство темпа стенда (1 блк/с, 10 мс). (д) Д5: разрез после высоты 46 на 6 view — момент выбран мной; тест утверждает, что разрез внутри эпохи 1 и ключ выпущен, но НЕ утверждает, что согласование в момент разреза было в полёте — название теста обещает больше, чем посылка проверяет. (е) Посылка «floor поднялся» в первом Д3-тесте — метрика `processed_height`, которую двигает КАЖДЫЙ ack, а не только прыжок; прыжок-специфичная посылка — непустой список посадок и floor ≥ последней посадки.
9. **Слабее всего** — §6: замер полосы (Д4) на стенде, чей темп не продовый; Д8 без отрицательного контроля на «не набирают»; Д5 без доказательства, что разрез накрыл согласование; Д7-половина про наш узел — по коду; §10 проекта (69 «принято» строк) и §0.1–§0.9 проекта в этом проходе не перечитывались.

## §1. Таблица дыр

| Д | исход | свидетельство | куда внесено | чем пытался опровергнуть и почему не вышло |
|---|---|---|---|---|
| Д1 | закрыта кодом | скрипт по тексту: старые строки `1e5f394e` найдены в `1b11b61f` по содержимому (206 совпали), 21 якорь-ссылка на переписанный код сопоставлена вручную (`node/dpos.rs:1400-1489`, `:1090-1110`; `consensus/dpos.rs:3416-3487`, `:3225-3256`; `epoch_manager.rs:768-893`; `cert_inlet.rs:318-361`); 1 предмет утрачен | §5/§6/§7/§10 проекта — пометка `[перепинено 1b11b61f]` у каждого из 206; §6 «Следствия 5.0 для Э4» (8 строк) | выборочно открыл 8 перепинутых строк на HEAD (`executor.rs:2441`, `:3291`, `:3330-3340`; `node/dpos.rs:733`, `:814-822`, `:1383`; `cert_inlet.rs:823`, `:894-896`) — код тот же; для коротких строк проверил число совпадений в новом файле — 1 у всех, кроме `executor.rs:2207` (2 совпадения, взято ближайшее к старому номеру) |
| Д2 | закрыта кодом | `git grep 'CommitteeReads\|qual_read_at\|read_at()'`: трейт `beacon/plane.rs:108-148`, три impl'а (`node/dpos.rs:1400-1489`, `consensus/dpos.rs:3416-3487`, `stand.rs:1850-1898`), проекции `plane.rs:589-607`, `follower.rs:121-133`; контракт `consensus.rs:632-635`; мемо `carry.rs:238-240` | §5.1 «Кто читает» (таблица 6 строк + внетрейтовые держатели); §7 4.1 подшаг + оценка; §6 «Следствия 5.0» строки 1–4; PLAN 4.1 | искал сценарий, где `qual_read_at` нужен и у модуля: бит эпохи E может измениться после `commit_height(E)`? — нет, он пишется одним `set_checked` в том же вызове, что и запись (`:632-635`), и нигде больше не пишется (grep `dkg_qual_accessor` — две точки: запись `:632`, чтение `:655`) |
| Д3 | закрыта экспериментом (и кодом) | тесты §0.2; код: `store.rs:223-226`/`:261-264`, `outer.rs:422-423`, `core/actor.rs:698-702`, `:808-818`, `:1369-1393`, `epoch_manager.rs:1428-1439` (`boundary_lookup` требует только блок), CW `standard/inline.rs:274-293` (перепредложение) | §5.2 «Триггер и лестница» (абзац [ГИПОТЕЗА] заменён), §5.4 строка «Ступень никем не обслужена», §0.10 (2), §7 4.2 (пин уже стоит) | пытался получить узел БЕЗ пары на терминале: гейт 80 даёт дыру 160..177 у прыгнувшего узла — но терминалы в дыру не попали (159 сохранён до заморозки, 191 — после посадки); терминал внутри дыры требует `boundary_fetch`, которого в стенде нет (`executor.rs:2311-2313`) — это и записано как единственный открытый край, вне адресата ступени |
| Д4 | закрыта экспериментом (замер) | `FakeChain::head_gap_on_fcu`/`head_gap_on_landing` (событийно) + `Outcome::head_gap_series` (тик 100 мс); прогоны: честные 4 узла 10 мс — `{1: 80}` на каждом; 250 мс + 5 % потерь — `{1: 82, 2: 2}` у узла 2, `{1: 84}`/`{1: 86}`/`{1: 79}` у остальных; ротация с прыжками — `{1: 168}` ×3, `{1: 108}` + посадки `{30: 2}`; гейт 80 — посадка `{85: 1}`; разрез 2|2 — `{1: 24}` ×4; догон через plane — `{1: 25}` ×3, `{1: 20}` | §6 П-1 «что теряется» (числа); §0.10 (7) | тиковая выборка дала 0 везде — заподозрил, что спекуляции в стенде нет; событийная гистограмма показала ровно 1 на каждом FCU, значит спекуляция есть, а тик её не ловит (живёт один round-trip); девнетный замер не делал |
| Д5 | закрыта экспериментом (замер) | `fakes::BodyTap` на приёмнике BROADCAST, тела по keccak; B1: 1 на (эпоха 2, отправитель); B2/C9: 1 на каждую из эпох 2, 3, 5; B3: 1; разрез в окне согласования: 1; `Plan::Forward` шлёт тот же digest (`dkg_agree.rs:1523-1541`), deque его не растит (CW `buffered/engine.rs:331-337`) | §5.3 (`deque_size = 2`), §7 4.3, §0.10 (5) | пытался поднять число разрезом сети во время согласования — 1; не доказано, что разрез накрыл согласование (§0.8 (д)); перепредложение после nullify в замере не встретилось — отсюда +1 запас |
| Д6 | закрыта кодом [KNOWN] | RETH `crates/node/core/src/args/pruning.rs:70-80` (`--full`: `account_history`/`storage_history` = `Distance(MINIMUM_UNWIND_SAFE_DISTANCE)`), `crates/prune/types/src/target.rs:12` (`= 32·2 + 10 000`), `crates/storage/provider/src/providers/state/historical.rs:189-190` (`StateAtBlockPruned`); девнет: `docker-compose.dpos.yml:10-36` без `--full`, `dpos_harness/stack/compose_gen.py:121`, `:151` (`SIM_PRUNE_PROFILE` по умолчанию `full`), интервал 32 (`cases/growth.py:204`) | §0.10 (1) | искал, читает ли кто-то из путей узла состояние на глубине > 10 064 — нет (все на `fin`/`number − K`/якоре); довод против П-1 держится только для продовых эпох (~172 800 блоков за две эпохи) — записано честно, не как «доказано на девнете» |
| Д7 | закрыта кодом для reth; наш узел — [ГИПОТЕЗА по коду], приёмка 4.2 | RETH `tree/mod.rs:862-941` (`on_new_head`: нет проверки finalized), `:1294-1299` (реорг до тегов), `:3109-3140`/`:3117-3122`; наш узел: guard #2 `executor.rs:3216-3238`, обратная проверка `:3302-3320` | §7 4.2 «приёмка», §0.10 (4) | искал в `on_forkchoice_updated` (`:1117-1147`) и `validate_forkchoice_state` (`:1157-1180`) отказ на голову вне текущего finalized — только `invalid ancestor` кэш и «pipeline syncing»; живого прогона нет |
| Д8 | закрыта экспериментом | `a_secondary_peer_on_the_authenticated_transport_is_accepted_and_heard` (CW `authenticated::discovery::Network`, `Config::local`, детерминированный рантайм, 3 пира, `TrackedPeers{primary: {A,B}, secondary: {C}}` у всех): `A.send(One(C))` отчитался C; C получил `All` от A и от B; A и B получили от C; попытки дозвона по метрике `peer{i}_dialer_attempts_total`: B→A, C→A, C→B; A никого, к C никто; всё за 6.1 с виртуального | §7 4.3, §0.10 (3) | отрицательный контроль «C не набирают по политике» невозможен в этой сборке — без дозвона C у A/B нет его адреса; девнет-дозвон оставлен приёмкой |
| Д9 | закрыта кодом | `cert_follow.rs:95-98` (`decode_artifact` → `[`crate::beacon::ArtifactFetch`]`), `:110-111` (`for_follower` → `[`crate::beacon::build_follower`]`), `plane_upstream.rs:186-187` (ссылка в приватный `log_resolver::LogHandler` → словами); `cargo doc --no-deps` — `unresolved link` 9 до и 9 после; `application.rs::randomness` — записано в §5.5 как удаляемое | §5.5 проекта | `grep -c "unresolved link"` до/после; ссылки не были intra-doc (обратные кавычки), поэтому счётчик и не мог вырасти — заменил на настоящие intra-doc там, где цель публична |
| Д10 | закрыта (пересчёт) | §7 проекта: 4.1 4–5 нед, 4.2 3–4 нед, 4.3 2 нед, суммарно 9–11 нед — все [ГИПОТЕЗА] с перечнем состава | §7, PLAN блок Э4 (1 хунк, 4 строки) | подшаг фасада (~3 дн) не поднял 4.1: «пять closure'ов» стали одним impl'ом, пин 4.2 уже написан — компенсация в обе стороны, но это оценка, не замер |

## §2. Д3 подробно

**Посылка (утверждается тестом до вывода).** Прогон A (`every_node_serves_…`, гейт `min(1024, 32)`): `!timed_out`, нет halt/ERROR; `jump_calls[3]` содержит посадки `[125, 157]` (из 95 и 127); `node3_outer_marshal_processed_height` = 170 ≥ 157 и > `last(3)` = 127; все `heights` = 168 ≥ `start(5)` = 160, то есть на две эпохи дальше `last(3)`. Прогон B (`a_node_more_than_two_epochs_behind_…`, гейт 80): ровно одна посадка, `from` = 95, потреблённый tip 183 > 95 + 80, посадка 180 > 159 = `last(epoch(95)+2)`; floor 268 ≥ 180; члены дошли до 264.

**Таблица «узел × эпоха → пара на `last(E)`»** (`pair` = сертификат и блок; читалось через `MarshalMailbox::get_finalization(h)` и `get_block(h)` — те же два чтения, что у `handle_produce`):

| | last(0)=31 | last(1)=63 | last(2)=95 | last(3)=127 | last(4)=159 | last(5)=191 | last(6)=223 | last(7)=255 |
|---|---|---|---|---|---|---|---|---|
| A: node0 | pair | pair | pair | pair | pair | — | — | — |
| A: node1 | pair | pair | pair | pair | pair | — | — | — |
| A: node2 | pair | pair | pair | pair | pair | — | — | — |
| A: node3 (прыгал 95→125, 127→157) | pair | pair | pair | pair | pair | — | — | — |
| B: node0 | pair | pair | pair | pair | pair | pair | pair | pair |
| B: node1 | pair | pair | pair | pair | pair | pair | pair | pair |
| B: node2 | pair | pair | pair | pair | pair | pair | pair | pair |
| B: node3 (стоял на 95, прыгал 95→180) | pair | pair | pair | pair | pair | pair | pair | pair |

Полный скан архива (все высоты 1..tip): прогон A — у всех четырёх `missing = []`, `block-only = []`; прогон B — у узлов 0–2 то же, у узла 3 `missing = 160..=177`, `block-only = 178..=182` (обратный parent-walk marshal'а от потреблённого сертификата 183 кладёт блоки без сертификатов), всё ≥ 183 — пары.

**Вывод.** (а) Обслуживается на глубину «всё сохранённое», без прунинга (код + оба прогона; floor узла 3 прошёл через все терминалы, пары остались). (б) `last(E)` финализируется явно — на честных прогонах нет ни одной высоты без собственного сертификата; исключение — дыра прыгнувшего узла, где `block-only` возникает из parent-walk, а не из неявной финализации. Лестница стартует как написана. Единственный открытый край — терминал внутри дыры прыгнувшего узла: его закрывает `seed_boundary_below_floor` (`executor.rs:2310-2365`), а стенд его не моделирует (`boundary_fetch: None`), и такой узел не адресат ступени.

**Что добавлено в стенд** (всё в `testbed/`): `StandConfig::archive_scan` (по умолчанию `false`; при `true` после прогона опрашивает marshal каждого узла по высотам 1..max_tip); `Outcome::archive: Vec<BTreeMap<u64, ArchiveEntry{finalization, block}>>`; `NodeHandles::marshal` (тот же `OnceLock<MarshalMailbox>`, что заполняется после `OuterBuilder::build`); для Д4 — `FakeChain::head_gap_on_fcu`/`head_gap_on_landing` (гистограммы `gap → count`, пишутся после каждого `canonicalize`/`land_jump`) и `Outcome::head_gap_series` (тик); для Д5 — `fakes::BodyTap` + `fakes::TapReceiver<R>` (обёртка `commonware_p2p::Receiver`, все пять приёмников узла завёрнуты, тап только у BROADCAST; парсит sub-channel через `mux::parse`), `Outcome::bodies`. Ничего из этого не меняет поведения узла: `TapReceiver` прозрачен, скан выполняется после остановки драйвера.

## §3. Д1 — счёт якорей

Диапазон §5–§10 (строки 439–829 проекта до правок): якорей всего 517; в 30 переписанных файлов — 229; перепинено 206; без изменений (номер совпал) — 22; потерял предмет — 1: `node/dpos.rs:1408-1432` (closure `committee_pair_for` → provided-метод `CommitteeReads::committee_pair`, `beacon/plane.rs:143-147`; помечен в §9 проекта). Сменили носителя (сопоставлены вручную, 21 вхождение): `node/dpos.rs:1382-1405`/`:1386-1403`/`:1386-1406`/`:1400-1406`/`:1400-1403`/`:1403`/`:1450-1500`/`:1365-1410` → `PlaneCommitteeReads::read_at` `:1422-1435` (+ `committee_cursor` `:1090-1096`); `:1432-1502` → impl `:1406-1489`; `:1382-1525` → `:1383-1500`; `:1690-1706` → `:1662-1678`; `consensus/dpos.rs:3453` → `:3455-3465`, `:3432` → `:3416-3487`, `:3415-3460` → `:3416-3487`; `epoch_manager.rs:769-822` → `:768-893`, `:396-460` → `:420-484`, `:715-830` → `:740-895`, `:1285-1295` → `:1358-1368`; `cert_inlet.rs:328-358`/`:325-360` → `:318-361`; `scheme.rs:25-80` → `:25-78`. Якоря §0 (кроме §0.10) и §2–§4 не трогались — вне задания. Якоря в нетронутые файлы (CW, RETH, `epoch_transition.rs`, `reader.rs`, `node/evm.rs`, `cold_start_jump.rs`, `p2p/lib.rs`, контракт) не трогались.

## §4. Ворота (verbatim)

~~~
$ cargo test -p fluentbase-consensus --lib testbed::
test result: ok. 32 passed; 0 failed; 0 ignored; 0 measured; 613 filtered out; finished in 21.80s

$ cargo test -p fluentbase-consensus --lib --features dpos-devnet-byzantine testbed::
test result: ok. 40 passed; 0 failed; 0 ignored; 0 measured; 613 filtered out; finished in 32.49s

$ cargo clippy -p fluentbase-consensus --all-targets --features dpos-devnet-byzantine 2>&1 | grep -E '^(warning|error)' | wc -l
0

$ cargo fmt --check 2>&1 | grep -v '^Warning' | wc -l
0

$ cargo doc -p fluentbase-consensus --no-deps 2>&1 | grep -c "unresolved link"
9   (до правок Д9 — 9)
~~~

Новые тесты — `testbed/preconditions.rs` (модуль `testbed::preconditions`, без фичи и с ней): `every_node_serves_the_terminal_pair_of_every_passed_epoch_after_re_jumps` (пинует Д3 (а)+(б) на гейте `min(1024, 32)`), `a_node_more_than_two_epochs_behind_freezes_its_marshal_at_the_ceiling_and_jumps_over_a_hole` (пинует потолок `last(epoch(fin)+2)`, дыру прыжка, терминалы вне дыры), `the_head_gap_band_is_measured_on_fcu_events` (замер Д4, утверждает только, что FCU были), `dkg_bodies_per_peer_are_measured_under_a_partition_in_the_agreement_window` (замер Д5, утверждает разрез внутри эпохи 1 и выпуск ключа), `a_secondary_peer_on_the_authenticated_transport_is_accepted_and_heard` (Д8, пять фактов + карта дозвонов). Красным на посылке: первый, один раз, из-за моей формулировки посылки (§0.6). Правки прод-кода — только три док-строки (Д9).

## §5. Оставить как есть

- Форму трейта §5.1 (`committee(epoch) -> Result<Arc<CommitteeRecord>, CommitteeError>` …) не подгонял под существующий `beacon::CommitteeReads` и не переименовывал существующий: оба остаются, связь — фасад (подшаг 4.1). Причина: Э5 5.1 уже назначен местом, где решится форма трейта (журнал 5.0 В§5), а Э4 не должен трогать beacon-внутренности.
- Якорь модуля `executed_state_hash(ordering_finalized)` не пересматривал, хотя замер показал полосу шириной 1: потеря оценена в 1 блок, довод про спекулятивный сиблинг (§5.1 «Якорь после 3-го прохода») замером не опровергнут.
- Ступень `last(T+1)` не менял на `last(T+2)`, хотя прогон B показал, что сегодняшний код прыгает на `Latest` за пределы окна схем (посадка в эпохе 5 при схемах до 4) и авторизует на состоянии посадки — это E4-01, а не довод за широкую ступень.
- `deque_size = 4` для BROADCAST не пересчитывал: `BodyTap` считает отличные тела за прогон (до 41 на отправителя за 100 блоков), а не одновременно ожидаемые — для ордеринг-плоскости замер не о том; значение из §5.3 оставлено.
- `Outcome::head_gap_series` (тиковая выборка) оставлен рядом с событийными гистограммами, хотя он всегда 0: это документированное свидетельство того, что тик полосу не видит.
- Формулу `commit_height(E) = start(E−2)` не переписывал в §5.1, только записал уточнение для E ≤ 2 в §6 «Следствия 5.0» — контрактный `initialize` не открывался.
- §0.1–§0.9 проекта (ответы прошлых проходов) не переписывал под 5.0 — они датированы; переписаны только §0.10 и таблицы/строки, названные заданием.

## §6. Где проверка была слабее всего

- Д4 — стенд идёт 1 блк/с с линком 10 мс и без нагрузки; «ширина 1» — свойство темпа стенда; 250 мс/5 % выбраны мной; продовая ширина не мерялась.
- Д5 — не доказано, что разрез накрыл фазу согласования (только «внутри эпохи 1, ключ выпущен»); перепредложение после nullify не наблюдалось, +1 — запас, не замер.
- Д8 — «не набирают» без отрицательного контроля (C дозвонился первым); девнет с реальными сокетами не прогонялся.
- Д7 — половина про наш узел (guard #2 против самоизлечения реоргом) — чтение `executor.rs:3216-3238`/`:3302-3320`, не прогон; в стенде R-001 var Б узел σ-less и не derive'ит, так что путь не достигается.
- Д3 — терминал внутри дыры прыгнувшего узла не воспроизведён (`boundary_fetch: None`); явность финализации доказана «нет block-only на честных прогонах» плюс чтением `inline.rs:274-293`, а не тестом, который бы построил неявно финализированный `last(E)` и увидел отказ.
- Д1 — 206 перепиновок сделаны скриптом по совпадению текста; вручную открыты 8; остальные — совпадение строк, не семантики.
- Д6 — девнет читался по compose и генератору; живой узел с `--full` не запускался.
- Не открывались в этом проходе: `E4-DESIGN-CRITIQUE.md`, `E4-RECONCILIATION-REVIEW.md`, §10 проекта целиком (69 «принято» строк), `beacon/dkg_engine.rs` кроме `:300-330`, `contracts/staking/src/consensus.rs` кроме `:595-660`.
- Счётчики текстовых проверок (проект / PLAN): строки на многоточие — 0 / 0; нечётное число обратных кавычек — 0 / 0; двойная запятая — 0 / 0; пустая пара кавычек — 0 / 0; пустые скобки вне кода — 0 / 0; строк длиннее 500 символов — 116 / 22 (читались `sed -n` и Read). `git diff --stat .dpos-study/PLAN.md` — `1 file changed, 4 insertions(+), 4 deletions(-)`, один хунк, блок «### Э4.» (строки 4.1–4.3 и «Суммарно»); блок PLAN байт-в-байт равен блоку «Стало (итог)» §7 (diff пуст).

## §7. Замечено вне рамок

- Имя `CommitteeReads` уже занято трейтом `beacon/plane.rs:108` с другой формой — проект §5.1 до этого прохода называл им новый трейт; теперь развязано фасадом, но при написании 4.1 это первое, обо что споткнётся компиляция.
- Обратный parent-walk marshal'а кладёт блоки без сертификата (прогон B: 178..182) — такой узел держит блок, но `Finalized{h}` на эти высоты отдать не может; для Э5 5.2 (обслуживание по высоте) это ещё один источник «есть блок, нет пары».
- `seed_boundary_below_floor` сидирует `b` и `b+1` (`executor.rs:2318`), а cold-start сидирование в `outer.rs:876-895` — только `b` с комментарием, что `b+1` больше не нужен; асимметрия двух копий одной логики.
- `commit_height(1) = commit_height(2) = 1`, не `0` (бутстрап коммитит только эпоху 0; C3 пинует `uncommitted[1] ≥ 1` на генезисе) — формула `start(E−2)` проекта для E ≤ 2 неточна на один блок.
- Прогон B показал: узел, отставший более чем на две эпохи, сегодня прыгает по `Latest` за окно схем (посадка в эпохе 5 при схемах до 4) и авторизует на состоянии посадки — это работающий E4-01-путь; после 4.2 этот прыжок должен идти лестницей.
- Шапка `.claude/COMMONWARE_INTERNALS.md` называет чекаут `monorepo-9732103c47eb4665`, задание — `monorepo-27b478c9bb41d208`; оба на диске, оба `3c4e02c`.
- Метрика `processed_height` marshal'а двигается каждым ack — «floor» в терминах проекта (§5.2) и «floor» marshal'а — одно поле, но первое подразумевает только прыжок.
- В §0.5/§0.8 проекта остались формулировки про «три beacon-closure'а» и «пять closure'ов» — датированы 3-м проходом, не переписаны (вне задания).

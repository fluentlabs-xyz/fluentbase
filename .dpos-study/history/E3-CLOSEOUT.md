<!-- Э3, закрытие. Дерево /home/djadjka/Work/fluentbase, ветка djadjka/dpos-reth-2.2-squashed, старт на 38c546d4. Ведёт Fable 5.1 (оркестратор); исполнители и контрагенты — Opus 5, свежий контекст на каждого. -->

# Э3 — закрытие: что сделано, что снято, что осталось

## 0. Объём и правила

Решение 2026-09-10 (после сессии A 3.3): закрыть Э3 урезанным объёмом — B1 (прод-прыжок в стенде + R-001 вариант Б + минимальный R-004), B2 (R-009, R-006 сценарий 1), 3.1 в форме «известные расхождения `FakeChain` с reth — явно, в коде», 3.4 снять (механизмы Ex-21/Ex-19 закрыты стендом), 3.9 — половина (e2e-копии ABI; `bootstrap.rs` под `devnet/local-dpos-smoke/**` — незакоммиченная работа пользователя, не трогать). 3.10/3.11 — там же, не трогать, остаются открытыми.

На каждый пункт: исполнитель (Opus 5) → контрагент (Opus 5, свежий контекст, не видел брифа исполнителя) → перепроверка оркестратором по file:line/прогону → строка в `PLAN.md`. Реляции агентов помечены как реляции; [KNOWN] только по тому, что оркестратор открыл или запустил сам.

Коммиты — по явным путям, после «ок» пользователя, пакетом в конце.

## 1. Трек T1 — прод-прыжок в стенде (3.3 B1 шаг 1)

**Исполнитель (Opus 5), реляция:** тело `ReJumpFn` в `stand.rs` заменено вызовом `cold_start_jump_with_threshold(from, &up, &committees, &el, None, activation, threshold, ctx)` по образцу `consensus/src/dpos.rs:2492-2536`; рукописная модель удалена. Новые фейки в `fakes.rs`: `JumpCommittees: CommitteeSource` — `scheme_at(epoch, at_hash)` читает `FakeStaking::epoch_committee_snapshot(epoch, at_hash)` по исполненному хэшу и строит верификатор как `RethCommitteeSource::build_at`, каждый вызов пишет `(epoch, at_hash)` в `Outcome.jump_committee_reads`; `JumpElSync: ElSync` — `sync_to` через `FakeChain::land_jump`, неотдаваемый хэш → `StalledWithPeers` после `EL_SYNC_STALL_ESCAPE` (300 вирт. с), `holds` не вызывается при `l1_checkpoint = None` (`cold_start_jump.rs:860`). `Outcome.jump_landings`. Новый тест C9 `the_rejump_runs_the_production_jump_and_authenticates_at_the_landing`: посадки `[(125, 0xecf0…), (157, 0xca92…)]`, чтения комитета `[(4, 0xecf0…), (5, 0xca92…)]` — по одному на посадку, на хэше посадки. До/после (временный `eprintln`): B2 узел 3, R-008 узлы 3/4, C8 — посадки и `rejump_calls` те же. Прод-правка одна: `const EL_SYNC_STALL_ESCAPE` → `pub(crate)` (видимость). Ворота: 635+3+5+13/0, стенд 30/0/0 (134 с), clippy чисто, rustfmt ок. Доки: `04_…` §4.3, `15_…` §15.a, `00_preamble.md`, `TOC.md`.

**Квитанции оркестратора [KNOWN]:** `stand.rs:1559` — вызов `cold_start_jump_with_threshold`; `fakes.rs:600/635` — `epoch_committee_snapshot(epoch, at_hash)`; `fakes.rs:693` — `sleep(EL_SYNC_STALL_ESCAPE)`; `git diff cold_start_jump.rs` — одна строка видимости; стенд своим прогоном 30/0/0 (136 с).

**Контрагент V1 (Opus 5, свежий контекст), реляция:** все семь критериев держатся (ворота прогнал сам, после `cargo clean -p`); 20 находок. Существенные: F3 — в `FakeStaking` состав комитета — чистая функция эпохи, `at_hash` влияет только на «закоммичен ли», так что чтение «на хэше посадки» пока без поведенческих зубов (это ровно то, что даст роль лгущего upstream'а — комитет атакующего под его хэшем); F5 — прогон опровергает прод-док-комментарий `cold_start_jump.rs:43-46/:651-656` «посадка `tip − K` в той же эпохе, что tip»: при `EPOCH_LEN = 32` посадки 125/157 в эпохах 3/4, tip'ы 128/160 — первые блоки эпох 4/5; чтение проходит через ahead-commit, вывод верен, довод ложен; F7/F8 — `land_jump == false` для двух разных причин (не может отдать / префикс расходится) оба → `StalledWithPeers`, которого executor не ротирует, плюс 300 вирт. с заморозки derive внутри waiter'а; F9 — два разных источника хэша в ветках `sync_to`; F12 — гейт стал строже (модель садилась при `uf.height − 3 > from`, прод — только за порогом 32), фикстура полосу не задевает; F13 — ассерт хэша C9 сравнивает две выборки одной цепи; F15 — все не-`Landed` исходы невидимы в `Outcome` (`rotate = None`), `rejump_calls >= 1` в B2 прошёл бы и при `AuthFailed` на каждом вызове; F16 — ветки `local_landing` прод-формы садятся без обращения к пиру; F18 — шапка `testbed/mod.rs:34` всё ещё называет re-jump «counted no-op»; F19 — четыре протухших якоря в новых комментариях; F20 — активация литералом `0` в трёх местах. Не проверено контрагентом: посадки B2 «те же, что у модели» — без дифф-прогона (по тождеству фикстуры и арифметике), негативный тест BLS-гейта.

**Решение:** в код F18, F19, F20, F15 (типизированные исходы прыжка в `Outcome`, C9 утверждает «все `Landed`»), F7+F9 (расходящийся префикс → `SyncFailure::Invalid`, единый источник хэша), F13 (хэш посадки = `block.result` потреблённого сертификата + согласие с честными), F5 (правка прод-комментария, `docs(consensus)`). Оставлено: F1 (арифметика `tip − K` в фейке `ElSync` — шов трейта), F3 (для T2), F6/F8/F12 (в доку фейка).

**Применено исполнителем (реляция):** `mod.rs:34-42` шапка; якоря перечитаны (F5 сдвинул `cold_start_jump.rs` на +8); одна константа `fakes::DPOS_ACTIVATION_BLOCK` для `FakeStaking`, `JumpElSync`, аргумента прыжка и `OuterBuilder`; `fakes::JumpCall { from, outcome, consumed, landed }` → `Outcome.jump_calls` (заменил `jump_landings`), C9 утверждает «все `Landed`»; `TeeingUpstream` пишет `(height, result)` потреблённого сертификата — C9 утверждает `landing_hash == consumed.result`, `landing == consumed.height − K`, `diverged == None`, узлы 0–2 держат тот же хэш на посадке; `FakeChain::land_jump` → `JumpLanding { Landed, Unservable, ConflictingPrefix{…} }`, `Unservable` → `StalledWithPeers` после ожидания, `ConflictingPrefix` → `SyncFailure::Invalid` (с `[ГИПОТЕЗА]` про реальный вердикт reth); оба плеча `sync_to` через `spec_hash_at`. Прод-правки: (1) видимость `EL_SYNC_STALL_ESCAPE`; (2) `docs(consensus)` — `cold_start_jump.rs:42-56` и `:648-656`: довод «посадка в эпохе tip» заменён на настоящий — ahead-commit `MAX_COMMITTEE_LOOKAHEAD_EPOCHS = 2` (`staking_protocol.rs:73`). Отклонённые F1/F6/F8/F12 записаны в доку `JumpElSync` (`fakes.rs:745-767`). Ворота: 635+3+5+13/0, стенд 30/0/0 (133 с), clippy 0, rustfmt ок. C9: `calls=[{from:95, Landed, consumed:(128, 0xecf0…), landed:(125, 0xecf0…)}, {from:127, Landed, consumed:(160, 0xca92…), landed:(157, 0xca92…)}]`, чтения `[(4, 0xecf0…), (5, 0xca92…)]`.

**Квитанции оркестратора:** прогон стенда после дозаказа — см. §7 (общий прогон перед коммитом; параллельно T4 правит те же файлы, cargo на крейт не запускался, чтобы не держать target).

## 2. Трек T5 — 3.9: `bootstrap.rs` и четыре e2e-копии ABI → `fluentbase-staking-abi`

Разблокирован коммитом `26d291b7` (пользователь: «можешь закоммитить local-dpos-smoke») — `bootstrap.rs` перестал быть незакоммиченной чужой работой.

**Исполнитель (Opus 5), реляция:** в общий `sol!` добавлены `initialize` (16 аргументов в порядке `InitializeCommand`, `contracts/staking/src/types.rs:11-32`) и `getConsensusKeys` (чтобы не оставлять вторую `ConsensusKeys`); `bootstrap.rs` кодирует `initializeCall`/`commitEpochCommitteeCall` из общего крейта; в `e2e/src/staking{,_bls,_cost,_reserve}.rs` удалены копии `initialize`, `recordProduction`, `commitEpochCommittee`, `slashEquivocationNotarize`, `getConsensusKeys` (в `staking_bls.rs` копия была с плоским 3-кортежем возврата — расходилась с тем, что декодирует `staking.rs`), событие `EpochBlendRewardsCommitted`, дубль `EpochConsensusKeys`. Оставлены как копии — только контрактные обработчики без узлового вызывающего (`registerValidator`, `delegate`, `set*`, `claim*`, `blocksInEpoch` и т. п.) с комментарием почему. Calldata `initialize` побайтно та же до/после (селектор `fecaf0f1`), `commitEpochCommittee` — `e505b249`. Ворота: `cargo check --workspace` ок; `fluentbase-genesis-bootstrap` 6+8/0; `fluentbase-staking-abi` 2/0; `fluentbase-e2e --release` 113/9/9 — те же девять `builtins::*`; clippy чисто; rustfmt ок.

**Дозаказ:** контрактный `consts.rs` ещё выводил `SIG_INITIALIZE`/`getConsensusKeys` из строк — G3 в `agreement_check.py` красный (наблюдал сам, прогон ниже в §T6). Агенту разрешён `contracts/staking/src/consts.rs`; ожидание — G3 зелёный, `cargo test` контракта 175/0 без изменений, блоб не перегенерируется (значения селекторов те же).

**Дозаказ 1 выполнен:** `consts.rs:39/179` → `sig::<abi::initializeCall>()`/`sig::<abi::getConsensusKeysCall>()`; G3 зелёный (15 деклараций); контракт 175/0, 176/0; блоб не тронут; `00_preamble.md` — «13 вызовов» → 15 + `verified-against`.

**Контрагент V5 (Opus 5, свежий контекст), реляция:** AC1/AC3/AC4/AC5 держатся — calldata `initialize` переустановлена независимо (scratch-крейт: старая спелловка с HEAD и `initializeCall` общего крейта на 16 нетривиальных значениях, `assert_eq!` на полном векторе, порядок/ширины против `InitializeCommand` поле в поле; пин-тест `tests.rs:1192` сравнивает с голыми hex-литералами — независим). 13 находок: #1/#2/#4/#5/#6 — «объявлен рядом с единственным вызывающим» ложно: `registerValidator` (две копии в e2e + python-харнесс), `blocksInEpoch` ×3, `getEpochRewards` ×2, `setBlendStipendPerEpoch` ×3 (+ `production_path.py:550-554`), `setBlendReserve` ×2, `setProductionLivenessDisabled`, `claimValidatorFee` — мотивация ячейки («смена арности не соберётся») для них не выполнена; #7 — `bootstrap_smoke.rs:85-95` держит ещё копию `ConsensusKeys`/`getConsensusKeys`/`getEpochCommitteeWithStakes` (и `getActiveValidatorsLength() returns (uint32)` при `u64` в контракте); #8 — e2e-модули за `#[cfg(test)]`, `cargo check --workspace` их не компилирует; #9 — `#![allow(unused)]` прячет мёртвые декларации (`setBlendReserve` в `staking_cost.rs` без вызовов); #10 — `features = ["std"]` — дрейф фич, узел собирается без него, `--workspace` и `-p fluent` получали разные наборы фич `alloy-sol-types`; #11 — nightly-fmt не чист по всему крейту (до правки); #12 — «fourteen» в доке теста; #13 — `STAKING_ARTEFACT.md:426-428` устарел.

**Решение:** в код #1–#7, #9, #10, #12, #13; оставлено #8 (зафиксировано: реальные ворота для e2e — clippy `--all-targets` и прогон), #11 (до правки, крейт целиком). Исполнитель применил: девять обработчиков (плюс `delegate`/`undelegate` — харнесс пишет их строками) в общий крейт, `consts.rs` выводит все 24, копий ноль (grep по 31 имени: только `IStakingPool::initialize(address)` и `IFluentGovernance::initialize(address,uint32)` — другие контракты), `bootstrap_smoke.rs` импортирует общий крейт и декодирует `u64`, `std` снят (собирается без), пины 15 из 24 (остальные без строки в скане блоба — свидетель e2e-раунд-трип + G3). Ворота: `cargo check --workspace`; clippy abi+e2e+genesis-bootstrap `--all-targets` 0; staking-abi 2/0; genesis-bootstrap 6+8/0; контракт 175/0, 176/0; e2e `--release` полный 113/9/9 (те же девять `builtins::*`); rustfmt 0 diffs; G3 24 декларации, G16 17+16.

**Квитанции оркестратора [KNOWN]:** чекер 15/0/0 с G3 «24 shared declarations» и G16 «17 shared + 16 contract-only … 113 sites»; grep `function|struct <name>` по 12 именам в e2e/bootstrap/bootstrap_smoke — ноль (кроме двух чужих `initialize`); `features` на `staking-abi` в двух Cargo.toml — нет; `consts.rs` — 24 строки `sig::<abi::`; блобов в `git status` нет. Повторного контрагента на правки не запускал.

## 2б. Трек T6 — 3.10 + 3.11: `agreement_check.py`

**Исполнитель (Opus 5), реляция:** литерал `51` — только в `compose_gen.py` (в `floor_halt_case.py`/`seed_continuity.py` его уже не было) — стал именованной константой `MAX_COMMITTEE_SIZE` с настоящим якорем `staking_protocol.rs:38`; два теста харнесса читают её вместо `51`/`52`; `agreement_check.py` G8 сверяет её с Rust (Python не импортирует Rust-константу — та же схема, что G1 для commonware). Новые оффлайн-чеки: G4b — арность `getRegistryWithKeys` (обработчик делегирует в `write_validators_with_keys`, `consensus.rs:257-272`, помощник идёт на один уровень делегации); G15 — ширины возврата трёх chain-config вьюх `getEpochBlockInterval`, `getDposActivationBlock`, `getActiveValidatorsLength` (выводятся из `config.rs` × общий крейт, не списком; пустой набор = FAIL); G16 — каждая сигнатура/селектор, который харнесс пишет строкой или 4-байтной константой (AST, не текст), совпадает с общим крейтом (11 из 22 деклараций, 26 мест). Каждый чек сломан нарочно (7 разломов, включая два вакуумных) и восстановлен. Ворота: чекер 15/0 на чистом дереве; pytest харнесса 1 failed / 2082 passed / 9 skipped — то же, что до (падение `test_the_two_jump_gates_are_the_values_the_product_uses` старое); py_compile ок; pyflakes — только два старых предупреждения.

**Квитанции оркестратора [KNOWN]:** запустил `agreement_check.py` сам после T6: 15 чеков, G4b/G15/G16 `[ok ]`, G8 `[ok ]` с `compose_gen.py transcribes 51`; единственный `[FAIL]` — G3 из-за незавершённой контрактной половины T5 (не T6). `compose_gen.py:65` `MAX_COMMITTEE_SIZE = 51`, `:112` гейт читает константу.

**Вне объёма (от T6, не делал):** харнесс декодирует два chain-config вьюха как `uint32` при объявленном `uint64` — 6 мест (`production_path.py:744,750`, `writes.py:254,317`, `shadow.py:188`, `test_prod_substrate.py:394,401`); безвредно, пока значения влезают в 32 бита. `scripts/xp/README.md:27` врёт, что `STAKING_CONTRACT_SRC` обязателен.

**Контрагент V6 (Opus 5, свежий контекст), реляция:** AC3/AC4 держатся (все разломы воспроизвёл сам, pytest 1/2082/9 как до), но покрытие уже формулировки: F1 — G16 сравнивал только имена из общего крейта, 24 сигнатуры харнесса из `consts.rs` (~78 мест) пропускались молча; F2 — слеп к f-string/конкатенации и к селекторам без `0x`/bytes (живой пример `floor_halt_case.py:70`); F3 — прозаические метки `name(word)` парсились как сигнатуры и роняли G16; F4 — наборы не скорятся (3→1 вьюха = ok); F5 — шесть мест харнесса декодируют `uint32` при объявленном `uint64`; F6 — делегация «первый попавшийся» даёт ложный PASS на decoy; F7 — только арность; F8 — pytest после правки самореферентен, значение кэпа пинуется только ручным `make xp-agree`; F9 README; F10 якорь-строка; F11 regex ломается на хвостовом комментарии; F12 shell-вызовы вне G16; F13 int-коллизия (GUESS).

**Решение:** принято в код F1, F2 (частично: без f-string), F3, F4, F5, F6, F8, F9, F11, F12; оставлено F7 (метка «арность» честная, как G4), F10, F13 (строка в докстринге). Исполнитель применил: G16 двухъярусный (общий крейт → `consts.rs` → FAIL; 113 мест, таблицы `OFF_CONTRACT`/`NOT_CALLED` для ERC20/Governor и absence-проб), `_sol_type`/`_split_types`, G15 из общего крейта с обязательным обработчиком и скорингом декодов, шесть мест → `uint64`, делегация по исключению (0 или >1 писателя = отказ), pytest `test_protocol_constant_transcriptions.py`, `*.sh` в скане. Разломы повторены (10 строк в отчёте агента).

**Квитанции оркестратора [KNOWN]:** чекер после правок — `15 checks, 0 disagree, 0 unread`; `test_protocol_constant_transcriptions.py` + `test_compose_gen.py` — 14 passed; grep `(uint32)` по трём файлам харнесса — остался только аргумент сеттера `setActiveValidatorsLength(uint32)` (тип аргумента по контракту, не декод). Повторного контрагента на правки не запускал — проверил прогоном и чтением сам.

## 3. Трек T4 — 3.1 в форме «расхождения FakeChain с reth»

Объём по решению 2026-09-10: не conformance-набор против in-process reth (3–4 нед., `reth-e2e-test-utils` не подключён), а то, что стенд обязан иметь по правилу §2 плана («R-006 показал: фейк-оракул делает тесты зелёными на ложной семантике»): `FakeChain` совпадает с reth там, где читают гварды executor'а, остальные расхождения объявлены в одном месте с якорями в `RETH_INTERNALS.md`.

**Исполнитель (Opus 5), реляция:** `FakeChain` получил `tree: BTreeMap<B256, TreeBlock{height, parent}>` (исполненные блоки по хэшу; `insert_tree` = `InsertExecutedBlock`, уже известный хэш — тихий no-op по правилу форка `f6fb181`, RETH_INTERNALS §11) отдельно от `canonical`; `canonicalize(head)` — обход к точке развилки, `retain(h <= head_h)` + расширение (`NewCanonicalChain::Commit|Reorg`); `land_canonical` — генезис и префикс посадки прыжка. `FakeDeriver::derive_and_execute` только вставляет в дерево; `FakeBeacon::new(chain)` — `import_derived` → `insert_tree`, `fork_choice_updated` → `canonicalize`. Ярусы: `spec_executed_hash` = канон (как `provider.block_hash`) — совпадает; `finalized_executed_hash` — совпадает; `executed_tip` — канон-tip против `last_block_number()` у прода (объявленное расхождение: нет лага персистенции); `advance_finalized` — курсор + два стендовых побочных действия. Блок «Divergences from reth» на `FakeChain` (`fakes.rs:153-231`): 4 смоделировано / 8 объявлено (ACCEPTED не производится — как и reth; SYNCING недостижим; ancestor-FCU short-circuit без safe/finalized-тегов; eager `MakeCanonical`; cert-follow finalized-freeze; engine-fatal `Err`; лаг header-index по хэшу; ярус `executed_tip`; нет персистенции/прунинга). Ни один из 30 старых тестов не изменился и не упал — ни одна стендовая обвязка не опиралась на раннюю канонизацию (`Outcome.hashes` уже читал tier-F, `land_jump` — канон). Новый тест `guard_two_on_the_catch_up_path_reads_a_pre_fcu_height` (`tests.rs:250`, без фичи): узел 3 в разрезе 8 view после h=3, `DivergentResult{at: 6}`, лечится на 3 при tip 17 (≥ K позади). Старая семантика: `heights=[24,24,24,5] halted=[(3,ResultDivergence)]`, guard #2 на 6 сработал (`executor.rs:3165`). Новая: `heights=[24,24,24,8]`, `guard2=[]`, halt по обратной проверке `h − K` на 9 (`executor.rs:3247`) — после того как 6, 7, 8 FCU'нуты и курсор продвинут. **R-006 сценарий 1 воспроизведён.** Ворота: 636+3+5+13/0, стенд 31/0/0 (137 с), clippy 0, rustfmt ок. Доки: `15_…` §15.a, `02_…` — утверждение «guard #2 ловит НЕМЕДЛЕННО / единственный детектор на догоне» опровергнуто на месте, `13_…` правило 42 («EL-фейк обязан разделять executed и canonical ровно там, где reth»), `00_preamble.md`, `TOC.md`.

**Для оркестратора (от T4):** единственный оставшийся зелёный тест на снятой семантике — `executor.rs:7448` `guard2_convergence_mismatch_engages_safety_halt` на собственной фикстуре executor'а (`land_on_import` выключен по умолчанию) — прод-тест, вне объёма; R-006 его и называет. Вне объёма: `executed.rs::executed_state_hash` гейтится на `best_block_number()`, а `ProviderExecutedChain::executed_tip` — на `last_block_number()`: три разных чтения reth за двумя швами.

**Квитанции оркестратора [KNOWN]:** `fakes.rs:264/280/313` — `insert_tree`/`canonicalize`/`land_canonical`; `:153` блок расхождений; `stand.rs:1803` `FakeBeacon::new(chain.clone())`; `tests.rs:250` новый тест; стенд своим прогоном 31/0/0 (133 с).

**Следствие для плана:** R-006 сценарий 1 закреплён здесь; T3 сужается до R-009 (+ черновик статуса R-006).

**Контрагент V4 (Opus 5, свежий контекст), реляция:** воспроизведение R-006 сценария 1 подтверждено повторным прогоном (`heights=[24,24,24,8]`, `guard2=[]`, halt `executor.rs:3247` на 9; `h − K` — другой сайт (`:3234-3253`), чем guard #2 (`:3140-3187`); под старой семантикой не сработал бы — guard #2 остановил бы на 6). AC3 держится (в `tests.rs` только добавления; «если бы `canonicalize` ничего не делал — упали бы все» — `executor.rs:3389` ждёт `spec_executed_hash == Some(derived)`). 15 находок: F1 — в блоке расхождений нет INVALID, хотя дока `FakeBeacon` на него ссылается (`executor.rs:3311-3320` недостижим); F2 — проза ярусной таблицы инвертирована (ancestor/descendant); F3 — `canonicalize` при разрыве дерева коммитит сегмент с дырой (воспроизведено на дословном порте функции; reth ветка 5 не канонизирует ничего); F4 — `note_hash` вставляет родителя `ZERO`, `or_insert` не даёт честному re-derive его починить; F5 — `JumpElSync::holds` читает дерево, прод — канон (`provider.block_number(hash)`); F6 — `land_jump` теперь перезаписывает derived-но-не-FCU'd высоту вопреки своему комментарию; F7 — `advance_finalized` молча пропускает неканонические высоты при публикации в `ElNetwork`; F8 — SYNCING приписан плечу `import_derived`, где прод отвечает `Valid` безусловно; F9 — `invalid_state` по неизвестному safe/finalized не объявлен; F10 — формулировка про ветки 2/3; F12 — тест не утверждает, что guard #2 вообще сработал и прочитал `None` (пустой захват совместим с «не вооружился»); F13 — лог-ассерты условны на `log_capture_live`; F14 — самопроверка тампера проходит на плейсхолдере `B256::ZERO`; F15 — `clippy -D warnings` без фичи красный (`upstream_frontier_peak` не читается; `make pr` гоняет именно так). AC6: `executor.rs:7448` по-прежнему зелёный на своей фикстуре — формулировка R-006 верна, устарела строка статуса («сценарий 1 открыт»).

**Решение:** в код F1–F10, F12–F15 (дозаказ T4 — после того как T2 закончит правки тех же файлов; текст в `scratchpad/t4-followup.md`); F11 — мой якорь. Статус R-006 в `REGISTER.md` — оркестратор при закрытии.

**Применено исполнителем (реляция):** `canonicalize` → `bool`, fail-closed (обход обязан дойти до уже канонического блока своей высоты, иначе ничего не коммитится), `FakeBeacon::fork_choice_updated` отвечает `Syncing` на `false` — reth ветка 5; выбрано потому, что все FCU-сайты executor'а терпят `is_syncing()` (`executor.rs:3309`, `:2717`); `insert_tree` чинит родителя-плейсхолдер `ZERO` через `and_modify` (нужно для реплея); `land_canonical` паникует на отсутствующем родителе; `holds` → `canonical_holds` (как `provider.block_number(hash)`); `advance_finalized` паникует на derived-но-неканонической высоте (проба `seeds.contains_key`), недеривленную пропускает с задокументированным законным случаем (курсор с acked-высоты маршала при пустой `FakeChain`); блок расхождений: INVALID, `invalid_state` по safe/finalized, SYNCING разделён по плечам, «no-op на канонической цепи»; `ElEvent {Derived, Canonicalized}` — один интерливинг-лог на узел в `Outcome::el_events`; тест: `assert!(log_capture_live)` первым, самопроверка тампера по `el_events` (`Some` с обеих сторон) для 6, 7 и 8, и прямое наблюдение `Derived(6)` раньше `Canonicalized(6)`, 6–8 канонизированы, `Derived(9)` без `Canonicalized(9)` — вердикт опередил FCU. F15 уже закрыт T2 (гейт `upstream_frontier_peak`). Ворота: `clippy -D warnings` чист с фичей и без; 636+3+5+13/0; стенд 33/0/0 (230 с); rustfmt ок. Ни одна паника и ни один `Syncing` не сработали в 33 тестах.

**Квитанции оркестратора [KNOWN]:** `fakes.rs:372` `canonicalize -> bool`, `:459` `canonical_holds`, `:733` `Syncing`, `:148` `ElEvent`; `tests.rs:328` `log_capture_live` в ассерте; `cargo clippy … -D warnings` с фичей и без — `Finished` без предупреждений (свой прогон).

## 4. Трек T2 — роли R-004 (минимальный) и R-001 вариант Б (3.3 B1)

Исполнитель — **Opus 4.8** (по указанию пользователя 2026-09-10: бриф T2 дважды отклонён классификатором Fable по категории `cyber` — описание атакующих ролей стенда; агент-тип `opus48` создан в `~/.claude/agents/`). Две фазы, чтобы не править файлы стенда одновременно с T4.

**Фаза 1 (реляция):** план правок в `scratchpad/t2-plan.md`. Швы: `ForgedSeedProducer::produce` (`byzantine_roles.rs:591-624`) — плечо `Latest` (`:593-595`) для R-004/R-001; проба `stand.rs:1503-1516` → `get_latest` → `fetch_max` в `upstream_frontier` (`executor.rs:1936-1938`); `maybe_re_jump` (`:2179-2226`), гейты заморозки (`:1191-1194`, `:1407-1430`); прод-прыжок `cold_start_jump.rs:802-898`, `verify_jump_structural` `:633` (для самопроверки тампера). Открытые вопросы до прогона: (1) R-004 — срабатывает ли проба у здорового узла до заморозки (проба идёт только на тике без смены tip); (2) R-001 — отдаётся ли расходящийся префикс узла 3 из `ElNetwork` (первый писатель побеждает) и остаётся ли `FakeChain` узла 0 на чужом префиксе после `sync_to` до аутентификации (последствие (в) записи); (3) R-001 Б — `OrderBlock::digest()` включает `result` (`order_block.rs:148`): подмена `result` меняет digest, `payload == digest` требует переставить `proposal.payload`, и настоящая мультиподпись перестаёт сходиться ⇒ ожидание — отказ (`AuthFailed`), не захват; захват (б) записи требует варианта А (комитет атакующего под хэшем посадки), который `FakeStaking` пока не умеет. (4) Всё это зависит от новой семантики `FakeChain` из T4.

**Фаза 2 (реляция):** `byzantine_roles.rs` — `ForgeMode` (`:486`), обобщённый `ForgedSeedProducer` (`forge_latest` `:542`, `forge_inflated_latest` `:656`, `forge_lying_latest` `:675`), самопроверка на декодированных обратно байтах с `verify_jump_structural` до подмены; `fakes.rs` — поля `ByzFacts` (`latest_inflated` `:1239`, `result_forged` `:1252`); `stand.rs` — `Role::InflatedProbe`/`LyingUpstream` (`:274`/`:281`), `StandConfig::upstream_only_link` (`:136`, фронтир жертвы только через лгущий узел по upstream-плоскости), `Outcome::upstream_frontier_peak` (`:428`, за фичей). Прод не тронут (diff `cold_start_jump.rs` — из T1).
- `an_inflated_latest_probe_wedges_a_rotated_out_validator_in_re_jumps` (`tests.rs:2751`) — **R-004 ветка (а), воспроизведено:** `peak[0] = 1_000_749`, `jump_calls[0]` = 2× `StalledWithPeers`, `rejump_calls[0] = 3`, `heights = [95, 750, 750, 750]`; 762 вирт. с / ~50 с. Слепое пятно: здоровая жертва недостижима — в детерминированном рантайме темп блока (1 с) и интервал холостой пробы (1 с) идут в ногу, tip здорового узла движется на каждом тике и `get_latest` не вызывается (`committees = All` дал `latest_inflated = 0`); жертва — выбывший из комитета узел.
- `a_lying_upstreams_forged_result_is_unservable_and_the_jump_stalls` (`tests.rs:2894`) — **R-001 Б, ветка (в) по формулировке исполнителя:** `result' = keccak256(result ‖ "lie")`, payload переставлен, мультиподпись настоящая; `jump_calls[0]` = 2× `StalledWithPeers` (потреблены tip'ы 128/419), чтений комитета ноль, halt нет, узел 0 стоит на 95, `diverged = None`; 769 вирт. с / ~48 с. Исполнитель называет две «защиты стенда»: (i) общий first-writer-wins `ElNetwork` — подделанный хэш неотдаваем; (ii) `JumpCommittees` отдаёт честный комитет при любом хэше посадки.
- Ворота: 636+3+5+13/0; стенд 33/0/0 (230 с); clippy чисто с фичей и без (после гейта `upstream_frontier_peak`); rustfmt ок. Доки: `15_…` §15.a, `00_preamble.md`, `TOC.md`.
- Черновики статусов R-004/R-001 — в отчёте агента; в реестр — после V2.

**Оценка оркестратора до V2:** для R-001 «неотдаваемый хэш» — это не «не воспроизведено», а «путь не достигнут»: в механизме записи атакующий отдаёт свою ветку по devp2p, и вопрос записи — что делает аутентификация на посадке; здесь до неё не дошло, потому что `ElNetwork` первого писателя не умеет держать вторую ветку по хэшу. Вопрос поставлен V2 как главный.

**Контрагент V2 (Opus 5, свежий контекст), реляция — 19 находок.** Главные: F1 — картина R-004 совпадает с соседней фикстурой без подделки (порог прыжка проходится от `ordering_finalized = 95` при tip 750 сам по себе, `executor.rs:2191-2203`); F3 — `calls.len() >= 2` вакуумно; F5 — в проде раздутая высота при настоящем хэше даёт `Stalled` (ротирует), а не `StalledWithPeers`; стенд отвечал `Unservable` из-за ключа `(height, hash)` в `ElNetwork`; F8 — «не опускается» не наблюдалось; F9 — заморозка не атрибутирована гейту.

По R-001: F10 — переставленный `proposal.payload` проваливает `Finalization::verify` над `Subject::Finalize{proposal}` (`CW:consensus/src/simplex/types.rs:1469-1486`, `cold_start_jump.rs:688`) при любом комитете, так что «вариант А сделал бы BLS проходимым» неверно без переподписи; F11 — «путь не достигнут»: `ElNetwork` первого писателя по высоте не умеет держать вторую ветку, нужен ключ по хэшу с родителями и `land_jump` по цепочке родителей; F12/F13 — нет связи «потреблено = подменено» и нет проверки утечки к узлам 1–3; F14 — ветка через `deliveries_rejected` недостижима (счётчик только по ошибкам декода); F16 — нет `!timed_out`; F17 — фикстура на ~40 % длиннее нужного. Вердикт V2: R-004 — раздутие воспроизведено, «клин» не атрибутирован; R-001 — путь не достигнут.

**Решение:** в код F1, F3, F4, F5, F7, F8, F9, F10–F14, F16, F17, F19; заметками F2, F6, F15, F18.

**Переработка (Opus 4.8, реляция):** фундамент — `ElNetwork` по хэшу (`ElBlock {height, parent}`, `publish_branch`), `land_jump(hash)` идёт по цепочке родителей до канонической точки развилки и канонизирует только префикс (продвижение курсора убрано — это работа `reseed_forward` на `Landed`), `JumpElSync::sync_to` в прод-порядке (сначала хэш, потом высота посадки; неразрешимая высота → `SyncFailure::Stalled`). Контроль внутри каждого теста (`lying_upstream_stand(role3)`, узел 3 честный). R-004: `peaked = 1_000_194`, ряд неубывающий, 14 вызовов все `Stalled`, пробы 437 против 293 у контроля, `heights = [95,194,194,194]` против `[192×4]`; ~13 с. R-001 (тест переименован в `a_lying_upstream_lands_a_divergent_branch_and_authentication_refuses_it`): узел 3 сеет дивергентную ветку с `LYING_DIVERGE_AT = 100`, прыжок садится, `el_events[0]` показывает канонизацию чужого префикса, 41 вызов — все `AuthFailed`, чтения комитета на хэшах посадки, halt нет; ~14 с. Отклонения исполнителя: F7 — контроль (тоже выбывший) тоже пробует чаще 1/блок, утверждено «роль > контроль»; F12 — `forged_result_to` хранит последнюю подмену, утверждено `consumed.result == divergent_hash(landing)` на каждый вызов + одна связь со свидетелем. Ворота: 636+3+5+13/0, стенд 33/0/0 (186 с), `clippy -D warnings` чист в обе стороны, rustfmt ок.

**Контрагент V2b (Opus 5, свежий контекст), реляция — 20 находок, вердикт «воспроизведено частично» для обоих.** Ворота и C9 сошлись побайтно с §1. Главные: F1 — стоячий гейт клина R-004 — `awaiting_seed` (`executor.rs:1418`), а не `jump_done` (прыжок `Stalled` завершается сразу); F3 — «неубывающий ряд» вакуумно (единственный писатель — `fetch_max`); F4 — утверждается `>= 10^6`, а не `real + 10^6`; F7 — тест не различает плечо `AuthFailed` (BLS-`ensure!` `cold_start_jump.rs:686-692` vs ошибка `scheme_at` `:695-707`; текста ошибки в захвате нет; аналитически — BLS); F8 — состав комитета `FakeStaking` не зависит от `at_hash`, механизм записи R-001 («проверка проходит на подменённом состоянии») здесь недостижим; F9 — посеянная ветка — список хэшей без тел, `land_canonical` канонизирует безусловно, а «последствие (в)» реестр снял по Ex-2 на живом узле (`REGISTER.md:27/:32`, `EXPERIMENTS.md:20`: FCU на неотдаваемый хэш → `SYNCING`, `finalized` не двигается); F10 — плечи `Unservable`/`ConflictingPrefix` в `land_jump` не покрыты ни одним тестом; F17 — после первого прыжка EL узла 0 держит честную цепь до ~191, «клин» только по ордер-курсору; F18 — механизм записи R-004 (FCU на несуществующий хэш, 300 с блокировки derive) намеренно не показан (роль сохраняет настоящий хэш).

**Решение (последний раунд):** в код F4, F7 (текст ошибки `AuthFailed` в `JumpCall`), F10 (тест на оба плеча отказа `land_jump`); в доку/статусы F1, F2, F3, F8, F9, F11–F14, F17, F18 (оба теста — «частично»); заметками F5, F6, F15, F16, F19, F20.

**Финальный раунд (Opus 4.8, реляция):** в код — `peak == inflate_to` (F4), `JumpCall.outcome_detail` с текстом ошибки, R-001 утверждает «FAILED BLS verification against committee[» и не «unreadable» (F7; наблюдено на всех 41 вызовах), unit-тест `land_jump_refuses_an_unservable_tip_and_a_conflicting_prefix` (F10); доки/статусы — гейт `awaiting_seed`, ярус клина (F17: EL узла унесён за курсор), «частично», модель посадки вместо (в), Latin/Cyrillic, вакуумные/несущие ассерты названы. Ворота: 636+3+5+13/0 (2 ignored), стенд 34/0/0 (186 с), `clippy -D warnings` в обе стороны, rustfmt ок. Статусы R-004/R-001 внесены оркестратором в `REGISTER.md` (2026-09-10).

**Квитанции оркестратора [KNOWN]:** стенд 33/0/0 своим прогоном после раунда 2 (188 с); после финального раунда — общий прогон перед коммитом, §7.

## 5. Трек T3 — R-009 (3.3 B2; R-006 сценарий 1 закрыт в T4)

Исполнитель — **Opus 4.8** (два этапа, как T2). **Реляция:** `ForgeMode::WrongHeightFinalized` (`byzantine_roles.rs:549`), окно `WRONG_HEIGHT_WINDOW = 6..=40`, плечо `produce` отдаёт `inner.produce(Finalized{h−1})` — настоящую пару узла, только чужой высоты; самопроверка `height == h−1` и `payload == digest` до отдачи (`record_wrong_height` `:812`); `Role::WrongHeightFinalized` (`stand.rs:289`); поля `ByzFacts.wrong_height_*`; фикстура `wrong_height_stand` и тест `a_wrong_height_answer_satisfies_the_fetch_and_starves_the_by_height_gap` (`tests.rs:3441`) с честным контролем. **R-009 воспроизведён:** жертва приняла все 42 пары `(6, 5)` (`deliveries_rejected == 0` — счётчик ловит только ошибки декода; `FrontierHandler::deliver` не сверяет высоту с ключом, `plane_upstream.rs:201`), upstream «доставил» 43 ответа, marshal молча отверг несовпадение высоты (`CW:marshal/core/actor.rs`, ветка Finalized: `block.height() != height ⇒ send_lossy(false)`, без лога), дыра не закрылась — узел стоит на 5, контроль дошёл до 15; `rejump_calls == 0` — re-jump не маскирует. 1,4 с реальных. Не поставлено: мультипировая гонка «пока пир самый быстрый» (единственный источник), плечо `CertInlet::ingest`, ротация/штраф пира резолвером. Ворота: 636+3+5+13/0 (2 ignored), стенд 35/0/0 (187 с), `clippy -D warnings` в обе стороны, nightly-fmt без diff в `testbed/`. Вне объёма: комментарий `fakes.rs:1350` называет `deliveries_rejected` «плечом R-009» — неверно; плечо роли производит лишний `serve_requests` на источнике.

**Контрагент V3 (Opus 5, свежий контекст), реляция — вердикт «воспроизведено».** Механизм в проде настоящий (`plane_upstream.rs:201-213` ничего не связывает; `CW:marshal/core/actor.rs:987-993` отвергает молча, лога нет ни на одном уровне), путь прод-код end-to-end (marshal и `UpstreamResolver` не подменены, `outer.rs:1441,1672`). Замечания: потребление в marshal утверждалось только через `finalized_delivered > 0` (счётчик удовлетворённого waiter'а), 43/43 циклов — только в печати; молчаливая ветка выведена дифференциально (соседние `stale delivery` плечи логируют на DEBUG, стенд ловит WARN); `rejump_calls == 0` — тавтология фикстуры; изоляция `upstream_only_link` односторонняя (`remove_link` направлен, 1→3 и 2→3 остаются); `cargo fmt --check` на pinned stable красен по 5 чужим файлам (nightly-опции `rustfmt.toml`) — не этот слой. Прод не тронут. **Решение:** три мелкие правки исполнителю (связь `finalized_delivered >= wrong_height_served` и `calls == delivered`, `heights == 5`, `control.errors()` пуст; дока — гейт фикстуры, дифференциальный вывод, односторонняя изоляция). Статус R-009 внесён в `REGISTER.md`.

**Квитанции оркестратора [KNOWN]:** стенд 35/0/0 своим прогоном (188 с); база 636+3+5+13/0; `clippy -D warnings` в обе стороны чист (§7).

## 6. 3.4 — не снято, а отложено до после Э4/Э5

**Заявка оркестратора:** снять 3.4 как «покрыто стендом» (Ex-21 ≡ тест R-008, Ex-19 ≡ C7/R-121). **Контрагент (Opus 5, свежий контекст, read-only; сам прогнал стенд: 29/29):** вердикт «нет — не в этой формулировке; только как отложено после Э4/Э5 и с перечнем непокрытого». Реляция; ниже что я перепроверил сам.

Находки контрагента и мои квитанции [KNOWN — открыл сам]:
- A-1. Плечо `CertInlet::ingest` с `ensure_key(epoch, PinEffort::Local)` стендом не исполняется: `ensure_key(…, Local)` — единственный вызов `cert_inlet.rs:693`; `CertInlet::new` — только `crates/node/src/cert_inlet.rs:110`; стенд сам говорит «the stand has no inlet» (`testbed/stand.rs:1478`). Приём в тесте R-008 идёт через фронтир-плоскость → `UpstreamResolver::spawn_finalized`. Та же ветка `NoKey`, другой вызывающий. Ex-21 в девнет-форме (`cert-mitm-proxy.py`, WS перед `--cert-follow`) бьёт именно по отсутствующему плечу.
- A-2. `record_data_fault` существует только в `cert_inlet.rs`; «ротации не было» в стенде тривиально (журнал сессии A это уже понижал).
- A-4/A-5. Ветка (а) R-008 достижима только благодаря стендовому re-jump'у (модель, не прод) — это закрывает T1 этого закрытия (прод-прыжок в стенде).
- A-7/B-7. Живой слой противоречил заявке: `REGISTER.md` R-008 «Ex-21 в очереди первым», `DECISIONS.md` Д-3 то же, `EXPERIMENTS.md` §1 без строк про шаги 3–5 и сессию A, §3 строки 1–2 не тронуты, указатель `EXPERIMENTS.md:122` вёл на список «не показывает» шага 1. Черновики статусов R-002/R-008 из `E3-3-ROLES-1.md` §5 в реестр не были внесены.
- B-1. C7 не задевает R-007/R-018/R-026, которые Ex-19 числит своими: grep по `testbed/` — ни одного упоминания. B-5: ротация в C7 — замыкание расписания, `FakeStaking` реестр не мутирует; on-chain ротация и пол `MIN_COMMITTEE_LENGTH` — только девнет.
- C-4. Правило §2 плана («стенд раньше executor — R-006 показал: фейк-оракул делает тесты зелёными на ложной семантике») — контраргумент против «стенд уже показал».

Что сделано (правки живого слоя, без кода): `PLAN.md` 3.4 → отложено с перечнем непокрытого; `PLAN.md` 5.3 «нужно до» больше не ссылается на 3.4; `EXPERIMENTS.md` — новая секция §1 за 09-09 (шаги 3/4/5, C7→R-121, R-002, R-008), §3 строки 1–2 помечены «механизм закреплён стендом, девнет — приёмка после», указатель «не показывает» → `E3-2-STAND-4.md` §5.3 + `E3-3-ROLES-1.md` §7/§8; `DECISIONS.md` Д-3 — статус дополнен; `REGISTER.md` — статусы R-002 и R-008 из черновиков сессии A внесены (с оговоркой про inlet), «Ход по плану» R-121 — девнет после Э4, `[LIKELY]` не снят. `PLAN.md:161` «не поставлены» оставлен как есть — Ex-19/Ex-21 на девнете действительно не ставились.

Не сделано: третий режим `cert-mitm-proxy.py` (подстановка σ) — заказчик остался, делать при приёмке.

## 7. Итог ворот и коммиты

**Ворота (оркестратор, свои прогоны 2026-09-10 [KNOWN]):** `cargo test -p fluentbase-consensus` — 636 + 3 + 5 + 13 / 0 (2 ignored); `--features dpos-devnet-byzantine --lib testbed -- --test-threads=1` — 35/0/0, 188 с; `cargo clippy -p fluentbase-consensus --all-targets -- -D warnings` с фичей и без — чисто; `agreement_check.py` — 15 чеков, 0 расхождений (G3: 24 декларации). По реляциям исполнителей (не перепроверял сам): контракт 175/0 и 176/0; e2e `--release` 113/9/9 (девять `builtins::*` как до); genesis-bootstrap 6+8/0; pytest харнесса 1 failed (старое `test_the_two_jump_gates_…`) / 2085 passed / 9 skipped.

**Итог Э3:** 3.1 — урезано и сделано; 3.2 — сделано (09-09); 3.3 — сделано; 3.4 — отложено до после Э4/Э5; 3.9, 3.10, 3.11 — сделано. Стенд: 26 → 35 тестов, ни один старый ассерт не менялся. Прод-правки: видимость `EL_SYNC_STALL_ESCAPE` и правка прод-комментария в `cold_start_jump.rs`; в `contracts/staking/src/consts.rs` — вывод селекторов из общего крейта (значения не изменились, блоб не перегенерирован).

**Заметка о процессе:** брифы ролей (подмена фронтира, лгущий upstream) четырежды резались классификатором Fable по категории `cyber` на стороне оркестратора; исполнители T2/T3 переведены на Opus 4.8 (`~/.claude/agents/opus48.md`), длинные постановки — файлами в scratchpad и короткими сообщениями. Все контрагенты — Opus 5, свежий контекст.

**Коммиты (по явным путям, после «ок» пользователя):**
1. `refactor(staking-abi): declare every externally called staking handler once and derive its selector from the shared crate` — `crates/staking-abi/`, `contracts/staking/src/consts.rs`, `devnet/local-dpos-smoke/genesis-bootstrap/`, `e2e/Cargo.toml`, `e2e/src/staking*.rs`, `Cargo.lock`, `devnet/local-dpos-smoke/contracts/STAKING_ARTEFACT.md`.
2. `test(devnet): check the harness's ABI spellings, chain-config widths and the committee cap against the shared crate` — `devnet/local-dpos-smoke/scripts/xp/{agreement_check.py,README.md}`, `dpos_harness/stack/compose_gen.py`, `dpos_harness/tests/{test_compose_gen,test_topology,test_protocol_constant_transcriptions}.py`, шесть файлов харнесса с `(uint64)`.
3. `test(consensus): run the production jump in the testbed, give the fake chain reth's canonical semantics, and add the frontier roles` — `crates/dpos/consensus/src/testbed/*`, `crates/dpos/consensus/src/cold_start_jump.rs`, `crates/dpos/consensus/src/executor.rs` (два комментария по §8, поведение не менялось).
4. `docs(dpos): close the testbed stage` — `.dpos-study/{PLAN,REGISTER,EXPERIMENTS,DECISIONS}.md`, `.dpos-study/history/{E3-CLOSEOUT,E3-REVIEW}.md`. Правки дрейфа в `.claude/dpos_architecture/{00_preamble,TOC,15_…}.md` (§8) в коммит не входят: `.claude/` целиком в `.gitignore:46`, слой живёт только локально.

## 8. Независимая оценка закрытия (Fable 5.1, свежая сессия) и что по ней сделано

**Оценка:** `history/E3-REVIEW.md` (316 строк, 09-10; прогоны и якоря — её собственные). Вердикт: «Э3 закрыт, стенд готов быть регрессией» — да с оговорками. Регрессия есть для П-9 (R-002), R-006 сц. 1 (П-6), `deliver`-половины и фронтир-половины П-4 (R-009, R-004), Д-3/П-2 (R-008). Регрессии НЕТ для центральной части П-4 (комитет из локального finalized, аутентификация до `sync_to`) — вариант А R-001 не построен, `FakeStaking::committee` не зависит от хэша; для «после»-половины R-004/R-009 (жертва прикована к лжецу, честного пира нет); для П-1 (веса/тумбстоуны фиксированы); для всего через `CertInlet::ingest`. Переоценённых статусов реестра — один (R-008 «целиком»), одно неверное имя теста (R-122). Из 12 пунктов блока «Divergences from reth» по pinned checkout подтверждён один, два локализованы, остальное — пересказ `RETH_INTERNALS.md`. Контр-ревью V1–V6 оценены как реляции без мутаций и без чекаута reth; единственная отклонённая зря находка — V1 F3 = V2b F8 (состав `FakeStaking` по хэшу), ушедшая «в доку» вместо плана.

**Проверка оценки оркестратором [KNOWN]:** 25 якорей `file:line` из §1, §2, §5 открыты — все существуют и говорят заявленное (`fakes.rs:944-954`, `:1280-1290`, `:203-205` против `:411-413`/`:435`, `mod.rs:33`, `stand.rs:1941`, `consts.rs:178/:184`, `00_preamble.md:105/:236-238`, `cold_start_jump.rs:687-692`, `plane_upstream.rs:206-208`, `executor.rs:1936-1938/:3140-3148`, `stand.rs:961-973/:129-136/:1605-1610`, `fakes.rs:1350`, `tests.rs:3229/:3503`, `REGISTER.md:905`, шесть счётчиков `TOC.md`). §7 оценки — не пересказ контр-ревью: находки `fakes.rs:203-205`↔код, `Derived(8)`×4, `SnapshotReader`, селектор в preamble, имя теста R-122, `live_height` до проверки ни в одном V не были. Уточнение: записей `2026-09-10` в `verified-against` шесть, не девять (в постановке оценки было девять — ошибка постановки).

**Мутации прод-кода (оркестратор, свои прогоны 09-10, каждая откачена; `md5sum -c` по трём файлам — ЦЕЛ) [KNOWN]:**
| Мутация | Тест | Предсказание оценки | Факт |
|---|---|---|---|
| `cold_start_jump.rs:687-692` — `ensure!(verify…)` → `let _ = verify…` | R-001 `a_lying_upstream_lands_a_divergent_branch_and_authentication_refuses_it` | красный на `tests.rs:3229` | красный на `:3229`: прыжок `Landed` на (125, 0x0a0e…), затем узел 0 `SafetyHalt(ResultDivergence)` на 100, `diverged = Minority{0, 100}` — ветка (b) теста и «последствие (б)» записи R-001 показаны живьём; контроль зелёный |
| `plane_upstream.rs:206` — после `decode_frontier` отказ, если `Finalized{height}` ≠ `uf.block.height` | R-009 `a_wrong_height_answer_satisfies_the_fetch_and_starves_the_by_height_gap` | красный на `:3503` | красный на `:3503` (`deliveries_rejected: 1`) |
| `executor.rs:1936-1938` — снять `upstream_frontier.fetch_max` | R-004 `an_inflated_latest_probe_wedges_a_rotated_out_validator_in_re_jumps` | красный на `:2961` | красный на `:2961` (`Some(0)` против `Some(1000194)`) |

Три из трёх — стенд различает включённую и выключенную проверку на каждом из трёх швов П-4. `Derived(8)`×4 в (3b) воспроизведён своим прогоном (`el_events[3]`: `Derived(8)` четырежды до `Canonicalized(8)`).

**Применено по оценке:** дрейф доков — `00_preamble.md` (`stand.rs:1803`→`:1941`; селекторы: `0xad36f42f` — `getConsensusKeys`, `commitEpochCommittee` — `0xe505b249`), `TOC.md` (6 счётчиков), `15_…` §15.a (3b) («each height ONCE» → 4..7 раз, 8 — четырежды); `REGISTER.md` R-122 (имя теста), R-008 (снято «целиком»); комментарии: `testbed/mod.rs:33` (`SnapshotReader`→`FakeStaking`, семантика чтения по хэшу), `mod.rs:77` (пять ролей), `fakes.rs:203-205` (no-op на канонической голове), `fakes.rs:1350` (`deliveries_rejected` — не плечо R-009), `stand.rs:129-136` (только исходящие линки), `stand.rs:1607-1609` (обещание → ссылка на 4.0(в)); прод-комментарии `executor.rs:3140-3148` («caught IMMEDIATELY» → молчит до FCU, вердикт с `h − K`) и `:7443-7446` (тест зелёный из-за land-at-derive). `PLAN.md`: строка 4.0 — предусловия к 4.1/4.2 (вариант А R-001; двухисточниковые фикстуры; стендовый `CertInlet`; роль «эпоха ≠ высота»; `l1_checkpoint`/R-003; conformance или живой прогон `sync_to` до его перестановки; ярлыки веток R-008/R-001). Не применено: сами предусловия (работа Э4, ~1 нед. + (е)), `capture.rs` порог WARN, `committed_at` с литералом `0`, каталоги temp — §8 оценки, оставлено как есть с причиной.

**Ворота после правок (оркестратор, свои прогоны 09-10) [KNOWN]:** lib 636 + 3 + 5 + 13 / 0 (2 ignored); стенд с фичей 35/0/0, 187,5 с; `clippy --all-targets -D warnings` чист с фичей и без; `cargo fmt --check` — 5 диффов в `application.rs`, `cert_inlet.rs` (×2), `spec_exec.rs`, `executor.rs:4715` — файлы, не тронутые в Э3 (на HEAD те же), не наш дефект.

# Э5 — проход предусловий (Д1–Д8). Журнал 2026-09-12

Ветка `djadjka/dpos-reth-2.2-squashed`, HEAD `27485df6` (код = `48412eb3`). Проект — `history/E5-BEACON-DESIGN.md` (писался 2026-09-10 против `42280d93`; между ними 78 коммитов: 5.0 `777f7916` с отклонениями Д-1…Д-14 в `history/E5-0-BOUNDARY.md` А§2/В§2, Э4 `fcd3b2ba`…`27485df6` — 33 коммита, контрактные правки 09-11/12). Commonware — `~/.cargo/git/checkouts/monorepo-27b478c9bb41d208/3c4e02c` (`3c4e02ceede03126f524216605a1195e1cee7d0e`, `Cargo.lock:3255`, тот же sha, что в шапке проекта). Пути без префикса — `crates/dpos/consensus/src/`; `beacon/` = `crates/dpos/consensus/src/beacon/`; `node/` = `crates/node/src/`; `CW:` — чекаут commonware. Теги: [KNOWN] — открыл/прогнал в этой сессии; [ГИПОТЕЗА] — вывод; реляция — помечена словом «реляция». Рабочее дерево во время прохода НЕ равно HEAD: параллельная сессия реализует R-128 (на 20:05 изменены `committee/{mod,store,tests}.rs`, `epoch_manager.rs`, `sync_metrics.rs`, `testbed/{committee_tests,fakes,stand}.rs`; её `cargo test` шёл в это же время) — все чтения кода в этом журнале сделаны по `git show HEAD:…`/файлам, чей `git status` чист, кроме названного в §6.

## §0. Прямые ответы

1. **Якоря.** По команде задания (`grep -oE '[a-z_/]+\.rs:[0-9]+(-[0-9]+)?' <(sed -n '97,733p' …) | sort -u | wc -l`) — **372** уникальных (552 вхождения). На месте — **132** (плюс 18 в чекаут commonware, который не менялся, и 1 внешний `tokio sync/broadcast.rs`); сдвинулись — **189**; сняты (символ удалён или переименован) — **32**. Каждое вхождение помечено в тексте: `[перепинено 27485df6]` / `[перепинено 27485df6: было N, стало M]` / `[снято 27485df6: символ удалён коммитом <sha> — куда]`. Из 32 снятых 24 забрал сам 5.0 (`777f7916`), 4 — 4.1 (`0f9be693`, `65a23db7`, `04eb9b14`), 3 — 4.2 (`20f47287`, `dacd1bfa`, `48bf62ed`), 1 — цитата документного дрейфа (`outer.rs:246`). Счёт — §3.
2. **Строки 5.1–5.4.** 5.1: «Работа» — Д-7 переписан (бит с якоря модуля, не «на курсоре `max(fin, live)`»; `committee_pair_for`/страж `carry.rs:238-240` уходят; форма `CommitteeReads` сжимается; хвосты 5.0 Д-10/Д-11/Д-12 перенесены сюда); «Нужно до» — 5.0 закрыт, Д-7 ратифицировать. 5.2: «Работа» — читатели вердикта `let _observed`, потребитель `faults()`, `for_seeds`/`waiters`, R-129; «Нужно до» — Ex-7 снят, `4.0(в)` → 5.0а, Д-3/Д-9 ратифицировать, Ex-21 — приёмка; «Оценка» — без «1–2 дн чужой работы». 5.3: «Работа» — вход p2p одним источником (Д-123/R-127), R-126, P-16, Д-6 = defer; «Нужно до» — Ex-22 выполнен (имя теста), Д-6 «предложено». 5.4: «Работа» — что уже сделано Э4 и что осталось (см. 3), якоря band-sweep/защёлки перепинены; «Нужно до» — R-128 до 5.4, tee-тест требует 5.0а. Плюс 5.0 «Оценка» — «сделано `777f7916`» вместо «не закоммичено».
3. **5.4 после Э4.** Уже сделано: (а) `watch` от marshal Tip существует — `FluentApp::ordering_tip()` `application.rs:452-459`, писатель `report(Update::Tip)` `:1072-1081` (`send_replace`, начальное значение `0`, стартовый Tip marshal-а приходит тем же ребром), потребитель `epoch_manager.rs:679` (4.2-В `bcebd3b7`) [KNOWN]; (б) band-sweep edge-driven с мемо `swept_to` (`epoch_manager.rs:338-398`, `:471-476`; 4.2-В `48bf62ed`, R-125) [KNOWN]; (в) `LiveFrontierTee` ужат до одного курсора `dkg_height_tx` + `plane_clock` (`cert_inlet.rs:274-283`) [KNOWN]; (г) `Tasks{supervised, drain}` + супервизор — 5.0 (`beacon/plane.rs:174-215`), узел держит один `("beacon", …)` `node/dpos.rs:797`, `:818-820` [KNOWN]. Осталось: beacon всё ещё берёт `heights: mpsc::Receiver<u64>` (`plane.rs:504-507`) с тремя фидерами — поллер `fin + K` `node/dpos.rs:1496`, `:1526`, tee `cert_inlet.rs:713`, app `application.rs:1081`; `git grep dkg_height_tx -- crates` — 31 строка (задание называло 23; считал сам); `ordering_tip` создаётся внутри `FluentApp::new` (`:424`) ПОСЛЕ `beacon::build` (`node/dpos.rs:1877`) — 5.4 либо выносит канал наружу (как `dkg_height_tx` `node/dpos.rs:1326`), либо даёт beacon-у отложенный attach; перенос `prune_agreements`/`dkg_agreements` (`:471`)/`agreement_intake` (`:481`, `:693-698`, `:980-1016`) и мемо `swept_to`; защёлка `is_engaged()` `:1004-1017`; третье поле `Tasks.agreement_intake` и `pub(crate) agreement_partition` (`beacon/mod.rs:129`); имена партиций `dpos.rs:195-204`.
4. **Д-7 после 4.1.** Строка 5.1 была неверна — переписана. Курсор `max(fin, live)` удалён `0f9be693` (`committee_read_hash`, `committee_cursor`) и `777f7916` (`dkg_qual_at`); единственная реализация `beacon::CommitteeReads` — `committee::CommitteeReadsFacade` (`committee/facade.rs:36-91`): `read_at()` = `Committee::anchor_hash()`, `dkg_qual(epoch, _)` = `(record.changed, true)` (`:88-90`), `at` игнорируется (`:66-80`). Но решение «дилю ли я» бит НЕ читает: `maybe_start` сравнивает ростеры через `committee_pair_for` (`beacon/actor.rs:1663-1690`) [KNOWN]. Итог для 5.1: «читать бит `changed` с якоря модуля, перевести `maybe_start` на бит, снять `committee_pair_for` и страж `!(bit || committed)` `carry.rs:238-240`»; расхождение «якорь вместо `max(fin, live)`» принято Э4 4.1 (`E4-CORE-DESIGN.md` §5.1). Статус Д-7 в `DECISIONS.md` — «предложено».
5. **Стендовый `CertInlet`.** Строка **5.0а** заведена (после 5.0, форма «закрывает / нужно до / оценка»). Оценка 1–2 дн [ГИПОТЕЗА] — все входы уже в стенде: marshal-хэндл `NodeHandles::marshal`, модуль комитета, `Arc<dyn Beacon>`, `UpstreamFinalized` (`stand.rs:21`, `byzantine_roles.rs:59,638`), `CertUpstream`-фейк `CountingUpstream` (`fakes.rs:1601-1619`), `plane_upstream::new_bridge` (`stand.rs:36`); нового — задача над `CertInlet::new(marshal, committee, ctx)` (`cert_inlet.rs:404`, `M: MarshalSink`) `.with_tee` `.with_randomness` `.with_rotate` (`:446-479`), фидер `UpstreamFinalized` из архива другого узла, `StandConfig`-переключатель, счётчик ротаций (`record_data_fault` `:758`). `git grep CertInlet testbed/` на HEAD пуст [KNOWN].
6. **Ex-7 — снят.** R-009 закрыта `7cac7f3b` (4.2-А): `FrontierHandler::deliver` сверяет `block.height == h` и `epoch_of(height) == round.epoch` до `verify_block`, несовпадение — `false` ⇒ CW исключает пира; стенд Э3 переписан как «после» — `a_wrong_height_answer_costs_the_liar_the_channel_and_never_satisfies_the_fetch` (`testbed/tests.rs:3955`) [KNOWN по имени и `REGISTER.md` R-009 статус 09-12; тело `deliver` в этой сессии не перечитывал — реляция журнала 4.2-А]. Условие Ex-7 из Д-9 («надёжность резолвера при `deliver == true` с неверным телом») беспредметно: такой ответ теперь `false`. `EXPERIMENTS.md` §3 п.6 — снят; п.3 (Ex-22) — выполнен 09-09.
7. **Ратифицировать до старта** (все «предложено» в `DECISIONS.md`, состояние 09-09): Д-3 = вариант (в) — стоит 5.2; Д-7 = читать бит `changed` с якоря модуля — стоит 5.1; Д-9 = beacon-owned `SeedIndex` + intake (ни П-2, ни B-1 дословно) — стоит 5.2; Д-6 = defer (локальный бан + сохранённая пара, контракт не трогаем) при открытом Д-4 — стоит 5.3. Плюс Д-1 в `DECISIONS.md` до сих пор «предложено (нужно до Э4.1)», хотя 4.1 реализовала (а) — устарело, владельцу. Без ратификации оркестратор по hard-stop (2) («реализация невозможна без изменения решения в `DECISIONS.md`», `E4-ORCHESTRATOR.md:5`) остановит 5.1 на первом же шаге.
8. **Итоговая строка блока** (verbatim, `PLAN.md` после строки 5.4): «Суммарно Э5: 8,5–10,5 нед [ГИПОТЕЗА] (проект §0.4; 5.0 закрыт `777f7916`; остаток 5.0а (1–2 дн) + 5.1 (2–3) + 5.2 (1,5–2) + 5.3 (3) + 5.4 (1) ≈ 7,5–9 нед; проход предусловий 2026-09-12, `history/E5-PRECONDITIONS.md`, оценок не менял). Порядок: 5.0а → 5.1 → 5.2 → 5.3 → 5.4; Э4 закрыт `27485df6`, запреты «4.2 с 5.4» и «4.3 с 5.3/5.4» сняты. Можно параллельно: 5.0а с 5.1 при разделении файлов (5.0а — `testbed/stand.rs`/`fakes.rs`, 5.1 — `beacon/*` и `testbed/tests.rs`; общая точка `StandConfig` — согласовать); Э7-правки вне `beacon/`. Нельзя: 5.1 с 5.2 (общие `beacon/surface.rs` — `certificate_verdict` `:324` и `for_keys`/`signer_scheme` `:1009`/`:2478`, `plane.rs`, `follower.rs`); 5.1 с 5.3 (`actor.rs`/`surface.rs`); 5.2 с 6.1 (executor seed intake: `executor.rs:3002`, `:3301`); 5.3 с 5.4 (`actor.rs`, `dkg_engine.rs`); 5.4 с R-128 (`epoch_manager.rs`) — R-128 раньше. До старта ратифицировать в `DECISIONS.md` (все четыре «предложено»): Д-3 = (в) — 5.2; Д-7 = читать бит `changed` с якоря модуля — 5.1; Д-9 = beacon-owned `SeedIndex` + intake, ни П-2, ни B-1 дословно — 5.2; Д-6 = defer при открытом Д-4 — 5.3; иначе hard-stop (2) на первой же строке.»
9. **Ворота** — §4 verbatim.
10. **Что в проекте §5 неверно по коду HEAD** (самое опасное первым): (1) §5.1 `committees: Arc<dyn CommitteeReads>` «dkg_qual читается на ТОМ ЖЕ курсоре max(fin, live)» — курсора нет, читается с якоря модуля (`facade.rs:58-63`, `:88-90`); строка плана 5.1 повторяла это как задание — исправлено. (2) §5.3 «инстанс agreement»/§5.4 «band-sweep переносится как есть» — sweep стал edge-driven с мемо `swept_to` (`epoch_manager.rs:338-398`, `:471-476`); перенос «как есть» без мемо вернул бы 8 `Storage::remove` на блок (R-125). (3) §5.2 «входы автомата — p2p-сообщения» — после 4.3 кадр BEACON классифицируется до декода `GatedReceiver`/`TrackedWindow` (`p2p/lib.rs:338-401`), а в акторе второй раз `beacon_member` через `committee_for` (`actor.rs:1943`) и окно `epoch_is_actionable` (`:1923`); два источника (Д-123/R-127), кадр `now+3` роняется (R-126) — проект про вход p2p молчит. (4) §5.1/§5.3 «один `clock` — watch» — watch уже есть, но канал создаётся после `beacon::build` (`application.rs:424` против `node/dpos.rs:1877`) — порядок сборки, который §5.1 не учитывает. (5) §5.1 «`Tasks{supervised, drain}` ровно два хэндла» — три (`agreement_intake`, `plane.rs:174-197`). (6) §5.1 «трейт из 9 методов» — 11 (`surface.rs:126-219`). (7) §5.1 «`absent_unregistered` в двух конструкторах» — в одном (`cert_inlet.rs:410`; `04eb9b14`). (8) §5.3 «`committee_pair_for` удаляется: решение — только по биту» — оно ещё стоит (`actor.rs:1663-1690`) — это работа 5.1, не факт. (9) §6 «Ex-7 остаётся условием 5.2» — снят (R-009 закрыта). (10) §7 «5.4: имена партиций — в модуль» — `dpos.rs:195-204`, якорь `116-125` сдвинут. Каждое помечено в проекте строкой `[изм. предусловия 2026-09-12: …]`.
11. **Принял без чтения кода.** (а) Тело `FrontierHandler::deliver` после 4.2-А — по `REGISTER.md` R-009 и `E4-CLOSEOUT.md` §2 (реляция); открыл только имя стенд-теста. (б) R-124 (ET двойная парковка) — реляция F11, ассерт открыл (`epoch_transition.rs:484`), прогон не повторял. (в) Механизм tee «избыточен» (`cert_inlet.rs:713` → `report_finalization` → marshal Tip → `application.rs:1072-1081`) — CW `core/actor.rs:1453-1457` в этой сессии не открывал; чекаут тот же sha, что у проекта. (г) Что R-128 реализуется параллельно — со слов задания и по `git status`/`git diff` дерева (`committee_tests.rs` описывает `ContractFork` halt); содержимого их правок `epoch_manager.rs` не читал. (д) 189 сдвинутых якорей перепинены скриптом по совпадению текста (до трёх строк блока); руками открыты 40 «одно-строчных» совпадений (§3) и все 32 снятых. (е) Оценки строк — не менял, не проверял.
12. **Длинные строки** читал `Read` и `sed -n 'N,Mp'`; поиск — `awk 'length > 500 {c++} END {print c+0}'` (проект 65 строк, PLAN 26, EXPERIMENTS 11); `cut -c`/`head -c` не применял.
13. **Готов ли Э5 к оркестратору в понедельник — да, с двумя условиями вне моего объёма:** ратификация четырёх решений (п.7) и завершение R-128 в параллельной сессии до 5.4 (5.0а/5.1/5.2/5.3 от неё не зависят). Стенд-хвост: ворота этой сессии прогнаны на дереве с чужими правками (§4).

## §1. Таблица дыр

| Д | исход | свидетельство | куда внесено | чем пытался опровергнуть и почему не вышло |
|---|---|---|---|---|
| Д1 якоря | закрыта скриптом + чтением | `scratchpad/anchors.py` + `mark.py`: старый текст `42280d93` найден на HEAD по содержимому (блок до 3 строк, при промахе — одна строка); 372 уникальных: 132 на месте, 189 сдвинулись, 32 сняты, 18 CW, 1 tokio; 40 одно-строчных совпадений сверены руками (старая и новая первая строка совпали у всех), 4 неоднозначных (`actor.rs:926` → `:941`, `mod.rs:60` → `:68`, `surface.rs:1961` → `:2453`, `surface.rs:1986-2108` → `:2478-2600`) разведены по контексту; 32 снятых — каждый найден символом (`git grep`) и коммитом (`git log -S`) | проект §2/§4/§5/§6/§7 — пометка у каждого вхождения (341 строка изменена) | для «на месте» проверял равенство ВСЕГО блока, не первой строки; два блока совпали по началу и разошлись хвостом — `dkg_engine.rs:513-519`, `metrics.rs:28-167` (помечены как на месте: символ тот же) |
| Д2 5.0 vs проект | закрыта чтением | Д-1…Д-14 журнала 5.0 (А§2, В§2) против HEAD: Д-1 два конструктора (`plane.rs:471`, `follower.rs:117`), Д-2 11 методов (`surface.rs:126-219`), Д-3 правило подписки (`executor.rs:1350`, `epoch_manager.rs:732`), Д-4/Д-5 `BeaconEvent` без нагрузки/без `Stalled`, Д-6 `Tasks.agreement_intake` (`plane.rs:174-197`), Д-7 две реализации (`LiveBeacon`, `FollowerRandomness` `follower.rs:394`), Д-8 `faults_armed` (`surface.rs:2278,2341,2707`), Д-9 `agreement_partition` `pub(crate)` (`mod.rs:129`), Д-10 `absent_unregistered` в `cert_inlet.rs:410` (из `FluentApp` ушёл `04eb9b14`), Д-11 `ArtifactFetch` (`follower.rs`), Д-12 `PinEffort` `pub`, Д-13 `beacon::testing` (`mod.rs:146`), Д-14 `qual_read_at` — закрыт 4.1 (`0f9be693`), символа нет | таблица §2 ниже; PLAN 5.0/5.1/5.2/5.4 | искал в HEAD второй курсор `qual_read_at` — только док-коммент `plane.rs:109-119`; искал `for_keys`/`for_seeds` вне `beacon/` — потребители только `executor.rs:5012,5051` (тесты) через `beacon::testing` |
| Д3 Э4 vs Э5 | закрыта чтением | §2 ниже | проект §5.1/§5.3/§5.4/§6/§7 (`[изм. предусловия]`), PLAN 5.1/5.3/5.4 | — |
| Д4 стендовый `CertInlet` | закрыта чтением [ГИПОТЕЗА по оценке] | `cert_inlet.rs:290-360` (поля), `:395-404` (`new`, `M: MarshalSink`), `:446-520` (`with_*`, `ingest`), `:758` (`record_data_fault`), `node/cert_inlet.rs:57-96` (продакшн-сборка), стенд: `stand.rs:21,29,36,2156`, `fakes.rs:1601-1619`, `byzantine_roles.rs:638` | PLAN 5.0а; проект §6 «Порядок», §7 5.2/5.4 | искал `CertInlet` в `testbed/` — пуст; искал `LiveFrontierTee` в стенде — пуст (стенд кормит `dkg_height_tx` только через `FluentApp`, `stand.rs:2228-2233`, `:2340`) |
| Д5 устаревшие предусловия | закрыта | R-009 закрыта (`REGISTER.md` R-009 статус 09-12, `7cac7f3b`), тест `tests.rs:3955`; Ex-22 — `tests.rs:2948` + `EXPERIMENTS.md` §1 09-09; Ex-21 — `EXPERIMENTS.md` §3 п.1 «приёмка после Э5.3» | PLAN 5.2/5.3, EXPERIMENTS §3 п.3/п.6, проект §6 | `EXPERIMENTS.md` §3 держал Ex-7 и Ex-22 в очереди при закрытых записях — это и есть рассинхрон, исправлен |
| Д6 решения | закрыта чтением | `DECISIONS.md` (состояние 09-09): Д-3/Д-6/Д-7/Д-9 «предложено», Д-4 «принято 09-04, под вопросом 09-07», `D4-BRIEF.md` §6 — рекомендация (А); Д-1 «предложено (нужно до Э4.1)» при реализованном 4.1 | §0.7; PLAN «Нужно до» 5.1/5.2/5.3 и итоговая строка; `DECISIONS.md` не правил (владелец) | — |
| Д7 порядок | закрыта чтением | общие файлы: `surface.rs` держит и `certificate_verdict` (`:324`, 5.2), и `for_keys`/`signer_scheme` (`:1009`, `:2478`, 5.1); `plane.rs`/`follower.rs` — обе строки; `executor.rs:3002`, `:3301` — 5.2 и 6.1; `actor.rs`/`dkg_engine.rs` — 5.3 и 5.4; `epoch_manager.rs` — 5.4 и R-128 | PLAN итоговая строка | 5.0а ‖ 5.1: 5.1 пишет стенд-тесты (persist-ошибки, C7) — `tests.rs`, возможно `StandConfig` в `stand.rs` — записано как условие разделения |
| Д8 ворота | см. §4 | — | §4 | — |

## §2. Д3 подробно — что Э4 изменила из того, на что опирается Э5

**(а) `beacon::CommitteeReads` — фасад.** Трейт на HEAD (`beacon/plane.rs:120-145`): `read_at() -> Option<B256>`, `committee(epoch, at) -> Option<Set<PeerPubkey>>`, `committee_bls(epoch, at) -> Option<EpochCommittee>`, `dkg_qual(epoch, at) -> Option<(bool, bool)>`, provided `committee_pair(target)` (два `committee` на одном `read_at`). `qual_read_at` удалён (`0f9be693`; док-коммент `:109-119` объясняет почему). Реализация одна — `committee::CommitteeReadsFacade` (`facade.rs:36-91`; держатели `node/dpos.rs:1417-1418`, `dpos.rs:3294-3295` follower, `testbed/stand.rs:2292-2293`): `read_at` = `Committee::anchor_hash()` = `executed_state_hash(ordering_finalized)`; `at` игнорируется (write-once, `:66-80`); `dkg_qual` = `(record.changed, true)` (`:88-90`), любой `CommitteeError` ⇒ `None` (`:47-54`) — различие «не закоммичено»/«форк контракта» здесь теряется (модуль логирует). Проекции beacon-а над ним — `plane.rs:589-608` (`committee_for`, `committee_pair_for`, `committee_source`, `frozen_dkg_qual(read_at, dkg_qual)`), follower — `follower.rs:121-132`. Что 5.1 удаляет из формы: параметр `at` (нечего решать), `dkg_qual` в форме `(bit, committed)` (`committed` теперь тавтологично `true`), provided `committee_pair` (решение о старте — по биту), `frozen_dkg_qual`-мемо и страж `!(bit || committed)` (`carry.rs:238-240`) — вместе с `carry.rs`. Что 5.1 берёт из модуля напрямую (`committee/mod.rs:277-420`): `committee(epoch) -> Result<Arc<CommitteeRecord>, CommitteeError>` (запись с `participants`, `bls`, `changed`, `weights`), `changed(epoch)`, `is_member(epoch, peer)`, `subscribe() -> watch<u64>` (пробуждение на каждом `anchor_advanced` и на заморозке геометрии), `geometry()`/`epoch_of`, `anchor_hash()`. **Д-7 сегодня:** бит читается на якоре модуля, но `maybe_start` (`actor.rs:1649-1725`) решает по `next == cur` через `committee_pair_for` (`:1663-1690`); строка PLAN 5.1 говорила «читать бит НА КУРСОРЕ `max(fin, live)` (`node/dpos.rs:1382-1404`)» — устарело дважды (курсора нет; бит уже на якоре) — переписана. Потеря против `max(fin, live)` для inlet-валидатора (полоса `(ordering_finalized, min(live, best)]`) принята Э4 4.1 (`E4-CORE-DESIGN.md` §5.1 «Якорь после 3-го прохода»).

**(б) `ordering_tip` и 5.4.** `FluentApp.ordering_tip: Arc<watch::Sender<u64>>` (`application.rs:340-368` док, `:424` создание в `new`, `:452-459` `ordering_tip()`), писатель — `report(Update::Tip)` `:1072-1081` (`send_replace`, рядом `plane_clock.record_ordering_tip` и lossy `dkg_height_tx.try_send`), потребитель — `epoch_manager.rs:679` (`tip: cfg.app.ordering_tip()`), правило live-эпохи `:1089-1094` (`is_live_epoch_at(geometry, *tip.borrow(), epoch)`, `:1935-1937`). Это ровно «один `clock` от marshal Tip» §5.3, но для epoch_manager, не для beacon: `ValidatorInputs.heights: mpsc::Receiver<u64>` (`plane.rs:504-507`) по-прежнему кормят три фидера — поллер `node/dpos.rs:1496` (`cs_fin_num + K` при старте), `:1526` (`fin + K` на каждом finalized-ребре), tee `cert_inlet.rs:713`, app `application.rs:1081`; `DkgActor::on_height` клампит (`actor.rs:1160-1168`, `last_height: Option<u64>` `:437`). Порядок сборки: `beacon::build` в `node/dpos.rs:1877` идёт ДО `OuterBuilder::build`, внутри которого рождается `FluentApp` (`outer.rs:1008-1015`, `with_dkg_heights`) — watch-канал придётся создавать снаружи (как `dkg_height_tx` `:1326`) или отдавать beacon-у после сборки. 5.4 из проекта осталось: замена `heights` на watch, снятие фидеров и `LiveFrontierTee` (тип после 4.2 несёт только `dkg_height_tx` + `plane_clock`, `cert_inlet.rs:274-283`; в стенде типа нет), `with_dkg_heights`/`OuterBuilder.dkg_height_tx` (`outer.rs:448`, `:1011-1014`)/`SharedBeaconPlane.dkg_height_tx` (`node/dpos.rs:1014`, `:1967-1973`)/стенд (`stand.rs:2233`, `:2340`, `:2362`); перенос `prune_agreements` (`epoch_manager.rs:338-398`), `dkg_agreements` (`:471`), `swept_to` (`:472-476`), `agreement_intake` (`:481`, `:693-698`, `:758`, `:980-1016`) и защёлки (`:1004-1017`: `safety_halt.is_engaged()` ⇒ `handle.abort()` при адопции); `abort_below`/sweep на SafetyHalt `:789`, `:1030`. `dkg_height_tx` на HEAD: 31 строка под `crates/` (`git grep`), 75 с документами.

**(в) Вход BEACON после 4.3.** `PeerSetSink::track(epoch, TrackedPeers{committees, secondary})`; `TrackedWindow` (`p2p/lib.rs:338`), `classify(peer) -> Option<Ingress>` (`:376`), `Ingress{Member{epochs}, Tracked, Dropped}` (`:401`); `GatedReceiver` роняет кадр до декода; в акторе — `epoch_is_actionable` (`actor.rs:1923`: `[now, now+2]` ∪ идущие церемонии, `now = epoch_of(last_height)`, окно только после первого тика — `last_height: Option<u64>`, Д-129) и `beacon_member(from, epoch)` через `committee_for` (`:1943`) — второй источник классификации (Д-123/R-127). `ValidatorInputs` (`plane.rs:471-540`) окна не получает; поле `peers` несёт ПРЕДУСЛОВИЕ «ET трекает `C[E−1] ∪ C[E] ∪ C[E+1]` как primary» док-комментом (`:491-497`). Проект §5 про вход BEACON говорит только «p2p-сообщения» как один из четырёх входов автомата (§5.2 шапка) и `p2p: beacon_channel` в `BeaconInputs` (§5.1) — классификации нет. Ложится в **5.3** («входы автомата — clock + артефакт + p2p + resolver»): маска — один параметр `ValidatorInputs`, `beacon_member` уходит, окно `epoch_is_actionable` — правило автомата; кадр `now+3` (R-126) — там же. PLAN 5.3 дополнена.

**(г) R-124, R-126, P-16.** R-124 — `staking-reader/src/epoch_transition.rs:484` (`debug_assert!` «two boundaries pending at once»), крейт `staking-reader`, ни одна строка Э5 его не трогает — НЕ в Э5; `E4-CLOSEOUT.md` §6 относил к «Э5 (ET)» ошибочно по адресу: правка — инвариант `interval > MAX_PENDING_ACKS + K` или очередь — Э7/тикет; в PLAN не вносил (строк Э7 в таблице нет, список Э7 — реестр). R-126 — 5.3 (окно `epoch_is_actionable`). P-16 (`deque_size = 2` при перепредложении после nullify) — 5.3 (`dkg_transport.rs:123`-класс, замер Д5 предусловий Э4 не встретил перепредложения).

**(д) `epoch_manager` после 4.1/4.2.** Один `EpochTransition` (`479b60b3`): `PlaneEpochTransition{transition, bridge_rx}` (`node/dpos.rs:1975-1978`); band-sweep — `:299-337` док + `prune_agreements` `:338-398` (abort + JOIN `:363-374`, пропуск живых `:386-389`, полоса `cutoff − 8 .. cutoff`, мемо `swept_to` поднимается, не присваивается `:397`); защёлка — арм `agreement_intake` `:980-1016` (`:1004-1017`); `dkg_agreements: BTreeMap<Epoch, Handle<()>>` `:471`; `reconcile_roles` `:1102`, `is_member = !safety_halt.is_engaged() ∧ …` `:1203`; `reconcile_live` `:1574`; `soft_enter` `:1188` (вместо `register_soft_entered`, `65a23db7`); `handle_msg_for_unregistered_epoch`/корроборация — удалены `48bf62ed`; `boundary_base` `:157-168` (`&dyn Beacon`); `repair_keyless_schemes` `:1873-1908`. Проектные якоря `:300-366` → `:299-398`, `:829-852` → `:1004-1017`, `:433` → `:471`, `:828-865` → `:980-1016`, `:158-169` → `:157-168`, `:1667-1702` → `:1873-1908`.

**(е) R-128.** Решение владельца 09-12 (`REGISTER.md` R-128: (а) `Fault::corruption`/fail-stop + отравленный слот); реализация идёт в параллельной сессии — на 20:05 [KNOWN по `git status`/`git diff --stat`]: `committee/{mod,store,tests}.rs`, `epoch_manager.rs`, `sync_metrics.rs`, `testbed/{committee_tests,fakes,stand}.rs`; описание теста в `committee_tests.rs` — «`SafetyHalt` latch engaged with `ContractFork` on every node». Следствия для Э5: 5.4 (перенос `agreement_intake`/защёлки из `epoch_manager.rs`) — ПОСЛЕ R-128, тот же файл; 5.1 — фасад `facade.rs:47-54` сворачивает `Read(_)` в `None`: если R-128 ставит «отравленный слот» в сторе, 5.1 при сжатии `CommitteeReads` обязана сохранить, что постоянный отказ доходит до beacon-а как отказ, а не как «ещё не закоммичено»; R-129 (`scoped ⇒ None` ⇒ `deliver == false` на marshal-резолвере) достижим через R-128 — фикстура в 5.0а/5.2.

## §3. Д1 — счёт якорей

Команда задания: `grep -oE '[a-z_/]+\.rs:[0-9]+(-[0-9]+)?' <(sed -n '97,733p' history/E5-BEACON-DESIGN.md) | sort -u | wc -l` → **372** (до правок; после пометок команда посчитает и якоря внутри пометок). Вхождений — 552. По категориям (уникальные): на месте — 132; сдвинулись — 189; сняты — 32; чекаут commonware (`3c4e02c`, не менялся) — 18; внешний (`tokio sync/broadcast.rs`) — 1. Снятые (32, по коммиту): `777f7916` — `application.rs:1956-1965`, `cert_inlet.rs:3012-3043`, `:3034-3040`, `:967-968`, `dpos.rs:3414-3438`, `:3421`, `:3444`, `epoch_manager.rs:805`, `executor.rs:1478-1482`, `node/dpos.rs:1410`, `:1519-1543`, `:2609`, `:834`, `:837`, `:856-873`, `outer.rs:1821`, `plane.rs:294-364`, `:384-420`, `:684-693`, `:786-802`, `spec_exec.rs:108-115`, `:97-125`; `0f9be693` — `node/dpos.rs:1382-1404`; `65a23db7` — `cert_inlet.rs:403`, `dpos.rs:2722-2724`, `engine.rs:225`, `:80`, `epoch_manager.rs:1545-1561`; `04eb9b14` — `application.rs:377`; `48bf62ed` — `epoch_manager.rs:752-757`, `outer.rs:1192-1193`, `scheme.rs:57-78`; `20f47287` — `stand.rs:1601-1610`; `dacd1bfa` — `dpos.rs:3629-3636` (кусок составного якоря); цитата документного дрейфа — `outer.rs:246`. Составные якоря вида `executor.rs:134,199,564` помечены по частям (`134→134, 199→239, 564→724`). Скрипты — `scratchpad/anchors.py` (классификация по тексту), `mark.py` (пометки, 92 ручных переопределения). §0.1–§0.9 и §1 проекта не трогались (вне задания).

## §4. Ворота (verbatim)

Дерево во время прогона: HEAD `27485df6` + незакоммиченные правки параллельной сессии R-128 (см. шапку). Ворота ждали, пока её `cargo test` завершится (правило «стенд не гонять параллельно»).

~~~
(заполняется после прогона — см. ниже)
~~~

## §5. Оставить как есть

- Оценки строк 5.1–5.4 — не менял (проект §0.4); 5.0а добавлена как 1–2 дн из той же оценки «чужой работы 4.0(в)».
- `REGISTER.md` не правил: статусов, которые проект считает иными, чем реестр, нет; есть только устаревшие указатели «по плану — Э5 5.x» с нумерацией ДО проекта (R-002 → «5.2», сейчас 5.3; R-016, R-069 → «5.3», сейчас 5.2; R-020, R-090 → «5.1/5.3», сейчас 5.1/5.2; R-026 → «5.2», сейчас 5.3) — это не статусы, оставлены до закрытия строк.
- `DECISIONS.md` не правил (владелец): Д-1 устарела («нужно до Э4.1»), Д-3/Д-6/Д-7/Д-9 — «предложено».
- Текст проекта §5/§7 не переписывал — только пометки якорей и строки `[изм. предусловия 2026-09-12: …]` (25 мест).
- R-124 в Э5 не вносил (не beacon, `staking-reader`).
- Форму нового трейта в 5.1 не проектировал — записал, что читается и что берётся из модуля; форма — работа 5.1.
- `08:`/`13:` — якоря в `.claude/dpos_architecture/` не проверял (не .rs; документы не свидетельство).

## §6. Где проверка была слабее всего

- Ворота — на дереве с чужими правками (R-128), не на чистом HEAD; чистый HEAD — по `E4-CLOSEOUT.md` §7 (реляция: lib 685/0, стенд 47/0, node 55/0, doc 6).
- 189 сдвинутых якорей — совпадение текста, не семантики; открыты руками 40 + 4 + 32 (§1 Д1).
- `deliver` после 4.2-А, `swept_to`-семантика, tee-механизм в CW — по журналам Э4 и коду HEAD без прогона.
- Оценка 5.0а — без прототипа; «все входы в стенде» — по grep и чтению `stand.rs:2156-2233`, `fakes.rs:1601-1619`, не по сборке.
- Ex-21 «плечо `CertInlet::ingest`/`ensure_key(Local)`» — `ensure_key(epoch, PinEffort::Local)` на серт стоит в `cert_inlet.rs:596` [KNOWN по grep]; путь `ingest` (`:520`) целиком не перечитывал.
- Счётчики текстовых проверок (проект / PLAN / EXPERIMENTS / этот файл): строки на «…» — 0/0/0/0; нечётное число обратных кавычек — 0/0/0/0; две запятые подряд — 0/0/0/0; пустые `` `` `` — 0/0/0/0; пустые `()` вне кода — в проекте только внутри ограждения `~~~` §5.1 (как и до прохода), в PLAN — те же три вне блока Э5 плюс `faults()` в коде; строк длиннее 500 символов — 65 / 26 / 11 / см. `awk`.
- Не открывались: `E5-DESIGN-CRITIQUE.md` §A–§D целиком (только §0), `E5-0-BOUNDARY.md` Б§0–Б§5, §1–§7 захода А, `E4-CORE-DESIGN.md` вне §5.1–§5.3, тесты beacon.

## §7. Замечено вне рамок

- `E4-CLOSEOUT.md` §6 относит R-124 к «Э5 (ET)» — ET живёт в `staking-reader`, Э5 его не трогает; правильный адрес — Э7/тикет.
- `REGISTER.md` R-127 «актор живёт по `fin + K`» — точнее «по max трёх фидеров» (`actor.rs:1160-1168`); с 5.4 (один watch) расхождение с `TrackedWindow` (по `fin`) станет `tip − fin`, не `K`.
- Комментарий `application.rs:340-345` («третий фидер … `max(fin + K, tip)`») и `node/dpos.rs:936-940` описывают три фидера — после 5.4 оба уходят; `cert_inlet.rs:258` («ONE cursor left») уже верен.
- `outer.rs:256` `EpochSchemeProvider` после 4.1 — вид на карту модуля; проект §2.6 п.6 «схемы с оракулом вне beacon» — теперь один держатель (`Committee::scheme`/`upgrade_scheme`, `committee/mod.rs:296-330`), E5-30 частично закрыт 4.1 — в реестре не отмечено.
- `EXPERIMENTS.md` §3 «Решение 09-04» всё ещё называет Ex-4/Ex-7 как потребителей «заготовки клиента плоскости» — Ex-4 (R-004) и Ex-7 (R-009) оба закрыты 4.2; заготовка потеряла обоих потребителей.
- `PLAN.md` §1 «Работает»/«Сломано» не обновлял; блок Э4 не трогал.
- Параллельная сессия R-128 держит незакоммиченные правки в `testbed/stand.rs`/`fakes.rs` — 5.0а (тоже `stand.rs`) стартовать после её коммита.

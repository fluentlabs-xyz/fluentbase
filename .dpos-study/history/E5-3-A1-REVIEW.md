# E5-3-A1 — финальное ревью захода А1 (Opus 5, свежий контекст, 2026-09-15)

База `HEAD = 60d6c3e3`, объект — незакоммиченное дерево (15 файлов под `crates/`, `git diff --stat`: 5226+/1497−). md5 на входе совпали и совпадают на выходе (`actor.rs 23beb851`, `artifact.rs 478413f0`, `share_state.rs b3585037`, `ceremony.rs 56bb0ffd`) — все мутации откачены, проверено `md5sum` после каждого отката и повторным зелёным прогоном `beacon::` (267/0). Пути без префикса — от `crates/dpos/consensus/src/beacon/`. Теги: [KNOWN] = прочитал/прогнал в этой сессии; [ГИПОТЕЗА] = вывод без прямого прогона.

## §0 — прямые ответы

**0.1 Ворота (все мои прогоны, `CARGO_BUILD_JOBS=6`, последовательно; логи `scratchpad/rev/*.txt`).** [KNOWN]
- `--lib`: `717 passed; 0 failed; 0 ignored` (174 s)
- стенд с фичей `dpos-devnet-byzantine testbed::`: `58 passed; 0 failed; 668 filtered out`
- стенд без фичи `testbed::`: `49 passed; 0 failed; 668 filtered out`
- node `--lib`: `57 passed; 0 failed`
- reader: `64 passed; 0 failed` (+ `0 passed; 1 ignored` doc-тестов)
- slasher_integration: `16 passed; 0 failed`
- clippy (3 крейта, all-targets): exit 0, 2 warning'а — оба чужие (`staking-reader/src/epoch_transition.rs:3024` `await_holding_lock`, `node/src/dpos.rs:1991` `large_enum_variant`), в 15 файлах строки — 0
- clippy с фичей: exit 0, 0 warning'ов
- fmt: exit 0 (три строки — «unstable features» rustfmt, не диагностика)
- doc: exit 0, `58 warnings` — столько же, сколько у исполнителя (`gates/r3-doc.txt`, `s1-doc.txt`: 58); ни одного в файлах строки (проверил `-->` всех 59 записей)
- harness (`make harness-test`): `2 failed, 2086 passed, 10 skipped` — те же два (`test_smoke_boundary_cases.py::test_every_grepped_token…[EL-sync fast-forwarded the anchor]`, `::test_the_two_jump_gates…`), что в базе исполнителя (2/2086/10); не про beacon.

**0.2 Автомат.** [KNOWN] Варианты `EpochState` (`actor.rs:302-362`) и переходы по коду:
| вариант | входы | выходы |
|---|---|---|
| `InTransition` | `take_state` `:1419` | `set_state`/`put_back` до конца входа; `debug_assert_settled` на всех пяти входах (`:1514,1580,2296,3455,3928`) |
| `Dealing{ceremony, agreed}` | `recover` (`:3080`: журнал Present + `h<seal`, Torn/NoFile + `h<seal`) | seal-дедлайн ⇒ `Sealed`/`Agreed` (`:2181-2218`); артефакт ⇒ `Dealing{Some}` (`:1640-1650`); второе значение ⇒ `Conflict` (`:1623-1635`, `:3687-3701`) |
| `Sealed{ceremony}` | seal (`:2215`), `recover` (`:3082`, `E>now`) | артефакт ⇒ `Agreed` (`:1651`); body-lost ⇒ `ArtifactForCeremony` (`:1488-1515`); `E≤now` на тике ⇒ `ArtifactForCeremony` (`:3623-3642`); `Conflict` |
| `Agreed{ceremony,set}` | seal с `agreed` (`:2213`), артефакт из `Sealed`/`ArtifactForCeremony` (`:1651-1666`), `recover` (`:3081`) | finalize (`:2660-2747`): `Keyed` / `Acquiring(Logs)` / `Unrecoverable`; probe не готов ⇒ остаётся (`Stalled{Quorum|BodyMissing}`); `Conflict` |
| `Acquiring(ArtifactForCeremony)` | body-lost, `E≤now`, `recover` (`:3084`) | артефакт ⇒ `Agreed` (`:1659`); `Conflict` |
| `Acquiring(ArtifactForShare)` | `recover` (`:2996`) | артефакт ⇒ `key_held_share` ⇒ `Keyed`/`Acquiring(Logs)`+`OffPolynomial` (`:1671-1682`, `:1726-1757`); комитет нечитаем ⇒ остаётся, перечитывается со стора (`:3688`) |
| `Acquiring(ArtifactForKey)` | `recover` (`:3023`) | артефакт ⇒ `KeyOnly{Some}` (`:1683`) |
| `Acquiring(Logs)` | finalize Err/refusal (`:2695-2739`), `key_held_share` (`:1741-1755`) | `try_recompute` (`:3748-3888`): `Keyed` / `Unrecoverable` (MissingPlayerDealing, off-poly при пустом `want`); `PersistFailed`/`HealFailed` ⇒ остаётся |
| `Keyed{digest}` | три сайта — см. 0.3 | `Conflict` (`:1623`, `:3689`, `:3695`); sweep |
| `KeyOnly{digest}` | `recover` (`:3012`, `:3020`), `ArtifactForKey` (`:1684`) | `Conflict`; sweep |
| `SatOut{key}` | `recover` (`:3051`, `:3060`) | ключ ⇒ `SatOut{Some}` (`:1691`); sweep |
| `Unrecoverable{key}` | `recover` (`:3041,3071,3076`), finalize (`:2720`), recompute (`:3821,3857`) | ключ ⇒ `{Some}` (`:1694`); sweep |
| `Conflict{held,second}` | `conflict()` (`:1809-1827`), `recover` (`:2966,2977`) | только sweep (`:2077-2105`) — намеренно |

Без выхода до sweep, достижимо штатно: только терминалы (намеренно) и `Acquiring(Logs)` с пустым `want`, `attempted=true`, `HealFailed` — но это записано (Д-А1-22) и у HEAD был тот же тупик (`can_finalize=false`), теперь с защёлкой. Сверка с §5.2 (`E5-BEACON-DESIGN.md:596-616`) построчно: расхождения — `Agreed ──(bodies missing)──▶ Acquiring{logs}` (в коде остаётся `Agreed` и фетчит — DA-08/09/10), `Sealed ──(committee unreadable)──▶ Acquiring` (Д-А1-3), `Finalizing` не состояние (Д-А1-8), `Idle` = нет слота (Д-А1-9), `KeyOnly` вместо «Keyed{no share}» (имя). Незаписанных расхождений не нашёл. Таблица рестарта (`:616`) — совпадает по всем девяти клеткам (`recover` `:2938-3091`), плюс две строки сверх дизайна: маркер/стор-divergent первым (`:2943-2981`) и F-02 на клетке (share, artifact) (`:2989-2995`).

**0.3 Единственный путь в `Keyed`.** [KNOWN] `git grep 'EpochState::Keyed {'` в продакшне — три сайта: `:1739` (`key_held_share`, гейт `share_on_artifact` `:1738` прямо в guard), `:2693` (finalize, через `adopt_share` `:2686` → `share_on_artifact` `:1997`), `:3882` (recompute, через `adopt_share` `:3843`). Провенанс `group_key`: во всех трёх — `AgreedSet.group_key` = `proposal.group_key` артефакта (`:263-269`), сам proposal — либо из канала (прекондиция `:868-876`: инстанс `dkg_engine.rs:385-395` резолвит тело по сертификату, seam `artifact.rs:1725-1750`/`:1041-1066` — `verify_artifact_for_epoch` до `insert`), либо из стора (`stored().held`, `:1629`, `:2982`), куда попадает только проверенное. Локальный `_out` церемонии нигде не гейт (`:2678`, `:3806`). Мутация MUT-1 (`key_held_share`: guard → `true`): красные ровно два — `a_held_share_off_the_arriving_artifacts_polynomial_is_refused_not_keyed` (`:12215`, `keyed` ≠ `acquiring_logs`) и `recover_refuses_a_reloaded_share_off_the_stored_artifacts_polynomial` (`:12273`); остальные 86 зелёные.

**0.4 `Conflict` durable и по значению.** [KNOWN] Цепь: второе значение → `ArtifactStore::insert` возвращает проигравшего под write-lock (`artifact.rs:555-564`) → `note_divergent` (`:612-641`): пара `value_digest`, first-wins под `divergent`-lock, `persist_conflict` (fsync, `share_state.rs:675-690`) ДО RAM-вставки → hand-off в актор (`:1786`, `dkg_engine.rs:466`) → `apply_artifact`/`reconcile_with_store` → `conflict()` `:1809` → `stop_signing` `:1843-1880`: `load_conflict` (уже есть — маркер стора) иначе `persist_conflict`, ЗАТЕМ `drop_share` (`:1868`, store + файл) → `recover` читает маркер первым (`:2943-2967`) или `stored.divergent` (`:2969-2981`) → `Conflict`. Точки смерти: (а) после `note_divergent` до тика — маркер есть ⇒ `Conflict` + `drop_share`; (б) между маркером и `drop_share` — то же (тест с `die_between_verdict_and_eviction` `:897`, `:1864-1867`); (в) `persist_conflict` провалился у ОБОИХ (диск) и смерть до тика — свидетель потерян, рестарт re-key'ит (ERROR + `dpos_artifact_store_conflict_marker_failed_total` `:627`, `dkg_conflict_marker_failed` `:1850`) — принятый остаток диска; (г) маркер записан, а write-behind запись САМОГО артефакта не успела — см. F-01. `value_digest` — одна функция `artifact.rs:326-339` (`epoch ‖ n ‖ (idx,hash)* ‖ encode_outcome(group_key)`), используется в акторе (`:267,1609,1630,2971,3021,3035,3686`), сторе (`:615`), мосте (лог `:1784-1785`). `resolve_artifact` (`dkg_engine.rs`) и `verify_artifact` (`artifact.rs:269-271`) сверяют `certificate.proposal.payload == proposal.digest()` — это правильно: сертификат подписан над `DkgProposal::digest` (с `confirms`), `value_digest` там неприменим; исполнитель, отвергший EA-07, прав. Маркер испорчен ⇒ `ConflictMarker::Malformed` (`share_state.rs:705-720`) ⇒ `Conflict{ZERO,ZERO}` (`:2956-2964`), стор грузит как `divergent = ZERO` (`artifact.rs:523-526`) — fail-closed, файл не удаляется. Мутация MUT-2 (`note_divergent` без `persist_conflict`): красные ровно два — `a_second_value_noted_by_the_store_is_conflict_on_a_restart_before_the_tick` (`:12673`, `None` ≠ `Some(Pair(..))`) и `artifact::tests::a_served_value_that_loses_the_insert_is_noted_durably_not_lost` (`artifact.rs:2707`).

**0.5 Стор — владелец.** [KNOWN] `reconcile_with_store` `:3674-3704` — первый шаг `drive_acquisition` `:3615`, до `entered`-перехода и до `pull` `:3653`; `on_height` вызывает его до `fetch_missing_logs` (`:3287`, `:3295`). «Актор `Keyed` на X при сторе Y»: недостижимо — каждый путь в `Keyed` стоит на `AgreedSet`, принятом `apply_artifact` только после сравнения со стором (`:1629-1635`) или взятом из стора (`:2993`); стор без вытеснения и first-wins, значит `held` не меняется после. `insert → Result`: все вызывающие обрабатывают `Err` — `dkg_engine.rs:443` (`note_divergent`), `artifact.rs:1068-1077` (`note_divergent`), `:1757-1795` (`note_divergent` + wake), тесты — `.is_ok()`/`drop(..)` (`epoch_manager.rs:2114`, `artifact.rs:1309`, `oracle.rs:534`, `actor.rs:11038`).

**0.6 `recover(E)`.** [KNOWN] Ровно раз на эпоху: `decide` `:2822` гейтит `contains_key`; вызывающие — `decide_window` `:2798` (каждый тик) и `on_artifact` `:1569` (после первого тика). Окно `decidable_epochs` `:616-621` = `[max(2, now−8), now] ∪ {now+1}`; `decidable` `:2811` — та же функция. Частичный успех: share есть, артефакта нет ⇒ `Acquiring(ArtifactForShare)` `:2996`, pull каждый тик, `Stalled{NoArtifact}` при `E≤now` `:3650`, по приходу — `key_held_share`. Резюмированная эпоха `E≤now` без артефакта ⇒ `ArtifactForCeremony`, без отправок (`:3083-3089`, `debug_assert` `:2804`). Порядок строк: маркер → стор-divergent → share → mints → member → журнал (`:2943-3078`) — как в доке 08.

**0.7 Живость.** [KNOWN] Защёлки — `EpochSlot.stalled` `:444`, `stall()` `:1456-1481` одна строка + один шаг gauge, снятие — `set_state` по `carries` `:1402-1411`, `sweep` `:2096-2098`. Спинов на тик: нет — `HealFailed` при NoFile/Torn — одно чтение файла без крипто (`:3772-3783`), `Err(other)` — `attempted` (`:3790`); off-poly при пустом `want` — терминал (`:3845-3860`); unmappable — счётчик на тик как у HEAD, строка на защёлке (`:2622-2648`); `PersistFailed` в heal — повтор на тик с ERROR из `adopt_share` `:2013` (как у HEAD, диск сломан). Body-lost — `try_send` без ретрая (`dkg_engine.rs:406-408`), актор тянет сам с границы (`:3623-3642`). Гейт `NoArtifact` для терминалов без ключа — снимается приходом ключа (`needs_artifact` `:422-432`). Замечания: F-02 (body-lost в `Dealing` теряется до границы), F-03 (двойной gauge `BodyMissing`+`QuorumMissing`).

**0.8 Д-А1-12.** [KNOWN] Live: `on_message` `:3387-3401` — `durable = append_journal(step.journal)`; `!durable` ⇒ `withhold_acks` `:2892-2898` → `DkgCeremony::withhold_ack` `ceremony.rs:668-670` (удаляет из `emitted_acks`); outgoing уходит только при `durable`. Дрейн: `decide` `:2877-2884` — тот же гейт. Ретрансмит дилера → `handle` → `try_ack` `ceremony.rs:618-657`: `dealer_message` вернёт `None` (view уже держит), кэш пуст ⇒ `Step::default()` — ack не уходит. Resume: `emitted_acks` строится только из журналированных `ReceivedDealing` (`ceremony.rs:1059-1060`), `outgoing`-acks — оттуда же (`:1065-1075`). `seal_dealings` шлёт только `Reveal` (`ceremony.rs:718-760`). Удержанный ack без durable-журнала уйти не может. Мутация MUT-3 (`withhold_ack` → no-op): красный ровно `acked_dealing_withheld_on_append_failure_still_recoverable` (`:10513`, `revealed_fail`).

**0.9 Тесты.** [KNOWN] В диффе 33 новых `fn` тестов, 4 удалены (2 из них переименованы) — 699→717 сходится. Свойства под мутациями: M1–M4 трёх кругов (журнал `:158-161`, `:392-395`) + мои шесть: MUT-1 (0.3), MUT-2 (0.4), MUT-3 (0.8), MUT-4 (`reconcile_with_store` без ветки `stored.divergent`) — красный ровно `a_divergent_value_noted_by_the_store_reaches_conflict_without_the_hand_off` (`:12594`, `keyed` ≠ `conflict`); MUT-5 (`apply_artifact` без сравнения со стором `:1629-1635`) — красный ровно `a_second_value_against_the_stores_is_a_conflict_even_when_the_actor_holds_none` (`:12521`, `acquiring_logs` ≠ `conflict` — F-02 на finalize всё равно отбил чужое значение, но `Conflict` не достигнут); MUT-6 (`recover` игнорирует маркер: `.filter(|_| false)`) — красные `a_conflict_survives_a_restart` (`:12370`, `acquiring_artifact_for_share` ≠ `conflict`), `a_death_between_the_verdict_and_the_share_eviction_restarts_as_conflict` (`:12790`), `a_malformed_conflict_marker_is_still_a_verdict` (`:12944`). **Оговорка по MUT-6**: откат MUT-5 сорвался на неуникальном якоре (`assert count==1`, второй `take_state` в `conflict()`), и MUT-6 прогнан ПОВЕРХ MUT-5 — четвёртая красная (`a_second_value_against_the_stores…`) — это остаток MUT-5; три маркерных красных — MUT-6. Оба откачены вторым скриптом, md5 сверены, `beacon::` 267/0. Фикстура нулевого окна (`at_a_zero_width_deal_window_a_missing_journal_at_the_deadline_sits_out` `:13149`) — свойство R-036 покрыто M1 круга 1. Ни один из проверенных тестов не остаётся зелёным при сломанном свойстве.

**0.10 Доки.** [KNOWN] `08_…md:1006-1089` — по коду верно по существу (варианты, таблица переходов, владение стором, marker-first), но якоря кругов 1/2 устарели: `key_held_share :1660` (код `:1726`), `share_on_artifact :1696` (`:1762`), `drop_share :1725` (`:1791`), `withhold_acks :2818` (`:2892`), `adopt_share :2481-2660` (`:1990-2041`), `artifact.rs:1640-1665` (`:1781-1790`), `plane.rs:735,873,901` (`:733,881,909`) — F-04. `00_preamble.md:5-111` — якоря третьего круга верны (`:612,511,554/555,1113,1843,3725,1428,1178,616,694`); строка `:107-111` «пять comment-only упоминаний … left for А2» устарела — дифф их уже обновил (`dkg_agree.rs:995`, `dkg_oracle.rs:17`, `log_resolver.rs:449`, `log_store.rs:51`, `node/dpos.rs:1295`). `CHANGELOG.md:19-24` — `Result<(), AgreedArtifact>` vs код `Box<AgreedArtifact>` (`artifact.rs:555`). `git grep` по удалённым носителям в `crates/` — пусто; в доках — только ретирменты в CHANGELOG `:17-23,1995` и список в preamble `:107-108` (история, не живые ссылки). 03/13 — упоминания `value_digest`/`Unrecoverable` согласуются.

**0.11 Граница и гигиена.** [KNOWN] `git status`: под `crates/` ровно 15 файлов; `#[allow]` в диффе — 0 (существующий `too_many_arguments` `:907` не новый); продакшн `expect` — было 4 сайта в HEAD (`actor_head.rs:2002` и др.), стало 1 (`:2860` «just entered Dealing», тот же сайт), `ceremony.rs:1313` — замена текста на том же сайте; `unwrap` новых нет; все `pub` внутри приватных модулей (`mod.rs:68-93`) — наружу крейта ничего нового; `#[cfg(test)]`-поле `die_between_verdict_and_eviction` `:897-898` + проверка `:1864-1867` — приемлемо (прецедент `adopted_outcomes` `:891`, в продакшне не компилируется). Мёртвый/избыточный код: `restart_replay` (`plane.rs:771,927-941`) — его push в `on_artifact` до первого тика отбрасывается (`:1559-1567`), а первый тик читает стор сам (`:2968`); остаётся работать лишь в гонке `select!` — кандидат на удаление в А2 (§2).

**0.12 Hard-stop.** [KNOWN] `DECISIONS.md` менять не нужно: пришпиливание не тронуто (`dkg_agree.rs` — комментарий), Д-6/Д-7/П-3 соблюдены (ничего на цепь, бит `changed` — `mints_at` `:3715`, гейт в `adopt_share`). BLOCKER'ов нет.

**0.13 Вердикт: КОММИТИТЬ.** Что НЕ прочитал: тесты `actor.rs:4089-13421` — только имена всех новых + тела четырёх (`:10479-10530`, `:12480-12610`) и строки паник по мутациям; `artifact.rs` — дифф + `:255-320`, `:1020-1070`, `:1690-1760`, не весь файл; `ceremony.rs` — дифф + `:600-680`, `:800-830`, `:1055-1140`; `share_state.rs`, `metrics.rs`, `dkg_engine.rs` (кроме `:380-470`), `plane.rs` (кроме `:730-790`, `:850-880`) — только дифф; `testbed/tests.rs` — дифф (комментарии); журнал `E5-3-A1.md` — только список Д-А1-nn и таблицы мутаций; dsh-отчёты — не читал (не свидетельство).

**0.14 Слабее всего (по убыванию).** 1) F-01 — окно «маркер fsync'нут, артефакт write-behind не долетел» ⇒ узел в `Conflict` без `PK_E` и без pull (byzantine-предусловие, узкое окно). 2) Д-А1-16/(в) — двойной провал записи маркера (стор + актор) и смерть до тика ⇒ re-key (принято, диск). 3) F-02 — body-lost во время `Dealing` откладывает heal до границы. 4) Тестовое покрытие `Conflict` из `KeyOnly`/`SatOut`/`Unrecoverable` (не-член) — не нашёл теста, только код `:1623-1627`/`:3689` (общий путь, [ГИПОТЕЗА] что покрыт косвенно). 5) Doc-якоря кругов 1/2 (F-04).

## §1 Находки

| id | серьёзность | file:lines | что не так | чем пытался опровергнуть | уверенность |
|---|---|---|---|---|---|
| F-01 | LOW | `artifact.rs:555-593` (insert: RAM + write-behind `durable`), `:612-641` (маркер fsync до RAM), `actor.rs:2943-2967`, `:422-432`, `:1610-1622` | Смерть после `note_divergent` (маркер на диске) и до сброса write-behind журнала артефакта ⇒ рестарт: `recover` ⇒ `Conflict{pair}`, стор без артефакта; `Conflict` не `needs_artifact()`, `apply_artifact` для него возвращает сразу, `reconcile_with_store` его пропускает ⇒ `PK_E` на узле не появится никогда ⇒ сертификаты E не проверяются, σ `Pending`, исполнение паркуется на первой высоте E. У HEAD (без durable-`Conflict`) такого состояния не было (узел re-key'ил). | Искал sync артефакта до маркера — нет: `insert` шлёт в `durable`-канал без ожидания; предусловие — ≥2q−n Byzantine + смерть в окне одной записи. Не BLOCKER: сеть уже вне модели, узел verify-only по замыслу; но «терминал без ключа» — новый тупик. Лечение — терминал `Conflict` тоже тянет артефакт при `held` отсутствующем в сторе (needs_artifact) или маркер пишется после `sync` артефакта. | средняя |
| F-02 | LOW | `actor.rs:1488-1497` (body-lost только для `Sealed`), `:3618-3642` | Сигнал body-lost, пришедший пока эпоха ещё `Dealing` (часы узла отстают, инстанс сертифицировал раньше печати), сбрасывается с debug; после печати эпоха `Sealed` до `E≤now` и только тогда тянет — heal «сразу» (R-026) откладывается до границы. Паритет с HEAD (boundary-pull), но §5.2 обещает `Sealed ──(body lost)──▶ Acquiring` без задержки — при `Dealing{agreed: None}` сигнал нечем запомнить. | Проверил, что до границы никто не тянет для `Sealed` (`needs_artifact` false, `:3643-3656`) и что артефакт может прийти иначе только hand-off'ом пира (не для члена без pull). | высокая (поведение), низкая (значимость) |
| F-03 | LOW | `actor.rs:481-490`, `:2611-2616`, `:2648` | `BodyMissing` и `QuorumMissing` оба «носятся» `Agreed`: когда все тела дошли, а кворума нет, защёлка `body_missing` остаётся (gauge `dpos_dkg_stalled{body_missing}` завышен на 1 до смены фазы). Строка — паритет с HEAD (`deferred_reported`), gauge новый. | `carries` не различает под-условия фазы; снять можно в `drive_finalization` при смене reason. | высокая |
| F-04 | MINOR (docs) | `.claude/dpos_architecture/08_…md:1020-1089`; `00_preamble.md:107-111`; `DPOS_ARCHITECTURE_CHANGELOG.md:19-24` | Устаревшие якоря кругов 1/2 (список в 0.10); preamble «left for А2: five comment-only mentions» — уже сделано в диффе; CHANGELOG `Result<(), AgreedArtifact>` vs `Box<AgreedArtifact>`. Существо описания — по коду. | Сверил каждую строку `grep -n` по коду. | высокая |

BLOCKER — нет. Пришпиливание (`AgreedSet.pinned` = `proposal.logs` `:265`, `pinned_by_dealer` `:596`, `dkg_agree.rs` без кодовых правок) — не тронуто. Доля над непроверенным полиномом — нет (0.3). `Conflict` от непроверенного значения — нет (0.4: три продюсера проверяют до `insert`). Состояние без выхода, из которого HEAD выходил — нет (0.2). Регрессия живучести стенда — нет (0.1).

## §2 Оставить как есть

- `#[cfg(test)]`-точка смерти в продакшн-типе (`actor.rs:893-898`, `:1864-1867`) — компилируется только в тестах, прецедент `adopted_outcomes`; альтернатива (трейт-хук) дороже.
- `KeyOnly{digest: None}` + артефакт в сторе ⇒ `apply_artifact` кладёт фазу обратно через `set_state` (debug-строка «transition» на тик, `:1698`, `:1709`) — недостижимо в продакшне (стор не держит артефакт carry-forward-эпохи).
- `restart_replay` (`plane.rs:771`, `artifact.rs:1438-1470`) — избыточен после DA-01 (стор читается на первом тике; push до первого тика отбрасывается `:1559-1567`), но безвреден (first-wins по значению). Удалять в А2, не здесь.
- `dpos_dkg_artifact_conflict_total` не растёт при рестарте в `Conflict` по маркеру (`:2966` без `stop_signing`) — событие уже посчитано до рестарта либо в сторе; уровень несёт `dpos_dkg_stalled{conflict}` (Д-А1-24).
- `Acquiring(Logs)` + `PersistFailed`: ERROR из `adopt_share` `:2013` на каждый тик при сломанном диске — как у HEAD; шум оправдан.
- `Stalled{HealFailed}` при пустом `want` и детерминированном `Err` — тупик до sweep, записан (Д-А1-22), у HEAD тот же (`can_finalize=false`).
- `validate_share_on_poly` (`outcome.rs:99-112`) проверяет `share.index`, а не позицию `me` в комитете — не менялось в строке, вне рамок (см. §3).

## §3 Вне рамок строки

- `outcome.rs:99-112` — гейт F-02 не привязывает `share.index` к `committee.position(me)`; файл доли с чужим индексом, лежащий на полиноме, пройдёт. Отдельный тикет/строка.
- `persist_conflict` (`share_state.rs:675-690`) пишет маркер `std::fs::write` прямо в целевой путь (без tmp+rename): полузапись ⇒ `Malformed` ⇒ fail-closed — приемлемо, но atomic-rename был бы чище; общий стиль `share_state`.
- Harness: два красных `test_smoke_boundary_cases.py` (`:491`, `:528`) — про cold-start/jump строки продукта, к beacon не относятся; в базе исполнителя те же.
- doc-warnings 58 (все чужие: `plane.rs:108-133,557-559,1091-1096`, `surface.rs`, `cold_start_jump.rs`, …) — не из этой строки.

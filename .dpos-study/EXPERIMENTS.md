# EXPERIMENTS — что доказано на стенде, что не воспроизвелось, очередь, как ставить

Состояние на 2026-09-09. Обозначение экспериментов — `Ex-n` (бывшее `Э-n`; `Э0..Э8` без дефиса — этапы плана, см. `GLOSSARY.md`). Полные постановки, логи и наблюдения — в `history/` (документ назван в каждой строке); здесь — только вердикт и следствие. Статусы записей, которые эксперименты затрагивают, — в `REGISTER.md`.

## Оглавление

- §1 Доказано на стенде (по датам)
- §2 Ставилось и не воспроизвелось; снято; стендом не разрешимо
- §3 Очередь
- §4 Как ставить (стенд, скрипты, генератор, ловушки)
- §5 Что стенд проверяет на самом деле

## §1 Доказано на стенде

### 2026-09-04 — первая волна (`history/EXPERIMENTS.md` часть 2; узел `4c885491`, образ от 08-27, 4 валидатора + full-node, interval 32)

| Ex | Что | Вердикт | Следствие для реестра |
|---|---|---|---|
| Ex-17 | Prometheus при двух реестрах метрик | подтверждено, но не тот механизм: семей-дублей нет (280 семей), дублируются СЕРИИ per-epoch simplex-движков — 223 серии до 7 раз, 55,6 % samples теряется молча при `up=1`; 8 семей без префикса `dpos_` — точно | R-085 NIT→MINOR; закрыта Э0.4 (атрибут `epoch`) |
| Ex-2 | reth: FCU `finalized = X`, которого никто не отдаёт | опровергнуто: `finalized` не двигается, ответ `SYNCING`, следующий честный FCU `VALID` через 132 мс; FCU назад на предка — `VALID`, указатель не двигается | R-001 пункт (в) снят; R-004 держится только на гейте узла (`executor.rs` `jump_done`) |
| Ex-23 | заголовок отброшенного сайдчейна разрешим по хэшу | подтверждено для ЗАПЕРСИСТИВШЕГОСЯ заголовка (≥ 20 с каноническим); `holds(l1)` подтверждает чекпойнт на неканонической ветке | R-001 L1-ветвь; правка — сверять `block_hash(number) == hash`. Побочно: `newPayload` с чужим `feeRecipient` отвергается EL (`malformed beneficiary`) — последствие R-010 пересмотрено |
| Ex-1 | reth принимает ветку с развилкой ниже локального finalized | подтверждено полностью: скачивает, канонизирует, переносит `finalized` на её голову; обратно тем же FCU. Локальный finalized — ярлык, не барьер | R-001 усилен |
| Ex-6 | `Finalization` без `Notarization` при разрыве (docker pause 3/45/120 с) | механизм подтверждён (~0,5 события на 3-с разрыв, только у паузящегося узла); удержание < 60 с всегда, `seed_hold_stalled_total` не появился; длинные простои закрываются by-height путём | R-007: режим коротких икот; > f одновременно на n=4 не отделить от потери кворума |
| Ex-11 | 20 SIGKILL валидатора без upstream в случайные моменты | 0 из 20 не поднялись; `archive_finalized ≥ last_execution_finalized_height` на каждом старте (разрыв 0–2 блока) — архив marshal durable-впереди | R-020 не воспроизведено; три точные точки AUDIT не проверены (нужен инструментированный узел) |
| — | инцидент по дороге к Ex-5: один эквивокатор на комитете из 4 | все три честных валидатора умерли за 15 с после тумбстоуна: `commitEpochCommittee(epoch)` ревёрт `0x0a87ec8d(3,4)` → `Corruption` → abort-all | заведены R-111, R-112, R-113 |
| Ex-5 | ложный SafetyHalt: спекуляция на сиблинге + отставание ≥ K (троттлинг CPU до 0.05) | не воспроизвелось: lag до 32 при K=3, 46 re-derive — 0 срабатываний; отставший узел спекулировать перестаёт | R-006 сценарий 2 не подтверждён; сценарий 1 (guard пуст на догоне) неотличим от «ловить нечего» без расходящегося блока |
| Ex-14 | граница при interval = 12 | 51 граница без потери, `pending_boundary` gauge 0; DKG при нулевом окне закрывается за 300 мс (`dkg_ceremony_ok_total = 1`) | R-046 не воспроизведено; R-024 переформулировать: «печать закрывается в тот же тик; сколько дилингов успеет — вопрос n и RTT» (Ex-12) |

### 2026-09-04 — инцидент пола R-111 (`history/EXPERIMENTS.md` часть 5; стенд `genN.py`, interval 32, activation 64)

| Прогон | Что | Вердикт |
|---|---|---|
| E1 | n=4, один эквивокатор | сеть мертва через 35,3 с после включения; честные узлы умирают с разбросом 4 мс; `docker compose start` не лечит (та же смерть каждые ~20 с) |
| E3 | n=5, один эквивокатор | жива: V 5→4 = `MIN_COMMITTEE_LENGTH`, блок 774 и дальше |
| E4 | n=5, второй эквивокатор | мертва (V 4→3), 13,3 с |
| E2 | n=4, один честный `undelegate` полного самостейка (`status 1`) | все четыре узла, включая вышедший, умерли на первом блоке эпохи `E_снятия + 1`, разброс 3,2 мс; ноль строк о тумбстоуне; рестарт не лечит |
| E5 | n=6, честные выходы | выходы V 6→5→4 безвредны (цепь живёт эпохами), выход 4→3 убивает сеть при `status 1` у транзакции; формула `T = V − 3` подтверждена на двух точках |

Вывод, который остался в силе после 1.0: порог — число селекционно-видимых ПОСЛЕ снятия (`V_после ≥ 4` живёт, `≤ 3` фатально); сериализации выходов нет (популяция может перескочить 4). `ERR_EPOCH_NOT_YET_COMMITTABLE` — недостижим (три причины, §5.5.8 там же). После 1.0 (перенос удалён) исход «отказ на полу = остановка» принят как проектный — `DECISIONS.md` Д-11/1.0.

### 2026-09-04 — Э0 и смоуки (`history/E0-LOG.md`, `history/SMOKE.md`)

- E2 и E1 переиграны на carry-over-сборке контракта: сеть жила (38 подряд перенесённых эпох). **Механизм отменён 1.0** — эти наблюдения описывают удалённый код.
- `smoke-all` (21 кейс `SUITE`): 12 PASS / 3 FAIL / 6 ERROR — три детерминированных FAIL узла и шесть ERROR окружения; что именно — `PLAN.md` §1 («Сломано и наблюдалось»); ловушки окружения — §4.
- `seed-continuity` починен (терминальный σ из сертификата): PASS, 18 контролей, 71/79 расхождений.
- `floor_case.py` 4 exit / 4 byz / 6 decay — PASS на carry-over и FAIL «как должен» на блобе `bc42042a`. Скрипт заменён `floor_halt_case.py` (1.0).
- `agreement_check.py` 12 оффлайн + 6 живых согласны; `metrics_dupcheck.py` 0 коллизий после Э0.4.

### 2026-09-08 — закрытие Э1 (`history/E1-CLOSEOUT.md` §1–§2, `history/E1-8-TESTS.md` §4; блоб `9f8e0241…`, образ пересобран)

Живые прогоны, все семь дошли до кода:

| Прогон | Вердикт |
|---|---|
| `make case-growth` (6 валидаторов) | PASS, fin 135→419, комитет 4→5→6 через две границы — первый живой прогон 1.9 (девять эпох закрылись через `assign_epoch_shares`) |
| `floor_halt_case.py 4 exit` / `4 byz` | отказ `CommitteeTooSmall(3, 4)` на всех честных узлах; все контейнеры `exited/0`; `nextEpochToCommit` застыл на 3. Отказ ловится в плане ИСПОЛНЕНИЯ (`derive.rs`, `stage="finalize"`) на всех узлах в пределах 31 мс — не при построении у лидера и не при валидации (F5) |
| `floor_halt_case.py 5 exit` / `5 byz` | цепь жива до `finalized=410`; комитет пересажен без вышедшего, `dkgQual=true` на эпохе пересадки (снято руками `cast call`); скрипт печатает FAIL по построению (F4 — `PLAN.md` §1) |
| `make smoke-byzantine` (5 узлов) | PASS: эквивокатор джейлится, транспорт рвётся, `committee[3]` пересажен без него (4 из 5 мест), финализация 162 ≥ 160 |
| претензия на живой цепи (комиссия выставлена руками 1000 = 10 %) | `claimValidatorFee` от постороннего адреса: резерв −2 BLEND, владелец +2 BLEND, баланс контракта не изменился, посторонний получил 0 — стипендия не заходит на контракт (Д-10 живьём); 1.9(г) живьём: ставка доходит до денег через три эпохи |

e2e на настоящем rWasm (`e2e/src/staking_reserve.rs`, коммиты `50e87d33`, `eb09db32`):

- X3 закрыт: токен-двойник в слоте резерва (реверт / горелка / усечённое слово) — закрытие эпохи успешно, `getEpochRewards(E) == 0`, ровно одно `EpochBlendRewardsCommitted{E, 0}`; двойник проверяет сам себя (`assert_token_refuses`); обе половины правила краснеют мутацией (`U256::MAX` вместо нуля — 4 failed; `Err` вместо нуля — 4 failed).
- F1 (бюджет): горелка съедает 29 579 516 из 30 000 000 газа системного вызова, остаток 420 484 — правило 63/64 при передаче газа в дочерний кадр, одинаково при комитете 5/21/51 (чтение резерва — последняя нога `close_epoch`). Запинено `the_fuel_burning_read_against_the_production_system_call_budget` с порогом 400 000; мутация EM3 (пять записей после чтения) роняет запас до 309 984 и валит порог.
- Форфейт при РЕАЛЬНО коротком резерве — на настоящем `universal-token`: баланс или allowance на одну единицу меньше пота ⇒ эпоха сгорает, пополнение той половины финансирует следующую, сгоревшая не воскресает (мутации EM1/EM2 красные).
- K-32 живьём: одна беспермиссионная претензия между двумя закрытиями опускает резерв под пот, закрывающаяся эпоха сгорает целиком.

### 2026-09-09 — Э2 (`history/E2-ABI.md` §9)

- `make case-growth` на общем ABI (`9b6213be`+`065003ad`): PASS, fin 132→423, 6 валидаторов; `commitEpochCommittee` (fail-loud) прошёл, `EpochCommitteeCommitted` декодировался — calldata и topic0 из общего крейта сошлись с контрактом живьём. Из трёх вьюх, переведённых на `uint64`, живой прогон покрывает 1,5 (`getEpochBlockInterval` каждую границу, `getActiveValidatorsLength` один раз на старте, `getUndelegatePeriod` узел не читает).

### 2026-09-09 — стенд в крейте, Э3.2 шаги 1/6/2 (`history/E3-2-STAND-1.md` §2–§3; коммиты `3e37d505`, `19e677cf`, `87d9e2ba`)

Не девнет — `consensus/src/testbed/`, `commonware_runtime::deterministic` + simulated p2p, фейки ниже швов крейта (derive канонизирует при derive — R-006 не проверяется), комитеты снимками, `StaticRandomness`, без upstream. Доказано на нём:
- Снятие фичи `external` даёт виртуальное время: `dkg_engine::tests` 21,4 с → 0,17 с, тот же набор тестов. Прод не менялся (tokio-`Pacer::pace` — identity).
- Общий раздел `consensus_epoch_{E}` у N узлов в одном Storage НЕ маскируется: фаза «все живы» чистая, реплей паникует в voter’е commonware `replaying notarize from another signer`. С префиксом `node{i}-` реплей поднимает всех четверых и цепь идёт дальше (6 → 12). Следствие: R-020-сценарии в стенде возможны только как «drop + replay» (in-memory Storage).
- Разрез `[0,1]|[2,3]` на пять leader-таймаутов: ни один узел не финализировал (высоты `[3,3,3,3]` на срезе и на восстановлении, по исполненному ярусу), после восстановления одна цепь.
- Расходящийся `result` на узле 2 (h=3): чужие предложения отвергнуты, `ForkSafety(ResultDivergence)` на h=6=3+K только на узле 2, честные трое в lockstep.
- seed=1 трижды — побайтно одна трасса `(height, view, leader, digest, hash)`; seed=2 — другая (ключи тоже от seed).
- Выбывший из комитета (4→3, `epoch_len=5`), оставшийся в tracked peer set, ДОГОНЯЕТ цепь без upstream (`[24,24,24,24]`, четыре границы); вне tracked set с снятыми линками — стоит на границе (`[16,16,16,4]`). Следствие для памяти `dpos-verifier-mode-is-isolated` и шага 3: upstream-плоскость нужна незарегистрированному, не выбывшему зарегистрированному.
- Эквивокатор (`dpos-devnet-byzantine`): честные трое идут, сам узел не финализирует ничего (движок подменён); улика до слэшера не доходит (`slasher_evidence: None`).

## §2 Ставилось и не воспроизвелось; снято; стендом не разрешимо

Не воспроизвелось (постановки — `history/EXPERIMENTS.md` часть 2): Ex-5 сценарий 2 (R-006), Ex-11 (R-020), Ex-14 (R-046); Ex-6 — последствие (удержание до порога re-jump) не наблюдалось.

Снято (`history/EXPERIMENTS.md` 3.1): Ex-20 — смены consensus-ключей в контракте нет (`ERR_CONSENSUS_KEYS_ALREADY_SET`); Ex-24 — анализ, не стенд (равномерность `PK_E` для потребителей `prev_randao` — вопрос спецификации); `ERR_EPOCH_NOT_YET_COMMITTABLE` — недостижим чтением; Ex-18 — угроза поглощена R-111 (остаётся как класс «verify-only до рестарта», в конце очереди); Ex-3 — больше не устанавливает достижимость, только демонстрирует end-to-end (единственный сценарий про безопасность, а не живучесть); N-2 — закрыт слиянием `f16fdd90` (дрейфа веток нет, `history/E2-ABI.md` §2.4); D-4 AUDIT-CONTRACT (точка вне кривой) — снят чтением верификатора.

Стендом не разрешимо в принципе (`history/EXPERIMENTS.md` часть 4): R-006 сценарий 1 (нужен расходящийся блок — византийский предложенец); три точных момента kill -9 R-020 (нужна синхронизация в узле); тяжесть R-007 при > f удерживающих (нужен комитет ≥ 7, где f ≥ 2); семантика `latest.primary` commonware (Ex-8 — чтение `CW:broadcast/src/buffered/engine.rs`, не стенд); Ex-24; половина R-085 про дубли семей (прод регистрирует один раз на процесс).

## §3 Очередь

Порядок — по ценности открытого вопроса к стоимости, пересчитан 09-04 (`history/EXPERIMENTS.md` 3.2) с учётом уже построенного стенда на N узлов; после 09-04 очередь не пересматривалась, кроме вычеркнутого N-2 и добавленных снизу пунктов.

| # | Что | Запись | Что нужно | Оценка (EXPERIMENTS 3.2) |
|---|---|---|---|---|
| 1 | Ex-21 — follower с upstream, отдающим сертификаты с подменённым σ до прихода `PK_E` (что видит оператор; отравление архива). Ex-6 его НЕ закрывает: там удержание снималось каналом `Notified`, а здесь σ отбрасывается как `Invalid` и по-раундового запроса больше нет (`TAG_SEED_RETIRED`) — путь выхода другой | R-008, Д-3 | `cert-mitm-proxy.py` умеет `seed-slot` (очистка); нужен третий режим — подстановка σ предыдущего сертификата | 4–8 ч |
| 2 | Ex-19 — полная смена комитета (нулевое пересечение) на 8 узлах | R-007, R-026, R-018; память проекта утверждает halt | `genN.py 8` + `cast send` ротация; стенд есть | 3–4 ч |
| 3 | Ex-22 — византийский dealer с двумя валидными `Reveal` разным пирам | R-002 (единственный сценарий про нарушение самой BFT-границы), R-036 | новый режим `--dpos.byzantine` в `crates/` (правка прод-кода) — Э3.3 | 1 д узла + 1 д стенда |
| 4 | R-006 сценарий 1 — предложенец с расходящимся `result` | R-006 | та же модификация узла | +1 д после Ex-22 |
| 5 | Ex-4 — ответ на `get_latest` высотой `ordering_finalized + 10⁶` | R-004 (гейт узла + немонотонность `upstream_frontier`) | заготовка клиента консенсусной плоскости | заготовка + 3–4 ч |
| 6 | Ex-7 — ответ на `Finalized{h}` парой высоты h−1 | R-009 | заготовка | заготовка + 3–4 ч |
| 7 | Ex-3 — ложный `Latest` ⇒ захват узла (демонстрация) | R-001, R-016 | заготовка + своя EL-ветка (`scripts/e1-up.sh`) | заготовка + 1 д |
| 8 | Ex-18 — verify-only двумя пирами реестра | R-003 | заготовка + один кадр на сабканал | заготовка + 2 ч |
| 9 | Ex-10 — голоса с растущими view от члена комитета | R-014 | модификация узла либо заготовка + кодек голоса | +4 ч |
| 10 | Ex-12 — DKG при n=51 под WAN 100–300 мс | R-024 | стенд на 51 + netem (`NET_ADMIN`) | 1–2 д + железо |
| 11 | Ex-8/9/13/16 — ресурсы при n=51 | R-013, R-037, R-038, R-067 | стенд на 51 либо bench-крейт; Ex-9 — заливающий пир | 1 д |
| 12 | Ex-15 — время первого `chain_key_epoch` на эпохе 10 000 | R-056 | ≈22 ч работы стенда | 1 д wall-clock |
| — | N-1 — умирает ли full-node вместе с валидаторами на полу | R-111 формулировка «вся сеть» | 15 мин на любом подъёме; после 1.0 — ответ уже есть частично: full-node не финализировал дальше 96 (E1-CLOSEOUT прогон 2) | 15 мин |
| — | N-3 — минимальный операторский путь возврата после остановки на полу | R-111 | 1–2 ч на собранном стенде | 1–2 ч |
| — | D-8 AUDIT-CONTRACT — поведение BLEND при отказе (`transferFrom` без allowance: пустой `Ok` или revert/`false`) | K-12 | `cast call` на стенде | минуты |
| — | стоимость `transferFrom`/`transfer` настоящего BLEND внутри контракта (`history/PUSH.md` часть 5) | push-выплата (`DECISIONS.md`) | стенд `xp4`, замер газа | часы |
| — | перенос активации governance после старта узлов | R-101 | стенд `bare` с runtime-деплоем; следствие (раскол по геометрии) сначала смоделировать | — |
| — | `smoke-all` повторно: три детерминированных FAIL 09-04 (вернувшийся валидатор не ведёт view) — на текущем и на старом бинаре | — | стенд по умолчанию; освободить порт 29100; `SOLIDITY_CONTRACTS_DIR` (§4) | ~2 ч |

Решение 09-04 (`history/EXPERIMENTS.md` 3.3): заготовку «клиент консенсусной плоскости» (1–2 дня; нужна Ex-4/Ex-7, Ex-3/Ex-18 — демонстрации) сейчас не строить — амортизируется на два сценария вместо четырёх, а два самых ценных пункта (Ex-21, Ex-19) её не требуют; строить, когда Ex-4/Ex-7 останутся последними открытыми или появится третий потребитель. Прозрачный прокси невозможен: плоскость аутентифицирована и зашифрована.

Стендовые работы плана (Э3, `PLAN.md` §2): conformance-набор для reth-трейтов (3.1), детерминированный многоузловой стенд в крейте (3.2), византийские роли (3.3), Ex-21/Ex-19 на devnet (3.4), `bootstrap.rs` на общий ABI (3.9), литералы `51` в питоне (3.10), покрытие `agreement_check.py` (3.11).

## §4 Как ставить

Стенд в крейте (не докер): `cargo test -p fluentbase-consensus --lib testbed -- --nocapture --test-threads=1` (≈13 с с компиляцией, seed/N/латентность/потери/комитеты по эпохам/партиции/роли — `testbed/stand.rs::StandConfig`, `Stand::node(i).role(..)`, `Stand::partition(..)`); эквивокатор — `--features dpos-devnet-byzantine`. Что он не показывает — `history/E3-2-STAND-1.md` §5.

Стенд: `devnet/local-dpos-smoke` (память проекта `soak-operations-index` — читать первой при работе там). Штатный compose: `docker-compose.yml` + `docker-compose.dpos.yml` — 4 валидатора + full-node, `EPOCH_BLOCK_INTERVAL=32`, `DPOS_ACTIVATION_BLOCK=64`; секвенсер → флеш → `--dpos` → сходимость за якорем (`StaticStack.bring_up_dpos`). `smoke-byzantine` с 09-07 на ПЯТИ узлах (`StaticProfile.committee()`), на четырёх тумбстоун = остановка на полу; 32 юнит-теста стенда перепинены, порт по составу узлов больше не сверяется с bash-эталоном — правки стенда 09-07/09-08 (31 файл, три untracked) на 09-09 **не закоммичены** [KNOWN: `git status`].

Стенд на N узлов (`scripts/xp/`, внесён Э0.5 `5b5e536c`): `genN.py N [byz]` пишет пару compose-файлов в `out/`, `bringupN.sh N` поднимает через ту же миграцию, `statusN.sh`, `byz_overlay.sh` (оверлей `FLUENT_DPOS_BYZANTINE=equivocate`), `inc.py N <byz-idx>` (эквивокатор + наблюдение), `exits.py`/`e2exit.sh`/`e2watch.sh` (честные выходы `undelegate`, гейт готовности `wait_dpos_ready` — без него первый прогон E5 был негоден), `engine.py` (JWT + engine-API, `XP_JWT_PATH`), `build.py` (своя EL-ветка через `forkchoiceUpdatedV3`/`getPayloadV5`/`newPayloadV4`). Make-цели: `xp-up`, `xp-status`, `xp-e5`, `xp-e2`, `xp-byz`, `xp-down` с `N=<n>`. Византийский оверлей отказывает при `N < 5`; обход — `XP_ALLOW_SMALL_BYZ=1` (при `N = 4` прогон проверяет пол, а не эквивокацию). Сценарные скрипты: `floor_halt_case.py N exit|byz [--keep-up|--reuse]` — утверждает ОТКАЗ на полу (F4 — `PLAN.md` §1); `agreement_check.py [--stand N]` — 12 оффлайн-групп согласия узел↔контракт (после 09-09 читает общие крейты; G3 падает на пустом предмете) + 6 живых; `metrics_dupcheck.py --stand N` — дубли серий на обоих реестрах. Другой набор артефактов контракта — `SMOKE_CONTRACTS_DIR` / `XP_CONTRACTS_DIR` (так проверялось «до фикса»). `cert-mitm-proxy.py` лежит в `devnet/local-dpos-smoke/scripts/` (режим `seed-slot`; `history/PLAN.md` §6 утверждал, что его нет — неверно, `history/E0-LOG.md` п.6).

Метрики: с Э0.4 все валидаторы в `docker-compose.dpos.yml` открывают реестр reth (`--metrics=0.0.0.0:9001`, проброс `1901i`) рядом с commonware (`:9100`, `1910i`); семьи `metrics::` (`reth_dpos_executor_*`, `reth_dpos_derive_*`, `reth_slasher_evidence_*`) видны только там. Prometheus v3 в той же docker-сети — конфиг в `~/Work/dpos-experiments/prom/` (вне дерева).

Контрактный блоб: `devnet/local-dpos-smoke/contracts/fluentbase_contracts_staking.{wasm,rwasm}` (gitignored), ритуал сборки и дайджесты — `devnet/local-dpos-smoke/contracts/STAKING_ARTEFACT.md` (`cargo clean -p fluentbase-contracts` перед `cargo build --release -p fluentbase-genesis --features devnet-views`, иначе e2e линкует старый wasm). Образ смоука блоб НЕ содержит — `contracts/` это bind-mount, `genesis-bootstrap` читает `.rwasm` при старте, `.wasm` ставится через `runtime-upgrade install-local`; пересобирать образ нужно только потому, что `golden.py::_image_id` — единственный ключ инвалидации голдена (F3). Совпадение блоба e2e (`target/release/build/fluentbase-genesis-*/out/`) с вендорным ничем не проверяется (F9). С 09-09 `crates/genesis/build.rs` встраивает in-tree блоб, а `bootstrap.rs` перезаписывает его вендорным — кандидат на снятие ритуала (`PLAN.md` §2 Э3).

Ловушки, наблюдённые живьём: комиссия на всех стендах 0 (F6 — `PLAN.md` §1); `DPOS_CONVERGE_S = 120` калиброван на четырёх узлах; правка `.dpos-study/*.md` сбрасывает кэш слоя `COPY` образа (в `Dockerfile` исключены только `.git`, `dist`, `devnet`) — первый `make smoke-*` после правки документа пересобирает `fluent` ~5 мин; хост-порт `29100` оверлея `docker-compose.cert-follow.yml` может быть занят (VS Code) — `smoke-cert-follow`/`-keyless` падают ERROR; prod-стек (`smoke-production-path`, `-vrf-rotation`, `-vrf-dkg-halt`, `-durability`) делает `forge create contracts/staking/mocks/MockBlendToken.sol` из соседнего checkout `solidity-contracts`, где на текущей ветке файла нет — четыре кейса ERROR, лечится `SOLIDITY_CONTRACTS_DIR` или вендоренным `MockBlendToken.json`; `.claude/session-reads/` хука попадает в bind-mount `contracts/` (F10). Юнит-набор харнесса — `make harness-test` (throwaway `python:3.12-slim`, нужна сеть для `pip install`; 2083 passed / 10 skipped на 09-04).

Временные харнессы и выгрузки первой волны — `~/Work/dpos-experiments/` (вне дерева; скрипты перенесены 0.5, `out/` не переносился).

## §5 Что стенд проверяет на самом деле

По `history/SMOKE.md` §1 (09-04), с поправками позже. Вердикт «поведение» — кейс падает при поломке названного кода; «не может упасть» — проходит при любом коде.

- 30 записей `cli.py:CASES`: 2 агрегата (`smoke-base` = tx/epoch/vrf/vrf-boundary; `smoke-fault` = deferred/peers/vrf-fault/crash-survivor/full-restart) и 28 самостоятельных; 27 — поведение. Сходимость везде — `converge.aligned_reading`: «на цепи validator-0 и выше пола», не «на одном tip».
- Не может упасть (tripwire, задокументировано): `OLD_FATAL` в `smoke-cert-catchup`, `tx-route ISOLATED` в `smoke-tx-cascade`.
- `smoke-deferred`: срез артефакта по фиксированному смещению `WIRE_HEADER_FIELDS` — с 09-04 сверяется с `order_block.rs` (`evaluate_wire_layout`), при дрейфе падает как ошибка харнесса, не как result-divergence. Свидетель дрейфа — юнит `tests/test_smoke_fault_verdicts.py`; вне `make harness-test` его не запускали [LIKELY]. Докстринг `15_smoke_cases…` сам помечает числа K/K+1 как UNVERIFIED, и кейс до них не доходит.
- `smoke-byzantine` (5 узлов с 09-07): джейл + разрыв транспорта + пересадка комитета без нарушителя через границу; на четырёх узлах — остановка на полу (её проверяет `floor_halt_case.py 4 byz`).
- `smoke-weighted-vrf` проверяет только взвешивание, не VRF (докстринг сам это говорит). `quorum` не доказывает, что узел голосует. `seed-continuity` не проверяет корректность σ как пороговой подписи и согласие лидера при медленном лидере.
- `sim` (`smoke-sim/-quick/-ram`): гейт правилами 4b/7 запрещает тумбстоун и выход ниже `min_committee` — sim по построению не заходит в путь пола.
- Единственный мок внешней системы в смоуках — `MockRollup` (cert-cascade); «литерал против своей копии» в питоне не найдено; такие пины живут в Rust-юнитах (после Э2 селекторные копии удалены; `FakeDeriver` канонизирует при derive — R-006, Э3.1).

Чего смоуки не ловят (`history/SMOKE.md` §3, обновлено): остановку на полу — теперь ловит `floor_halt_case.py` (не в `SUITE`); R-001/R-006 сц.1/R-007 — нужен византийский пир или расходящийся блок; R-020 — `smoke-crash-survivor` один SIGKILL с upstream; R-024/R-046 — нет кейса с interval < 32 в `SUITE`; R-101 — никто не переносит активацию; класс дублей — `agreement_check.py`, не смоук; стипендия/claim/`EpochBlendRewardsCommitted` — ни один смоук (`history/PUSH.md` часть 5), на стендах комиссия 0 (F6); `dpos_epoch_committee_size` — потребителя нет (R0.2).

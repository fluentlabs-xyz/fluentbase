# PLAN — план работ по crates/dpos и стейкинг-контракту (индекс)

Состояние на 2026-09-09. Это индекс, не хранилище: у каждой работы одна строка статуса; разборы — в журналах `history/` (`E0-LOG`, `E1-CLOSEOUT`, `E1-REFLECTION`, `E1-8-TESTS`, `E2-ABI`); решения — в `DECISIONS.md`; статусы находок — в `REGISTER.md`; стенд — в `EXPERIMENTS.md`; обозначения — в `GLOSSARY.md`. Что даёт `git log`, код и `.claude/dpos_architecture/`, здесь не повторяется.

Условия, под которые написан план (09-04, в силе): код нигде не задеплоен; совместимость не нужна ни обратная, ни между версиями узла; форматы провода, диска, ABI и хранилища контракта меняются свободно; сеть можно пересоздавать с генезиса; работает один человек. Оценки трудоёмкости — из исходных документов с пометкой источника; это прикидки (VERIFY-REDESIGN: сумма REDESIGN занижена на ~1,5 недели, П-8 стоит на непроверенной готовности in-process reth).

## §1 Состояние

**Дерево.** Контракт с 2026-09-09 в этом же дереве, `contracts/staking/` (слияние `f16fdd90`); worktree `~/Work/audit-482/pr482-study` — история. Правки стенда 09-07/09-08 (пять узлов `smoke-byzantine`, 32 перепиненных юнит-теста, `floor_halt_case.py`, compose/Makefile/README) **не закоммичены**: `git status` 2026-09-09 — 34 записи под `devnet/local-dpos-smoke/**` [KNOWN].

**Работает** (живые прогоны, источник и дата): devnet 4 валидатора + full-node финализирует 1 блк/с; DKG при интервале 12 и 32; 20 из 20 SIGKILL-перезапусков поднялись; 51 граница подряд (09-04, `history/EXPERIMENTS.md`); `make case-growth` — 6 валидаторов, два живых `registerValidator` с PoP через предеплои, комитет 4→5→6 (09-08 на `50e87d33`, 09-09 на общем ABI — `history/E1-CLOSEOUT.md`, `history/E2-ABI.md` §9); `smoke-byzantine` на пяти узлах PASS; `floor_halt_case.py` при N=5 — цепь жива, комитет пересажен; живая претензия `claimValidatorFee` списывает с резерва прямо владельцу (09-08); стенд в крейте (`consensus/src/testbed/`, deterministic, 09-09 — `history/E3-2-STAND-1.md` §3): N=4/8 lockstep за 0,7–2 с реальных, расходящийся deriver изолирован `SafetyHalt`’ом на одном узле, разрез 2|2 не финализирует, реплей per-node журналов поднимает всех, выбывший из комитета отслеживаемый узел догоняет без upstream.

**Сломано и наблюдалось.**
- Отказ на полу останавливает цепь: N=4 один выход или один тумбстоун — все узлы `exited/0`, ловится в плане исполнения (`derive.rs`, `stage="finalize"`) — **принято** решением 1.0 (`DECISIONS.md` Д-11), не дефект; `smoke-byzantine` держится только пятым узлом.
- `smoke-all` 09-04: три детерминированных FAIL с одной сигнатурой — вернувшийся после остановки валидатор промоутируется в Signer, но за целую эпоху не ведёт ни одного view (`smoke-rejump-signer`, `smoke-liveness`, `smoke-vrf-dkg-live-heal`); регрессия после Э0 или давняя дыра — не установлено; **с 09-04 не перезапускалось** (`history/SMOKE.md` §6.4).
- `floor_halt_case.py` при N=5 печатает FAIL на здоровой цепи по построению (ждёт `CommitteeTooSmall(4,4)`) — автосвидетеля у половины признака завершения Э1 нет (F4).
- Окружение стенда: `smoke-cert-follow`/`-keyless` (порт 29100) и четыре prod-кейса (`MockBlendToken.sol` в соседнем checkout) — ERROR 09-04; ловушки и обход — `EXPERIMENTS.md` §4.
- `cargo fmt --check` в корне красный на шести чужих файлах, красный и на HEAD (`consensus/src/{application,beacon/plane,cert_inlet,executor,spec_exec}.rs`, `genesis-bootstrap/tests/bootstrap_smoke.rs`; Ф-9, 09-09). `cargo test -p fluentbase-e2e`: девять `builtins::*` падают до и после слияния (учёт топлива, `rwasm 0.4.6`).
- На всех стендах комиссия 0 (`INIT_COMMISSION_RATE`, `commissionRate: 0`) — гейт tombstoned-комиссии и правило `min` ставки структурно ненаблюдаемы (F6).
- `agreement_check.py` после Э2 был fail-open («ok, 0 сигнатур») — переписан 09-09, G3 падает на пустом предмете; урок: работа, снимающая источник, который читает стендовый чекер, обязана чинить чекер в той же работе.
- Датчик `dpos_epoch_committee_size` без `describe_gauge!` и без потребителя, пишется и при переисполнении (R0.2). `StakingPool` стенда не умеет `withdrawDelegatorPrincipal` — тело вкладчика пула невыводимо до пересборки пула; `.sol` пула и губернатора нет ни в одном дереве (R7.11, X5).
- Dpos_architecture: правки 09-08 сняли перенос комитета и стипендиальный курсор из `00_preamble`, `06`, `07`, `08`. Остаток дрейфа (ревью 09-09 — `history/E2-ABI.md` §10; перепроверено 09-09): `15_smoke_cases…:41` и `00a_errata…:11` по-прежнему описывают `carry_committee_forward`; блок пунктов в `08_…` ниже переписанного абзаца помечен как история, не переписан (F8); якоря `consts.rs:444` в `06` больше нет — этот пункт ревью снят. Сверх ревью: `06` описывает отбор как эпохо-чистый, а кап и популяция читаются живьём (дрейф не по именам удалённых вьюх, X4); `07_…` §7.1 не знает сброса лестницы `KICK_LADDER_RESET_EPOCHS = 30`, добавленного 1.9в (`50e87d33`); `02_…` считает опрос result-gate как «40 ×», по коду проходов 41 (`for tick in 0..=polls`, R-094).

**Сломано по коду, на стенде не ставилось** — статусы и якоря в `REGISTER.md`: любой пир из реестра уводит reth валидатора на чужую цепь или замораживает исполнение (R-001, R-004, R-009); один dealer останавливает выпуск seed (R-002); буферы тел и `VoteStore` не ограничены (R-013, R-014); две группы дублей на границе с commonware держатся вниманием (R-115, R-116); поверхность контракта, которую зовут только его тесты, — не меньше 11 точек из 66 (R5.6, не пересчитывалось).

**Не проверено ничем, кроме чтения.** R-006 сценарий 1 (guard #2 пуст на догоне); R-007 при > f удерживающих; R-020 в трёх точных точках; все ресурсные находки при n = 51; поведение BLEND при отказе (K-12); ~41 тыс. строк тестовых модулей узла не читались (известный класс — `FakeDeriver` канонизирует при derive); `evidence.rs` (744 строки + 21 тест) не читался после аудита; достижимость арма `snapshot_payout` при `selection_total == 0` (R9.1); согласие двух реализаций BLS12-381 (arkworks на Fluent, blst у узла) проверено на векторах, теста в дереве нет; отрицательные ветки BLS (`PrecompileFailed`, `InfinityPoint`) — только мок PAIRING и заглушки MODEXP/MAP/G1ADD, на rWasm покрыты только положительные векторы и один поддельный PoP; узел и контракт сходятся по namespace только пока он короче 128 байт (LEB128 против полного varint); сегодня 23–32, теста нет; `evidence.msg1 == proposal.encode()` побайтно; статичность семи `static_call` в юнит-хосте (мок, мутирующий Rust-состояние, невидим); ширина `uint64` трёх вьюх живьём — 1,5 из 3; мутации трёх новых тестов Э2 и группы 7 (`StorageUint112`) не делались; 74 теста контракта ни разу не краснели — не измерены, не доказаны избыточными; форфейт при реально коротком резерве — только e2e, не девнет; 1.9 (а), (б), (в) на живой цепи не наблюдались; `make harness-test` и `cargo test -p fluentbase-node` в дереве B после правок доков 09-08 не перезапускались; арифметика Д-13 второй сессией не пересчитывалась; ~`epoch_transition` и `weighted_vrf` конфигурации из `history/COVERAGE.md` §5 (десять слабых мест аудита) — как были.

**Ворота** (дата замера, источник):
| Ворота | Результат | Дата / источник |
|---|---|---|
| `cargo test` из `contracts/staking` | 175/0; 176/0 с `--features devnet-views` | 09-09, `history/E2-ABI.md` §9 |
| `cargo test -p fluentbase-node` | 57/0 (59 до удаления двух селекторных тестов-копий) | 09-09, там же |
| `cargo test -p fluentbase-staking-reader` | 59/0 (было 63: −4 теста эпохи, −2 копии, +2) | 09-09, там же |
| `cargo test -p fluentbase-consensus` | 636 + 3 + 5 + 13 / 0 (2 ignored: doc-тест и conformance); с `--features dpos-devnet-byzantine` стенд **35/0/0**; `clippy --all-targets -- -D warnings` чист с фичей и без; lib 22 с, стенд 188 с | 09-10 после закрытия Э3, `history/E3-CLOSEOUT.md` §7 |
| `cargo test -p fluentbase-p2p -p fluentbase-bls` | зелёные | 09-09, там же |
| `cargo test -p fluentbase-e2e --release` | 113 / 9 / 9 ignored; девять `builtins::*` — до слияния тоже; все `staking*` зелёные (13/0 по фильтру) | 09-10 после 3.9, `history/E3-CLOSEOUT.md` §2 |
| `cargo test -p fluentbase-testing` | 6/0 | 09-08, `history/E1-8-TESTS.md` |
| `cargo check --workspace`; clippy затронутых крейтов; контракт clippy/fmt | чисто; после Э3.2 шагов 1/6/2 — чисто, `--all-targets` тоже (контр-ревью) | 09-09, `history/E2-ABI.md` §9; `history/E3-2-STAND-1.md` §4, §10 |
| `cargo fmt --check` (корень) | красный на 6 чужих файлах (см. выше) | 09-09 |
| селекторный скан блоба; `agreement_check.py` | OK; 12 checks, 0 disagree | 09-09 |
| `make case-growth` | PASS, fin 132→423 | 09-09 |
| `make harness-test` | 2083 passed / 10 skipped | 09-04, `history/SMOKE.md` §6 |
| мутации контракта (1.8) | 97 мутаций; выжившие M1, M23/M23b/M23c, M24, M62, M63, C5, M97–M100 — избыточные копии или недостижимая ветка, не дыры | 09-08, `history/E1-8-TESTS.md` §2 |

## §2 Порядок

Порядок задан зависимостями, не темами (09-04, в силе): (1) контракт раньше узла — форма записи комитета (П-1), общий ABI и правило `MIN_COMMITTEE_LENGTH` зависят от того, каким контракт станет; (2) общий источник величин раньше любой правки узла, читающей контракт; (3) стенд раньше executor и раньше доказательства П-2/П-4 (R-006 показал: фейк-оракул делает тесты зелёными на ложной семантике); (4) внутри узла П-1 → П-4 → П-5; П-3 → П-9; П-2 → П-6. Нельзя вести одновременно: П-2 и П-6 (один `try_derive`), П-3 и П-9 (один `beacon/actor.rs`), П-1 и CB-11 (П-1 делает CB-11 ненужным). Всё про поэтапный выкат, окна «временно менее безопасно», миграцию диска и координированный wire-релиз из порядка исключено (§7).

Коробки: `[x]` сделано, `[~]` решение принято или часть кода есть, `[ ]` открыто, `[-]` отменено. Коробки Э0–Э2 выставлены по коду и прогонам, названным в журналах; Э3–Э8 против дерева не сверялись — `[ ]` по умолчанию.

### Э0. Разблокировать рабочее место — закрыт 2026-09-04 (журнал `history/E0-LOG.md`)
| # | Работа | Статус |
|---|---|---|
| [x] 0.1 | явный список четырёх крейтов вместо глоба `crates/dpos/*` | сделано — `997152ed`; тот же глоб у `bins/*` несёт ту же ловушку (§6) |
| [-] 0.2 | carry-over вместо ревёрта (КB-1) | отменено 09-07 работой 1.0; было сделано `b22a8ed1`/`50ed414d`, откачено `c31c258f`/`802f0b8d` |
| [x] 0.3 | R-010: `fee_recipient` удалён из `OrderBlock` | сделано — `30e2dd68` |
| [x] 0.4 | метрики: реестр reth в compose, атрибут `epoch` на per-epoch движках (Д-8) | сделано — `2cc13bcd`; единого реестра нет (R-070 наполовину) |
| [x] 0.5 | стенд на N узлов в дереве (`scripts/xp/`) | сделано — `5b5e536c` |

### Э1. Контракт — закрыт 2026-09-08 с открытыми 1.2, 1.3, 1.4, 1.6, 1.8 (журналы `history/E1-CLOSEOUT.md`, `history/E1-REFLECTION.md`, `history/E1-8-TESTS.md`)
Признак завершения (переписан 09-08 вслед за 1.0): `cargo test` контракта зелёный; блоб пересобран и совпадает с `STAKING_ARTEFACT.md` и деревом; devnet поднимается; E1/E2/E5 при n=5 живут, при n=4 останавливаются отказом; Д-4 принято и реализовано; перенос удалён; датчик `dpos_epoch_committee_size` есть. На 09-09 не выполнено: Д-4 (1.2), автосвидетель «при n=5 живут» (F4). Точка «до Э1» — `bc42042a`, не `29ae97ef`. Продакшн-код контракта не уменьшился (7 405 → 7 464 строк), проще стала только консенсусная сторона; денежная — сложна иначе (`history/E1-REFLECTION.md`, ответы).
| # | Работа | Статус |
|---|---|---|
| [x] 1.0 | перенос комитета удалён, ниже пола — отказ; `smoke-byzantine` на пяти узлах; `floor_halt_case.py` | сделано — `c31c258f` (контракт), `802f0b8d` (узел); решение — `DECISIONS.md` Д-11; остатки: остановка на ближайшем коммите (R0.1), датчик без потребителя (R0.2), F4 |
| [x] 1.1 | КB-2: один алгоритм отбора по текущему состоянию, фильтр до среза | сделано — `c31c258f`; газ закрытия 4 600 → 0, коммита 12 800 → 17 100 (осознанно); K-7 НЕ закрыта, K-17 частично; остатки R1.1–R1.7 (`history/E1-REFLECTION.md` §1) |
| [~] 1.2 | Д-4 = КB-4: один системный маршрут слэшинга, удалить `evidence.rs` и WAL-маршрут узла | открыто — Д-4 принято 09-04, под вопросом с 09-07, не реализовано (`DECISIONS.md` Д-4) |
| [~] 1.3 | таймлок 7 эпох на `setSlashFundAddress`, `setBlendReserve` | открыто — решено 09-04, не реализовано (`DECISIONS.md` §3) |
| [ ] 1.4 | КB-5: самостейк владельца как обычная делегация с правилом выхода | открыто (K-4, K-5) |
| [x] 1.5 | КB-6: удалить поверхность без потребителя | сделано — `2fa46f1b` (11 из 15 удалены; `a42fba12` — только запись в `STAKING_ARTEFACT.md`; оставлены `getValidators`, `getValidatorStatus`, `getEpochRewards`, `isValidatorActive`); столбец «Закрывает» неверен для K-18, K-21, K-28, K-32 (отчёт §4); остатки R5.1–R5.8 |
| [~] 1.6 | точечно: cap ≥ 4 в `validate_initialization`, отказ фонда, `ensure_initialized` на конфиг-вьюхах, строгий ABI-декодер SDK | открыто — гейт cap есть только в сеттере; `ensure_initialized` 0 в `config.rs` (09-08) |
| [x] 1.7 | Д-10: стипендия не входит в баланс контракта; форфейт; раздел претензии и вывода тела | сделано — `100c02c4` (контракт), `802f0b8d` (узел); X3 доказан на rWasm 09-08 (`50e87d33`); остатки R7.1–R7.12, цена — `DECISIONS.md` Д-10 |
| [~] 1.8 | Д-12: тесты контракта краснеют при снятии охраняемой проверки | сделано в объёме «деньги/личность» — `eb09db32` (97 мутаций, 7 правок, 2 e2e); открыто: 12 тестов маршрута улик (ждут Д-4), одноцветные фикстуры трёх тестов личности (F-1), `install_stipend_token` без баланса контракта (F-3), 74 теста без замера, арифметика не мутировалась (`history/E1-8-TESTS.md` §7–§8) |
| [x] 1.9 | четыре политики наград (K-10б, K-8, K-24, ставка) | сделано — `50e87d33`; открыто Д-13; R9.1; X1; на стендах ненаблюдаемо (F6) |
| [x] 1.10 | BLS-верификатор инлайн, внешний адрес удалён | сделано — `f70ceffe`; исходник — `solidity-contracts@f641789f` (`history/BLS_VERIFY_PORT_SPEC.md`); топливо 606 273 → 491 418 (регистрация), 605 524 → 372 655 (улика); остаток R10.1–R10.3 |

### Э2. Общий источник для величин на границе узел↔контракт — 2.1, 2.2 закрыты 2026-09-09; 2.3, 2.4 открыты (журнал `history/E2-ABI.md`)
| # | Работа | Статус |
|---|---|---|
| [x] 2.1 | общий `sol!` — крейт `crates/staking-abi`; узел удалил четыре своих; тесты «копия против копии» удалены | сделано — `9b6213be` + `065003ad`, ревью `0972059d`; группа 4 держится тестом, не компилятором |
| [x] 2.2 | числовые пределы и две функции — `crates/types/src/staking_protocol.rs`; одно имя `MAX_COMMITTEE_SIZE` | сделано — те же коммиты; R-102 частично (арм `activation == 0` у контракта осознанно); третья копия формулы в `epocher.rs` найдена ревью и переведена |
| [ ] 2.3 | namespace подписи и порядок комитета (группы 1, 2): тест узла против пиннутого commonware, контракт получает вектор от узла | открыто — зависит от 1.2/Д-4 (группа 1 исчезает с `evidence.rs`); группа 2 остаётся в любом случае (R-115, R-116) |
| [ ] 2.4 | B-9/П-10: `ProtocolParams` в chainspec, сеттеры геометрии удалить, `freeze_or_warn`/`read_geometry` уходят | открыто (R-011, R-101, R-024, R-062) |
После Э2: любое переименование в контракте — ошибка компиляции узла. Слияние деревьев — `DECISIONS.md` §3. Побочно: `crates/genesis/build.rs` встраивает in-tree блоб, а `genesis-bootstrap` перезаписывает его вендорным — кандидат на снятие ритуала `STAKING_ARTEFACT.md` (F3/F9), не сделано.

### Э3. Стенд — закрыт 2026-09-10 (журнал `history/E3-CLOSEOUT.md`; 3.4 отложено до после Э4/Э5, 3.1 в урезанной форме) — независимая оценка закрытия: `history/E3-REVIEW.md` (Fable 5.1, свежая сессия, 09-10): «да с оговорками»; оговорки вынесены в 4.0
| # | Работа | Статус / что становится проверяемым |
|---|---|---|
| [x] 3.1 | conformance-набор для `BeaconEngineLike`/`ExecutedChain`/`DerivedBlockBuilder` против `FakeChain` и in-process reth | **урезано и сделано в урезанной форме 2026-09-10** (`history/E3-CLOSEOUT.md` §3; контрагент Opus 5: 15 находок, 13 в код — fail-closed канонизация с `Syncing`, `holds` по канону, паники вместо пропусков, INVALID/`invalid_state` в блоке расхождений, лог `Derived`/`Canonicalized` в тесте): полный conformance против in-process reth (3–4 нед., `reth-e2e-test-utils` не подключён) не строится — вместо него `FakeChain` стенда получил семантику reth там, где читают гварды executor'а: исполненные блоки в дереве по хэшу (`InsertExecutedBlock`, уже известный = no-op), канон только по FCU (`FakeBeacon` делит цепь с `FakeChain`, reorg к точке развилки), `spec_executed_hash` = канон; остальные расхождения объявлены одним блоком «Divergences from reth» на `FakeChain` с якорями `RETH_INTERNALS.md` (4 смоделировано / 8 объявлено). Ни один из 30 тестов не опирался на раннюю канонизацию. Новый тест воспроизвёл R-006 сценарий 1: guard #2 на догоне читает высоту до её FCU ⇒ `None` ⇒ молчит; halt — обратной проверкой `h − K` на h+K (`executor.rs:3247`), когда h..h+2 уже FCU'нуты. Полный conformance — если Э4–Э6 упрутся в ложь фейка второй раз. Остаток: `executor.rs:7448` `guard2_convergence_mismatch_engages_safety_halt` по-прежнему зелёный на собственной фикстуре с ранней канонизацией (прод-тест, Э6 П-6) |
| [x] 3.2 | детерминированный многоузловой стенд в крейте (`commonware_runtime::deterministic` + simulated p2p) | сделано 2026-09-09; разведка 2026-09-09 — `history/E3-2-STAND-RESEARCH.md`; шаги 1, 6, 2 сделаны 2026-09-09 — `87d9e2ba`, `3e37d505`, `19e677cf` (стенд `consensus/src/testbed/`, 9 тестов + 1 `#[ignore]` + 1 за фичей `dpos-devnet-byzantine`; `external` снят, dkg-тесты 21,4 с → 0,17 с; префикс разделов по узлу); шаг 3 сделан 2026-09-09 — `68e7f7f3` (таймаут frontier-fetch по `Clock`), `69a14556` (upstream-плоскость в стенде: 14 тестов + 1 за фичей, 0 `#[ignore]`; `first_divergence` детерминирован); шаг 4 сделан 2026-09-09 — `40fe8f3e` (префикс key/seed/artifact-журналов плоскости по узлу), `08870d69` (живой DKG в стенде: `Beacon::Live`, 20 тестов + 1 за фичей; B1–B5 + негативный контроль), `a860e588` (поправки контр-ревью); шаг 5 сделан 2026-09-09 — `ad9ccd02` (границы стенда идут через настоящий `EpochTransition` над `fakes::FakeStaking` с контрактным правилом высоты коммита; `cold_start` считает эпоху сам; `dkg_qual_at` — реальный финализированный хэш; `geometry` — `frozen_geometry()`; смоделированы оба прод-выхода отставшего узла — tee живого фронтира и steady-state re-jump; +5 тестов: C2, C3+C5, C4, C7 — ловушка (7) воспроизведена впервые, C8 — «до» для дыры в будильниках ремонтного sweep'а), `3b23ffbb` (`docs(staking-reader)`: комментарий про ahead-commit врал про удалённое расписание `H_qual`), `e8645d0a`, `a07520ac` (правки по контр-ревью: ярус комитетных чтений опущен до `tip − K` — прод читает на `result_final`, а не на ордер-курсоре). Прод-правок кода ноль. **3.2 закрыта: 6 шагов из 6, ворота зелёные** (стенд 26/0, крейт 634+3+5+13/0) — `history/E3-2-STAND-4.md` |
| [x] 3.3 | византийские роли: dealer с двумя `Reveal`, предложенец с расходящимся `result`, ложный `Latest`, пара h−1 на `Finalized{h}`, вздутая проба | **сделано 2026-09-10** (`history/E3-CLOSEOUT.md` §1, §4, §5; сессия A 2026-09-09 — `history/E3-3-ROLES-1.md`). Сначала прод-прыжок в стенде: `ReJumpFn` = `cold_start_jump_with_threshold` над `JumpCommittees` (комитет по хэшу посадки из `FakeStaking`) и `JumpElSync` (`land_jump` по цепочке родителей в `ElNetwork` по хэшу, `sync_to` в прод-порядке), исходы прыжка в `Outcome.jump_calls`. Роли и вердикты «до правки»: R-002 — воспроизведено (сессия A); R-008 — воспроизведено с отравлением архива (сессия A); R-006 сценарий 1 — воспроизведено (3.1); R-009 — воспроизведено (`WrongHeightFinalized`: 42 пары чужой высоты приняты, дыра не закрылась, 5 против 15 у контроля); R-004 — частично (`InflatedProbe`: раздутие до `real+10^6` и `Stalled` на каждом прыжке против честного контроля; механизм записи с 300 с блокировки derive не показан, гейт клина — `awaiting_seed`); R-001 вариант Б — частично (`LyingUpstream`: посадка на дивергентную ветку и `AuthFailed` на BLS-плече; проход проверки на подменённом состоянии недостижим — состав комитета фейка не зависит от хэша; вариант А требует per-hash комитета и переподписи). Здоровая жертва R-004 недостижима в детерминированном рантайме (проба идёт в ногу с блоком). Каждая роль самопроверяет подмену; каждый тест с честным контролем; контр-ревью Opus 5 на каждый шаг (V1 20, V2 19, V2b 20, V3 — минорные). Прод-правок кода ноль (одна видимость константы + правка прод-комментария про эпоху посадки). Стенд 35/0/0 |
| [ ] 3.4 | devnet: Ex-21 (follower с прокси, подменяющим σ) и Ex-19 (полная смена комитета на 8 узлах) | **отложено до после Э4/Э5 (П-2/П-4)** — решение 2026-09-10 по контр-ревью (`history/E3-CLOSEOUT.md` §6): механизмы закреплены стендом (R-008 — тест подделанного σ, `history/E3-3-ROLES-1.md`; нулевое пересечение — C7/R-121, `history/E3-2-STAND-4.md`), но стенд НЕ покрывает: плечо `CertInlet::ingest`/`ensure_key(Local)` (стенд без inlet'а), `record_data_fault`/ротацию upstream'а, «другие видят data fault» (не достигнуто), настоящий re-jump за reth EL sync, on-chain ротацию через контракт и пол `MIN_COMMITTEE_LENGTH`, R-007 при > f, R-026, R-018. Девнет — приёмка после правок, не «до» |
| [x] 3.9 | `genesis-bootstrap/src/bootstrap.rs` берёт `initialize`/`commitEpochCommittee` из `fluentbase-staking-abi`; `initialize` в общий крейт; четыре копии в `e2e/src/staking*.rs` | сделано 2026-09-10 (`history/E3-CLOSEOUT.md` §2; разблокировано коммитом `26d291b7`): общий `sol!` вырос с 13 до 24 деклараций (`initialize` в порядке `InitializeCommand`, `getConsensusKeys`, и по контр-ревью ещё девять с внешними вызывающими — `registerValidator`, `delegate`, `undelegate`, `blocksInEpoch`, `getEpochRewards`, `claimValidatorFee`, `setProductionLivenessDisabled`, `setBlendStipendPerEpoch`, `setBlendReserve`); `consts.rs` выводит все 24 через `sig::<abi::…>()`, строки удалены; копий в `bootstrap.rs`, `bootstrap_smoke.rs` и четырёх e2e-файлах ноль (`staking_bls.rs` без `sol!` вовсе); правило записано в шапки обоих файлов. Calldata `initialize` побайтно та же (контрагент переустановил независимо), селекторы не сдвинулись, блоб не перегенерирован (G3: 24 декларации, каждая ровно раз в блобе). Ворота: contract 175/0 и 176/0, e2e `--release` 113/9/9 (девять `builtins::*` — как до), genesis-bootstrap 6+8/0, clippy `--all-targets` (e2e-модули за `#[cfg(test)]`, `cargo check` их не компилирует), rustfmt ок. Контрагент Opus 5: 13 находок, 9 в код |
| [x] 3.10 | питоновские литералы `51` (`compose_gen.py`, `floor_halt_case.py`, `seed_continuity.py`) — один источник или чек в `agreement_check.py` | сделано 2026-09-10 (`history/E3-CLOSEOUT.md` §2б): литерал остался только в `compose_gen.py` (в двух других его не было и на HEAD — посылка ячейки была неверна) → именованная `MAX_COMMITTEE_SIZE` с якорем `staking_protocol.rs:38`; `agreement_check.py` G8 сверяет её с Rust; pytest `test_protocol_constant_transcriptions.py` пинует её и `MIN_COMMITTEE_LENGTH` из `floor_halt_case.py` против `staking_protocol.rs`. Контрагент Opus 5: 13 находок, 10 в код |
| [x] 3.11 | `agreement_check.py`: форма возврата `getRegistryWithKeys`, ширины возврата трёх chain-config вьюх, селекторы, которые зовёт стенд | сделано 2026-09-10 (там же): G4b — арность `getRegistryWithKeys` через делегирование в `write_validators_with_keys` (неоднозначность = отказ); G15 — три вьюхи `getEpochBlockInterval`/`getDposActivationBlock`/`getActiveValidatorsLength` выводятся из общего крейта, каждая обязана иметь обработчик в `config.rs`, декодирование харнесса скорится (шесть мест `(uint32)` → `(uint64)`); G16 — каждая сигнатура/селектор харнесса (AST + `*.sh`, 113 мест) против общего крейта, иначе `consts.rs`, иначе FAIL. Чекер 15/0/0. Не покрыто: f-string/конкатенация сигнатур, типы ног (только арность) |
Также: снять ритуал `STAKING_ARTEFACT.md` (F3/F9); параметризовать хост-порт 29100; `SOLIDITY_CONTRACTS_DIR` или вендоренный `MockBlendToken.json` для prod-стека; `floor_halt_case.py` при N=5 — режим «ожидать выживание» (F4); закоммитить правки стенда 09-07/09-08.

### Э4. Ядро узла: комитет как значение, один фронтир, членство на границе — открыт
| # | Работа | Закрывает / смягчает | Нужно до | Оценка |
|---|---|---|---|---|
| [ ] 4.0 | предусловия к 4.1/4.2 из оценки стенда (`history/E3-REVIEW.md` §4, §9; мутации прод-кода 09-10 — `history/E3-CLOSEOUT.md` §8): (а) вариант А R-001 — `FakeStaking` с составом по ветке хэша + переподпись роли (`build_signer`/`assemble` как в `WithholdingRandomness`), тест «аутентификация ПРОХОДИТ на подменённом состоянии» — единственный red→green свидетель для «комитет из локального finalized»; (б) двухисточниковые фикстуры R-009/R-004 (`upstream_only_link` → список пар): после П-4 жертва должна отвергнуть лжеца И догнать от честного — сейчас `heights == 95/5` останутся зелёными; (в) стендовый `CertInlet` в `build_node` (`cert_inlet.rs:110-114`, generic по `CommitteeSource`/`Randomness`) — без него R-008 «до» для Д-3 и R-004 «до» для П-4 проверяют не тот вход, что прод; (г) роль «эпоха раунда ≠ `epoch_of(height)`» на `Latest`/`Finalized{h}` (`cold_start_jump.rs:683` не сверяет); (д) фикстура с `l1_checkpoint` для `holds` (Ex-23) и тест R-003 `corroborate_frontier` — в `testbed/` не упоминаются; (е) до перестановки `sync_to`: либо одноузловой reth-conformance (`RethImporter` → FCU → `block_hash`, 5 сценариев), либо живой прогон трёх исходов `sync_to` — `JumpElSync` несёт `[ГИПОТЕЗА]` в коде (`fakes.rs:1280-1285`). Ярлыки веток R-008/R-001 («PATH NOT REACHED») переписать под «после» до начала 4.2 | П-4, П-1 (свидетели) | Э3 | (а) ~2 дн., (б) ~1 дн., (в) 1–2 дн., (г)–(д) ~1 дн., (е) ~2 нед. или девнет-прогон; всё — [ГИПОТЕЗА] оценки |
| [ ] 4.1 | П-1 в форме по Д-1 (`DECISIONS.md`) | R-027, R-040, R-019, R-076 / R-033, R-035, R-045, R-075, R-063 | Э2, Д-1, стенд 3.2 | REDESIGN 3–4 нед. |
| [ ] 4.2 | П-4: `Frontier`, продвигаемый только проверенной финализацией; аутентификация до `sync_to`; `holds(l1)` через `block_hash(number) == hash` (Ex-23); следствие — Д-2 | R-001, R-003, R-004, R-009, R-040, R-096, CB-12, CB-13 / R-016, R-095 | 4.1 | 2–3 нед. |
| [ ] 4.3 | П-5: `TrackedPeers{primary: committee[e] ∪ committee[e+1], secondary: registry}`, `Ingress::Member`/`Tracked`, свой бан по каналу; сначала прочитать `CW:p2p/.../tracker/directory.rs` | R-054, R-029, R-096 / R-013, R-037, R-023 | 4.1 | 2 нед. |
| [ ] 4.4 | точечно: `deque_size ≤ 4` + байтовый лимит BROADCAST; `deque_size = 1..2` agreement bodies; `VoteStore` после batch-verify и ≤ `floor + activity_timeout`; фильтр `target_epoch ∈ [now, now+2]` в `on_confirm` | R-013, R-037, R-014, R-023 | 4.3 | дни |

### Э5. Beacon: одна граница, один владелец у каждого факта, DKG-автомат — открыт (проект: `history/E5-BEACON-DESIGN.md`, контр-рецензия: `history/E5-DESIGN-CRITIQUE.md`, сверка §10)
| # | Работа | Закрывает / смягчает | Нужно до | Оценка |
|---|---|---|---|---|
| [ ] 5.0 | Граница: трейт `Beacon` из 9 методов + `subscribe` + `faults` (вместо 15), `BeaconInputs` (`CommitteeReads`, `clock`, `geometry: watch`, `ArtifactUpstream`), `observe_certificate` с синхронным вердиктом, `can_participate` рядом с `signer`, события как пробуждения (`Lagged` = пробуждение); потребители переведены (spec_exec, cert_inlet, executor, epoch_manager, crash-replay); ТЕСТОВЫЕ реализации трейта переписаны (`StaticRandomness`, `Absent`, `Canned`, `WithholdingRandomness` стенда — `stand.rs:1782-1788`, `byzantine_roles.rs:351-370`); внутренние модули приватные (`mod x;`), `for_keys`/`for_seeds` удалены; `prev_randao`/`observe_frontier` в границу не вводятся; состояние `Unfrozen` вместо `geometry None ⇒ Err` | E5-01, E5-02, E5-20, E5-34, E5-44, E5-46 / R-069 (половина) | ничего (до 4.1) | 1–1,5 нед [ГИПОТЕЗА] (было 3–4 дн; добавлен шов стенда) |
| [ ] 5.1 | Факт PK_E: артефакт — единственный владелец; удалить `keys.rs`, `key_journal.rs`, `carry.rs` (memo и его правила остаются), `resolve.rs`, W1/W3, копию артефакта в share-файле и `Output` в `CeremonyStore`; `Sharing` из `artifact.group_key`; `validate_share_on_poly` перед любым `adopt_share`; приобретение артефакта для не-члена живой эпохи и follower одним кодом (артефактная половина `follower.rs` уходит здесь; throttle fetch-а сохраняется); persist: share ⇒ отказ, артефакт ⇒ RAM + повтор, оба с `Stalled{PersistFailed}`; ЧАСТИЧНЫЙ УСПЕХ (share на диске, артефакт нет) ⇒ `Acquiring{artifact}` — названное изменение живучести (§5.4); одна ретенция `SCHEME_RETENTION_EPOCHS`; Д-7 = читать бит НА КУРСОРЕ `max(fin, live)` (`node/dpos.rs:1382-1404`), не на finalized-хэше; `signer` гейтится `mandatory_at` явно (замена `Some(None)` из `carry.rs:118-129`) | R-017, R-025, R-039, R-053, R-056, R-068, R-090, R-052, R-021, R-121, R-122, BB-3, BB-6, BB-12, B-7 / R-012, R-020, E5-07, E5-08, E5-09, E5-13, E5-18, E5-26, E5-29, E5-31 | 5.0 | 2–3 нед [ГИПОТЕЗА] |
| [ ] 5.2 | Факт σ: `SeedIndex` — RAM-индекс, синхронный `seed`, журнал только для рестарта — единственный владелец; ретенция `SEED_RETENTION` + исключение терминального раунда как правило; карантин как состояние `Pending`; вердикт внутри `observe_certificate` (синхронный `Refused`) + поздний `DataFault` через `faults()` (Д-3 = вариант (в)), дедуп ERROR-строки защёлкой на эпоху; удалить промоутер-задачу, `waiters`, terminal-карту как отдельную карту, `on_invalid_seed`, seed-половину и сам файл `follower.rs`; crash-replay через `observe_certificate` (`Pending ⇒ Defer`, возобновление по `KeyAvailable`); `hint_finalized` как выход из hold не вводить; затем R-016 порог абсолютный. ОСТАТОК: три транзиентные копии σ в executor (`executor.rs:134,199,564`) до 6.1 | R-007 (частично), R-008 (Д-3), R-069, R-086, R-088, BB-1, BB-7, BB-11, B-6 / R-018 (событие `SeedRecorded` для epoch_manager), R-016, E5-03, E5-10, E5-28, E5-42, E5-45 (мьютексы σ) | 5.0, Ex-7 (в очереди, `REGISTER.md:109`), PLAN 4.0(в) — стендовый `CertInlet` для проверки `DataFault`-ротации; Ex-21 — девнет-приёмка после | 1,5–2 нед [ГИПОТЕЗА] + 1–2 дн чужой работы 4.0(в) |
| [ ] 5.3 | DKG-автомат (П-9): идентичность лога `(dealer, hash)`, refetch по хэшу, две подписи одного дилера — локальный бан + сохранённая пара; явные состояния и терминалы (`Unfrozen`, `SatOut`, `Unrecoverable`, `Conflict` с суженными триггерами, `Stalled{reason}`); входы автомата — `clock` + артефакт + p2p + resolver; body-lost ⇒ `Acquiring{artifact}`; `Torn`/`NoFile` одно правило: до seal ⇒ Dealing с reconstruct, при/после seal ⇒ `SatOut`; finalize Err ⇒ `Acquiring{logs}`; resolver-exit = смерть движка ⇒ fatal (деградация «gossip-only» убирается сознательно); `Option`-швы → конфиг; один реестр метрик; `recover(E)` одной функцией (с веткой частичного успеха) | R-002, R-036, R-038, R-026, R-051, R-072, R-067, R-070, BB-4, BB-10 / R-071, E5-11, E5-12, E5-15, E5-17, E5-23, E5-24, E5-35, E5-36, E5-43 | 5.1 (5.2 желательно), стенд 3.3 (Ex-22 выполнен) | 3 нед [ГИПОТЕЗА] |
| [ ] 5.4 | Владение диском и жизненным циклом: beacon сам держит и подметает `dkg_epoch_*` — band-sweep ПО НОМЕРУ ЭПОХИ переносится как есть (`abort` + join перед удалением, пропуск живых, `epoch_manager.rs:300-366`); `epoch_manager.dkg_agreements`/`prune_agreements`/`agreement_intake` удаляются; SafetyHalt читается как ЗАЩЁЛКА при старте инстанса (`epoch_manager.rs:829-852`); один `clock` (`watch` от marshal Tip, начальное значение — стартовый Tip marshal-а), поллер `fin + K` и inlet tee как фидеры удаляются (tee избыточен по механизму: `cert_inlet.rs:921-931` → marshal `core/actor.rs:1453-1457` → `application.rs:1027-1034`); узлу — `Tasks{supervised, drain}` с явным супервизором, drain-писатели вне линии спавна движка (тест `node/dpos.rs:2648` остаётся); имена партиций — в модуль | E5-04, E5-05, E5-06, E5-40 / R-041 (частично) | 5.3; тест «догоняющий валидатор дилит на живом фронтире без tee» — требует `CertInlet` в стенде (PLAN 4.0(в)) | 1 нед [ГИПОТЕЗА] (было 3–5 дн; band-sweep, join-семантика и защёлка — не «перенос кода») |

### Э6. Executor и супервизия — открыт
| # | Работа | Закрывает / смягчает | Нужно до | Оценка |
|---|---|---|---|---|
| [ ] 6.1 | П-6 | R-006, R-015, R-031, R-043, R-074 / R-061 | Э3, 5.3 | 4–6 нед. (нижняя граница) |
| [ ] 6.2 | П-7 | R-032 / R-022, R-033, R-042, R-064, R-078 | 6.1 | 1–1,5 нед. |
| [ ] 6.3 | B-2 + CB-2 + CB-10: один `launch(role)`; объём зависит от Д-5 | R-042, половина R-049, ветка `Noop` R-063, ветка `Plane` R-007 | Д-5 | AUDIT 4–6 дней |

### Э7. Поштучные правки, переживающие перестройку — открыт
Независимы; делать по мере касания файла. Список — `REGISTER.md` (статус «по плану — Э7»): R-041, R-073 (партиции и `RotatedKey`), R-050, R-077 (маркер halt, gauges), R-046, R-047, R-048 (`epoch_transition`), R-035/R-045 (повтор спавна, elector), R-067, R-059, R-060, R-079, комментарии R-105, R-106, R-110, R-066, R-093, документ оператора R-058. Плюс из Э0: линт `promtool` (183 семьи без HELP, 40 `_total_total`).

### Э8. Гигиена без изменения поведения — открыт
B-10 (R-100, K-11 bypass), B-11 (R-030), CB-1, CB-5, CB-7, CB-8 (R-065), CB-9, BB-2, BB-4, BB-8, BB-10, BB-12 и NIT из `REGISTER.md` (статус «по плану — Э8»). Плюс: шесть чужих файлов с дрейфом `rustfmt`; `README.md` контракта и мелкие вестигии дерева A (W6: `write_ring_compact`, «unreachable, kept» перепроверки, мёртвый `visible_at`, `claimValidatorFeeAtEpoch` без близнеца).

## §3 Что закрывается структурно, что нет

Числа от 09-04 по INDEX (исторические, после Э0–Э2 не пересчитывались): 52 записи закрываются структурными работами Э1–Э6, 24 смягчаются с остатком, 40 — поштучно, 3 не трогать (R-005, R-055, R-057). Записи, закрываемые структурно, в очередь поштучных правок не ставить. Актуальная разбивка — столбец «Ход по плану» и статус в `REGISTER.md`. По VERIFY-REDESIGN (09-04): десятью П закрываются 30 записей, смягчаются 21; пятью свойствами диагнозов 1–5 — 20 и 14. Пометки «покрыто тестами» из исходников доверия не заслуживают, пока не сделано Э3 (`FakeDeriver` канонизирует при derive; режим `gate_landing_on_import` канонизирует при import — reth тоже не делает — и включён в 1 тесте из 118; тесты контракта шли на заглушках — часть закрыта 1.8).

## §4 Разногласия исходников и как они решены

- **R-003, тяжесть.** REGISTER BLOCKER, VERIFY-BLOCKERS SERIOUS. Нужны f+1 записей Active-реестра, туда пускает только governance — не один участник. Принято SERIOUS; на порядок не влияет (П-4).
- **R-005, тяжесть.** REGISTER MODERATE, VERIFY-BLOCKERS NIT. PoP проверяет контракт; остаток был K-1/K-23 — закрыты 1.10 и чтением верификатора. Принято MINOR.
- **R-010, последствие.** Не «лидер присваивает комиссии»: EL требует `beneficiary == PRECOMPILE_FEE_MANAGER` при импорте по devp2p (Ex-23 побочно) — последствие «неимпортируемый блок». Правка — удалить поле (Э0.3), сделано.
- **R-119, тяжесть.** DUPLICATES/REGISTER SERIOUS («дрейф между ветками существует»); источник — комментарий, признанный устаревшим (R-107). Принято MODERATE; 09-09 дрейф опровергнут слиянием.
- **R-028 и B-3.** Обработчик `slashEquivocation(uint64,uint32)` в контракте есть — B-3 отменён; остаток R-028 входит в 1.2.
- **REDESIGN «оставить» п.4** (приём при `NoKey`) снят VERIFY как стоящий на несуществующем механизме — вопрос в Д-3.
- **Порядок REDESIGN «П-4 первым»** — без деплоя срочности нет; остаётся зависимость П-1 → П-4.
- **REDESIGN «около 45 записей сводятся к пяти свойствам»** — VERIFY: 20 закрываются и 14 смягчаются; в §3 числа VERIFY.
- **Оценки.** REDESIGN 22–30 недель → VERIFY 23,4–31,1; «~4 тыс. строк» → 2 790 файлов целиком, ≈3 900 всего, ≈2 200 продакшна; «~4 тыс.» держится только со счётом тестов и при условии, что `keys.rs` сожмётся, как обещано.
- **Д-11 против 1.0.** Carry-over (принят 09-04, реализован Э0.2) отменён решением 1.0 (09-07) — `DECISIONS.md` Д-11.
- **Признак завершения Э1** («E1/E2/E5 живут») противоречил 1.0 — переписан 09-08 (D3).
- **Д-4** принято 09-04, поставлено под вопрос 09-07 — `DECISIONS.md` Д-4; не реализовано.
- Полный список противоречий корпуса с цитатами — `CONSOLIDATION-REPORT.md` §4.

## §5 Решения

Все решения и предложения — `DECISIONS.md` (Д-1..Д-13, П-1..П-10, таймлок, ставка, K-политики, слияние, push-выплата, стенд на пять узлов). Открытые на 09-09: Д-1, Д-2, Д-3, Д-5, Д-6, Д-7, Д-9, Д-13; Д-4 — под вопросом.

## §6 Дыры в материалах

Не задачи, а границы уверенности (09-04, вычеркнуто закрытое):
- Тестовые модули узла (~41 тыс. строк) и `tests/` не читались ни одним проходом; известный класс — тесты, закрепляющие ложную семантику reth (R-006).
- `bls` прошёл один проход; `encoding.rs` принят по conformance-тестам; blst-вызовы и `union_unique` commonware не читались. Две реализации BLS12-381 в цепочке (arkworks/blst) — согласие только на векторах (`history/BLS_VERIFY_PORT_SPEC.md` §8.1).
- BLS-порт, остатки спеки: кофакторная очистка проверена эмпирически, не по коду (§8.2); недостижимые объекты в git B не смотрели (§8.5); `registerValidator` не ограничивает длину `message`, на пути улик туда идёт `evidence.msg` (§8.6); согласие arkworks на rwasm против её нативной сборки не доказано («Прямые ответы») — `history/BLS_VERIFY_PORT_SPEC.md`.
- Слой хранения commonware (`Ordinal`, `Metadata`, архивы marshal) — по именам методов; `ENOSPC`/`EROFS` в `append_mode_0600` не прослежены.
- Marshal core при удержанных ack и `set_floor`; `UpstreamResolver` × handler при stale-ключе; чей сертификат подаётся в `elect(round, cert)` (`history/COVERAGE.md` §5 п.3, 7, 8).
- Семантика `TrackedPeers::secondary` — не читалась; от неё зависит объём П-5.
- Готовность `reth-e2e-test-utils` под rWasm-EVM — не проверялась; от неё зависит оценка Э3.1.
- ~~Исходник BLS-верификатора отсутствует~~ — найден: `~/Work/audit-482/sol/…` (09-04) и `solidity-contracts@f641789f` (09-08); порт сделан из исходника. K-23 опровергнут.
- Поведение токена BLEND при отказе (K-12) — гипотеза. ~~Семантика `fuel: None` и отката вложенного фрейма (T-5)~~ — изоляция доказана на rWasm (X3); `fuel: None` сайтов стало пять (K-34).
- Не поставлены: Ex-3, Ex-4, Ex-7, Ex-8, Ex-9, Ex-10, Ex-12, Ex-13, Ex-15, Ex-16, Ex-18, Ex-19, Ex-21, Ex-22, R-006 сценарий 1, R-020 в трёх точках, R-007 при > f, N-1, N-3 — очередь `EXPERIMENTS.md` §3.
- Ex-24: смещение `PK_E` в Joint-Feldman без commit-раунда — вопрос спецификации.
- UNDERSTANDING §11.4 (`initial_epoch` против tracked `e+1` на границе) и §11.21 (`ArtifactStore` без вытеснения) — ни один аудит не разбирал.
- ~~Ветки контракта в локальном checkout отсутствуют~~ — слиты `f16fdd90`, дрейфа не было.
- ~~Прокси `cert-mitm-proxy.py` не найден~~ — есть в `devnet/local-dpos-smoke/scripts/` (`history/E0-LOG.md` п.6).
- Ни одного многоузлового simplex+marshal теста в крейте; смоук не запускается `make`-целью целиком (`make smoke-all` есть, но 09-04 дал 12/3/6 — §1).
- Раскладка слотов контракта менялась трижды за Э1 (1.1, 1.7, 1.10) без единого ассерта на раскладку; «только свежий генезис» — условие, не выбор (W7).
- Глоб `bins/*` в `Cargo.toml` несёт ту же ловушку, что снятый `crates/dpos/*` (любой каталог без `Cargo.toml` под `bins/` ломает workspace).
- Правила упаковки `#[derive(Storage)]` и `FUEL_DENOM_RATE` — только по тестам (`history/CONTRACT-UNDERSTANDING.md`, «где понимание слабое»).
- ~~Работы 1.0/1.1 не проверялись на живом стенде~~ — код побывал на узле через case-growth; отказ на полу живьём 09-08 (F5); свойство «безключевой с большим стейком не занимает место» живьём не воспроизводилось (R1.7).

## §7 Что потеряло смысл после снятия требования совместимости

- REGISTER часть 2 группа 3 «один координированный несовместимый релиз» и семь пунктов «временно менее безопасна» — про выкладку на живую сеть. По существу осталось два замечания: R-029 (свой бан вводить с сухим режимом «только метрика»), R-025 (дольше хранимые журналы DKG расширяют окно R-052/R-055).
- AUDIT часть C (пять этапов rolling-release); AUDIT-CORE/BEACON «цена» в терминах «что ломается на живой сети».
- REDESIGN: строки «миграция» в каждом П; обоснование «П-4 первым» через дыру, открытую неделями; часть 4 «временно менее безопасные окна»; П-10 «дешёвый вариант» — сразу полный B-9 (Э2.4).
- AUDIT-CONTRACT: столбцы «Что ломает: ABI / хранилище» у КB-2..КB-6 — нулевая цена; K-14 — на свежем состоянии не про что.
- DUPLICATES столбец «вероятность сдвига» — при общем источнике класс исчезает.
- BB-5 (v1-ветви share-файла) и R-052 — бесплатно. R-019 «передеплой с другим cap» и К-5 — с общей константой невозможен.
- Checkpoint-тулинг (B-5) — из предусловия выкладки стал продуктовым решением Д-2.

## §8 Вне объёма

Из REDESIGN часть 3 с поправкой VERIFY (п.4 снят — Д-3):
1. `NoopBlocker` на consensus-каналах: commonware `block!` глобален по транспорту, честный пир с расхождением кодека потерял бы связность; защита — своя, по каналу (Э4.3).
2. Синхронность `SeedOracle` и `record_seed`: `verify_attestation` вызывается inline в батчере, `await` там — на пути голосования.
3. Проверка каждого partial'а отдельно, O(n) pairing: порог seed равен кворуму, один плохой partial ломает восстановление, нужна атрибуция; цена не измерена — оставить, пока n ≤ 51.
4. `park_halted` навсегда с удержанием всех `Exact`-ack: сброшенный ack = смерть marshal.
5. Один simplex-движок и одна партиция на эпоху: делает exit-at-transition безопасным; правильно — sweep старых партиций (R-041), не общая партиция.
6. Result-gate как poll 41×25 мс внутри `verify`: стоимость пренебрежима.
7. Вторая simplex-плоскость для agreement ключа с собственным namespace: сходимость на граничном блоке невозможна, общий namespace = самослэш.
8. `Randomness` как единственный шов ядро↔beacon: после П-2 сжимается с 15 до 9 методов.
9. Deferred execution с K = 3 и двухуровневой финальностью — продуктовое решение; R-006/R-015 — дефекты guard'а, не модели.
10. Детерминированный dealer RNG от подписи ключа (R-055): без него `resume` невозможен и R-036 стал бы нормой.
Также вне объёма плана: `fluent-stf-sp1::dpos_exec.rs` (четвёртая копия ABI, вне дерева — при слиянии импортировать `fluentbase-staking-abi`); пересборка `StakingPool`/`FluentGovernance` (`.sol` нет ни в одном дереве).

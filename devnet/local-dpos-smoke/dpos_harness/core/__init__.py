"""core — the leaf layer: process exec, RPC/metrics transport, read-only node helpers, the
devnet topology (service naming, port map, network, chain identity), PRNG, set algebra, chain
pacing, the event log, terminal IO and shared policy constants. Imports nothing from above it."""


[package]
name = "fluentbase-contracts-blake2f"
version = "0.1.0"
edition = "2021"

[dependencies]
fluentbase-sdk = { workspace = true }
precompile = { workspace = true }

[features]
default = ["std"]
std = ["fluentbase-sdk/std", "precompile/std"]

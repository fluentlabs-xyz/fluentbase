[package]
name = "fluentbase-example-nitro-verifier"
version = "0.1.0"
edition = "2021"

[dependencies]
fluentbase-sdk = { workspace = true }
hex-literal = { version = "0.4.1", default-features = false }
coset = { version = "0.3.8", default-features = false }
ciborium = { version = "^0.2.1", default-features = false }
x509-cert = { version = "0.2.5", default-features = false, features = ["pem"] }
der = { version = "0.7.9", features = ["alloc"] }
ecdsa = { version = "0.16.9", features = ["digest", "alloc", "der"], default-features = false }
p384 = { version = "0.13.0", features = ["ecdsa-core", "ecdsa"], default-features = false }
sha2 = { version = "0.10.8", default-features = false }
spki = "0.7.3"



[lib]
crate-type = ["cdylib", "staticlib"]
path = "lib.rs"

[features]
default = ["std"]
std = [
    "fluentbase-sdk/std"
]


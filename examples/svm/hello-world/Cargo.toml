[package]
name = "fluentbase-examples-svm-hello-world"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]
path = "src/solana_program.rs"

[dependencies]
solana-pubkey = { workspace = true, default-features = false }
solana-account-info = { workspace = true, default-features = false }
solana-program-entrypoint = { workspace = true, default-features = false }
solana-program-error = { workspace = true, default-features = false }
solana-msg = { workspace = true, default-features = false }

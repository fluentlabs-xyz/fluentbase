[package]
edition = "2021"
name = "fluentbase-example-fuel"
version = "0.1.0"

[dependencies]
fluentbase-sdk = { workspace = true }

[lib]
crate-type = ["cdylib", "staticlib"]
path = "lib.rs"

[features]
default = ["std"]
std = [
    "fluentbase-sdk/std",
]

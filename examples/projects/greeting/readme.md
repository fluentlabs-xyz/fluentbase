# Greeting Example

## Ho to run

### Build the project

```bash
make build
```

### Deploy contract

```bash
npm install
npx hardhat deploy --network [NETWORK]
npx hardhat run scripts/call_contract.ts --network [NETWORK]
```

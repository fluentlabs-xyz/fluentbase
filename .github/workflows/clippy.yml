name: Clippy

on:
  push:
    branches: [ main, devel ]
    paths-ignore: [ "**.md", "docs/**" ]
  pull_request:
    branches: [ main, devel ]
    paths-ignore: [ "**.md", "docs/**" ]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  clippy:
    name: Clippy (${{ matrix.name }})
    runs-on: self-hosted
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: root workspace
            command: cargo clippy --workspace --all-targets -- -D warnings
          - name: contracts workspace
            command: cargo clippy --manifest-path=./contracts/Cargo.toml --workspace --all-targets -- -D warnings
          - name: examples workspace
            command: cargo clippy --manifest-path=./examples/Cargo.toml --workspace --all-targets -- -D warnings

    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive
          fetch-depth: 1

      - name: Install system deps (clang/libclang)
        run: |
          if [ "$(id -u)" -eq 0 ]; then
            apt-get update
            apt-get install -y clang libclang-dev
          else
            sudo -n apt-get update
            sudo -n apt-get install -y clang libclang-dev
          fi

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable

      - name: Ensure clippy component is installed
        run: rustup component add clippy

      - name: Install wasm32 target
        run: rustup target list --installed | grep -q '^wasm32-unknown-unknown$' || rustup target add wasm32-unknown-unknown

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
          shared-key: clippy-${{ matrix.name }}

      - name: Prefetch deps
        run: cargo fetch --locked

      - name: Run clippy
        run: ${{ matrix.command }}

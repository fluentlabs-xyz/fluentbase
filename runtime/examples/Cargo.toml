[package]
name = "fluentbase-runtime-example"
version = "0.1.0"
edition = "2021"

[dependencies]
fluentbase-core = { path = "../../core", default-features = false }
fluentbase-rwasm = { path = "../../rwasm", default-features = false }

[lib]
crate-type = ["cdylib"]

[features]
greeting = []
panic = []
translator = []